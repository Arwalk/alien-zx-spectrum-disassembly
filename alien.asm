  ORG $4000

; Data block at DisplayFile
DisplayFile:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R1C8:
  DEFB $00,$38,$00,$00,$E0,$00,$00,$FE
  DEFB $00,$00,$7F,$00,$00,$44,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$AA,$A8,$4A,$10,$42,$90
  DEFB $45,$15,$12,$2A,$29,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$01,$FC
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$0E,$49,$00
  DEFB $01,$80,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$07,$12,$88,$00
  DEFB $00,$01,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$F3,$28,$80,$00
  DEFB $00,$00,$28,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$44,$00,$00,$40,$00,$00,$92
  DEFB $00,$00,$21,$00,$00,$64,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$C4,$68,$EA,$3A,$43,$10
  DEFB $35,$65,$0B,$93,$92,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$0F,$C3
  DEFB $80,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$1C,$A1,$00
  DEFB $00,$C0,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$0F,$35,$08,$00
  DEFB $00,$05,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$FE,$48,$00,$00
  DEFB $00,$00,$04,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$44,$00,$00,$40,$00,$00,$10
  DEFB $00,$00,$24,$00,$00,$54,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$1F,$80
  DEFB $40,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$3F,$0A,$00
  DEFB $00,$60,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$1F,$E1,$00,$00
  DEFB $00,$02,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$03,$60,$90,$00,$00
  DEFB $00,$00,$02,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$7C,$00,$00,$40,$00,$00,$10
  DEFB $00,$00,$3C,$00,$00,$54,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$3D,$00
  DEFB $30,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$77,$D2,$00
  DEFB $00,$30,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$1F,$C5,$04,$00
  DEFB $00,$01,$60,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$07,$F8,$20,$80,$00
  DEFB $00,$00,$03,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$44,$00,$00,$40,$00,$00,$10
  DEFB $00,$00,$24,$00,$00,$4C,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$C6,$00
  DEFB $10,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$EC,$D0,$00
  DEFB $00,$18,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$3F,$8E,$00,$00
  DEFB $00,$00,$20,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$07,$E1,$00,$00,$00
  DEFB $00,$00,$11,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$44,$00,$00,$42,$00,$00,$92
  DEFB $00,$00,$21,$00,$00,$44,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$CA,$6E,$EA,$10,$E3,$28
  DEFB $36,$35,$0B,$13,$12,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$01,$EE,$00
  DEFB $0C,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$01,$F6,$40,$00
  DEFB $00,$0C,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$7F,$10,$00,$00
  DEFB $00,$01,$70,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$E2,$48,$00,$00
  DEFB $00,$00,$00,$80,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$EE,$00,$00,$FE,$00,$00,$FE
  DEFB $00,$00,$7F,$00,$00,$EE,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$AA,$88,$4A,$30,$22,$A8
  DEFB $45,$45,$10,$A8,$A9,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$03,$F6,$00
  DEFB $06,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$BD,$40,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$7F,$52,$40,$00
  DEFB $00,$00,$90,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$E4,$70,$00,$00
  DEFB $00,$00,$04,$80,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R1C0L7:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$CA,$AC,$44,$10,$23,$10
  DEFB $E6,$27,$11,$29,$29,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$07,$ED,$00
  DEFB $03,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$FB,$40,$00
  DEFB $00,$07,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$FD,$84,$80,$00
  DEFB $00,$00,$08,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$68,$A0,$00,$00
  DEFB $00,$00,$01,$40,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$1F,$D8,$20,$00,$00
  DEFB $00,$00,$02,$C0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R9C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$FC,$10,$80,$00,$00
  DEFB $00,$00,$00,$2C,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$07,$BE,$44,$80,$00,$00
  DEFB $00,$00,$00,$05,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$A0,$00,$00,$00
  DEFB $00,$00,$00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R12C8:
  DEFB $00,$00,$1F,$FF,$A7,$CD,$70,$00
  DEFB $00,$00,$02,$7F,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FB,$4E,$FE,$95,$2E
  DEFB $41,$07,$FF,$FA,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$FF,$E1,$9C,$BF,$7F
  DEFB $FF,$00,$7D,$FC,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R15C8:
  DEFB $00,$00,$00,$1F,$FF,$FF,$7F,$7F
  DEFB $FF,$5E,$FF,$80,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$1D,$E1,$00,$00,$00
  DEFB $00,$00,$00,$A0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$F9,$25,$20,$00,$00
  DEFB $00,$00,$00,$0C,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$AE,$82,$40,$00,$00
  DEFB $00,$00,$00,$05,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$FE,$00,$00,$00
  DEFB $00,$00,$00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FB,$F5,$CC,$DC,$80
  DEFB $00,$00,$03,$BF,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FE,$46,$DD,$FF,$6F
  DEFB $E1,$1F,$7F,$FF,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$FF,$F6,$8B,$55,$BF
  DEFB $FF,$00,$FB,$F8,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$FF,$F7,$FF,$FF
  DEFB $FF,$7E,$FF,$80,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$3F,$E2,$80,$00,$00
  DEFB $00,$00,$00,$60,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$FC,$05,$00,$00,$00
  DEFB $00,$00,$00,$14,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FF,$06,$00,$00,$00
  DEFB $00,$00,$00,$0B,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$E7,$CC,$00,$00,$00
  DEFB $00,$00,$00,$0B,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FE,$DB,$A6,$7F,$00
  DEFB $00,$00,$1F,$7F,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FE,$32,$AE,$E4,$9B
  DEFB $50,$31,$BF,$EF,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$FF,$FF,$9F,$BB,$FF
  DEFB $FE,$00,$7F,$F8,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$FF,$F9,$7F,$7F
  DEFB $FF,$F7,$7F,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$3F,$04,$80,$00,$00
  DEFB $00,$00,$06,$70,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$F8,$48,$00,$00,$00
  DEFB $00,$00,$00,$0A,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FD,$DB,$00,$00,$00
  DEFB $00,$00,$00,$07,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$FA,$80,$00,$00
  DEFB $00,$00,$00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$E9,$6F,$59,$67,$80
  DEFB $00,$02,$3E,$FF,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$07,$FF,$11,$7F,$E5,$FE
  DEFB $CF,$E7,$FF,$D3,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$7F,$FF,$AE,$DF,$FF
  DEFB $FF,$00,$3F,$F0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$07,$FF,$F5,$BF,$BF
  DEFB $FE,$FB,$7F,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$7E,$11,$40,$00,$00
  DEFB $00,$00,$00,$30,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R9C0L4:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$FF,$11,$00,$00,$00
  DEFB $00,$00,$00,$12,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$A7,$8C,$40,$00,$00
  DEFB $00,$00,$00,$07,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$F5,$F4,$10,$00,$00
  DEFB $00,$00,$00,$83,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$F2,$BD,$FC,$D0,$61
  DEFB $00,$04,$FF,$FF,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$07,$FF,$89,$EF,$B4,$79
  DEFB $EF,$E7,$FF,$B7,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$7F,$FE,$E9,$6B,$FF
  DEFB $FC,$03,$7F,$F0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$FF,$FA,$FF,$FF
  DEFB $FF,$7F,$FE,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$7C,$84,$40,$00,$00
  DEFB $00,$00,$01,$78,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$AE,$01,$08,$00,$00
  DEFB $00,$00,$00,$12,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FD,$2C,$00,$00,$00
  DEFB $00,$00,$00,$0F,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$ED,$6A,$08,$10,$00
  DEFB $00,$00,$00,$45,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$F4,$36,$CA,$61,$7B
  DEFB $00,$01,$BD,$7D,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$FF,$5D,$D7,$5F,$FD
  DEFB $F7,$6B,$FF,$DE,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$3F,$FE,$FF,$61,$7F
  DEFB $FE,$1C,$FF,$E0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$FF,$FD,$FF,$FF
  DEFB $FF,$BF,$FC,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$F8,$29,$04,$00,$00
  DEFB $00,$00,$01,$28,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$EC,$10,$00,$00,$00
  DEFB $00,$00,$00,$09,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$EF,$0A,$40,$00,$00
  DEFB $00,$00,$00,$01,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$6A,$21,$68,$00
  DEFB $00,$00,$08,$8B,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FF,$FB,$D0,$67,$B1
  DEFB $40,$0A,$FF,$BB,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$FF,$A9,$FA,$FD,$1F
  DEFB $BF,$7F,$FF,$EE,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$3F,$FE,$AA,$4E,$BF
  DEFB $FF,$BD,$FF,$E0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$FF,$FF,$FF,$FF
  DEFB $FF,$FF,$7C,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$F8,$02,$00,$00,$00
  DEFB $00,$00,$00,$18,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$07,$FF,$A8,$00,$00,$00
  DEFB $00,$00,$00,$05,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$C4,$40,$00,$00
  DEFB $00,$00,$00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$E5,$D9,$40,$04,$00
  DEFB $00,$00,$01,$3F,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$F2,$AC,$F4,$ED,$1B
  DEFB $C1,$07,$FF,$FF,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$FF,$D7,$9D,$76,$FF
  DEFB $DD,$BE,$FE,$DC,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$1F,$FF,$B5,$77,$7F
  DEFB $FF,$FD,$FF,$C0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$3F,$FF,$FF,$FF
  DEFB $FB,$96,$F8,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$1F,$FE,$37,$FF
  DEFB $B7,$BE,$F0,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R17C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$04,$B1,$26,$FF,$7F
  DEFB $FF,$D0,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$01,$22,$42
  DEFB $44,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R19C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R20C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R20C16:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R21C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R22C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R23C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R23C8:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$0F,$FF,$5F,$FF
  DEFB $3F,$B9,$E0,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0A,$E6,$81,$FF,$FF
  DEFB $7A,$80,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$E2,$00
  DEFB $20,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$3C,$42,$82,$00
  DEFB $44,$7E,$82,$00,$FE,$3C,$00,$3C
  DEFB $3C,$42,$7E,$00,$3C,$40,$3E,$7E
  DEFB $42,$24,$18,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$07,$FE,$EF,$7F
  DEFB $FF,$7F,$40,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R17C0L2:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$03,$7C,$CB,$FD,$CE
  DEFB $A5,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$02,$58
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$42,$62,$44,$00
  DEFB $48,$40,$44,$00,$10,$42,$00,$40
  DEFB $42,$42,$40,$00,$42,$40,$08,$40
  DEFB $62,$7E,$28,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$FF,$5F,$7F
  DEFB $FF,$F4,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R17C0L3:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$05,$F0,$69,$FF,$59
  DEFB $AA,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$01,$81
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$42,$52,$28,$00
  DEFB $70,$7C,$28,$00,$10,$42,$00,$3C
  DEFB $42,$42,$7C,$00,$42,$40,$08,$7C
  DEFB $52,$24,$08,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$01,$FE,$BF,$FF
  DEFB $FE,$6D,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$32,$D4,$7F,$E0
  DEFB $70,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$7E,$4A,$10,$00
  DEFB $48,$40,$10,$00,$10,$42,$00,$02
  DEFB $7E,$42,$40,$00,$7E,$40,$08,$40
  DEFB $4A,$24,$08,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$20,$00,$FF,$FF,$FA
  DEFB $E8,$F6,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R17C0L5:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$3B,$6A,$7F,$CA
  DEFB $20,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$42,$46,$10,$00
  DEFB $44,$40,$10,$00,$10,$42,$00,$42
  DEFB $42,$24,$40,$00,$42,$40,$08,$40
  DEFB $46,$7E,$08,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$31,$80,$7F,$BF,$EF
  DEFB $E4,$22,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$B6,$40,$14
  DEFB $00,$C0,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$42,$42,$10,$00
  DEFB $42,$7E,$10,$00,$10,$3C,$00,$3C
  DEFB $42,$18,$7E,$00,$42,$7E,$3E,$7E
  DEFB $42,$24,$3E,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$A9,$09,$3F,$BF,$E7
  DEFB $FF,$CC,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$9A,$A7,$02
  DEFB $04,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
AttrFileOrigin:
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$04,$04,$04,$04,$04,$04
  DEFB $04,$04,$04,$04,$04,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$04,$04,$04,$04,$04,$04
  DEFB $04,$04,$04,$04,$04,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
AttrRow10:
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $07,$07,$04,$04,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$38,$04,$07,$04
  DEFB $07,$04,$04,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$38,$04,$04,$07
  DEFB $04,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $07,$3C,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $07,$04,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $04,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$04,$07,$04,$04
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
MsgLineAttrs:
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07

; Data block at SystemArea
SystemArea:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $FF,$00,$00,$00,$FF,$00,$23,$0D
SysLastKey:
  DEFB $0D,$23,$05,$00,$00,$00,$11,$00
  DEFB $01,$00,$06,$00,$0B,$00,$01,$00
  DEFB $01,$00,$06,$00,$10,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$3C
  DEFB $40,$00,$FF,$CD,$01,$FC,$5F,$00
  DEFB $00,$00,$00,$00,$FF,$0A,$00,$07
  DEFB $07,$00,$00,$D8,$5E,$7F,$5D,$B6
  DEFB $5C,$B6,$5C,$CB,$5C,$BF,$5D,$CA
  DEFB $5C,$D9,$5E,$DC,$5E,$AF,$5D,$EF
  DEFB $5E,$DE,$5E,$DE,$5E,$DE,$5E,$2D
  DEFB $92,$5C,$10,$00,$00,$00,$00,$00
  DEFB $00,$02,$6C,$D8,$7D,$1A,$00,$00
SysFrames:
  DEFB $07,$00,$00,$58,$FF,$00,$00,$21
  DEFB $00,$5B,$21,$17,$00,$40,$E0,$50
  DEFB $21,$18,$21,$17,$01,$00,$00,$07
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$FF,$5F,$FF,$FF,$F4,$09
  DEFB $A8,$10,$4B,$F4,$09,$C4,$15,$53
  DEFB $81,$0F,$C4,$15,$52,$F4,$09,$C4
  DEFB $15,$50,$80,$00

; Data block at LoadedCode
LoadedCode:
  DEFB $00,$6C,$00,$EA,$DD,$21,$00,$40
  DEFB $11,$80,$1A,$37,$3E,$FF,$CD,$56
  DEFB $05,$DD,$21,$00,$60,$11,$FC,$53
  DEFB $37,$3E,$FF,$CD,$56,$05,$DD,$21
  DEFB $60,$EA,$11,$B8,$0B,$37,$3E,$FF
  DEFB $CD,$56,$05,$01,$69,$8E,$C9,$3E
  DEFB $FF,$DD,$21,$00,$40,$11,$80,$1A
  DEFB $CD,$C2,$04,$C9,$3E,$FF,$DD,$21
  DEFB $00,$60,$11,$FC,$53,$CD,$C2,$04
  DEFB $C9,$3E,$FF,$DD,$21,$60,$EA,$11
  DEFB $B8,$0B,$CD,$C2,$04,$C9,$30,$10
  DEFB $89,$11,$68,$30,$30,$30,$30,$30
  DEFB $30,$30,$30,$30,$30,$30,$30,$30
  DEFB $30,$30,$30,$30,$30,$30,$0D,$00
  DEFB $0A,$80,$00,$FD,$32,$34,$35,$37
  DEFB $35,$0E,$00,$00,$FF,$5F,$00,$3A
  DEFB $D9,$30,$0E,$00,$00,$00,$00,$00
  DEFB $3A,$DA,$30,$0E,$00,$00,$00,$00
  DEFB $00,$3A,$E7,$30,$0E,$00,$00,$00
  DEFB $00,$00,$3A,$FB,$3A,$F4,$32,$33
  DEFB $36,$35,$39,$0E,$00,$00,$6B,$5C
  DEFB $00,$2C,$30,$0E,$00,$00,$00,$00
  DEFB $00,$3A,$F1,$6C,$3D,$C0,$28,$32
  DEFB $35,$36,$0E,$00,$00,$00,$01,$00
  DEFB $2A,$BE,$32,$33,$36,$33,$36,$0E
  DEFB $00,$00,$54,$5C,$00,$2B,$BE,$32
  DEFB $33,$36,$33,$35,$0E,$00,$00,$53
  DEFB $5C,$00,$2B,$35,$0E,$00,$00,$05
  DEFB $00,$00,$29,$3A,$F2,$30,$0E,$00
  DEFB $00,$00,$00,$00,$3A,$F1,$4C,$3D
  DEFB $C0,$4C,$0D,$00,$0F,$61,$00,$FD
  DEFB $32,$34,$35,$37,$35,$0E,$00,$00
  DEFB $FF,$5F,$00,$3A,$E7,$30,$0E,$00
  DEFB $00,$00,$00,$00,$3A,$DA,$30,$0E
  DEFB $00,$00,$00,$00,$00,$3A,$D9,$37
  DEFB $0E,$00,$00,$07,$00,$00,$3A,$FB
  DEFB $3A,$F1,$50,$3D,$32,$35,$36,$0E
  DEFB $00,$00,$00,$01,$00,$2A,$BE,$32
  DEFB $33,$36,$33,$36,$0E,$00,$00,$54
  DEFB $5C,$00,$2B,$BE,$32,$33,$36,$33
  DEFB $35,$0E,$00,$00,$53,$5C,$00,$3A
  DEFB $F1,$4C,$3D,$C0,$28,$50,$2B,$35
  DEFB $0E,$00,$00,$05,$00,$00,$29,$0D
  DEFB $00,$14,$6B,$00,$F8,$22,$41,$4C
  DEFB $49,$45,$4E,$30,$22,$CA,$31,$30
  DEFB $0E,$00,$00,$0A,$00,$00,$3A,$F5
  DEFB $AC,$32,$31,$0E,$00,$00,$15,$00
  DEFB $00,$2C,$34,$0E,$00,$00,$04,$00
  DEFB $00,$3B,$22,$41,$4E,$59,$20,$4B
  DEFB $45,$59,$20,$54,$4F,$20,$53,$41
  DEFB $56,$45,$20,$22,$3A,$EB,$4E,$3D
  DEFB $31,$0E,$00,$00,$01,$00,$00,$CC
  DEFB $33,$0E,$00,$00,$03,$00,$00,$3A
  DEFB $F5,$AC,$32,$31,$0E,$00,$00,$15
  DEFB $00,$00,$2C,$32,$30,$0E,$00,$00
  DEFB $14,$00,$00,$3B,$22,$41,$4C,$49
  DEFB $45,$4E,$23,$22,$3B,$4E,$0D,$00
  DEFB $1E,$33,$00,$FA,$A6,$C9,$22,$22
  DEFB $CB,$F1,$4C,$3D,$C0,$28,$28,$50
  DEFB $2B,$33,$35,$0E,$00,$00,$23,$00
  DEFB $00,$29,$2B,$31,$33,$0E,$00,$00
  DEFB $0D,$00,$00,$2A,$4E,$29,$3A,$F3
  DEFB $4E,$3A,$E2,$3A,$EC,$32,$30,$0E
  DEFB $00,$00,$14,$00,$00,$0D,$00,$28
  DEFB $0A,$00,$EC,$33,$30,$0E,$00,$00
  DEFB $1E,$00,$00,$0D,$80,$EF,$22,$22
  DEFB $0D,$80,$00,$00,$D0,$5C,$00,$00
  DEFB $00,$05,$00,$00,$20,$00,$00,$00
  DEFB $FF,$5F,$00,$20,$20,$20,$20,$20
  DEFB $20,$00,$00,$00,$80,$00,$00,$00
  DEFB $41,$4C,$49,$45,$4E,$30,$20,$20
  DEFB $20,$20,$19,$02,$00,$00,$0D,$02
  DEFB $0D,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $DB,$02,$B1,$33,$DE,$5E,$00,$CB
  DEFB $9B,$36,$B1,$33,$B7,$2D,$DE,$5E
  DEFB $01,$00,$F7,$5C,$2B,$2D,$65,$33
  DEFB $00,$00,$ED,$10,$3A,$00,$52,$00
  DEFB $5D,$1C,$BA,$CD,$52,$1C,$76,$1B
  DEFB $03,$13,$00,$3E

; Ship-map screen data — three deck maps
;
; The map screen shown by DrawShipMap (DrawShipMap) as three stacked deck
; views: 3 decks x 19 rows x 20 columns of tile codes (the same 20-wide grid
; format the room views use). Row texts like "UPPER DECK" are plain ASCII tile
; codes; other values are map glyphs (room outlines, corridors, and the legend
; symbols: 123 '{' ladder up, 124 '|' hatchway down, 125 '}' duct grille).
ShipMapData:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 0
  DEFB $00,$55,$50,$50,$45,$52,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 1
  DEFB $00,$44,$45,$43,$4B,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 2
  DEFB $00,$00,$00,$00,$00,$00,$05,$0B,$0C,$01,$01,$0F,$01,$0C,$01,$01,$01,$24,$00,$00 ; UPPER deck
                                                                                       ; row 3
  DEFB $00,$00,$00,$00,$00,$05,$03,$04,$02,$60,$00,$00,$00,$02,$60,$00,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 4
  DEFB $00,$00,$00,$00,$05,$03,$04,$60,$02,$00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 5
  DEFB $00,$00,$00,$00,$1D,$04,$00,$00,$13,$01,$01,$01,$22,$86,$01,$0C,$0F,$15,$00,$00 ; UPPER deck
                                                                                       ; row 6
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00,$00,$02,$60,$00,$02,$60,$02,$00,$00 ; UPPER deck
                                                                                       ; row 7
  DEFB $00,$00,$00,$00,$02,$00,$7C,$00,$10,$00,$00,$00,$02,$00,$00,$02,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 8
  DEFB $00,$00,$00,$00,$13,$0F,$01,$01,$15,$00,$00,$00,$10,$00,$00,$10,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 9
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$10,$00,$00,$00,$02,$00,$00,$02,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 10
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00,$60,$02,$00,$00,$02,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 11
  DEFB $00,$00,$00,$00,$1E,$09,$00,$00,$13,$01,$01,$01,$1A,$1B,$0F,$14,$01,$15,$00,$00 ; UPPER deck
                                                                                       ; row 12
  DEFB $00,$00,$00,$00,$07,$08,$09,$60,$02,$00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 13
  DEFB $00,$00,$00,$00,$00,$07,$08,$09,$02,$60,$00,$00,$00,$02,$60,$00,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 14
  DEFB $00,$00,$00,$00,$00,$00,$07,$1F,$14,$01,$01,$0F,$01,$14,$01,$01,$01,$25,$00,$00 ; UPPER deck
                                                                                       ; row 15
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 16
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 17
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 18
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$05,$0B,$0C,$01,$0C,$01,$01,$0D,$09 ; MIDDLE
                                                                                       ; deck row 0
  DEFB $00,$4D,$49,$44,$44,$4C,$45,$00,$00,$00,$05,$03,$04,$02,$60,$02,$60,$00,$07,$0E ; MIDDLE
                                                                                       ; deck row 1
  DEFB $00,$44,$45,$43,$4B,$00,$00,$00,$00,$05,$03,$04,$00,$02,$00,$02,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 2
  DEFB $00,$00,$00,$00,$00,$00,$05,$0B,$01,$12,$04,$00,$60,$02,$00,$02,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 3
  DEFB $00,$00,$00,$00,$00,$05,$03,$04,$00,$13,$01,$0F,$01,$14,$0F,$15,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 4
  DEFB $00,$00,$00,$00,$05,$03,$04,$00,$00,$02,$00,$00,$60,$00,$00,$10,$00,$00,$05,$11 ; MIDDLE
                                                                                       ; deck row 5
  DEFB $05,$0B,$01,$01,$12,$04,$00,$00,$18,$1A,$1B,$01,$01,$0C,$0F,$1C,$01,$01,$16,$17 ; MIDDLE
                                                                                       ; deck row 6
  DEFB $1D,$04,$00,$00,$02,$60,$00,$00,$02,$7C,$02,$00,$00,$02,$00,$02,$00,$00,$07,$0E ; MIDDLE
                                                                                       ; deck row 7
  DEFB $02,$00,$00,$00,$13,$0F,$01,$01,$15,$00,$02,$00,$00,$02,$60,$02,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 8
  DEFB $02,$60,$00,$00,$10,$00,$60,$7B,$10,$00,$10,$00,$60,$02,$00,$10,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 9
  DEFB $02,$00,$00,$00,$13,$01,$01,$0F,$15,$60,$02,$00,$00,$02,$00,$02,$60,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 10
  DEFB $1E,$09,$00,$00,$02,$60,$00,$00,$02,$00,$02,$00,$00,$02,$00,$02,$00,$00,$05,$11 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 11
  DEFB $07,$1F,$01,$01,$21,$09,$00,$00,$19,$22,$86,$01,$01,$14,$0F,$1C,$01,$01,$16,$17 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 12
  DEFB $00,$00,$00,$00,$07,$08,$09,$00,$00,$02,$00,$00,$60,$00,$00,$10,$00,$00,$07,$0E ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 13
  DEFB $00,$00,$00,$00,$00,$07,$08,$09,$00,$13,$01,$0F,$01,$0C,$0F,$15,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 14
  DEFB $00,$00,$00,$00,$00,$00,$07,$1F,$01,$21,$09,$00,$60,$02,$00,$02,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 15
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$07,$08,$09,$00,$02,$00,$02,$60,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 16
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$07,$08,$09,$02,$60,$02,$00,$00,$05,$11 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 17
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$07,$1F,$14,$01,$14,$01,$01,$0A,$04 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 18
  DEFB $00,$00,$00,$00,$00,$00,$18,$01,$0C,$01,$01,$01,$26,$27,$27,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 0
  DEFB $00,$4C,$4F,$57,$45,$52,$02,$60,$02,$00,$00,$00,$2D,$2C,$2C,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 1
  DEFB $00,$44,$45,$43,$4B,$00,$02,$00,$02,$00,$00,$00,$2D,$2C,$2C,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 2
  DEFB $00,$00,$00,$00,$00,$00,$02,$00,$02,$00,$00,$00,$2D,$2C,$2C,$2B,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 3
  DEFB $00,$00,$00,$00,$00,$05,$11,$00,$02,$00,$00,$00,$2D,$2C,$2C,$2C,$2B,$00,$00,$00 ; LOWER deck
                                                                                       ; row 4
  DEFB $00,$00,$00,$00,$05,$03,$04,$00,$02,$60,$00,$00,$2D,$2C,$2C,$2C,$2C,$2B,$00,$00 ; LOWER deck
                                                                                       ; row 5
  DEFB $00,$00,$00,$00,$40,$5B,$0F,$01,$1C,$01,$0F,$01,$2E,$2C,$2C,$2C,$5F,$3B,$01,$24 ; LOWER deck
                                                                                       ; row 6
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$02,$7B,$00,$00,$2D,$2C,$2C,$2C,$3C,$60,$00,$02 ; LOWER deck
                                                                                       ; row 7
  DEFB $00,$00,$00,$00,$10,$00,$00,$00,$02,$00,$00,$00,$2F,$3B,$3B,$3B,$3F,$00,$00,$02 ; LOWER deck
                                                                                       ; row 8
  DEFB $00,$00,$00,$00,$10,$60,$00,$00,$10,$00,$00,$00,$10,$00,$00,$60,$10,$00,$00,$02 ; LOWER deck
                                                                                       ; row 9
  DEFB $00,$00,$00,$00,$10,$00,$00,$00,$02,$60,$00,$00,$89,$3D,$3D,$3D,$3E,$00,$00,$02 ; LOWER deck
                                                                                       ; row 10
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00,$00,$2D,$2C,$2C,$2C,$3C,$00,$00,$02 ; LOWER deck
                                                                                       ; row 11
  DEFB $00,$00,$00,$00,$5C,$5D,$0F,$01,$1C,$01,$0F,$01,$2E,$2C,$2C,$2C,$5E,$3D,$01,$25 ; LOWER deck
                                                                                       ; row 12
  DEFB $00,$00,$00,$00,$07,$08,$09,$00,$02,$60,$00,$00,$2D,$2C,$2C,$2C,$2C,$2A,$00,$00 ; LOWER deck
                                                                                       ; row 13
  DEFB $00,$00,$00,$00,$00,$07,$0E,$60,$02,$00,$00,$00,$2D,$2C,$2C,$2C,$2A,$00,$00,$00 ; LOWER deck
                                                                                       ; row 14
  DEFB $00,$00,$00,$00,$00,$00,$13,$0F,$15,$00,$00,$00,$2D,$2C,$2C,$2A,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 15
  DEFB $00,$00,$00,$00,$00,$00,$02,$00,$02,$00,$00,$00,$2D,$2C,$2C,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 16
  DEFB $00,$00,$00,$00,$00,$00,$02,$60,$02,$00,$00,$00,$2D,$2C,$2C,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 17
  DEFB $00,$00,$00,$00,$00,$00,$19,$01,$14,$01,$01,$01,$29,$28,$28,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 18

; Room-interior layout template 0 (RLE-compressed)
;
; Selected by room type (RoomTypeByte) at PickRoomLayout (PickRoomLayout) and
; decompressed into the grid buffer at RoomGridBuffer by RoomLayoutExpand
; (RoomLayoutExpand): byte < 30 = that many zero cells, 30-254 = literal tile
; code, 255 = end.
RoomLayout0:
  DEFB $14,$14,$14,$06,$7D,$7E,$7E,$7D,$7E,$7E,$7E,$7E,$7D,$05,$06,$7F
  DEFB $02,$7F,$04,$85,$05,$06,$7F,$02,$7F,$04,$85,$05,$06,$7F,$02,$7F
  DEFB $03,$80,$82,$05,$06,$7F,$02,$7F,$03,$7F,$06,$06,$7F,$02,$7F,$03
  DEFB $7F,$06,$06,$7F,$02,$7D,$7E,$7E,$7E,$7D,$7E,$7E,$7E,$7D,$83,$01
  DEFB $06,$7F,$02,$7F,$03,$7F,$04,$85,$01,$06,$7F,$02,$7F,$03,$7F,$04
  DEFB $8F,$01,$06,$7F,$02,$7F,$03,$81,$83,$05,$06,$7F,$02,$7F,$04,$85
  DEFB $05,$06,$7F,$02,$7F,$04,$85,$05,$06,$7D,$7E,$7E,$7D,$7E,$7E,$7E
  DEFB $7E,$7D,$05,$14,$14,$14,$FF

; Room-interior layout template 1 (RLE-compressed)
;
; Selected by room type (RoomTypeByte) at PickRoomLayout (PickRoomLayout) and
; decompressed into the grid buffer at RoomGridBuffer by RoomLayoutExpand
; (RoomLayoutExpand): byte < 30 = that many zero cells, 30-254 = literal tile
; code, 255 = end.
RoomLayout1:
  DEFB $14,$14,$09,$80,$7E,$7E,$7D,$7E,$7E,$83,$7D,$7E,$83,$01,$09,$7D
  DEFB $7E,$83,$03,$85,$02,$85,$01,$0B,$85,$03,$85,$02,$85,$01,$0B,$85
  DEFB $03,$85,$02,$85,$01,$05,$7D,$7E,$7E,$7E,$7E,$7E,$7D,$03,$85,$02
  DEFB $85,$01,$05,$7F,$02,$80,$7E,$7E,$82,$03,$85,$02,$85,$8E,$05,$7F
  DEFB $02,$7F,$06,$85,$02,$85,$85,$7D,$7E,$7E,$7E,$7E,$7D,$02,$7D,$02
  DEFB $7D,$7E,$7E,$7E,$7D,$7E,$7E,$7D,$82,$05,$7F,$02,$7F,$06,$85,$02
  DEFB $85,$01,$05,$7F,$02,$81,$7E,$7E,$83,$03,$85,$02,$85,$01,$05,$7D
  DEFB $7E,$7E,$7E,$7E,$7E,$7D,$03,$85,$02,$85,$01,$0B,$85,$03,$85,$02
  DEFB $85,$01,$0B,$85,$03,$85,$02,$85,$01,$09,$7D,$84,$82,$03,$85,$02
  DEFB $85,$01,$09,$81,$84,$84,$7D,$84,$84,$82,$7D,$84,$82,$01,$14,$14
  DEFB $FF

; Room-interior layout template 2 (RLE-compressed)
;
; Selected by room type (RoomTypeByte) at PickRoomLayout (PickRoomLayout) and
; decompressed into the grid buffer at RoomGridBuffer by RoomLayoutExpand
; (RoomLayoutExpand): byte < 30 = that many zero cells, 30-254 = literal tile
; code, 255 = end.
RoomLayout2:
  DEFB $14,$14,$01,$80,$7E,$7E,$7E,$7D,$7E,$7E,$7E,$83,$0A,$01,$7F,$07
  DEFB $85,$0A,$01,$7F,$07,$7D,$7E,$7E,$7E,$7E,$83,$05,$01,$7F,$0C,$85
  DEFB $01,$8E,$03,$01,$7F,$0C,$85,$01,$7F,$03,$01,$7F,$0C,$85,$01,$81
  DEFB $84,$7D,$01,$01,$7F,$0C,$85,$03,$85,$01,$01,$7D,$07,$7D,$84,$84
  DEFB $84,$84,$7D,$84,$84,$84,$82,$01,$01,$7F,$0C,$85,$05,$01,$7F,$0C
  DEFB $85,$05,$01,$7F,$0C,$85,$05,$01,$7F,$0C,$85,$05,$01,$81,$84,$84
  DEFB $84,$7D,$03,$7D,$84,$84,$84,$84,$82,$05,$05,$7F,$03,$85,$0A,$05
  DEFB $7F,$03,$85,$0A,$05,$7D,$84,$84,$84,$82,$0A,$14,$FF

; Room grid buffer — 19 rows x 20 columns
;
; Working buffer holding the decompressed cell grid of the current room view
; (or corridor view, decompressed from CorridorLayout). Blitted to the screen
; by DrawRoomBackground (DrawRoomBackground); the door drawer walks it via
; RoomCellCursor (RoomCellCursor). The bytes below are load-image leftovers.
RoomGridBuffer:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00

; Per-room view origin table — 34 rooms x (row, column)
;
; Read by SetRoomOrigin (SetRoomOrigin): for the current room, byte 0 = grid
; row and byte 1 = column where exit/door drawing starts; converted to
; RoomCellCursor (RoomGridBuffer + 20*row + col) and RoomAttrCursor (attr file
; + 32*row + col). Narcissus has no entry.
RoomOriginTable:
  DEFB $03,$09            ; 0 Airlock #1
  DEFB $0F,$09            ; 1 Airlock #2
  DEFB $02,$0C            ; 2 Armoury
  DEFB $04,$09            ; 3 Cargopod#1
  DEFB $09,$09            ; 4 Cargopod#2
  DEFB $0E,$09            ; 5 Cargopod#3
  DEFB $09,$00            ; 6 CommdCentr
  DEFB $09,$11            ; 7 Computer
  DEFB $09,$05            ; 8 Corridor#1
  DEFB $09,$08            ; 9 Corridor#2
  DEFB $06,$0B            ; 10 Corridor#3
  DEFB $09,$0F            ; 11 Corridor#4
  DEFB $0C,$0B            ; 12 Corridor#5
  DEFB $09,$0D            ; 13 Corridor#6
  DEFB $09,$0E            ; 14 Corridor#7
  DEFB $09,$0B            ; 15 Cryo Vault
  DEFB $0E,$05            ; 16 Engineerng
  DEFB $02,$10            ; 17 Engine #1
  DEFB $09,$12            ; 18 Engine #2
  DEFB $10,$10            ; 19 Engine #3
  DEFB $11,$05            ; 20 Eng Stores
  DEFB $06,$05            ; 21 Infirmary
  DEFB $03,$09            ; 22 Inf Stores
  DEFB $0C,$05            ; 23 Laboratory
  DEFB $0F,$09            ; 24 Lab Stores
  DEFB $07,$12            ; 25 Life Suppt
  DEFB $03,$06            ; 26 Livng Qtrs
  DEFB $09,$09            ; 27 Mess
  DEFB $0F,$06            ; 28 RecrtnArea
  DEFB $10,$0C            ; 29 Stores #1
  DEFB $03,$0E            ; 30 Stores #2
  DEFB $0F,$0E            ; 31 Stores #3
  DEFB $09,$01            ; 32 ShuttleBay
  DEFB $02,$05            ; 33 Shttlstore

; Room marker screen-address table
;
; One word per room (0-33): the display-file address where the room's animated
; marker (24x12 pixels) is XOR-blitted on that room's deck map. Read by
; GetRoomMarkerAddr (GetRoomMarkerAddr), which feeds both marker channels: A —
; the walking crew figure (CrewWalkA1/A2) on the room being viewed, placed by
; PlaceRoomMarkerA (PlaceRoomMarkerA) — and B — the blinking box cursor
; (RoomCursor0-3) on a candidate room: an exit row under the cursor in the room
; view, or a room picked from the Indicate Location list, placed by
; PlaceRoomMarkerB (PlaceRoomMarkerB). Rooms on different decks may share an
; address (Cargopod#2/Mess); the Narcissus (34) has no entry.
RoomMarkerAddrTable:
  DEFW $408A              ; 0 Airlock #1
  DEFW $48AA              ; 1 Airlock #2
  DEFW $404D              ; 2 Armoury
  DEFW $4049              ; 3 Cargopod#1
  DEFW $4809              ; 4 Cargopod#2
  DEFW $48C9              ; 5 Cargopod#3
  DEFW $4801              ; 6 CommdCentr
  DEFW $482F              ; 7 Computer
  DEFW $4E04              ; 8 Corridor#1
  DEFW $4908              ; 9 Corridor#2
  DEFW $468D              ; 10 Corridor#3
  DEFW $484D              ; 11 Corridor#4
  DEFW $4E8D              ; 12 Corridor#5
  DEFW $482D              ; 13 Corridor#6
  DEFW $4E0D              ; 14 Corridor#7
  DEFW $482A              ; 15 Cryo Vault
  DEFW $4E86              ; 16 Engineerng
  DEFW $4070              ; 17 Engine #1
  DEFW $4810              ; 18 Engine #2
  DEFW $48D1              ; 19 Engine #3
  DEFW $4EE6              ; 20 Eng Stores
  DEFW $40A6              ; 21 Infirmary
  DEFW $402B              ; 22 Inf Stores
  DEFW $4885              ; 23 Laboratory
  DEFW $500B              ; 24 Lab Stores
  DEFW $4831              ; 25 Life Suppt
  DEFW $40C5              ; 26 Livng Qtrs
  DEFW $4809              ; 27 Mess
  DEFW $4845              ; 28 RecrtnArea
  DEFW $48ED              ; 29 Stores #1
  DEFW $408F              ; 30 Stores #2
  DEFW $48AF              ; 31 Stores #3
  DEFW $4826              ; 32 ShuttleBay
  DEFW $4086              ; 33 Shttlstore

; RoomCursor0
;
; Room indicator cursor, phase 0 (used by AnimateCrewB AnimateCrewB, phase 0).
; XOR-blit sprite: 24px wide x 12px tall (3 bytes per row). Largest box — wide
; outer border with a small inner rectangle. This cursor blinks over the
; current room on the corridor map.
RoomCursor0:
  DEFB $03,$FF,$C0        ; row  0: top border
  DEFB $02,$00,$40        ; row  1
  DEFB $02,$00,$40        ; row  2
  DEFB $02,$00,$40        ; row  3
  DEFB $02,$3C,$40        ; row  4: inner box top
  DEFB $02,$24,$40        ; row  5: inner box sides
  DEFB $02,$24,$40        ; row  6: inner box sides
  DEFB $02,$3C,$40        ; row  7: inner box bottom
  DEFB $02,$00,$40        ; row  8
  DEFB $02,$00,$40        ; row  9
  DEFB $02,$00,$40        ; row 10
  DEFB $03,$FF,$C0        ; row 11: bottom border

; RoomCursor1
;
; Room indicator cursor, phase 1 (AnimateCrewB phase 1). Narrower 1-pixel
; border with an oval interior — cursor drawn at reduced size.
RoomCursor1:
  DEFB $00,$00,$00        ; row  0: blank
  DEFB $01,$FF,$80        ; row  1: top border
  DEFB $01,$00,$80        ; row  2
  DEFB $01,$7E,$80        ; row  3: oval top
  DEFB $01,$42,$80        ; row  4: oval sides
  DEFB $01,$42,$80        ; row  5
  DEFB $01,$42,$80        ; row  6
  DEFB $01,$42,$80        ; row  7
  DEFB $01,$7E,$80        ; row  8
  DEFB $01,$00,$80        ; row  9
  DEFB $01,$FF,$80        ; row 10
  DEFB $00,$00,$00        ; row 11: blank

; RoomCursor2
;
; Room indicator cursor, phase 2 (AnimateCrewB phase 2). Centre-only pattern —
; horizontal bars with inner ring detail.
RoomCursor2:
  DEFB $00,$00,$00        ; row  0: blank
  DEFB $00,$00,$00        ; row  1: blank
  DEFB $00,$FF,$00        ; row  2: top bar
  DEFB $00,$81,$00        ; row  3: outer edge
  DEFB $00,$BD,$00        ; row  4: inner ring
  DEFB $00,$A5,$00        ; row  5: eye sockets
  DEFB $00,$A5,$00        ; row  6
  DEFB $00,$BD,$00        ; row  7
  DEFB $00,$81,$00        ; row  8
  DEFB $00,$FF,$00        ; row  9: bottom bar
  DEFB $00,$00,$00        ; row 10: blank
  DEFB $00,$00,$00        ; row 11: blank

; RoomCursor3
;
; Room indicator cursor, phase 3 (AnimateCrewB phase 3). Smallest oval shape —
; cursor at its most contracted phase.
RoomCursor3:
  DEFB $00,$00,$00        ; row  0: blank
  DEFB $00,$00,$00        ; row  1: blank
  DEFB $00,$00,$00        ; row  2: blank
  DEFB $00,$7E,$00        ; row  3: head oval
  DEFB $00,$42,$00        ; row  4
  DEFB $00,$5A,$00        ; row  5: inner detail
  DEFB $00,$5A,$00        ; row  6
  DEFB $00,$42,$00        ; row  7
  DEFB $00,$7E,$00        ; row  8: head oval
  DEFB $00,$00,$00        ; row  9: blank
  DEFB $00,$00,$00        ; row 10: blank
  DEFB $00,$00,$00        ; row 11: blank

; CrewWalkA1
;
; Crew member walking sprite, phase 1 (AnimateCrewA AnimateCrewA, phases 0-1).
; XOR-blit sprite: 24px wide x 12px tall (3 bytes per row). Upright
; standing/walking pose — arms close to body, feet together. Left and right
; bytes are zero (figure fits in the 8-pixel centre column).
CrewWalkA1:
  DEFB $00,$00,$00        ; row  0: blank
  DEFB $00,$08,$00        ; row  1: head top
  DEFB $00,$1C,$00        ; row  2: head
  DEFB $00,$08,$00        ; row  3: chin
  DEFB $00,$3E,$00        ; row  4: shoulders
  DEFB $00,$5D,$00        ; row  5: torso
  DEFB $00,$3E,$00        ; row  6: body
  DEFB $00,$1C,$00        ; row  7: hips
  DEFB $00,$14,$00        ; row  8: upper legs
  DEFB $00,$14,$00        ; row  9: mid legs
  DEFB $00,$14,$00        ; row 10: lower legs
  DEFB $00,$36,$00        ; row 11: feet

; CrewWalkA2
;
; Crew member walking sprite, phase 2 (AnimateCrewA AnimateCrewA, phases 2-3).
; Mid-stride pose — arms pumping outward, legs spread in step. Row 5 has a
; stray pixel in the right byte (arm extension).
CrewWalkA2:
  DEFB $00,$08,$00        ; row  0: head
  DEFB $00,$14,$00        ; row  1: head wide
  DEFB $00,$22,$00        ; row  2: arms raising
  DEFB $00,$36,$00        ; row  3: upper arms
  DEFB $00,$41,$00        ; row  4: arms spread
  DEFB $00,$A2,$80        ; row  5: torso + arm extension
  DEFB $00,$41,$00        ; row  6
  DEFB $00,$22,$00        ; row  7: arms return
  DEFB $00,$2A,$00        ; row  8: legs mid-stride
  DEFB $00,$2A,$00        ; row  9
  DEFB $00,$6B,$00        ; row 10: feet contact
  DEFB $00,$49,$00        ; row 11: feet split

; AnimCrewAFrames
;
; 4 × 16-bit pointers to crew animation frame-A sprite bitmaps. Indexed by
; frame phase (0-3); phases 0-1 share one bitmap, 2-3 share another, producing
; a two-frame walk cycle at rate 10 (10 frames/step). Used by AnimateCrewA
; (AnimateCrewA) to XOR-blit crew sprites onto the screen.
CrewWalkAnimA:
  DEFW CrewWalkA1,CrewWalkA1,CrewWalkA2,CrewWalkA2

; AnimCrewBFrames
;
; 4 × 16-bit pointers to room indicator cursor bitmaps (faster channel, rate
; 5). Used by AnimateCrewB (AnimateCrewB) to XOR-blit the animated room cursor
; that highlights the current room on the corridor map.
CrewWalkAnimB:
  DEFW RoomCursor0,RoomCursor1,RoomCursor2,RoomCursor3

; CharTileBitmaps
;
; Master 8×8 pixel tileset used by DrawSpriteRow (DrawSpriteRow) for all
; corridor and room graphics. 169 tiles × 8 bytes each (one byte per pixel
; row). The first 128 tiles are arranged so that tile N is the glyph for ASCII
; code N, which lets DrawSprite (DrawSprite) render the 10-character text
; labels stored in RoomNameTable (RoomNameTable / UIStringTable) by treating
; each ASCII byte as a tile index. Tiles 128+ are non-text graphics (sprites,
; arrows, etc.). Tile N starts at CharBitmaps + N×8.
;
; SPRITE sheet — 16 tiles per row, 2× scale:
;
CharBitmaps:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$FF,$00,$00,$FF,$00,$00
  DEFB $24,$24,$24,$24,$24,$24,$24,$24
  DEFB $02,$04,$08,$11,$22,$44,$88,$10
  DEFB $20,$40,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$01
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $01,$00,$00,$00,$00,$00,$00,$00
  DEFB $10,$88,$44,$22,$11,$08,$04,$02
  DEFB $00,$00,$00,$00,$00,$80,$40,$20
  DEFB $02,$04,$F8,$01,$02,$FC,$00,$00
  DEFB $00,$00,$0F,$10,$20,$47,$88,$10
  DEFB $00,$00,$FF,$00,$00,$E7,$24,$24
  DEFB $00,$00,$FC,$02,$01,$F8,$04,$02
  DEFB $10,$88,$44,$24,$24,$24,$24,$24
  DEFB $00,$00,$00,$FF,$FF,$00,$00,$00
  DEFB $18,$18,$18,$18,$18,$18,$18,$18
  DEFB $24,$24,$24,$24,$24,$44,$88,$10
  DEFB $02,$04,$F8,$01,$02,$E4,$24,$24
  DEFB $24,$24,$27,$20,$20,$27,$24,$24
  DEFB $24,$24,$E7,$00,$00,$FF,$00,$00
  DEFB $24,$24,$E4,$04,$04,$E4,$24,$24
  DEFB $02,$04,$F8,$01,$01,$F8,$04,$02
  DEFB $20,$40,$80,$00,$00,$80,$40,$20
  DEFB $00,$00,$3F,$20,$20,$27,$24,$24
  DEFB $24,$24,$27,$20,$20,$3F,$00,$00
  DEFB $24,$24,$E4,$07,$07,$F8,$00,$00
  DEFB $00,$00,$1F,$E0,$E0,$27,$24,$24
  DEFB $24,$24,$E7,$00,$00,$E7,$24,$24
  DEFB $02,$04,$08,$11,$22,$24,$24,$24
  DEFB $24,$24,$24,$22,$11,$08,$04,$02
  DEFB $10,$88,$47,$20,$10,$0F,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $24,$24,$E4,$02,$01,$F8,$04,$02
  DEFB $00,$00,$F8,$07,$07,$E4,$24,$24
  DEFB $00,$44,$FE,$44,$44,$FE,$44,$00
  DEFB $00,$00,$FC,$04,$04,$E4,$24,$24
  DEFB $24,$24,$E4,$04,$04,$FC,$00,$00
  DEFB $00,$00,$FF,$07,$07,$E7,$27,$27
  DEFB $00,$00,$FF,$FF,$FF,$FF,$FF,$FF
  DEFB $FF,$FF,$FF,$FF,$FF,$FF,$00,$00
  DEFB $27,$27,$E7,$07,$07,$FF,$00,$00
  DEFB $FF,$FE,$FC,$F8,$F0,$E0,$C0,$80
  DEFB $80,$C0,$E0,$F0,$F8,$FC,$FE,$FF
  DEFB $FF,$FF,$FF,$FF,$FF,$FF,$FF,$FF
  DEFB $27,$27,$27,$27,$27,$27,$27,$27
  DEFB $27,$27,$E7,$07,$07,$E7,$27,$27
  DEFB $27,$27,$27,$20,$20,$27,$24,$24
  DEFB $7E,$46,$42,$42,$42,$42,$7E,$00
  DEFB $10,$10,$10,$18,$18,$18,$18,$00
  DEFB $7E,$06,$06,$06,$7E,$40,$7E,$00
  DEFB $7E,$06,$06,$06,$7E,$06,$7E,$00
  DEFB $40,$40,$40,$58,$7E,$18,$18,$00
  DEFB $7E,$40,$40,$40,$7E,$06,$7E,$00
  DEFB $40,$40,$40,$40,$7E,$46,$7E,$00
  DEFB $7E,$02,$02,$06,$06,$06,$06,$00
  DEFB $7E,$46,$46,$46,$7E,$46,$7E,$00
  DEFB $7E,$46,$46,$46,$7E,$06,$06,$00
  DEFB $00,$60,$60,$00,$00,$60,$60,$00
  DEFB $FF,$FF,$FF,$00,$00,$FF,$00,$00
  DEFB $E4,$E4,$E4,$E4,$E4,$E4,$E4,$E4
  DEFB $00,$00,$FF,$00,$00,$FF,$FF,$FF
  DEFB $24,$24,$E4,$04,$04,$E4,$E4,$E4
  DEFB $E4,$E4,$E4,$04,$04,$E4,$24,$24
  DEFB $02,$04,$08,$10,$20,$27,$24,$24
  DEFB $38,$44,$44,$7C,$44,$44,$EE,$00
  DEFB $FC,$42,$42,$7C,$42,$42,$FC,$00
  DEFB $3A,$46,$82,$80,$80,$42,$3C,$00
  DEFB $F8,$44,$42,$42,$42,$44,$F8,$00
  DEFB $7F,$21,$24,$3C,$24,$21,$7F,$00
  DEFB $7F,$21,$24,$3C,$24,$20,$70,$00
  DEFB $3A,$46,$82,$90,$9E,$42,$3C,$00
  DEFB $EE,$44,$44,$7C,$44,$44,$EE,$00
  DEFB $FE,$92,$10,$10,$10,$92,$FE,$00
  DEFB $FE,$92,$10,$10,$10,$90,$60,$00
  DEFB $C6,$48,$50,$68,$44,$44,$C6,$00
  DEFB $E0,$40,$40,$40,$40,$42,$FE,$00
  DEFB $44,$6C,$54,$54,$44,$44,$EE,$00
  DEFB $44,$64,$54,$54,$4C,$44,$EE,$00
  DEFB $38,$44,$82,$82,$82,$44,$38,$00
  DEFB $FC,$42,$42,$7C,$40,$40,$E0,$00
  DEFB $38,$44,$82,$82,$8A,$44,$3A,$00
  DEFB $FC,$42,$42,$7C,$44,$42,$E2,$00
  DEFB $7C,$82,$80,$7C,$02,$82,$7C,$00
  DEFB $FE,$92,$10,$10,$10,$10,$38,$00
  DEFB $82,$82,$82,$82,$82,$44,$38,$00
  DEFB $82,$82,$44,$44,$28,$28,$10,$00
  DEFB $92,$92,$92,$92,$BA,$C6,$82,$00
  DEFB $C6,$44,$28,$10,$28,$44,$C6,$00
  DEFB $82,$44,$28,$10,$10,$10,$38,$00
  DEFB $FE,$82,$0C,$10,$60,$82,$FE,$00
  DEFB $20,$40,$FF,$00,$00,$FF,$00,$00
  DEFB $24,$24,$27,$20,$10,$08,$04,$02
  DEFB $00,$00,$FF,$00,$00,$FF,$40,$20
  DEFB $E4,$E4,$E7,$E0,$E0,$FF,$FF,$FF
  DEFB $FF,$FF,$FF,$E0,$E0,$E7,$E4,$E4
  DEFB $00,$7E,$56,$6A,$56,$6A,$7E,$00
  DEFB $00,$7A,$84,$84,$84,$7A,$00,$00
  DEFB $40,$7C,$42,$42,$42,$FC,$00,$00
  DEFB $00,$7C,$82,$80,$82,$7C,$00,$00
  DEFB $04,$7C,$84,$84,$84,$7E,$00,$00
  DEFB $00,$7C,$82,$FE,$80,$7C,$00,$00
  DEFB $38,$40,$40,$78,$40,$40,$40,$00
  DEFB $00,$7A,$84,$84,$84,$7C,$04,$78
  DEFB $40,$78,$44,$44,$44,$EE,$00,$00
  DEFB $10,$00,$10,$10,$10,$38,$00,$00
  DEFB $10,$00,$10,$10,$10,$10,$90,$60
  DEFB $C0,$46,$58,$60,$68,$C6,$00,$00
  DEFB $30,$10,$10,$10,$10,$38,$00,$00
  DEFB $00,$7C,$92,$92,$92,$92,$00,$00
  DEFB $00,$F8,$84,$84,$84,$84,$00,$00
  DEFB $00,$7C,$82,$82,$82,$7C,$00,$00
  DEFB $00,$F8,$84,$84,$84,$F8,$80,$80
  DEFB $00,$7C,$84,$84,$84,$7C,$04,$06
  DEFB $00,$B8,$44,$40,$40,$E0,$00,$00
  DEFB $00,$7C,$80,$7C,$02,$7C,$00,$00
  DEFB $10,$10,$7C,$10,$12,$1C,$00,$00
  DEFB $00,$84,$84,$84,$84,$78,$00,$00
  DEFB $00,$82,$44,$44,$28,$10,$00,$00
  DEFB $00,$92,$92,$92,$92,$7C,$00,$00
  DEFB $00,$C6,$28,$10,$28,$C6,$00,$00
  DEFB $00,$84,$84,$84,$84,$7C,$04,$78
  DEFB $00,$FE,$86,$18,$62,$FE,$00,$00
  DEFB $3C,$24,$3C,$24,$3C,$24,$3C,$24
  DEFB $00,$7E,$66,$5A,$5A,$66,$7E,$00
  DEFB $FF,$CB,$D5,$CB,$D5,$CB,$D5,$FF
  DEFB $00,$F7,$00,$F7,$F7,$F7,$EF,$00
  DEFB $5E,$5E,$5E,$42,$1C,$5E,$5E,$5E
  DEFB $00,$0F,$38,$37,$69,$5E,$5E,$5E
  DEFB $5E,$5E,$5E,$69,$37,$38,$0F,$00
  DEFB $7A,$7A,$7A,$96,$EC,$1C,$F0,$00
  DEFB $00,$F0,$1C,$EC,$96,$7A,$7A,$7A
  DEFB $00,$EF,$F7,$F7,$F7,$00,$F7,$00
  DEFB $7A,$7A,$7A,$42,$38,$7A,$7A,$7A
  DEFB $24,$24,$27,$E0,$E0,$1F,$00,$00
  DEFB $01,$32,$34,$08,$10,$2C,$4C,$80
  DEFB $30,$48,$48,$48,$32,$54,$88,$76
  DEFB $24,$24,$27,$20,$20,$27,$27,$27
  DEFB $08,$1C,$3E,$08,$08,$08,$08,$00
  DEFB $00,$04,$06,$7F,$06,$04,$00,$00
  DEFB $08,$08,$08,$08,$3E,$1C,$08,$00
  DEFB $00,$20,$60,$FE,$60,$20,$00,$00
  DEFB $18,$24,$42,$42,$66,$7E,$5E,$5E
  DEFB $7E,$7E,$7E,$7E,$7E,$7E,$3C,$18
  DEFB $00,$00,$00,$00,$00,$10,$30,$00
  DEFB $08,$08,$10,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$0F,$3F,$FF
  DEFB $FC,$F0,$C0,$00,$00,$00,$00,$00
  DEFB $3F,$0F,$03,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$C0,$F0,$FC,$FF
  DEFB $80,$80,$80,$80,$80,$80,$80,$80
  DEFB $01,$01,$01,$01,$01,$01,$01,$01
  DEFB $00,$00,$00,$00,$00,$00,$00,$FF
  DEFB $80,$80,$C0,$C0,$E0,$E0,$F0,$F0
  DEFB $01,$01,$03,$03,$07,$07,$0F,$0F
  DEFB $07,$07,$03,$03,$01,$01,$00,$FF
  DEFB $E0,$E0,$C0,$C0,$80,$80,$00,$FF
  DEFB $FF,$FF,$FF,$00,$00,$00,$00,$FF
  DEFB $00,$00,$00,$66,$66,$00,$00,$00
  DEFB $00,$00,$00,$18,$18,$00,$00,$00
  DEFB $1F,$1F,$1F,$1F,$1F,$1F,$1F,$1F
  DEFB $80,$80,$C0,$E0,$E0,$F0,$F8,$F8
  DEFB $07,$03,$01,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$80,$80,$C0,$E0
  DEFB $1F,$0F,$07,$07,$03,$03,$01,$00
  DEFB $01,$01,$03,$07,$07,$0F,$1F,$1F
  DEFB $C0,$80,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$01,$01,$03,$07
  DEFB $FC,$FC,$F0,$F0,$E0,$E0,$C0,$C0

; RoomNameTable / UIStringTable
;
; 95-entry × 10-byte text table. DrawSprite (DrawSprite) reads 10 bytes
; starting at (RoomNameTable + N×10) and renders them as a 10-character label
; by blitting the CharBitmaps (CharBitmaps) glyph for each byte — i.e. the
; ASCII text *is* the "sprite data". Indices 0–46 are RoomNameTable (room /
; item / deck names); indices 47–94 are UIStringTable (UI / menu / action
; labels).
;
; ENTRIES 57–60 embed the four cursor-arrow UDG codes ($8A–$8D) inside the
; OTHERWISE-ASCII text.
RoomNameTable:
  DEFM "Airlock #1"       ; 0
  DEFM "Airlock #2"       ; 1
  DEFM "Armoury   "       ; 2
  DEFM "Cargopod#1"       ; 3
  DEFM "Cargopod#2"       ; 4
  DEFM "Cargopod#3"       ; 5
  DEFM "CommdCentr"       ; 6
  DEFM "Computer  "       ; 7
  DEFM "Corridor#1"       ; 8
  DEFM "Corridor#2"       ; 9
  DEFM "Corridor#3"       ; 10
  DEFM "Corridor#4"       ; 11
  DEFM "Corridor#5"       ; 12
  DEFM "Corridor#6"       ; 13
  DEFM "Corridor#7"       ; 14
  DEFM "Cryo Vault"       ; 15
  DEFM "Engineerng"       ; 16
  DEFM "Engine #1 "       ; 17
  DEFM "Engine #2 "       ; 18
  DEFM "Engine #3 "       ; 19
  DEFM "Eng Stores"       ; 20
  DEFM "Infirmary "       ; 21
  DEFM "Inf Stores"       ; 22
  DEFM "Laboratory"       ; 23
  DEFM "Lab Stores"       ; 24
  DEFM "Life Suppt"       ; 25
  DEFM "Livng Qtrs"       ; 26
  DEFM "Mess      "       ; 27
  DEFM "RecrtnArea"       ; 28
  DEFM "Stores #1 "       ; 29
  DEFM "Stores #2 "       ; 30
  DEFM "Stores #3 "       ; 31
  DEFM "ShuttleBay"       ; 32
  DEFM "Shttlstore"       ; 33
  DEFM "Narcissus "       ; 34
  DEFM "Elctrc Prd"       ; 35
  DEFM "Incineratr"       ; 36
  DEFM "Tracker   "       ; 37
  DEFM "Fire Extng"       ; 38
  DEFM "Harpn Gun "       ; 39
  DEFM "Laser Pist"       ; 40
  DEFM "Net       "       ; 41
  DEFM "Cat Box   "       ; 42
  DEFM "Spanner   "       ; 43
  DEFM "Upper Deck"       ; 44
  DEFM "MiddleDeck"       ; 45
  DEFM "Lower Deck"       ; 46
UIStringTable:
  DEFM "Get Item  "       ; 47
  DEFM "Leave Item"       ; 48
  DEFM "RmveGrille"       ; 49
  DEFM "Grille    "       ; 50
  DEFM "Get Jones "       ; 51
  DEFM "  ATTACK  "       ; 52
  DEFM "BlowLock#1"       ; 53
  DEFM "BlowLock#2"       ; 54
  DEFM "SealLock#1"       ; 55
  DEFM "SealLock#2"       ; 56
  DEFM "North ",$8A,"   " ; 57 (138 = up arrow UDG)
  DEFM "East  ",$8B,"   " ; 58 (139 = right arrow UDG)
  DEFM "South ",$8C,"   " ; 59 (140 = down arrow UDG)
  DEFM "West  ",$8D,"   " ; 60 (141 = left arrow UDG)
  DEFM "   QUIT   "       ; 61
  DEFM "Other List"       ; 62
  DEFM "AGF:PROTEK"       ; 63 joystick types
  DEFM " KEMPSTON "       ; 64
  DEFM " KEYBOARD "       ; 65
  DEFM " SINCLAIR "       ; 66
  DEFM "  FULLER  "       ; 67
  DEFM "Cat in Net"       ; 68
  DEFM "Cat in Box"       ; 69
  DEFM " Scuttle  "       ; 70
  DEFM " Nostromo "       ; 71
  DEFM " Override "       ; 72
  DEFM " Destruct "       ; 73
  DEFM "  Enter   "       ; 74
  DEFM "Hypersleep"       ; 75
  DEFM " Fight    "       ; 76
  DEFM "  Fire    "       ; 77
  DEFM "  Launch  "       ; 78
  DEFM " Indicate "       ; 79
  DEFM " Location "       ; 80
  DEFM "Narcissus "       ; 81
  DEFM " CONTROL  "       ; 82
  DEFM "Order:    "       ; 83
  DEFM " Display  "       ; 84
  DEFM "  Level   "       ; 85
  DEFM "move to:  "       ; 86
  DEFM " use:     "       ; 87
  DEFM "Special   "       ; 88
  DEFM "Damage    "       ; 89
  DEFM " in place "       ; 90
  DEFM " removed  "       ; 91
  DEFM "          "       ; 92 (blank)
  DEFM "In Duct   "       ; 93
  DEFM "Body of   "       ; 94

; Room adjacency map — corridor/door network
;
; The Nostromo's connectivity, as two 5-byte-per-room tables (this one and the
; air-duct network at RoomAdjDucts). Room IDs 0-34 index the room-name table at
; RoomNameTable. Each room has 5 direction slots holding the destination room
; reached by leaving in that direction; a slot equal to the room's own ID means
; "no exit that way". The graph is fully symmetric (every door works both
; ways).
;
; A direction slot is chosen by ScriptNibbleToDirection
;   (ScriptNibbleToDirection) from the
; ROM script stream, so AI wandering is a deterministic weighted random
; WALK. Read by the destination picker at PickNextRoom (which writes an actor's
; RECORD byte +2), by the alien's own per-move room step inside UpdateJones,
; AND by the door-exit-list builder inside DrawSprite that feeds the room
; VIEW.
RoomAdjCorridors:
  DEFB $0D,$0D,$00,$00,$00 ; 0 Airlock #1 -> 13 Corridor#6
  DEFB $0D,$0D,$01,$01,$01 ; 1 Airlock #2 -> 13 Corridor#6
  DEFB $0A,$0A,$0A,$02,$02 ; 2 Armoury    -> 10 Corridor#3
  DEFB $04,$04,$03,$03,$03 ; 3 Cargopod#1 -> 4 Cargopod#2
  DEFB $20,$03,$0E,$05,$09 ; 4 Cargopod#2 -> 3 Cargopod#1, 5 Cargopod#3, 9
                           ; Corridor#2, 14 Corridor#7, 32 ShuttleBay
  DEFB $04,$04,$05,$05,$05 ; 5 Cargopod#3 -> 4 Cargopod#2
  DEFB $08,$08,$08,$06,$06 ; 6 CommdCentr -> 8 Corridor#1
  DEFB $0D,$0D,$1E,$1E,$07 ; 7 Computer   -> 13 Corridor#6, 30 Stores #2
  DEFB $06,$15,$09,$17,$1A ; 8 Corridor#1 -> 6 CommdCentr, 9 Corridor#2, 21
                           ; Infirmary, 23 Laboratory, 26 Livng Qtrs
  DEFB $08,$04,$0A,$0F,$0C ; 9 Corridor#2 -> 4 Cargopod#2, 8 Corridor#1, 10
                           ; Corridor#3, 12 Corridor#5, 15 Cryo Vault
  DEFB $09,$16,$02,$11,$0B ; 10 Corridor#3 -> 2 Armoury, 9 Corridor#2, 11
                           ; Corridor#4, 17 Engine #1, 22 Inf Stores
  DEFB $0A,$0A,$0C,$0C,$12 ; 11 Corridor#4 -> 10 Corridor#3, 12 Corridor#5, 18
                           ; Engine #2
  DEFB $09,$18,$1D,$13,$0B ; 12 Corridor#5 -> 9 Corridor#2, 11 Corridor#4, 19
                           ; Engine #3, 24 Lab Stores, 29 Stores #1
  DEFB $1B,$00,$07,$1F,$01 ; 13 Corridor#6 -> 0 Airlock #1, 1 Airlock #2, 7
                           ; Computer, 27 Mess, 31 Stores #3
  DEFB $04,$04,$19,$19,$0E ; 14 Corridor#7 -> 4 Cargopod#2, 25 Life Suppt
  DEFB $09,$09,$0F,$0F,$0F ; 15 Cryo Vault -> 9 Corridor#2
  DEFB $20,$20,$14,$14,$10 ; 16 Engineerng -> 20 Eng Stores, 32 ShuttleBay
  DEFB $0A,$0A,$11,$11,$11 ; 17 Engine #1  -> 10 Corridor#3
  DEFB $0B,$0B,$12,$12,$12 ; 18 Engine #2  -> 11 Corridor#4
  DEFB $0C,$0C,$13,$13,$13 ; 19 Engine #3  -> 12 Corridor#5
  DEFB $10,$10,$10,$14,$14 ; 20 Eng Stores -> 16 Engineerng
  DEFB $08,$08,$08,$15,$15 ; 21 Infirmary  -> 8 Corridor#1
  DEFB $0A,$0A,$0A,$16,$16 ; 22 Inf Stores -> 10 Corridor#3
  DEFB $08,$08,$08,$17,$17 ; 23 Laboratory -> 8 Corridor#1
  DEFB $0C,$0C,$0C,$18,$18 ; 24 Lab Stores -> 12 Corridor#5
  DEFB $0E,$0E,$19,$19,$19 ; 25 Life Suppt -> 14 Corridor#7
  DEFB $1C,$08,$1B,$1B,$1A ; 26 Livng Qtrs -> 8 Corridor#1, 27 Mess, 28
                           ; RecrtnArea
  DEFB $1A,$1C,$0D,$1B,$1B ; 27 Mess       -> 13 Corridor#6, 26 Livng Qtrs, 28
                           ; RecrtnArea
  DEFB $1A,$1A,$1B,$1B,$1C ; 28 RecrtnArea -> 26 Livng Qtrs, 27 Mess
  DEFB $0C,$0C,$0C,$1D,$1D ; 29 Stores #1  -> 12 Corridor#5
  DEFB $07,$07,$1E,$1E,$1E ; 30 Stores #2  -> 7 Computer
  DEFB $0D,$0D,$1F,$1F,$1F ; 31 Stores #3  -> 13 Corridor#6
  DEFB $21,$22,$04,$10,$20 ; 32 ShuttleBay -> 4 Cargopod#2, 16 Engineerng, 33
                           ; Shttlstore, 34 Narcissus
  DEFB $20,$20,$20,$21,$21 ; 33 Shttlstore -> 32 ShuttleBay
  DEFB $20,$20,$20,$22,$22 ; 34 Narcissus  -> 32 ShuttleBay

; Room adjacency map — air-duct network
;
; Companion table to RoomAdjCorridors (RoomAdjCorridors): same 5-slot-per-room
; format, but describing the air-duct/crawlway network that connects the same
; rooms along different edges (e.g. Mess connects directly to both Airlocks,
; Computer to Engine #2). Only 34 entries — the Narcissus (room 34) cannot be
; reached through the ducts.
;
; AN actor whose record byte +1 has bit 6 set is travelling in this
; NETWORK; destinations fetched from here are stored with bit 6 set too,
; KEEPING the actor in duct space until a room handler clears the flag.
; READ by the duct branch of the destination picker at PickNextRoom, by the
; GRILLE-LIST builder inside DrawSprite that lists a room's duct openings,
; AND by the duct-entry move inside MoveToRoom that sends a crew member
; THROUGH a grille. The two one-way links (Airlock #2 -> Cargopod#1 and
; STORES #3 -> Airlock #2) are present in the original data.
RoomAdjDucts:
  DEFB $00,$1E,$1B,$1A,$00 ; 0 Airlock #1 -> 26 Livng Qtrs, 27 Mess, 30 Stores
                           ; #2
  DEFB $1B,$03,$01,$1C,$1B ; 1 Airlock #2 -> 3 Cargopod#1, 27 Mess, 28
                           ; RecrtnArea
  DEFB $02,$0B,$0B,$16,$16 ; 2 Armoury    -> 11 Corridor#4, 22 Inf Stores
  DEFB $21,$0E,$0E,$03,$03 ; 3 Cargopod#1 -> 14 Corridor#7, 33 Shttlstore
  DEFB $04,$0E,$0E,$0E,$04 ; 4 Cargopod#2 -> 14 Corridor#7
  DEFB $05,$0E,$14,$14,$05 ; 5 Cargopod#3 -> 14 Corridor#7, 20 Eng Stores
  DEFB $06,$08,$08,$06,$06 ; 6 CommdCentr -> 8 Corridor#1
  DEFB $07,$12,$12,$0D,$0D ; 7 Computer   -> 13 Corridor#6, 18 Engine #2
  DEFB $15,$08,$17,$06,$08 ; 8 Corridor#1 -> 6 CommdCentr, 21 Infirmary, 23
                           ; Laboratory
  DEFB $0A,$0A,$0C,$0C,$09 ; 9 Corridor#2 -> 10 Corridor#3, 12 Corridor#5
  DEFB $16,$0A,$09,$15,$0A ; 10 Corridor#3 -> 9 Corridor#2, 21 Infirmary, 22
                           ; Inf Stores
  DEFB $02,$12,$1D,$0F,$02 ; 11 Corridor#4 -> 2 Armoury, 15 Cryo Vault, 18
                           ; Engine #2, 29 Stores #1
  DEFB $09,$09,$18,$17,$17 ; 12 Corridor#5 -> 9 Corridor#2, 23 Laboratory, 24
                           ; Lab Stores
  DEFB $1E,$07,$1F,$1B,$0D ; 13 Corridor#6 -> 7 Computer, 27 Mess, 30 Stores
                           ; #2, 31 Stores #3
  DEFB $03,$19,$05,$04,$0E ; 14 Corridor#7 -> 3 Cargopod#1, 4 Cargopod#2, 5
                           ; Cargopod#3, 25 Life Suppt
  DEFB $0F,$0B,$0B,$0F,$0F ; 15 Cryo Vault -> 11 Corridor#4
  DEFB $10,$10,$14,$20,$20 ; 16 Engineerng -> 20 Eng Stores, 32 ShuttleBay
  DEFB $11,$12,$12,$11,$11 ; 17 Engine #1  -> 18 Engine #2
  DEFB $11,$07,$13,$0B,$19 ; 18 Engine #2  -> 7 Computer, 11 Corridor#4, 17
                           ; Engine #1, 19 Engine #3, 25 Life Suppt
  DEFB $13,$12,$12,$13,$13 ; 19 Engine #3  -> 18 Engine #2
  DEFB $10,$05,$05,$14,$14 ; 20 Eng Stores -> 5 Cargopod#3, 16 Engineerng
  DEFB $15,$0A,$08,$08,$15 ; 21 Infirmary  -> 8 Corridor#1, 10 Corridor#3
  DEFB $02,$0A,$0A,$16,$16 ; 22 Inf Stores -> 2 Armoury, 10 Corridor#3
  DEFB $08,$0C,$0C,$17,$17 ; 23 Laboratory -> 8 Corridor#1, 12 Corridor#5
  DEFB $18,$0C,$1D,$1D,$18 ; 24 Lab Stores -> 12 Corridor#5, 29 Stores #1
  DEFB $19,$19,$0E,$12,$12 ; 25 Life Suppt -> 14 Corridor#7, 18 Engine #2
  DEFB $1A,$00,$1C,$1C,$1A ; 26 Livng Qtrs -> 0 Airlock #1, 28 RecrtnArea
  DEFB $00,$0D,$01,$1B,$1B ; 27 Mess       -> 0 Airlock #1, 1 Airlock #2, 13
                           ; Corridor#6
  DEFB $1A,$01,$01,$1C,$1C ; 28 RecrtnArea -> 1 Airlock #2, 26 Livng Qtrs
  DEFB $1D,$0B,$0B,$18,$1D ; 29 Stores #1  -> 11 Corridor#4, 24 Lab Stores
  DEFB $1E,$1E,$0D,$00,$0D ; 30 Stores #2  -> 0 Airlock #1, 13 Corridor#6
  DEFB $0D,$0D,$1F,$01,$1F ; 31 Stores #3  -> 1 Airlock #2, 13 Corridor#6
  DEFB $21,$21,$10,$20,$20 ; 32 ShuttleBay -> 16 Engineerng, 33 Shttlstore
  DEFB $21,$03,$03,$20,$20 ; 33 Shttlstore -> 3 Cargopod#1, 32 ShuttleBay

; CrewNameTable
;
; 7 crew/role names separated by 255 ($FF) terminators: ALIEN, Dallas, Kane,
; Ripley, Ash, Lambert, Parker, Brett. Substituted into MessageTextTable
; placeholders 253/254 by the message renderer at RenderMessage.
CrewNameTable:
  DEFB $41,$4C,$49,$45,$4E,$FF,$44,$61
  DEFB $6C,$6C,$61,$73,$FF,$4B,$61,$6E
  DEFB $65,$FF,$52,$69,$70,$6C,$65,$79
  DEFB $FF,$41,$73,$68,$FF,$4C,$61,$6D
  DEFB $62,$65,$72,$74,$FF,$50,$61,$72
  DEFB $6B,$65,$72,$FF,$42,$72,$65,$74
  DEFB $74,$FF

; Actor record array — slot 0: the alien
;
; Eight 8-byte actor records (64 bytes in all). Slot k's name is entry k of
; CrewNameTable (CrewNameTable): slot 0 = ALIEN, slots 1-7 = Dallas, Kane,
; Ripley, Ash, Lambert, Parker, Brett. Most per-frame loops start from this
; base (LD IX,29566) and step 8 bytes per slot; the message renderer at
; RenderMessage passes the slot number straight to the name printer.
;
; PER-RECORD field layout (IX-relative):
; +0 = sprite index for the actor's current pose (20 = idle; 92 =
; BLANK; 255 = none) — also stepped as an action/frame counter
; +1 = current room: bits 0-5 = room ID 0-34 (indexes RoomNameTable names
; AND the RoomAdjCorridors adjacency map), bit 6 set = travelling in the
; AIR-DUCT network (RoomAdjDucts). $FF = HostMarker: slot excluded
; FROM normal crew processing
; +2 = destination room, written by the picker at PickNextRoom (bit 6
; CARRIES the duct flag along)
; +3 = action state (1=idle, 2=move, 3=wait, 5=investigate, 7=attack)
; +4 = strength / hit points (initial values 2-6; combat decrements)
; +5 = sprite X tile
; +6 = sprite Y tile
; +7 = status: 0 = alive/normal; 1 = killed (stamped by CrewHitsAlien's
; KILL path); 2 = android defeated; $FF = removed from play
; HYPERSLEEP, OR HOSTMARKER PARTNER OF +1=$FF
;
; Slot 0 is the alien: its +1 byte (29566+1) holds the alien's current room at
; runtime (initialised inside CommonGameInit CommonInit_ItemsGrilles, updated
; inside UpdateRoomActors; read by CrewHitsAlien's path at XorHAddE and by
; Jones's avoidance check in UpdateJones). Its +7 byte (ActorStatusColumn)
; anchors the status-column scans (+7 column, step 8). The whole array is
; overwritten at game start by the templates at LongGameCrewInit (Long Game) /
; ShortGameCrewInit (Short Game); the row values below are the tape-image
; leftovers.
ActorRecords:
  DEFB $00,$00,$00,$00,$00,$00,$00 ; slot 0 (+0..+6) — unused

; Actor record array — status column anchor
;
; Slot 0's +7 (status) byte. The alive/status scans walk the +7 column from
; here or from slot 1's +7 (CrewStatusColumn) in steps of 8.
ActorStatusColumn:
  DEFB $00

; Actor record array — crew slots 1-7
;
; See ActorRecords for the field layout. Slot 1's row is split after +6 so that
; the status byte (CrewStatusColumn, base of the crew alive-flag scan used by
; DrawStatusPanel) keeps its own address and label.
CrewRecords:
  DEFB $40,$0C,$00,$00,$02,$02,$02 ; slot 1 Dallas  +0..+6 — in Corridor#5
CrewStatusColumn:
  DEFB $00                ; slot 1 Dallas  +7 (alive-scan base)
  DEFB $40,$16,$00,$00,$04,$04,$04,$00 ; slot 2 Kane    — in Inf Stores
  DEFB $40,$4B,$00,$00,$04,$02,$02,$00 ; slot 3 Ripley  — in Corridor#4 duct
                                       ; (bit 6)
  DEFB $40,$09,$00,$00,$04,$04,$04,$00 ; slot 4 Ash     — in Corridor#2
  DEFB $40,$1B,$00,$00,$03,$04,$04,$00 ; slot 5 Lambert — in Mess
  DEFB $40,$0B,$00,$00,$03,$04,$04,$00 ; slot 6 Parker  — in Corridor#4
  DEFB $22,$13,$00,$00,$04,$02,$02,$00 ; slot 7 Brett   — in Engine #3

; CorridorPosTable — the 19-cell strip / action-menu rows
;
; 19 bytes indexed by CorridorCursor (CorridorCursor, range 0..18); the cell
; value under the cursor is mirrored to CursorCellValue ($8395) and is what the
; action key dispatches on. Cell r is drawn on screen row r+1, columns 22-31
; (row 0 holds the view title); a cell value >= 70 is the first row of a
; two-row label, so the cursor highlight covers two rows (see HighlightMenuRow
; HighlightMenuRow). The cells' meaning depends on the view: on the orders
; screen (mode 0) they hold the OrdersMenuTemplate (OrdersMenuTemplate) bytes;
; in the room view (mode 1) each cell holds a string index for the menu row
; drawn at that position (ResetActionMenu (ResetActionMenu) loads the skeleton
; from ActionMenuTemplate (ActionMenuTemplate); the load image holds a
; hypersleep- room variant: 73 " Destruct ", 74 "  Enter   ", 75 "Hypersleep");
; in the Indicate Location list (modes 2/3, IndicateLocation
; (IndicateLocation)) cells 1-17 hold room ids 0-16 or 17-33, framed by cell 0
; = 62 "Other List" (page toggle) and cell 18 = 61 "   QUIT   ". Read by the
; cursor-movement helpers around HandleInput at HandleInput.
CorridorPosTable:
  DEFB $49,$FF,$FF,$FF,$FF,$FF,$FF,$4A
; (cells 8-15; cell 8 = first " use:" row, written by BuildItemMenu)
MenuUseRows:
  DEFB $FF,$FF,$FF,$FF,$4B,$FF,$FF,$FF
; (cells 16-18; cell 16 = second fixture row, written by FixtureAction)
MenuFixtureRow:
  DEFB $FF,$FF,$FF
; 256 bytes of workspace/padding (zero in the load image)
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00

; Per-room type table
;
; One byte per room (0-34): the map-view layout type fed to RoomTypeByte
; (RoomTypeByte) — 0/1/2 pick the layout templates at RoomLayout0, RoomLayout1
; and RoomLayout2; 3 = the Narcissus. Read by LocationListAction
; (LocationListAction) when a room is picked from the Indicate Location list,
; and by UpdateRoomMarkerB (UpdateRoomMarkerB) to check an exit room against
; the displayed deck.
RoomTypeTable:
  DEFB $00,$00,$01,$02,$02,$02,$01 ; rooms 0-6
  DEFB $00,$01,$01,$01,$01,$01,$00 ; rooms 7-13
  DEFB $02,$01,$02,$01,$01,$01,$02 ; rooms 14-20
  DEFB $01,$01,$01,$01,$02,$00,$00 ; rooms 21-27
  DEFB $00,$01,$00,$00,$02,$02,$03 ; rooms 28-34

; Item location table
;
; One byte per portable item (ids 0-21, names via ItemIdToString
; (ItemIdToString)): the room where the item lies (bit 6 = inside the duct
; network), or a carry marker — 160+slot = held in that actor slot's front
; hand, 128+slot = back hand; 255 = does not exist (the two "Cat in ..." items
; are created when Jones is caught). Re-seeded from InitialItemRooms
; (InitialItemRooms) at game init. The Get Item picker handler ($8B22),
; LeaveItemHandler ($8BBA) and SwapHeldItems (SwapHeldItems) transfer items by
; rewriting these bytes.
ItemLocations:
  DEFB $18,$15,$14        ; 0-2: Elctrc Prd in Lab Stores/Infirmary/Eng Stores
  DEFB $06,$06,$10        ; 3-5: Incineratr in CommdCentr x2, Engineerng
TrackerLocations:
  DEFB $06,$10            ; 6-7: Tracker in CommdCentr, Engineerng
  DEFB $1B,$11,$12,$13    ; 8-11: Fire Extng in Mess, Engine #1-#3
  DEFB $20                ; 12: Harpn Gun in ShuttleBay
  DEFB $02,$02,$02        ; 13-15: Laser Pist in Armoury x3
  DEFB $18                ; 16: Net in Lab Stores
  DEFB $17                ; 17: Cat Box in Laboratory
  DEFB $1F,$14            ; 18-19: Spanner in Stores #3, Eng Stores
CatItemLocations:
  DEFB $FF,$FF            ; 20-21: Cat in Net / Cat in Box (not created)

; Viewed room's item list
;
; Item ids present in the viewed room (built by BuildRoomItemList
; (BuildRoomItemList) scanning ItemLocations top-down, so ids appear in
; descending order); 255-filled by ResetActionMenu (ResetActionMenu). The "Get
; Item" handler (GetItemMenu) turns it into a selection strip and GetItemPick
; (GetItemPick) moves the chosen item into the actor's hand.
RoomItemList:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00

; Per-item courage bonus
;
; Added to the carrier's courage (record byte +5) and morale (+6) while the
; item is held in the front hand (applied on Get at $8B26, removed on Leave at
; $8BAB and adjusted on Swap by SwapHeldItems (SwapHeldItems)). Weapons
; embolden the crew: prods/extinguishers/spanners +1,
; incinerators/harpoon/lasers +2, trackers and cat gear 0.
ItemCourageBonus:
  DEFB $01,$01,$01        ; Elctrc Prd
  DEFB $02,$02,$02        ; Incineratr
  DEFB $00,$00            ; Tracker
  DEFB $01,$01,$01,$01    ; Fire Extng
  DEFB $02                ; Harpn Gun
  DEFB $02,$02,$02        ; Laser Pist
  DEFB $00,$00            ; Net, Cat Box
  DEFB $01,$01            ; Spanner
  DEFB $01,$01            ; Cat in Net / Cat in Box

; Per-room damage meters
;
; 2-digit ASCII pairs ("00"), one per room (base + room*2, rooms 0-34, 70
; bytes). Incremented by AddRoomDamage (AddRoomDamage); the tens digit is the
; severity the engine scan compares against; crossing "30" raises alarms
; #22-#25.
RoomDamageDigits:
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 0-4
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 5-9
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 10-14
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 15-19
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 20-24
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 25-29
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 30-34

; Per-room duct-grille state
;
; One byte per room (rooms 0-33; the Narcissus, room 34, has no grille): 255 =
; grille in place, 0 = removed. Game init ($A5A8, within
; CommonInit_ItemsGrilles) re-arms only the first 33 entries with 255 and then
; clears entry 13, so Corridor#6 (13) starts with its grille removed, and room
; 33's byte at $759D keeps its load-image value 0 (Shttlstore's grille is
; always open). Removed by crew action 3 (CrewAction3_RemoveGrille, a 50-frame
; job queued by the AI at $90EE or by picking "RmveGrille" from the action
; menu). While in place the room view offers string 49 "RmveGrille" in the
; menu's Special slot (CorridorPosTable cell 13); once removed the room shows
; "Grille removed" and string 50 "Grille" becomes a move-to target (cell 1)
; leading into the duct network. Read at $7E58 (DrawItemsAndGrille), $90E5,
; $B073 and RefreshSpecialRow.
RoomGrilleState:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00

; Crew-condition strings
;
; 255-terminated descriptions printed after "NAME is " by the room-info panel
; (DrawCrewCondition, DrawCrewCondition): four injury states selected by actor
; strength (record byte +4), then five morale states selected by record byte
; +6.
CrewInjuryText:
  DEFM "Dead ",$FF
  DEFM "Collapsed ",$FF
  DEFM "Wounded ",$FF
  DEFM "OK ",$FF
CrewMoraleText:
  DEFM "Broken",$FF
  DEFM "Shaken",$FF
  DEFM "Uneasy",$FF
  DEFM "Stable",$FF
  DEFM "Confident",$FF

; Action-menu strip template
;
; 19-byte skeleton of the room-view action menu, copied to the strip at
; CorridorPosTable by ResetActionMenu whenever the room view is rebuilt. Cell
; values are string indexes into RoomNameTable; 255 = empty slot. Layout: cell
; 0 = header 86 "move to:" (destinations fill cells 1-6: exits from
; BuildExitList (BuildExitList) at 2-6, the open duct "Grille" at 1); cell 7 =
; header 87 " use:" (items from BuildItemMenu (BuildItemMenu) fill 8-11); cell
; 12 = header 88 "Special" (room actions fill 13-16, e.g. "RmveGrille"/"
; ATTACK  " at 13); cell 17 = 61 "   QUIT   ".
ActionMenuTemplate:
  DEFB $56,$FF,$FF,$FF,$FF,$FF,$FF ; "move to:" + 6 destination slots
  DEFB $57,$FF,$FF,$FF,$FF ; " use:" + 4 item slots
  DEFB $58,$FF,$FF,$FF,$FF ; "Special" + 4 action slots
  DEFB $3D,$FF            ; "QUIT"

; Orders-menu template (the mode-0 menu strip)
;
; 19-byte template copied verbatim to CorridorPosTable (CorridorPosTable) by
; InitGameView (InitGameView) — the cell layout of the orders screen (room mode
; 0), drawn on screen rows 1-19 under the " CONTROL  " title: cell 0     83
; "Order:    "  header cells 1-7  52  crew rows — InitGameView draws the seven
; crew NAMES here (via PrintName PrintName), so the 52s are never rendered;
; they only give the rows a nonzero dispatch value cells 8-9  79,80  " Indicate
; "/" Location " (2-row label; the action key on row 8 opens the room-location
; list, IndicateLocation IndicateLocation) cells 10-11 84,85 " Display  "/"
; Level   " header (2-row label) cells 12-14 44,45,46 "Upper
; Deck"/"MiddleDeck"/"Lower Deck" (SelectDeckMap SelectDeckMap redraws the map
; pane) cells 15-18 255 blank The lower half (from cell 8) is also drawn
; directly from this template by the tail of OrdersMenuNextRow
; (OrdersMenuNextRow), pointer set to OrdersMenuLowerHalf. The next 504 bytes
; starting at CrewPortraits are the crew portrait bitmaps; see CrewPortraits.
OrdersMenuTemplate:
  DEFB $53,$34,$34,$34,$34,$34,$34,$34
OrdersMenuLowerHalf:
  DEFB $4F,$50,$54,$55,$2C,$2D,$2E,$FF
  DEFB $FF,$FF,$FF

; Crew portrait bitmaps
;
; Seven 3x3-character (24x24 pixel) raw bitmap portraits, drawn by
; ArmStraight_19. Used by DrawShipMap (DrawShipMap -- all 7 at once) and by the
; crew-status display (single-portrait draw, crew index from DrawSlotIndex, via
; DrawSprite). Crew member names from CrewNameTable (CrewNameTable); portrait N
; is at address CrewPortraits + (N-1)*72. Portrait data continues into
; CrewPortraitsCont and CrewPortraitsCont2 due to block boundaries.
;
; DALLAS (1):
; KANE (2):
; RIPLEY (3):
; ASH (4):
; LAMBERT (5):
; PARKER (6):
; BRETT (7):
CrewPortraits:
  DEFB $00,$00,$00,$01,$01
  DEFB $03,$03,$03,$7D,$FF,$FF,$DF,$BF
  DEFB $99,$00,$6B,$80,$C0,$C0,$E0,$E0
  DEFB $E0,$60,$70,$03,$03,$03,$03,$01
  DEFB $01,$01,$03,$08,$08,$14,$18,$2E
  DEFB $22,$FF,$7F,$70,$70,$30,$70,$60
  DEFB $E0,$C0,$40,$04,$04,$18,$E6,$01
  DEFB $00,$00,$00,$BE,$00,$80,$41,$12
  DEFB $00,$00,$00,$60,$90,$8C,$33,$C0
  DEFB $00,$00,$00,$00,$00,$01,$03,$03
  DEFB $03,$03,$03,$1E,$FF,$FF,$FF,$9F
  DEFB $03,$00,$73,$00,$00,$80,$C0,$E0
  DEFB $E0,$E0,$60,$03,$02,$02,$02,$01
  DEFB $00,$00,$03,$37,$44,$04,$0C,$00
  DEFB $9D,$81,$43,$60,$E0,$20,$60,$C0
  DEFB $80,$80,$60,$0C,$10,$E0,$1C,$02
  DEFB $00,$00,$00,$BF,$81,$C3,$FF,$7F
  DEFB $00,$00,$00,$18,$84,$8B,$03,$40
  DEFB $00,$00,$00,$00,$03,$07,$07,$0F
  DEFB $0F,$0F,$0F,$FF,$FF,$FF,$FF,$3B
  DEFB $00,$00,$73,$C0,$E0,$F0,$F0,$F8
  DEFB $78,$38,$B8,$0F,$0F,$0F,$0F,$0F
  DEFB $0F,$0F,$07,$33,$02,$02,$06,$80
  DEFB $8E,$80,$C0,$38,$38,$38,$78,$F8
  DEFB $78,$F8,$F8,$07,$07,$07,$03,$06
  DEFB $00,$00,$00,$E1,$DE,$C0,$E0,$33
  DEFB $00,$00,$00,$F8,$F8,$F8,$F0,$18
  DEFB $00,$00,$00,$00,$00,$00,$01,$03
  DEFB $03,$03,$02,$00,$7F,$FF,$8F,$C0
  DEFB $C0,$C0,$99,$00,$80,$C0,$E0,$20
  DEFB $30,$10,$90,$02,$02,$03,$01,$00
  DEFB $00,$00,$07,$02,$82,$46,$00,$8E
  DEFB $C0,$A1,$9E,$30,$30,$20,$60,$40
  DEFB $C0,$80,$E0,$08,$F0,$10,$13,$1C
  DEFB $00,$00,$00,$84,$49,$21,$D2

; CrewPortraitsCont
;
; Crew portrait bitmaps, continued (portraits 4 end, 5, 6 start).
CrewPortraitsCont:
  DEFB $0C,$00,$00,$00,$90,$0F,$04,$FC
  DEFB $00,$00,$00,$00,$00,$01,$03,$07
  DEFB $07,$07,$07,$07,$7F,$FF,$FF,$E7
  DEFB $C0,$80,$30,$09,$00,$C0,$E0,$F0
  DEFB $F0,$70,$B0,$30,$07,$07,$03,$03
  DEFB $03,$01,$01,$00,$32,$02,$02,$41
  DEFB $81,$80,$C7,$C0,$F0,$30,$30,$30
  DEFB $60,$60,$60,$C0,$00,$00,$01,$0E
  DEFB $00,$00,$00,$00,$E0,$58,$C7,$63
  DEFB $3F,$00,$00,$00,$40,$80,$C0,$F0
  DEFB $80,$00,$00,$00,$00,$00,$00,$01
  DEFB $03,$02,$00,$03,$3E,$7F,$FF,$FF
  DEFB $80,$00,$7F,$FF,$00,$80,$C0,$E0
  DEFB $20,$00,$80,$E0,$03,$03,$03,$01
  DEFB $01,$00,$00,$03,$CE,$FB,$FD,$F5
  DEFB $FB,$F1,$7F,$BF,$60,$E0,$C0,$C0
  DEFB $C0,$80,$80,$70,$7E,$82

; CrewPortraitsCont2
;
; Crew portrait bitmaps, continued (portrait 6 end, portrait 7).
CrewPortraitsCont2:
  DEFB $02,$01,$01,$00,$00,$00,$DF,$60
  DEFB $7F,$7F,$3E,$00,$00,$00,$4F,$90
  DEFB $90,$20,$40,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$01,$00,$3F,$77
  DEFB $6B,$63,$FF,$FF,$C0,$BB,$80,$C0
  DEFB $C0,$C0,$C0,$C0,$60,$60,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$88,$88
  DEFB $8C,$40,$40,$4C,$20,$21,$60,$50
  DEFB $10,$20,$40,$C0,$C0,$40,$00,$0F
  DEFB $3E,$7E,$FF,$00,$00,$00,$FE,$20
  DEFB $A0,$A1,$52,$00,$00,$00,$78,$57
  DEFB $AF,$5F,$9F,$00,$00,$00

; Room tile-index maps
;
; Tile-index sequences defining the visual appearance of each room type. Loaded
; into HL within DrawSprite (DrawSprite) and used via RoomLayoutExpand.
CorridorLayout:
  DEFB $14,$02
  DEFB $92,$93,$0C,$94,$95,$02,$92,$93
  DEFB $92,$93,$0C,$94,$95,$94,$95,$92
  DEFB $93,$10,$94,$95,$14,$14,$02,$92
  DEFB $93,$0C,$94,$95,$02,$92,$93,$92
  DEFB $93,$96,$0A,$97,$94,$95,$94,$95
  DEFB $92,$93,$02,$96,$A0,$03,$7D,$7D
  DEFB $03,$A0,$97,$02,$94,$95,$04,$96
  DEFB $A0,$03,$7D,$7D,$03,$A0,$97,$04
  DEFB $04,$96,$0A,$97,$04,$04,$96,$02
  DEFB $9E,$9E,$9E,$9E,$9E,$9E,$02,$97
  DEFB $04,$02,$92,$93,$01,$9A,$9F,$9F
  DEFB $9F,$9F,$9F,$9F,$9F,$9F,$99,$01
  DEFB $94,$95,$02,$92,$93,$03,$9C,$98
  DEFB $9D,$9D,$9D,$9D,$9D,$9D,$98,$9B
  DEFB $03,$94,$95,$14,$04,$A5,$04,$A5
  DEFB $01,$A1,$04,$A1,$03,$08,$A7,$A6
  DEFB $01,$A2,$A3,$07,$08,$A8,$03,$A4
  DEFB $07,$14,$FF,$09,$09,$09,$09,$09
  DEFB $09,$09,$09,$09,$09,$09,$09,$09
  DEFB $09,$09,$09,$09,$09,$09,$09,$09
  DEFB $09,$0A,$11,$12,$12,$12,$12,$12
  DEFB $12,$12,$12,$12,$12,$12,$12,$11
  DEFB $0A,$09,$09,$0A,$11,$10,$02,$12
  DEFB $47,$47,$47,$47,$47,$47,$47,$47
  DEFB $47,$47,$12,$02,$10,$11,$0A,$10
  DEFB $02,$07,$07,$12,$47,$47,$47,$47
  DEFB $47,$47,$47,$47,$47,$47,$12,$07
  DEFB $07,$02,$10,$07,$07,$07,$07,$12
  DEFB $47,$47,$47,$47,$47,$47,$47,$47
  DEFB $47,$47,$12,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$12,$47,$47,$47,$47
  DEFB $47,$47,$47,$47,$47,$47,$12,$07
  DEFB $07,$07,$07,$07,$07,$02,$10,$12
  DEFB $12,$12,$12,$12,$12,$12,$12,$12
  DEFB $12,$12,$12,$10,$02,$07,$07,$02
  DEFB $10,$11,$0A,$08,$09,$09,$09,$09
  DEFB $09,$09,$09,$09,$09,$09,$08,$0A
  DEFB $11,$10,$02,$11,$0A,$09,$09,$08
  DEFB $0C,$24,$24,$09,$07,$07,$09,$24
  DEFB $24,$21,$08,$09,$09,$0A,$11,$09
  DEFB $09,$09,$09,$08,$0C,$24,$24,$09
  DEFB $07,$07,$09,$24,$24,$21,$08,$09
  DEFB $09,$09,$09,$09,$09,$09,$09,$08
  DEFB $09,$09,$09,$09,$09,$09,$09,$09
  DEFB $09,$09,$08,$09,$09,$09,$09,$09
  DEFB $09,$09,$09,$08,$09,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$09,$08,$09
  DEFB $09,$09,$09,$09,$09,$0D,$29,$28
  DEFB $28,$06,$06,$06,$06,$06,$06,$06
  DEFB $06,$28,$28,$29,$0D,$09,$09,$0D
  DEFB $29,$2D,$2D,$2D,$05,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$05,$2D,$2D
  DEFB $2D,$29,$0D,$2D,$2D,$2D,$2D,$2D
  DEFB $2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D
  DEFB $2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D
  DEFB $2D,$2D,$2D,$2F,$38,$38,$38,$38
  DEFB $39,$2D,$39,$38,$38,$38,$38,$2F
  DEFB $2D,$2D,$2D,$2D,$2D,$2D,$09,$09
  DEFB $09,$09,$09,$39,$0F,$2D,$0F,$39
  DEFB $09,$09,$09,$09,$09,$2D,$2D,$2D
  DEFB $2D,$2D,$09,$09,$09,$09,$09,$0F
  DEFB $09,$2D,$09,$0F,$09,$09,$09,$09
  DEFB $09,$2D,$2D,$2D,$2D,$2D,$09,$09
  DEFB $09,$09,$09,$09,$09,$2D

; Per-screen RAM state
;
; First 35 bytes of the runtime RAM map. The remaining per-game-state
; scratchpad variables live further down in the code segment near AlienFlags —
; see that block for the rest of the RAM map.
;
; Variables:
;           #R31232 GameStateBase    9-byte status block (crew alive flags?)
;           #R31241 DrawSlotIndex    actor slot (0-7) currently drawn/processed
;           #R31242 CurrentRoom      room of that actor; "the viewed room"
;           #R31243 RoomTypeByte     current room type id
;           #R31244 RoomAttrStep     word: attr-cursor stride for door drawing
;           #R31246 RoomCellStep     word: cell-grid stride for door drawing
;           #R31248 HeldItemFront    item id in the actor's front hand (255 =
;                   none)
;           #R31249 HeldItemBack     item id in the actor's back hand (255 =
;                   none)
;           #R31250 RoomMateA        first other actor in the room (255 = none)
;           #R31251 RoomMateB        second other actor in the room (255 =
;                   none)
;           #R31252 RoomModeByte     current room mode (drives
;                   RoomDispatchTable)
;           #R31253 ShipSystemFlags  bit 0 destruct armed; bits 3-5 engine
;                   state
;           #R31255 RoomAttrCursor   word: attribute-file cursor (room drawing)
;           #R31257 DrawScreenPtr    word: THE display-file draw pointer (~120
;                   refs)
;           #R31259 RoomCellCursor   word: 20-wide room-cell-grid cursor
;           #R31261 CorridorTilePtr  pointer into corridor tile source
;           #R31263 CrewAnimFlags    XOR-blit toggle for crew sprite set A
;           #R31264 AnimFramePhase   XOR-blit toggle for set B / cursor
;           #R31265 ScriptPointer    ROM-traversal AI script pointer
;
; The bytes from DrawRoomBackground onwards in the original tape image hold
; executable code (the corridor-drawing routine called from InitGameView_0).
; That code is preserved here as DEFB data only because the section was
; originally classified as RAM; it is invoked via CALL DrawRoomBackground.
GameStateBase:
  DEFB $09,$09,$09,$09,$09,$09,$09,$2D
  DEFB $2D
DrawSlotIndex:
  DEFB $05
CurrentRoom:
  DEFB $06                ; room of the actor being processed (record byte +1,
                          ; cached by SelectSlotByDrawIdx SelectSlotByDrawIdx);
                          ; doubles as "the viewed room" in encounter checks
RoomTypeByte:
  DEFB $01
RoomAttrStep:
  DEFB $00,$00            ; word: attribute-cursor stride for door drawing
RoomCellStep:
  DEFB $00,$00            ; word: room-cell-grid stride for door drawing
HeldItemFront:
  DEFB $FF                ; corridor-strip cell (0-21) of the actor's id+160
                          ; marker; 12 = the alien's lair cell (see
                          ; FindHeldItems)
HeldItemBack:
  DEFB $FF                ; corridor-strip cell of the actor's id+128 marker
RoomMateA:
  DEFB $FF                ; slots of up to two OTHER actors in the current
RoomMateB:
  DEFB $FF                ; room (255 = none) - printed as "Also here" / "Also
                          ; Bodies of"; found by RecalcMorale
RoomModeByte:
  DEFB $00
ShipSystemFlags:
  DEFB $00                ; bit 0 = self-destruct armed; bits 3-5 = Engine
                          ; #1/#2/#3 state (ship-systems tick at
                          ; EngineStateScan)
  DEFB $00
RoomAttrCursor:
  DEFB $00,$58            ; word: attribute-file cursor for room/door drawing
                          ; (initially the attr-file base)
DrawScreenPtr:
  DEFB $00,$40            ; word: THE main display-file draw pointer used by
                          ; every blit routine (initially the display file)
RoomCellCursor:
  DEFB $00,$60            ; word: cursor into the 20-cell-wide room grid,
                          ; walked in step with RoomAttrCursor
CorridorTilePtr:
  DEFB $00,$00
CrewAnimFlags:
  DEFB $80
AnimFramePhase:
  DEFB $80
ScriptPointer:
  DEFB $00,$00
DrawRoomBackground:
  DEFB $11,$0C,$00,$21,$00,$58,$0E,$13
  DEFB $06,$14,$36,$04,$23,$10,$FB,$19
  DEFB $0D,$20,$F5,$11,$7C,$01,$21,$00
  DEFB $60,$3A,$0B,$7A,$FE,$03,$CA,$6C
  DEFB $83,$FE,$04,$CA,$07,$9B,$A7,$28
  DEFB $04,$19,$3D,$20,$FC
DrawRoomBackground_0:
  DEFB $22,$1B,$7A,$21,$00,$40,$22,$19
  DEFB $7A,$0E,$13,$06,$14,$C5,$ED,$5B
  DEFB $1B,$7A,$CD,$89,$7A,$21,$19,$7A
  DEFB $34,$2A,$1B,$7A,$23,$22,$1B,$7A
  DEFB $C1,$10,$EA,$11,$0C,$00,$2A,$19
  DEFB $7A,$7D,$FE,$E0,$38,$03,$11,$0C
  DEFB $07,$19,$22,$19,$7A,$0D,$20,$D3
  DEFB $C9

; DrawSpriteRow
;
; Draws one 8-pixel-tall column of a sprite tile onto the screen. Reads a tile
; index byte from (DE), multiplies by 8 to get an offset into the
; 8-byte-per-row character bitmap table at $6935 ($CharBitmaps + index×8), then
; writes all 8 bytes vertically downward from the current draw pointer at
; DrawSlotIndex (INC H steps through ZX Spectrum display rows within the same
; character cell column).
;
;  Input: (DE) = tile/character index; (DrawScreenPtr) = screen destination
;         address
; Output: screen updated; DE advanced; H incremented 8 times
;
; Used by the routines at DrawSprite, UpdateCrewAI, UpdateRoomActors,
; DrawIntroScreen and DrawStatusPanel.
DrawSpriteRow:
  LD A,(DE)               ; read tile index from source pointer
; This entry point is used by the routines at DrawSprite, UpdateCrewAI and
; UpdateRoomActors.
DrawSpriteRow_0:
  LD L,A                  ; L = tile index
  LD H,$00
  ADD HL,HL               ; HL = index × 2
  ADD HL,HL               ; HL = index × 4
  ADD HL,HL               ; HL = index × 8
  LD BC,CharBitmaps       ; BC = $6935 (character bitmap base)
  ADD HL,BC               ; HL = address of first row of tile bitmap
  EX DE,HL                ; DE = bitmap source; HL = old DE (tile index ptr)
  LD HL,(DrawScreenPtr)   ; HL = screen destination (DrawScreenPtr)
  LD B,$08                ; 8 pixel rows per character
DrawSpriteRow_1:
  LD A,(DE)               ; read bitmap byte for this pixel row
  LD (HL),A               ; write to display file
  INC H                   ; next display row (INC H steps within 8-row band)
  INC DE                  ; next bitmap byte
  DJNZ DrawSpriteRow_1    ; repeat for all 8 rows
  RET
; This entry point is used by the routine at AnimateCrewB.
DrawSpriteRow_2:
  LD A,H
  AND $07
  CP $07
  JR Z,DrawSpriteRow_3
  INC H
  RET
DrawSpriteRow_3:
  LD A,L
  CP $E0
  JR NC,DrawSpriteRow_4
  LD DE,$F920
  ADD HL,DE
  RET
DrawSpriteRow_4:
  LD DE,$0020
  ADD HL,DE
  RET

; InitGameView
;
; Initialises the corridor view for the current room. Clears the room mode
; byte, freezes both crew animation channels (sets bit 7 of
; CrewAnimFlags/AnimFramePhase), resets the corridor cursor to segment 8,
; copies the 19-byte corridor position template from OrdersMenuTemplate to
; CorridorPosTable, then draws the room's attribute colours and initial crew
; sprites.
;
; USED by the routines at OpenCrewRoomView, LocationListAction, MoveToRoom,
;      GameEntry, XorHAddE, UpdateRoomActors,
; R40826 AND R45401.
InitGameView:
  XOR A                   ; A = 0
  LD (RoomModeByte),A     ; clear room mode byte at RoomModeByte
  LD HL,CrewAnimFlags     ; HL = CrewAnimFlags (crew animation flags)
  SET 7,(HL)              ; freeze animation channel A (bit 7 = freeze)
  INC HL
  SET 7,(HL)              ; freeze animation channel B
  LD A,$80                ; $80
  LD (AlienXPhase),A      ; set alien pixel X sentinel (AlienXPhase)
  LD (JonesPhase),A       ; set alien pixel Y sentinel (JonesPhase)
  LD A,$08                ; segment 8 = centre of 19-segment corridor
  LD (CorridorCursor),A   ; store as current corridor cursor (CorridorCursor)
  LD A,$20                ; space char
  CALL ArmStraight_12     ; draw blank room border / clear game window
  LD A,(RoomTypeByte)     ; read room-type byte (RoomTypeByte)
  CP $04                  ; room type 4 = special (engine room)
  JR NZ,InitGameView_0
  LD A,$01                ; remap type 4 → type 1
  LD (RoomTypeByte),A
InitGameView_0:
  CALL DrawRoomBackground ; draw room background (walls, doors, floor tiles)
  LD DE,CorridorPosTable  ; DE = CorridorPosTable (corridor position table, 19
                          ; bytes)
  LD HL,OrdersMenuTemplate ; HL = OrdersMenuTemplate (corridor position
                           ; templates)
  LD BC,$0013             ; copy 19 entries
  LDIR                    ; initialise position table from template
  LD HL,$5816             ; HL = $5816 (attribute row for top of game window)
  LD A,$07                ; white ink on black paper
  LD C,$01                ; 1 column wide
  CALL FillAttrColumn     ; paint attribute column
  LD A,$28
  LD C,$08
  CALL FillAttrColumn
  LD A,$20
  LD C,$02
  CALL FillAttrColumn
  LD A,$17
  LD C,$05
  CALL FillAttrColumn
  XOR A
  LD C,$03
  CALL FillAttrColumn
  LD C,$01
  LD A,$07
  CALL FillAttrColumn
  LD HL,$4016
  LD (DrawScreenPtr),HL
  LD A,$52
  CALL DrawSprite
  LD HL,OrdersMenuTemplate
  LD (CorridorTilePtr),HL
  CALL DrawNextCorridorTile
  LD B,$07
InitGameView_1:
  PUSH BC
  LD A,$5C
  CALL DrawSprite
  POP BC
  DJNZ InitGameView_1
  LD HL,$4056
  LD (DrawScreenPtr),HL
  LD A,$01
  EX AF,AF'
  LD A,$07
; This entry point is used by the routine at OrdersMenuNextRow. Orders-screen
; crew-name loop head: A' holds the next crew id, A the remaining row count (7
; names on menu rows 2-8).
OrdersCrewNameLoop:
  EX AF,AF'               ; A = crew id to print
  DEFB $2A,$19            ; first 2 bytes of "LD HL,(draw pointer)": the
                          ; operand-high byte is the LD A,D at
                          ; OrdersCrewNameRow, so falling through loads HL and
                          ; continues after it

; Orders-screen crew-name row
;
; Body of the loop above (never entered at OrdersCrewNameRow itself — the LD
; A,D opcode is just the operand high byte of the overlapping instruction):
; prints crew name A via PrintName (PrintName), then steps the menu draw
; pointer (DrawScreenPtr) one character row down: +32 within a display-file
; third, or +1824 (via the fall-through block below) when crossing a third
; boundary (L >= $E0).
OrdersCrewNameRow:
  LD A,D                  ; (dead when fallen into: overlap operand)
  PUSH AF                 ; save crew id
  PUSH HL                 ; save draw pointer
  CALL PrintName          ; PrintName: draw crew name A on this row
  POP HL
  LD DE,$0020             ; normal stride: next char row in same third
  LD A,L
  CP $E0                  ; crossing into the next display third?
  JR C,OrdersMenuAdvance  ; no: advance by 32

; Orders-menu build, lower part
;
; Tail of the orders-screen build started in InitGameView (InitGameView). First
; the third-boundary case of the crew-name row stride (+1824), then the shared
; loop tail: store the draw pointer, advance A' to the next crew id and loop
; until all 7 names are printed. After the loop, draws menu rows 9-18 straight
; from OrdersMenuLowerHalf (OrdersMenuLowerHalf) (" Indicate "/" Location ", "
; Display  "/"  Level   ", the three deck rows, blanks), re-highlights the
; cursor row and mirrors its cell value (HighlightMenuRow), and redraws the
; oxygen clock on row 19 (OxygenCheckRedraw).
OrdersMenuNextRow:
  LD DE,$0720             ; big stride: next char row across a third boundary
; This entry point is used by the routine at OrdersCrewNameRow.
OrdersMenuAdvance:
  ADD HL,DE
  LD (DrawScreenPtr),HL   ; store draw pointer for the next row
  POP AF                  ; A = crew id just printed
  INC A
  EX AF,AF'               ; A' = next crew id, A = row counter
  DEC A
  JR NZ,OrdersCrewNameLoop ; loop for all 7 crew rows
  LD HL,OrdersMenuLowerHalf ; then draw rows 9-18 from OrdersMenuLowerHalf
  LD (CorridorTilePtr),HL
  LD B,$0A
OrdersMenuLowerLoop:
  PUSH BC
  CALL DrawNextCorridorTile ; draw next menu row from the template
  POP BC
  DJNZ OrdersMenuLowerLoop
  CALL HighlightMenuRow   ; highlight the cursor row / mirror its value
  CALL OxygenCheckRedraw  ; redraw the oxygen clock (row 19)
  RET
; This entry point is used by the routines at InitGameView, DrawSprite,
; HandleInput, IndicateLocation and GetItemPick. FillAttrColumn: paint a
; 10-cell-wide attribute block — C rows of attribute A starting at HL (stride
; 22 = 32-10 to the next row). Used to colour the menu rows.
FillAttrColumn:
  LD DE,$0016             ; row stride remainder (32 - 10)
OrdersMenuNextRow_0:
  LD B,$0A                ; 10 attribute cells per row
OrdersMenuNextRow_1:
  LD (HL),A
  INC HL
  DJNZ OrdersMenuNextRow_1
  ADD HL,DE               ; next row
  DEC C
  JR NZ,OrdersMenuNextRow_0
  RET

; DrawNextCorridorTile
;
; Reads the next tile index from the corridor position pointer at
; CorridorTilePtr, draws it by falling through into DrawSprite (DrawSprite),
; and advances the pointer.
;
; USED by the routines at InitGameView, OrdersMenuNextRow, DrawSprite,
;      DrawLocationList and GetItemPick.
DrawNextCorridorTile:
  LD HL,(CorridorTilePtr) ; HL = corridor tile-source pointer (CorridorTilePtr)
  LD A,(HL)               ; A = next tile index
  INC HL                  ; advance pointer
  LD (CorridorTilePtr),HL ; save updated pointer; fall through to DrawSprite

; DrawSprite
;
; Renders a 10-character text label at the current draw position
; (DrawScreenPtr). The "sprite" data is the 10-byte text entry at RoomNameTable
; + A×10 (see RoomNameTable / UIStringTable); each byte is used as a tile index
; into CharBitmaps, which is laid out so that index N == ASCII N produces the
; glyph for that character. After drawing 10 columns, advances DrawScreenPtr to
; the next character row accounting for the ZX Spectrum display-file row
; wrapping.
;
; Input: A = text/label index (255 → use blank label, entry 92)
;       DrawScreenPtr = screen destination address
;
; Used by the routines at InitGameView, DrawCrewStatusHalf, DrawLocationList,
; SwapHeldItems, GetItemPick, GameEntry, UpdateJones, UpdateCrewAI, XorHAddE,
; UpdateRoomActors, UpdateAlien, PlayMusic and UpdateAllCrew.
DrawSprite:
  CP $FF                  ; sprite index 255 = invalid/absent
  JR NZ,DrawSprite_0      ; valid index: draw it
  LD A,$5C                ; substitute blank sprite (index 92)
DrawSprite_0:
  LD L,A                  ; L = sprite index
  LD H,$00
  ADD HL,HL               ; HL = index × 2
  PUSH HL                 ; save HL = index × 2
  ADD HL,HL               ; HL = index × 4
  ADD HL,HL               ; HL = index × 8
  POP DE                  ; DE = index × 2
  ADD HL,DE               ; HL = index × 10 (label record size)
  LD DE,RoomNameTable     ; DE = RoomNameTable (RoomNameTable base)
  ADD HL,DE               ; HL = address of the 10-byte text entry
  EX DE,HL                ; DE = source pointer (10 ASCII tile-indices)
  LD B,$0A                ; 10 character columns per label
DrawSprite_1:
  PUSH BC                 ; save column counter
  PUSH DE                 ; save sprite data pointer
  CALL DrawSpriteRow      ; DrawSpriteRow: draw one tile column (8px tall)
  POP DE                  ; restore sprite data pointer
  INC DE                  ; advance to next tile index byte
  LD HL,DrawScreenPtr     ; HL → draw pointer (DrawSlotIndex)
  INC (HL)                ; move draw pointer right by 1 character column
  POP BC
  DJNZ DrawSprite_1       ; repeat for all 10 columns
  LD HL,(DrawScreenPtr)   ; HL = draw pointer after last column
  DEC L                   ; back to start of this character row
  LD DE,$0017             ; advance 23 columns to next row (normal case)
  LD A,L
  CP $E0                  ; $E0: check for display-file third-bank boundary
  JR C,DrawSprite_2       ; below $E0: normal row advance (+23)
  LD DE,$0717             ; at/above $E0: large offset to wrap to next band
DrawSprite_2:
  ADD HL,DE               ; move draw pointer to start of next character row
  LD (DrawScreenPtr),HL   ; save updated draw pointer
  RET

; PrintName
;
; Prints actor name A (0 = ALIEN, 1-7 = crew) by skipping A 255-terminated
; names in CrewNameTable and drawing the next one one glyph at a time. The
; second entry point three bytes in is also entered directly with DE pre-set to
; walk other string lists.
;
; USED by the routines at OrdersCrewNameRow, UpdateCrewAI, DrawShipMap,
;      DrawStatusPanel and
; #R45401.
PrintName:
  LD DE,CrewNameTable     ; CrewNameTable
; This entry point is used by the routine at DrawShipMap.
PrintName_0:
  AND A                   ; name 0: draw from the list head
  JR Z,PrintName_2
  LD B,A                  ; skip A 255-terminated names
PrintName_1:
  LD A,(DE)
  INC DE
  CP $FF
  JR NZ,PrintName_1
  DJNZ PrintName_1
PrintName_2:
  PUSH DE                 ; draw one glyph via DrawSpriteRow
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr     ; draw pointer right one column
  INC (HL)
  LD A,(DE)               ; until the 255 terminator
  CP $FF
  JR NZ,PrintName_2
  RET

; SelectSlotByDrawIdx
;
; Re-points IX at the 8-byte record indexed by (DrawSlotIndex) and loads the
; slot's identity into (CurrentRoom) for downstream lookups. Critically, if the
; slot wears the HostMarker (byte+1 == $FF), the routine returns immediately
; without writing CurrentRoom / RoomTypeByte — every per-frame caller that
; walks into this entry point therefore skips that slot during routine corridor
; processing. In the Long Game this means the chestburster host is excluded
; from normal crew handling; in the Short Game the same exclusion applies to
; all four pre-stamped slots.
;
; THIS entry point is used by the routine at OpenCrewRoomView.
SelectSlotByDrawIdx:
  LD A,(DrawSlotIndex)    ; A = current draw index (slot 0..7)
  ADD A,A
  ADD A,A
  ADD A,A                 ; A *= 8 (record stride)
  LD E,A
  LD D,$00
  LD IX,ActorRecords      ; IX = $7376 (slot table base)
  ADD IX,DE               ; IX = &slot[index]
  LD A,(IX+$01)           ; A = slot.byte+1
SelectSlot_HostCheck:
  CP $FF                  ; HostMarker?
  RET Z                   ; yes → bail out (slot is not a normal crew member)
  LD (CurrentRoom),A      ; cache slot identity for the rest of the room
                          ; handler
  AND $3F                 ; mask off direction bit → raw crew ID (0..63)
  LD E,A
  LD D,$00
  LD HL,RoomTypeTable     ; RoomTypeTable[crew's room]
  ADD HL,DE
  LD A,(HL)
  LD (RoomTypeByte),A     ; RoomTypeByte for this crew
  RET

; BuildExitList
;
; Builds the current room's door-exit list (CorridorPosTable + 2): walks the
; room's 5 direction slots in RoomAdjCorridors and stores each destination that
; differs from the current room (up to 5 entries). The room view draws one door
; per entry.
BuildExitList:
  LD A,(CurrentRoom)      ; A = current room
  LD L,A
  LD H,$00
  PUSH HL
  ADD HL,HL
  ADD HL,HL
  POP DE
  ADD HL,DE
  LD DE,RoomAdjCorridors
  ADD HL,DE
  LD DE,$73C0
  LD B,$05
BuildExitList_0:
  LD C,(HL)
  CP C
  JR Z,BuildExitList_1
  LD A,(CurrentRoom)
  CP C
  JR Z,BuildExitList_1
  LD A,C
  LD (DE),A
  INC DE
BuildExitList_1:
  INC HL
  DJNZ BuildExitList_0
  RET
; BuildRoomItemList: list the item ids lying in the current room — scan
; ItemLocations (ItemLocations) from the top down for entries equal to the room
; number, appending each match to RoomItemList (RoomItemList).
BuildRoomItemList:
  LD A,(CurrentRoom)      ; C = current room
  LD C,A
  LD HL,$7509             ; scan ItemLocations from item 21 down
  LD DE,RoomItemList      ; DE -> RoomItemList write pointer
  LD B,$16
BuildExitList_2:
  LD A,(HL)               ; item's location
  CP C                    ; lying in this room?
  JR NZ,BuildExitList_3
  LD A,B                  ; yes: append its id (= B-1) to the list
  DEC A
  LD (DE),A
  INC DE
BuildExitList_3:
  DEC HL
  DJNZ BuildExitList_2
  RET
; This entry point is used by the routines at SwapHeldItems, GetItemPick and
; PlayMusic. RefreshHeldItems: find what the current actor (DrawSlotIndex)
; holds.
RefreshHeldItems:
  LD A,(DrawSlotIndex)

; FindHeldItems
;
; Finds what actor A is carrying: scans ItemLocations (ItemLocations) for the
; slot+160 marker (front hand) into HeldItemFront (HeldItemFront) and the
; slot+128 marker (back hand) into HeldItemBack (HeldItemBack). The item id is
; the table position where the marker sits.
;
; USED by the routines at XorHAddE and UpdateAlien.
FindHeldItems:
  ADD A,$80               ; C = slot + 128, E = slot + 160
  LD C,A
  ADD A,$20
  LD E,A
  LD HL,$7509             ; scan ItemLocations from item 21 down
  LD B,$16
FindHeldItems_0:
  LD A,(HL)
  CP C                    ; back-hand marker?
  JR NZ,FindHeldItems_1
  LD A,B                  ; yes: HeldItemBack = item id (B-1)
  DEC A
  LD (HeldItemBack),A
  JR FindHeldItems_2
FindHeldItems_1:
  CP E                    ; front-hand marker?
  JR NZ,FindHeldItems_2
  LD A,B                  ; yes: HeldItemFront = item id
  DEC A
  LD (HeldItemFront),A
FindHeldItems_2:
  DEC HL
  DJNZ FindHeldItems_0
  RET
; This entry point is used by the routines at SwapHeldItems, GetItemPick and
; XorHAddE.

; ItemIdToString
;
; Maps an item id (in A) to its name's string index: ids 0-2 -> 35 Elctrc Prd,
; 3-5 -> 36 Incineratr, 6-7 -> 37 Tracker, 8-11 -> 38 Fire Extng, 12 -> 39
; Harpn Gun, 13-15 -> 40 Laser Pist, 16 -> 41 Net, 17 -> 42 Cat Box, 18-19 ->
; 43 Spanner; ids 20/21 (Jones captured) -> strings 68 "Cat in Net" / 69 "Cat
; in Box".
ItemIdToString:
  LD E,$23                ; E = first item string ("Elctrc Prd")
  CP $03
  JR C,ItemIdToString_0
  INC E
  CP $06
  JR C,ItemIdToString_0
  INC E
  CP $08
  JR C,ItemIdToString_0
  INC E
  CP $0C
  JR C,ItemIdToString_0
  INC E
  CP $0C
  JR Z,ItemIdToString_0
  INC E
  CP $10
  JR C,ItemIdToString_0
  INC E
  CP $10
  JR Z,ItemIdToString_0
  INC E
  CP $11
  JR Z,ItemIdToString_0
  INC E
ItemIdToString_0:
  CP $14
  JR NZ,ItemIdToString_1
  LD E,$44
  JR ItemIdToString_2
ItemIdToString_1:
  CP $15
  JR NZ,ItemIdToString_2
  LD E,$45
ItemIdToString_2:
  LD A,E
  RET

; BuildItemMenu
;
; Fills the action menu's " use:" rows (strip cells 8-11) for the current
; actor: row 8 = name of the front-hand item, row 9 = name of the back-hand
; item, row 10 = "Get Item" (47) when the room has items lying about and the
; back hand is free, row 11 = "Leave Item" (48) when the front hand is loaded.
BuildItemMenu:
  LD C,$FF                ; C = "empty" sentinel
  LD HL,MenuUseRows       ; menu strip cell 8
  LD A,(HeldItemFront)    ; front-hand item
  CP C
  CALL NZ,ItemIdToString  ; -> item name string
  LD (HL),A               ; row 8
  INC HL
  LD A,(HeldItemBack)     ; back-hand item
  CP C
  CALL NZ,ItemIdToString
  LD (HL),A               ; row 9
  INC HL
  LD DE,RoomItemList      ; any items lying in this room?
  LD A,(DE)
  CP C
  JR Z,BuildItemMenu_0
  LD A,(HeldItemBack)     ; and the back hand free?
  CP C
  JR NZ,BuildItemMenu_0
  LD A,$2F                ; yes: row 10 = "Get Item"
  LD (HL),A
BuildItemMenu_0:
  INC HL
  LD A,(HeldItemFront)    ; front hand loaded?
  CP C
  RET Z
  LD A,$30                ; yes: row 11 = "Leave Item"
  LD (HL),A
  RET

; DrawRoomSpecials
;
; Draws the special actors into the room view when the viewed room hosts them:
; the activated Android (via AlienTargetID's record) and Jones the cat, plus
; follow-on room furniture.
DrawRoomSpecials:
  LD A,(AlienTargetID)    ; Android slot chosen for this run
  CP $08
  JR NC,DrawRoomSpecials_0
  LD A,(AlienActiveFlag)
  AND A
  JR Z,DrawRoomSpecials_0
  LD HL,(AlienTargetPtr)
  INC HL
  LD A,(CurrentRoom)
  CP (HL)
  CALL Z,DrawRoomSpecials_16
DrawRoomSpecials_0:
  LD A,(CurrentRoom)
  LD HL,JonesRoom
  CP (HL)
  CALL Z,DrawRoomSpecials_12
  LD HL,$737F
  CP (HL)
  CALL Z,DrawRoomSpecials_13
  LD HL,ShipSystemFlags
  LD DE,$73CD
  CP $06
  JR Z,DrawRoomSpecials_2
  CP $0D
  JR Z,DrawRoomSpecials_9
  CP $0F
  JR Z,DrawRoomSpecials_4
  CP $11
  JR Z,DrawRoomSpecials_5
  CP $12
  JR Z,DrawRoomSpecials_6
  CP $13
  JR Z,DrawRoomSpecials_7
  CP $22
  JR Z,DrawRoomSpecials_1
  RET
DrawRoomSpecials_1:
  BIT 6,(HL)
  RET NZ
  LD A,$4E
  LD (DE),A
  LD A,$51
  INC DE
  LD (DE),A
  RET
DrawRoomSpecials_2:
  LD A,$46
  BIT 0,(HL)
  JR Z,DrawRoomSpecials_3
  LD A,$48
DrawRoomSpecials_3:
  LD (DE),A
  INC DE
  INC A
  LD (DE),A
  RET
DrawRoomSpecials_4:
  LD A,$4A
  LD (DE),A
  INC DE
  INC A
  LD (DE),A
  RET
DrawRoomSpecials_5:
  BIT 3,(HL)
  JR DrawRoomSpecials_8
DrawRoomSpecials_6:
  BIT 4,(HL)
  JR DrawRoomSpecials_8
DrawRoomSpecials_7:
  BIT 5,(HL)
DrawRoomSpecials_8:
  RET Z
  LD A,$4C
  LD (DE),A
  INC A
  INC DE
  LD (DE),A
  RET
DrawRoomSpecials_9:
  LD A,$35
  BIT 1,(HL)
  JR Z,DrawRoomSpecials_10
  LD A,$37
DrawRoomSpecials_10:
  LD (DE),A
  INC DE
  LD A,$36
  BIT 2,(HL)
  JR Z,DrawRoomSpecials_11
  LD A,$38
DrawRoomSpecials_11:
  LD (DE),A
  RET
; Jones is in the viewed room: menu Special row 14 ($73CC) offers string 51
; "Get Jones " (its handler needs the Net or the Cat Box), and the cat is
; drawn.
DrawRoomSpecials_12:
  PUSH AF
  LD DE,$73CC
  LD A,$33
  LD (DE),A
  CALL InitJonesAnim
  POP AF
  RET
; The alien is in the viewed room: draw it and offer "  ATTACK  ".
DrawRoomSpecials_13:
  PUSH AF
  JP PlayMusic_9
; This entry point is used by the routine at PlayMusic.
DrawRoomSpecials_14:
  LD A,$34                ; menu row = "  ATTACK  "
  LD (DE),A
  XOR A
  LD (AlienXPhase),A
; This entry point is used by the routine at PlayMusic.
DrawRoomSpecials_15:
  POP AF
  RET
; The activated Android is in the viewed room: draw it, offer ATTACK.
DrawRoomSpecials_16:
  PUSH AF
  JP PlayMusic_17
; This entry point is used by the routine at PlayMusic.
DrawRoomSpecials_17:
  LD A,$34                ; menu row = "  ATTACK  "
  LD (DE),A
; This entry point is used by the routine at PlayMusic.
DrawRoomSpecials_18:
  POP AF
  RET
; This entry point is used by the routine at XorHAddE. DrawRoomTitle: the
; panel's top line — the room name (or "In Duct") and, for rooms, "Damage" plus
; the two ASCII damage digits.
DrawRoomTitle:
  LD A,(CurrentRoom)      ; room-name string = room number,
  BIT 6,A                 ; or 93 "In Duct" inside the ducts
  JR Z,DrawRoomSpecials_19
  LD A,$5D
DrawRoomSpecials_19:
  LD HL,Screen_R19C0      ; draw pointer -> title row
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  LD HL,CurrentRoom       ; in the ducts: no damage meter
  BIT 6,(HL)
  RET NZ
  LD HL,$506B             ; "Damage    " (89)
  LD (DrawScreenPtr),HL
  LD A,$59
  CALL DrawSprite
  LD A,(CurrentRoom)      ; DE -> RoomDamageDigits[room]
  ADD A,A
  LD L,A
  LD H,$00
  LD DE,RoomDamageDigits
  ADD HL,DE
  EX DE,HL
  LD HL,$5072             ; draw the two ASCII digits
  LD (DrawScreenPtr),HL
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  LD HL,DrawScreenPtr
  INC (HL)
  INC DE
  CALL DrawSpriteRow
  LD HL,DrawScreenPtr
  INC (HL)
  LD A,$87                ; damage-meter icon glyph
  CALL DrawSpriteRow_0
  RET
; Room-info panel text fragments (was mis-disassembled as code): ASCII strings
; drawn one glyph at a time by the panel routines below. Byte 136 is the
; grille-icon UDG.
ItemsPresentText:
  DEFM "Items Present"
GrilleText:
  DEFM "Grille"
IsText:
  DEFM " is ",$88," "
AlsoHereText:
  DEFM "Also here "
AndText:
  DEFM "and "
ButText:
  DEFM "but "
AlsoText:
  DEFM "Also "
BodiesOfText:
  DEFM "Bodies of "
GrilleIconText:
  DEFM " ",$88," "
; DrawItemsAndGrille: room-info panel section under the room name. Draws the
; "Items Present" header when the viewed room's item list ($750A) is non-empty,
; then (except aboard the Narcissus, room 34) prints "Grille" plus its state ("
; in place "/" removed ") from RoomGrilleState (RoomGrilleState) and plants the
; matching action-menu entry: string 49 "RmveGrille" in the Special slot (strip
; cell 13) while in place, or string 50 "Grille" as a move-to target (strip
; cell 1) once removed. Used by the routine at RedrawRoomScene.
DrawItemsAndGrille:
  LD HL,RoomItemList      ; HL -> viewed room's item list
  LD A,(HL)               ; first entry: 255 = no items here
  CP $FF
  JR Z,DrawRoomSpecials_21 ; no items: skip the header
  LD HL,Screen_R20C0      ; draw pointer -> items header row
  LD (DrawScreenPtr),HL
  LD DE,ItemsPresentText  ; "Items Present"
  LD B,$0D                ; 13 characters
DrawRoomSpecials_20:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow      ; DrawSpriteRow: one glyph
  POP DE
  POP BC
  INC DE                  ; next character
  LD HL,DrawScreenPtr
  INC (HL)                ; draw pointer right one column
  DJNZ DrawRoomSpecials_20
DrawRoomSpecials_21:
  LD A,(CurrentRoom)      ; A = viewed room
  CP $22                  ; Narcissus: no grille aboard the shuttle
  RET Z
  LD DE,GrilleText        ; "Grille"
  LD HL,Screen_R20C16     ; draw pointer -> grille status row
  LD (DrawScreenPtr),HL
  LD B,$06                ; 6 characters
DrawRoomSpecials_22:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow      ; DrawSpriteRow: one glyph
  POP DE
  POP BC
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  DJNZ DrawRoomSpecials_22
  LD A,(CurrentRoom)      ; index RoomGrilleState by room number
  AND $3F                 ; (mask off the in-ducts flag, bit 6)
  LD L,A
  LD H,$00
  LD DE,RoomGrilleState
  ADD HL,DE
  LD A,(HL)
  CP $FF                  ; 255 = grille still in place
  JR Z,DrawRoomSpecials_23
  LD A,$5B                ; removed: print strip 91 " removed  "
  CALL DrawSprite
  LD DE,$73BF             ; menu strip cell 1 (a move-to target):
  LD A,$32                ; string 50 "Grille" = enter the open duct
  LD (DE),A
  RET
DrawRoomSpecials_23:
  LD A,$5A                ; in place: print strip 90 " in place "
  CALL DrawSprite
  LD DE,$73CB             ; menu strip cell 13 (the Special slot):
  LD A,$31                ; string 49 "RmveGrille"
  LD (DE),A
  RET
; DrawCrewCondition: prints the room-info line "NAME is <injury> and/ but
; <morale>" for the selected crew member (slot in DrawSlotIndex), using
; CrewInjuryText (CrewInjuryText, index = strength clamped to 0-3) and
; CrewMoraleText (CrewMoraleText, index = morale byte clamped to 0-4); "but" is
; picked when injury and morale disagree. This entry point is used by the
; routines at SwapHeldItems, UpdateRoomActors, WrapScreenAddr and
; UpdateAllCrew.
DrawCrewCondition:
  LD A,(DrawSlotIndex)    ; A = selected crew slot
  LD HL,Screen_R21C0      ; draw pointer -> condition row
  LD (DrawScreenPtr),HL
  CALL PrintName          ; PrintName
  LD DE,IsText            ; " is " (shared tail of the grille text)
  LD B,$04                ; 4 characters
DrawRoomSpecials_24:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ DrawRoomSpecials_24
  LD A,(IX+$04)           ; injury index from strength:
  CP $03                  ; 0/1/2 as-is (Dead/Collapsed/Wounded),
  JR C,DrawRoomSpecials_25 ; 3 -> 2 (Wounded), >3 -> 3 (OK)
  LD A,$03
  JR NZ,DrawRoomSpecials_25
  LD A,$02
DrawRoomSpecials_25:
  LD DE,CrewInjuryText    ; print CrewInjuryText[A]
  CALL PrintName_0
  LD DE,AndText           ; "and "
  LD A,(IX+$04)           ; pick the conjunction: "but " when
  CP $04                  ; injury and morale disagree (healthy
  JR C,DrawRoomSpecials_26 ; but low morale, or hurt but high
  LD A,(IX+$06)           ; morale), else "and "
  CP $03
  JR NC,DrawRoomSpecials_28
  JR DrawRoomSpecials_27
DrawRoomSpecials_26:
  LD A,(IX+$06)
  CP $03
  JR C,DrawRoomSpecials_28
DrawRoomSpecials_27:
  LD DE,ButText           ; "but "
DrawRoomSpecials_28:
  LD B,$04                ; draw the 4-character conjunction
DrawRoomSpecials_29:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ DrawRoomSpecials_29
  LD A,(IX+$06)           ; morale index, clamped to 0-4
  CP $05
  JR C,DrawRoomSpecials_30
  LD A,$04
DrawRoomSpecials_30:
  LD DE,CrewMoraleText    ; print CrewMoraleText[A]
  CALL PrintName_0
  JP PanelColCheck
; FindRoomMates: scan ActorRecords' +1 room bytes for other actors in the
; current room, storing the first two slot numbers found into
; RoomMateA/RoomMateB (31250/31251).
FindRoomMates:
  LD HL,$7387             ; HL -> slot 1's +1 (room) byte
  LD DE,RoomMateA         ; DE -> RoomMateA
  LD BC,$0701             ; B = 7 slots, C = slot number 1
  LD A,(DrawSlotIndex)    ; A' = the selected actor (excluded)
  EX AF,AF'
  LD A,(CurrentRoom)      ; A = the viewed room
DrawRoomSpecials_31:
  CP (HL)                 ; same room?
  JR NZ,DrawRoomSpecials_33
  EX AF,AF'               ; and not the selected actor itself?
  CP C
  JR Z,DrawRoomSpecials_32
  EX DE,HL                ; store the slot number
  LD (HL),C
  EX DE,HL
  INC DE
DrawRoomSpecials_32:
  EX AF,AF'
DrawRoomSpecials_33:
  PUSH DE                 ; next 8-byte record
  LD DE,$0008
  ADD HL,DE
  POP DE
  INC C
  DJNZ DrawRoomSpecials_31
  RET
; This entry point is used by the routines at GameEntry and UpdateAllCrew.
; DrawRoomMates: the "Also here NAME" / "Also Bodies of NAME" panel line.
; FindRoomMates fills RoomMateA/B; if every listed mate is dead (ActorRecords
; byte +7 != 0) the line reads "Also Bodies of", else "Also here" with the
; first mate's name (dead mates get the strip-94 overprint from
; DrawCrewStatusHalf, DrawCrewStatusHalf).
DrawRoomMates:
  CALL FindRoomMates      ; FindRoomMates -> RoomMateA/B
  LD HL,Screen_R22C0      ; draw pointer -> "Also here" row
  LD (DrawScreenPtr),HL
  LD BC,$0200             ; B = 2 mates to check, C = 0
  LD DE,RoomMateA         ; DE -> RoomMateA
DrawRoomSpecials_34:
  PUSH BC
  PUSH DE
  LD A,(DE)               ; no (more) mates: alive path
  CP $FF
  JR Z,DrawRoomSpecials_35
  ADD A,A                 ; HL -> that slot's +7 status byte
  ADD A,A
  ADD A,A
  LD E,A
  XOR A
  LD D,A
  LD HL,ActorStatusColumn
  ADD HL,DE
  CP (HL)                 ; still alive? -> alive path
  JR Z,DrawRoomSpecials_35
  POP DE                  ; this one is dead; check the next
  INC DE
  POP BC
  DJNZ DrawRoomSpecials_34
  LD DE,AlsoText          ; "Also Bodies of " (15 contiguous chars)
  LD B,$0F                ; all mates dead:
  CALL DrawStatusPanel_14 ; draw "Also Bodies of "
  LD A,(RoomMateA)        ; first body's name
  CALL PrintName
  LD DE,GrilleIconText    ; " ",136," " grille-icon spacer
  LD B,$03
  CALL DrawStatusPanel_14
  LD A,(RoomMateB)        ; second body's name
  CALL PrintName
  JP BlankRestOfRow       ; blank-pad the rest of the row
DrawRoomSpecials_35:
  POP BC
  POP DE
  LD HL,RoomMateA         ; anyone here at all?
  LD A,(HL)
  CP $FF
  JR Z,BlankRestOfRow     ; no: just blank the row
  LD DE,AlsoHereText      ; "Also here "
  LD B,$0A                ; draw "Also here " one glyph at a time
DrawRoomSpecials_36:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ DrawRoomSpecials_36
  LD A,(RoomMateA)        ; mate A: dead-overprint check + name
  CALL DrawCrewStatusHalf
  CALL PrintName
  LD A,(RoomMateB)        ; a second mate too?
  CP $FF
  JR Z,BlankRestOfRow
  LD A,$90                ; separator glyph
  CALL DrawSpriteRow_0
  LD HL,DrawScreenPtr
  INC (HL)
  LD A,(RoomMateB)        ; mate B: dead-overprint check + name
  CALL DrawCrewStatusHalf
  JP PrintNameThenPanel
; This entry point is used by the routine at UpdateAllCrew. BlankRestOfRow:
; draw blank tiles until the draw pointer reaches column 0 of the next row.
BlankRestOfRow:
  XOR A
  CALL DrawSpriteRow_0
  LD HL,DrawScreenPtr
  INC (HL)
  LD A,(HL)
  AND $1F
  RET Z
  JR BlankRestOfRow
; ResetActionMenu: restore the action-menu strip at CorridorPosTable
; (CorridorPosTable) from ActionMenuTemplate (ActionMenuTemplate), clear the
; viewed room's 20-entry item list ($750A) to 255 and invalidate the cached
; actor cell/name state ($7A10-$7A13).
ResetActionMenu:
  LD DE,CorridorPosTable
  LD HL,ActionMenuTemplate ; copy the 19-byte menu skeleton
  LD BC,$0013
  LDIR
  LD HL,RoomItemList      ; item list: 20 x 255 = empty
  LD A,$FF
  LD B,$14
DrawRoomSpecials_37:
  LD (HL),A
  INC HL
  DJNZ DrawRoomSpecials_37
  LD (HeldItemBack),A     ; no actor back/front cell
  LD (HeldItemFront),A
  LD (RoomMateA),A        ; no crew names cached
  LD (RoomMateB),A
  RET

; RedrawRoomScene
;
; Rebuilds the whole room scene: resets the action-menu strip and the room item
; state (ResetActionMenu), redraws the corridor strip, rebuilds the exit and
; item menus, the info panel (room name, damage, items present, grille state,
; crew condition) and the special actors, then updates the status panel. Called
; by everything that changes what the room shows (item transfers, deaths,
; grille removal, ...).
;
; USED by the routines at OpenCrewRoomView, GetItemPick, ResetCrewTimers,
;      UpdateRoomActors and
; #R44815.
RedrawRoomScene:
  CALL ResetActionMenu    ; ResetActionMenu
  LD HL,DrawMenuStrip
  LD (CrewAnimFlags),HL
  LD A,$80
  LD (JonesPhase),A
  LD (AlienXPhase),A
  CALL SelectSlotByDrawIdx
  LD A,(CurrentRoom)
  LD HL,$737F
  CP (HL)
  JR NZ,RedrawRoomScene_0
  LD HL,RoomTypeByte
  LD (HL),$04
RedrawRoomScene_0:
  BIT 6,A
  JR NZ,RedrawRoomScene_1
  CALL DrawRoomBackground
  CALL BuildExitList
  JR RedrawRoomScene_2
RedrawRoomScene_1:
  CALL RedrawRoomView
RedrawRoomScene_2:
  CALL ArmStraight_14
  CALL BuildRoomItemList
  CALL RefreshHeldItems
  CALL BuildItemMenu
  CALL ArmStraight_12
  CALL DrawRoomTitle
  CALL DrawItemsAndGrille
  CALL DrawCrewCondition
  CALL DrawRoomMates
  CALL DrawRoomSpecials
  CALL RedrawRoomScene_3
  LD A,(RoomTypeByte)
  CP $04
  RET Z
  LD A,(CurrentRoom)
  BIT 6,A
  CALL Z,PlaceRoomMarkerA
  RET
; This entry point is used by the routine at PlayMusic.
RedrawRoomScene_3:
  LD HL,$4016
  LD (DrawScreenPtr),HL
  PUSH HL
RedrawRoomScene_4:
  LD A,$FF
  CALL DrawSprite
  POP HL
  INC HL
  LD (DrawScreenPtr),HL
  LD A,(DrawSlotIndex)
  CALL PrintName
  LD HL,$5816
  LD A,$07
  LD C,$01
  CALL FillAttrColumn
  LD A,$0F
  LD C,$07
  CALL FillAttrColumn
  LD A,$20
  LD C,$03
  CALL FillAttrColumn
  LD A,$17
  LD C,$01
  CALL FillAttrColumn
  LD A,$30
  LD C,$01
  CALL FillAttrColumn
  LD A,$1F
  LD C,$05
  CALL FillAttrColumn
  LD A,$38
  LD C,$02
  CALL FillAttrColumn
  LD A,$3D
  LD (CursorCellValue),A
DrawMenuStrip:
  LD A,$11
  LD (CorridorCursor),A
  LD A,$01
  LD (RoomModeByte),A
  LD HL,CorridorPosTable
  LD (CorridorTilePtr),HL
  LD HL,$4036
  LD (DrawScreenPtr),HL
  LD B,$12
RedrawRoomScene_5:
  PUSH BC
  CALL DrawNextCorridorTile
  POP BC
  DJNZ RedrawRoomScene_5
  CALL HighlightMenuRow
  CALL OxygenCheckRedraw
  RET
; Cell-glyph walk-handler table (was mis-disassembled as code): 3-byte records
; (glyph, handler word) looked up by DrawDoorArm (DrawDoorArm), which patches
; the handler into its CALL as it walks an exit path over the room-map grid.
; Glyphs 126/127/132/133 are straight wall segments -> ArmStraight
; (ArmStraight, a no-op: keep heading); 128-131 are the four corner pieces ->
; steer handlers that bend the walk by approach direction; 125 '}' (duct
; grille) and 142/143 (junction nodes) -> ArmEndNode (ArmEndNode): paint and
; end the walk.
CellDrawTable:
  DEFB $7D,$6C,$82        ; '}' duct grille -> ArmEndNode
  DEFB $7E,$FB,$81        ; horizontal wall -> ArmStraight
  DEFB $7F,$FB,$81        ; vertical wall -> ArmStraight
  DEFB $80,$FC,$81        ; top-left corner -> ArmCornerTopLeft
  DEFB $81,$18,$82        ; bottom-left corner -> ArmCornerBottomLeft
  DEFB $82,$34,$82        ; bottom-right corner -> ArmCornerBottomRight
  DEFB $83,$50,$82        ; top-right corner -> ArmCornerTopRight
  DEFB $84,$FB,$81        ; horizontal wall -> ArmStraight
  DEFB $85,$FB,$81        ; vertical wall -> ArmStraight
  DEFB $8E,$6C,$82        ; junction node -> ArmEndNode
  DEFB $8F,$6C,$82        ; junction node -> ArmEndNode

; RedrawRoomView
;
; Rebuilds the room-interior view for the current room: picks the layout
; template by room type, decompresses it into the grid buffer, draws
; walls/furniture, then walks the exit list drawing one door arm per neighbour
; (and grille arms for duct openings). Room type 4 (the alien's lair view)
; resets the encounter animation instead.
RedrawRoomView:
  LD A,(RoomTypeByte)     ; A = room type
  CP $04                  ; alien-lair view?
  JR NZ,PickRoomLayout
  CALL ResetAlienXAnim    ; yes: reset alien body animation
  CALL ArmStraight_7
  RET
PickRoomLayout:
  LD HL,RoomLayout0       ; room type 0: layout template 0
  AND A
  JR Z,RedrawRoomView_0
  LD HL,RoomLayout1
  CP $01
  JR Z,RedrawRoomView_0
  LD HL,RoomLayout2
RedrawRoomView_0:
  CALL RoomLayoutExpand
  CALL ArmStraight_7
  CALL ArmStraight_4
  CALL SetRoomOrigin
  LD HL,(RoomAttrCursor)  ; mark the room's anchor cell (attr 71:
  LD (HL),$47             ; bright white) — the arms start here
  PUSH HL
  LD HL,(RoomCellCursor)
  PUSH HL
  LD A,($73C0)            ; 1st exit (menu strip cell 2)?
  CP $FF
  JR Z,RedrawRoomView_1
  LD DE,$FFEC             ; yes: walk the NORTH arm — heading -20
  LD (RoomCellStep),DE    ; (grid) / -32 (attr), starting one cell
  ADD HL,DE               ; above the anchor
  LD (RoomCellCursor),HL
  LD HL,(RoomAttrCursor)
  LD DE,$FFE0             ; DE = -32 (screen stride: one cell up within a
                          ; third)
  LD (RoomAttrStep),DE
  ADD HL,DE
  LD (RoomAttrCursor),HL
  CALL DrawDoorArm
RedrawRoomView_1:
  LD A,($73C1)            ; 2nd exit (menu strip cell 3)?
  CP $FF
  JR Z,RedrawRoomView_2
  POP HL                  ; yes: walk the EAST arm — heading +1/+1,
  POP DE                  ; starting one cell right of the anchor
  PUSH DE
  PUSH HL
  LD BC,$0001
  INC HL
  LD (RoomCellCursor),HL
  INC DE
  LD (RoomAttrCursor),DE
  LD (RoomAttrStep),BC
  LD (RoomCellStep),BC
  CALL DrawDoorArm
RedrawRoomView_2:
  LD A,($73C2)            ; 3rd exit (menu strip cell 4)?
  CP $FF
  JR Z,RedrawRoomView_3
  POP HL                  ; yes: walk the SOUTH arm — heading +20/+32,
  POP DE                  ; starting one cell below the anchor
  PUSH DE
  PUSH HL
  LD BC,$0014
  LD (RoomCellStep),BC
  ADD HL,BC
  LD (RoomCellCursor),HL
  LD HL,$0020
  LD (RoomAttrStep),HL
  ADD HL,DE
  LD (RoomAttrCursor),HL
  CALL DrawDoorArm
RedrawRoomView_3:
  POP HL
  POP DE
  LD A,($73C3)            ; 4th exit (menu strip cell 5)?
  CP $FF
  RET Z
  DEC DE                  ; yes: walk the WEST arm — heading -1/-1,
  DEC HL                  ; starting one cell left of the anchor
  LD (RoomCellCursor),HL
  LD (RoomAttrCursor),DE
  LD HL,$FFFF
  LD (RoomAttrStep),HL
  LD (RoomCellStep),HL
  CALL DrawDoorArm
  RET

; RoomLayoutExpand
;
; Decompresses the RLE room layout at HL into the 20x19 grid buffer: byte < 30
; = that many zero cells, 30-254 = literal tile code, 255 = end.
;
; USED by the corridor-view builder at DrawCrewStatusHalf and the room
;      renderer.
RoomLayoutExpand:
  LD DE,RoomGridBuffer    ; DE = grid buffer; HL = RLE source
RoomLayoutExpand_0:
  LD A,(HL)
  CP $1E
  JR C,RoomLayoutExpand_1
  CP $FF
  RET Z
  LD (DE),A
  INC HL
  INC DE
  JR RoomLayoutExpand_0
RoomLayoutExpand_1:
  LD B,A
  XOR A
RoomLayoutExpand_2:
  LD (DE),A
  INC DE
  DJNZ RoomLayoutExpand_2
  INC HL
  JR RoomLayoutExpand_0

; SetRoomOrigin
;
; Loads the current room's (row, column) pair from RoomOriginTable and sets
; RoomCellCursor (grid buffer + 20*row + col) and RoomAttrCursor (attribute
; file + 32*row + col) for the door drawing pass.
SetRoomOrigin:
  LD A,(CurrentRoom)      ; A = current room
  AND $3F
  ADD A,A
  LD D,$00
  LD E,A
  LD HL,RoomOriginTable
  ADD HL,DE
  LD B,(HL)
  INC HL
  LD C,(HL)
  PUSH BC
  LD DE,$0014
  LD HL,RoomGridBuffer
SetRoomOrigin_0:
  ADD HL,DE
  DJNZ SetRoomOrigin_0
  ADD HL,BC
  LD (RoomCellCursor),HL
  POP BC
  LD HL,AttrFileOrigin
  LD DE,$0020
SetRoomOrigin_1:
  ADD HL,DE
  DJNZ SetRoomOrigin_1
  ADD HL,BC
  LD (RoomAttrCursor),HL
  RET

; DrawDoorArm
;
; Highlights one exit path of the room-map view: walks the (invisibly drawn)
; wall glyphs from the room's anchor point, painting each visited attribute
; cell cyan (attr 5), until it reaches a grille or junction node. Per cell it
; reads the grid under RoomCellCursor, looks the glyph up in CellDrawTable
; (CellDrawTable) and patches the handler into the CALL below (self-modifying).
; Straight segments keep the heading, corner glyphs 128-131 turn it, and node
; glyphs (125/142/143) end the walk via ArmEndNode (which pops the return
; address). The stride pair RoomAttrStep/RoomCellStep encodes the heading: attr
; +32/-32/+1/-1 = south/north/east/west (grid steps +20/-20/+1/-1 in lockstep,
; the grid being 20 cells wide).
DrawDoorArm:
  LD HL,(RoomCellCursor)  ; read grid cell under the cursor
  LD A,(HL)
  LD HL,CellDrawTable     ; find its 3-byte record in CellDrawTable
DrawDoorArm_0:
  CP (HL)
  JR Z,DrawDoorArm_1
  INC HL
  INC HL
  INC HL
  JR DrawDoorArm_0
DrawDoorArm_1:
  INC HL                  ; fetch the handler word
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
  LD ($81DF),HL           ; patch it into the CALL below
  CALL ArmStraight        ; steer/stop for this glyph
  LD HL,(RoomAttrCursor)  ; paint the attribute cell cyan
  LD (HL),$05
  LD DE,(RoomAttrStep)    ; step the attribute cursor by the heading
  ADD HL,DE
  LD (RoomAttrCursor),HL
  LD HL,(RoomCellCursor)  ; step the grid cursor in lockstep
  LD DE,(RoomCellStep)
  ADD HL,DE
  LD (RoomCellCursor),HL
  JR DrawDoorArm

; ArmStraight
;
; Walk handler for straight wall glyphs (126/127/132/133): a no-op, the walk
; keeps its current heading.
ArmStraight:
  RET
; ArmCornerTopLeft — walk handler for corner glyph 128, which joins a
; south-going edge to an east-going one: a walker heading north exits east; any
; other heading exits south.
ArmCornerTopLeft:
  LD HL,(RoomAttrStep)    ; L = low byte of current heading
  LD A,L
  LD DE,$0020             ; default: turn south
  LD HL,$0014
  CP $E0                  ; heading north (-32, low byte 224)?
  JR NZ,ArmStraight_0
  LD DE,$0001             ; yes: turn east
  LD HL,$0001
ArmStraight_0:
  LD (RoomAttrStep),DE
  LD (RoomCellStep),HL
  RET
; ArmCornerBottomLeft — corner glyph 129 joins a north-going edge to an
; east-going one: heading south exits east; otherwise exits north.
ArmCornerBottomLeft:
  LD HL,(RoomAttrStep)    ; L = low byte of current heading
  LD A,L
  LD DE,$0001             ; default: turn east
  LD HL,$0001
  CP $20                  ; heading south?
  JR Z,ArmStraight_1
  LD DE,$FFE0             ; no: turn north (DE = -32, HL = -20)
  LD HL,$FFEC
ArmStraight_1:
  LD (RoomAttrStep),DE
  LD (RoomCellStep),HL
  RET
; ArmCornerBottomRight — corner glyph 130 joins a west-going edge to a
; north-going one: heading east exits north; otherwise exits west.
ArmCornerBottomRight:
  LD HL,(RoomAttrStep)    ; L = low byte of current heading
  LD A,L
  LD DE,$FFE0             ; default: turn north (DE = -32, HL = -20)
  LD HL,$FFEC
  CP $01                  ; heading east?
  JR Z,ArmStraight_2
  LD DE,$FFFF             ; no: turn west
  LD HL,$FFFF
ArmStraight_2:
  LD (RoomAttrStep),DE
  LD (RoomCellStep),HL
  RET
; ArmCornerTopRight — corner glyph 131 joins a west-going edge to a south-going
; one: heading east exits south; otherwise exits west.
ArmCornerTopRight:
  LD HL,(RoomAttrStep)    ; L = low byte of current heading
  LD A,L
  LD DE,$0020             ; default: turn south
  LD HL,$0014
  CP $01                  ; heading east?
  JR Z,ArmStraight_3
  LD DE,$FFFF             ; no: turn west
  LD HL,$FFFF
ArmStraight_3:
  LD (RoomAttrStep),DE
  LD (RoomCellStep),HL
  RET
; ArmEndNode — walk handler for the duct grille (125) and junction nodes
; (142/143): pops DrawDoorArm's looping return address, paints the node cell
; cyan and ends the walk.
ArmEndNode:
  POP AF
  LD HL,(RoomAttrCursor)
  LD (HL),$05
  RET
ArmStraight_4:
  LD DE,$000C
  LD HL,AttrFileOrigin
  LD C,$13
ArmStraight_5:
  LD B,$14
ArmStraight_6:
  LD (HL),$00
  INC HL
  DJNZ ArmStraight_6
  ADD HL,DE
  DEC C
  JR NZ,ArmStraight_5
  LD HL,RoomGridBuffer
  CALL DrawRoomBackground_0
  RET
ArmStraight_7:
  LD A,(CurrentRoom)
  AND $3F
  LD L,A
  LD H,$00
  PUSH HL
  ADD HL,HL
  ADD HL,HL
  POP DE
  ADD HL,DE
  LD DE,RoomAdjDucts
  ADD HL,DE
  LD DE,$73C0
  LD B,$04
ArmStraight_8:
  LD C,(HL)
  CP C
  JR Z,ArmStraight_9
  LD A,(CurrentRoom)
  AND $3F
  CP C
  JR Z,ArmStraight_9
  LD A,C
  LD (DE),A
ArmStraight_9:
  INC DE
  INC HL
  DJNZ ArmStraight_8
  LD B,$04
  LD HL,$73C0
  LD C,$39
ArmStraight_10:
  LD A,(HL)
  CP $FF
  JR Z,ArmStraight_11
  LD (HL),C
ArmStraight_11:
  INC C
  INC HL
  DJNZ ArmStraight_10
  RET
; This entry point is used by the routines at InitGameView and PauseMenu.
ArmStraight_12:
  LD HL,Screen_R19C0
  LD (DrawScreenPtr),HL
  LD B,$80
ArmStraight_13:
  PUSH BC
  XOR A
  CALL DrawSpriteRow_0
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ ArmStraight_13
  RET
ArmStraight_14:
  LD HL,$59E1
  LD DE,$001D
  LD C,$03
ArmStraight_15:
  LD B,$03
ArmStraight_16:
  LD (HL),$39
  INC HL
  DJNZ ArmStraight_16
  DEC C
  ADD HL,DE
  JR NZ,ArmStraight_15
  LD DE,$0048
  LD HL,CrewPortraits
  LD A,(DrawSlotIndex)
  DEC A
  JR Z,ArmStraight_18
  LD B,A
ArmStraight_17:
  ADD HL,DE
  DJNZ ArmStraight_17
ArmStraight_18:
  EX DE,HL
  LD HL,$48E1
; This entry point is used by the routine at DrawShipMap.
ArmStraight_19:
  LD (DrawScreenPtr),HL
  LD B,$03
ArmStraight_20:
  LD C,$03
ArmStraight_21:
  PUSH BC
  LD HL,(DrawScreenPtr)
  LD B,$08
ArmStraight_22:
  LD A,(DE)
  LD (HL),A
  INC H
  INC DE
  DJNZ ArmStraight_22
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DEC C
  JR NZ,ArmStraight_21
  PUSH DE
  LD HL,(DrawScreenPtr)
  LD DE,$001D
  LD A,L
  CP $E0
  JR C,ArmStraight_23
  LD DE,$071D
ArmStraight_23:
  ADD HL,DE
  LD (DrawScreenPtr),HL
  POP DE
  DJNZ ArmStraight_20
  RET

; AdvanceScriptPtr
;
; Steps the ROM-traversal script pointer forward by one byte and returns the
; low nibble (0-15) of the new byte in A. The pointer lives at ScriptPointer
; and walks through ZX Spectrum ROM bytes starting at $1041 (SEED $9041 with H
; masked to $10). Each nibble encodes one game command for the AI scheduler.
;
; Output: A = next script command (0-15)
;
; Used by the routines at GetItemPick, GameEntry, UpdateJones, UpdateCrewAI,
; XorHAddE, UpdateRoomActors and TriggerAlienEvent.
AdvanceScriptPtr:
  LD HL,(ScriptPointer)   ; HL = current script pointer (ScriptPointer)
  INC HL                  ; advance to next ROM byte
  LD (ScriptPointer),HL   ; save updated pointer
  LD A,(HL)               ; read ROM byte at new address
  AND $0F                 ; keep only low nibble → command 0-15
  RET

; ResetScriptPtr
;
; Resets the ROM-traversal script pointer to its starting address. Reads the
; SEED value from SysFrames (ZX Spectrum system variable SEED, updated by
; RAND), calls the address-masking helper at WrapScreenAddr to compute $1041
; (ROM address $9041 with H masked to $10), and stores the result at
; ScriptPointer.
;
; USED by the routines at GameEntry and PlayMusic.
ResetScriptPtr:
  LD HL,(SysFrames)       ; HL = SEED (SysFrames): ZX ROM random seed value
  CALL WrapScreenAddr     ; mask H to $10: HL → ROM address in range
                          ; $1000-$1FFF
  LD (ScriptPointer),HL   ; store as script pointer at ScriptPointer
  RET

; Routine at DrawCrewStatusHalf
;
; Used by the routine at DrawSprite.
DrawCrewStatusHalf:
  PUSH AF
  LD HL,ActorStatusColumn
  LD DE,$0008
  LD B,A
DrawCrewStatusHalf_0:
  ADD HL,DE
  DJNZ DrawCrewStatusHalf_0
  LD A,(HL)
  AND A
  JR Z,DrawCrewStatusHalf_1
  LD HL,(DrawScreenPtr)
  PUSH HL
  LD A,$5E
  CALL DrawSprite
  POP HL
  LD DE,$0008
  ADD HL,DE
  LD (DrawScreenPtr),HL
DrawCrewStatusHalf_1:
  POP AF
  RET
  LD HL,CorridorLayout
  CALL RoomLayoutExpand
  LD DE,$788D
  LD HL,AttrFileOrigin
  LD C,$13
DrawCrewStatusHalf_2:
  LD B,$14
DrawCrewStatusHalf_3:
  LD A,(DE)
  LD (HL),A
  INC DE
  INC HL
  DJNZ DrawCrewStatusHalf_3
  PUSH DE
  LD DE,$000C
  ADD HL,DE
  POP DE
  DEC C
  JR NZ,DrawCrewStatusHalf_2
  LD HL,RoomGridBuffer
  CALL DrawRoomBackground_0
  RET

; Game state scratchpad RAM
;
; 110 bytes of mixed 1- and 2-byte variables stamped inline within the code
; region. The initial byte values come from the tape image, but every byte is
; treated as RAM by the running game: written by the main-loop subsystems and
; read back on subsequent frames. Despite living inside the code segment, none
; of these bytes are executed.
;
; KEY variables:
; #R33682 AlienFlags        bit-packed input (R,L,D,U,Fire)
; #R33683 CorridorCursor    current corridor index (0..18)
; #R33688 SpriteDestAddr    sprite blitter screen pointer
; #R33690 FrameCounterB     free-running 50Hz counter
; #R33699 SoundRequest      one-shot sound trigger
; #R33700 CrewIndex         current crew member (0..6)
; #R33701 AlienTargetPtr         pointer to current crew record
; #R33703 AlienTargetID     crew ID being targeted (255=none)
; #R33704 AlienActiveFlag   alien is loose flag
; #R33706 JonesRoom         Jones the cat's current room (255 = off-map)
; #R33731 SoundReqFlag      pending sound parameters
; #R33734 SprScreenAddrA    crew sprite A destination
; #R33736 SprScreenAddrB    crew sprite B destination
; #R33737 MusicActiveFlag   music phrase is playing
; #R33739 AnimFrameCnt      crew animation tick
;
; All cells in this block now carry meaningful labels; the message-queue
; cluster (MsgDrawnCol through MsgLastEnqueued) is documented at
; TickMessageQueue (TickMessageQueue).
AlienFlags:
  DEFB $00                ; Keyboard/joystick scan bitmask (bit 0=R, 1=L, 2=D,
                          ; 3=U, 4=Fire)
CorridorCursor:
  DEFB $11                ; Currently selected corridor / cursor position
                          ; (0-18)
SavedRowAttr:
  DEFB $00                ; attribute saved from under the menu-cursor
                          ; highlight (HighlightMenuRow $8842 /
                          ; UnhighlightMenuRow $882B)
CursorCellValue:
  DEFB $00                ; CorridorPosTable cell value under the cursor
                          ; ($8395): what the action key dispatches on
SprDestA:
  DEFB $00
SprDestAHi:
  DEFB $40                ; high byte of SprDestA (marker A screen address)
SpriteDestAddr:
  DEFW DisplayFile        ; Screen destination pointer used by sprite blitter
FrameCounterB:
  DEFB $19                ; Free-running frame counter (low byte, ~50 Hz)
FrameCounterA:
  DEFB $19                ; frame counter for marker channel A (reset to 10)
NearRoomList:
  DEFB $FF,$FF,$FF        ; 3-room scratch list ($FF = empty), matched against
                          ; actor rooms at $8E30
NearestCrewC:
  DEFB $FF                ; third nearest-crew slot; $839F-$83A1 are also
                          ; iterated as a 3-list
NearestCrewA:
  DEFB $FF                ; nearest crew ID, slot A (255 = none)
NearestCrewB:
  DEFB $FF                ; nearest crew ID, slot B (255 = none)
DistanceAccum:
  DEFB $00                ; running distance/count accumulator for the
                          ; nearest-crew scan
SoundRequest:
  DEFB $00                ; One-shot sound request (255 = idle)
CrewIndex:
  DEFB $01                ; Current crew member index (0-6)
AlienTargetPtr:
  DEFB $86,$73            ; pointer to the alien-target / Android ActorRecords
                          ; slot
AlienTargetID:
  DEFB $09                ; In the Long Game this is the **Android** slot index
                          ; (1..7): the dormant traitor crew member that
                          ; UpdateAlien (UpdateAlien) activates on first
                          ; encounter with the alien (the first frame
                          ; AlienActiveFlag is set and the slot still has its
                          ; template +0=+7=0). Once activated, the Android
                          ; enters action state 7 and wounds nearby crew via
                          ; CrewAction7_Handler (CrewAction7_Handler). Values
                          ; >= 8 (e.g. ShortGameInit's hard-wired 9) disable
                          ; the mechanic — TriggerAlienEvent
                          ; (TriggerAlienEvent) treats them as "no valid
                          ; target".
AlienActiveFlag:
  DEFB $00                ; Non-zero when the alien is loose in corridors
SoundPending:
  DEFB $00                ; SoundPending: nonzero = a sound effect is queued;
                          ; PlayMusic (PlayMusic) plays and clears it
JonesRoom:
  DEFB $06                ; Jones the cat's current room (255 = off-map)
JonesMoveTimer:
  DEFB $FF                ; frames until Jones's next move
MsgDrawnCol:
  DEFB $00                ; message line: column reached by the renderer (bit 7
                          ; set)
MsgVisibleTimer:
  DEFB $00                ; frames the current message stays on screen
MsgAttrPtr:
  DEFB $E0,$5A            ; pointer into the message line's attribute row
                          ; ($5AE0)
MsgParam1:
  DEFB $FF                ; current message parameter 1 (254 = actor name, 252
                          ; = room name)
MsgParam2:
  DEFB $FF                ; current message parameter 2 (253 = target name)
MessageQueue:
  DEFB $00,$00,$00        ; message queue: 3-byte entries (msg#, param1,
                          ; param2),
MessageQueueBody:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; ...rest of the
                                                               ; queue buffer
                                                               ; (write ptr at
                                                               ; $83CD)
SoundReqFlag:
  DEFB $00,$00,$00        ; Sound request word (3 bytes incl. frequency /
                          ; duration)
SprScreenAddrA:
  DEFB $00,$00            ; Screen destination for crew sprite A
SprScreenAddrB:
  DEFB $00                ; Screen destination low byte for crew sprite B
MusicActiveFlag:
  DEFB $00                ; Non-zero when background music phrase is playing
Unused83CA:
  DEFB $FF
AnimFrameCnt:
  DEFB $FF                ; Frame counter for crew sprite animation timing
Unused83CC:
  DEFB $FF
MsgQueueWritePtr:
  DEFB $B2,$83            ; message-queue write pointer (starts at MessageQueue
                          ; $83B2)
MsgLastEnqueued:
  DEFB $FF,$FF            ; last enqueued (msg#, param1) pair - suppresses
                          ; duplicates
AlienStopCell:
  DEFB $0F                ; corridor cell at which the walking alien stops
                          ; (fires $AC5D)
ItemCharges:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00 ; per-item charge counters, items 0-7
                                       ; (prods, incinerators, trackers):
                                       ; consumed at $9344
ItemChargesHi:
  DEFB $03,$03,$03,$03,$01,$08,$08,$08 ; ...items 8-15: extinguishers 3
                                       ; charges, Harpoon Gun 1, laser pistols
                                       ; 8
JonesCatchBySlot:
  DEFB $00,$0E,$0E,$0D,$0D,$0D,$0F,$0E ; per-slot Get Jones threshold: the
                                       ; catch succeeds when the next script
                                       ; nibble beats it ($8C13; -4 when using
                                       ; the Net rather than the Cat Box)
GrilleTimeBySlot:
  DEFB $00,$64,$78,$B4,$64,$B4,$50,$50 ; per-slot RmveGrille base time in
                                       ; frames (StartGrilleRemoval $8BD3):
                                       ; Dallas 100, Kane 120, Ripley 180, Ash
                                       ; 100, Lambert 180, Parker/Brett 80
ActionTimeByStrength:
  DEFB $00,$00,$30,$10,$00,$00,$00,$00 ; SetActionTimer ($8BE6) adjustment by
                                       ; strength (IX+4)&7: Wounded (2) +48,
                                       ; strength 3 +16, else +0
ActionTimeBySlot:
  DEFB $00,$07,$05,$00,$07,$03 ; SetActionTimer per-slot adjustment. Only 6
                               ; bytes: slots 6-7 read past the end into
                               ; TileTableBounds (10 and 5)

; Data block at TileTableBounds
TileTableBounds:
  DEFB $0A,$05

; RoomDispatchTable
;
; Word table with a clever triple use. The action key (HandleInput at $881B)
; dispatches table[room_mode] with room_mode from RoomModeByte: 0-1
; RoomModeDispatch   (RoomModeDispatch) 2-3  LocationListAction
; (LocationListAction) — Indicate Location list, pages 1 (rooms 0-16) and 2
; (rooms 17-33) 4    GetItemPick        (GetItemPick) — item picked from the
; Get strip For modes 0 and 1 RoomModeDispatch then re-dispatches on the cursor
; ROW through overlapping halves of this same table (entry 5 onward = base
; $840C, entry 23 onward = base $8430):
;
; MODE-0 rows (orders screen), base $840C:
; 0 "Order:" header       -> RoomHandlerNop (RoomHandlerNop)
; 1-7 crew slots 1-7        -> OpenCrewRoomView (OpenCrewRoomView)
; 8 " Indicate "          -> IndicateLocation (IndicateLocation): opens
; THE room-location list (mode 2)
; 9 " Location "          -> nop (2nd row of the 2-row label)
; 10-11 " Display  "/"  Level"-> nop (header for the deck rows)
; 12-14 Upper/Middle/Lower    -> SelectDeckMap (SelectDeckMap): deck map
; 15-18 blank                 -> nop
;
; Mode-1 rows (room view / action menu), base $8430: 0     "move to:" header
; -> nop 1     "Grille" (open duct)  -> MoveThroughGrille (MoveThroughGrille)
; 2-6   exit rooms            -> MoveToRoom (MoveToRoom) 7     " use:" header
; -> nop 8     front-hand item       -> RoomHandlerNop2 (RoomHandlerNop2); but
; the pre-dispatch hook ($B2C0) zeroes the actor's action countdown — pressing
; the held-item row cancels a pending timed job 9     back-hand item        ->
; SwapHeldItems (SwapHeldItems) 10    "Get Item"            -> GetItemMenu
; (GetItemMenu) 11    "Leave Item"          -> LeaveItemHandler
; (LeaveItemHandler) 12    "Special" header      -> nop 13    special action
; -> $8BCB -> SpecialRowAction ($B306): 52 ATTACK / 49 RmveGrille / 238 =
; RmveGrille swapped to ATTACK under the cursor (hostile in the room — see
; PreDispatchHook $B2B9) 14    "Get Jones"           -> $8C05 (needs Net or Cat
; Box) 15-16 fixture actions       -> FixtureAction (FixtureAction) 17    "
; QUIT   "          -> QuitRoomView (QuitRoomView) 18    blank
; -> nop (reuses CrewActionDispatch+0) The mode-1 row words for rows 11-17 are
; the "RoomDispatchTailExtra" bytes at RoomDispatchTailExtra; row 18 overlaps
; CrewActionDispatch's first entry.
RoomDispatchTable:
  DEFW RoomModeDispatch,RoomModeDispatch,LocationListAction,LocationListAction
  DEFW GetItemPick,RoomHandlerNop,OpenCrewRoomView,OpenCrewRoomView
  DEFW OpenCrewRoomView,OpenCrewRoomView,OpenCrewRoomView,OpenCrewRoomView
  DEFW OpenCrewRoomView,IndicateLocation,RoomHandlerNop,RoomHandlerNop
  DEFW RoomHandlerNop,SelectDeckMap,SelectDeckMap,SelectDeckMap
  DEFW RoomHandlerNop,RoomHandlerNop,RoomHandlerNop,RoomHandlerNop
  DEFW MoveThroughGrille,MoveToRoom,MoveToRoom,MoveToRoom
  DEFW MoveToRoom,MoveToRoom,RoomHandlerNop,RoomHandlerNop2
  DEFW SwapHeldItems,GetItemMenu

; Data block at RoomDispatchTailExtra
;
; Previously misclassified as a code routine ("GetCorridorTableEntry"). This
; range is actually three back-to-back data tables used by the rest of the
; game:
;
; R33862.. R33875 (14 bytes, 7 × 16-bit pointers) — likely the tail of
; THE RoomDispatchTable at RoomDispatchTable (extra entries for modes >33, all
; SHARING the small handler pool used by RoomDispatchTable).
; R33876.. R33891 (16 bytes, 8 × 16-bit pointers) — CrewActionDispatch,
; INDEXED by crew/alien action state in (IX+3). Action 0 returns
; EARLY; actions 1-7 jump to the matching handler. Used by the
; ROUTINE at DispatchCrewAction.
; R33892.. R34635 (744 bytes) — MessageTextTable, 34 strings terminated
; BY byte 255. Substitution codes inside the strings: 254 = actor
; NAME, 253 = target name, 252 = room name, 145 = apostrophe glyph.
; WALKED and rendered by the routine at RenderMessage.
;
; Triggers (message # -> firing condition): 0  Jones enters a living crew
; member's room (UpdateJones) 1  alien attack tick, victim strength still >= 4
; 2  Get Jones action succeeds (JonesRoom := 255 = carried) 3/4  crew nets /
; hits the alien (CrewHitsAlien outcomes) 5/6/7  weapon spent: Tracker /
; extinguisher (also fire handler with no charges) / Laser 8  room damage
; event, cell value 55 9  fire breaks out in a room 10 alien attack tick,
; victim strength 2-3 11/12 hypersleep pod entered (UpdateRoomActors) /
; completed (CrewAction6_Handler) 13/14 destruct lever armed / aborted
; (DestructArm/DestructAbort) 15 engine-room fire fully extinguished 16/17
; airlock blown (BlowLock) / resealed (SealLock) 18 alien re-enters via
; ShuttleBay after surviving an airlock blow 19 actor stands beside Jones's
; strip cells 20/21 20 actor shares a room with a dead crew member 21 victim
; strength exhausted - collapse 22/23/24/25 accumulated room damage crosses the
; '3x' threshold in CommdCentr / Computer / Life Suppt / Narcissus
; (AddRoomDamage AddRoomDamage, damage digits at RoomDamageDigits) 26 oxygen
; clock reaches "05:00" 27/28 launch check: Jones missing / crew not all aboard
; (LaunchGate LaunchGate) 29/30/31/32 Android combat set (UpdateAlien /
; CrewHitsAndroid) 33 Long Game opening: chestburster host named (LongGameInit)
RoomDispatchTailExtra:
  DEFW LeaveItemHandler,RoomHandlerNop,SpecialRowJump,$8C05 ; 7 × 16-bit
                                                            ; pointers
                                                            ; (room-handler
                                                            ; tail)
  DEFW FixtureAction,FixtureAction,QuitRoomView
CrewActionDispatch:
  DEFW RoomHandlerNop,CrewAction1_Idle
  DEFW CrewAction2_Move,CrewAction3_RemoveGrille
  DEFW CrewAction4_Handler,CrewAction5_Investigate
  DEFW CrewAction6_Handler,CrewAction7_Handler
; #0: "{actor} Sees Jones the Cat"
MessageTextTable:
  DEFB $FE
  DEFM " Sees Jones the Cat"
  DEFB $FF
; #1: "The ALIEN is attacking {actor}"
  DEFM "The ALIEN is attacking "
  DEFB $FE
  DEFB $FF
; #2: "{actor} has captured Jones"
  DEFB $FE
  DEFM " has captured Jones"
  DEFB $FF
; #3: "{actor} nets the ALIEN"
  DEFB $FE
  DEFM " nets the ALIEN"
  DEFB $FF
; #4: "{actor} hits the ALIEN"
  DEFB $FE
  DEFM " hits the ALIEN"
  DEFB $FF
; #5: "{actor}'s Tracker is broken"
  DEFB $FE,$91
  DEFM "s Tracker is broken"
  DEFB $FF
; #6: "{actor}'s extinguisher is empty"
  DEFB $FE,$91
  DEFM "s extinguisher is empty"
  DEFB $FF
; #7: "{actor}'s Laser is exhausted"
  DEFB $FE,$91
  DEFM "s Laser is exhausted"
  DEFB $FF
; #8: "Structural Damage in {room}"
  DEFM "Structural Damage in "
  DEFB $FC
  DEFB $FF
; #9: "WARNING:Fire in {room}"
  DEFM "WARNING:Fire in "
  DEFB $FC
  DEFB $FF
; #10: "{actor} is being wounded"
  DEFB $FE
  DEFM " is being wounded"
  DEFB $FF
; #11: "{actor} prepares for hypersleep"
  DEFB $FE
  DEFM " prepares for hypersleep"
  DEFB $FF
; #12: "{actor} enters hypersleep"
  DEFB $FE
  DEFM " enters hypersleep"
  DEFB $FF
; #13: "DESTRUCT SEQUENCE INITIATED"
  DEFM "DESTRUCT SEQUENCE INITIATED"
  DEFB $FF
; #14: "DESTRUCT SEQUENCE ABORTED"
  DEFM "DESTRUCT SEQUENCE ABORTED"
  DEFB $FF
; #15: "Fire extinguished in {room}"
  DEFM "Fire extinguished in "
  DEFB $FC
  DEFB $FF
; #16: "WARNING:{room}DEPRESSURISED"
  DEFM "WARNING:"
  DEFB $FC
  DEFM "DEPRESSURISED"
  DEFB $FF
; #17: "{room} PRESSURE RESTORED"
  DEFB $FC
  DEFM " PRESSURE RESTORED"
  DEFB $FF
; #18: "INTRUDER ENTERING {room}"
  DEFM "INTRUDER ENTERING "
  DEFB $FC
  DEFB $FF
; #19: "Jones is looking uneasy"
  DEFM "Jones is looking uneasy"
  DEFB $FF
; #20: "{actor} finds {target}'s body"
  DEFB $FE
  DEFM " finds "
  DEFB $FD,$91
  DEFM "s body"
  DEFB $FF
; #21: "{actor} has collapsed"
  DEFB $FE
  DEFM " has collapsed"
  DEFB $FF
; #22: "Partial loss of control systems"
  DEFM "Partial loss of control systems"
  DEFB $FF
; #23: "Computer Malfunction detected"
  DEFM "Computer Malfunction detected"
  DEFB $FF
; #24: "Environmental irregularities"
  DEFM "Environmental irregularities"
  DEFB $FF
; #25: "Narcissus status RED"
  DEFM "Narcissus status RED"
  DEFB $FF
; #26: "Oxygen status CRITICAL"
  DEFM "Oxygen status CRITICAL"
  DEFB $FF
; #27: "BioUnit Jones not present"
  DEFM "BioUnit Jones not present"
  DEFB $FF
; #28: "LAUNCH COUNTERMANDED"
  DEFM "LAUNCH COUNTERMANDED"
  DEFB $FF
; #29: "{target} is attacking {actor}"
  DEFB $FD
  DEFM " is attacking "
  DEFB $FE
  DEFB $FF
; #30: "{actor} is attacking {target}"
  DEFB $FE
  DEFM " is attacking "
  DEFB $FD
  DEFB $FF
; #31: "{actor} nets {target}"
  DEFB $FE
  DEFM " nets "
  DEFB $FD
  DEFB $FF
; #32: "{actor} hits the Android" Enqueued by CrewHitsAndroid
; (CrewHitsAndroid). The "Android" is the long-game AlienTargetID slot
; (AlienTargetID) — the dormant traitor crew member that UpdateAlien
; (UpdateAlien) activates on first encounter with the alien. The actor
; placeholder is filled from CrewIndex (CrewIndex) = the slot currently being
; dispatched in combat state 4. Because the (IX+1) sprite-class match in
; CrewAction4_Handler is by class (not unique slot ID), the message can fire
; for any crew sharing the Android's class — see CrewAction4_Handler header for
; the ambiguity.
  DEFB $FE
  DEFM " hits the Android"
  DEFB $FF
; #33: "Alien has hatched from {actor}"
  DEFM "Alien has hatched from "
  DEFB $FE
  DEFB $FF

; KeyboardScanner
;
; Reads the keyboard half-rows used by the game and packs the active
; directional / fire keys into AlienFlags. Bit 4 = fire/action, bit 3 = right
; (key 8), bit 2 = left (key 5), bit 1 = up (key 6), bit 0 = down (key 7). Used
; by the routine at HandleInput (HandleInput).
KeyboardScanner:
  CALL DefaultKeyScanner  ; scan into AlienFlags via the selected device's
  RET                     ; scanner (the CALL operand at $874D is patched from
                          ; InputScannerTable by OptionsScreen)
DefaultKeyScanner:
  LD HL,AlienFlags        ; HL points to AlienFlags
  LD (HL),$00
  LD BC,$F7FE             ; port $F7FE: half-row 1-5
  IN A,(C)
  LD B,$EF                ; next read: port $EFFE, half-row 6-0
KeyboardScanner_0:
  BIT 4,A                 ; key 5 pressed? (active low)
  JR NZ,KeyboardScanner_1
  SET 1,(HL)              ; bit 1 = key 5
KeyboardScanner_1:
  IN A,(C)                ; read half-row 6-0
  BIT 2,A                 ; key 8?
  JR NZ,KeyboardScanner_2
  SET 0,(HL)              ; bit 0 = key 8
KeyboardScanner_2:
  BIT 3,A                 ; key 7?
  JR NZ,KeyboardScanner_3
  SET 2,(HL)              ; bit 2 = key 7
KeyboardScanner_3:
  BIT 4,A                 ; key 6?
  JR NZ,KeyboardScanner_4
  SET 3,(HL)              ; bit 3 = key 6
KeyboardScanner_4:
  BIT 0,A                 ; key 0 (action)?
  RET NZ
  SET 4,(HL)              ; bit 4 = action
  RET

; HandleInput
;
; Reads the keyboard and moves the corridor cursor. Calls the key-scanner at
; KeyboardScanner, which packs five keys into AlienFlags (set bit = pressed):
; bit 0 = key 8, bit 1 = key 5, bit 2 = key 7, bit 3 = key 6, bit 4 = key 0
; (action). While the action key is held the border flashes yellow/black; on
; RELEASE control jumps to HandleInput_12, which dispatches through
; RoomDispatchTable (RoomDispatchTable) on the current room mode (RoomModeByte)
; — this is how a room's action handler / menu is opened. Otherwise: key 6 ->
; HandleInput_5 (advance the cursor along the corridor), key 7 -> HandleInput_8
; (retreat), and keys 8/5 move between rows when the room view has more than
; one row (mode != 0).
;
; USED by the routine at GameEntry.
HandleInput:
  CALL KeyboardScanner    ; scan keyboard → DrawCrewStatusHalf_1 bitmask
  LD A,(AlienFlags)       ; A = key bitmask (DrawCrewStatusHalf_1)
  AND A                   ; any key pressed?
  RET Z                   ; no: return immediately
  BIT 4,A                 ; action/fire key held?
  JR Z,HandleInput_1      ; no: check directional keys
  XOR A                   ; A = 0 (border black)
HandleInput_0:
  EX AF,AF'               ; save border colour to A'
  CALL KeyboardScanner    ; re-scan keyboard (wait for release)
  LD A,(AlienFlags)       ; read updated key state
  BIT 4,A                 ; action key still held?
  JP Z,HandleInput_12     ; released: open the room-action dispatch
  EX AF,AF'               ; restore A' border colour
  XOR $06                 ; toggle between 0 (black) and 6 (yellow)
  OUT ($FE),A             ; flash border
  JR HandleInput_0        ; repeat until key released
HandleInput_1:
  BIT 3,A                 ; key 6 (advance)?
  JR NZ,HandleInput_5     ; yes: advance cursor along the corridor
  BIT 2,A                 ; key 7 (retreat)?
HandleInput_2:
  JR NZ,HandleInput_8     ; yes: retreat cursor along the corridor
  AND $03                 ; key 5 or key 8 (row movement)?
HandleInput_3:
  RET Z                   ; no directional key: return
  LD A,(RoomModeByte)     ; A = room mode (RoomModeByte)
  AND A                   ; mode 0 = single-row corridor (no up/down)
  RET Z                   ; in single-row mode: up/down keys ignored
HandleInput_4:
  LD A,(CursorCellValue)  ; A = cell value under the cursor ($8395)
  CP $3D                  ; column 61 = leftmost valid position
  RET Z                   ; at left wall: cannot move
  CALL HandleInput_5      ; move one step (shares forward-move code)
  JR HandleInput_4        ; keep moving while at edge
; This entry point is used by the routines at GetItemPick, UpdateJones and
; XorHAddE.
HandleInput_5:
  CALL UnhighlightMenuRow
  LD HL,CorridorCursor
  LD A,(CursorCellValue)
  CP $46
  JR C,HandleInput_6
  INC (HL)
HandleInput_6:
  LD HL,CorridorCursor
  LD A,(HL)
  CP $12
  JR C,HandleInput_7
  LD A,$FF
HandleInput_7:
  INC A
  LD (HL),A
  LD D,$00
  LD E,A
  LD HL,CorridorPosTable
  ADD HL,DE
  LD A,(HL)
  CP $52
  JR NC,HandleInput_6
  CALL HighlightMenuRow
  CALL UpdateRoomMarkerB
  RET
HandleInput_8:
  CALL UnhighlightMenuRow
HandleInput_9:
  LD A,(CorridorCursor)
  AND A
  JR NZ,HandleInput_10
  LD A,$13
HandleInput_10:
  DEC A
  LD (CorridorCursor),A
  LD D,$00
  LD E,A
  LD HL,CorridorPosTable
  ADD HL,DE
  LD A,(HL)
  CP $52
  JR NC,HandleInput_9
  CP $46
  JR C,HandleInput_11
  DEC HL
  LD A,(HL)
  CP $46
  JR C,HandleInput_11
  CP $52
  JR NC,HandleInput_11
  LD HL,CorridorCursor
  DEC (HL)
HandleInput_11:
  CALL HighlightMenuRow
  CALL UpdateRoomMarkerB
  RET
HandleInput_12:
  XOR A                   ; action key released:
  OUT ($FE),A             ; restore black border
  LD HL,RoomDispatchTable ; RoomDispatchTable
  LD A,(RoomModeByte)     ; A = current room mode
  LD E,A
  LD D,$00
  ADD HL,DE
  ADD HL,DE               ; HL = table + mode*2
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
  JP (HL)                 ; jump to the room's action handler
; UnhighlightMenuRow: undo the cursor highlight — restore the saved attribute
; (SavedRowAttr) on the row under the cursor; two rows when the cell value is
; >= 70 (first word of a two-row label).
UnhighlightMenuRow:
  LD A,(CorridorCursor)   ; A = cursor row
  CALL GetMenuRowAttrAddr ; HL = its attribute address
  LD A,(CursorCellValue)  ; cell value >= 70 (two-row label)?
  LD C,$01
  CP $46
  JR C,UnhighlightMenuRow_Restore
  INC C                   ; yes: restore two rows
UnhighlightMenuRow_Restore:
  LD A,(SavedRowAttr)     ; repaint the saved attribute
  CALL FillAttrColumn
  RET
; This entry point is used by the routines at OrdersMenuNextRow, DrawSprite,
; DrawLocationList and GetItemPick. HighlightMenuRow: mirror the
; CorridorPosTable cell under the cursor into CursorCellValue ($8395), save the
; attribute under the cursor row into SavedRowAttr, then paint the row bright
; white (attr 120) — two rows when the cell value is >= 70 (two-row label).
HighlightMenuRow:
  LD A,(CorridorCursor)   ; A = cursor row
  PUSH AF
  LD E,A
  LD D,$00
  LD HL,CorridorPosTable
  ADD HL,DE
  LD A,(HL)               ; A = cell value under the cursor
  LD (CursorCellValue),A  ; mirror it for the dispatchers
  EX AF,AF'               ; keep the cell value in A'
  POP AF
  CALL GetMenuRowAttrAddr ; HL = the row's attribute address
  LD A,(HL)
  LD (SavedRowAttr),A     ; save the attribute for the un-highlight
  EX AF,AF'               ; cell value >= 70 (two-row label)?
  LD C,$01
  CP $46
  JR C,HighlightMenuRow_Paint
  INC C                   ; yes: highlight two rows
HighlightMenuRow_Paint:
  LD A,$78                ; bright white paper, black ink
  CALL FillAttrColumn
  RET
; GetMenuRowAttrAddr: HL = attribute address of menu row A — the strip rows sit
; on screen rows 1-19, columns 22-31, so row 0 -> $5836 (22582) and each
; further row adds 32.
GetMenuRowAttrAddr:
  LD HL,$5836             ; attr addr of menu row 0 (screen row 1, col 22)
  AND A
  RET Z
  LD DE,$0020
  LD B,A
GetMenuRowAttrAddrLoop:
  ADD HL,DE               ; +32 per row
  DJNZ GetMenuRowAttrAddrLoop
  RET
; UpdateRoomMarkerB: cursor-move hook, called from HandleInput's cursor
; handlers. Erases the blinking candidate-room box (marker channel B), then, in
; the room view only, re-places it if the row now under the cursor is a room
; (an exit row, value < 34) that lies on the deck currently shown in the map
; pane.
UpdateRoomMarkerB:
  CALL EraseRoomMarkerB   ; erase/freeze marker channel B
  LD A,(RoomModeByte)     ; room view (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(CursorCellValue)  ; cell under the cursor a room id?
  CP $22
  RET NC                  ; no: done
  LD E,A
  LD D,$00
  LD HL,RoomTypeTable     ; RoomTypeTable[room]
  ADD HL,DE
  LD A,(RoomTypeByte)     ; on the deck currently displayed?
  CP (HL)
  RET NZ                  ; no: done
  LD A,(CursorCellValue)  ; yes: blink that room's indicator box
  CALL PlaceRoomMarkerB
  RET

; AnimateCrewA
;
; Animates marker channel A — the walking crew figure that marks the viewed
; room on the deck map (placed by PlaceRoomMarkerA PlaceRoomMarkerA). Skips if
; bit 7 of the animation-freeze flag at CrewAnimFlags is set. Decrements the
; frame counter at AnimFrameCnt; when it reaches zero, resets the counter to 10
; and XOR-blits two consecutive 3×12-byte sprite frames from the animation
; table at CrewWalkAnimA (indexed by the frame phase in CrewAnimFlags bits 0-1)
; onto the display at the address stored in SprDestA (SprDestA). Calling this
; routine twice per frame (once to erase, once to draw the next frame) produces
; flicker-free sprite animation via double-buffered XOR.
;
; USED by the routines at GameEntry, UpdateRoomActors and DrawShipMap.
AnimateCrewA:
  LD A,(CrewAnimFlags)    ; A = animation-freeze / frame-phase byte
                          ; (CrewAnimFlags)
  BIT 7,A                 ; bit 7 = freeze flag
  RET NZ                  ; frozen: skip animation this frame
  LD HL,FrameCounterA     ; HL = frame counter A (AnimFrameCnt)
  DEC (HL)                ; tick down counter
  RET NZ                  ; not expired: nothing to do
  LD (HL),$0A             ; reset counter to 10 frames per phase
  LD B,$02                ; draw 2 sprite rows
; This entry point is used by the routines at AnimateCrewB and DrawShipMap.
AnimateCrewA_0:
  PUSH BC                 ; save row counter
  LD E,A                  ; E = frame phase (0-3)
  LD D,$00
  LD HL,CrewWalkAnimA     ; HL = base of crew-A animation table (CrewWalkAnimA)
  ADD HL,DE               ; index by phase × 2
  ADD HL,DE
  LD A,(HL)               ; read 16-bit sprite bitmap pointer
  INC HL
  LD H,(HL)
  LD L,A                  ; HL = pointer to 3-byte sprite row data
  EX DE,HL                ; DE = sprite data source
  LD HL,(SprDestA)        ; HL = screen destination address (SprDestA)
  CALL AnimateCrewB_1     ; XOR-blit 3 bytes × 12 rows onto screen
  POP BC                  ; restore row counter
  DEC B
  RET Z                   ; done after 2 rows
  LD A,(CrewAnimFlags)    ; advance to next frame phase
  INC A
  AND $03                 ; wrap phase 0-3
  LD (CrewAnimFlags),A    ; store updated phase
  JR AnimateCrewA_0       ; blit second sprite row

; AnimateCrewB
;
; Animates marker channel B — the blinking box cursor on a candidate room
; (placed by PlaceRoomMarkerB PlaceRoomMarkerB). Identical logic to
; AnimateCrewA (AnimateCrewA) but uses a faster frame rate (counter resets to 5
; rather than 10), the B-set animation table at CrewWalkAnimB, and the
; B-channel screen address at SpriteDestAddr (SpriteDestAddr). Cycles through
; four cursor shapes (RoomCursor0-3) at twice the speed of the crew walk
; animation.
;
; USED by the routines at GameEntry, UpdateRoomActors and DrawShipMap.
AnimateCrewB:
  LD A,(AnimFramePhase)   ; A = animation-freeze / frame-phase byte
                          ; (AnimFramePhase)
  BIT 7,A                 ; bit 7 = freeze flag
  RET NZ                  ; frozen: skip
  LD HL,FrameCounterB     ; HL = frame counter B ($83CA)
  DEC (HL)                ; tick down counter
  RET NZ                  ; not expired
  LD (HL),$05             ; reset to 5 frames per phase (2× speed of set A)
  LD B,$02                ; draw 2 sprite rows
; This entry point is used by the routine at DrawShipMap.
AnimateCrewB_0:
  PUSH BC
  LD E,A                  ; E = frame phase (0-3)
  LD D,$00
  LD HL,CrewWalkAnimB     ; base of crew-B animation table (CrewWalkAnimB)
  ADD HL,DE               ; index by phase × 2
  ADD HL,DE
  LD A,(HL)               ; read 16-bit sprite bitmap pointer
  INC HL
  LD H,(HL)
  LD L,A                  ; HL = pointer to sprite row data
  EX DE,HL                ; DE = sprite data source
  LD HL,(SpriteDestAddr)  ; HL = screen destination address (SpriteDestAddr)
  CALL AnimateCrewB_1     ; XOR-blit 3 bytes × 12 rows onto screen
  POP BC
  DEC B
  RET Z
  LD A,(AnimFramePhase)   ; advance frame phase
  INC A
  AND $03                 ; wrap 0-3
  LD (AnimFramePhase),A
  JR AnimateCrewB_0
; This entry point is used by the routine at AnimateCrewA.
AnimateCrewB_1:
  LD C,$0C
AnimateCrewB_2:
  LD B,$03
AnimateCrewB_3:
  LD A,(DE)
  XOR (HL)
  LD (HL),A
  INC HL
  INC DE
  DJNZ AnimateCrewB_3
  DEC HL
  DEC HL
  DEC HL
  PUSH DE
  CALL DrawSpriteRow_2
  POP DE
  DEC C
  JR NZ,AnimateCrewB_2
  RET
; GetRoomMarkerAddr: A = room id (0-33) -> HL = display-file address of the
; room's indicator box on its deck map, from RoomMarkerAddrTable
; (RoomMarkerAddrTable).
GetRoomMarkerAddr:
  ADD A,A
  LD D,$00
  LD E,A
  LD HL,RoomMarkerAddrTable ; RoomMarkerAddrTable + room*2
  ADD HL,DE
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A                  ; HL = the room's marker screen address
  RET
; This entry point is used by the routine at DrawSprite. PlaceRoomMarkerA: aim
; marker channel A (the walking crew figure that marks the viewed room on the
; deck map) at the current room (CurrentRoom) and draw its first frame; no
; marker on the Narcissus (deck type 3). Channel A then keeps animating via
; AnimateCrewA (AnimateCrewA).
PlaceRoomMarkerA:
  LD A,(RoomTypeByte)     ; on the Narcissus (type 3)?
  CP $03
  RET NC                  ; yes: no marker
  LD A,(CurrentRoom)      ; A = the viewed room
  CALL GetRoomMarkerAddr
  LD (SprDestA),HL        ; channel A dest = its marker address
  LD HL,CrewAnimFlags
  RES 7,(HL)              ; unfreeze channel A
  LD A,(HL)               ; A = current phase
  LD B,$01
  CALL AnimateCrewA_0     ; XOR-blit one frame (place the box)
  RET
; This entry point is used by the routines at HandleInput and
; LocationListAction. PlaceRoomMarkerB: aim marker channel B (the "candidate
; room" blinker) at room A and draw its first frame. Used for the exit row
; under the cursor in the room view (UpdateRoomMarkerB UpdateRoomMarkerB) and
; for a room picked from the Indicate Location list (LocationListAction
; LocationListAction). Channel B then keeps blinking via AnimateCrewB
; (AnimateCrewB).
PlaceRoomMarkerB:
  CALL GetRoomMarkerAddr  ; HL = marker address of room A
  LD (SpriteDestAddr),HL  ; channel B dest = it
  LD HL,AnimFramePhase
  RES 7,(HL)              ; unfreeze channel B
  LD A,(HL)               ; A = current phase
  LD B,$01
  CALL AnimateCrewB_0     ; XOR-blit one frame (place the box)
  RET
; This entry point is used by the routine at HandleInput. EraseRoomMarkerB: if
; marker channel B is live, XOR-blit its current frame once more (which removes
; it from the screen) and freeze the channel.
EraseRoomMarkerB:
  LD HL,AnimFramePhase
  BIT 7,(HL)              ; already frozen?
  RET NZ                  ; yes: nothing to erase
  PUSH HL
  LD A,(HL)               ; A = current phase
  LD B,$01
  CALL AnimateCrewB_0     ; XOR-blit once = erase
  POP HL
  SET 7,(HL)              ; freeze the channel
  RET

; OpenCrewRoomView — orders-screen rows 1-7 (the crew names)
;
; Opens the room view for the crew member on the chosen row. The cursor row IS
; the actor slot number (1-7); it is saved as the selected slot (DrawSlotIndex)
; for everything that follows (menu dispatch, action timers, the alien-presence
; check at ForceAttackRowIfHostile). The menu cursor is parked on the action
; menu's QUIT row (17, value 61). SelectSlotByDrawIdx (SelectSlotByDrawIdx)
; then points IX at the slot's ActorRecords entry and caches its current room
; (CurrentRoom) and deck type (RoomTypeByte). Bails back to the orders screen
; (InitGameView) if the slot has been removed (IX+7 nonzero) or if the alien is
; loose and currently targeting this crew member (the encounter sequence owns
; the screen); otherwise builds the room view via RedrawRoomScene
; (RedrawRoomScene).
OpenCrewRoomView:
  LD A,(CorridorCursor)   ; A = cursor row = the actor slot picked (1-7)
  LD (DrawSlotIndex),A    ; save as the selected slot (DrawSlotIndex)
  LD A,$11
  LD (CorridorCursor),A   ; park the cursor on the QUIT row (17)
  LD A,$3D
  LD (CursorCellValue),A  ; mirrored cell value = 61 "   QUIT   " ($8395)
  CALL SelectSlotByDrawIdx ; IX = slot record; cache its room + deck type
  XOR A
  CP (IX+$07)             ; slot removed (+7 nonzero)?
  JP NZ,InitGameView      ; yes: back to the orders screen
  LD A,(AlienActiveFlag)  ; alien loose (AlienActiveFlag)?
  AND A
  JP Z,RedrawRoomScene    ; no: build the room view
  LD HL,AlienTargetID     ; HL = alien's target slot ($83C7)
  LD A,(DrawSlotIndex)    ; targeting the crew member just picked?
  CP (HL)
  JP Z,InitGameView       ; yes: bounce back to the orders screen
  CALL RedrawRoomScene    ; no: build the room view
  RET

; SelectDeckMap — orders-screen rows 12-14 ("Upper Deck"/"MiddleDeck"/ "Lower
; Deck"): redraw the map pane to show that deck. The cursor row minus 12 is
; exactly the deck's layout type (0/1/2), which is stored as RoomTypeByte and
; handed to the map-pane drawer at DrawRoomBackground.
SelectDeckMap:
  LD A,(CorridorCursor)   ; A = cursor row (12-14)
  SUB $0C                 ; -> deck layout type 0-2
  LD (RoomTypeByte),A     ; RoomTypeByte = chosen deck
  CALL DrawRoomBackground ; redraw the map pane for that deck
  RET

; IndicateLocation — orders-screen row 8 (" Indicate "/" Location ")
;
; Opens the room-location list: replaces the menu strip with a pageable list of
; room names. Page 1 (room mode 2, entered here) lists rooms 0-16; page 2 (room
; mode 3, entered at IndicateLocationPage2) lists rooms 17-33 — the Narcissus
; (34) is never listed. The 19 CorridorPosTable cells become: cell 0 = 62
; "Other List" (switches to the other page), cells 1-17 = the seventeen room
; ids, cell 18 = 61 "   QUIT   " (returns to the orders screen). The cursor
; starts on QUIT. While the list is up, the action key dispatches straight to
; LocationListAction (LocationListAction) via RoomDispatchTable entries 2/3:
; picking a room draws its deck map in the map pane with the room's indicator
; box blinking on it (marker channel B).
;
; USED by the routine at LocationListAction.
IndicateLocation:
  LD A,$02                ; room mode 2: page 1,
  LD E,$00                ; first room id 0
  JR BuildLocationList
; This entry point is used by the routine at LocationListAction.
IndicateLocationPage2:
  LD A,$03                ; room mode 3: page 2,
  LD E,$11                ; first room id 17
BuildLocationList:
  LD (RoomModeByte),A     ; set the room mode (2 or 3)
  LD A,$12
  LD (CorridorCursor),A   ; cursor to row 18 (the QUIT cell)
  LD A,$3D
  LD (CursorCellValue),A  ; mirrored cell value = 61 "   QUIT   "
  LD HL,CorridorPosTable  ; fill CorridorPosTable:
  LD (HL),$3E             ; cell 0 = 62 "Other List" (page toggle)
  INC HL
  LD B,$11
BuildLocationListLoop:
  LD (HL),E               ; cells 1-17 = room ids E..E+16
  INC HL
  INC E
  DJNZ BuildLocationListLoop
  LD (HL),$3D             ; cell 18 = 61 "   QUIT   "
  LD HL,$5836             ; colour the strip (attr rows 1-19, cols 22-31):
  LD A,$38
  LD C,$01                ; "Other List" row: black ink on white
  CALL FillAttrColumn
  LD A,$0F
  LD C,$11                ; the 17 room rows: white ink on blue
  CALL FillAttrColumn
  LD A,$38
  LD C,$01                ; QUIT row: black ink on white
  CALL FillAttrColumn
  LD HL,$4016             ; draw pointer to row 0, column 22
  LD (DrawScreenPtr),HL   ; (fall through to draw the list)

; DrawLocationList
;
; Renders the room-location list built above: the " Location " title on screen
; row 0, then the 19 CorridorPosTable cells (room names and the two control
; rows) on rows 1-19, and finally the cursor highlight via HighlightMenuRow
; (HighlightMenuRow).
DrawLocationList:
  LD A,$50                ; draw title 80 " Location " on row 0
  CALL DrawSprite
  LD HL,CorridorPosTable  ; then the 19 cells from CorridorPosTable
  LD (CorridorTilePtr),HL
  LD B,$13
DrawLocationListLoop:
  PUSH BC
  CALL DrawNextCorridorTile ; draw next cell (room name / control row)
  POP BC
  DJNZ DrawLocationListLoop
  CALL HighlightMenuRow   ; highlight the cursor row / mirror its value
  RET

; RoomModeDispatch
;
; Menu-row dispatch for room modes 0 and 1: re-dispatches the action key on the
; cursor ROW (CorridorCursor) through the second half of the same word table —
; base $840C for mode 0 (the orders screen) or $8430 for mode 1 (the room view;
; see the row list at the RoomDispatchTable header).
RoomModeDispatch:
  CALL PreDispatchHook    ; A = room mode (0 or 1 here)
  LD HL,$840C             ; mode-0 row-handler base
  LD DE,$0024
  AND A
  JR Z,DispatchOnCursorRow
  ADD HL,DE               ; mode 1: the room-view row table
DispatchOnCursorRow:
  LD A,(CorridorCursor)   ; cursor row
  LD E,A
  ADD HL,DE
  ADD HL,DE               ; base + row*2
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
  JP (HL)                 ; jump to the row's handler

; LocationListAction — action key in the room-location list (modes 2/3)
;
; Dispatched directly from RoomDispatchTable entries 2 and 3, on the cell value
; under the cursor: 61 "   QUIT   " leaves the list and rebuilds the orders
; screen (InitGameView InitGameView); 62 "Other List" switches to the other
; page (mode 2 -> page 2, mode 3 -> page 1); any other value is a room id —
; show where that room is: look up its deck in RoomTypeTable (RoomTypeTable),
; make it the displayed deck (RoomTypeByte), redraw the map pane and set the
; room's indicator box blinking on it (marker channel B via PlaceRoomMarkerB
; PlaceRoomMarkerB). The list stays up, so several rooms can be looked up in a
; row; note the displayed deck keeps its last value after QUIT.
LocationListAction:
  LD A,(CursorCellValue)  ; A = cell value under the cursor
  CP $3D                  ; "   QUIT   "?
  JP Z,InitGameView       ; yes: back to the orders screen
  CP $3E                  ; "Other List"?
  JR NZ,LocationListShowRoom
  LD A,(RoomModeByte)     ; yes: toggle page —
  CP $02                  ; on page 1 (mode 2)?
  JP Z,IndicateLocationPage2 ; build page 2
  JP IndicateLocation     ; else build page 1
LocationListShowRoom:
  LD E,A                  ; a room id was picked:
  LD D,$00
  LD HL,RoomTypeTable     ; RoomTypeTable[room]
  ADD HL,DE
  LD A,(HL)
  LD (RoomTypeByte),A     ; = the deck to display
  CALL DrawRoomBackground ; redraw the map pane for that deck
  LD A,(CursorCellValue)  ; and blink the room's indicator box
  CALL PlaceRoomMarkerB   ; on it (marker channel B)
  RET

; MoveToRoom — action on a "move to:" destination row (2-6): set the actor's
; destination room from the row's value; in the ducts the destination comes
; from RoomAdjDucts by direction slot instead.
MoveToRoom:
  LD A,$40
  CALL SetActionTimer     ; set the actor's action timer
  LD (IX+$03),$01         ; action state = 1
  LD A,(CurrentRoom)
  BIT 6,A                 ; in the duct network?
  JR NZ,MoveToRoom_Duct
  LD A,(CursorCellValue)  ; no: destination = the row's room number
  LD (IX+$02),A
  RET
MoveToRoom_Duct:
  LD A,(CorridorCursor)   ; yes: duct direction = row - 2
  SUB $02
  PUSH AF
  LD A,(CurrentRoom)
  AND $3F
  LD L,A
  LD H,$00
  PUSH HL
  ADD HL,HL
  ADD HL,HL
  POP DE
  ADD HL,DE               ; room * 5
  LD DE,RoomAdjDucts      ; + RoomAdjDucts
  ADD HL,DE
  POP AF
  LD D,$00
  LD E,A
  ADD HL,DE               ; + direction slot
  LD A,(HL)
  OR $40                  ; destination stays inside the ducts
  LD (IX+$02),A
  RET
; QuitRoomView — action on the QUIT row (17): freeze the crew animation and
; return to the ship view.
QuitRoomView:
  LD HL,CrewAnimFlags
  SET 7,(HL)
  JP InitGameView

; Routine at RoomHandlerNop
RoomHandlerNop:
  RET

; Routine at RoomHandlerNop2
RoomHandlerNop2:
  RET
  RET

; SwapHeldItems
;
; Action on the back-item row (9): swap the actor's front and back hands —
; exchange HeldItemFront/HeldItemBack, flip the two markers in ItemLocations
; (ItemLocations) by +/-32, adjust the sprite position by the difference of the
; two courage bonuses, and redraw the front item's name.
SwapHeldItems:
  CALL RefreshHeldItems   ; refresh HeldItemFront/Back for this actor
  LD (IX+$00),$00
  LD DE,(HeldItemFront)   ; E = front item, D = back item
  LD HL,ItemCourageBonus  ; ItemCourageBonus:
  PUSH HL
  LD C,E
  LD B,$00
  ADD HL,BC
  LD E,(HL)               ; E = bonus[front]
  LD C,D
  POP HL
  ADD HL,BC
  LD D,(HL)               ; D = bonus[back]
  LD A,(IX+$05)           ; courage += bonus[back] - bonus[front]
  SUB E
  ADD A,D
  CP $7F                  ; (clamp underflow to 0)
  JR C,SwapItems_SetCourage
  XOR A
SwapItems_SetCourage:
  LD (IX+$05),A
  LD A,(IX+$06)           ; morale likewise
  SUB E
  ADD A,D
  CP $7F
  JR C,SwapItems_SetMorale
  XOR A
SwapItems_SetMorale:
  LD (IX+$06),A
  LD BC,(HeldItemFront)   ; C = front item, B = back item
  LD L,B                  ; swap HeldItemFront <-> HeldItemBack
  LD H,C
  LD (HeldItemFront),HL
  LD HL,ItemLocations     ; ItemLocations:
  PUSH HL
  LD E,C                  ; old front item's marker -32
  LD D,$00                ; (160+slot -> 128+slot: now in the back hand)
  ADD HL,DE
  LD A,(HL)
  SUB $20
  LD (HL),A
  POP HL
  LD E,B                  ; old back item's marker +32
  ADD HL,DE               ; (128+slot -> 160+slot: now in front)
  LD A,(HL)
  ADD A,$20
  LD (HL),A
  LD HL,$4836             ; redraw the " use:" rows:
  LD (DrawScreenPtr),HL
  LD A,(HeldItemFront)
  CALL ItemIdToString
  CALL DrawSprite         ; draw the new front item's name
  LD A,(HeldItemBack)
  CALL ItemIdToString     ; and the new back item's name
  CALL DrawSprite
  CALL DrawCrewCondition
  CALL GameEntry_21
  RET

; MoveThroughGrille
;
; Action on the "Grille" move-to row (1, offered once the room's grille is
; removed): send the actor through the duct opening — the destination is the
; same room with the in-ducts flag (bit 6) flipped.
;
; USED by the routine at GameEntry.
MoveThroughGrille:
  LD A,$40
  CALL SetActionTimer     ; set the actor's action timer
  LD (IX+$03),$02         ; action state = 2 (move)
  LD A,(CurrentRoom)
  XOR $40                 ; same room, other side of the grille
  LD (IX+$02),A
  RET

; GetItemPick
;
; Room mode 4: the "Get Item" picker strip is up (see GetItemMenu, GetItemMenu)
; and the action key fires on a row. QUIT rebuilds the room scene; otherwise
; the picked row's entry in RoomItemList (RoomItemList) is the item id to take:
; it goes into the front hand (marker 160+slot in ItemLocations
; (ItemLocations), courage/morale raised by ItemCourageBonus) if that hand is
; free, else into the back hand (128+slot).
GetItemPick:
  LD A,(CursorCellValue)  ; row value under the cursor
  CP $3D                  ; QUIT?
  JP Z,RedrawRoomScene
  LD A,(CorridorCursor)   ; cursor row
  LD E,A
  LD D,$00
  LD HL,RoomItemList      ; E = RoomItemList[row] = picked item id
  ADD HL,DE
  LD E,(HL)
  LD C,$80                ; assume back hand (marker = slot+128)
  LD A,(HeldItemFront)    ; front hand free?
  CP $FF
  JR NZ,GetItemPick_Take
  LD HL,ItemCourageBonus  ; yes: raise courage and morale
  ADD HL,DE               ; by ItemCourageBonus[item]
  LD A,(IX+$05)
  ADD A,(HL)
  LD (IX+$05),A
  LD A,(IX+$06)
  ADD A,(HL)
  LD (IX+$06),A
  LD C,$A0                ; and use the front hand (slot+160)
GetItemPick_Take:
  LD A,(DrawSlotIndex)    ; ItemLocations[item] = hand marker:
  ADD A,C                 ; the item is now carried
  LD HL,ItemLocations
  ADD HL,DE
  LD (HL),A
  JP RedrawRoomScene      ; rebuild the room scene
; GetItemMenu — action on the "Get Item" row (10): rebuild the strip as an item
; picker listing the room's items — copy RoomItemList (RoomItemList) into
; CorridorPosTable, converting each id to its name string via ItemIdToString
; (ItemIdToString) — put the cursor on the QUIT row and switch to room mode 4
; (GetItemPick above handles the selection).
GetItemMenu:
  LD (IX+$00),$00         ; clear the actor's action timer
  LD DE,CorridorPosTable  ; DE -> CorridorPosTable
  LD HL,RoomItemList      ; HL -> RoomItemList
  LD B,$11                ; copy 17 entries
GetItemMenu_CopyLoop:
  LD A,(HL)
  CP $FF                  ; 255 = empty row, copied as-is
  JR Z,GetItemMenu_StoreCell
  PUSH DE
  CALL ItemIdToString     ; item id -> name string index
  POP DE
GetItemMenu_StoreCell:
  LD (DE),A
  INC HL
  INC DE
  DJNZ GetItemMenu_CopyLoop
  LD A,$3D                ; row 17 = "   QUIT   ",
  LD ($73CF),A            ; cursor on it
  LD (CursorCellValue),A
  LD A,$11
  LD (CorridorCursor),A
  LD HL,$5836             ; menu attributes: 17 rows white,
  LD A,$0F                ; 1 row highlighted
  LD C,$11
  CALL FillAttrColumn
  LD A,$38
  LD C,$01
  CALL FillAttrColumn
  LD HL,CorridorPosTable  ; draw the 19 menu rows
  LD (CorridorTilePtr),HL
  LD HL,$4036
  LD (DrawScreenPtr),HL
  LD B,$13
GetItemMenu_DrawLoop:
  PUSH BC
  CALL DrawNextCorridorTile
  POP BC
  DJNZ GetItemMenu_DrawLoop
  LD A,$04                ; room mode = 4 (item picker)
  LD (RoomModeByte),A
  CALL HighlightMenuRow
  RET
; LeaveItemHandler — action on the "Leave Item" row (11): drop the back-hand
; item if there is one, otherwise the front-hand item (undoing its courage
; bonus); the dropped item's ItemLocations entry becomes the current room.
LeaveItemHandler:
  LD (IX+$00),$00
  LD A,(HeldItemBack)     ; back hand loaded?
  CP $FF
  JR NZ,LeaveItem_BackHand ; yes: drop the back item
  LD A,(HeldItemFront)    ; no: drop the front item —
  LD E,A
  LD D,$00
  LD HL,ItemCourageBonus  ; undo its courage bonus
  ADD HL,DE
  LD A,(IX+$05)
  SUB (HL)
  CP $7F                  ; (clamp underflow to 0)
  JR C,LeaveItem_SetCourage
  XOR A
LeaveItem_SetCourage:
  LD (IX+$05),A
  LD A,(IX+$06)
  SUB (HL)
  CP $7F
  JR C,LeaveItem_SetMorale
  XOR A
LeaveItem_SetMorale:
  LD (IX+$06),A
LeaveItem_Drop:
  LD HL,ItemLocations     ; ItemLocations[item] = current room:
  ADD HL,DE               ; the item now lies here
  LD A,(CurrentRoom)
  LD (HL),A
  CALL RedrawRoomScene    ; rebuild the room scene
  RET
LeaveItem_BackHand:
  LD E,A                  ; E = back item id (no bonus to undo)
  LD D,$00
  JR LeaveItem_Drop
; Special-row handler (row 13, from RoomDispatchTailExtra): the shim at $B306
; sorts out the 238 marker first, then re-enters below.
SpecialRowJump:
  JP SpecialRowAction
; This entry point is used by the routine at UpdateAllCrew.
SpecialRowNormal:
  CP $34                  ; "  ATTACK  " selected?
  JP Z,StartAttack        ; yes: attack handler
; This entry point is used by the routine at UpdateAllCrew. StartGrilleRemoval:
; queue the timed RmveGrille job — action state 3 fires
; CrewAction3_RemoveGrille (CrewAction3_RemoveGrille) when the countdown
; expires.
StartGrilleRemoval:
  LD A,(DrawSlotIndex)    ; base time = GrilleTimeBySlot[slot]
  LD E,A                  ; (100-180 frames, crew-dependent)
  LD D,$00
  LD HL,GrilleTimeBySlot
  ADD HL,DE
  LD A,(HL)
  CALL SetActionTimer     ; set the action countdown
  LD (IX+$03),$03         ; action state = 3 (remove the grille)
  RET
; This entry point is used by the routines at MoveToRoom, MoveThroughGrille,
; GameEntry and XorHAddE. SetActionTimer: (IX+0) = A + ActionTimeBySlot[slot] +
; ActionTimeByStrength[(IX+4) & 7] — the shared countdown formula for every
; queued crew action (move, grille, attack, ...).
SetActionTimer:
  PUSH AF                 ; A = base time from the caller
  LD HL,ActionTimeBySlot
  LD A,(DrawSlotIndex)    ; + per-slot adjustment
  LD D,$00
  LD E,A
  ADD HL,DE
  LD C,(HL)
  LD A,(IX+$04)           ; + per-strength adjustment
  AND $07                 ; (Wounded +48, strength 3 +16, else +0)
  LD E,A
  LD HL,ActionTimeByStrength
  ADD HL,DE
  LD A,(HL)
  ADD A,C
  LD C,A
  POP AF
  ADD A,C
  LD (IX+$00),A           ; arm the countdown
  RET
  LD (IX+$00),$00
  LD A,(HeldItemFront)
  CP $11
  JR Z,GetJones_Threshold
  CP $10
  RET NZ
GetJones_Threshold:
  LD A,(DrawSlotIndex)
  LD E,A
  LD D,$00
  LD HL,JonesCatchBySlot
  ADD HL,DE
  LD E,(HL)
  LD A,(HeldItemFront)
  CP $11
  JR Z,GetJones_Roll
  LD A,E
  SUB $04
  LD E,A
GetJones_Roll:
  CALL AdvanceScriptPtr
  CP E
  JR NC,GetJones_Catch
  LD HL,JonesMoveTimer
  LD (HL),$01
  CALL UpdateJones
  RET
GetJones_Catch:
  LD A,(HeldItemFront)
  LD HL,ItemLocations
  LD E,A
  LD D,$00
  ADD HL,DE
  LD (HL),$FF
  LD E,$04
  ADD HL,DE
  LD A,(DrawSlotIndex)
  ADD A,$A0
  LD (HL),A
  CALL RefreshHeldItems
  LD A,(HeldItemFront)
  CALL ItemIdToString
  LD (MenuUseRows),A
  LD HL,$4836
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  CALL BumpCourageMorale
  LD (HL),$FF
  LD HL,$48F6
  LD (DrawScreenPtr),HL
  LD A,$FF
  LD ($73CC),A
  CALL DrawSprite
  CALL HandleInput_5
  CALL TriggerJonesAnim
  LD A,(DrawSlotIndex)
  LD L,A                  ; L = capturing crew slot
  LD A,$02                ; msg #2 "{actor} has captured Jones"
  CALL UpdateCrewAI_8
  RET

; Routine at ResetCrewTimers
;
; Used by the routine at GameEntry.
ResetCrewTimers:
  PUSH IX
  XOR A
  LD (CrewIndex),A
  LD IX,ActorRecords
  LD B,$08
CrewTimersLoop:
  PUSH BC
  LD A,(IX+$00)
  AND A
  JR Z,CrewTimersNext
  DEC A
  LD (IX+$00),A
  CALL PlayMusic_25
CrewTimersNext:
  LD DE,$0008
  ADD IX,DE
  LD HL,CrewIndex
  INC (HL)
  POP BC
  DJNZ CrewTimersLoop
  POP IX
  RET
; DispatchCrewAction Dispatches one crew/alien actor through CrewActionDispatch
; (CrewActionDispatch) based on (IX+3). Action 0 returns early. Otherwise jumps
; to the per-action handler via JP (HL). Used by the per-frame status panel
; update at PlayMusic.
DispatchCrewAction:
  CALL GameEntry_3        ; preliminary actor update
  AND A                   ; A = current action state from caller
  RET Z                   ; action 0: nothing more to do
  LD E,(IX+$03)           ; E = action state (1..7)
  LD D,$00
  LD HL,CrewActionDispatch ; HL = CrewActionDispatch base
  ADD HL,DE               ; HL += action*2 (16-bit-pointer stride)
  ADD HL,DE
  LD A,(HL)               ; HL = *(table + action*2)
  INC HL
  LD H,(HL)
  LD L,A
  JP (HL)                 ; tail-call into action handler
; CrewAction1_Idle — dispatched via CrewActionDispatch entry 1. This entry
; point is used by the routine at XorHAddE.
CrewAction1_Idle:
  LD A,(IX+$02)
  CP (IX+$01)
  RET Z
  LD (IX+$01),A
  BIT 6,A
  JR NZ,CrewIdle_AirlockCheck
  LD A,$01
  LD (SoundRequest),A
CrewIdle_AirlockCheck:
  CALL RecalcMorale
  LD A,(IX+$01)
  LD E,$02
  AND A
  JR Z,CrewIdle_AirlockVented
  CP $01
  JR NZ,CrewIdle_RefreshView
  LD E,$04
CrewIdle_AirlockVented:
  LD A,E
  LD HL,ShipSystemFlags
  AND (HL)
  JR Z,CrewIdle_RefreshView
  LD (IX+$01),$0D
CrewIdle_RefreshView:
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(CrewIndex)
  LD HL,DrawSlotIndex
  CP (HL)
  JP Z,RedrawRoomScene
  CALL RefreshCrewCondition
  RET
; CrewAction2_Move — dispatched via CrewActionDispatch entry 2.
CrewAction2_Move:
  LD E,(IX+$01)
  LD A,(IX+$05)
  BIT 6,E
  JR NZ,CrewMove_BumpCourage
  DEC A
  DEC A
CrewMove_BumpCourage:
  INC A
  CP $7F
  JR C,CrewMove_SetCourage
  XOR A
CrewMove_SetCourage:
  LD (IX+$05),A
  LD A,E
  XOR $40
  LD (IX+$01),A
  CALL RecalcMorale
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(CrewIndex)
  LD HL,DrawSlotIndex
  CP (HL)
  JP Z,RedrawRoomScene
  JP RefreshCrewCondition
; This entry point is used by the routine at GameEntry.
; CrewAction3_RemoveGrille — dispatched via CrewActionDispatch entry 3. Fires
; when a crew member's timed "RmveGrille" job completes (queued by the AI at
; $90EE or by the action-menu Special handler at $8BD3): clears the actor's
; room's byte in RoomGrilleState (RoomGrilleState), queues a sound effect, and
; rebuilds the room view if that room is on screen.
CrewAction3_RemoveGrille:
  LD A,$01                ; queue a sound effect for PlayMusic
  LD (SoundPending),A
  LD A,(IX+$01)           ; E = actor's current room
  AND $3F                 ; (mask off the in-ducts flag, bit 6)
  LD HL,RoomGrilleState
  LD E,A
  LD D,$00
  ADD HL,DE
  LD (HL),$00             ; RoomGrilleState[room] = 0: grille removed
  LD A,(RoomModeByte)     ; room mode 1 (normal room view)?
  CP $01
  RET NZ
  LD A,(CurrentRoom)      ; is the actor's room the one on screen?
  AND $3F
  CP E
  JP Z,RedrawRoomScene    ; yes: full room-view rebuild
  JP GameEntry_21
RecalcMorale:
  LD HL,$FFFF
  LD (RoomMateA),HL
  LD HL,$7387
  LD C,$01
  LD A,(CrewIndex)
  AND A
  RET Z
  EX AF,AF'
  LD DE,RoomMateA
  LD A,(IX+$01)
  LD B,$07
RecalcMorale_ScanLoop:
  CP (HL)
  JR NZ,RecalcMorale_NextSlot
  EX AF,AF'
  CP C
  JR Z,RecalcMorale_SkipSelf
  EX DE,HL
  LD (HL),C
  EX DE,HL
  INC DE
RecalcMorale_SkipSelf:
  EX AF,AF'
RecalcMorale_NextSlot:
  PUSH DE
  LD DE,$0008
  ADD HL,DE
  POP DE
  INC C
  DJNZ RecalcMorale_ScanLoop
  PUSH IX
  LD A,(CrewIndex)
  LD (NearestCrewC),A
  LD HL,(RoomMateA)
  LD (NearestCrewA),HL
  CALL MoraleFromCompanion
  LD HL,(NearestCrewA)
  LD A,(NearestCrewC)
  LD (NearestCrewB),A
  LD (NearestCrewC),HL
  CALL MoraleFromCompanion
  LD HL,(NearestCrewA)
  LD A,(NearestCrewC)
  LD (NearestCrewB),A
  LD (NearestCrewC),HL
  CALL MoraleFromCompanion
  POP IX
  RET
MoraleFromCompanion:
  XOR A
  LD (DistanceAccum),A
  LD A,(NearestCrewC)
  CP $FF
  RET Z
  LD B,A
  LD IX,ActorRecords
  LD DE,$0008
MoraleComp_FindSlot:
  ADD IX,DE
  DJNZ MoraleComp_FindSlot
  XOR A
  CP (IX+$07)
  RET NZ
  PUSH IX
  LD B,$02
  LD DE,NearestCrewA
MoraleComp_PairLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)
  CP $FF
  JR Z,MoraleComp_NextPair
  LD IX,ActorRecords
  LD DE,$0008
MoraleComp_RecSeek:
  ADD IX,DE
  DEC A
  JR NZ,MoraleComp_RecSeek
  CP (IX+$07)
  JR Z,MoraleComp_AddCourage
  INC A
  CP (IX+$07)
  JR NZ,MoraleComp_Minus
  CALL DecayCrewCourage
  POP DE
  PUSH DE
  LD A,(DE)
  LD H,A
  LD A,(NearestCrewC)
  LD L,A                  ; L = the discovering crew slot
  LD A,$14                ; msg #20 "{actor} finds {target}'s body"
  CALL UpdateCrewAI_8
  INC (IX+$07)
MoraleComp_Minus:
  LD HL,DistanceAccum
  DEC (HL)
  JR MoraleComp_NextPair
MoraleComp_AddCourage:
  LD A,(IX+$05)
  SUB $02
  LD E,A
  LD A,(DistanceAccum)
  ADD A,E
  LD (DistanceAccum),A
MoraleComp_NextPair:
  POP DE
  INC DE
  POP BC
  DJNZ MoraleComp_PairLoop
  POP IX
  LD B,$03
  LD HL,NearRoomList
  LD A,(IX+$01)
MoraleComp_RoomScan:
  CP (HL)
  JR Z,MoraleComp_RoomBonus
  INC HL
  DJNZ MoraleComp_RoomScan
  JR MoraleComp_Apply
MoraleComp_RoomBonus:
  LD HL,DistanceAccum
  INC (HL)
MoraleComp_Apply:
  LD HL,DistanceAccum
  LD A,(IX+$05)
  ADD A,(HL)
  CP $7F
  JR C,MoraleComp_SetMorale
  XOR A
MoraleComp_SetMorale:
  LD (IX+$06),A
  RET
; This entry point is used by the routine at UpdateAllCrew.
DecayCrewCourage:
  LD HL,$738B
  LD DE,$0008
  LD B,$07
; This entry point is used by the routine at UpdateAllCrew.
DecayCourageLoop:
  DEC (HL)
  LD A,$7F
  CP (HL)
  JR NC,DecayCourage_Tail
  LD (HL),$00
DecayCourage_Tail:
  JP DecayMoraleStep
  RET
; This entry point is used by the routines at XorHAddE, FrameTiming,
; DrawShipMap, ScreenTransition and PlayMusic.
BusyWait:
  LD BC,$12FF
BusyWaitLoop:
  DEC BC
  LD A,C
  OR B
  JR NZ,BusyWaitLoop
  RET

; GameEntry
;
; Main game entry point. Initialises the stack, runs the title and options
; screens, then enters the per-frame main loop. The loop calls every major
; subsystem once per frame and repeats indefinitely.
;
; USED by the routines at OptionsScreen and PauseMenu.
GameEntry:
  LD SP,$FFFA             ; stack at $FFFA (top of game RAM, below extra data)
  CALL ResetScriptPtr     ; ResetScriptPtr: seed ROM-traversal script pointer
  CALL DrawIntroScreen    ; DrawIntroScreen: animated Alien title screen
  CALL DrawShipMap        ; DrawShipMap: ship layout / room-selection screen
  CALL OptionsScreen      ; OptionsScreen: difficulty selection (keys 1-4)
  CALL ScreenTransition   ; ScreenTransition: wipe to black, set white
                          ; attributes
  CALL UpdateAllCrew      ; InitCrewPositions: sort crew records by pixel Y
  CALL InitGameView       ; InitGameView: draw initial corridor view
; Main per-frame loop body. Re-entered every frame via JR back to here.
GameEntry_0:
  CALL HandleInput        ; HandleInput: scan keyboard, dispatch crew movement
  CALL FrameTiming        ; FrameTiming: ~1/50s frame wait or play queued sound
  CALL UpdateAlien        ; UpdateAlien: alien movement & crew-attack logic
  CALL TriggerAlienEvent  ; TriggerAlienEvent: poll alien event timer
  CALL UpdateCrewAI       ; UpdateCrewAI: give first idle crew member a new
                          ; action
  CALL TickMessageQueue   ; TickMessageQueue: tick the queued per-crew
                          ; animation slots
  CALL UpdateJones        ; UpdateJones: walk Jones the cat one room when due
  CALL ResetCrewTimers    ; ResetCrewTimers / corridor pass 1
  CALL AnimateCrewA       ; AnimateCrewA: XOR-blit crew sprite set A (10-frame)
  CALL AnimateCrewB       ; AnimateCrewB: XOR-blit cursor / sprite set B
                          ; (5-frame)
  CALL PlayMusic          ; PlayMusic: emit one beeper note from current phrase
  CALL AnimateCrewA       ; AnimateCrewA: second blit erases via XOR
                          ; (double-buf)
  CALL AnimateCrewB       ; AnimateCrewB: second blit erases via XOR
  CALL TickMessageQueue   ; TickMessageQueue: second animation-tick pass
  CALL UpdateRoomActors   ; UpdateRoomActors: corridor advance / room actor
                          ; logic
  CALL DrawStatusPanel    ; DrawStatusPanel: refresh crew-alive portrait column
  LD BC,$F7FE             ; BC = port 63486 ($F7FE): keyboard half-row 1-5
  IN A,(C)                ; read key row (active-low: 0 = key pressed)
  BIT 0,A                 ; test key 1 (pause / restart)
  CALL Z,PauseMenu        ; key 1 down → open PauseMenu
  CALL AdvanceScriptPtr   ; AdvanceScriptPtr: consume next ROM script nibble
  CP $00                  ; nibble 0 = no-op
  CALL Z,AdvanceScriptPtr ; skip a second nibble
  CP $00
  CALL Z,ResetScriptPtr   ; two consecutive 0s → ResetScriptPtr (reseed from
                          ; SEED)
  JR GameEntry_0          ; tail-loop back to the start of the per-frame body
GameEntry_1:
  LD A,(IX+$03)
  CP $04
  JR NZ,GameEntry_2
  CALL PickNextRoom
  LD (IX+$03),$01
  LD (IX+$00),$14
  XOR A
  RET
GameEntry_2:
  CP $03
  RET NC
  JR GameEntry_4
; This entry point is used by the routine at ResetCrewTimers.
GameEntry_3:
  LD A,(CrewIndex)
  AND A
  CPL
  RET Z
  XOR A
  CP (IX+$03)
  RET Z
  LD A,(AlienTargetID)
  LD HL,CrewIndex
  CP (HL)
  JR Z,GameEntry_1
  XOR A
  CP (IX+$06)
  JR Z,GameEntry_9
  LD A,(IX+$03)
  CP $03
  RET NC
GameEntry_4:
  LD A,(IX+$02)
  LD HL,$7387
  LD DE,$0008
  LD BC,$0700
GameEntry_5:
  CP (HL)
  JR NZ,GameEntry_6
  INC C
GameEntry_6:
  ADD HL,DE
  DJNZ GameEntry_5
  LD A,(IX+$02)
  BIT 6,A
  JR Z,GameEntry_7
  LD A,(IX+$06)
  CP $02
  JR C,GameEntry_9
  LD A,C
  AND A
  JR NZ,GameEntry_8
  CPL
  RET
GameEntry_7:
  LD A,C
  CP $03
  JR NC,GameEntry_8
  CPL
  RET
GameEntry_8:
  LD A,$20
  CALL SetActionTimer
  XOR A
  RET
GameEntry_9:
  BIT 6,(IX+$01)
  JR Z,GameEntry_10
  LD A,(IX+$03)
  CP $01
  RET NZ
  CALL CrewAction3_RemoveGrille
  CALL MoveThroughGrille
  XOR A
  RET
GameEntry_10:
  LD HL,ItemLocations
  LD A,(CrewIndex)
  ADD A,$80
  LD E,A
  ADD A,$20
  LD D,A
  LD C,(IX+$01)
  LD B,$16
GameEntry_11:
  LD A,(HL)
  CP E
  JR Z,GameEntry_12
  CP D
  JR NZ,GameEntry_13
GameEntry_12:
  LD (HL),C
GameEntry_13:
  INC HL
  DJNZ GameEntry_11
  CALL PickNextRoom
  JR GameEntry_4
; This entry point is used by the routines at UpdateCrewAI, XorHAddE,
; UpdateRoomActors and UpdateAlien. PickNextRoom: choose the destination room
; for the actor whose record IX points at, and store it in record byte +2. The
; actor's current room is byte +1; bit 6 of +1 selects which network it is
; travelling in.
PickNextRoom:
  LD A,(IX+$01)           ; A = actor's current room (bit 6 = duct network)
  LD HL,RoomAdjCorridors  ; assume corridor adjacency map (RoomAdjCorridors)
  BIT 6,A                 ; travelling in the ducts?
  JR Z,GameEntry_14       ; no: keep corridor map
  LD HL,RoomAdjDucts      ; yes: use duct map (RoomAdjDucts)
  AND $3F                 ; strip the network flag -> room ID 0-33
GameEntry_14:
  LD E,A
  LD D,$00
  LD B,$05
GameEntry_15:
  ADD HL,DE
  DJNZ GameEntry_15       ; HL = map + room*5 (the room's 5 direction slots)
  PUSH HL
  CALL AdvanceScriptPtr   ; AdvanceScriptPtr: next ROM script nibble
  CALL ScriptNibbleToDirection ; ScriptNibbleToDirection: nibble -> slot 0-4
  LD E,A
  POP HL
  ADD HL,DE
  LD A,(HL)               ; A = destination room for that direction
  BIT 6,(IX+$01)          ; moving through the ducts?
  JR Z,GameEntry_16
  OR $40                  ; yes: tag the destination with the duct flag
GameEntry_16:
  LD (IX+$02),A           ; record +2 = destination room
  RET
; ScriptNibbleToDirection Maps a 4-bit ROM-script command (in A, from
; AdvanceScriptPtr AdvanceScriptPtr) to one of 5 direction-class codes: 0-2 → 0
; (no direction), 3-5 → 1, 6-8 → 2, 9-11 → 3, 12-15 → 4. Used to translate the
; deterministic ROM-byte stream into AI movement decisions. Used by the
; routines at UpdateJones (UpdateJones), UpdateCrewAI (UpdateCrewAI) and
; UpdateRoomActors (UpdateRoomActors).
ScriptNibbleToDirection:
  CP $03                  ; nibble 0..2?
  JR NC,GameEntry_17
  XOR A                   ; → direction 0
  RET
GameEntry_17:
  CP $06                  ; nibble 3..5?
  JR NC,GameEntry_18
  LD A,$01                ; → direction 1
  RET
GameEntry_18:
  CP $09                  ; nibble 6..8?
  JR NC,GameEntry_19
  LD A,$02                ; → direction 2
  RET
GameEntry_19:
  CP $0C                  ; nibble 9..11?
  JR NC,GameEntry_20
  LD A,$03                ; → direction 3
  RET
GameEntry_20:
  LD A,$04                ; default (12..15) → direction 4
  RET
; This entry point is used by the routines at SwapHeldItems, ResetCrewTimers
; and WrapScreenAddr.
GameEntry_21:
  LD HL,$FFFF
  LD (RoomMateA),HL
  CALL DrawRoomMates
  LD A,(CurrentRoom)
  LD HL,$737F
  CP (HL)
  JR Z,GameEntry_22
  CALL TriggerAlienXAnim
  LD A,(AlienActiveFlag)
  AND A
  RET Z
  LD HL,(AlienTargetPtr)
  INC HL
  LD A,(CurrentRoom)
  CP (HL)
  JP PlayMusic_14
GameEntry_22:
  JP PlayMusic_11
; This entry point is used by the routine at PlayMusic.
GameEntry_23:
  LD HL,DrawMenuStrip
  LD (CrewAnimFlags),HL
  LD A,$04
  LD (RoomTypeByte),A
; This entry point is used by the routine at PlayMusic.
GameEntry_24:
  LD A,$34
  LD ($73CB),A
  LD HL,$48D6
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  RET

; UpdateJones
;
; Walks Jones the cat around the ship. JonesRoom (JonesRoom) is the cat's
; current room (255 = off-map / disabled); JonesMoveTimer (JonesMoveTimer) is a
; frame countdown — when it hits zero (every 255 frames, ~5s) Jones takes one
; step along the corridor adjacency map (RoomAdjCorridors), steered by the ROM
; script via ScriptNibbleToDirection like every other wanderer.
;
; HOUSE-KEEPING around each step:
; - if Jones LEFT the room currently shown in the room view (mode 1),
; CELL 14 of the corridor strip (CorridorPosTable + 14) is cleared;
; - if a living crew member (record +7 = 0) occupies Jones's new room,
; MESSAGE #0 "{actor} sees Jones the cat" is enqueued via UpdateCrewAI_8;
; - if Jones stepped into the ALIEN's room (slot 0 byte +1), the move
; IS re-rolled — the cat refuses to share a room with the alien;
; - if Jones ENTERED the viewed room, sprite strip 51 (the cat) is
; DRAWN into corridor cell 14.
UpdateJones:
  LD A,(JonesRoom)        ; A = primary alien-event timer
  CP $FF                  ; 255 = disabled
  RET Z                   ; disabled: do nothing this frame
  LD HL,JonesMoveTimer    ; HL → JonesMoveTimer
  DEC (HL)                ; tick it down
  RET NZ                  ; not yet zero: wait another frame
  LD (HL),$FF             ; expired: reset secondary counter to 255
  EX AF,AF'               ; save A for later use
Jones_PickMove:
  LD A,(JonesRoom)        ; A = Jones's current room
  LD HL,RoomAdjCorridors  ; corridor adjacency map (RoomAdjCorridors)
  LD E,A
  LD D,$00
  LD B,$05
Jones_AdjLoop:
  ADD HL,DE
  DJNZ Jones_AdjLoop      ; HL = map + room*5
  PUSH HL
  CALL AdvanceScriptPtr   ; AdvanceScriptPtr: next ROM script nibble
  CALL ScriptNibbleToDirection ; ScriptNibbleToDirection: nibble -> slot 0-4
  LD E,A
  POP HL
  ADD HL,DE
  LD A,(HL)               ; A = Jones's destination room
  LD HL,JonesRoom
  LD (HL),A               ; JonesRoom = destination
  LD A,(RoomModeByte)
  CP $01                  ; is the room view in corridor mode 1?
  JR NZ,Jones_ScanRooms   ; no: skip the display update
  LD HL,CurrentRoom       ; HL -> currently viewed room
  EX AF,AF'               ; A = Jones's OLD room
  CP (HL)                 ; did Jones just leave the viewed room?
  JR NZ,Jones_ScanRooms   ; no: nothing to erase
  LD A,$FF
  LD ($73CC),A            ; corridor cell 14 (Jones's slot) := empty
  LD HL,$48F6
  LD (DrawScreenPtr),HL   ; draw pointer -> cell 14's screen position
  CALL DrawSprite         ; blit blank strip (erase the cat)
  LD A,(CorridorCursor)
  CP $0E                  ; player cursor parked on Jones's cell?
  CALL Z,HandleInput_5    ; yes: nudge it forward one position
  CALL TriggerJonesAnim   ; one-shot Jones (tail) animation
Jones_ScanRooms:
  LD A,(JonesRoom)        ; A = Jones's new room
  LD HL,$737F             ; slot 0 byte +1: walk the +1 (room) column
  LD BC,$0800             ; B = 8 slots, C = slot counter (0)
  LD DE,$0008             ; record stride
Jones_ScanLoop:
  CP (HL)                 ; is this actor in Jones's room?
  JR Z,Jones_Companion
Jones_ScanNext:
  INC C
  ADD HL,DE
  DJNZ Jones_ScanLoop     ; next slot
  RET                     ; nobody shares Jones's room
Jones_Companion:
  LD A,C
  AND A                   ; slot 0 = the ALIEN's room:
  JR Z,Jones_PickMove     ; the cat won't stay - re-roll the move
  PUSH HL
  PUSH DE
  LD E,$06
  ADD HL,DE               ; HL -> the slot's +7 status byte
  XOR A
  CP (HL)                 ; alive (status 0)?
  POP DE
  POP HL
  LD A,(JonesRoom)
  JR NZ,Jones_ScanNext    ; not alive: keep scanning
  XOR A                   ; A = message #0
  LD L,C                  ; L = witnessing crew slot
  LD H,$00
  CALL UpdateCrewAI_8     ; enqueue "{actor} sees Jones the cat"
  LD A,(RoomModeByte)
  CP $01                  ; corridor view mode?
  RET NZ
  LD A,(JonesRoom)
  LD HL,CurrentRoom
  CP (HL)                 ; did Jones walk into the viewed room?
  RET NZ
  LD HL,$48F6             ; yes: draw pointer -> corridor cell 14
  LD (DrawScreenPtr),HL
  LD A,$33                ; sprite strip 51 = Jones the cat
  LD ($73CC),A            ; corridor cell 14 := Jones
  CALL DrawSprite         ; blit the cat into the corridor
  CALL InitJonesAnim      ; start the Jones animation from frame 0
  RET

; UpdateCrewAI
;
; Assigns a new action to the first idle crew member. Scans the 7-entry crew
; data table at ActorRecords (IX+0 = action counter). If a crew member's
; counter is non-zero they are busy; the first one with counter = 0 gets a new
; action sourced from the ROM-traversal script via AdvanceScriptPtr
; (AdvanceScriptPtr) and ScriptNibbleToDirection (ScriptNibbleToDirection). The
; action sets IX+3 to one of: 1 = idle (action timer 80 frames) 2 = move
; (action timer 50 frames) 3 = wait (action timer 50 frames) 5 = investigate
; (action timer 60 frames) and IX+0 to the appropriate frame countdown.
;
; USED by the routine at GameEntry.
UpdateCrewAI:
  PUSH IX                 ; save caller's IX
  LD IX,ActorRecords      ; IX = crew data base (first 7-entry crew table)
  XOR A                   ; A = 0
  CP (IX+$00)             ; is crew member 0's action counter zero (idle)?
  JR Z,UpdateCrewAI_0     ; yes: assign new action at AIActionSelect
  POP IX                  ; crew member busy: restore IX
  RET                     ; return without change
UpdateCrewAI_0:
  JP AIActionSelect       ; tail-call: assign action from script byte
; This entry point is used by the routine at UpdateAllCrew.
UpdateCrewAI_1:
  LD HL,$FFFF
  LD (NearestCrewC),HL
  LD DE,NearestCrewC
  LD HL,$7387
  LD BC,$0701
UpdateCrewAI_2:
  CP (HL)
  JR NZ,UpdateCrewAI_3
  EX DE,HL
  LD (HL),C
  EX DE,HL
  INC DE
UpdateCrewAI_3:
  INC C
  PUSH DE
  LD DE,$0008
  ADD HL,DE
  POP DE
  DJNZ UpdateCrewAI_2
  LD A,(NearestCrewC)
  CP $FF
  JR Z,UpdateCrewAI_4
  LD (IX+$00),$3C         ; timer = 60 frames
  LD (IX+$03),$05         ; action state = 5 (investigate)
  POP IX
  RET
UpdateCrewAI_4:
  LD A,(IX+$01)           ; actor's current room
  AND $3F
  LD E,A
  LD D,$00
  LD HL,RoomGrilleState   ; RoomGrilleState[room]
  ADD HL,DE
  LD A,(HL)
  CP $FF                  ; grille still in place here?
  JR NZ,UpdateCrewAI_5    ; no: consider moving out instead
  CALL AdvanceScriptPtr   ; AdvanceScriptPtr → A = next script nibble
  CALL ScriptNibbleToDirection ; ScriptNibbleToDirection → A ∈ [0..4]
  AND A                   ; direction 0?
  JR NZ,UpdateCrewAI_7    ; no: branch to idle assignment
  LD (IX+$00),$32         ; timer = 50 frames
  LD (IX+$03),$03         ; action state = 3 (remove the grille)
  POP IX
  RET
UpdateCrewAI_5:
  CALL AdvanceScriptPtr   ; AdvanceScriptPtr → A
  CALL ScriptNibbleToDirection ; ScriptNibbleToDirection
  CP $02                  ; direction < 2?
  JR NC,UpdateCrewAI_7    ; no: idle
; This entry point is used by the routine at UpdateAllCrew.
UpdateCrewAI_6:
  LD A,(IX+$01)
  XOR $40
  LD (IX+$02),A
  LD (IX+$00),$32         ; timer = 50 frames
  LD (IX+$03),$02         ; action state = 2 (move)
  POP IX
  RET
UpdateCrewAI_7:
  CALL PickNextRoom
  LD (IX+$00),$50         ; timer = 80 frames
  LD (IX+$03),$01         ; action state = 1 (idle / wander)
  LD A,(IX+$01)
  CP (IX+$02)
  LD A,$01
  CALL Z,AddAlienRoomDamage
  POP IX
  RET
; This entry point is used by the routines at GetItemPick, ResetCrewTimers,
; UpdateJones, XorHAddE, UpdateRoomActors, PlayMusicPhrase, UpdateAlien,
; TriggerAlienEvent and UpdateAllCrew.
UpdateCrewAI_8:
  EX DE,HL
  LD HL,(MsgLastEnqueued)
  CP L
  JR NZ,UpdateCrewAI_9
  LD A,E
  CP H
  RET Z
  LD A,L
UpdateCrewAI_9:
  EX AF,AF'
  LD HL,(MsgQueueWritePtr)
  LD A,(HL)
  CP $FF
  RET Z
  EX AF,AF'
  LD (HL),A
  INC HL
  LD (HL),E
  INC HL
  LD (HL),D
  INC HL
  LD (MsgQueueWritePtr),HL
  LD L,A
  LD H,E
  LD (MsgLastEnqueued),HL
  LD A,(MsgVisibleTimer)
  AND A
  RET Z
  LD A,$01
  LD (MsgVisibleTimer),A
  RET
; This entry point is used by the routine at TickMessageQueue.
RenderMessage:
  LD HL,Screen_R23C0      ; HL = screen address for bottom status line
  LD (DrawScreenPtr),HL
  LD DE,MessageTextTable
  AND A
  JR Z,UpdateCrewAI_11
  LD B,A
UpdateCrewAI_10:
  LD A,(DE)
  INC DE
  CP $FF
  JR NZ,UpdateCrewAI_10
  DJNZ UpdateCrewAI_10
UpdateCrewAI_11:
  LD A,(DE)
  CP $FF
  JR Z,UpdateCrewAI_16
  CP $FE
  JR Z,UpdateCrewAI_12
  CP $FD
  JR Z,UpdateCrewAI_13
  CP $FC
  JR Z,UpdateCrewAI_15
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  JR UpdateCrewAI_11
UpdateCrewAI_12:
  LD A,(MsgParam1)
  JR UpdateCrewAI_14
UpdateCrewAI_13:
  LD A,(MsgParam2)
UpdateCrewAI_14:
  INC DE
  PUSH DE
  CALL PrintName
  POP DE
  JR UpdateCrewAI_11
UpdateCrewAI_15:
  INC DE
  PUSH DE
  LD A,(MsgParam1)
  LD HL,(DrawScreenPtr)
  PUSH HL
  CALL DrawSprite
  POP HL
  LD DE,$000A
  ADD HL,DE
  LD (DrawScreenPtr),HL
  POP DE
  JR UpdateCrewAI_11
UpdateCrewAI_16:
  LD HL,(DrawScreenPtr)
  LD A,L
  AND $1F
  OR $80
  LD (MsgDrawnCol),A
  RET
; This entry point is used by the routine at UpdateAllCrew.
UpdateCrewAI_17:
  LD HL,Screen_R23C0
  LD (DrawScreenPtr),HL
  LD B,$20
UpdateCrewAI_18:
  PUSH BC
  XOR A
  CALL DrawSpriteRow_0
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ UpdateCrewAI_18
  LD HL,MsgLineAttrs
  LD (HL),$12
  LD (MsgAttrPtr),HL
  INC HL
  LD B,$1F
UpdateCrewAI_19:
  LD (HL),$00
  INC HL
  DJNZ UpdateCrewAI_19
  LD HL,MsgVisibleTimer
  LD (HL),B
  RET

; TickMessageQueue
;
; Per-frame ticker for the on-screen status-message display. Decrements the
; message-visible countdown at MsgVisibleTimer; when it expires, peeks at the
; queued-message head at MsgDrawnCol and either swaps in the next queued
; message (copying a 21-byte entry from MessageQueue to MessageQueueBody and
; re-invoking the renderer at RenderMessage) or clears the message line. Called
; twice per frame from the main loop at GameEntry.
;
; USED by the routine at GameEntry.
TickMessageQueue:
  LD A,(MsgVisibleTimer)  ; A = current message-visible timer
  AND A
  JR Z,MsgQueue_Fetch
  DEC A
  LD (MsgVisibleTimer),A
  RET NZ
  LD A,(MsgAttrPtr)
  AND $1F
  LD (MsgDrawnCol),A
  RET
MsgQueue_Fetch:
  LD A,(MsgDrawnCol)
  AND A
  JR NZ,MsgLine_Sweep
  LD A,(MsgQueueWritePtr)
  LD HL,MessageQueue
  CP L
  JR NZ,MsgQueue_Dequeue
  RET
MsgQueue_Dequeue:
  LD A,(HL)
  INC HL
  LD E,(HL)
  INC HL
  LD D,(HL)
  LD (MsgParam1),DE
  LD BC,$0015
  LD DE,MessageQueue
  LD HL,MessageQueueBody
  LDIR
  LD HL,(MsgQueueWritePtr)
  DEC HL
  DEC HL
  DEC HL
  LD (MsgQueueWritePtr),HL
  CALL RenderMessage
  RET
MsgLine_Sweep:
  BIT 7,A
  JR Z,MsgLine_SweepBack
  LD HL,(MsgAttrPtr)
  LD (HL),$28
  INC HL
  LD (HL),$52
  LD (MsgAttrPtr),HL
  DEC A
  CP $80
  JR Z,MsgLine_SetTimer
  LD (MsgDrawnCol),A
  RET
MsgLine_SetTimer:
  XOR A
  LD (MsgDrawnCol),A
  LD A,(MsgQueueWritePtr)
  LD HL,MessageQueue
  CP L
  LD A,$32
  JR Z,MsgLine_StoreTimer
  LD A,$02
MsgLine_StoreTimer:
  LD (MsgVisibleTimer),A
  RET
MsgLine_SweepBack:
  LD HL,(MsgAttrPtr)
  LD (HL),$00
  DEC HL
  LD (HL),$52
  LD (MsgAttrPtr),HL
  DEC A
  DEFB $32                ; opcode of "LD (msg-head var),A": its operands are
                          ; the XOR H / ADD A,E at XorHAddE, so falling through
                          ; stores A and hits that entry's RET

; Routine at XorHAddE
XorHAddE:
  XOR H
  ADD A,E
  RET
; This entry point is used by the routine at GetItemPick. StartAttack — action
; on the "  ATTACK  " row: queue the combat job — action state 4
; (CrewAction4_Handler -> CrewHitsAlien / CrewHitsAndroid) with base countdown
; 40, replacing any pending action. Then put the special row back to what the
; room warrants (49 "RmveGrille" or blank, via the grille-state refresh at
; $B0B8) and move the cursor on.
StartAttack:
  LD A,$28                ; base countdown 40
  CALL SetActionTimer     ; arm it (SetActionTimer)
  LD (IX+$03),$04         ; action state = 4 (combat)
  LD A,$FF                ; (dead: A is overwritten inside the call)
  CALL RefreshSpecialRow  ; cell 13 = 49 or blank per RoomGrilleState
  LD HL,$48D6             ; and redraw the special row
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  CALL HandleInput_5      ; advance the cursor off the row
  RET
; CrewAction4_Handler — dispatched via CrewActionDispatch entry 4 (combat).
; Branches a combat-state actor into one of two parallel handlers based on
; whose sprite-class byte (+1) matches the actor's: 1. If the actor's (IX+1)
; matches the alien slot's identity at $737F → CALL CrewHitsAlien
; (CrewHitsAlien) — message #3 "nets the ALIEN" or #4 "hits the ALIEN". 2.
; Else, if AlienActiveFlag is non-zero AND the actor's (IX+1) matches the
; alien-target slot's (IX+1) — i.e. the **Android** slot picked by LongGameInit
; (LongGameInit) via LongGameTargetTable (LongGameTargetTable) and pointed to
; by AlienTargetPtr (AlienTargetPtr) — → CALL CrewHitsAndroid (CrewHitsAndroid)
; — message #31 "nets {target}" or #32 "hits the Android". The Android role:
; LongGameInit randomly picks one of crew slots {1, 2, 4, 6} (Dallas / Kane /
; Ash / Parker) and stores it in AlienTargetID (AlienTargetID). That crew
; member is dormant at first — they appear and behave like a normal crew
; member. UpdateAlien (UpdateAlien) turns them hostile the first frame the
; alien is loose and the slot is still in its template state (the "first
; encounter"), stamping them into action state 7 and giving them their
; sprite-class kin as targets via NearestCrewA/B (see GatherSameClassSlots).
; After activation the Android wounds nearby crew through CrewAction7_Handler
; (CrewAction7_Handler); CrewHitsAndroid here is the **player-response
; branch**, dispatched when other crew engage the Android in combat state 4.
; CAVEAT: the (IX+1) byte is NOT a unique slot ID in the LongGameCrewInit
; template (LongGameCrewInit) — slots 1/2/3 share +1=6, slots 4/5/6 share
; +1=27. So branch 2's match fires for the whole **sprite class** of the
; Android, not strictly the chosen slot: - AlienTargetID ∈ {1, 2}: matches
; {Dallas, Kane, Ripley} - AlienTargetID ∈ {4, 6}: matches {Ash, Lambert,
; Parker} Whether that ambiguity is intentional design or a missing per-slot ID
; write is unresolved — verify on an emulator before extending. The short game
; hard-wires AlienTargetID = 9 (out of range), so branch 2 never fires there.
; After dispatch, CALL PlayMusic_4 clears IX+3 (action state) and returns.
CrewAction4_Handler:
  LD A,($737F)            ; A = the alien's current room (slot 0 byte +1)
  CP (IX+$01)             ; current actor IS the alien slot?
  JR Z,DispatchHitsAlien  ; yes → CrewHitsAlien branch
  LD A,(AlienActiveFlag)  ; A = AlienActiveFlag
  AND A                   ; alien loose on ship?
  RET Z                   ; no: nothing to dispatch
  LD HL,(AlienTargetPtr)  ; HL = pointer to alien-target crew record
  INC HL                  ; HL → target's identity byte (+1)
  LD A,(HL)               ; A = target's identity
  CP (IX+$01)             ; current actor IS the alien's target (= the
                          ; Android)?
  RET NZ                  ; no: not in combat with either alien or android
Combat_HitAlien:
  CALL CrewHitsAndroid    ; yes → CrewHitsAndroid (msg #32 / #31)
  JR ClearActionState
DispatchHitsAlien:
  CALL CrewHitsAlien      ; alien slot → CrewHitsAlien (msg #4 / #3)
ClearActionState:
  CALL PlayMusic_4        ; reset IX+3 = 0 (action state cleared post-combat)
  NOP
  RET
Combat_RefreshRow:
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(DrawSlotIndex)
  LD HL,CrewIndex
  CP (HL)
  RET NZ
  LD A,$34
  CALL PlayMusic_26
  LD HL,$48D6
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  RET
; CrewHitsAlien — alien-side combat handler. Called from CrewAction4_Handler
; (DispatchHitsAlien) when the actor in state 4 has (IX+1) matching $737F (the
; alien's identity from CommonGameInit, CommonInit_AlienRoom). The outcome
; depends on the WEAPON: FindHeldItems (FindHeldItems) loads the attacking crew
; member's front-hand item id into HeldItemFront (HeldItemFront) and the effect
; dispatches on it. It also houses the Harpoon Gun's kill primitive at
; AlienKillPrimitive. Guards: re-asserts that (IX+1) matches the alien
; identity, then requires pixel coords IX+4 >= 2 and IX+6 >= 2 (sprite fully
; placed). Calls PlayMusic_8 to clear the held-item slots, then FindHeldItems
; (FindHeldItems) to fetch the attacker's weapons: 17 (Cat Box)       → silent:
; JP to ClearActionState >= 20 (bare hands) → silent 16 (Net)           → "net"
; outcome: write $FF to $7376 (slot 0 byte +0) to disable the alien, then
; enqueue message #3 "{actor} nets the ALIEN" else               → "hit"
; outcome at CrewHitsAlien_HitPath: enqueue message #4 "{actor} hits the
; ALIEN", then dispatch on the weapon id: - 0-5 Elctrc Prd/Incineratr, 18-19
; Spanner: INC alien's wound counter at $73B2 (byte +4 of slot 0). If it
; reaches the AlienStopCell threshold, JP 44125 (alien- defeated path). - 6-7
; Tracker: enqueue msg #5 "Tracker is broken" - 8-11 Fire Extng: decrement the
; item's charge in ItemCharges; on underflow enqueue msg #6 "extinguisher
; empty". - 12 Harpn Gun: the kill primitive at AlienKillPrimitive — see
; AlienKillPrimitive below. - 13-15 Laser Pist: JP 37831 — decrement the
; charge, then either msg #7 "Laser is exhausted" or JP 41022. This routine
; parallels CrewHitsAndroid (CrewHitsAndroid) in structure but refers to the
; alien (slot 0 at $7376) and emits messages #3 / #4 instead of #31 / #32.
CrewHitsAlien:
  LD A,($737F)            ; A = the alien's current room (slot 0 byte +1)
  CP (IX+$01)             ; re-check: IX is the alien slot
  RET NZ                  ; defensive: not the alien → bail
  LD A,(IX+$04)           ; A = pixel X-tile
  CP $02
  RET C                   ; X-tile < 2 → alien not fully on screen, bail
  LD A,(IX+$06)           ; A = pixel Y
  CP $02
  RET C                   ; Y < 2 → bail
  CALL PlayMusic_8        ; clear (31248) scan slot, A = CrewIndex
  CALL FindHeldItems      ; populate (31248)/(31249) with corridor positions
  LD A,(HeldItemFront)    ; A = alien's corridor scan position
  CP $11
  JP Z,Combat_RefreshRow  ; pos 17 → silent
  CP $14
  JP NC,Combat_RefreshRow ; pos >= 20 → silent (sentinel cells)
  CP $10
  JR NZ,CrewHitsAlien_HitPath ; pos != 16 → "hits the ALIEN" branch
  LD HL,ActorRecords      ; HL = $7376 (alien slot byte 0)
  LD (HL),$FF             ; mark alien slot as "netted/inactive"
  CALL DrawFrontItem
  LD A,(CrewIndex)        ; A = CrewIndex (= alien-slot index)
  LD L,A                  ; L = actor index
  LD A,$03                ; A = msg #3 "{actor} nets the ALIEN"
  CALL UpdateCrewAI_8     ; enqueue
  JP Combat_RefreshRow
; "{actor} hits the ALIEN" branch (CrewHitsAlien continued).
CrewHitsAlien_HitPath:
  PUSH AF                 ; stash (31248) for post-message dispatch
  LD A,(CrewIndex)        ; A = CrewIndex
  LD L,A                  ; L = actor index
  LD A,$04                ; A = msg #4 "{actor} hits the ALIEN"
  CALL UpdateCrewAI_8     ; enqueue
  POP AF                  ; A = (31248) restored
  CP $06
  JR C,AlienStep_Advance
  CP $12
  JR Z,AlienStep_Advance
  CP $13
  JR NZ,WeaponCheck_Low
AlienStep_Advance:
  LD HL,$7382
  INC (HL)
  LD A,(AlienStopCell)
  CP (HL)
  JP Z,EndgameScreen
  JP AlienScareCheck
WeaponCheck_Low:
  CP $08
  JR NC,WeaponCheck_Charged
  CALL DrawFrontItem
  LD HL,(CrewIndex)
  LD A,$05
  CALL UpdateCrewAI_8
  JR AlienStep_Advance
WeaponCheck_Charged:
  CP $0C
  JR NC,HarpoonGate
  LD HL,ItemCharges
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  AND A
  JR Z,WeaponSpentMsg
  DEC (HL)
  JP AlienScareCheck
WeaponSpentMsg:
  LD HL,(CrewIndex)
  LD A,$06
  CALL UpdateCrewAI_8
  JP Combat_RefreshRow
; AlienKillPrimitive — the Harpoon Gun outcome, reached from CrewHitsAlien
; (CrewHitsAlien) when the attacker's front-hand item (HeldItemFront) is 12,
; the Harpn Gun. A harpoon strike is devastating for both sides:
; GatherSameClassSlots gathers up to three slots (NearestCrewC +
; NearestCrewA/B) sharing the alien's sprite class; for each, every
; ItemLocations (ItemLocations) entry that the slot carries or that lies in the
; alien's room is destroyed (set to 255), and if the slot's byte +7 is still
; zero (alive) it is stamped to 1 (killed) with its byte +4 wound counter
; cleared. Then the alien's own wound counter is bumped by 5 (harpoon hits push
; it quickly over the AlienStopCell threshold → dies via JP 44125), the
; corridor view is rebuilt (CALL 31417) and the per-frame chain continues at
; AlienScareCheck (AlienScareCheck).
HarpoonGate:
  JP NZ,WeaponCheck_Laser ; (31248) != 12 → 37831 (decrement ItemCharges)
AlienKillPrimitive:
  CALL GatherSameClassSlots ; populate NearestCrewC + NearestCrewA/B with the
                            ; three slots sharing the alien's sprite class
  LD DE,NearestCrewC
  LD B,$03
KillPrim_SlotLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)
  CP $FF
  JR Z,KillPrim_NextSlot
  PUSH AF
  ADD A,$80               ; E = victim's back-hand marker
  LD E,A
  ADD A,$20               ; D = victim's front-hand marker
  LD D,A
  LD C,(IX+$01)           ; C = the alien's room
  LD HL,ItemLocations     ; destroy every item the victim holds
  LD B,$16                ; or that lies in the alien's room:
KillPrim_ItemLoop:
  LD A,(HL)
  CP E
  JR Z,KillPrim_WipeItem
  CP D
  JR Z,KillPrim_WipeItem
  CP C
  JR NZ,KillPrim_NextItem
KillPrim_WipeItem:
  LD (HL),$FF             ; ItemLocations entry -> gone
KillPrim_NextItem:
  INC HL
  DJNZ KillPrim_ItemLoop
  POP AF
  LD HL,ActorRecords
  ADD A,A
  ADD A,A
  ADD A,A
  ADD A,$07
  LD E,A
  LD D,$00
  ADD HL,DE
  XOR A
  CP (HL)
  JR NZ,KillPrim_NextSlot
  LD (HL),$01
  DEC HL
  DEC HL
  DEC HL
  LD (HL),A
KillPrim_NextSlot:
  POP DE
  INC DE
  POP BC
  DJNZ KillPrim_SlotLoop
  LD A,$0F
  CALL AddAlienRoomDamage
  LD HL,$7382
  LD A,(HL)
  ADD A,$05
  LD (HL),A
  LD HL,AlienStopCell
  CP (HL)
  JP NC,EndgameScreen
  CALL InitGameView
  JP AlienScareCheck
WeaponCheck_Laser:
  LD E,A
  LD D,$00
  LD HL,ItemCharges
  ADD HL,DE
  XOR A
  CP (HL)
  JR Z,LaserSpentMsg
  DEC (HL)
  LD A,$06
  CALL AddAlienRoomDamage
  LD HL,$7382
  LD A,(HL)
  ADD A,$02
  LD (HL),A
  LD HL,AlienStopCell
  CP (HL)
  JP NC,EndgameScreen
  JP AlienScareCheck
LaserSpentMsg:
  LD HL,(CrewIndex)
  LD A,$07
  CALL UpdateCrewAI_8
  JP Combat_RefreshRow
; This entry point is used by the routines at PlayMusicPhrase and UpdateAlien.
GatherSameClassSlots:
  LD HL,$FFFF
  LD (NearestCrewA),HL
  LD DE,NearestCrewA
  LD A,(CrewIndex)
  LD (NearestCrewC),A
; This entry point is used by the routine at UpdateRoomActors.
GatherSlots_Scan:
  LD HL,$7387
  LD C,$01
  EX AF,AF'
  LD A,(IX+$01)
  LD B,$07
GatherSlots_Loop:
  CP (HL)
  JR NZ,GatherSlots_Next
  EX AF,AF'
  CP C
  JR Z,GatherSlots_Skip
  EX DE,HL
  LD (HL),C
  EX DE,HL
  INC DE
GatherSlots_Skip:
  EX AF,AF'
GatherSlots_Next:
  PUSH DE
  LD DE,$0008
  ADD HL,DE
  POP DE
  INC C
  DJNZ GatherSlots_Loop
  RET
; This entry point is used by the routine at UpdateAlien.
DrawFrontItem:
  LD HL,$4836
  LD (DrawScreenPtr),HL
  LD BC,(HeldItemFront)
  LD E,C
  LD D,$00
  LD HL,ItemLocations
  ADD HL,DE
  JP PlayMusic_5
; This entry point is used by the routine at PlayMusic.
UpdateHandRows:
  CP B
  JR NZ,UpdateHandRows_Swap
  LD (MenuUseRows),A
  CALL PlayMusic_7
  RET
UpdateHandRows_Swap:
  LD L,B
  LD H,A
  LD (HeldItemFront),HL
  LD A,L
  CALL ItemIdToString
  LD (MenuUseRows),A
  CALL DrawSprite
  LD A,$FF
  LD ($73C7),A
  CALL DrawSprite
  LD A,(HeldItemFront)
; This entry point is used by the routine at PlayMusic.
GetItemLocation:
  LD HL,ItemLocations
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  JP PickupItemBoost
  RET
AlienScareCheck:
  CALL GatherSameClassSlots
  XOR A
  LD (DistanceAccum),A
  PUSH IX
  LD DE,NearestCrewC
  LD B,$03
Scare_SlotLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)
  CP $08
  JR NC,Scare_NextSlot
  PUSH AF
  LD IX,ActorRecords
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  ADD IX,DE
  LD A,$02
  CP (IX+$04)
  JR NC,Scare_SkipSlot
  LD A,$04
  CP (IX+$03)
  JR NZ,Scare_SkipSlot
  LD HL,DistanceAccum
  DEC A
  CP (IX+$06)
  JR NC,Scare_TrackerBonus
  INC (HL)
Scare_TrackerBonus:
  POP AF
  CALL FindHeldItems
  LD HL,DistanceAccum
  LD A,(HeldItemFront)
  CP $0D
  JR NC,Scare_AddDistance
  CP $06
  JR NZ,Scare_AddDistance
  CP $07
  JR NZ,Scare_AddDistance
  INC (HL)
Scare_AddDistance:
  LD A,($7382)
  SRA A
  SRA A
  ADD A,(HL)
  LD (HL),A
  PUSH AF
Scare_SkipSlot:
  POP AF
Scare_NextSlot:
  POP DE
  INC DE
  POP BC
  DJNZ Scare_SlotLoop
  CALL AdvanceScriptPtr
  CP $0F
  JR Z,Scare_Done
  LD HL,DistanceAccum
  CP (HL)
  JR NC,Scare_Done
  LD IX,ActorRecords
Scare_PickFlee:
  CALL PickNextRoom
  LD A,(IX+$01)
  CP (IX+$02)
  JR Z,Scare_PickFlee
  LD A,(CrewIndex)
  PUSH AF
  XOR A
  LD (CrewIndex),A
  CALL CrewAction1_Idle
  POP AF
  LD (CrewIndex),A
  CALL TriggerAlienXAnim
  LD (IX+$00),$00
  LD DE,NearestCrewC
  LD B,$03
Scare_FleeLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)
  CP $08
  JR NC,Scare_FleeNext
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,$7383
  CALL IncPairAt
  CP $09
  JR C,Scare_FleeNext
  LD (HL),$08
Scare_FleeNext:
  POP DE
  INC DE
  POP BC
  DJNZ Scare_FleeLoop
  JP RefreshCondIfViewed
Scare_Done:
  POP IX
  JP Combat_RefreshRow
; This entry point is used by the routine at UpdateCrewAI.
AddAlienRoomDamage:
  LD HL,$737F
  LD E,(HL)
; This entry point is used by the routines at UpdateRoomActors and UpdateAlien.
; AddRoomDamage: add A damage points to room E's damage meter — a 2-digit ASCII
; pair in RoomDamageDigits (RoomDamageDigits; tens digit is the severity the
; engine scan at EngineStateScan compares against). Crossing "30" raises the
; room-specific alarm messages #22-#25.
AddRoomDamage:
  BIT 6,E                 ; no damage meters in the duct network
  RET NZ
  LD HL,RoomDamageDigits  ; per-room ASCII damage digits (tens,units)
  LD D,$00
  ADD HL,DE
  ADD HL,DE
  INC HL                  ; HL -> units digit
  LD B,A                  ; B = damage points to add
Damage_PointLoop:
  PUSH BC
  PUSH HL
  PUSH DE
  LD A,(HL)
  CP $35
  JR Z,Damage_TensCarry
  LD (HL),$35
  JR Damage_NextPoint
Damage_TensCarry:
  LD (HL),$30
  DEC HL
  INC (HL)
  LD A,(HL)
  CP $33
  JR NZ,Damage_ChkStructural
  LD A,E                  ; damage tens digit reached '3': room alarms
  CP $06                  ; CommdCentr?
  JR NZ,Damage_ChkComputer
  LD A,$16                ; msg #22 "Partial loss of control systems"
  JR Damage_SendMsg
Damage_ChkComputer:
  CP $07                  ; Computer room?
  JR NZ,Damage_ChkLifeSupt
  LD A,$17                ; msg #23 "Computer Malfunction detected"
  JR Damage_SendMsg
Damage_ChkLifeSupt:
  CP $19                  ; Life Support?
  JR NZ,Damage_ChkNarcissus
  LD A,$18                ; msg #24 "Environmental irregularities"
  JR Damage_SendMsg
Damage_ChkNarcissus:
  CP $22
  JR NZ,Damage_ChkEngines
  LD A,$19
  JR Damage_SendMsg
Damage_ChkEngines:
  CP $11
  JR C,Damage_NextPoint
  CP $14
  JR NC,Damage_NextPoint
  AND $03
  LD B,A
  LD A,$04
Damage_EngineBit:
  ADD A,A
  DJNZ Damage_EngineBit
  LD HL,ShipSystemFlags
  OR (HL)
  LD (HL),A
  LD L,E                  ; L = room for the [251] room-name field
  LD A,$09                ; msg #9 "WARNING: Fire in {room}"
  CALL UpdateCrewAI_8
  JR Damage_NextPoint
Damage_ChkStructural:
  CP $37
  JR NZ,Damage_ChkOverflow
  LD L,E                  ; L = room
  LD A,$08                ; msg #8 "Structural Damage in {room}"
Damage_SendMsg:
  CALL UpdateCrewAI_8
  JR Damage_NextPoint
Damage_ChkOverflow:
  CP $3A
  CALL Z,DamageOverflowFlash
Damage_NextPoint:
  POP DE
  POP HL
  POP BC
  DJNZ Damage_PointLoop
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(CurrentRoom)
  CP E
  CALL Z,DrawRoomTitle
  RET
; This entry point is used by the routine at UpdateRoomActors.
DamageOverflowFlash:
  PUSH BC
  PUSH AF
  LD B,$64
DamageFlash_Loop:
  PUSH BC
  CALL BusyWait
  LD A,$07
  CALL FillAttributes
  CALL BusyWait
  LD A,$38
  CALL FillAttributes
  POP BC
  LD A,B
  AND $07
  OUT ($FE),A
  DJNZ DamageFlash_Loop
  POP AF
  POP BC
  JP DrawStatusPanel_15
; Ship-status dashboard: counters and ASCII digit cells (previously
; mis-disassembled as code). The digit cells double as the source text drawn on
; the status bar, and the endgame renderer copies them into the "The Nostromo
; will explode in .. minutes .. seconds" screen.
EngineFixCounts:
  DEFB $00,$00,$00        ; per-engine-room (17/18/19) fire-fighting progress,
                          ; +1 per extinguisher squirt (fire handler,
                          ; UpdateRoomActors)
ShipTickCounter:
  DEFB $32                ; ship-systems tick countdown (reloaded with 100 by
                          ; the engine-state scan at EngineStateScan)
DestructTickCounter:
  DEFB $03                ; destruct-second countdown (reloaded with 20 by the
                          ; ticker at DestructTicker)
AdjTestRoom:
  DEFB $00                ; scratch: room of the actor under adjacency test
OxygenTickInit:
  DEFB $0A                ; oxygen tick reload value
DestructTimeText:
  DEFM "TIME: "           ; status-bar label for the destruct countdown
DestructMinutes:
  DEFB $00                ; minutes digit, ASCII ('5' when armed at
                          ; DestructArm)
  DEFM ":"
DestructSeconds:
  DEFB $00,$00            ; seconds digits, ASCII ("00" when armed); the low
                          ; digit also drives the border flicker while armed
OxygenText:
  DEFM "TOOH: "           ; oxygen label as stored on the tape (sic)
OxygenHi:
  DEFM "75"               ; oxygen level, ASCII, counting down; msg #26
OxygenLo:
  DEFM "00"               ; "Oxygen status CRITICAL" fires at "05"+"00"
EventPaceCounter:
  DEFB $00                ; free-running pace counter gating an event every 8
                          ; ticks (inside UpdateRoomActors)

; UpdateRoomActors — per-frame ship-simulation driver
;
; Despite the old name, this block is the SHIP-SIMULATION layer: it drives the
; destruct countdown, fixture proximity events, the oxygen clock, engine states
; and the alien encounter animation, and hosts the fixture-action handlers
; (destruct lever, hypersleep pods, fires, airlocks, launch console) that the
; action key dispatches into.
;
; USED by the routine at GameEntry.
UpdateRoomActors:
  CALL DestructTicker     ; DestructTicker: countdown + border flash
  CALL FindTrackerHolders ; fixture-proximity checks on strip cells 6/7/20
  CALL OxygenTick         ; oxygen clock / status line
  CALL ViewedTrackerCheck ; event pacing / ambient events
  CALL EngineStateScan    ; EngineStateScan: engine damage/repair states
  CALL UpdateAlienAnim    ; alien encounter animation layers
  RET
; FixtureAction: the action-key (key 0) handler for corridor fixtures,
; dispatched via the RoomDispatchTable tail. Reads the corridor cell under the
; cursor and dispatches on the CELL VALUE: 70 -> arm self-destruct     72 ->
; abort self-destruct 74 -> enter hypersleep pod  76 -> fight fire (engine
; rooms) 78 -> Narcissus launch      53/54 -> blow airlock #1/#2 55/56 -> seal
; airlock #1/#2
FixtureAction:
  CALL CancelActionTimer
  LD HL,CorridorPosTable  ; corridor position table
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)               ; A = cell value under the cursor
  LD (CursorCellValue),A
  CP $46                  ; destruct lever (off)?
  JR Z,DestructArm
  CP $48                  ; destruct lever (armed)?
  JR Z,DestructAbort
  CP $4A                  ; hypersleep pod?
  JR Z,HypersleepStart
  CP $4C                  ; fire?
; This entry point is used by the routine at XorHAddE.
FixtureDispatch:
  JR Z,FightFire
  CP $4E                  ; launch console?
  JP Z,LaunchGate
  CP $35                  ; airlock #1 door (closed)?
  JP Z,BlowLock           ; blow it
  CP $36                  ; airlock #2 door (closed)?
  JP Z,BlowLock           ; blow it
  CP $37                  ; airlock #1 door (blown)?
  JP Z,SealLock           ; seal it
  CP $38                  ; airlock #2 door (blown)?
  JP Z,SealLock           ; seal it
  RET                     ; anything else: no action
DestructArm:
  LD A,$0D                ; msg #13 "DESTRUCT SEQUENCE INITIATED"
  CALL UpdateCrewAI_8
  LD A,$48                ; cells 15/16 := armed lever sprites 72/73
  LD ($73CD),A
  LD HL,$5016
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  LD A,$49
  LD (MenuFixtureRow),A
  CALL DrawSprite
  JR DestructSetTimer
DestructAbort:
  LD A,$0E                ; msg #14 "DESTRUCT SEQUENCE ABORTED"
  CALL UpdateCrewAI_8
  LD A,$46                ; cells 15/16 := off lever sprites 70/71
  LD ($73CD),A
  LD HL,$5016
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  LD A,$47
  LD (MenuFixtureRow),A
  CALL DrawSprite
DestructSetTimer:
  LD HL,$3030             ; HL = "00" in ASCII
  LD (DestructSeconds),HL ; DestructSeconds := "00"
  LD HL,DestructMinutes
  LD (HL),$35             ; DestructMinutes := '5' -> 5:00 countdown
  LD HL,ShipSystemFlags
  LD A,(HL)
  XOR $01                 ; toggle ShipSystemFlags bit 0 (destruct armed)
  LD (HL),A
  RET
HypersleepStart:
  LD (IX+$03),$06         ; action state 6 = entering hypersleep
  LD (IX+$00),$FF
  LD A,$0B                ; msg #11 "{actor} prepares for hypersleep"
  LD HL,(DrawSlotIndex)
  CALL UpdateCrewAI_8
  RET
FightFire:
  LD A,(HeldItemFront)    ; A = actor's corridor cell
  CP $08                  ; fire cells occupy positions 8-11
  RET C
  CP $0C
  RET NC
  LD E,A
  LD D,$00
  LD HL,ItemCharges
  ADD HL,DE               ; HL -> extinguisher charges for this position
  XOR A
  CP (HL)
  JR NZ,FightFireSquirt
  LD HL,(DrawSlotIndex)   ; empty:
  LD A,$06                ; msg #6 "{actor}'s extinguisher is empty"
  CALL UpdateCrewAI_8
  RET
FightFireSquirt:
  DEC (HL)                ; use one extinguisher charge
  LD A,(CurrentRoom)
  SUB $11                 ; engine-room index 0-2 (rooms 17/18/19)
  LD E,A
  LD HL,EngineFixCounts
  ADD HL,DE
  INC (HL)                ; bump this engine room's fire-fighting progress
  RET
SealLock:
  LD HL,ShipSystemFlags   ; SealLock: repressurise an airlock
  LD A,(CorridorCursor)
  SUB $0F                 ; cursor cell - 15 -> 0 = airlock #1, 1 = #2
  AND A
  JR Z,SealLock1
  RES 2,(HL)              ; clear "airlock #2 blown" flag
  LD A,$36                ; cell := closed-door sprite 54
  LD (MenuFixtureRow),A
  LD HL,$5036
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  JR SealLockMsg
SealLock1:
  RES 1,(HL)              ; clear "airlock #1 blown" flag
  LD A,$35                ; cell := closed-door sprite 53
  LD ($73CD),A
  LD HL,$5016
  LD (DrawScreenPtr),HL
  CALL DrawSprite
SealLockMsg:
  LD A,(CorridorCursor)
  SUB $0F
  LD L,A                  ; L = airlock room (0/1) for the {sprite} field
  LD A,$11                ; msg #17 "{room} PRESSURE RESTORED"
  CALL UpdateCrewAI_8
  RET
BlowLock:
  LD HL,ShipSystemFlags   ; BlowLock: blow an airlock open
  LD A,(CorridorCursor)
  SUB $0F                 ; cursor cell - 15 -> 0 = airlock #1, 1 = #2
  AND A
  JR Z,BlowLock1
  SET 2,(HL)              ; set "airlock #2 blown" flag
  LD A,$38                ; cell := blown-door sprite 56
  LD (MenuFixtureRow),A
  LD HL,$5036
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  JR BlowLockSweep
BlowLock1:
  LD A,$37                ; cell := blown-door sprite 55
  SET 1,(HL)              ; set "airlock #1 blown" flag
  LD ($73CD),A
  LD HL,$5016
  LD (DrawScreenPtr),HL
  CALL DrawSprite
BlowLockSweep:
  LD A,(CorridorCursor)
  SUB $0F                 ; A = the depressurised room (0/1)
  PUSH IX
  LD B,$08                ; sweep all 8 actor slots (alien included!)
  LD IX,ActorRecords
  LD DE,$0008
BlowLock_SweepLoop:
  CP (IX+$01)             ; actor in the depressurised room?
  JR NZ,BlowLock_NextActor
  LD (IX+$01),$FF         ; yes: swept into space - room := none,
  LD (IX+$07),$01         ; status := dead
BlowLock_NextActor:
  ADD IX,DE
  DJNZ BlowLock_SweepLoop
  POP IX
  LD HL,ItemLocations     ; clear their markers from the corridor strip
  LD B,$16
BlowLock_ItemLoop:
  CP (HL)
  JR NZ,BlowLock_NextItem
  LD (HL),$FF
BlowLock_NextItem:
  INC HL
  DJNZ BlowLock_ItemLoop
  LD L,A
  LD A,$10                ; msg #16 "WARNING: {room} DEPRESSURISED"
  CALL UpdateCrewAI_8
  LD HL,$737F             ; was the ALIEN in there?
  LD A,$FF
  CP (HL)                 ; alien's room = 255 means it was swept out
  RET NZ
  LD A,($7382)            ; alien already wounded 3+ times:
  CP $03
  JP NC,EndgameScreen     ; it dies in space -> endgame/rating screen
  CALL AdvanceScriptPtr   ; else ROM-script roll: nibble >= 2
  CP $02
  JP NC,EndgameScreen     ; (14 in 16): the alien dies -> endgame
  LD A,$20                ; survived! it crawls back in:
  LD ($737F),A            ; alien's room := 32 (ShuttleBay)
  LD L,A
  LD A,$12                ; msg #18 "INTRUDER ENTERING {ShuttleBay}"
  CALL UpdateCrewAI_8
  LD HL,ActorRecords
  LD (HL),$00             ; reset the alien's action byte
  RET
; CrewAction6_Handler — dispatched via CrewActionDispatch entry 6.
CrewAction6_Handler:
  LD HL,(CrewIndex)
  LD A,$0C
  CALL UpdateCrewAI_8
  LD (IX+$07),$FF
  LD (IX+$01),$FF
  LD HL,ItemLocations
  LD A,(CrewIndex)
  ADD A,$80
  LD E,A
  ADD A,$20
  LD D,A
  LD C,(IX+$01)
  LD B,$16
RedirectRoomLoop:
  LD A,(HL)
  CP E
  JR Z,RedirectRoomSet
  CP D
  JR NZ,RedirectRoomNext
RedirectRoomSet:
  LD (HL),$0F
RedirectRoomNext:
  INC HL
  DJNZ RedirectRoomLoop
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(DrawSlotIndex)
  LD HL,CrewIndex
  JP KillActor_Bounce
  NOP
  NOP
DestructTicker:
  LD HL,ShipSystemFlags   ; DestructTicker (per frame)
  BIT 0,(HL)              ; destruct armed?
  RET Z                   ; no: done
  LD A,($95DF)            ; flash the border from the seconds digit
  AND $03
  OUT ($FE),A
  LD HL,DestructTickCounter
  DEC (HL)
  RET NZ
  LD (HL),$14             ; every 20 frames = one game second
  LD A,$2F                ; '/' = '0'-1: ASCII underflow sentinel
  LD HL,(DestructSeconds) ; L = seconds tens, H = seconds units
  DEC H
  CP H
  JR NZ,StorePanelPtr
  LD H,$39                ; units wrapped: back to '9'
  DEC L
  CP L
  JR NZ,StorePanelPtr
  LD L,$35                ; tens wrapped: back to '5' (fresh :59..:50)
  LD (DestructSeconds),HL
  LD HL,DestructMinutes
  DEC (HL)                ; borrow into the minutes digit
  CP (HL)
  JP Z,DamageOverflowFlash ; minutes below '0': BOOM - flash + game over
  JP OxygenCheckRedraw    ; redraw countdown / oxygen status
StorePanelPtr:
  LD (DestructSeconds),HL
  JP OxygenCheckRedraw
FindTrackerHolders:
  LD HL,NearRoomList
  LD A,(TrackerLocations)
  CP $A0
  CALL NC,TrackerHolderRoom
  INC HL
  LD A,($74FB)
  CP $A0
  CALL NC,TrackerHolderRoom
  INC HL
  LD A,(CatItemLocations)
  CP $A0
  CALL NC,TrackerHolderRoom
  LD A,($7509)
  CP $A0
  CALL NC,TrackerHolderRoom
  RET
TrackerHolderRoom:
  CP $FF
  RET Z
  PUSH HL
  SUB $A0
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,$737F
  ADD HL,DE
  LD A,(HL)
  LD (AdjTestRoom),A
  LD BC,RoomAdjCorridors
  BIT 6,A
  JR Z,AdjRowFromRoom
  CALL DuctSearchInit
AdjRowFromRoom:
  LD L,A
  LD H,$00
  PUSH HL
  ADD HL,HL
  ADD HL,HL
  POP DE
  ADD HL,DE
  ADD HL,BC
  LD DE,$0008
  LD B,$05
PickRoomScanLoop:
  PUSH BC
  PUSH HL
  LD C,(HL)
  LD A,(AdjTestRoom)
  CP C
  JR Z,PickRoomScanNext
  LD A,(JonesRoom)
  JP RoomRejectCheck
; This entry point is used by the routine at UpdateAllCrew.
PickRoom_ActorScan:
  LD B,$08
  LD HL,$737F
PickRoom_ActorLoop:
  LD A,(HL)
  AND $3F
  CP C
  JR NZ,PickRoom_ActorNext
  PUSH HL
  ADD HL,DE
  DEC HL
  DEC HL
  XOR A
  CP (HL)
  JR Z,PickRoom_PopReject
  POP HL
PickRoom_ActorNext:
  ADD HL,DE
  DJNZ PickRoom_ActorLoop
PickRoomScanNext:
  POP HL
  INC HL
  POP BC
  DJNZ PickRoomScanLoop
  POP HL
  LD A,(AdjTestRoom)
  LD (HL),A
  RET
PickRoom_PopReject:
  POP HL
; This entry point is used by the routine at UpdateAllCrew.
PickRoom_Reject:
  POP HL
  POP BC
  POP HL
  LD (HL),$FF
  RET
EngineStateScan:
  LD D,$08
  LD A,(ShipSystemFlags)
  LD E,$11
  BIT 3,A
  CALL NZ,FireDamageTick
  SLA D
  INC E
  BIT 4,A
  CALL NZ,FireDamageTick
  SLA D
  INC E
  BIT 5,A
  CALL NZ,FireDamageTick
  LD HL,ShipTickCounter
  DEC (HL)
  RET NZ
  LD (HL),$64
  RET
FireDamageTick:
  PUSH AF
  PUSH DE
  LD C,$01
  LD B,D
  LD D,$00
  LD HL,RoomDamageDigits
  ADD HL,DE
  ADD HL,DE
  LD A,(HL)
  CP $36
  JR C,FireThresholdCheck
  INC C
  CP $38
  JR C,FireThresholdCheck
  INC C
FireThresholdCheck:
  LD HL,$95BE
  ADD HL,DE
  LD A,(HL)
  CP C
  JR C,FireSpreadCheck
  LD A,(ShipSystemFlags)
  XOR B
  LD (ShipSystemFlags),A
  LD A,(RoomModeByte)
  CP $01
  JR NZ,FireExtinguishedMsg
  LD A,(CurrentRoom)
  CP E
  JR NZ,FireExtinguishedMsg
  LD HL,$FFFF
  LD ($73CD),HL
  LD A,L
  LD HL,$5016
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  LD A,$FF
  CALL DrawSprite
FireExtinguishedMsg:
  POP HL
  PUSH HL
  LD A,$0F                ; msg #15 "Fire extinguished in {room}"
  CALL UpdateCrewAI_8
  POP DE
  POP AF
  RET
FireSpreadCheck:
  LD A,(ShipTickCounter)
  DEC A
  LD A,$01
  CALL Z,AddRoomDamage
  POP DE
  POP AF
  RET
ViewedTrackerCheck:
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(DrawSlotIndex)
  ADD A,$A0
  LD C,A
  LD HL,NearRoomList
  LD A,(TrackerLocations)
  CP C
  JR Z,AmbientBeepTick
  LD A,($74FB)
  INC HL
  CP C
  JR Z,AmbientBeepTick
  INC HL
  LD A,(CatItemLocations)
  CP C
  JR Z,JonesUneasyMsg
  LD A,($7509)
  CP C
  JR Z,JonesUneasyMsg
  RET
AmbientBeepTick:
  LD A,(HL)
  CP $FF
  RET NZ
  LD HL,EventPaceCounter
  INC (HL)
  LD A,(HL)
  AND $07
  CALL Z,RomBeep
  RET
JonesUneasyMsg:
  LD A,(HL)
  CP $FF
  RET NZ
  LD A,(CurrentRoom)
  LD L,A                  ; L = current room
  LD A,$13                ; msg #19 "Jones is looking uneasy"
  CALL UpdateCrewAI_8
  RET
; This entry point is used by the routine at DrawShipMap.
RomBeep:
  PUSH IX
  LD DE,$0005
  LD HL,$0225
  CALL $03B5
  POP IX
  RET
; CrewAction5_Investigate — dispatched via CrewActionDispatch entry 5.
CrewAction5_Investigate:
  LD HL,$FFFF
  LD (NearestCrewA),HL
  LD DE,NearestCrewC
  LD A,$FF
  LD (DE),A
  XOR A
  CALL GatherSlots_Scan
  LD C,$00
  LD HL,NearestCrewC
  LD A,$FF
  LD B,$03
Investigate_CountLoop:
  CP (HL)
  JR Z,Investigate_CountNext
  INC C
Investigate_CountNext:
  INC HL
  DJNZ Investigate_CountLoop
  LD A,C
  AND A
  RET Z
  CP $01
  JR NZ,Investigate_PickTwo
  XOR A
  JR Investigate_TakeSlot
Investigate_PickTwo:
  CP $02
  JR NZ,Investigate_PickAny
  CALL AdvanceScriptPtr
  AND $01
  JR Investigate_TakeSlot
Investigate_PickAny:
  CALL AdvanceScriptPtr
  AND $07
  CALL ScriptNibbleToDirection
Investigate_TakeSlot:
  LD E,A
  LD D,$00
  LD HL,NearestCrewC
  ADD HL,DE
  LD C,(HL)
  LD (HL),$FF
  LD A,C
Investigate_SlotAddr:
  ADD A,A
  ADD A,A
  ADD A,A
  ADD A,$07
  LD E,A
  LD D,$00
  LD HL,ActorRecords
  ADD HL,DE
  XOR A
  CP (HL)
  JP NZ,Wound_PanicRoll
  DEC HL
  DEC HL
  DEC HL                  ; HL -> victim's +4 strength
  DEC (HL)                ; the alien drains one strength point
  LD A,(HL)
  CP $02                  ; below 2: victim collapses
  JR C,VictimItemDrop
  CP $04                  ; 2-3: wounded
  JR C,WoundMsg
  LD L,C                  ; still healthy:
  LD A,$01                ; msg #1 "The ALIEN is attacking {actor}"
  CALL UpdateCrewAI_8
  RET
WoundMsg:
  LD L,C
  LD A,$0A                ; msg #10 "{actor} is being wounded"
  CALL UpdateCrewAI_8
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  PUSH IX
  LD A,(DrawSlotIndex)
  ADD A,A
  ADD A,A
  ADD A,A
  LD IX,ActorRecords
  LD E,A
  LD D,$00
  ADD IX,DE
  CALL DrawCrewCondition
  POP IX
  RET
; This entry point is used by the routine at PlayMusicPhrase.
VictimItemDrop:
  INC HL
  INC HL
  CALL KillActorRefresh
  LD DE,$FFF9
  ADD HL,DE
  LD (HL),$00
  LD A,C
  PUSH AF
  ADD A,$80
  LD E,A
  ADD A,$20
  LD D,A
  LD C,(IX+$01)
  LD B,$16
  LD HL,ItemLocations
VictimItems_Loop:
  LD A,(HL)
  CP E
  JR Z,VictimItems_Set
  CP D
  JR NZ,VictimItems_Next
VictimItems_Set:
  LD (HL),C
VictimItems_Next:
  INC HL
  DJNZ VictimItems_Loop
  POP AF
  LD L,A
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(DrawSlotIndex)
  CP L
  RET NZ
  LD A,$15                ; msg #21 "{actor} has collapsed"
  CALL UpdateCrewAI_8
  JP InitGameView
Wound_PanicRoll:
  XOR A
  CP (IX+$05)
  JR NZ,Wound_TrioPick
  PUSH HL
  CALL AdvanceScriptPtr
  POP HL
  CP $04
  JR NC,Wound_TrioPick
  LD DE,$FFFA
  ADD HL,DE
  LD (HL),$FF
  CALL SetCourageRefresh
  RET
Wound_TrioPick:
  LD DE,$0001
  LD HL,NearestCrewC
  CP $07
  JR C,Wound_TrioLoop
  LD DE,$FFFF
  LD HL,NearestCrewB
Wound_TrioLoop:
  LD B,$03
Wound_TrioRead:
  LD A,(HL)
  CP $FF
  JR Z,Wound_TrioNext
  CP C
  JR Z,Wound_TrioNext
  LD (HL),$FF
  LD C,A
  JP Investigate_SlotAddr
Wound_TrioNext:
  ADD HL,DE
  DJNZ Wound_TrioRead
  CALL PickNextRoom
  LD (IX+$00),$28
  LD (IX+$03),$01
  RET
OxygenTick:
  LD HL,OxygenTickInit
  DEC (HL)
  RET NZ
  LD HL,ActorStatusColumn
  LD DE,$0008
  LD BC,$0800
  XOR A
OxygenTick_CountLoop:
  CP (HL)
  JR Z,OxygenTick_CountNext
  INC C
OxygenTick_CountNext:
  ADD HL,DE
  DJNZ OxygenTick_CountLoop
  LD A,C
  ADD A,A
  LD (OxygenTickInit),A
  LD A,$2F
  LD HL,$95E9
OxygenTick_Borrow:
  DEC (HL)
  CP (HL)
  JR NZ,OxygenCheckRedraw
  LD (HL),$39
  DEC HL
  JR OxygenTick_Borrow
; This entry point is used by the routines at OrdersMenuNextRow and DrawSprite.
OxygenCheckRedraw:
  LD HL,(OxygenHi)
  LD A,L
  CP $30
  JR NZ,ClockPanelUpdate
  LD A,H
  CP $35
  JR NZ,ClockPanelUpdate
  LD HL,(OxygenLo)
  LD A,$30
  CP L
  JR NZ,ClockPanelUpdate
  CP H
  JR NZ,ClockPanelUpdate
  LD A,$1A
  CALL UpdateCrewAI_8
ClockPanelUpdate:
  LD A,($95E5)
  CP $20
  JP NZ,OptionsScreen_29
  LD A,(RoomModeByte)
  CP $02
  RET Z
  CP $03
  RET Z
  LD HL,$5076
  LD (DrawScreenPtr),HL
  LD DE,OxygenText
  LD HL,ShipSystemFlags
  BIT 0,(HL)
  JR Z,ClockDraw10
  LD DE,DestructTimeText
ClockDraw10:
  LD B,$0A
ClockDrawLoop:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  POP BC
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  DJNZ ClockDrawLoop
  RET
  RET

; InitJonesAnim
;
; Resets Jones animation and blits the first frame. Sets Y position to 8,
; clears the Y phase freeze flag, then calls BlitJonesFrame with B=1 to draw
; frame 0. This entry point is used by the routines at DrawSprite and
; UpdateJones.
InitJonesAnim:
  XOR A                   ; A = 0: clear freeze flag
  LD (JonesPhase),A       ; JonesPhase = 0
  LD A,$08
  LD (JonesPos),A         ; JonesPos = 8 (initial Y pixel offset)
  LD B,$01                ; B = 1 iteration
  CALL BlitJonesFrame     ; draw first tail frame
  RET

; TriggerJonesAnim
;
; Advances the Jones animation by one frame if not frozen. Sets the freeze flag
; (bit 7 of JonesPhase) after blitting so the animation runs exactly once. This
; entry point is used by the routines at GetItemPick and UpdateJones.
TriggerJonesAnim:
  LD B,$01                ; B = 1 iteration
  LD A,(JonesPhase)       ; A = JonesPhase
  AND $80                 ; test freeze flag (bit 7)
  RET NZ                  ; already frozen: do nothing
  CALL BlitJonesFrame     ; blit one tail frame
  LD A,$80
  LD (JonesPhase),A       ; freeze: JonesPhase |= 128
  RET

; ResetAlienXAnim
;
; Clears the alien body animation area from the screen, then resets the X phase
; counter so the next trigger starts from frame 0. This entry point is used by
; the routines at DrawSprite and PlayMusic.
ResetAlienXAnim:
  CALL ClearAlienArea     ; clear 19x20-cell screen area (ClearAlienArea)
  XOR A                   ; A = 0
  LD (AlienXPhase),A      ; AlienXPhase = 0 (unfreeze, frame 0)
  RET

; TriggerAlienXAnim
;
; Arms the alien body animation (sets freeze flag so a single blit occurs next
; frame), or ends the encounter sequence. If (RoomModeByte)=1 the crew member
; was killed: jump to RedrawRoomScene to handle that. Otherwise clears the
; screen area and returns. This entry point is used by the routines at
; GameEntry and XorHAddE.
TriggerAlienXAnim:
  LD A,(AlienXPhase)      ; A = AlienXPhase
  AND $80                 ; test freeze flag
  RET NZ                  ; already frozen: do nothing
  LD A,$80
  LD (AlienXPhase),A      ; freeze: AlienXPhase |= 128
  LD A,(RoomModeByte)     ; A = encounter outcome byte
  CP $01                  ; crew member killed?
  JP Z,RedrawRoomScene    ; yes: handle death sequence
  CALL ClearAlienArea     ; no: clear alien screen area
  RET

; UpdateAlienAnim
;
; Per-frame alien encounter update: refreshes the map view, then advances both
; animation layers. Y layer (tail sweep) if not frozen, X layer (body) if not
; frozen.
UpdateAlienAnim:
  CALL AnimateCrewA       ; redraw room background
  CALL AnimateCrewB       ; redraw room actors
  LD A,(JonesPhase)       ; A = JonesPhase
  AND $80                 ; test freeze flag
  CALL Z,AnimJones        ; not frozen: advance Y animation
  LD A,(AlienXPhase)      ; A = AlienXPhase
  AND $80                 ; test freeze flag
  CALL Z,BlitAlienFrame   ; not frozen: blit next alien body frame
  RET

; AlienAnimVars
;
; Inline state variables for the alien encounter animation. Addresses are
; self-modified at runtime; pointer table entries are fixed in ROM.
AlienXPhase:
  DEFB $00                ; body frame index 0-3 (bit 7 = freeze flag)
  DEFW AlienFrame0        ; AlienFrame0 (AlienFrame0 sprite ptr)
  DEFW AlienFrame1        ; AlienFrame1 (AlienFrame1 sprite ptr)
  DEFW AlienFrame2        ; AlienFrame2 (AlienFrame2 sprite ptr)
  DEFW AlienFrame3        ; AlienFrame3 (AlienFrame3 sprite ptr)
JonesPos:
  DEFB $00                ; pixel Y offset 0-135
AlienScreenPtr:
  DEFW $4800              ; current screen draw ptr (runtime, initially $4800)
JonesPhase:
  DEFB $00                ; tail phase 0-4 (bit 7 = freeze flag)
  DEFW JonesFrame0        ; JonesFrame0 (JonesFrame0 sprite ptr)
  DEFW JonesFrame1        ; JonesFrame1 (JonesFrame1 sprite ptr)
  DEFW JonesFrame2        ; JonesFrame2 (JonesFrame2 sprite ptr)
  DEFW JonesFrame3        ; JonesFrame3 (JonesFrame3 sprite ptr)
  DEFW JonesFrame4        ; JonesFrame4 (JonesFrame4 sprite ptr)

; BlitAlienFrame
;
; Advances AlienXPhase (0->1->2->3->0), loads the corresponding sprite frame
; pointer from the table at AlienXPhase+2, then LDIRs 6 bytes x 63 rows of
; sprite data to the display file at col 7, char-row 1, pixel-row 0, handling
; ZX Spectrum interleaved scan-line addressing at each row boundary.
BlitAlienFrame:
  LD HL,AlienXPhase       ; HL -> AlienXPhase
  LD A,(HL)
  INC A
  AND $03                 ; cycle frame index 0->1->2->3->0
  LD (HL),A               ; store new frame index
  LD E,A
  LD D,$00
  INC HL                  ; HL -> frame pointer table
  ADD HL,DE
  ADD HL,DE               ; HL -> table[frame] (2 bytes/entry)
  LD E,(HL)
  INC HL
  LD D,(HL)               ; DE = sprite frame data ptr
  LD HL,$4807             ; HL = display file col 7, char-row 1, pixel-row 0
  EX DE,HL                ; DE = screen dst, HL = sprite src
  LD B,$3F                ; 63 pixel rows per frame
  LD C,$00
BlitAlienFrameRow:
  PUSH BC
  LD B,C
  LD C,$06
  LDIR
  EX DE,HL
  LD A,H
  AND $07
  CP $07
  JR Z,BlitAlienFrameBankCheck
  INC H
  JR BlitAlienFrameNextRow
; ZX Spectrum display-file scan-line bank wrap: H&7 = 7 means we finished a
; character row. If L < 0xE0 (224) add -1760 to jump to the next screen third;
; otherwise add 32 to advance to the next character row within the same third.
; Then subtract 6 from HL to step back past the LDIR'd bytes.
BlitAlienFrameBankCheck:
  LD A,L
  CP $E0                  ; L >= 0xE0: last char-row of this screen third?
  JR NC,BlitAlienFrameNextThird ; yes: next third
  LD BC,$F920             ; BC = -1760: wrap to next column in next third
  ADD HL,BC
  JR BlitAlienFrameNextRow
BlitAlienFrameNextThird:
  LD BC,$0020             ; BC = 32: advance to next character row
  ADD HL,BC
BlitAlienFrameNextRow:
  LD BC,$FFFA             ; BC = -6: rewind HL past the 6 LDIR'd bytes
  ADD HL,BC
  EX DE,HL                ; DE = screen dst, HL = sprite src
  POP BC
  DJNZ BlitAlienFrameRow  ; next row
  RET

; AnimJones
;
; Entry point called from UpdateAlienAnim with B=2 to advance the Y position
; counter before blitting. Calls BlitJonesFrame with B=2, which will call
; AdvanceJonesPos.
AnimJones:
  LD B,$02                ; B=2: advance position then blit

; BlitJonesFrame
;
; Blits the alien tail (Y channel) XOR sprite for the current phase and
; position. First XORs ink colour bits of a 4x2 attribute block to produce a
; flash effect. Then loads the current Y-phase sprite pointer (3 bytes/row, 21
; rows) and chooses between an unshifted XOR blit (JonesPos&7 < 4) or a shifted
; one (>= 4). The shift amount is encoded directly into the self-modifying JR
; at BlitJonesShiftCode (the JR offset byte is patched to select how many
; SRL/RR passes execute). DJNZ at the end re-uses B to call AdvanceJonesPos
; when B=2.
BlitJonesFrame:
  PUSH BC                 ; save B (iteration count)
  LD A,(AttrFileOrigin)   ; A = attribute at top-left of screen
                          ; (AttrFileOrigin)
  CPL
  AND $07                 ; invert and mask ink colour bits
  LD ($9BC1),A            ; patch XOR 7 opcode operand for attribute flash
  LD A,(JonesPos)         ; A = JonesPos (pixel Y offset 0-135)
  RRA
  RRA
  RRA                     ; A = JonesPos >> 3 (character row 0-16)
  AND $1F
  LD E,A
  LD D,$00                ; DE = char row index
  LD HL,Screen_R9C0L4     ; HL = 0x4C00+1 (display col 1, char-row 0)
  ADD HL,DE               ; HL = screen row address for JonesPos
  LD (AlienScreenPtr),HL  ; AlienScreenPtr = screen row ptr
  LD HL,AttrRow10         ; HL = AttrRow10 (attribute row 10, col 0)
  ADD HL,DE               ; HL = attr address for JonesPos char row
  LD DE,$001D             ; DE = attr row stride - 4 (skip to next pair)
  LD C,$02                ; 2 attribute rows
BlitJonesAttrRow:
  LD B,$04                ; 4 attribute columns per row
BlitJonesAttrInner:
  LD A,(HL)
  XOR $07                 ; toggle ink colour (self-modifying: operand byte
                          ; patched)
  LD (HL),A               ; write flashed attribute
  INC HL
  DJNZ BlitJonesAttrInner ; next column
  ADD HL,DE               ; skip to next attribute row
  DEC C
  JR NZ,BlitJonesAttrRow  ; next row
  LD HL,JonesPhase        ; HL -> JonesPhase
  LD E,(HL)               ; E = current Y phase (0-4)
  LD D,$00
  INC HL                  ; HL -> Y frame pointer table
  ADD HL,DE
  ADD HL,DE               ; HL -> table[phase] (2 bytes/entry)
  LD E,(HL)
  INC HL
  LD D,(HL)               ; DE = tail sprite data ptr for this phase
  LD A,(JonesPos)         ; A = JonesPos
  AND $07                 ; A = sub-character Y pixel offset (0-7)
  CP $04
  JP NC,BlitJonesShifted  ; >= 4: shifted blit (BlitJonesShifted)
  CPL
  AND $03                 ; shift count = 3-(JonesPos&7) in [0,3]
  LD L,A
  RLCA
  RLCA
  RLCA                    ; A = shift_count * 8
  SUB L                   ; A = shift_count * 7 (bytes to skip in shift chain)
  LD ($9BFC),A            ; patch JR offset: BlitJonesShiftCode entry skips N
                          ; SRL/RR pairs
  LD B,$15                ; 21 pixel rows per tail frame
BlitJonesFrameRow:
  PUSH BC
  EX DE,HL
  LD B,(HL)
  INC HL
  LD C,(HL)
  INC HL
  LD E,(HL)
  INC HL
  PUSH HL
  LD HL,(AlienScreenPtr)
  PUSH HL
  XOR A
; Self-modifying right-shift chain. The JR operand byte is patched before entry
; to skip 0-3 SRL/RR groups, giving a right-shift of 0-3 bits across the 3+1
; sprite bytes in {B,C,E,A}. After shifting, the result is XOR-blitted to
; screen.
BlitJonesShiftCode:
  JR BlitJonesShiftCode   ; self-modifying: JR offset patched to skip N SRL/RR
                          ; groups
  SRL B                   ; shift group 3 (shift 3 bits right)
  RR C
  RR E
  RRA
  SRL B                   ; shift group 2
  RR C
  RR E
  RRA
  SRL B                   ; shift group 1 (shift 1 bit right)
  RR C
  RR E
  RRA
  EX AF,AF'               ; swap: A' = carry spill byte
  LD A,B
  XOR (HL)
  LD (HL),A               ; XOR-blit byte 0 to screen
  INC HL
  LD A,C
  XOR (HL)
  LD (HL),A               ; XOR-blit byte 1
  INC HL
  LD A,E
  XOR (HL)
  LD (HL),A               ; XOR-blit byte 2
  INC HL
  EX AF,AF'
  XOR (HL)
  LD (HL),A               ; XOR-blit carry spill byte
  POP HL
  LD A,H
  AND $07
  CP $07
  JR Z,BlitJonesFrameBankCheck
  INC H
  JR BlitJonesFrameNextRow
BlitJonesFrameBankCheck:
  LD A,L
  CP $E0
  JR NC,BlitJonesFrameNextThird
  LD BC,$F920
  ADD HL,BC
  JR BlitJonesFrameNextRow
BlitJonesFrameNextThird:
  LD BC,$0020
  ADD HL,BC
BlitJonesFrameNextRow:
  LD (AlienScreenPtr),HL
  POP DE
  POP BC
  DJNZ BlitJonesFrameRow
  POP BC
  DJNZ AdvanceJonesPos
  RET

; AdvanceJonesPos
;
; Increments JonesPos by 4. If the new value >= 136 the tail sweep is complete:
; freeze the Y animation (JonesPhase |= 128) and return. Otherwise store the
; new position, cycle the phase counter (0->1->2->3->4->0) and jump back to
; BlitJonesFrame.
AdvanceJonesPos:
  LD A,(JonesPos)         ; A = JonesPos
  ADD A,$04               ; advance by 4 pixels
  CP $88                  ; reached bottom of sweep area?
  JR C,AdvanceJonesPosUpdate ; no: update position
  LD A,$80                ; yes: A = freeze flag
  LD (JonesPhase),A       ; JonesPhase = 128 (frozen)
  RET                     ; done
AdvanceJonesPosUpdate:
  LD (JonesPos),A         ; JonesPos = new value
  LD A,(JonesPhase)       ; A = current phase (0-4)
  INC A
  CP $05                  ; wrapped past phase 4?
  JR C,AdvanceJonesPosSetPhase ; no: keep incremented value
  XOR A                   ; yes: wrap to 0
AdvanceJonesPosSetPhase:
  LD (JonesPhase),A       ; JonesPhase = new phase
  JP BlitJonesFrame       ; blit next tail frame

; BlitJonesShifted
;
; Handles the alien tail XOR blit when JonesPos&7 >= 4 (sprite straddles the
; next character row). Uses SLA/RL chains for a left-shift instead of SRL/RR,
; mirroring the logic of BlitJonesFrame's right-shift section. The shift amount
; (pixels left past the half-character boundary) is patched into the
; self-modifying JR at the JR at BlitJonesShiftCode2, then 21 rows of 4 bytes
; are XOR-blitted.
BlitJonesShifted:
  SUB $04                 ; A = (JonesPos&7) - 4, left-shift amount in [0,3]
  LD L,A
  RLCA
  RLCA
  RLCA                    ; A = shift_count * 8
  SUB L                   ; A = shift_count * 7 (JR offset into SLA/RL chain)
  LD ($9C83),A            ; patch BlitJonesShiftCode2 JR offset
  LD B,$15                ; 21 pixel rows
BlitJonesShiftedRow:
  PUSH BC
  EX DE,HL
  LD B,(HL)
  INC HL
  LD C,(HL)
  INC HL
  LD E,(HL)
  INC HL
  PUSH HL
  LD HL,(AlienScreenPtr)
  PUSH HL
  XOR A
BlitJonesShiftCode2:
  JR BlitJonesShiftCode2
  SLA E
  RL C
  RL B
  RLA
  SLA E
  RL C
  RL B
  RLA
  SLA E
  RL C
  RL B
  RLA
  SLA E
  RL C
  RL B
  RLA
  XOR (HL)
  LD (HL),A
  INC HL
  LD A,B
  XOR (HL)
  LD (HL),A
  INC HL
  LD A,C
  XOR (HL)
  LD (HL),A
  INC HL
  LD A,E
  XOR (HL)
  LD (HL),A
  POP HL
  LD A,H
  AND $07
  CP $07
  JR Z,BlitJonesShiftedBankCheck
  INC H
  JR BlitJonesShiftedNextRow
BlitJonesShiftedBankCheck:
  LD A,L
  CP $E0
  JR NC,BlitJonesShiftedNextThird
  LD BC,$F920
  ADD HL,BC
  JR BlitJonesShiftedNextRow
BlitJonesShiftedNextThird:
  LD BC,$0020
  ADD HL,BC
BlitJonesShiftedNextRow:
  LD (AlienScreenPtr),HL
  POP DE
  POP BC
  DJNZ BlitJonesShiftedRow
  POP BC
  DEC B
  JP NZ,AdvanceJonesPos
  RET

; ClearAlienArea
;
; Clears the 19x20-character area of the ZX Spectrum display file and attribute
; RAM used by the alien body animation. Iterates 19 rows of 20 columns from
; DisplayFile, calling DrawSpriteRow_0 to zero each cell, then repeats for the
; attribute area at AttrFileOrigin.
ClearAlienArea:
  LD HL,DisplayFile       ; HL = DisplayFile: start of display file
  LD (DrawScreenPtr),HL   ; store current screen cell address
  LD C,$13                ; 19 character rows
ClearAlienAreaCol:
  LD B,$14
ClearAlienAreaRow:
  PUSH BC
  XOR A
  CALL DrawSpriteRow_0
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ ClearAlienAreaRow
  LD DE,$000C
  LD HL,(DrawScreenPtr)
  LD A,L
  CP $E0
  JR C,ClearAlienAreaNextRow
  LD DE,$070C
ClearAlienAreaNextRow:
  ADD HL,DE
  LD (DrawScreenPtr),HL
  DEC C
  JR NZ,ClearAlienAreaCol
  LD DE,$000C
  LD HL,AttrFileOrigin
  LD C,$13
ClearAlien_RowLoop:
  LD B,$14
ClearAlien_CellLoop:
  LD (HL),$04
  INC HL
  DJNZ ClearAlien_CellLoop
  ADD HL,DE
  DEC C
  JR NZ,ClearAlien_RowLoop
  RET
; LaunchGate: fixture 78 (Narcissus launch console). The launch is
; COUNTERMANDED (msg #28) unless every living crew member's room is 34
; (Narcissus); then Jones must be aboard too (JonesRoom = 34, or the cat
; present in the shuttle strip cells) or msg #27 "BioUnit Jones not present"
; fires first.
LaunchGate:
  LD HL,CrewStatusColumn  ; walk the +7 status column of all 7 crew
  LD DE,$0008
  LD B,$07
Launch_CrewLoop:
  XOR A
  CP (HL)
  JR NZ,Launch_NextCrew
  PUSH DE
  LD DE,$FFFA
  ADD HL,DE
  POP DE
  LD A,(HL)
  CP $22
  JR NZ,Launch_Countermand
Launch_NextCrew:
  ADD HL,DE
  DJNZ Launch_CrewLoop
  LD A,(JonesRoom)
  CP $22
  JR Z,LaunchSequence
  CP $FF
  JR NZ,Launch_NoJones
  LD HL,CatItemLocations
  LD B,$02
Launch_CatItemLoop:
  LD A,(HL)
  CP $FF
  JR Z,Launch_CatItemNext
  CP $22
  JR Z,LaunchSequence
  CP $80
  JR NC,LaunchSequence
Launch_CatItemNext:
  INC HL
  DJNZ Launch_CatItemLoop
Launch_NoJones:
  LD A,$1B
  CALL UpdateCrewAI_8
Launch_Countermand:
  LD A,$1C
  CALL UpdateCrewAI_8
  RET
LaunchSequence:
  LD B,$4B
Launch_FlashLoop:
  PUSH BC
  LD A,$01
  LD (SoundRequest),A
  CALL FrameTiming
  LD DE,$000C
  LD HL,AttrFileOrigin
  LD C,$13
Launch_AttrRows:
  LD B,$14
Launch_AttrLoop:
  LD A,(HL)
  XOR $40
  LD (HL),A
  INC HL
  DJNZ Launch_AttrLoop
  ADD HL,DE
  DEC C
  JR NZ,Launch_AttrRows
  POP BC
  DJNZ Launch_FlashLoop
  JP OptionsScreen_25

; FrameTiming
;
; Controls frame timing and plays a queued sound effect. Reads the sound-
; request flag at SoundReqFlag. If zero, calls the standard one-frame wait at
; BusyWait (approx 1/50s on a 48K Spectrum) and returns. If non-zero, plays a
; beeper tone by reading 1-KB ROM bytes starting at $0200 and toggling bit 4 of
; port $FE for each byte; the outer loop runs 255 × 4 iterations with a short
; delay (D=64 counts) between each toggle. After playback, clears the
; sound-request flag.
;
; USED by the routines at GameEntry, UpdateRoomActors and DrawShipMap.
FrameTiming:
  LD A,(SoundRequest)     ; A = sound-request flag (SoundReqFlag)
  AND A                   ; any sound queued?
  JR NZ,FrameTiming_0     ; yes: play beeper effect
  CALL BusyWait           ; no: standard one-frame timing wait (~1/50s)
  RET
FrameTiming_0:
  LD HL,$0200             ; HL = $0200 (ROM tone data source)
  LD D,$40                ; D = inter-toggle delay count
  LD B,$FF                ; outer loop: 255 iterations
FrameTiming_1:
  PUSH BC
  LD B,$04                ; 4 sub-iterations per outer step
FrameTiming_2:
  PUSH BC
  PUSH DE
  LD A,(HL)               ; read ROM byte
  INC HL                  ; advance data pointer
  AND $10                 ; isolate bit 4 (EAR/MIC output)
  OUT ($FE),A             ; toggle beeper
FrameTiming_3:
  DEC D                   ; delay loop
  JR NZ,FrameTiming_3
  POP DE
  POP BC
  DJNZ FrameTiming_2      ; repeat 4 sub-iterations
  POP BC
  DJNZ FrameTiming_1      ; repeat 255 outer iterations
  XOR A
  LD (SoundRequest),A     ; clear sound-request flag
  RET

; NoteTable
;
; 9 × 4-byte entries used by PlayNoteByIndex. Each entry feeds the ROM BEEP
; routine at $03B5: DE = note duration (number of cycles), HL = T-state
; half-period (frequency).
NoteTable:
  DEFW $000D,$06AA        ; note 0: dur=13, period=1706 T-states
  DEFW $0011,$04F4        ; note 1: dur=17, period=1268 T-states
  DEFW $000D,$0642        ; note 2: dur=13, period=1602 T-states
  DEFW $000A,$086D        ; note 3: dur=10, period=2157 T-states
  DEFW $000B,$077A        ; note 4: dur=11, period=1914 T-states
  DEFW $0013,$0464        ; note 5: dur=19, period=1124 T-states
  DEFW $0019,$0342        ; note 6: dur=25, period=834 T-states
  DEFW $0014,$0425        ; note 7: dur=20, period=1061 T-states
  DEFW $000E,$058F        ; note 8: dur=14, period=1423 T-states

; PhrasePairs
;
; 8 × 2-byte phrase pairs consumed by PlayMusicPhrase. Each pair is
; (note_A_index, note_B_index) into NoteTable; the two notes are played
; alternately 8 times to form a trill, then the next phrase starts.
PhrasePairs:
  DEFB $00,$05            ; phrase 0: notes 0,5
  DEFB $00,$05            ; phrase 1: notes 0,5
  DEFB $01,$06            ; phrase 2: notes 1,6
  DEFB $01,$06            ; phrase 3: notes 1,6
  DEFB $02,$07            ; phrase 4: notes 2,7
  DEFB $02,$07            ; phrase 5: notes 2,7
  DEFB $03,$08            ; phrase 6: notes 3,8
  DEFB $04,$01            ; phrase 7: notes 4,1

; PhraseStateScratch — 2 bytes of mutable state used by PlayMusicPhrase: +0..+1
; = current pointer into PhrasePairs
PhraseStateScratch:
  DEFB $00,$00

; PlayNoteByIndex
;
; Plays one beeper note via the ZX Spectrum ROM BEEP routine at $03B5. Input: A
; = note index (0..8) into NoteTable at NoteTable. Each table entry is 4 bytes:
; 2-byte duration (passed in DE) and 2-byte period in T-states (passed in HL).
; Used twice per inner iteration of PlayMusicPhrase.
PlayNoteByIndex:
  ADD A,A                 ; A *= 2
  ADD A,A                 ; A *= 2 (now A = index × 4 = byte offset)
  LD E,A
  LD D,$00                ; DE = byte offset
  LD HL,NoteTable         ; HL → NoteTable base
  ADD HL,DE               ; HL → 4-byte entry for note A
  LD E,(HL)
  INC HL
  LD D,(HL)               ; DE = duration (cycles)
  INC HL
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A                  ; HL = T-state period
  CALL $03B5              ; ROM BEEP at $03B5 — play tone
  RET

; PlayMusicPhrase
;
; Plays the title-screen jingle: 8 successive 2-note phrases drawn from the
; phrase table at PhrasePairs (PhrasePairs). Each phrase byte is a (loA, hiB)
; pair selecting two notes from NoteTable at NoteTable (PlayNoteByIndex). The
; inner loop repeats each pair 8 times (so the phrase audibly trills on its
; 2-note interval), and the keyboard rows are polled between notes — any key
; press aborts playback via Music_Abort.
;
; USED by the routine at DrawIntroScreen (DrawIntroScreen).
PlayMusicPhrase:
  LD HL,PhrasePairs       ; HL = PhrasePairs base
  LD (PhraseStateScratch),HL
  LD B,$08
Music_NextNotePair:
  PUSH BC
  LD HL,(PhraseStateScratch)
  LD E,(HL)
  INC HL
  LD D,(HL)
  INC HL
  LD (PhraseStateScratch),HL
  LD ($9DDE),DE
  LD B,$08
Music_PlayPair:
  PUSH BC
  LD A,($9DDE)
  CALL PlayNoteByIndex
  LD A,($9DDF)
  CALL PlayNoteByIndex
  LD BC,$FEFE
Music_KeyPoll:
  IN A,(C)
  AND $1F
  XOR $1F
  JR NZ,Music_Abort
  RLC B
  JR C,Music_KeyPoll
  POP BC
  DJNZ Music_PlayPair
  POP BC
  DJNZ Music_NextNotePair
  RET
Music_Abort:
  POP BC
  POP BC
  RET
KeyboardModeFlag:
  NOP
EndgameFlag:
  NOP

; Data block at IntroTileMap
;
; Two screen tables consumed by DrawIntroScreen (DrawIntroScreen): IntroTileMap
; at IntroTileMap (165 bytes) — a 15 × 11 grid of tile indices. The outer loop
; drives 15 vertical column-strips and the inner loop draws 11 tiles down each
; strip via DrawSpriteRow. A tile index N is converted into a bitmap address by
; multiplying by 8 and adding TileBitmaps (TileBitmaps). IntroAttributes at
; IntroAttributes (88 bytes) — an 8 × 11 grid of attribute bytes painted into
; the bottom rows of the attribute file. Mostly bright-white-on-black (7) with
; magenta (4) accents and a couple of brighter cells (56, 60).
IntroTileMap:
  DEFB $00,$00,$00,$01,$02,$03,$04,$00,$00,$00,$00 ; col  0
  DEFB $00,$00,$05,$06,$07,$00,$08,$09,$00,$00,$00 ; col  1
  DEFB $00,$00,$0A,$0B,$0C,$00,$00,$0D,$0E,$00,$00 ; col  2
  DEFB $00,$0F,$10,$11,$12,$00,$00,$00,$13,$14,$00 ; col  3
  DEFB $00,$15,$16,$17,$00,$00,$00,$00,$18,$19,$00 ; col  4
  DEFB $1A,$1B,$1C,$1D,$00,$00,$00,$00,$00,$1E,$00 ; col  5
  DEFB $1F,$20,$21,$22,$00,$00,$00,$00,$00,$23,$24 ; col  6
  DEFB $25,$26,$27,$28,$29,$00,$00,$00,$2A,$2B,$2C ; col  7
  DEFB $2D,$2E,$2F,$30,$31,$32,$33,$34,$35,$36,$37 ; col  8
  DEFB $38,$39,$3A,$3B,$3C,$3D,$3E,$3F,$40,$41,$42 ; col  9
  DEFB $43,$44,$45,$46,$47,$48,$49,$4A,$4B,$4C,$00 ; col 10
  DEFB $00,$4D,$4E,$4F,$50,$51,$52,$53,$54,$55,$00 ; col 11
  DEFB $00,$56,$57,$58,$59,$5A,$5B,$5C,$5D,$00,$00 ; col 12
  DEFB $00,$5E,$5F,$60,$61,$62,$63,$64,$00,$00,$00 ; col 13
  DEFB $00,$00,$00,$65,$66,$67,$68,$00,$00,$00,$00 ; col 14
IntroAttributes:
  DEFB $07,$07,$07,$07,$04,$07,$07,$07,$04,$04,$07 ; row  0
  DEFB $07,$07,$38,$04,$07,$04,$07,$04,$04,$07,$07 ; row  1
  DEFB $07,$07,$38,$04,$04,$07,$04,$07,$07,$07,$07 ; row  2
  DEFB $07,$07,$07,$07,$04,$07,$07,$3C,$07,$07,$07 ; row  3
  DEFB $07,$07,$07,$07,$04,$07,$07,$04,$07,$07,$07 ; row  4
  DEFB $07,$07,$07,$07,$04,$07,$04,$07,$07,$07,$07 ; row  5
  DEFB $07,$07,$04,$07,$04,$04,$07,$07,$07,$07,$07 ; row  6
  DEFB $07,$07,$07,$07,$04,$07,$07,$07,$07,$07,$07 ; row  7

; CrewAction7_Handler
;
; Action-state 7 handler dispatched from CrewActionDispatch
; (CrewActionDispatch, entry index 7). Called via JP (HL) from
; DispatchCrewAction. Calls GatherSameClassSlots to update the sprite position,
; then walks the two nearest-crew slots (NearestCrewA and NearestCrewB) — if
; either crew member is still alive, decrements one of the per-crew counters at
; offset -3 from its alive flag and, depending on the value, either jumps to
; VictimItemDrop or initiates a 29-step approach via UpdateCrewAI_8. Returns
; without further action if both nearest-crew entries are dead or absent (255).
CrewAction7_Handler:
  CALL GatherSameClassSlots
  LD A,(NearestCrewA)
  CP $FF
  JR Z,Android_SecondTarget
  LD C,A
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,ActorStatusColumn
  ADD HL,DE
  XOR A
  CP (HL)
  JR Z,Android_WoundVictim
Android_SecondTarget:
  LD A,(NearestCrewB)
  CP $FF
  RET Z
  LD C,A
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,ActorStatusColumn
  ADD HL,DE
  XOR A
  CP (HL)
  RET NZ
Android_WoundVictim:
  DEC HL
  DEC HL
  DEC HL
  DEC (HL)
  LD A,(HL)
  CP $02
  JP C,VictimItemDrop
  CP $04
  RET NC
  LD L,C
  LD A,(AlienTargetID)
  LD H,A
  LD A,$1D
  CALL UpdateCrewAI_8
  RET

; UpdateAlien
;
; Per-frame driver for the alien's combat behaviour. The alien itself does
; still attack crew directly elsewhere in the per-frame chain — this routine
; specifically handles the **first-encounter activation of the Android**: once
; the alien is loose and meets the alien_target slot for the first time, this
; routine pulls that slot into action state 7 (attack), turning the Android
; from a normal-looking crew member into a hostile actor that wounds nearby
; crew.
;
; RETURNS immediately if any of:
; - AlienActiveFlag (AlienActiveFlag) == 0 (alien not yet loose / no encounter)
; - alien_target's byte +0 != 0 (Android already activated; the
; ACTIVATION stamp at Android_Activate sets +0 = 80, so on every subsequent
; FRAME this guard short-circuits the routine)
; - alien_target's byte +7 != 0 (lockout sentinel — CrewHitsAndroid's
; COLLAPSE path at Android_Collapsed writes +7 = 2, and that prevents
; RE-ACTIVATION if the Android has been defeated)
;
; In other words: the routine fires exactly ONCE per game for the Android, the
; first frame the alien is active and the slot is still in its as-issued
; template state (LongGameCrewInit, LongGameCrewInit, gives +0 = +7 = 0 for
; every slot). That single firing is the "encounter". When all guards pass: 1.
; IX = alien_target record; stamp (IX+0) = 80 (activation marker and timer that
; also blocks future re-entry into this branch). 2. Cache AlienTargetID into
; CrewIndex (CrewIndex) for downstream message enqueues. 3. CALL
; GatherSameClassSlots — scan crew records 1..7 for slots sharing the Android's
; (IX+1) sprite-class and write up to two slot indices into NearestCrewA /
; NearestCrewB (the Android's "kin" — its attack pool). 4. If at least one of
; NearestCrewA/B is alive: enqueue message #30 "{actor} is attacking {target}"
; with actor = AlienTargetID (the Android's name, e.g. "Ash is attacking
; Lambert"), put the alien_target into action state 7 with timer 40, then
; return. 5. If neither kin is reachable, fall through to PickNextRoom (wander)
; and reset the Android to action state 1. Once the Android is in state 7,
; subsequent ResetCrewTimers frames dispatch it through CrewAction7_Handler
; (CrewAction7_Handler) which decrements the wound counter on its
; NearestCrewA/B kin and can collapse them via VictimItemDrop (msg #21 "{actor}
; has collapsed"). This routine is NOT the alien's own attack: the alien kills
; crew that end up in its room (slot 0 record byte +1) — see CrewHitsAlien
; (CrewHitsAlien) and the kill primitive at AlienKillPrimitive. Used by the
; routine at GameEntry.
UpdateAlien:
  LD A,(AlienActiveFlag)  ; A = AlienActiveFlag
  AND A                   ; alien loose on ship?
  RET Z                   ; no: no encounter yet, nothing to activate
  LD HL,(AlienTargetPtr)  ; HL = alien_target record (AlienTargetPtr)
  LD A,(HL)               ; A = byte +0
  AND A                   ; Android already activated (+0 stamped to 80)?
  RET NZ                  ; yes: this routine has already fired once, skip
  LD DE,$0007             ; offset to byte +7 (defeated-lockout sentinel)
  ADD HL,DE               ; HL → +7
Android_ActivateCheck:
  CP (HL)                 ; A=0 here; Z iff byte +7 == 0 (template, not
                          ; defeated)
  RET NZ                  ; +7 != 0: Android collapsed earlier, stay dormant
  PUSH IX                 ; save caller's IX
  LD IX,(AlienTargetPtr)  ; IX = Android record (= alien_target)
Android_Activate:
  LD (IX+$00),$50         ; stamp +0 = 80 — activation marker / blocks re-entry
  LD A,(AlienTargetID)    ; A = AlienTargetID (= Android slot index)
  LD (CrewIndex),A        ; CrewIndex = AlienTargetID (Android's slot)
  CALL GatherSameClassSlots ; pick the Android's two sprite-class kin as
                            ; targets
  LD A,(NearestCrewA)     ; A = nearest crew ID 1 (NearestCrewA)
  CP $FF                  ; $FF = no crew in range
  JR Z,UpdateAlien_0      ; no primary target: wander
  LD C,A                  ; C = crew ID
  ADD A,A                 ; A × 8 (crew record size)
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,ActorStatusColumn ; HL = crew alive-table base ($7385)
  ADD HL,DE               ; HL → NearestCrewA's alive-flag byte
  XOR A                   ; A = 0
  CP (HL)                 ; Z iff (HL) == 0 (which is the "alive" value here)
  JR Z,UpdateAlien_1      ; NearestCrewA alive: attack them
  LD A,(NearestCrewB)     ; A = NearestCrewB
  CP $FF
  JR Z,UpdateAlien_0      ; no secondary target either: wander
  LD C,A
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,ActorStatusColumn ; crew alive-table base
  ADD HL,DE
  XOR A
  CP (HL)                 ; Z iff NearestCrewB's flag == 0 (alive)
  JR Z,UpdateAlien_1      ; alive: attack them
UpdateAlien_0:
  CALL PickNextRoom       ; no reachable crew: alien wanders
  LD (IX+$03),$01         ; Android slot → action state 1 (idle/wander)
  LD (IX+$00),$3C         ; with timer 60 frames
  POP IX
  RET
UpdateAlien_1:
  LD H,C                  ; H = nearest-crew ID (message target)
  LD A,(AlienTargetID)    ; A = AlienTargetID
  LD L,A                  ; L = Android slot index (message actor)
  LD A,$1E                ; A = message #30 "{actor} is attacking {target}"
  CALL UpdateCrewAI_8     ; enqueue: "{Android name} is attacking {nearest}"
  LD (IX+$03),$07         ; Android slot → action state 7 (attack)
  LD (IX+$00),$28         ; with timer 40 frames (next dispatch runs the
                          ; attack)
  POP IX
  RET
; CrewHitsAndroid — combat-resolution dialogue when the current actor engages
; the Android (called from CrewAction4_Handler at Combat_HitAlien). Fires from
; action state 4 (combat) when the actor's (IX+1) sprite-class byte matches the
; alien-target slot's (IX+1). Because (IX+1) is not unique in LongGameCrewInit
; (LongGameCrewInit), this fires for the whole class that contains the Android
; (see CrewAction4_Handler header) — multiple crew members can trigger this
; routine, not only the Android itself. The Android is the slot picked by
; LongGameInit (LongGameInit) from LongGameTargetTable (LongGameTargetTable)
; and stored in AlienTargetID (AlienTargetID), record pointer cached at
; AlienTargetPtr (AlienTargetPtr). It starts dormant and is activated by
; UpdateAlien (UpdateAlien) on first encounter with the alien (the first frame
; AlienActiveFlag is set while the slot's +0/+7 are still template-zero),
; entering action state 7 and wounding its sprite-class kin via
; CrewAction7_Handler (CrewAction7_Handler). This routine is the
; **player-response leg**: when the Android (or another crew in its sprite
; class) is engaged in combat state 4, log the dialogue and apply the
; per-corridor-cell outcome. Guards: pixel coords IX+4 >= 2 and IX+6 >= 2
; (actor placed on screen). Then FindHeldItems locates the actor's corridor
; scan position into (31248): (31248) == 17       → silent: JP to
; UpdateAlien_13 (post-combat fade-out) (31248) >= 20       → silent: JP to
; UpdateAlien_13 (sentinel cells) (31248) == 16       → "net" outcome: write
; $FF to alien-target slot byte +0 (locks the Android out for the rest of this
; engagement), then enqueue message #31 "{actor} nets {target}" with actor =
; CrewIndex (CrewIndex) — the crew slot currently being dispatched — and target
; high byte from (33702). JP to UpdateAlien_13. else                → "hit"
; outcome at CrewHitsAndroid_HitPath: enqueue message #32 "{actor} hits the
; Android" with actor = CrewIndex. Then run the per-(31248) follow-up: -
; decrement alien-target's byte +4 (wound counter); if it underflows past 2,
; stamp byte +7 = 2 (lockout sentinel that prevents UpdateAlien
; re-commandeering on subsequent frames, see UpdateAlien's byte-+7 gate at
; Android_ActivateCheck) and enqueue message #21 "{Android} has collapsed" - if
; (31248) ∈ {6, 7}: enqueue message #5 "{actor}'s Tracker is broken" - if
; (31248) < 12: decrement the slot's entry in ItemCharges (ItemCharges) and
; enqueue message #6 if it expires - other (31248) values branch into the
; corridor-display rebuild at UpdateAlien_5 / UpdateAlien_11 (clears the
; Android's class entries from the corridor display and respawns the room)
; Caveat on the actor name: when (33700) is itself the Android's slot (the
; dispatch ran for the Android slot), the message reads "{Android} hits the
; Android" — apparent self-reference. With the class-not-ID matching above, the
; message ALSO fires for non-Android slots in the same class, e.g. "Lambert
; hits the Android" if AlienTargetID = 4 (Ash). The semantics here are best
; confirmed with an emulator run rather than further static reading. This
; routine is the parallel of CrewHitsAlien (CrewHitsAlien) — identical control
; flow but referring to the Android slot (AlienTargetPtr) and emitting messages
; #31 / #32 instead of #3 / #4.
CrewHitsAndroid:
  LD A,(IX+$04)           ; A = actor pixel X-tile
  CP $02
  RET C                   ; X-tile < 2 → not yet placed: bail
  LD A,(IX+$06)           ; A = actor pixel Y
  CP $02
  RET C                   ; Y < 2 → bail
  LD A,(CrewIndex)        ; A = CrewIndex (slot being dispatched)
  CALL FindHeldItems      ; populate (31248)/(31249) with corridor positions
  LD A,(HeldItemFront)    ; A = scan position in 22-byte anim table
  CP $11
  JP Z,UpdateAlien_13     ; pos 17 → silent post-combat
  CP $14
  JP NC,UpdateAlien_13    ; pos >= 20 → silent (sentinel cells)
  CP $10
  JR NZ,CrewHitsAndroid_HitPath ; pos != 16 → "hits the Android" branch
  LD HL,(AlienTargetPtr)  ; HL = alien-target (Android) slot pointer
  LD (HL),$FF             ; lock Android's byte +0 = $FF for this engagement
  CALL DrawFrontItem
  LD A,(CrewIndex)        ; A = CrewIndex
  LD HL,($83A6)           ; H = high byte of AlienTargetPtr
  LD L,A                  ; L = actor (CrewIndex)
  LD A,$1F                ; A = msg #31 "{actor} nets {target}"
  CALL UpdateCrewAI_8     ; enqueue
  JP UpdateAlien_13
; "{actor} hits the Android" branch (CrewHitsAndroid continued).
CrewHitsAndroid_HitPath:
  PUSH AF                 ; stash (31248) for the post-message dispatch
  LD A,(CrewIndex)        ; A = CrewIndex (slot being dispatched)
  LD L,A                  ; L = actor for the message
  LD A,$20                ; A = msg #32 "{actor} hits the Android"
  CALL UpdateCrewAI_8     ; enqueue
  POP AF                  ; A = (31248) restored
  CP $06
  JR C,Android_TakeHit
  CP $12
  JR Z,Android_TakeHit
  CP $13
  JR NZ,UpdateAlien_2
Android_TakeHit:
  LD HL,(AlienTargetPtr)
  LD DE,$0004
  ADD HL,DE
  DEC (HL)
  LD A,(HL)
  CP $02
  JP NC,UpdateAlien_13
  INC HL
  INC HL
  INC HL
Android_Collapsed:
  LD (HL),$02
  LD DE,$FFF9
  ADD HL,DE
  LD (HL),$00
  LD A,(AlienTargetID)
  LD L,A
  LD A,$15
  CALL UpdateCrewAI_8
  CALL PlayMusic_19
  LD (HL),$00
  RET
UpdateAlien_2:
  CP $08
  JR NC,UpdateAlien_3
  CALL DrawFrontItem
  LD A,$05
  LD HL,(CrewIndex)
  CALL UpdateCrewAI_8
  JR Android_TakeHit
UpdateAlien_3:
  CP $0C
  JR NC,UpdateAlien_5
  LD HL,ItemCharges
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  AND A
  JR Z,UpdateAlien_4
  DEC (HL)
  JP UpdateAlien_13
UpdateAlien_4:
  LD HL,(CrewIndex)
  LD A,$06
  CALL UpdateCrewAI_8
  JP UpdateAlien_13
UpdateAlien_5:
  JP NZ,UpdateAlien_11
  CALL GatherSameClassSlots
  LD DE,NearestCrewC
  LD B,$03
UpdateAlien_6:
  PUSH BC
  PUSH DE
  LD A,(DE)
  CP $FF
  JR Z,UpdateAlien_10
  PUSH AF
  ADD A,$80
  LD E,A
  ADD A,$20
  LD D,A
  LD C,(IX+$01)
  LD HL,ItemLocations
  LD B,$16
UpdateAlien_7:
  LD A,(HL)
  CP D
  JR Z,UpdateAlien_8
  CP E
  JR Z,UpdateAlien_8
  CP C
  JR Z,UpdateAlien_9
UpdateAlien_8:
  LD (HL),$FF
UpdateAlien_9:
  INC HL
  DJNZ UpdateAlien_7
  POP AF
  LD HL,ActorStatusColumn
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  ADD HL,DE
  XOR A
  CP (HL)
  JR NZ,UpdateAlien_10
  LD (HL),$01
  LD DE,$FFF9
  ADD HL,DE
  LD (HL),A
UpdateAlien_10:
  POP DE
  INC DE
  POP BC
  DJNZ UpdateAlien_6
  LD A,$0F
  LD E,(IX+$01)
  CALL AddRoomDamage
  JP InitGameView
UpdateAlien_11:
  LD E,A
  LD D,$00
  LD HL,ItemCharges
  ADD HL,DE
  XOR A
  CP (HL)
  JR Z,UpdateAlien_12
  DEC (HL)
  LD A,$06
  LD E,(IX+$01)
  CALL AddRoomDamage
  JP Android_TakeHit
UpdateAlien_12:
  LD HL,(CrewIndex)
  LD A,$07
  CALL UpdateCrewAI_8
  JP UpdateAlien_13
UpdateAlien_13:
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(DrawSlotIndex)
  LD HL,CrewIndex
  CP (HL)
  RET NZ
  LD A,(AlienActiveFlag)
  AND A
  LD A,(HL)
  RET Z
  LD A,$34
  CALL PlayMusic_26
  LD HL,$48D6
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  RET

; TriggerAlienEvent
;
; Per-frame guard: when the alien's current corridor index is >= 6 AND the
; alien-target crew id is < 8 AND the targeted crew member is dead (alive flag
; at offset 7 of the crew record is zero), fire a "hatch" event by setting the
; alien-source byte to 255 and jumping to the alien-event dispatcher at
; AlienEventDispatch. Otherwise simply returns.
;
; USED by the routine at GameEntry.
TriggerAlienEvent:
  LD HL,$7382             ; HL → alien's current corridor index
  LD A,(HL)               ; A = corridor
  CP $06                  ; corridor 0-5 = pre-alien spawn area
  RET C                   ; below 6: alien not yet active here
  LD A,(AlienTargetID)    ; A = AlienTargetID (crew ID being targeted)
TriggerAlien_TargetCheck:
  CP $08                  ; crew IDs are 0..6; 8+ = none/invalid
  RET NC                  ; no valid target: nothing to do
  LD HL,(AlienTargetPtr)  ; HL = pointer to current crew record
                          ; (AlienTargetPtr)
  LD DE,$0007             ; offset 7 = alive flag
  ADD HL,DE
  XOR A
  CP (HL)
  RET NZ
  LD A,$FF
  JP AlienEventDispatch
  RET
  LD HL,AlienFlags
  LD (HL),$00
  LD BC,$001F
  IN A,(C)
  BIT 2,A
  JR Z,Kempston_UpBit
  SET 3,(HL)
Kempston_UpBit:
  BIT 3,A
  JR Z,Kempston_Merge
  SET 2,(HL)
Kempston_Merge:
  AND $13
  OR (HL)
  LD (HL),A
  RET
  LD HL,AlienFlags
  LD (HL),$00
  LD BC,$EFFE
  IN A,(C)
  CPL
  RRCA
  RRCA
  JR NC,Sinclair_Bit1
  SET 2,(HL)
Sinclair_Bit1:
  RRCA
  JR NC,Sinclair_Bit2
  SET 3,(HL)
Sinclair_Bit2:
  RRCA
  JR NC,Sinclair_Bit3
  SET 0,(HL)
Sinclair_Bit3:
  RRCA
  JR NC,Sinclair_Fire
  SET 1,(HL)
Sinclair_Fire:
  AND $08
  RET Z
  SET 4,(HL)
  RET

; AlienInitTable
;
; 16-byte lookup table indexed by a ROM-script nibble (0-15) inside
; CommonGameInit (CommonGameInit). The looked-up value is ORed with $40
; (direction flag, bit 6) and written to $737F (the alien slot's
; identity/heading byte), randomising the alien's starting attributes.
AlienInitTable:
  DEFB $03,$04,$05,$09,$0A,$0B,$0D,$0E,$12,$13,$14,$14,$16,$18,$19,$1E

; LongGameHostSlotTable
;
; 16-byte lookup table indexed by a ROM-script nibble (0-15) inside
; LongGameInit (LongGameInit). Selects which 8-byte record slot at ActorRecords
; will be the **chestburster host** for this run — i.e. the crew member whose
; name will be substituted into the opening status-bar message #33 "Alien has
; hatched from {actor}" enqueued at LongGame_HatchMsg. The chosen slot has byte
; +1 and byte +7 overwritten with $FF (the HostMarker pattern); that tag is
; what every "skip this slot during normal corridor processing" check keys off
; (e.g. `CP 255` on byte +1 at SelectSlot_HostCheck inside
; SelectSlotByDrawIdx). Values are restricted to {1, 2, 5}, so the host is
; always Dallas, Kane or Lambert.
LongGameHostSlotTable:
  DEFB $01,$02,$05,$01,$02,$05,$01,$02,$05,$01,$02,$05,$01,$02,$05,$02

; LongGameTargetTable
;
; 16-byte lookup table indexed by a ROM-script nibble (0-15) inside
; LongGameInit (LongGameInit). Used to randomise AlienTargetID (AlienTargetID):
; the selector loops until it picks a value different from the host slot chosen
; via LongGameHostSlotTable (LongGameHostSlotTable), then stores the value in
; AlienTargetID and uses it to compute the targeted crew's record pointer at
; AlienTargetPtr. Values are restricted to {1, 2, 4, 6} = the four crew slots
; {Dallas, Kane, Ash, Parker}, always different from the chestburster host
; slot.
;
; THIS selection is the long-game **Android assignment**. The chosen
; CREW member behaves like a normal crew member at first; UpdateAlien
; UpdateAlien activates them as hostile the first time the alien is
; LOOSE AND their slot is still in template state (i.e. they haven't
; BEEN activated or defeated yet). After activation they enter action
; STATE 7 and wound their sprite-class kin via CrewAction7_Handler
; CrewAction7_Handler . Status-bar messages #30 / #32 name that slot.
;
; The four candidate slots correspond to the canonical hatch-victim (Kane = 2)
; plus three crew the alien can plausibly puppet (Dallas / Ash / Parker). Note
; Ash (slot 4) — the canonical film android — is one of the four but the pick
; is random per run.
LongGameTargetTable:
  DEFB $01,$02,$04,$06,$01,$02,$04,$06,$01,$02,$04,$06,$01,$02,$04,$06

; InitialItemRooms
;
; 22 bytes copied by CommonGameInit (CommonInit_ItemsGrilles) to ItemLocations
; (ItemLocations): the starting room of every portable item (see the
; ItemLocations header for the id -> name map). The final two entries are 255 —
; the "Cat in Net"/"Cat in Box" items don't exist until Jones is caught.
InitialItemRooms:
  DEFB $18,$15,$14,$06,$06,$10,$06,$10,$1B,$11,$12,$13,$20,$02,$02,$02
  DEFB $18,$17,$1F,$14,$FF,$FF

; ItemChargesInit
;
; 16 bytes copied by CommonGameInit (CommonInit_Charges) to ItemCharges
; ($83D2): zeros the ItemCharges area then writes 3,3,3,3,1,8,8,8 into
; ItemChargesHi.
ItemChargesInit:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$03,$03,$03,$03,$01,$08,$08,$08

; LongGameCrewInit
;
; 64-byte starting state (eight 8-byte records) copied by LongGameInit
; (LongGameInit) into $7376-$73B5. Layout, with ActorRecords ($7376) as record
; 0:
;
; SLOT 0 ($7376): alien-slot header (all zero)
; SLOT 1 ($737E): alien gameplay slot proper
; SLOT 2 ($7386): crew[0]    \
; SLOT 3 ($738E): crew[1]     |
; SLOT 4 ($7396): crew[2]     |  Initial state for the 6 crew that
; SLOT 5 ($739E): crew[3]     |  share the 64-byte block. crew[6] at
; SLOT 6 ($73A6): crew[4]     |  $73B6 is initialised separately.
; SLOT 7 ($73AE): crew[5]    /
;
; Per-record field layout: see ActorRecords (ActorRecords). In particular +1 is
; the STARTING ROOM (6 = CommdCentr, 27 = Mess) and +4 is the crew member's
; strength. The Long Game opens with Dallas, Kane and Ripley on the bridge and
; Ash, Lambert and Parker in the Mess — the film's dinner scene, ready for
; message #33 "Alien has hatched from {actor}". LongGameInit stamps $FF into
; +1/+7 of the host slot chosen via LongGameHostSlotTable (the HostMarker). All
; seven crew slots start with non-zero +4/+5/+6, so every crew member is alive
; and placed when a Long Game begins. LongGameInit's script-driven step at
; LongGame_PickHost-LongGame_PickTarget then marks one slot as the chestburster
; host — the only crew whose +1 and +7 will hold $FF after init.
LongGameCrewInit:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00 ; slot 0 ALIEN (room written by
                                       ; CommonGameInit)
  DEFB $00,$06,$00,$00,$06,$04,$04,$00 ; slot 1 Dallas  — CommdCentr, strength
                                       ; 6
  DEFB $00,$06,$00,$00,$05,$04,$04,$00 ; slot 2 Kane    — CommdCentr, strength
                                       ; 5
  DEFB $00,$06,$00,$00,$04,$03,$03,$00 ; slot 3 Ripley  — CommdCentr, strength
                                       ; 4
  DEFB $00,$1B,$00,$00,$05,$04,$04,$00 ; slot 4 Ash     — Mess, strength 5
  DEFB $00,$1B,$00,$00,$06,$04,$04,$00 ; slot 5 Lambert — Mess, strength 6
  DEFB $00,$1B,$00,$00,$06,$04,$04,$00 ; slot 6 Parker  — Mess, strength 6
  DEFB $00,$00,$00,$00,$05,$03,$03,$00 ; slot 7 Brett   — placed separately by
                                       ; init

; ShortGameCrewInit
;
; 64-byte starting state (eight 8-byte records) copied by ShortGameInit
; (ShortGameInit) into $7376-$73B5. Same record layout as LongGameCrewInit
; (LongGameCrewInit) — see that block for field semantics.
;
; SLOT occupancy comparison (ShortGameCrewInit vs LongGameCrewInit):
;
; slot 0 ALIEN   : all-zero in both slot 1 Dallas  : Short: $FF in +1/+7 — out
; of play slot 2 Kane    : Short: $FF in +1/+7 — out of play slot 3 Ripley  :
; alive — starts in CommdCentr (+1=6), strength 4 slot 4 Ash     : Short: $FF
; in +1/+7 — out of play slot 5 Lambert : alive — starts in CommdCentr (+1=6),
; strength 4 slot 6 Parker  : alive — starts in CommdCentr (+1=6), strength 6
; slot 7 Brett   : Short: $FF in +1/+7 — out of play The Short Game is the
; film's final act: Dallas, Kane, Ash and Brett are already gone (their slots
; carry the same $FF removed-from-play pattern the Long Game applies to its
; single chestburster host), and Ripley, Lambert and Parker start together on
; the bridge. Combined with the fixed AlienTargetID AlienTargetID=9 (which the
; alien-target guard at TriggerAlien_TargetCheck reads as "no valid target", so
; no crew member turns Android), the Short Game is a pure evade-and-escape
; scenario — whereas the Long Game uses script randomisation to pick a single
; chestburster host plus the Android.
ShortGameCrewInit:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00 ; slot 0 ALIEN (room written by
                                       ; CommonGameInit)
  DEFB $00,$FF,$00,$00,$00,$00,$00,$FF ; slot 1 Dallas  — out of play
  DEFB $00,$FF,$00,$00,$00,$00,$00,$FF ; slot 2 Kane    — out of play
  DEFB $00,$06,$06,$00,$04,$03,$03,$00 ; slot 3 Ripley  — CommdCentr, strength
                                       ; 4
  DEFB $00,$FF,$00,$00,$00,$00,$00,$FF ; slot 4 Ash     — out of play
  DEFB $00,$06,$06,$00,$04,$03,$03,$00 ; slot 5 Lambert — CommdCentr, strength
                                       ; 4
  DEFB $00,$06,$00,$00,$06,$04,$04,$00 ; slot 6 Parker  — CommdCentr, strength
                                       ; 6
  DEFB $00,$FF,$00,$00,$00,$00,$00,$FF ; slot 7 Brett   — out of play

; UI and endgame text bank
;
; Plain-text strings for the ship-map legend, the input-device menu ("Select
; Keyboard or Joystick" - the options screen is device selection, not
; difficulty), the key-redefinition flow, the encounter epilogue ("The Alien
; has been killed / is dead / was the Android", "Competence Rating") and the
; endgame screens. Drawn B characters at a time from DE by the loop at
; DrawStatusPanel_14 via the glyph blitter DrawSpriteRow.
;
; RUNTIME-PATCHED cells: the Competence Rating digits (StrRatingDigits), the
; DESTRUCT countdown minutes and seconds (the blank fields after
; #R42119), poked by the endgame renderer before drawing.
UITextBlock:
  DEFM "MAPPING SYMBOLS"  ; "MAPPING SYMBOLS" (ship-map legend title)
StrConnectingDoor:
  DEFM $01,$0F,$01," : Connecting Door" ; 1,15,1 + " : Connecting Door"
                                        ; (legend)
StrLadderUp:
  DEFM "{  : Ladder to Deck Above" ; "{  : Ladder to Deck Above" (legend)
StrHatchDown:
  DEFM "|  : Hatchway to Deck Below" ; "|  : Hatchway to Deck Below" (legend)
StrSubjectInd:
  DEFM ": Subject Indicator" ; ": Subject Indicator" (legend)
StrRoomInd:
  DEFM ": Room Indicator" ; ": Room Indicator" (legend)
StrGrilleDuct:
  DEFM "}  : Grille to Ducting" ; "}  : Grille to Ducting" (legend)
StrSelectDevice:
  DEFM "Select Keyboard or joystick" ; "Select Keyboard or Joystick" (options
                                     ; title)
StrKempston:
  DEFM "1 : KEMPSTON"     ; "1 : KEMPSTON"
StrAGFProtek:
  DEFM "2 : AGF",$90,"PROTEK" ; "2 : AGF/PROTEK"
StrSinclair:
  DEFM "3 : SINCLAIR"     ; "3 : SINCLAIR"
StrKeyboard:
  DEFM "4 : KEYBOARD"     ; "4 : KEYBOARD"
StrSelect:
  DEFM "5 : SELECT"       ; "5 : SELECT"
StrPressKey:
  DEFM "PRESS APPROPRIATE KEY" ; "PRESS APPROPRIATE KEY"
StrPressPause:
  DEFM "During Game Press 1 to Pause" ; "During Game Press 1 to Pause"
StrUserDefinable:
  DEFM "The keyboard is user definable" ; "The keyboard is user definable"
StrRedefineKeys:
  DEFM "Press Y to redefine keys" ; "Press Y to redefine keys"
StrAnyOtherKey:
  DEFM "Any other key to continue" ; "Any other key to continue"
StrWhichKey:
  DEFM "Which key do you wish to" ; "Which key do you wish to"
StrUseForUp:
  DEFM "use for: UP"      ; "use for: UP"
StrDown:
  DEFM "DOWN"             ; "DOWN"
StrFire:
  DEFM "FIRE"             ; "FIRE"
StrReturnTo:
  DEFM "Return to "       ; "Return to " prefix
  DEFM "QUIT"             ; "QUIT"
; Input-scanner vector table: 4 words indexed by the options-screen device
; choice (0-3). OptionsScreen ($AA5A) patches the chosen word into the CALL
; operand at $874D, so KeyboardScanner's first CALL goes to the device's
; scanner: 41293 Kempston (port 31), 34640 AGF-Protek (cursor-joystick keys
; 5/6/7/8/0 = the keyboard default), 41320 Sinclair (port $EFFE), 44041
; redefinable Keyboard.
InputScannerTable:
  DEFM "M",$A1,"P",$87,"h",$A1,$09,$AC ; DEFW 41293,34640,41320,44041
StrAlienKilled:
  DEFM "The Alien has been killed" ; "The Alien has been kil"
StrIsDead:
  DEFM " is dead"         ; "led" / " is dead"
StrWasAndroid:
  DEFM " was the Android" ; " was the Android"
StrCompetence:
  DEFM "Competence Rating: 0" ; "Competence Rating: "
StrRatingDigits:
  DEFM "0",$87            ; rating digits "00" (runtime-patched)
StrNarcissusLaunched:
  DEFM "The Narcissus is launched" ; "The Narcissus is launched" (escape
                                   ; ending)
StrWillExplode:
  DEFM "The Nostromo will explode " ; "The Nostromo will explode "
DestructTextIn:
  DEFM "in"               ; "in"
  DEFM "   "              ; destruct minutes field (runtime-patched)
  DEFM "minutes"          ; "minutes"
  DEFM "   "              ; destruct seconds field (runtime-patched)
  DEFM " seconds"         ; " seconds"
StrAlienUnleashed:
  DEFM "The Alien will be unleashed" ; "The Alien will be unleashed"
                                     ; (no-destruct ending)
StrUponEarth:
  DEFM "upon the Earth"   ; "upon the Earth"
StrNostromoExploded:
  DEFM "Nostromo has exploded" ; "Nostromo has exploded" (destruct ending)
StrLifeSupportGone:
  DEFM "Life Support Systems exhausted" ; "Life Support Systems exhausted"
StrLifeLevelsMinimal:
  DEFM "Humanoid life levels minimal " ; "Humanoid life levels minimal"
PressAnyKeyText:
  DEFM "PRESS ANY KEY"    ; "PRESS ANY KEY"

; LongGameInit
;
; Game-mode handler for "2: Long Game" (dispatched from GameModeDispatchTable).
;
; THE Long Game is the full-length scenario. All 7 crew start alive but
; ONE of them is selected (via the ROM-traversal script, see AdvanceScriptPtr
; ADVANCESCRIPTPTR) to be the **chestburster host** — the crew member
; THE alien physically hatches out of. That slot is marked by stamping
; THE HostMarker pattern ($FF on bytes +1 and +7 of its 8-byte record)
; SO per-frame routines like SelectSlotByDrawIdx (SelectSlotByDrawIdx) can skip
;    it
; WITH a `CP 255` test on byte +1. The host's name is also baked into
; THE opening status-bar message #33 enqueued at step 7 below.
;
; The script independently chooses the alien's initial stalk target, so each
; Long Game has both a different host AND a different first victim. The
; android-traitor mechanic lives on the OTHER randomised slot picked below:
; AlienTargetID (LongGameTargetTable values {1, 2, 4, 6} = Dallas / Kane / Ash
; / Parker). That slot is dormant at game start — it appears as just another
; crew member. UpdateAlien (UpdateAlien) flips it hostile on the **first
; encounter** with the alien: the first frame AlienActiveFlag is non-zero AND
; the slot still carries the template's zero values in bytes +0 and +7,
; UpdateAlien stamps it into action state 7 (attack) and arms it with
; sprite-class kin as targets. From then on the Android wounds nearby crew via
; CrewAction7_Handler (CrewAction7_Handler), named in status-bar dialogue (msg
; #30 "{Android} is attacking {target}"). When other crew fight it in combat
; state 4, CrewHitsAndroid (CrewHitsAndroid, msg #32 "{actor} hits the
; Android") logs the response. The alien itself kills crew that share its room
; via CrewHitsAlien (CrewHitsAlien) and the kill primitive at
; AlienKillPrimitive — independent of this Android subplot. Steps: 1. Point the
; message queue head (MsgQueueWritePtr MsgQueueWritePtr) at the queue base
; MessageQueue (MessageQueue) and clear the two run-state bytes
; MsgDrawnCol/MsgVisibleTimer. 2. CALL PlayMusic_24: reseed the script pointer
; from the ZX SEED system variable and reset the message-dup marker
; (MsgLastEnqueued MsgLastEnqueued) to $FFFF. 3. LDIR 64 bytes from
; LongGameCrewInit (LongGameCrewInit) into $7376 to populate the alien slot and
; the seven crew slots with the long-game starting state (all crew alive,
; normal alien position). 4. Pull one script nibble, look it up in
; LongGameHostSlotTable (LongGameHostSlotTable) to get the host slot index
; A_host (one of {1, 2, 5}), then stamp the HostMarker on that record by
; overwriting bytes +1 and +7 with $FF. 5. Loop pulling more script nibbles,
; looking them up in LongGameTargetTable (LongGameTargetTable), until the
; looked-up value A_target differs from A_host — the Android can't be the same
; slot the alien just hatched out of. 6. Store A_target in AlienTargetID
; (AlienTargetID) — this slot is the per-run **Android**: a dormant traitor
; crew member that UpdateAlien (UpdateAlien) flips hostile on first encounter
; with the loose alien. 7. Enqueue message #33 ("Alien has hatched from
; {actor}") via UpdateCrewAI_8, with the host slot index as the actor parameter
; — this is the long game's opening narrative beat. 8. Store &slot[A_target] in
; AlienTargetPtr (AlienTargetPtr) so UpdateAlien and CrewHitsAndroid can access
; the Android's crew record directly. 9. Fall through into the common init at
; CommonGameInit (CommonGameInit). Used by the dispatcher at GameModeJump via
; the table at GameModeDispatchTable.
LongGameInit:
  LD HL,MessageQueue      ; HL = MessageQueue (event queue base)
  LD (MsgQueueWritePtr),HL ; event queue head = queue base (empty queue)
  XOR A                   ; A = 0
  LD (MsgVisibleTimer),A  ; clear run-state byte MsgVisibleTimer
  LD (MsgDrawnCol),A      ; clear run-state byte MsgDrawnCol
  CALL PlayMusic_24       ; reset message-dup marker + reseed script pointer
  LD HL,LongGameCrewInit  ; HL = LongGameCrewInit ($A1E4)
  LD DE,ActorRecords      ; DE = $7376 (alien slot + crew table base)
LongGame_SetupRecords:
  LD BC,$0040             ; 64 bytes = 8 × 8-byte records
LongGame_CopyRecords:
  LDIR                    ; copy long-game starting crew/alien state
  CALL AdvanceScriptPtr   ; A = script nibble (0-15)
  LD E,A
  LD D,$00
  LD HL,LongGameHostSlotTable ; HL = LongGameHostSlotTable ($A19E)
  ADD HL,DE
LongGame_PickHost:
  LD A,(HL)               ; A_host = chestburster-host slot index for this run
  PUSH AF                 ; stash A_host
  ADD A,A
  ADD A,A
  ADD A,A                 ; A_host × 8 (record stride)
  LD E,A
  LD HL,ActorRecords
  ADD HL,DE               ; HL = &slot[A_host]
  INC HL                  ; HL = &slot[A_host] + 1 (crew ID byte)
  LD A,(HL)
  LD ($73B7),A            ; save original ID at $73E7 (never read; effectively
                          ; scratch)
  LD (HL),$FF             ; stamp HostMarker: slot[A_host].byte+1 = $FF
  LD DE,$0006
  ADD HL,DE               ; HL = &slot[A_host] + 7 (alive flag)
LongGame_PickTarget:
  LD (HL),$FF             ; stamp HostMarker: slot[A_host].byte+7 = $FF
PickAlienTargetLoop:
  CALL AdvanceScriptPtr   ; A = next script nibble
  LD HL,LongGameTargetTable ; HL = LongGameTargetTable ($A1AE)
  LD E,A
  LD D,$00
  ADD HL,DE
  LD C,(HL)               ; C = candidate alien-target index
  POP AF                  ; A = A_host
  CP C
LongGame_TargetOk:
  JR NZ,StoreAlienTarget  ; if C != A_host → accept
  PUSH AF
LongGame_Retry:
  JR PickAlienTargetLoop  ; else: pull another nibble (avoid colliding with
                          ; host)
StoreAlienTarget:
  LD HL,AlienTargetID     ; HL = &AlienTargetID
LongGame_SetTarget:
  LD (HL),C               ; AlienTargetID = C (randomised)
  PUSH BC
  LD L,A                  ; L = A_host (actor param for message)
  LD A,$21                ; A = 33 → message "Alien has hatched from {actor}"
LongGame_HatchMsg:
  CALL UpdateCrewAI_8     ; enqueue opening narrative naming the host
LongGame_StoreTargetPtr:
  POP BC
  LD A,C                  ; A = AlienTargetID
  ADD A,A
  ADD A,A
  ADD A,A                 ; A × 8
  LD E,A
  LD D,$00
  LD HL,ActorRecords
  ADD HL,DE               ; HL = &slot[AlienTargetID]
  LD (AlienTargetPtr),HL  ; store alien-target record pointer at AlienTargetPtr
; This entry point is used by the routine at ScreenTransition. CommonGameInit:
; shared post-init for both Short and Long modes. Called (via JP) from
; ShortGameInit_Done (ShortGameInit) and fallen into from LongGameInit
; (LongGameInit). Resets the alien runtime state, seeds the crew workspace
; tables, clears the corridor cursor at $757C, picks the initial alien event
; timer from the script, and switches the room mode to 1 (corridor view) so the
; player drops straight into gameplay.
CommonGameInit:
  XOR A
  LD (AlienActiveFlag),A
  LD HL,DrawMenuStrip     ; HL = $8080: two "frozen" bytes for both channels
  LD (CrewAnimFlags),HL
  LD A,L
  LD (AlienXPhase),A
  LD (JonesPhase),A
CommonInit_ItemsGrilles:
  LD HL,InitialItemRooms  ; HL = InitialItemRooms ($A1BE)
  LD DE,ItemLocations     ; DE = ItemLocations ($74B4)
  LD BC,$0016
  LDIR                    ; place the 22 items in their start rooms
  LD B,$21                ; RoomGrilleState: put all grilles in place
  LD HL,RoomGrilleState   ; (33 bytes; room 33's byte stays 0 = open)
  LD A,$FF
CommonInit_GrilleLoop:
  LD (HL),A
  INC HL
  DJNZ CommonInit_GrilleLoop
  LD E,$0D                ; ...except Corridor#6 (room 13):
  LD D,$00                ; its grille starts removed
  LD HL,RoomGrilleState
  ADD HL,DE
  LD (HL),D
  LD HL,ShipSystemFlags
  LD (HL),D
  CALL AdvanceScriptPtr
  LD (JonesRoom),A
  CALL AdvanceScriptPtr
  LD E,A
  LD D,$00
  LD HL,AlienInitTable    ; HL = AlienInitTable ($A18E)
  ADD HL,DE
  LD A,(HL)               ; A = AlienInitTable[script_byte]
  OR $40                  ; set direction bit (bit 6)
CommonInit_AlienRoom:
  LD ($737F),A            ; set the alien's starting room (slot 0 byte +1)
CommonInit_Charges:
  LD HL,ItemChargesInit   ; HL = ItemChargesInit ($A1D4)
  LD DE,ItemCharges
  LD BC,$0010
  LDIR
  LD HL,RoomDamageDigits
  LD (HL),$30
  LD DE,$7537
  LD BC,$0043
  LDIR
  LD HL,$3030
  LD (OxygenLo),HL
  LD HL,$3537
  LD (OxygenHi),HL
  LD A,$20
  LD ($95E5),A
  LD A,$01
  LD (RoomTypeByte),A
  RET

; ClearDisplay
;
; Fills the entire ZX Spectrum display file with zero bytes, blanking all pixel
; data. The display file occupies DisplayFile-$57FF (6144 bytes). Uses a single
; LDIR from DisplayFile into $4001 after priming the first byte to 0.
;
; USED by the routines at DrawIntroScreen, DrawShipMap, OptionsScreen and
;      ScreenTransition.
ClearDisplay:
  LD HL,DisplayFile       ; HL = DisplayFile (start of display file)
  LD DE,$4001             ; DE = $4001 (destination = source+1)
  LD BC,$17FF             ; BC = 6143 bytes to copy
  LD (HL),$00             ; prime first byte = 0
  LDIR                    ; propagate zero across entire display file
  RET

; FillAttributes
;
; Fills the entire ZX Spectrum attribute file with the value in A. The
; attribute file occupies AttrFileOrigin-$5AFF (768 bytes). Uses a single LDIR
; after priming the first byte. Common attribute values: 0=black on black
; (hidden), 7=white ink on black paper, 32=green ink on black paper.
;
; Input: A = attribute byte to flood-fill
;
; Used by the routines at XorHAddE, DrawIntroScreen, DrawShipMap, OptionsScreen
; and ScreenTransition.
FillAttributes:
  LD HL,AttrFileOrigin    ; HL = AttrFileOrigin (start of attribute file)
  LD DE,$5801             ; DE = $5801
  LD (HL),A               ; prime first attribute byte
  LD BC,$02FF             ; BC = 767 remaining bytes
  LDIR                    ; propagate across entire attribute file
  RET

; Ship-map screen title and credits
;
; Plain ASCII (previously misclassified as code), drawn by DrawShipMap
; (DrawShipMap): the big "ALIEN" title (one glyph per cell, 3-column spacing)
; and the author/publisher credit lines "John Heap" / "Argus Press Software".
StrAlien:
  DEFM "ALIEN"
  DEFM "        "
StrJohn:
  DEFM "John"
StrHeap:
  DEFM "Heap"
StrArgus:
  DEFM "Argus"
StrPress:
  DEFM "Press"
StrSoftware:
  DEFM "Software"
StrCopyright:
  DEFM "Copyright 1984"

; DrawIntroScreen
;
; Draws and animates the Alien title screen. Clears the display, sets border to
; black, fills attributes (0 = black on black = hidden). Renders a 15×11 tile
; grid: each byte of the tile map at IntroTileMap is a tile index; multiplied
; by 8 gives an offset into the 8-byte-per-tile bitmap array at TileBitmaps
; (actually at TileBitmaps = TileBitmaps decimal, in the extra-data area loaded
; at AlienFrame0). Tile rows are blitted column by column down into the display
; file. After drawing, loops animating colours until any key on row $F7FE is
; pressed.
;
; USED by the routine at GameEntry.
DrawIntroScreen:
  CALL ClearDisplay       ; ClearDisplay: blank all pixel data
  XOR A                   ; A = 0 (black border, black attributes)
  OUT ($FE),A             ; set border colour = black
  CALL FillAttributes     ; FillAttributes(0): all black-on-black (invisible)
  LD DE,IntroTileMap      ; DE = IntroTileMap (intro tile-map data)
  LD HL,$408A             ; HL = $408A (screen address: top-left of tile area)
  LD (DrawScreenPtr),HL   ; store as current draw pointer (DrawSlotIndex)
  LD B,$0F                ; 15 tile columns
DrawIntroScreen_0:
  PUSH BC
  LD B,$0B
DrawIntroScreen_1:
  PUSH BC
  PUSH DE
  LD A,(DE)
  LD L,A
  LD H,$00
  ADD HL,HL
  ADD HL,HL
  ADD HL,HL
  LD DE,TileBitmaps
  ADD HL,DE
  LD DE,(DrawScreenPtr)
  LD B,$08
DrawIntroScreen_2:
  LD A,(HL)
  LD (DE),A
  INC D
  INC HL
; This entry point is used by the routine at FillAttributes.
DrawIntroScreen_3:
  DJNZ DrawIntroScreen_2
  LD HL,DrawScreenPtr
  INC (HL)
; This entry point is used by the routine at FillAttributes.
DrawIntroScreen_4:
  POP DE
  INC DE
  POP BC
  DJNZ DrawIntroScreen_1
  LD BC,$0015
  LD HL,(DrawScreenPtr)
  LD A,L
  CP $E0
  JR C,DrawIntroScreen_5
  LD BC,$0715
DrawIntroScreen_5:
  ADD HL,BC
  LD (DrawScreenPtr),HL
  POP BC
  DJNZ DrawIntroScreen_0
  LD A,$07
  CALL FillAttributes
  LD HL,$596A
  LD DE,IntroAttributes
  LD B,$08
DrawIntroScreen_6:
  PUSH BC
  LD B,$0B
DrawIntroScreen_7:
  LD A,(DE)
  LD (HL),A
  INC DE
  INC HL
  DJNZ DrawIntroScreen_7
  LD BC,$0015
  ADD HL,BC
  POP BC
  DJNZ DrawIntroScreen_6
  LD HL,$4029
  LD (DrawScreenPtr),HL
  LD DE,StrAlien
  LD B,$05
DrawIntroScreen_8:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  LD HL,DrawScreenPtr
  INC (HL)
  INC (HL)
  INC (HL)
  CALL PlayMusicPhrase
  POP DE
  INC DE
  POP BC
  DJNZ DrawIntroScreen_8
  LD HL,$5084
  LD (DrawScreenPtr),HL
  LD B,$04
  LD DE,StrJohn
  CALL DrawStatusPanel_14
  LD HL,$50A4
  LD (DrawScreenPtr),HL
  LD B,$04
  LD DE,StrHeap
  CALL DrawStatusPanel_14
  LD HL,$5096
  LD (DrawScreenPtr),HL
  LD B,$05
  LD DE,StrArgus
  CALL DrawStatusPanel_14
  LD HL,$50B6
  LD (DrawScreenPtr),HL
  LD B,$05
  LD DE,StrPress
  CALL DrawStatusPanel_14
  LD HL,$50D6
  LD (DrawScreenPtr),HL
  LD B,$08
  LD DE,StrSoftware
  CALL DrawStatusPanel_14
  LD HL,Screen_R23C8
  LD (DrawScreenPtr),HL
  LD B,$0E
  LD DE,StrCopyright
  CALL DrawStatusPanel_14
DrawIntroScreen_9:
  CALL PlayMusicPhrase
  LD BC,$FEFE
DrawIntroScreen_10:
  IN A,(C)
  AND $1F
  XOR $1F
  RET NZ
  RLC B
  JR C,DrawIntroScreen_10
  JR DrawIntroScreen_9

; DrawShipMap
;
; Draws the Nostromo ship overview map and handles game-mode selection. Sets
; border green, fills attributes (32), clears display, then blits the deck plan
; as tile blocks at fixed screen positions. After drawing, prints the crew
; names via PrintName (one name per corridor segment) and the four-line menu
; ("1: Short Game", "2: Long Game", "3: Introduction", "4: Select") from the
; text table at GameModeText (GameModeText).
;
; KEY input loop reads port $F7FE:
; KEY 1 — highlight "Short Game"   (selection 0)
; KEY 2 — highlight "Long Game"    (selection 1)
; KEY 3 — highlight "Introduction" (selection 2)
; KEY 4 — confirm: dispatch through 3-word table at GameModeDispatchTable
;     (GameModeDispatchTable)
; TO the handler for the highlighted mode, then return
;
; "4: Select" is a confirm key, not a fourth game mode. Used by the routine at
; GameEntry.
DrawShipMap:
  LD A,$04                ; A = 4 (green border)
  OUT ($FE),A             ; set border green
  LD A,$20                ; attribute 32 = green ink on black paper
  CALL FillAttributes     ; FillAttributes(32)
  CALL ClearDisplay       ; ClearDisplay
  CALL OptionsScreen_21   ; wait for display to stabilise
  LD DE,CrewPortraits
  LD HL,$4044
  LD B,$04
DrawShipMap_0:
  PUSH BC
  PUSH HL
  PUSH DE
  CALL ArmStraight_19
  POP DE
  LD HL,$0048
  ADD HL,DE
  EX DE,HL
  POP HL
  LD BC,$0007
  ADD HL,BC
  POP BC
  DJNZ DrawShipMap_0
  LD HL,$4807
  LD B,$03
DrawShipMap_1:
  PUSH BC
  PUSH HL
  PUSH DE
  CALL ArmStraight_19
  POP DE
  LD HL,$0048
  ADD HL,DE
  EX DE,HL
  POP HL
  LD BC,$0007
  ADD HL,BC
  POP BC
  DJNZ DrawShipMap_1
  LD HL,$40C3
  LD A,$01
  LD (DrawScreenPtr),HL
  CALL PrintName
  LD HL,$40CB
  LD (DrawScreenPtr),HL
  LD A,$02
  CALL PrintName
  LD HL,$40D1
  LD (DrawScreenPtr),HL
  LD A,$03
  CALL PrintName
  LD HL,$40D9
  LD (DrawScreenPtr),HL
  LD A,$04
  CALL PrintName
  LD HL,$4885
  LD (DrawScreenPtr),HL
  LD A,$05
  CALL PrintName
  LD HL,$488D
  LD (DrawScreenPtr),HL
  LD A,$06
  CALL PrintName
  LD HL,$4895
  LD (DrawScreenPtr),HL
  LD A,$07
  CALL PrintName
  LD DE,GameModeText
  LD HL,$48E9
  LD (DrawScreenPtr),HL
  PUSH DE
  XOR A
  CALL PrintName_0
  LD HL,$5029
  LD (DrawScreenPtr),HL
  POP DE
  PUSH DE
  LD A,$01
  CALL PrintName_0
  LD HL,$5069
  LD (DrawScreenPtr),HL
  POP DE
  PUSH DE
  LD A,$02
  CALL PrintName_0
  LD HL,$50A9
  LD (DrawScreenPtr),HL
  POP DE
  PUSH DE
  LD A,$03
  CALL PrintName_0
  LD HL,$50E6
  LD (DrawScreenPtr),HL
  POP DE
  LD A,$04
  CALL PrintName_0
  XOR A
  LD (CorridorCursor),A
  CALL HighlightGameMode
DrawShipMap_2:
  LD BC,$F7FE
  IN A,(C)
  BIT 0,A
  JR Z,DrawShipMap_3
  BIT 1,A
  JR Z,DrawShipMap_4
  BIT 2,A
  JR Z,DrawShipMap_5
  BIT 3,A
  JR Z,DrawShipMap_6
  JR DrawShipMap_2
DrawShipMap_3:
  LD A,(CorridorCursor)
  AND A
  JR Z,DrawShipMap_2
  CALL HighlightGameMode
  XOR A
  LD (CorridorCursor),A
  CALL HighlightGameMode
  JR DrawShipMap_2
DrawShipMap_4:
  LD A,(CorridorCursor)
  CP $01
  JR Z,DrawShipMap_2
  CALL HighlightGameMode
  LD A,$01
  LD (CorridorCursor),A
  CALL HighlightGameMode
  JR DrawShipMap_2
DrawShipMap_5:
  LD A,(CorridorCursor)
  CP $02
  JR Z,DrawShipMap_2
  CALL HighlightGameMode
  LD A,$02
  LD (CorridorCursor),A
  CALL HighlightGameMode
  JR DrawShipMap_2
DrawShipMap_6:
  LD A,(CorridorCursor)
  ADD A,A
  LD HL,GameModeDispatchTable
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
GameModeJump:
  JP (HL)

; GameModeDispatchTable
;
; 3-word jump table indexed by the highlighted game mode (0-2). JP (HL) at
; GameModeJump lands here; the word for the current selection is loaded into HL
; and jumped to.
;
; MODE 0 — Short Game   → ShortGameInit (ShortGameInit)
; MODE 1 — Long Game    → LongGameInit (LongGameInit)
; MODE 2 — Introduction → IntroductionMode (IntroductionMode)
;
; Short vs Long Game summary (see the two init routines for full detail):
; AlienTargetID (AlienTargetID): - Short Game = 9, hard-wired at
; ShortGame_SetTarget. Crucially, this is >= 8, which the alien-event guard at
; TriggerAlien_TargetCheck (TriggerAlienEvent's "CP 8 / RET NC") treats as "no
; valid target" — so the alien's targeted- stalk logic is effectively disabled
; in Short Game and the alien just roams. - Long Game  = random pick from
; LongGameTargetTable (LongGameTargetTable, values in {1, 2, 4, 6}) at
; LongGame_SetTarget. Always < 8, so the alien actively hunts the chosen crew
; member. Chestburster host slot (HostMarker = byte+1=$FF, byte+7=$FF): - Long
; Game  = exactly ONE slot, picked randomly via LongGameHostSlotTable
; (LongGameHostSlotTable, values {1,2,5}). That crew is the chestburster host —
; message #33 enqueued at startup ("Alien has hatched from {actor}")
; substitutes this crew's name in. - Short Game = FOUR pre-stamped slots in the
; template: Dallas (1), Kane (2), Ash (4) and Brett (7) are out of play,
; leaving only Ripley, Lambert and Parker — the film's final act — and no host
; subplot. (The in-game "Android" of message #32 "{actor} hits the Android" is
; the crew slot selected into AlienTargetID AlienTargetID — see the notes at
; LongGameTargetTable and the activation logic in UpdateAlien.) Starting
; crew/alien record state at $7376: - Short Game = ShortGameCrewInit
; (ShortGameCrewInit) — four slots pre-stamped with the HostMarker, yielding a
; smaller "active" starting cast - Long Game  = LongGameCrewInit
; (LongGameCrewInit) — every slot starts with normal (non-$FF) data;
; LongGameInit then picks ONE slot via LongGameHostSlotTable
; (LongGameHostSlotTable, values in {1, 2, 5}) and stamps the HostMarker on
; that record's bytes +1 and +7 Script-driven startup message: - Short Game
; does not enqueue any startup message - Long Game enqueues message #33 ("Alien
; has hatched from {actor}") carrying the host slot index via UpdateCrewAI_8 —
; the long game's opening narrative beat naming the chestburster host Both
; modes then fall through to CommonGameInit (CommonGameInit), which finishes
; the per-session setup identically (alien position, animation tables, room
; mode = 1 corridor view). In short: the Short Game is a deterministic,
; abbreviated scenario; the Long Game uses the ROM-traversal script
; (AdvanceScriptPtr) to randomise both the alien's first target and one tagged
; crew slot per run, producing a longer and more varied game.
GameModeDispatchTable:
  DEFW ShortGameInit,LongGameInit,IntroductionMode

; GameModeText
;
; 0xFF-terminated text strings for the game-mode menu, rendered by DrawShipMap
; (DrawShipMap) via five calls to PrintName_0. Indices 0-3 are the four menu
; lines; index 4 is the footer prompt.
GameModeText:
  DEFB $31,$3A,$20,$53,$68,$6F,$72,$74,$20,$47,$61,$6D,$65,$FF ; "1: Short
                                                               ; Game"
  DEFB $32,$3A,$20,$4C,$6F,$6E,$67,$20,$47,$61,$6D,$65,$FF ; "2: Long Game"
  DEFB $33,$3A,$20,$49,$6E,$74,$72,$6F ; "3: Intro
  DEFB $64,$75,$63,$74,$69,$6F,$6E,$FF ; duction"
  DEFB $34,$3A,$20,$53,$65,$6C,$65,$63,$74,$FF ; "4: Select"
  DEFB $50,$72,$65,$73,$73,$20,$41,$70,$70,$72,$6F ; "Press Appro
  DEFB $70,$72,$69,$61,$74,$65,$20,$4B,$65,$79,$FF ; priate Key"

; HighlightGameMode
;
; XOR-toggles the attribute colour of the currently selected menu row, used by
; DrawShipMap (DrawShipMap) to highlight or erase the cursor. Loads the base
; attribute address $59E9 (row 15, col 9), steps 64 bytes per row (selection ×
; 64), then XORs 15 consecutive attribute bytes with 36 (flipping ink/paper to
; show highlight).
HighlightGameMode:
  LD HL,$59E9
; This entry point is used by the routine at OptionsScreen.
HighlightGameMode_0:
  LD DE,$0040
HighlightGameMode_1:
  LD A,(CorridorCursor)
  AND A
  JR Z,HighlightGameMode_3
  LD B,A
HighlightGameMode_2:
  ADD HL,DE
  DJNZ HighlightGameMode_2
HighlightGameMode_3:
  LD B,$0F
HighlightGameMode_4:
  LD A,(HL)
HighlightGameMode_5:
  XOR $24
  LD (HL),A
  INC HL
  DJNZ HighlightGameMode_4
  RET

; IntroductionMode
;
; Game-mode handler for "3: Introduction" (dispatched from
; GameModeDispatchTable). Clears display, sets border/attrs white, then replays
; the animated intro screen and returns to the main loop.
IntroductionMode:
  CALL ClearDisplay
  XOR A
  OUT ($FE),A
  LD A,$07
  CALL FillAttributes
  LD HL,Screen_R1C8
  LD (DrawScreenPtr),HL
  LD DE,UITextBlock
  LD B,$0F
  CALL DrawStatusPanel_14
  LD HL,$40A4
  LD (DrawScreenPtr),HL
  LD DE,StrConnectingDoor
  LD B,$15
  CALL DrawStatusPanel_14
  LD HL,$40E5
  LD (DrawScreenPtr),HL
  LD DE,StrLadderUp
  LD B,$19
  CALL DrawStatusPanel_14
  LD HL,$4825
  LD (DrawScreenPtr),HL
  LD DE,StrHatchDown
  LD B,$1B
  CALL DrawStatusPanel_14
  LD HL,Screen_R12C8
  LD (DrawScreenPtr),HL
  LD DE,StrSubjectInd
  LD B,$13
  CALL DrawStatusPanel_14
  LD HL,Screen_R15C8
  LD (DrawScreenPtr),HL
  LD DE,StrRoomInd
  LD B,$10
  CALL DrawStatusPanel_14
  LD HL,$5025
  LD (DrawScreenPtr),HL
  LD DE,StrGrilleDuct
  LD B,$16
  CALL DrawStatusPanel_14
  LD HL,$0000
  LD (CrewAnimFlags),HL
  LD HL,$4864
  LD (SprDestA),HL
  LD HL,$48C4
  LD (SpriteDestAddr),HL
  XOR A
  LD B,$01
  CALL AnimateCrewB_0
  XOR A
  LD B,$01
  CALL AnimateCrewA_0
  LD HL,$5085
  LD (DrawScreenPtr),HL
  LD DE,StrSoundOfA
  LD B,$16
  CALL DrawStatusPanel_14
  LD HL,$50C5
  LD (DrawScreenPtr),HL
  LD DE,StrDoorOpened
  LD B,$11
  CALL DrawStatusPanel_14
  LD B,$05
IntroductionMode_0:
  PUSH BC
  LD A,$01
  LD (SoundRequest),A
  CALL FrameTiming
  CALL IntroductionMode_4
  POP BC
  DJNZ IntroductionMode_0
  LD HL,$50C5
  LD (DrawScreenPtr),HL
  LD DE,StrGrilleRemoved
  LD B,$14
  CALL DrawStatusPanel_14
  LD B,$05
IntroductionMode_1:
  PUSH BC
  LD A,$01
  LD (SoundPending),A
  CALL PlayMusic
  CALL IntroductionMode_4
  POP BC
  DJNZ IntroductionMode_1
  LD HL,$50C5
  LD (DrawScreenPtr),HL
  LD DE,StrTrackerReading
  LD B,$18
  CALL DrawStatusPanel_14
  LD B,$0F
IntroductionMode_2:
  PUSH BC
  CALL RomBeep
  CALL IntroductionMode_4
  POP BC
  DJNZ IntroductionMode_2
  XOR A
  LD (SysLastKey),A
IntroductionMode_3:
  CALL BusyWait
  CALL AnimateCrewB
  CALL AnimateCrewA
  LD A,(SysLastKey)
  AND A
  JR Z,IntroductionMode_3
  JP DrawShipMap
IntroductionMode_4:
  LD B,$14
IntroductionMode_5:
  PUSH BC
  CALL AnimateCrewB
  CALL AnimateCrewA
  CALL BusyWait
  CALL AnimateCrewB
  CALL AnimateCrewA
  POP BC
  DJNZ IntroductionMode_5
  RET

; Introduction-mode sound-demo captions
;
; Plain ASCII (previously misclassified as code): the caption prefix and the
; three effect names shown while the Introduction mode plays each sound.
StrSoundOfA:
  DEFM "This is the sound of a"
StrDoorOpened:
  DEFM "Door being opened"
StrGrilleRemoved:
  DEFM "Grille being removed"
StrTrackerReading:
  DEFM "Positive Tracker Reading"

; OptionsScreen
;
; Draws the difficulty-selection screen and waits for a key. Clears display,
; fills attributes green (32), then draws the game title and five option lines
; using the tile-blit helper DrawStatusPanel_14. Reads port $F7FE in a loop:
; keys 1-4 (bits 0-3) set CorridorCursor (reused here as difficulty, 0-3) and
; redraw the highlighted option; key 5 (bit 4) confirms the selection and
; returns. The selected difficulty level is stored at CorridorCursor and used
; throughout the game to scale alien speed and crew health.
;
; USED by the routine at GameEntry.
OptionsScreen:
  CALL ClearDisplay       ; ClearDisplay
  LD A,$20                ; green ink on black
  CALL FillAttributes     ; FillAttributes(32)
  LD HL,$4042             ; HL = $4042 (screen row for title)
  LD (DrawScreenPtr),HL   ; set draw pointer
  LD DE,StrSelectDevice   ; DE = StrSelectDevice (title tile data)
  LD B,$1B                ; 27 characters wide
  CALL DrawStatusPanel_14 ; blit title text
  LD HL,$40EB
  LD (DrawScreenPtr),HL
; This entry point is used by the routine at DrawShipMap.
OptionsScreen_0:
  LD DE,StrKempston
  LD B,$0C
  CALL DrawStatusPanel_14
; This entry point is used by the routine at DrawShipMap.
OptionsScreen_1:
  LD HL,$482B
  LD (DrawScreenPtr),HL
; This entry point is used by the routine at DrawShipMap.
OptionsScreen_2:
  LD DE,StrAGFProtek
; This entry point is used by the routine at DrawShipMap.
OptionsScreen_3:
  LD B,$0E
; This entry point is used by the routine at DrawShipMap.
OptionsScreen_4:
  CALL DrawStatusPanel_14
  LD HL,$486B
  LD (DrawScreenPtr),HL
  LD DE,StrSinclair
  LD B,$0C
  CALL DrawStatusPanel_14
  LD HL,$48AB
  LD (DrawScreenPtr),HL
; This entry point is used by the routine at DrawShipMap.
OptionsScreen_5:
  LD DE,StrKeyboard
  LD B,$0C
  CALL DrawStatusPanel_14
  LD HL,$48EB
  LD (DrawScreenPtr),HL
; This entry point is used by the routine at DrawShipMap.
OptionsScreen_6:
  LD DE,StrSelect
  LD B,$0A
  CALL DrawStatusPanel_14
  LD HL,$5066
  LD (DrawScreenPtr),HL
  LD DE,StrPressKey
  LD B,$15
  CALL DrawStatusPanel_14
  LD HL,$50C2
  LD (DrawScreenPtr),HL
  LD DE,StrPressPause
  LD B,$1C
  CALL DrawStatusPanel_14
  XOR A
  LD (CorridorCursor),A
OptionsScreen_7:
  CALL OptionsScreen_15
OptionsScreen_8:
  LD HL,CorridorCursor
  LD BC,$F7FE
  IN A,(C)
  RRA
  JR NC,OptionsScreen_9
  RRA
  JR NC,OptionsScreen_10
  RRA
  JR NC,OptionsScreen_11
  RRA
  JR NC,OptionsScreen_12
  RRA
  JR NC,OptionsScreen_13
  JR OptionsScreen_8
OptionsScreen_9:
  LD A,(HL)
  AND A
  JR Z,OptionsScreen_8
  CALL OptionsScreen_15
  LD (HL),$00
  JR OptionsScreen_7
OptionsScreen_10:
  LD A,(HL)
  CP $01
  JR Z,OptionsScreen_8
  CALL OptionsScreen_15
  LD (HL),$01
  JR OptionsScreen_7
OptionsScreen_11:
  LD A,(HL)
  CP $02
  JR Z,OptionsScreen_8
  CALL OptionsScreen_15
  LD (HL),$02
  JR OptionsScreen_7
OptionsScreen_12:
  LD A,(HL)
  CP $03
  JR Z,OptionsScreen_8
  CALL OptionsScreen_15
  LD (HL),$03
  JR OptionsScreen_7
OptionsScreen_13:
  LD A,(HL)
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,InputScannerTable
  ADD HL,DE
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
  LD ($874D),HL
  LD A,(CorridorCursor)
  CP $03
  RET NZ
  CALL ClearDisplay
  LD A,$20
  CALL FillAttributes
  CALL OptionsScreen_21
  LD HL,$4041
  LD (DrawScreenPtr),HL
  LD DE,StrUserDefinable
  LD B,$1E
  CALL DrawStatusPanel_14
  LD A,(KeyboardModeFlag)
  AND A
  JR Z,OptionsScreen_14
  LD HL,$4084
  LD (DrawScreenPtr),HL
  LD DE,StrRedefineKeys
  LD B,$18
  CALL DrawStatusPanel_14
  LD HL,$40A4
  LD (DrawScreenPtr),HL
  LD DE,StrAnyOtherKey
  LD B,$19
  CALL DrawStatusPanel_14
  CALL OptionsScreen_19
  LD BC,$DFFE             ; BC = port 57342 ($DFFE): keyboard half-row
                          ; P,O,I,U,Y
  IN A,(C)                ; read keyboard row
  AND $10                 ; bit 4 = the Y key (active low)
  RET NZ                  ; return if Y not pressed (keep default keys)
  CALL OptionsScreen_21
OptionsScreen_14:
  LD HL,$4804
  LD (DrawScreenPtr),HL
  LD DE,StrWhichKey
  LD B,$18
  CALL DrawStatusPanel_14
  LD HL,$4844
  LD (DrawScreenPtr),HL
  LD DE,StrUseForUp
  LD B,$0B
  CALL DrawStatusPanel_14
  CALL OptionsScreen_19
  LD ($AC0F),BC
  LD ($AC14),A
  CALL OptionsScreen_21
  LD HL,$484D
  LD (DrawScreenPtr),HL
  LD DE,StrDown
  LD B,$04
  CALL DrawStatusPanel_14
  CALL OptionsScreen_19
  LD ($AC1B),BC
  LD ($AC20),A
  CALL OptionsScreen_21
  LD HL,$484D
  LD (DrawScreenPtr),HL
  LD DE,StrFire
  LD B,$04
  CALL DrawStatusPanel_14
  CALL OptionsScreen_19
  LD ($AC27),BC
  LD ($AC2C),A
  CALL OptionsScreen_21
  LD HL,$484D
  LD (DrawScreenPtr),HL
  LD DE,StrReturnTo
  LD B,$0E
  CALL DrawStatusPanel_14
  CALL OptionsScreen_19
  LD ($AC33),BC
  LD ($AC38),A
  CALL OptionsScreen_21
  LD A,$FF
  LD (KeyboardModeFlag),A
  RET
OptionsScreen_15:
  PUSH HL
  LD HL,$58EB
  CALL HighlightGameMode_0
  POP HL
  RET
  LD HL,AlienFlags
  LD (HL),$00
  LD BC,$7FFE
  IN A,(C)
  AND $01
  JR NZ,OptionsScreen_16
  SET 2,(HL)
  RET
OptionsScreen_16:
  LD BC,$7FFE
  IN A,(C)
  AND $02
  JR NZ,OptionsScreen_17
  SET 3,(HL)
  RET
OptionsScreen_17:
  LD BC,$7FFE
  IN A,(C)
  AND $04
  JR NZ,OptionsScreen_18
  SET 4,(HL)
  RET
OptionsScreen_18:
  LD BC,$7FFE
  IN A,(C)
  AND $08
  RET NZ
  SET 1,(HL)
  RET
; This entry point is used by the routine at PauseMenu.
OptionsScreen_19:
  LD BC,$FEFE
OptionsScreen_20:
  IN A,(C)
  AND $1F
  XOR $1F
  RET NZ
  RLC B
  JR C,OptionsScreen_20
  JR OptionsScreen_19
; This entry point is used by the routines at DrawShipMap and PauseMenu.
OptionsScreen_21:
  LD BC,$FEFE
OptionsScreen_22:
  IN A,(C)
  AND $1F
  XOR $1F
  JR NZ,OptionsScreen_21
  RLC B
  JR C,OptionsScreen_22
  RET
; This entry point is used by the routines at XorHAddE and UpdateRoomActors.
EndgameScreen:
  CALL ScreenTransition
  XOR A
  LD (EndgameFlag),A
  CALL ScreenTransition_5
  CALL ScreenTransition_8
; This entry point is used by the routine at DrawStatusPanel.
OptionsScreen_23:
  CALL ScreenTransition_11
  CALL DrawStatusPanel_2
OptionsScreen_24:
  CALL DrawStatusPanel_3
  CALL DrawStatusPanel_7
  CALL DrawStatusPanel_8
  CALL ScreenTransition_2
  JP GameEntry
; This entry point is used by the routine at UpdateRoomActors.
OptionsScreen_25:
  CALL ScreenTransition
  XOR A
  LD (EndgameFlag),A
  LD DE,StrNarcissusLaunched
  LD B,$19
  LD HL,$4003
  LD (DrawScreenPtr),HL
  CALL DrawStatusPanel_14
  LD HL,ShipSystemFlags
  BIT 0,(HL)
  JR NZ,OptionsScreen_26
  CALL ScreenTransition_4
  JP OptionsScreen_24
OptionsScreen_26:
  LD A,(DestructMinutes)
  LD ($A48A),A
  LD HL,(DestructSeconds)
  LD ($A494),HL
  LD HL,$4043
  LD (DrawScreenPtr),HL
  LD B,$19
  LD DE,StrWillExplode
  CALL DrawStatusPanel_14
  LD HL,$4063
  LD (DrawScreenPtr),HL
  LD B,$17
  LD DE,DestructTextIn
  CALL DrawStatusPanel_14
  CALL ScreenTransition_5
  CALL ScreenTransition_11
  JP OptionsScreen_24
; This entry point is used by the routine at DrawStatusPanel.
OptionsScreen_27:
  CALL ScreenTransition
  XOR A
  LD (EndgameFlag),A
  LD HL,$4002
  LD (DrawScreenPtr),HL
  LD DE,StrLifeLevelsMinimal
  LD B,$1C
  CALL DrawStatusPanel_14
OptionsScreen_28:
  LD HL,ShipSystemFlags
  BIT 0,(HL)
  JP NZ,OptionsScreen_26
  CALL ScreenTransition_4
  JP OptionsScreen_24
; This entry point is used by the routine at UpdateRoomActors.
OptionsScreen_29:
  CALL ScreenTransition
  LD HL,$4001
  LD (DrawScreenPtr),HL
  LD DE,StrLifeSupportGone
  LD B,$1E
  CALL DrawStatusPanel_14
  XOR A
  LD (EndgameFlag),A
  LD HL,CrewStatusColumn
  LD DE,$0008
  LD B,$07
OptionsScreen_30:
  LD (HL),$02
  ADD HL,DE
  DJNZ OptionsScreen_30
  JR OptionsScreen_28

; DrawStatusPanel
;
; Refreshes the crew-status display at the right side of the game window. Scans
; all 7 crew records starting at the alive-flag byte (CrewStatusColumn =
; $7386+7). For each crew member: if its crew-ID matches the alien's current
; target (AlienTargetID) it is highlighted; if its alive flag is 0 (dead) the
; scan stops early (crew records are kept in alive-descending order). Live crew
; members get their portrait sprite drawn at the status panel position ($40A4
; column).
;
; USED by the routine at GameEntry.
DrawStatusPanel:
  LD HL,CrewStatusColumn  ; HL = slot 1's +7 status byte (CrewStatusColumn)
  XOR A                   ; A = 0
  LD BC,$0701             ; B=7 (7 crew members), C=1 (crew index, 1-based)
  LD DE,$0008             ; DE = 8 (crew record stride)
DrawStatusPanel_0:
  LD A,(AlienTargetID)    ; A = alien's target crew ID (AlienTargetID)
  CP C                    ; is this crew member the alien's current target?
  JR Z,DrawStatusPanel_1  ; yes: always include (even if at door)
  XOR A                   ; A = 0
  CP (HL)                 ; alive flag = 0?
  RET Z                   ; dead crew member: stop scan here
DrawStatusPanel_1:
  INC C                   ; advance crew index
  ADD HL,DE               ; HL → alive-flag of next crew record
  DJNZ DrawStatusPanel_0  ; repeat for all 7 crew
  JP OptionsScreen_27     ; draw live-crew portraits in status column
; This entry point is used by the routine at OptionsScreen.
DrawStatusPanel_2:
  LD HL,$4044
  LD (DrawScreenPtr),HL
  LD B,$19
  LD DE,StrAlienKilled
  CALL DrawStatusPanel_14
  RET
; This entry point is used by the routine at OptionsScreen.
DrawStatusPanel_3:
  LD HL,$40A4
  LD (DrawScreenPtr),HL
  LD BC,$0701
  LD IX,CrewRecords
  LD DE,$0008
DrawStatusPanel_4:
  PUSH BC
  PUSH DE
  LD A,(AlienTargetID)
  CP C
  JR Z,DrawStatusPanel_6
  LD A,(IX+$07)
  AND A
  JR Z,DrawStatusPanel_6
  CP $FF
  JR Z,DrawStatusPanel_6
  LD HL,(DrawScreenPtr)
  PUSH HL
  LD A,C
  CALL PrintName
  LD DE,StrIsDead
  LD B,$08
  CALL DrawStatusPanel_14
  POP HL
  LD DE,$0040
  LD A,L
  CP $E0
  JR C,DrawStatusPanel_5
  LD DE,$0740
DrawStatusPanel_5:
  ADD HL,DE
  LD (DrawScreenPtr),HL
DrawStatusPanel_6:
  POP DE
  ADD IX,DE
  POP BC
  INC C
  DJNZ DrawStatusPanel_4
  RET
; This entry point is used by the routine at OptionsScreen.
DrawStatusPanel_7:
  LD HL,$5024
  LD (DrawScreenPtr),HL
  LD A,(AlienTargetID)
  CP $08
  RET NC
  CALL PrintName
  LD DE,StrWasAndroid
  LD B,$10
  CALL DrawStatusPanel_14
  RET
; This entry point is used by the routine at OptionsScreen.
DrawStatusPanel_8:
  LD A,(EndgameFlag)
  CP $64
  JR C,DrawStatusPanel_9
  LD A,$64
DrawStatusPanel_9:
  CALL PlayMusic_22
  LD ($A451),HL
  AND A
  JR Z,DrawStatusPanel_13
  LD B,A
  LD HL,StrRatingDigits
  LD A,$3A
DrawStatusPanel_10:
  INC (HL)
  CP (HL)
  JR NZ,DrawStatusPanel_12
  LD (HL),$30
  DEC HL
  INC (HL)
  CP (HL)
  JR NZ,DrawStatusPanel_11
  CALL PlayMusic_23
  INC HL
DrawStatusPanel_11:
  INC HL
DrawStatusPanel_12:
  DJNZ DrawStatusPanel_10
DrawStatusPanel_13:
  LD HL,$5084
  LD (DrawScreenPtr),HL
  LD DE,StrCompetence
  LD B,$16
  CALL DrawStatusPanel_14
  RET
; This entry point is used by the routines at DrawSprite, DrawIntroScreen,
; DrawShipMap, OptionsScreen, ScreenTransition and PauseMenu.
DrawStatusPanel_14:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ DrawStatusPanel_14
  RET
; This entry point is used by the routine at XorHAddE.
DrawStatusPanel_15:
  CALL ScreenTransition
  LD HL,CrewStatusColumn
  LD B,$07
  LD DE,$0008
DrawStatusPanel_16:
  LD (HL),$02
  ADD HL,DE
  DJNZ DrawStatusPanel_16
  LD HL,$4005
  LD (DrawScreenPtr),HL
  LD DE,StrNostromoExploded
  LD B,$15
  CALL DrawStatusPanel_14
  XOR A
  LD (EndgameFlag),A
  JP OptionsScreen_23

; ScreenTransition
;
; Animated wipe-to-black transition between screens. Progressively sets each of
; the 24 attribute rows to 0 (black-on-black, making text invisible) with a
; one-frame delay between rows, creating a top-to-bottom blackout animation.
; After all 24 rows are zeroed, clears the display file and resets attributes
; to 7 (white ink on black paper) ready for the next screen.
;
; USED by the routines at GameEntry, OptionsScreen and DrawStatusPanel.
ScreenTransition:
  LD HL,AttrFileOrigin    ; HL = AttrFileOrigin (start of attribute file)
  XOR A                   ; A = 0 (black-on-black attribute)
  LD B,$18                ; 24 character rows to wipe
ScreenTransition_0:
  PUSH BC                 ; save row counter
  LD B,$20                ; 32 attribute columns per row
ScreenTransition_1:
  LD (HL),A               ; write black-on-black: text becomes invisible
  INC HL
  DJNZ ScreenTransition_1 ; fill entire character row
  PUSH HL                 ; save attribute pointer
  CALL BusyWait           ; one-frame delay (~20ms): visible wipe effect
  POP HL
  POP BC
  DJNZ ScreenTransition_0 ; next row
  CALL ClearDisplay       ; ClearDisplay: blank all pixels
  LD A,$07                ; white ink on black paper
  CALL FillAttributes     ; FillAttributes(7): restore normal attributes
  RET
; This entry point is used by the routine at OptionsScreen.
ScreenTransition_2:
  CALL BusyWait
  LD HL,$50E9
  LD (DrawScreenPtr),HL
  LD DE,PressAnyKeyText
  LD B,$0D
  CALL DrawStatusPanel_14
  XOR A
  LD (SysLastKey),A
  CALL BusyWait
ScreenTransition_3:
  LD A,(SysLastKey)
  AND A
  JR Z,ScreenTransition_3
  RET
; This entry point is used by the routine at OptionsScreen.
ScreenTransition_4:
  LD DE,StrAlienUnleashed
  LD HL,$4043
  LD (DrawScreenPtr),HL
  LD B,$1B
  CALL DrawStatusPanel_14
  LD DE,StrUponEarth
  LD HL,$4069
  LD (DrawScreenPtr),HL
  LD B,$0E
  CALL DrawStatusPanel_14
  RET
; This entry point is used by the routine at OptionsScreen.
ScreenTransition_5:
  LD IX,CrewRecords
  LD DE,$0008
  XOR A
  EX AF,AF'
  LD BC,$0701
ScreenTransition_6:
  LD A,(AlienTargetID)
  CP C
  JR Z,ScreenTransition_7
  LD A,(IX+$07)
  AND A
  JR NZ,ScreenTransition_7
  LD L,(IX+$04)
  SLA L
  EX AF,AF'
  ADD A,L
  EX AF,AF'
ScreenTransition_7:
  ADD IX,DE
  INC C
  DJNZ ScreenTransition_6
  EX AF,AF'
  LD C,A
  LD A,(EndgameFlag)
  ADD A,C
  LD (EndgameFlag),A
  RET
; This entry point is used by the routine at OptionsScreen.
ScreenTransition_8:
  LD BC,$2200
  LD HL,RoomDamageDigits
ScreenTransition_9:
  LD A,(HL)
  CP $32
  JR NC,ScreenTransition_10
  INC C
ScreenTransition_10:
  INC HL
  INC HL
  DJNZ ScreenTransition_9
  LD A,(EndgameFlag)
  ADD A,C
  LD (EndgameFlag),A
  RET
; This entry point is used by the routine at OptionsScreen.
ScreenTransition_11:
  LD A,(OxygenHi)
  SUB $30
  SLA A
  LD C,A
  LD A,(EndgameFlag)
  ADD A,C
  LD (EndgameFlag),A
  RET

; ShortGameInit
;
; Game-mode handler for "1: Short Game" (dispatched from
; GameModeDispatchTable).
;
; THE Short Game is the abbreviated scenario. Several slots in the
; STARTING template (ShortGameCrewInit ShortGameCrewInit) are pre-stamped with
;          the
; HOSTMARKER pattern ($FF on bytes +1 and +7) and the alien's first
; TARGET is hard-wired to crew ID 9 — a value the alien-targeting guard
; AT TriggerAlien_TargetCheck treats as "no valid target", so the alien's
;    stalk-the-host
; LOGIC is effectively disabled. No script-driven randomisation runs.
; EVERY Short Game opens from exactly the same configuration, which is
; WHAT makes it shorter and more deterministic than the Long Game.
;
; Steps: 1. Clear the two run-state bytes at MsgDrawnCol / MsgVisibleTimer. 2.
; Point the message queue head (MsgQueueWritePtr MsgQueueWritePtr) at the queue
; base MessageQueue (MessageQueue). 3. Hard-wire AlienTargetID (AlienTargetID)
; = 9 — disables alien hunt logic. 4. LDIR 64 bytes from ShortGameCrewInit
; (ShortGameCrewInit) into $7376 to populate the alien slot and the seven crew
; slots with the short-game starting state (four slots wear the HostMarker so
; they're skipped by normal crew processing, leaving the alien header plus
; three active crew). 5. JP into the shared common init at CommonGameInit
; (CommonGameInit) — skipping the Long Game's script-driven host selection and
; target picking. Used by the dispatcher at GameModeJump via the table at
; GameModeDispatchTable.
ShortGameInit:
  XOR A                   ; A = 0
  LD (MsgDrawnCol),A      ; clear run-state byte MsgDrawnCol
  LD (MsgVisibleTimer),A  ; clear run-state byte MsgVisibleTimer
  LD HL,MessageQueue      ; HL = MessageQueue (event queue base)
  LD (MsgQueueWritePtr),HL ; event queue head = queue base (empty queue)
ShortGame_SetTarget:
  LD A,$09                ; A = 9 (fixed Short Game alien target)
  LD (AlienTargetID),A    ; AlienTargetID = 9 — no randomisation
  LD HL,ShortGameCrewInit ; HL = ShortGameCrewInit ($A224)
  LD DE,ActorRecords      ; DE = $7376 (alien slot + crew table base)
  LD BC,$0040             ; 64 bytes = 8 × 8-byte records
  LDIR                    ; copy short-game starting crew/alien state
ShortGameInit_Done:
  JP CommonGameInit       ; → CommonGameInit (skip Long Game randomisation)

; Pause-menu texts
;
; Plain ASCII (previously misclassified as code): the two lines PauseMenu
; (PauseMenu) draws.
StrPress1NewGame:
  DEFM "PRESS 1 FOR NEW GAME"
StrAnyKeyContinue:
  DEFM "ANY OTHER KEY TO CONTINUE"

; PauseMenu
;
; Pause screen overlay shown when the player presses key 1 during gameplay.
; Draws two lines of text (pause message and restart prompt) over the game
; screen using the tile-blit helper at DrawStatusPanel_14. Sets the border to
; yellow (6) and waits for the player to press key 1 again. If key 1 is
; pressed, jumps directly to GameEntry (GameEntry) to restart the game. Any
; other key resumes play by returning normally. If the corridor view was active
; (mode=1), calls RedrawRoomScene to restore the room display before returning.
;
; USED by the routine at GameEntry.
PauseMenu:
  CALL ArmStraight_12     ; draw game-window border
  LD HL,$5086             ; HL = $5086 (mid-screen attribute row)
  LD (DrawScreenPtr),HL   ; set draw pointer
  LD B,$14                ; 20-character message line
; This entry point is used by the routine at ScreenTransition.
PauseMenu_0:
  LD DE,StrPress1NewGame  ; DE = text data for pause message line 1
  CALL DrawStatusPanel_14 ; blit text
  LD HL,$50C3             ; next screen row
  LD (DrawScreenPtr),HL
  LD B,$19                ; 25-character second line
  LD DE,StrAnyKeyContinue ; text data for "PRESS 1 TO RESTART" line
  CALL DrawStatusPanel_14 ; blit text
  CALL OptionsScreen_21   ; short wait (debounce key release)
; This entry point is used by the routine at ScreenTransition.
PauseMenu_1:
  LD A,$06                ; 6 = yellow border
  OUT ($FE),A             ; flash border yellow (indicates pause)
  CALL OptionsScreen_19   ; wait loop (keyboard debounce / delay)
  LD BC,$F7FE             ; BC = $F7FE: keyboard row for keys 1-5
  IN A,(C)                ; read keyboard
; This entry point is used by the routine at ScreenTransition.
PauseMenu_2:
  RRA                     ; bit 0 → carry (key 1 = active-low)
  JP NC,GameEntry         ; key 1 held: restart game from entry
  XOR A                   ; A = 0
  OUT ($FE),A             ; restore border to black
  CALL ArmStraight_12     ; redraw game-window frame
  LD A,(RoomModeByte)     ; A = room mode (RoomModeByte)
; This entry point is used by the routine at ScreenTransition.
PauseMenu_3:
  CP $01                  ; mode 1 = corridor view active
; This entry point is used by the routine at ScreenTransition.
PauseMenu_4:
  CALL Z,RedrawRoomScene  ; if in corridor: redraw room contents
  RET                     ; resume game loop

; PlayMusic
;
; Plays a background music phrase through the ZX Spectrum beeper. Reads the
; music-active flag at MusicActiveFlag. If zero, falls through to the standard
; frame- timing wait at BusyWait and returns. Otherwise plays a descending
; sequence of tones: reads 32 consecutive ROM bytes from $0200 and for each
; byte toggles bit 4 of port $FE (beeper output), with a delay of C counts
; between toggles. After each 32-byte block, decrements C by 8 to raise the
; pitch, producing a descending arpeggio effect. Runs until C underflows (B=255
; exhausted), then returns.
;
; USED by the routines at GameEntry and DrawShipMap.
PlayMusic:
  LD A,(SoundPending)     ; A = sound-pending flag (queued by e.g.
  AND A                   ; CrewAction3_RemoveGrille); anything queued?
  JP Z,BusyWait           ; no: standard frame-timing wait and return
  LD HL,$0200             ; HL = $0200 (ROM data used as tone source)
  LD BC,$0880             ; B=8 (8 pitch steps), C=$88 (initial delay = 136)
PlayMusic_0:
  PUSH BC                 ; save pitch counter
  LD B,$20                ; 32 bytes (ROM data) per pitch step
PlayMusic_1:
  LD A,(HL)               ; read ROM byte
  AND $10                 ; isolate bit 4 (beeper toggle bit)
  OUT ($FE),A             ; output to port $FE (beeper + border)
  INC HL                  ; advance ROM pointer
  LD A,C                  ; A = current delay count
PlayMusic_2:
  DEC A                   ; count down delay
  JR NZ,PlayMusic_2       ; busy-wait
  DJNZ PlayMusic_1        ; next ROM byte in this pitch step
  LD B,$FF                ; inter-note gap: 255 NOP-equivalent iterations
PlayMusic_3:
  DJNZ PlayMusic_3        ; wait
  POP BC                  ; restore pitch counter
  LD A,C                  ; A = current delay value
  SUB $08                 ; reduce delay → raise pitch
  LD C,A                  ; store updated delay
  DJNZ PlayMusic_0
  XOR A
  LD (SoundPending),A
  RET
; This entry point is used by the routine at XorHAddE.
PlayMusic_4:
  LD (IX+$03),$00
  LD HL,$FFFF
  LD (HeldItemFront),HL
  CALL RefreshHeldItems
  RET
; This entry point is used by the routine at XorHAddE.
PlayMusic_5:
  LD A,$FF
  LD (HL),A
  LD A,(RoomModeByte)
  CP $01
  JR NZ,PlayMusic_6
  PUSH HL
  LD A,(CrewIndex)
  LD HL,DrawSlotIndex
  CP (HL)
  POP HL
  JR NZ,PlayMusic_6
  LD A,$FF
  JP UpdateHandRows
PlayMusic_6:
  LD A,$FF
  CP B
  LD A,(HeldItemBack)
  JP NZ,GetItemLocation
  RET
; This entry point is used by the routine at XorHAddE.
PlayMusic_7:
  CALL DrawSprite
  LD A,$FF
  LD ($73C9),A
  CALL RedrawRoomScene_3
  RET
; This entry point is used by the routine at XorHAddE.
PlayMusic_8:
  LD HL,$FFFF
  LD (HeldItemFront),HL
  LD A,(CrewIndex)
  RET
; This entry point is used by the routine at DrawSprite.
PlayMusic_9:
  LD A,(IX+$03)
  CP $04
  JR NZ,PlayMusic_10
  LD A,(IX+$00)
  OR A
  JP NZ,DrawRoomSpecials_15
PlayMusic_10:
  CALL PlayMusic_27
  LD DE,$73CB
  JP DrawRoomSpecials_14
; This entry point is used by the routine at GameEntry.
PlayMusic_11:
  PUSH IX
  PUSH DE
  LD A,(DrawSlotIndex)
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD IX,ActorRecords
  ADD IX,DE
  LD A,(IX+$03)
  CP $04
  JR NZ,PlayMusic_12
  LD A,(IX+$00)
  OR A
  JR NZ,PlayMusic_13
PlayMusic_12:
  POP DE
  POP IX
  CALL PlayMusic_27
  CALL ResetAlienXAnim
  JP GameEntry_23
PlayMusic_13:
  POP DE
  POP IX
  CALL ResetAlienXAnim
  LD HL,DrawMenuStrip
  LD (CrewAnimFlags),HL
  LD A,$04
  LD (RoomTypeByte),A
  RET
; This entry point is used by the routine at GameEntry.
PlayMusic_14:
  RET NZ
  PUSH IX
  PUSH DE
  LD A,(DrawSlotIndex)
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD IX,ActorRecords
  ADD IX,DE
  LD A,(IX+$03)
  CP $04
  JR Z,PlayMusic_15
  POP DE
  POP IX
  JR PlayMusic_16
PlayMusic_15:
  LD A,(IX+$00)
  OR A
  POP DE
  POP IX
  RET NZ
PlayMusic_16:
  CALL PlayMusic_27
  JP GameEntry_24
; This entry point is used by the routine at DrawSprite.
PlayMusic_17:
  LD A,(IX+$03)
  CP $04
  JR NZ,PlayMusic_18
  LD A,(IX+$00)
  OR A
  JP NZ,DrawRoomSpecials_18
PlayMusic_18:
  CALL PlayMusic_27
  LD DE,$73CB
  JP DrawRoomSpecials_17
; This entry point is used by the routine at UpdateAlien.
PlayMusic_19:
  LD A,(RoomModeByte)
  CP $01
  JR NZ,PlayMusic_21
  LD HL,(AlienTargetPtr)
  INC HL
  LD A,(CurrentRoom)
  CP (HL)
  JR NZ,PlayMusic_21
  LD HL,$737F
  CP (HL)
  JR Z,PlayMusic_21
  AND $3F                 ; refresh the menu's Special slot from
  LD E,A                  ; RoomGrilleState[viewed room]:
  LD HL,RoomGrilleState
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  CP $FF
  JR Z,PlayMusic_20
  LD A,$FF                ; grille removed: clear the Special slot
  LD ($73CB),A
  CALL RedrawRoomScene_3
  JR PlayMusic_21
PlayMusic_20:
  LD A,$31                ; grille in place: offer "RmveGrille"
  LD ($73CB),A
  CALL RedrawRoomScene_3
PlayMusic_21:
  LD HL,AlienActiveFlag
  RET
; This entry point is used by the routine at DrawStatusPanel.
PlayMusic_22:
  LD HL,$3020
  LD ($A450),HL
  LD L,$30
  RET
; This entry point is used by the routine at DrawStatusPanel.
PlayMusic_23:
  LD (HL),$30
  DEC HL
  LD (HL),$31
  RET
; This entry point is used by the routine at TriggerAlienEvent.
PlayMusic_24:
  LD HL,$FFFF
  LD (MsgLastEnqueued),HL
  CALL ResetScriptPtr
  RET
; This entry point is used by the routine at ResetCrewTimers.
PlayMusic_25:
  PUSH IX
  CALL Z,DispatchCrewAction
  POP IX
  RET
; This entry point is used by the routine at XorHAddE.
RefreshSpecialRow:
  LD A,(CurrentRoom)      ; refresh the menu's Special slot from
  AND $3F                 ; RoomGrilleState[viewed room]:
  LD E,A
  LD HL,RoomGrilleState
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  CP $FF
  JR Z,RefreshSpecialRow_InPlace
  LD A,$FF                ; grille removed: clear the Special slot
  LD ($73CB),A
  RET
RefreshSpecialRow_InPlace:
  LD A,$31                ; grille in place: offer "RmveGrille"
  LD ($73CB),A
  RET
; This entry point is used by the routines at XorHAddE and UpdateAlien.
PlayMusic_26:
  CALL PlayMusic_27       ; alien in the room: the Special slot
  LD A,$34                ; becomes "  ATTACK  " (52)
  LD ($73CB),A
  RET
PlayMusic_27:
  LD A,(CursorCellValue)  ; if the live selection was "RmveGrille"
  CP $31                  ; (49), swap it for "  ATTACK  " too
  RET NZ
  LD A,$34
  LD (CursorCellValue),A
  RET

; Routine at WrapScreenAddr
;
; Used by the routine at ResetScriptPtr.
WrapScreenAddr:
  LD A,H
  AND $1F
  LD H,A
  RET
; This entry point is used by the routine at XorHAddE.
PickupItemBoost:
  ADD A,$20
  LD (HL),A
  LD C,E
  LD B,$00
  LD HL,ItemCourageBonus
  ADD HL,BC
  LD B,(HL)
  LD A,(IX+$05)
  ADD A,B
  LD (IX+$05),A
  LD A,(IX+$06)
  ADD A,B
  LD (IX+$06),A
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(CrewIndex)
  LD HL,DrawSlotIndex
  CP (HL)
  RET NZ
  CALL DrawCrewCondition
  RET
; This entry point is used by the routine at ResetCrewTimers.
RefreshCrewCondition:
  CALL GameEntry_21
  LD A,(CrewIndex)
  CP $00
  RET Z
  LD A,(DrawSlotIndex)
  LD B,A
  LD IX,ActorRecords
  LD DE,$0008
RefreshCrewCond_Seek:
  ADD IX,DE
  DJNZ RefreshCrewCond_Seek
  CALL DrawCrewCondition
  RET
; This entry point is used by the routine at XorHAddE.
IncPairAt:
  ADD HL,DE
  INC (HL)
  INC HL
  INC (HL)
  DEC HL
  LD A,(HL)
  RET
; This entry point is used by the routine at XorHAddE.
RefreshCondIfViewed:
  LD A,(RoomModeByte)
  CP $01
  JR NZ,RefreshCond_Done
  LD A,(DrawSlotIndex)
  LD B,A
  LD IX,ActorRecords
  LD DE,$0008
RefreshCond_Seek2:
  ADD IX,DE
  DJNZ RefreshCond_Seek2
  CALL DrawCrewCondition
RefreshCond_Done:
  POP IX
  RET

; Routine at UpdateAllCrew
;
; Used by the routine at GameEntry.
UpdateAllCrew:
  LD IX,CrewRecords
  LD C,$06
CrewPass_Next:
  LD H,(IX+$01)
  LD L,(IX+$05)
  LD B,C
  PUSH IX
CrewPass_Scan:
  CALL NextSlotIX
  LD A,H
  CP (IX+$01)
  JR Z,CrewPass_MateBoost
CrewPass_NoMate:
  DJNZ CrewPass_Scan
  POP IX
  DEC C
  CALL NextSlotIX
  LD A,IXl
  CP $B6
  JR NZ,CrewPass_Next
  CALL UpdateCrewAI_17
  RET
  NOP
CrewPass_MateBoost:
  LD A,L
  SUB $02
  ADD A,(IX+$06)
  LD (IX+$06),A
  POP DE
  PUSH DE
  INC E
  INC E
  INC E
  INC E
  INC E
  INC E
  LD A,(DE)
  ADD A,(IX+$05)
  SUB $02
  LD (DE),A
  JR CrewPass_NoMate
NextSlotIX:
  LD DE,$0008
  ADD IX,DE
  RET
; This entry point is used by the routine at UpdateRoomActors.
KillActorRefresh:
  CALL KillActorMoraleHit
KillActor_View:
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(DrawSlotIndex)
  CP C
  RET Z
KillActor_Redraw:
  PUSH BC
  PUSH DE
  PUSH HL
  PUSH IX
  LD HL,$FFFF
  LD (RoomMateA),HL
  CALL DrawRoomMates
  POP IX
  POP HL
  POP DE
  POP BC
  RET
; This entry point is used by the routine at GetItemPick.
BumpCourageMorale:
  LD A,(IX+$05)
  ADD A,$01
  LD (IX+$05),A
  LD A,(IX+$06)
  ADD A,$01
  LD (IX+$06),A
  CALL DrawCrewCondition
  LD HL,JonesRoom
  RET
; This entry point is used by the routine at UpdateRoomActors.
SetCourageRefresh:
  LD (IX+$05),C
  JP KillActor_View
; This entry point is used by the routine at ResetCrewTimers.
DecayMoraleStep:
  INC HL
  DEC (HL)
  LD A,$7F
  CP (HL)
  JR NC,DecayMorale_Next
  LD (HL),$00
DecayMorale_Next:
  DEC HL
  ADD HL,DE
  DEC B
  JP NZ,DecayCourageLoop
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  PUSH BC
  PUSH IX
  LD A,(DrawSlotIndex)
  LD B,A
  LD IX,ActorRecords
  LD DE,$0008
ViewedSlotSeek:
  ADD IX,DE
  DJNZ ViewedSlotSeek
  CALL DrawCrewCondition
  POP IX
  POP BC
  RET
KillActorMoraleHit:
  INC HL
  LD (HL),$01
  PUSH BC
  PUSH IX
  PUSH HL
  LD H,$00
  LD A,(NearestCrewC)
  CP $FF
  CALL NZ,WitnessMoraleLoss
  LD A,(NearestCrewA)
  CP $FF
  CALL NZ,WitnessMoraleLoss
  LD A,(NearestCrewB)
  CP $FF
  CALL NZ,WitnessMoraleLoss
  LD A,H
  CP $00
  JR Z,KillMorale_Done
  POP HL
  LD (HL),$02
  PUSH HL
  LD L,A
  LD H,C
  LD A,$14
  CALL UpdateCrewAI_8
  CALL DecayCrewCourage
KillMorale_Done:
  POP HL
  POP IX
  POP BC
  RET
WitnessMoraleLoss:
  LD B,A
  LD IX,ActorRecords
  LD DE,$0008
WitnessMorale_Seek:
  ADD IX,DE
  DJNZ WitnessMorale_Seek
  LD B,A
  LD A,(IX+$07)
  OR A
  RET NZ
  DEC (IX+$06)
  LD A,$7F
  CP (IX+$06)
  JR NC,WitnessMorale_Keep
  LD (IX+$06),$00
WitnessMorale_Keep:
  LD H,B
  RET
; This entry point is used by the routine at UpdateRoomActors.
KillActor_Bounce:
  CP (HL)
  JP Z,InitGameView
  JP KillActor_Redraw
; This entry point is used by the routine at UpdateRoomActors.
DuctSearchInit:
  LD BC,RoomAdjDucts
  AND $3F
  LD (AdjTestRoom),A
  RET
; This entry point is used by the routine at DrawSprite.
PrintNameThenPanel:
  CALL PrintName
; This entry point is used by the routine at DrawSprite.
PanelColCheck:
  LD HL,DrawScreenPtr
  LD A,(HL)
  AND $1F
  RET Z
  JP BlankRestOfRow
; This entry point is used by the routine at UpdateRoomActors.
RoomRejectCheck:
  CP C
  JP Z,PickRoom_Reject
  LD A,($737F)
  AND $3F
  CP C
  JP Z,PickRoom_Reject
  JP PickRoom_ActorScan
; This entry point is used by the routine at UpdateCrewAI.
AIActionSelect:
  LD A,(IX+$01)
  CP $62
  JP Z,UpdateCrewAI_6
  JP UpdateCrewAI_1
; This entry point is used by the routine at TriggerAlienEvent.
AlienEventDispatch:
  LD (AlienActiveFlag),A
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(AlienTargetID)
  LD D,A
  LD A,(DrawSlotIndex)
  CP D
  RET NZ
  JP InitGameView
; This entry point is used by the routine at RoomModeDispatch. The
; hostile-in-room ATTACK-row swap and the 238 marker PreDispatchHook is called
; by RoomModeDispatch (RoomModeDispatch) at the start of EVERY action-key
; dispatch in room modes 0/1, before the row handler runs. In the room view it
; forces the special-action row (cell 13) to 52 "  ATTACK  " whenever a hostile
; is in the viewed room. The subtlety is a press that was aimed at 49
; "RmveGrille": the row is swapped to ATTACK by this very hook, inside the same
; key release that dispatches the row — so the hook leaves the value 238 in
; CursorCellValue ($8395) as a marker meaning "this press was made while the
; row still read RmveGrille". The special-row shim at $B306 then honours the
; player's original intent: it restores the mirror to 52 (matching the
; now-displayed ATTACK) but still starts the timed grille removal. Only the
; NEXT press, taken with the row visibly reading ATTACK, goes down the attack
; path. (Verified in the simulator: press 1 with the alien in the room ->
; mirror 49->238->52, cell 13 -> 52, actor gets action state 3 with its full
; grille timer; press 2 -> attack handler StartAttack, action state 4 timer 40,
; replacing the pending grille job.)
PreDispatchHook:
  CALL ForceAttackRowIfHostile ; run the hostile-row check, then
  LD A,(RoomModeByte)     ; return the room mode to the dispatcher
  RET
; ForceAttackRowIfHostile: room view only, and never on the QUIT row. A press
; on row 8 (the front-hand item) zeroes the actor's action countdown (+0),
; cancelling any pending timed job — the countdown only fires its action when
; it decrements to zero in ResetCrewTimers (ResetCrewTimers). Then, if the
; alien (slot 0) is in the viewed actor's room — or, once the alien is active
; (flag $FF), the alien-target / Android slot (pointer at $83A5) is — swap the
; special row to ATTACK.
ForceAttackRowIfHostile:
  LD A,(RoomModeByte)     ; room view (mode 1)?
  CP $01
  RET NZ                  ; no: nothing to do
  LD A,(CorridorCursor)
  CP $11                  ; cursor on the QUIT row?
  RET Z                   ; yes: leave everything alone
  CP $08                  ; cursor on the front-hand item row?
  JR NZ,ForceAttack_RoomCheck
  LD (IX+$00),$00         ; yes: cancel the actor's pending countdown
ForceAttack_RoomCheck:
  LD A,($737F)            ; alien (slot 0) in the viewed actor's room?
  CP (IX+$01)             ; (raw compare, so the duct bit must match too)
  JR Z,ForceAttack_HostilePresent ; yes: hostile present
  LD A,(AlienActiveFlag)  ; alien active ($FF, set by TriggerAlienEvent)?
  CP $FF
  RET NZ                  ; no: done
  LD HL,(AlienTargetPtr)  ; the alien-target / Android record:
  INC HL
  LD A,(HL)               ; its current room (+1)
  CP (IX+$01)             ; in the viewed actor's room?
  RET NZ                  ; no: done
ForceAttack_HostilePresent:
  LD A,(CursorCellValue)  ; is the press landing on 49 "RmveGrille"?
  CP $31
  JR NZ,ForceAttack_ShowAttack
  LD A,$EE                ; yes: mark the mirror with 238 so the
  LD (CursorCellValue),A  ; special-row shim honours the grille press
ForceAttack_ShowAttack:
  LD A,$34                ; special row (cell 13) = 52 "  ATTACK  "
  LD ($73CB),A
  LD HL,$48D6             ; and redraw it (screen row 14, column 22)
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  RET
; This entry point is used by the routine at GetItemPick. SpecialRowAction: the
; special-row (13) handler, reached via the JP at $8BCB. 238 -> the press was
; aimed at RmveGrille before the swap above: restore the visible value 52 and
; start the timed grille removal anyway. Anything else goes to the normal path
; at $8BCE (52 -> attack, 49 -> timed grille removal).
SpecialRowAction:
  LD A,(CursorCellValue)  ; A = value the press landed on
  CP $EE                  ; swapped-under-the-cursor marker?
  JP NZ,SpecialRowNormal  ; no: normal ATTACK / RmveGrille handling
  LD A,$34                ; yes: mirror = 52 to match the displayed
  LD (CursorCellValue),A  ; ATTACK row,
  JP StartGrilleRemoval   ; but still start the timed grille removal
; This entry point is used by the routine at UpdateRoomActors.
; CancelActionTimer: zero the actor's action countdown (+0); a countdown parked
; at 0 never fires (ResetCrewTimers only dispatches on the 1 -> 0 transition).
CancelActionTimer:
  LD A,(CorridorCursor)
  LD (IX+$00),$00
  RET

; Zero-padding between game code and extra-data area
;
; 14146 bytes of zeros bridging the last byte of game code and the start of
; AlienFrame0 (AlienFrame0). Reserved space in the runtime image; no code or
; data lives here. Note: a 16-bit constant inside this gap is used elsewhere as
; a Z80 I/O port number for the keyboard half-row B/N/M/SymShift/Space, not as
; a memory reference.
  DEFS $3742

; AlienFrame0
;
; Alien body frame 0 (phase 0).
AlienFrame0:
  DEFB $00,$00,$00,$0F,$80,$00
  DEFB $00,$00,$00,$3F,$C0,$00
  DEFB $00,$00,$00,$7F,$E0,$00
  DEFB $02,$00,$38,$FF,$E0,$00
  DEFB $04,$00,$7D,$FF,$60,$00
  DEFB $0F,$FF,$E9,$FD,$E0,$00
  DEFB $0F,$FF,$63,$FB,$DC,$00
  DEFB $0F,$FF,$63,$F7,$3E,$00
  DEFB $05,$FF,$63,$FE,$3E,$00
  DEFB $06,$07,$67,$F8,$76,$00
  DEFB $07,$80,$2F,$BE,$62,$00
  DEFB $07,$00,$1F,$D2,$EC,$00
  DEFB $07,$80,$1F,$D1,$DE,$00
  DEFB $07,$00,$1F,$D3,$B9,$00
  DEFB $07,$80,$1F,$A7,$77,$00
  DEFB $07,$00,$0F,$4E,$EF,$80
  DEFB $07,$80,$10,$CC,$1F,$80
  DEFB $07,$78,$0F,$5C,$6F,$C0
  DEFB $07,$7E,$00,$97,$C7,$E0
  DEFB $07,$BF,$8F,$78,$43,$E0
  DEFB $03,$DF,$EE,$EF,$C3,$F4
  DEFB $02,$EF,$F8,$30,$81,$F4
  DEFB $02,$B7,$F5,$EF,$00,$F8
  DEFB $03,$97,$FB,$30,$80,$70
  DEFB $03,$A7,$FC,$9F,$01,$F0
  DEFB $03,$BF,$FC,$C0,$1C,$E0
  DEFB $01,$5D,$FF,$2D,$FB,$E0
  DEFB $01,$5E,$FF,$F3,$F9,$C0
  DEFB $00,$AE,$7F,$FF,$F7,$D0
  DEFB $00,$1E,$3F,$FF,$F7,$B0
  DEFB $00,$3E,$0F,$FF,$E7,$70
  DEFB $00,$3E,$01,$FF,$DE,$F0
  DEFB $00,$3E,$18,$3E,$06,$F0
  DEFB $00,$3E,$38,$00,$35,$F0
  DEFB $00,$1E,$F7,$00,$F9,$F0
  DEFB $00,$1E,$CC,$01,$D9,$E0
  DEFB $00,$1E,$34,$02,$B3,$E0
  DEFB $00,$5E,$D0,$02,$F3,$C0
  DEFB $00,$DE,$40,$01,$53,$C0
  DEFB $01,$C1,$00,$01,$57,$C0
  DEFB $03,$AF,$00,$00,$A7,$C0
  DEFB $0E,$4F,$00,$00,$47,$C0
  DEFB $0D,$8A,$80,$00,$07,$C0
  DEFB $1A,$0B,$80,$00,$03,$80
  DEFB $1B,$0B,$80,$00,$04,$80
  DEFB $34,$0F,$00,$00,$0F,$00
  DEFB $36,$1F,$00,$00,$0F,$00
  DEFB $3C,$1B,$00,$00,$05,$00
  DEFB $34,$36,$00,$00,$0D,$00
  DEFB $38,$7E,$00,$00,$0F,$80
  DEFB $3C,$D6,$00,$00,$05,$C0
  DEFB $38,$B4,$00,$00,$06,$E0
  DEFB $3E,$00,$00,$00,$07,$E0
  DEFB $1C,$00,$00,$00,$06,$A0
  DEFB $1F,$00,$00,$00,$02,$C0
  DEFB $0F,$C0,$00,$00,$00,$00
  DEFB $07,$F8,$00,$00,$00,$00
  DEFB $03,$FE,$00,$00,$00,$00
  DEFB $00,$7F,$80,$00,$00,$00
  DEFB $00,$03,$80,$00,$00,$00
  DEFB $00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$C0,$00,$00,$00
  DEFB $00,$00,$C0,$00,$00,$00

; AlienFrame1
;
; Alien body frame 1 (phase 1).
AlienFrame1:
  DEFB $00,$00,$00,$3E,$00,$00
  DEFB $00,$00,$00,$7F,$00,$00
  DEFB $00,$00,$00,$FF,$80,$00
  DEFB $02,$00,$39,$FE,$80,$00
  DEFB $04,$00,$7D,$FF,$80,$00
  DEFB $0F,$FF,$EB,$FB,$80,$00
  DEFB $0F,$FF,$63,$F7,$1C,$00
  DEFB $0F,$FF,$67,$F4,$3E,$00
  DEFB $05,$FF,$67,$F8,$3E,$00
  DEFB $06,$07,$67,$B0,$76,$00
  DEFB $07,$80,$37,$DC,$62,$00
  DEFB $07,$00,$2F,$F2,$EC,$00
  DEFB $07,$80,$0F,$E9,$DE,$00
  DEFB $07,$00,$0F,$EB,$B9,$00
  DEFB $07,$80,$0F,$D7,$77,$00
  DEFB $07,$00,$07,$AE,$EF,$80
  DEFB $07,$80,$08,$6C,$1F,$80
  DEFB $07,$78,$07,$AC,$6F,$C0
  DEFB $07,$7E,$00,$57,$C7,$E0
  DEFB $07,$BF,$87,$B8,$43,$E0
  DEFB $03,$DF,$E7,$6F,$C3,$F4
  DEFB $02,$EF,$F8,$B0,$81,$F4
  DEFB $02,$B7,$F5,$EF,$00,$F8
  DEFB $03,$97,$FB,$30,$80,$70
  DEFB $03,$A7,$FC,$9F,$01,$F0
  DEFB $03,$BF,$FC,$C0,$1C,$E0
  DEFB $01,$5D,$FF,$2D,$FB,$E0
  DEFB $01,$5E,$FF,$2D,$FB,$E0
  DEFB $00,$AE,$7F,$F3,$F9,$C0
  DEFB $00,$1E,$3F,$FF,$F7,$D0
  DEFB $00,$3E,$0F,$FF,$F7,$B0
  DEFB $00,$3E,$01,$FF,$E7,$70
  DEFB $00,$3E,$18,$FF,$DE,$F0
  DEFB $00,$3E,$38,$3E,$06,$F0
  DEFB $00,$1E,$F7,$00,$35,$F0
  DEFB $00,$1E,$CC,$00,$F9,$F0
  DEFB $00,$1E,$34,$03,$59,$E0
  DEFB $00,$5E,$D0,$05,$B3,$E0
  DEFB $00,$DE,$40,$02,$E3,$C0
  DEFB $01,$C1,$00,$05,$63,$C0
  DEFB $03,$AF,$00,$0A,$A7,$C0
  DEFB $0E,$4F,$00,$05,$27,$C0
  DEFB $0D,$8A,$80,$00,$07,$C0
  DEFB $1A,$0B,$80,$00,$03,$80
  DEFB $1B,$0B,$80,$00,$04,$80
  DEFB $34,$0F,$00,$00,$0F,$00
  DEFB $36,$1F,$00,$00,$0F,$00
  DEFB $3C,$1B,$00,$00,$05,$00
  DEFB $34,$36,$00,$00,$0D,$00
  DEFB $38,$7E,$00,$00,$0F,$80
  DEFB $3C,$D6,$00,$00,$05,$C0
  DEFB $38,$B4,$00,$00,$06,$E0
  DEFB $3E,$00,$00,$00,$07,$E0
  DEFB $1C,$00,$00,$00,$06,$A0
  DEFB $1F,$00,$00,$00,$02,$C0
  DEFB $0F,$C0,$00,$00,$00,$00
  DEFB $07,$F8,$00,$00,$00,$00
  DEFB $03,$FF,$80,$00,$00,$00
  DEFB $00,$7F,$E0,$00,$00,$00
  DEFB $00,$01,$F0,$00,$00,$00
  DEFB $00,$00,$38,$00,$00,$00
  DEFB $00,$00,$18,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00

; AlienFrame2
;
; Alien body frame 2 (phase 2).
AlienFrame2:
  DEFB $00,$00,$00,$7C,$00,$00
  DEFB $00,$00,$01,$FE,$00,$00
  DEFB $00,$00,$03,$FF,$00,$00
  DEFB $02,$00,$3B,$FD,$00,$00
  DEFB $04,$00,$7B,$FB,$00,$00
  DEFB $0F,$FF,$E7,$F7,$00,$00
  DEFB $0F,$FF,$67,$F6,$1C,$00
  DEFB $0F,$FF,$67,$F8,$3E,$00
  DEFB $05,$FF,$67,$F0,$3E,$00
  DEFB $06,$07,$67,$F0,$76,$00
  DEFB $07,$80,$37,$DC,$62,$00
  DEFB $07,$00,$2F,$E2,$EC,$00
  DEFB $07,$80,$0F,$F1,$DE,$00
  DEFB $07,$00,$0F,$F3,$B9,$00
  DEFB $07,$80,$0F,$E7,$77,$00
  DEFB $07,$00,$07,$D6,$EF,$80
  DEFB $07,$80,$08,$34,$1F,$80
  DEFB $07,$78,$07,$EC,$6F,$C0
  DEFB $07,$7E,$03,$A7,$C7,$E0
  DEFB $07,$BF,$84,$58,$43,$E0
  DEFB $03,$DF,$E3,$AF,$C3,$F4
  DEFB $02,$EF,$FB,$B0,$81,$F4
  DEFB $02,$B7,$F4,$6F,$00,$F8
  DEFB $03,$97,$FB,$30,$80,$70
  DEFB $03,$A7,$FC,$9F,$01,$F0
  DEFB $03,$BF,$FC,$C0,$1C,$E0
  DEFB $01,$5D,$FF,$2D,$FB,$E0
  DEFB $01,$5E,$FF,$F3,$F9,$C0
  DEFB $00,$AE,$7F,$FF,$F7,$D0
  DEFB $00,$1E,$3F,$FF,$F7,$B0
  DEFB $00,$3E,$0F,$FF,$EF,$70
  DEFB $00,$3E,$01,$FF,$96,$F0
  DEFB $00,$3E,$18,$3E,$79,$F0
  DEFB $00,$3E,$38,$01,$F9,$F0
  DEFB $00,$1E,$F7,$06,$B1,$F0
  DEFB $00,$1E,$CC,$0B,$61,$E0
  DEFB $00,$1E,$34,$05,$C3,$E0
  DEFB $00,$5E,$D0,$0A,$C3,$C0
  DEFB $00,$DE,$40,$15,$43,$C0
  DEFB $01,$C1,$00,$0A,$47,$C0
  DEFB $03,$AF,$00,$00,$07,$C0
  DEFB $0E,$4F,$00,$00,$07,$C0
  DEFB $0D,$8A,$80,$00,$07,$C0
  DEFB $1A,$0B,$80,$00,$03,$80
  DEFB $1B,$0B,$80,$00,$04,$80
  DEFB $34,$0F,$00,$00,$0F,$00
  DEFB $36,$1F,$00,$00,$0F,$00
  DEFB $3C,$1B,$00,$00,$05,$00
  DEFB $34,$36,$00,$00,$0D,$00
  DEFB $38,$7E,$00,$00,$0F,$80
  DEFB $3C,$D6,$00,$00,$05,$C0
  DEFB $38,$B4,$00,$00,$06,$E0
  DEFB $3E,$00,$00,$00,$07,$E0
  DEFB $1C,$00,$00,$00,$06,$A0
  DEFB $1F,$00,$08,$00,$02,$C0
  DEFB $0F,$80,$18,$00,$00,$00
  DEFB $07,$F8,$F8,$00,$00,$00
  DEFB $03,$FF,$F0,$00,$00,$00
  DEFB $00,$FF,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00

; AlienFrame3
;
; Alien body frame 3 (phase 3).
AlienFrame3:
  DEFB $00,$00,$00,$7C,$00,$00
  DEFB $00,$00,$01,$FE,$00,$00
  DEFB $00,$00,$03,$FF,$00,$00
  DEFB $02,$00,$3B,$FF,$00,$00
  DEFB $04,$00,$7B,$FF,$00,$00
  DEFB $0F,$FF,$E7,$F9,$00,$00
  DEFB $0F,$FF,$67,$F6,$1C,$00
  DEFB $0F,$FF,$67,$F4,$3E,$00
  DEFB $05,$FF,$67,$F8,$3E,$00
  DEFB $06,$07,$67,$90,$76,$00
  DEFB $07,$80,$2F,$EC,$62,$00
  DEFB $07,$00,$2F,$F2,$EC,$00
  DEFB $07,$80,$0F,$F1,$DE,$00
  DEFB $07,$00,$0F,$E3,$B9,$00
  DEFB $07,$80,$07,$D7,$77,$00
  DEFB $07,$00,$08,$16,$EF,$80
  DEFB $07,$80,$07,$EC,$1F,$80
  DEFB $07,$78,$03,$CC,$6F,$C0
  DEFB $07,$7E,$04,$27,$C7,$E0
  DEFB $07,$BF,$85,$A8,$43,$E0
  DEFB $03,$DF,$E5,$AF,$C3,$F4
  DEFB $02,$EF,$FA,$50,$81,$F4
  DEFB $02,$B7,$F3,$CF,$00,$F8
  DEFB $03,$97,$F8,$30,$80,$70
  DEFB $03,$A7,$FF,$9F,$01,$F0
  DEFB $03,$BF,$FC,$C0,$18,$E0
  DEFB $01,$5D,$FF,$2D,$F7,$C0
  DEFB $01,$5E,$FF,$F3,$F3,$A0
  DEFB $00,$AE,$7F,$FF,$EF,$70
  DEFB $00,$1E,$3F,$FF,$CE,$F0
  DEFB $00,$3E,$0F,$FF,$1D,$F0
  DEFB $00,$3E,$01,$FC,$CB,$F0
  DEFB $00,$3E,$18,$23,$E0,$F0
  DEFB $00,$3E,$38,$1D,$61,$F0
  DEFB $00,$1E,$F7,$06,$C1,$F0
  DEFB $00,$1E,$CC,$1B,$C1,$E0
  DEFB $00,$1E,$34,$25,$43,$E0
  DEFB $00,$5E,$D0,$09,$43,$C0
  DEFB $00,$DE,$40,$12,$43,$C0
  DEFB $01,$C1,$00,$02,$07,$C0
  DEFB $03,$AF,$00,$00,$07,$C0
  DEFB $0E,$4F,$00,$00,$07,$C0
  DEFB $0D,$8A,$80,$00,$07,$C0
  DEFB $1A,$0B,$80,$00,$03,$80
  DEFB $1B,$0B,$80,$00,$04,$80
  DEFB $34,$0F,$00,$00,$0F,$00
  DEFB $36,$1F,$00,$00,$0F,$00
  DEFB $3C,$1B,$00,$00,$05,$00
  DEFB $34,$36,$00,$00,$0D,$00
  DEFB $38,$7E,$10,$00,$0F,$80
  DEFB $3C,$D6,$30,$00,$05,$C0
  DEFB $38,$B4,$30,$00,$06,$E0
  DEFB $3E,$00,$60,$00,$07,$E0
  DEFB $1E,$00,$E0,$00,$06,$A0
  DEFB $1F,$83,$C0,$00,$02,$C0
  DEFB $0F,$FF,$80,$00,$00,$00
  DEFB $07,$FF,$00,$00,$00,$00
  DEFB $01,$FC,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00

; JonesFrame0
;
; Jones the cat frame 0 (phase 0). 21 rows x 3 bytes, XOR-blitted.
JonesFrame0:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $30,$00,$00
  DEFB $18,$00,$00
  DEFB $0C,$00,$00
  DEFB $06,$00,$40
  DEFB $03,$E0,$E0
  DEFB $03,$FF,$F0
  DEFB $03,$FF,$F0
  DEFB $03,$FF,$C0
  DEFB $07,$BF,$C0
  DEFB $1F,$0F,$E8
  DEFB $34,$00,$74
  DEFB $20,$00,$18
  DEFB $00,$00,$0C
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00

; JonesFrame1
;
; Jones the cat frame 1 (phase 1). 21 rows x 3 bytes, XOR-blitted.
JonesFrame1:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $60,$00,$00
  DEFB $3C,$00,$10
  DEFB $07,$F8,$38
  DEFB $01,$FE,$FC
  DEFB $00,$FF,$FC
  DEFB $01,$FF,$F0
  DEFB $01,$FF,$E0
  DEFB $01,$E7,$C0
  DEFB $01,$80,$C0
  DEFB $01,$80,$C0
  DEFB $00,$C0,$C0
  DEFB $00,$C0,$E0
  DEFB $00,$00,$60
  DEFB $00

; JonesFrame2
;
; Jones the cat frame 2 (phase 2). 21 rows x 3 bytes, XOR-blitted.
JonesFrame2:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$10
  DEFB $80,$00,$38
  DEFB $E1,$E0,$FC
  DEFB $3F,$FF,$FC
  DEFB $07,$FF,$F0
  DEFB $03,$FF,$E0
  DEFB $01,$FF,$E0
  DEFB $00,$F6,$C0
  DEFB $00,$F0,$C0
  DEFB $00,$60,$C0
  DEFB $00,$31,$80
  DEFB $00,$1D,$80
  DEFB $00

; JonesFrame3
;
; Jones the cat frame 3 (phase 3). 21 rows x 3 bytes, XOR-blitted.
JonesFrame3:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$04
  DEFB $00,$00,$0E
  DEFB $00,$00,$7F
  DEFB $00,$0F,$FF
  DEFB $20,$FF,$F0
  DEFB $1F,$FF,$F0
  DEFB $0E,$7F,$F8
  DEFB $00,$FF,$3C
  DEFB $00,$F8,$14
  DEFB $01,$E0,$18
  DEFB $01,$80,$18
  DEFB $01,$80,$30
  DEFB $01,$80,$00
  DEFB $00,$C0,$00
  DEFB $00

; JonesFrame4
;
; Jones the cat frame 4 (phase 4). 21 rows x 3 bytes, XOR-blitted.
JonesFrame4:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $38,$00,$08
  DEFB $0E,$00,$1C
  DEFB $03,$FF,$FE
  DEFB $00,$FF,$FE
  DEFB $00,$FF,$E0
  DEFB $00,$FF,$E0
  DEFB $03,$F7,$F0
  DEFB $03,$E0,$7C
  DEFB $03,$00,$36
  DEFB $06,$00,$30
  DEFB $04,$00,$18
  DEFB $04,$00,$08
  DEFB $00,$00,$00
  DEFB $00
; Padding / unused.
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00

; IntroTileData
;
; 8×8 pixel tile bitmaps for the game's extra-data area (AlienFrame0-$F5FF).
; The first 8 tiles ($F1B0-$F22F, addresses 61936-61999) are blank padding.
; Tile graphics used by DrawIntroScreen (DrawIntroScreen) start at TileBitmaps
; (TileBitmaps): tile index N maps to address TileBitmaps + N×8. The tile map
; at IntroTileMap indexes into this set to build the 15×11 title-screen
; graphic. Subsequent tiles (beyond the intro set) are used by other game
; screens and the ship map.
;
; INTRO tile sheet — tiles starting at TileBitmaps, 16 per row, 2× scale:
;
IntroTileData:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
TileBitmaps:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$01,$03,$07
  DEFB $01,$0F,$1F,$3D,$C6,$EE,$F6,$ED
  DEFB $FC,$C3,$80,$00,$00,$00,$00,$00
  DEFB $00,$80,$40,$30,$10,$0C,$06,$03
  DEFB $00,$00,$00,$00,$00,$01,$03,$03
  DEFB $0E,$1C,$3F,$77,$EC,$F6,$BD,$FB
  DEFB $49,$A1,$0A,$D2,$D0,$40,$40,$40
  DEFB $01,$00,$00,$00,$00,$00,$00,$00
  DEFB $80,$C0,$60,$30,$18,$0C,$00,$07
  DEFB $07,$0F,$1F,$1F,$3F,$7F,$7F,$FD
  DEFB $12,$35,$E1,$C5,$8E,$10,$52,$84
  DEFB $88,$08,$00,$04,$00,$00,$40,$80
  DEFB $01,$05,$02,$01,$00,$01,$00,$00
  DEFB $00,$80,$80,$60,$20,$70,$90,$08
  DEFB $01,$01,$03,$07,$07,$0F,$0F,$0F
  DEFB $F3,$FE,$60,$F8,$E1,$E2,$E4,$68
  DEFB $28,$48,$90,$20,$00,$48,$70,$A0
  DEFB $80,$00,$00,$80,$00,$00,$00,$00
  DEFB $28,$04,$02,$03,$11,$00,$04,$01
  DEFB $00,$00,$00,$00,$00,$80,$80,$40
  DEFB $1F,$1D,$3F,$3F,$7E,$7C,$F8,$F8
  DEFB $D8,$E1,$E2,$04,$11,$84,$29,$02
  DEFB $20,$00,$80,$80,$40,$40,$04,$00
  DEFB $02,$00,$00,$06,$00,$01,$01,$00
  DEFB $C0,$A0,$60,$70,$30,$78,$28,$18
  DEFB $00,$01,$01,$01,$03,$03,$03,$07
  DEFB $FC,$F9,$FC,$F8,$FF,$AE,$EC,$FF
  DEFB $10,$25,$05,$48,$11,$01,$10,$A8
  DEFB $80,$20,$00,$00,$00,$08,$00,$00
  DEFB $2C,$0C,$14,$0A,$12,$12,$09,$05
  DEFB $07,$0F,$0F,$0F,$0F,$0F,$0F,$1F
  DEFB $BE,$AE,$FF,$FD,$A7,$FD,$EF,$FF
  DEFB $44,$82,$06,$DB,$8C,$2C,$0A,$C4
  DEFB $80,$40,$00,$00,$40,$00,$40,$40
  DEFB $05,$05,$0B,$07,$07,$0F,$01,$01
  DEFB $00,$00,$80,$80,$80,$80,$80,$C0
  DEFB $1F,$1F,$1F,$1F,$1F,$1F,$1F,$1F
  DEFB $FF,$FF,$E7,$FF,$F5,$ED,$FF,$E5
  DEFB $A0,$FE,$CC,$FA,$F4,$6A,$6A,$D9
  DEFB $00,$00,$00,$80,$10,$08,$21,$40
  DEFB $00,$00,$00,$00,$00,$10,$68,$04
  DEFB $00,$00,$00,$00,$00,$00,$08,$01
  DEFB $01,$01,$0B,$01,$83,$45,$8B,$3F
  DEFB $C0,$C0,$C0,$C0,$C0,$C0,$C0,$C0
  DEFB $1F,$1F,$1F,$1F,$1F,$1F,$0F,$0F
  DEFB $FF,$FB,$FE,$E9,$F2,$F4,$FF,$F2
  DEFB $A7,$F5,$DB,$6F,$BD,$36,$FB,$AC
  DEFB $CD,$CC,$A6,$59,$FC,$CA,$D0,$F4
  DEFB $70,$DC,$7F,$67,$D0,$61,$67,$ED
  DEFB $00,$80,$00,$80,$61,$7B,$B1,$1B
  DEFB $00,$00,$00,$00,$00,$00,$40,$C1
  DEFB $00,$00,$00,$02,$04,$01,$0A,$07
  DEFB $02,$03,$1F,$3E,$FF,$BD,$FF,$FF
  DEFB $7F,$BF,$7F,$FF,$FF,$7D,$BB,$FF
  DEFB $C0,$C0,$C0,$C0,$C0,$C0,$80,$80
  DEFB $0F,$0F,$0F,$07,$07,$03,$03,$01
  DEFB $FB,$FE,$FE,$FF,$FF,$FF,$FF,$FF
  DEFB $4E,$46,$32,$11,$89,$5D,$A9,$D7
  DEFB $FE,$DD,$AE,$7F,$EF,$D7,$FA,$9D
  DEFB $95,$FF,$E4,$E5,$B4,$5F,$FD,$76
  DEFB $2E,$6F,$9B,$FE,$79,$FD,$1F,$FF
  DEFB $41,$E1,$50,$CF,$EF,$F7,$BF,$DD
  DEFB $07,$1F,$31,$E7,$E7,$6B,$7F,$BE
  DEFB $FF,$7F,$BF,$FF,$FF,$FF,$FF,$FE
  DEFB $FA,$FF,$EF,$D3,$B7,$DE,$EE,$DC
  DEFB $80,$80,$00,$00,$00,$00,$00,$00
  DEFB $01,$00,$00,$00,$00,$00,$00,$00
  DEFB $FF,$FF,$FF,$7F,$7F,$3F,$3F,$1F
  DEFB $E1,$F6,$FF,$FF,$FE,$FE,$FE,$FF
  DEFB $9C,$8B,$9F,$AE,$E9,$FF,$AA,$B5
  DEFB $BF,$55,$BB,$DF,$6B,$61,$4E,$77
  DEFB $7F,$BF,$FF,$FF,$FF,$7F,$BF,$7F
  DEFB $FF,$FF,$FE,$FF,$FC,$FE,$FF,$FF
  DEFB $00,$00,$00,$00,$03,$1C,$BD,$FD
  DEFB $7D,$FB,$7F,$3F,$7F,$FF,$FF,$FF
  DEFB $FC,$F8,$F8,$F0,$F0,$E0,$E0,$C0
  DEFB $1F,$0F,$0F,$07,$01,$01,$00,$00
  DEFB $FF,$FF,$FF,$FF,$FF,$FF,$FF,$3F
  DEFB $FF,$F7,$F9,$F5,$FA,$FD,$FF,$FF
  DEFB $7F,$FF,$7F,$BF,$FF,$FF,$FF,$FF
  DEFB $7F,$FF,$7F,$BF,$FF,$FF,$FF,$FF
  DEFB $FF,$FF,$FF,$FE,$FF,$FF,$FF,$FB
  DEFB $5E,$7E,$F7,$FB,$7F,$BF,$FF,$96
  DEFB $FF,$FF,$7F,$7F,$FE,$FC,$7C,$F8
  DEFB $80,$80,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$20,$31,$A9
  DEFB $1F,$0F,$07,$03,$01,$00,$80,$09
  DEFB $FE,$FF,$FE,$FF,$FE,$FF,$7F,$3F
  DEFB $37,$5F,$EF,$5F,$BF,$FF,$BF,$BF
  DEFB $FF,$FF,$7F,$7F,$FF,$FA,$EF,$E7
  DEFB $B7,$3F,$FF,$FF,$FE,$E8,$E4,$FF
  DEFB $BE,$B9,$7F,$F4,$6D,$F6,$22,$CC
  DEFB $F0,$E0,$40,$80,$00,$00,$00,$00
  DEFB $04,$0A,$03,$05,$00,$00,$00,$00
  DEFB $B1,$E6,$7C,$F0,$32,$3B,$03,$00
  DEFB $26,$81,$CB,$69,$D4,$6A,$B6,$9A
  DEFB $FF,$FF,$FD,$FF,$7F,$7F,$40,$A7
  DEFB $7F,$FF,$CE,$59,$E0,$CA,$14,$02
  DEFB $FF,$7A,$A5,$AA,$70,$20,$00,$04
  DEFB $D0,$80,$00,$00,$00,$00,$C0,$00
  DEFB $01,$00,$00,$00,$00,$00,$00,$00
  DEFB $22,$E2,$02,$01,$00,$00,$00,$00
  DEFB $42,$00,$58,$81,$00,$00,$00,$00
  DEFB $44,$20,$00,$00,$00,$00,$00,$00
  DEFB $A7,$8A,$F5,$00,$00,$01,$4B,$55
  DEFB $53,$45,$D2,$64,$A7,$AC,$F5,$00
  DEFB $00,$01,$4B,$55,$D0,$E6,$A7,$00
  DEFB $00,$00,$00,$01,$4B,$44,$4F,$57
  DEFB $CE,$F0,$A7,$00,$00,$00,$00,$01
  DEFB $4B,$46,$49,$52,$C5,$FA,$A7,$00
  DEFB $00,$00,$00,$01,$4B,$51,$55,$49
  DEFB $D4,$04,$A8,$00,$00,$00,$00,$01
  DEFB $53,$43,$41,$4E,$B1,$10,$A8,$11
  DEFB $F6,$00,$00,$01,$43,$52,$57,$52
  DEFB $41,$D4,$1B,$AA,$00,$00,$00,$00
  DEFB $01,$44,$41,$4D,$52,$41,$D4,$4A
  DEFB $AA,$04,$F6,$38,$F6,$01,$45,$4E
  DEFB $44,$45,$58,$D0,$2A,$A8,$00,$00
  DEFB $00,$00,$01,$54,$49,$4D,$52,$41
  DEFB $D4,$62,$AA,$00,$00,$00,$00,$01
  DEFB $41,$4C,$44,$45,$C4,$E3,$A8,$00
  DEFB $00,$00,$00,$01,$45,$4E,$44,$45
  DEFB $4E,$C4,$30,$A8,$00,$00,$00,$00
  DEFB $01,$43,$52,$57,$44,$45,$C4,$F2

; Zero-padding between intro tile data and boot-time residual
;
; 2304 bytes of zeros separating the last tile row of IntroTileData from the
; small residual data block at BootResidual. Reserved space, never touched by
; the running game.
  DEFS $900

; Boot-time residual data
;
; 232 bytes of leftover content loaded into the top of memory by the tape
; loader. Includes a 64-byte data table (probably loader / BASIC interpreter
; state) followed by a partial 8x8-pixel font containing the upper-case letters
; A through U (21 glyphs from offset +84 onwards, each 8 bytes). The running
; game sets the stack pointer just below this region at GameEntry (GameEntry)
; so this content overlaps the active stack and is overwritten as soon as the
; game begins running. Not referenced by any game routine; the only references
; to in-range 16-bit values treat them as signed screen strides (e.g. -32 for
; one cell up within a third of the display), not as memory pointers.
BootResidual:
  DEFB $F3,$0D,$CE,$0B,$E3,$50,$CE,$0B
  DEFB $E4,$50,$1D,$17,$DC,$0A,$CE,$0B
  DEFB $E7,$50,$1A,$17,$DC,$0A,$D7,$18
  DEFB $38,$00,$38,$00,$0D,$19,$DB,$02
  DEFB $4D,$00,$06,$55,$FA,$00,$DB,$02
  DEFB $4D,$00,$00,$00,$21,$00,$B6,$5C
  DEFB $08,$01,$F3,$0D,$21,$17,$C6,$1E
  DEFB $FF,$5F,$76,$1B,$03,$13,$00,$3E
  DEFB $00,$3C,$42,$42,$7E,$42,$42,$00
  DEFB $00,$7C,$42,$7C,$42,$42,$7C,$00
  DEFB $00,$3C,$42,$40,$40,$42,$3C,$00
  DEFB $00,$78,$44,$42,$42,$44,$78,$00
  DEFB $00,$7E,$40,$7C,$40,$40,$7E,$00
  DEFB $00,$7E,$40,$7C,$40,$40,$40,$00
  DEFB $00,$3C,$42,$40,$4E,$42,$3C,$00
  DEFB $00,$42,$42,$7E,$42,$42,$42,$00
  DEFB $00,$3E,$08,$08,$08,$08,$3E,$00
  DEFB $00,$02,$02,$02,$42,$42,$3C,$00
  DEFB $00,$44,$48,$70,$48,$44,$42,$00
  DEFB $00,$40,$40,$40,$40,$40,$7E,$00
  DEFB $00,$42,$66,$5A,$42,$42,$42,$00
  DEFB $00,$42,$62,$52,$4A,$46,$42,$00
  DEFB $00,$3C,$42,$42,$42,$42,$3C,$00
  DEFB $00,$7C,$42,$42,$7C,$40,$40,$00
  DEFB $00,$3C,$42,$42,$52,$4A,$3C,$00
  DEFB $00,$7C,$42,$42,$7C,$44,$42,$00
  DEFB $00,$3C,$40,$3C,$02,$42,$3C,$00
  DEFB $00,$FE,$10,$10,$10,$10,$10,$00
  DEFB $00,$42,$42,$42,$42,$42,$3C,$00

