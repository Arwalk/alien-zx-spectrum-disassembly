  ORG $4000

; Loading screen — display file and attributes
;
; The ZX Spectrum screen memory ($4000-$5AFF) as captured in the tape image:
; 6144 bytes of pixel data followed by 768 attribute bytes. The tape loader
; fills $4000-$5A7F (6784 bytes, the first headerless block), so this doubles
; as the picture shown while the rest of the game loads. Sub-labels Screen_RxCy
; mark the character row/column where visible content starts.
DisplayFile:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R1C8:
  DEFB $00,$38,$00,$00,$E0,$00,$00,$FE
  DEFB $00,$00,$7F,$00,$00,$44,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$AA,$A8,$4A,$10,$42,$90
  DEFB $45,$15,$12,$2A,$29,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$01,$FC
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$0E,$49,$00
  DEFB $01,$80,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$07,$12,$88,$00
  DEFB $00,$01,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$F3,$28,$80,$00
  DEFB $00,$00,$28,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$44,$00,$00,$40,$00,$00,$92
  DEFB $00,$00,$21,$00,$00,$64,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$C4,$68,$EA,$3A,$43,$10
  DEFB $35,$65,$0B,$93,$92,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$0F,$C3
  DEFB $80,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$1C,$A1,$00
  DEFB $00,$C0,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$0F,$35,$08,$00
  DEFB $00,$05,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$FE,$48,$00,$00
  DEFB $00,$00,$04,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$44,$00,$00,$40,$00,$00,$10
  DEFB $00,$00,$24,$00,$00,$54,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$1F,$80
  DEFB $40,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$3F,$0A,$00
  DEFB $00,$60,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$1F,$E1,$00,$00
  DEFB $00,$02,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$03,$60,$90,$00,$00
  DEFB $00,$00,$02,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$7C,$00,$00,$40,$00,$00,$10
  DEFB $00,$00,$3C,$00,$00,$54,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$3D,$00
  DEFB $30,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$77,$D2,$00
  DEFB $00,$30,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$1F,$C5,$04,$00
  DEFB $00,$01,$60,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$07,$F8,$20,$80,$00
  DEFB $00,$00,$03,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$44,$00,$00,$40,$00,$00,$10
  DEFB $00,$00,$24,$00,$00,$4C,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$C6,$00
  DEFB $10,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$EC,$D0,$00
  DEFB $00,$18,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$3F,$8E,$00,$00
  DEFB $00,$00,$20,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$07,$E1,$00,$00,$00
  DEFB $00,$00,$11,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$44,$00,$00,$42,$00,$00,$92
  DEFB $00,$00,$21,$00,$00,$44,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$CA,$6E,$EA,$10,$E3,$28
  DEFB $36,$35,$0B,$13,$12,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$01,$EE,$00
  DEFB $0C,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$01,$F6,$40,$00
  DEFB $00,$0C,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$7F,$10,$00,$00
  DEFB $00,$01,$70,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$E2,$48,$00,$00
  DEFB $00,$00,$00,$80,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$EE,$00,$00,$FE,$00,$00,$FE
  DEFB $00,$00,$7F,$00,$00,$EE,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$AA,$88,$4A,$30,$22,$A8
  DEFB $45,$45,$10,$A8,$A9,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$03,$F6,$00
  DEFB $06,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$BD,$40,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$7F,$52,$40,$00
  DEFB $00,$00,$90,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$E4,$70,$00,$00
  DEFB $00,$00,$04,$80,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R1C0L7:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$CA,$AC,$44,$10,$23,$10
  DEFB $E6,$27,$11,$29,$29,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$07,$ED,$00
  DEFB $03,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$FB,$40,$00
  DEFB $00,$07,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$FD,$84,$80,$00
  DEFB $00,$00,$08,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$68,$A0,$00,$00
  DEFB $00,$00,$01,$40,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$1F,$D8,$20,$00,$00
  DEFB $00,$00,$02,$C0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R9C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$FC,$10,$80,$00,$00
  DEFB $00,$00,$00,$2C,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$07,$BE,$44,$80,$00,$00
  DEFB $00,$00,$00,$05,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$A0,$00,$00,$00
  DEFB $00,$00,$00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R12C8:
  DEFB $00,$00,$1F,$FF,$A7,$CD,$70,$00
  DEFB $00,$00,$02,$7F,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FB,$4E,$FE,$95,$2E
  DEFB $41,$07,$FF,$FA,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$FF,$E1,$9C,$BF,$7F
  DEFB $FF,$00,$7D,$FC,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R15C8:
  DEFB $00,$00,$00,$1F,$FF,$FF,$7F,$7F
  DEFB $FF,$5E,$FF,$80,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$1D,$E1,$00,$00,$00
  DEFB $00,$00,$00,$A0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$F9,$25,$20,$00,$00
  DEFB $00,$00,$00,$0C,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$AE,$82,$40,$00,$00
  DEFB $00,$00,$00,$05,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$FE,$00,$00,$00
  DEFB $00,$00,$00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FB,$F5,$CC,$DC,$80
  DEFB $00,$00,$03,$BF,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FE,$46,$DD,$FF,$6F
  DEFB $E1,$1F,$7F,$FF,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$FF,$F6,$8B,$55,$BF
  DEFB $FF,$00,$FB,$F8,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$FF,$F7,$FF,$FF
  DEFB $FF,$7E,$FF,$80,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$3F,$E2,$80,$00,$00
  DEFB $00,$00,$00,$60,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$FC,$05,$00,$00,$00
  DEFB $00,$00,$00,$14,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FF,$06,$00,$00,$00
  DEFB $00,$00,$00,$0B,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$E7,$CC,$00,$00,$00
  DEFB $00,$00,$00,$0B,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FE,$DB,$A6,$7F,$00
  DEFB $00,$00,$1F,$7F,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FE,$32,$AE,$E4,$9B
  DEFB $50,$31,$BF,$EF,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$FF,$FF,$9F,$BB,$FF
  DEFB $FE,$00,$7F,$F8,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0F,$FF,$F9,$7F,$7F
  DEFB $FF,$F7,$7F,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$3F,$04,$80,$00,$00
  DEFB $00,$00,$06,$70,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$F8,$48,$00,$00,$00
  DEFB $00,$00,$00,$0A,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FD,$DB,$00,$00,$00
  DEFB $00,$00,$00,$07,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$FA,$80,$00,$00
  DEFB $00,$00,$00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$E9,$6F,$59,$67,$80
  DEFB $00,$02,$3E,$FF,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$07,$FF,$11,$7F,$E5,$FE
  DEFB $CF,$E7,$FF,$D3,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$7F,$FF,$AE,$DF,$FF
  DEFB $FF,$00,$3F,$F0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$07,$FF,$F5,$BF,$BF
  DEFB $FE,$FB,$7F,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$7E,$11,$40,$00,$00
  DEFB $00,$00,$00,$30,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R9C0L4:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$FF,$11,$00,$00,$00
  DEFB $00,$00,$00,$12,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$A7,$8C,$40,$00,$00
  DEFB $00,$00,$00,$07,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$F5,$F4,$10,$00,$00
  DEFB $00,$00,$00,$83,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$F2,$BD,$FC,$D0,$61
  DEFB $00,$04,$FF,$FF,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$07,$FF,$89,$EF,$B4,$79
  DEFB $EF,$E7,$FF,$B7,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$7F,$FE,$E9,$6B,$FF
  DEFB $FC,$03,$7F,$F0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$FF,$FA,$FF,$FF
  DEFB $FF,$7F,$FE,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$7C,$84,$40,$00,$00
  DEFB $00,$00,$01,$78,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$AE,$01,$08,$00,$00
  DEFB $00,$00,$00,$12,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FD,$2C,$00,$00,$00
  DEFB $00,$00,$00,$0F,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$ED,$6A,$08,$10,$00
  DEFB $00,$00,$00,$45,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$F4,$36,$CA,$61,$7B
  DEFB $00,$01,$BD,$7D,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$FF,$5D,$D7,$5F,$FD
  DEFB $F7,$6B,$FF,$DE,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$3F,$FE,$FF,$61,$7F
  DEFB $FE,$1C,$FF,$E0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$FF,$FD,$FF,$FF
  DEFB $FF,$BF,$FC,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$F8,$29,$04,$00,$00
  DEFB $00,$00,$01,$28,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$EC,$10,$00,$00,$00
  DEFB $00,$00,$00,$09,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$EF,$0A,$40,$00,$00
  DEFB $00,$00,$00,$01,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$6A,$21,$68,$00
  DEFB $00,$00,$08,$8B,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$FF,$FB,$D0,$67,$B1
  DEFB $40,$0A,$FF,$BB,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$03,$FF,$A9,$FA,$FD,$1F
  DEFB $BF,$7F,$FF,$EE,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$3F,$FE,$AA,$4E,$BF
  DEFB $FF,$BD,$FF,$E0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$FF,$FF,$FF,$FF
  DEFB $FF,$FF,$7C,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$F8,$02,$00,$00,$00
  DEFB $00,$00,$00,$18,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$07,$FF,$A8,$00,$00,$00
  DEFB $00,$00,$00,$05,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$FF,$C4,$40,$00,$00
  DEFB $00,$00,$00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$1F,$E5,$D9,$40,$04,$00
  DEFB $00,$00,$01,$3F,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$0F,$F2,$AC,$F4,$ED,$1B
  DEFB $C1,$07,$FF,$FF,$80,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$01,$FF,$D7,$9D,$76,$FF
  DEFB $DD,$BE,$FE,$DC,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$1F,$FF,$B5,$77,$7F
  DEFB $FF,$FD,$FF,$C0,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$3F,$FF,$FF,$FF
  DEFB $FB,$96,$F8,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$1F,$FE,$37,$FF
  DEFB $B7,$BE,$F0,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R17C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$04,$B1,$26,$FF,$7F
  DEFB $FF,$D0,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$01,$22,$42
  DEFB $44,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R19C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R20C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R20C16:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R21C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R22C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R23C0:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R23C8:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$0F,$FF,$5F,$FF
  DEFB $3F,$B9,$E0,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$0A,$E6,$81,$FF,$FF
  DEFB $7A,$80,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$E2,$00
  DEFB $20,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$3C,$42,$82,$00
  DEFB $44,$7E,$82,$00,$FE,$3C,$00,$3C
  DEFB $3C,$42,$7E,$00,$3C,$40,$3E,$7E
  DEFB $42,$24,$18,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$07,$FE,$EF,$7F
  DEFB $FF,$7F,$40,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R17C0L2:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$03,$7C,$CB,$FD,$CE
  DEFB $A5,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$02,$58
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$42,$62,$44,$00
  DEFB $48,$40,$44,$00,$10,$42,$00,$40
  DEFB $42,$42,$40,$00,$42,$40,$08,$40
  DEFB $62,$7E,$28,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$FF,$5F,$7F
  DEFB $FF,$F4,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R17C0L3:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$05,$F0,$69,$FF,$59
  DEFB $AA,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$01,$81
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$42,$52,$28,$00
  DEFB $70,$7C,$28,$00,$10,$42,$00,$3C
  DEFB $42,$42,$7C,$00,$42,$40,$08,$7C
  DEFB $52,$24,$08,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$01,$FE,$BF,$FF
  DEFB $FE,$6D,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$32,$D4,$7F,$E0
  DEFB $70,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$7E,$4A,$10,$00
  DEFB $48,$40,$10,$00,$10,$42,$00,$02
  DEFB $7E,$42,$40,$00,$7E,$40,$08,$40
  DEFB $4A,$24,$08,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$20,$00,$FF,$FF,$FA
  DEFB $E8,$F6,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
Screen_R17C0L5:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$3B,$6A,$7F,$CA
  DEFB $20,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$42,$46,$10,$00
  DEFB $44,$40,$10,$00,$10,$42,$00,$42
  DEFB $42,$24,$40,$00,$42,$40,$08,$40
  DEFB $46,$7E,$08,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$31,$80,$7F,$BF,$EF
  DEFB $E4,$22,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$B6,$40,$14
  DEFB $00,$C0,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$42,$42,$10,$00
  DEFB $42,$7E,$10,$00,$10,$3C,$00,$3C
  DEFB $42,$18,$7E,$00,$42,$7E,$3E,$7E
  DEFB $42,$24,$3E,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$A9,$09,$3F,$BF,$E7
  DEFB $FF,$CC,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$9A,$A7,$02
  DEFB $04,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
AttrFileOrigin:
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$04,$04,$04,$04,$04,$04
  DEFB $04,$04,$04,$04,$04,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$04,$04,$04,$04,$04,$04
  DEFB $04,$04,$04,$04,$04,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
AttrRow10:
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $07,$07,$04,$04,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$38,$04,$07,$04
  DEFB $07,$04,$04,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$38,$04,$04,$07
  DEFB $04,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $07,$3C,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $07,$04,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $04,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$04,$07,$04,$04
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$04,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
MsgLineAttrs:
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$07,$07,$07,$07

; Boot-time residue: printer buffer and system variables
;
; This range ($5B00-$5CCB) is not loaded from tape (the loader's three blocks
; cover $4000-$5A7F, $6000-$B3FB and $EA60-$F617): it holds whatever the ROM
; and BASIC loader left behind at snapshot time — a zeroed printer buffer
; ($5B00-$5BFF) and the live system-variables area from $5C00 onwards. Never
; read by the game.
SystemArea:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $FF,$00,$00,$00,$FF,$00,$23,$0D
SysLastKey:
  DEFB $0D,$23,$05,$00,$00,$00,$11,$00
  DEFB $01,$00,$06,$00,$0B,$00,$01,$00
  DEFB $01,$00,$06,$00,$10,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$3C
  DEFB $40,$00,$FF,$CD,$01,$FC,$5F,$00
  DEFB $00,$00,$00,$00,$FF,$0A,$00,$07
  DEFB $07,$00,$00,$D8,$5E,$7F,$5D,$B6
  DEFB $5C,$B6,$5C,$CB,$5C,$BF,$5D,$CA
  DEFB $5C,$D9,$5E,$DC,$5E,$AF,$5D,$EF
  DEFB $5E,$DE,$5E,$DE,$5E,$DE,$5E,$2D
  DEFB $92,$5C,$10,$00,$00,$00,$00,$00
  DEFB $00,$02,$6C,$D8,$7D,$1A,$00,$00
SysFrames:
  DEFB $07,$00,$00,$58,$FF,$00,$00,$21
  DEFB $00,$5B,$21,$17,$00,$40,$E0,$50
  DEFB $21,$18,$21,$17,$01,$00,$00,$07
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$FF,$5F,$FF,$FF,$F4,$09
  DEFB $A8,$10,$4B,$F4,$09,$C4,$15,$53
  DEFB $81,$0F,$C4,$15,$52,$F4,$09,$C4
  DEFB $15,$50,$80,$00

; BASIC loader program "ALIEN0" with embedded machine code
;
; The 537-byte BASIC block from the tape, resident at PROG ($5CCC). Its
; machine-code payload (at offset +4, $5CD0) is the loader the BASIC runs via
; USR: three ROM LD-BYTES calls ($0556) that pull in the tape's headerless
; blocks — IX=$4000/DE=$1A80 (loading screen), IX=$6000/DE=$53FC (main game),
; IX=$EA60/DE=$0BB8 (extra data) — then LD BC,GameEntry / RET, handing the
; game's entry address back to BASIC as the USR result. It is followed by the
; matching SA-BYTES ($04C2) save stubs, a mastering-time leftover. Nothing in
; this block is referenced once the game is running.
LoadedCode:
  DEFB $00,$6C,$00,$EA,$DD,$21,$00,$40
  DEFB $11,$80,$1A,$37,$3E,$FF,$CD,$56
  DEFB $05,$DD,$21,$00,$60,$11,$FC,$53
  DEFB $37,$3E,$FF,$CD,$56,$05,$DD,$21
  DEFB $60,$EA,$11,$B8,$0B,$37,$3E,$FF
  DEFB $CD,$56,$05,$01,$69,$8E,$C9,$3E
  DEFB $FF,$DD,$21,$00,$40,$11,$80,$1A
  DEFB $CD,$C2,$04,$C9,$3E,$FF,$DD,$21
  DEFB $00,$60,$11,$FC,$53,$CD,$C2,$04
  DEFB $C9,$3E,$FF,$DD,$21,$60,$EA,$11
  DEFB $B8,$0B,$CD,$C2,$04,$C9,$30,$10
  DEFB $89,$11,$68,$30,$30,$30,$30,$30
  DEFB $30,$30,$30,$30,$30,$30,$30,$30
  DEFB $30,$30,$30,$30,$30,$30,$0D,$00
  DEFB $0A,$80,$00,$FD,$32,$34,$35,$37
  DEFB $35,$0E,$00,$00,$FF,$5F,$00,$3A
  DEFB $D9,$30,$0E,$00,$00,$00,$00,$00
  DEFB $3A,$DA,$30,$0E,$00,$00,$00,$00
  DEFB $00,$3A,$E7,$30,$0E,$00,$00,$00
  DEFB $00,$00,$3A,$FB,$3A,$F4,$32,$33
  DEFB $36,$35,$39,$0E,$00,$00,$6B,$5C
  DEFB $00,$2C,$30,$0E,$00,$00,$00,$00
  DEFB $00,$3A,$F1,$6C,$3D,$C0,$28,$32
  DEFB $35,$36,$0E,$00,$00,$00,$01,$00
  DEFB $2A,$BE,$32,$33,$36,$33,$36,$0E
  DEFB $00,$00,$54,$5C,$00,$2B,$BE,$32
  DEFB $33,$36,$33,$35,$0E,$00,$00,$53
  DEFB $5C,$00,$2B,$35,$0E,$00,$00,$05
  DEFB $00,$00,$29,$3A,$F2,$30,$0E,$00
  DEFB $00,$00,$00,$00,$3A,$F1,$4C,$3D
  DEFB $C0,$4C,$0D,$00,$0F,$61,$00,$FD
  DEFB $32,$34,$35,$37,$35,$0E,$00,$00
  DEFB $FF,$5F,$00,$3A,$E7,$30,$0E,$00
  DEFB $00,$00,$00,$00,$3A,$DA,$30,$0E
  DEFB $00,$00,$00,$00,$00,$3A,$D9,$37
  DEFB $0E,$00,$00,$07,$00,$00,$3A,$FB
  DEFB $3A,$F1,$50,$3D,$32,$35,$36,$0E
  DEFB $00,$00,$00,$01,$00,$2A,$BE,$32
  DEFB $33,$36,$33,$36,$0E,$00,$00,$54
  DEFB $5C,$00,$2B,$BE,$32,$33,$36,$33
  DEFB $35,$0E,$00,$00,$53,$5C,$00,$3A
  DEFB $F1,$4C,$3D,$C0,$28,$50,$2B,$35
  DEFB $0E,$00,$00,$05,$00,$00,$29,$0D
  DEFB $00,$14,$6B,$00,$F8,$22,$41,$4C
  DEFB $49,$45,$4E,$30,$22,$CA,$31,$30
  DEFB $0E,$00,$00,$0A,$00,$00,$3A,$F5
  DEFB $AC,$32,$31,$0E,$00,$00,$15,$00
  DEFB $00,$2C,$34,$0E,$00,$00,$04,$00
  DEFB $00,$3B,$22,$41,$4E,$59,$20,$4B
  DEFB $45,$59,$20,$54,$4F,$20,$53,$41
  DEFB $56,$45,$20,$22,$3A,$EB,$4E,$3D
  DEFB $31,$0E,$00,$00,$01,$00,$00,$CC
  DEFB $33,$0E,$00,$00,$03,$00,$00,$3A
  DEFB $F5,$AC,$32,$31,$0E,$00,$00,$15
  DEFB $00,$00,$2C,$32,$30,$0E,$00,$00
  DEFB $14,$00,$00,$3B,$22,$41,$4C,$49
  DEFB $45,$4E,$23,$22,$3B,$4E,$0D,$00
  DEFB $1E,$33,$00,$FA,$A6,$C9,$22,$22
  DEFB $CB,$F1,$4C,$3D,$C0,$28,$28,$50
  DEFB $2B,$33,$35,$0E,$00,$00,$23,$00
  DEFB $00,$29,$2B,$31,$33,$0E,$00,$00
  DEFB $0D,$00,$00,$2A,$4E,$29,$3A,$F3
  DEFB $4E,$3A,$E2,$3A,$EC,$32,$30,$0E
  DEFB $00,$00,$14,$00,$00,$0D,$00,$28
  DEFB $0A,$00,$EC,$33,$30,$0E,$00,$00
  DEFB $1E,$00,$00,$0D,$80,$EF,$22,$22
  DEFB $0D,$80,$00,$00,$D0,$5C,$00,$00
  DEFB $00,$05,$00,$00,$20,$00,$00,$00
  DEFB $FF,$5F,$00,$20,$20,$20,$20,$20
  DEFB $20,$00,$00,$00,$80,$00,$00,$00
  DEFB $41,$4C,$49,$45,$4E,$30,$20,$20
  DEFB $20,$20,$19,$02,$00,$00,$0D,$02
  DEFB $0D,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $DB,$02,$B1,$33,$DE,$5E,$00,$CB
  DEFB $9B,$36,$B1,$33,$B7,$2D,$DE,$5E
  DEFB $01,$00,$F7,$5C,$2B,$2D,$65,$33
  DEFB $00,$00,$ED,$10,$3A,$00,$52,$00
  DEFB $5D,$1C,$BA,$CD,$52,$1C,$76,$1B
  DEFB $03,$13,$00,$3E

; Ship-map data — the Nostromo's three deck maps
;
; 3 decks x 19 rows x 20 columns of tile codes (the same 20-wide grid format
; the room views use), drawn one deck at a time (380 bytes, selected by
; RoomTypeByte) by DrawRoomBackground (DrawRoomBackground) as the map pane /
; room-view background; the Upper/Middle/Lower rows of the orders menu switch
; decks via SelectDeckMap (SelectDeckMap). Row texts like "UPPER DECK" are
; plain ASCII tile codes; other values are map glyphs (room outlines,
; corridors, and the legend symbols: 123 '{' ladder up, 124 '|' hatchway down,
; 125 '}' duct grille).
ShipMapData:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 0
  DEFB $00,$55,$50,$50,$45,$52,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 1
  DEFB $00,$44,$45,$43,$4B,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 2
  DEFB $00,$00,$00,$00,$00,$00,$05,$0B,$0C,$01,$01,$0F,$01,$0C,$01,$01,$01,$24,$00,$00 ; UPPER deck
                                                                                       ; row 3
  DEFB $00,$00,$00,$00,$00,$05,$03,$04,$02,$60,$00,$00,$00,$02,$60,$00,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 4
  DEFB $00,$00,$00,$00,$05,$03,$04,$60,$02,$00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 5
  DEFB $00,$00,$00,$00,$1D,$04,$00,$00,$13,$01,$01,$01,$22,$86,$01,$0C,$0F,$15,$00,$00 ; UPPER deck
                                                                                       ; row 6
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00,$00,$02,$60,$00,$02,$60,$02,$00,$00 ; UPPER deck
                                                                                       ; row 7
  DEFB $00,$00,$00,$00,$02,$00,$7C,$00,$10,$00,$00,$00,$02,$00,$00,$02,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 8
  DEFB $00,$00,$00,$00,$13,$0F,$01,$01,$15,$00,$00,$00,$10,$00,$00,$10,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 9
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$10,$00,$00,$00,$02,$00,$00,$02,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 10
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00,$60,$02,$00,$00,$02,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 11
  DEFB $00,$00,$00,$00,$1E,$09,$00,$00,$13,$01,$01,$01,$1A,$1B,$0F,$14,$01,$15,$00,$00 ; UPPER deck
                                                                                       ; row 12
  DEFB $00,$00,$00,$00,$07,$08,$09,$60,$02,$00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 13
  DEFB $00,$00,$00,$00,$00,$07,$08,$09,$02,$60,$00,$00,$00,$02,$60,$00,$00,$02,$00,$00 ; UPPER deck
                                                                                       ; row 14
  DEFB $00,$00,$00,$00,$00,$00,$07,$1F,$14,$01,$01,$0F,$01,$14,$01,$01,$01,$25,$00,$00 ; UPPER deck
                                                                                       ; row 15
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 16
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 17
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; UPPER deck
                                                                                       ; row 18
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$05,$0B,$0C,$01,$0C,$01,$01,$0D,$09 ; MIDDLE
                                                                                       ; deck row 0
  DEFB $00,$4D,$49,$44,$44,$4C,$45,$00,$00,$00,$05,$03,$04,$02,$60,$02,$60,$00,$07,$0E ; MIDDLE
                                                                                       ; deck row 1
  DEFB $00,$44,$45,$43,$4B,$00,$00,$00,$00,$05,$03,$04,$00,$02,$00,$02,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 2
  DEFB $00,$00,$00,$00,$00,$00,$05,$0B,$01,$12,$04,$00,$60,$02,$00,$02,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 3
  DEFB $00,$00,$00,$00,$00,$05,$03,$04,$00,$13,$01,$0F,$01,$14,$0F,$15,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 4
  DEFB $00,$00,$00,$00,$05,$03,$04,$00,$00,$02,$00,$00,$60,$00,$00,$10,$00,$00,$05,$11 ; MIDDLE
                                                                                       ; deck row 5
  DEFB $05,$0B,$01,$01,$12,$04,$00,$00,$18,$1A,$1B,$01,$01,$0C,$0F,$1C,$01,$01,$16,$17 ; MIDDLE
                                                                                       ; deck row 6
  DEFB $1D,$04,$00,$00,$02,$60,$00,$00,$02,$7C,$02,$00,$00,$02,$00,$02,$00,$00,$07,$0E ; MIDDLE
                                                                                       ; deck row 7
  DEFB $02,$00,$00,$00,$13,$0F,$01,$01,$15,$00,$02,$00,$00,$02,$60,$02,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 8
  DEFB $02,$60,$00,$00,$10,$00,$60,$7B,$10,$00,$10,$00,$60,$02,$00,$10,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row 9
  DEFB $02,$00,$00,$00,$13,$01,$01,$0F,$15,$60,$02,$00,$00,$02,$00,$02,$60,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 10
  DEFB $1E,$09,$00,$00,$02,$60,$00,$00,$02,$00,$02,$00,$00,$02,$00,$02,$00,$00,$05,$11 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 11
  DEFB $07,$1F,$01,$01,$21,$09,$00,$00,$19,$22,$86,$01,$01,$14,$0F,$1C,$01,$01,$16,$17 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 12
  DEFB $00,$00,$00,$00,$07,$08,$09,$00,$00,$02,$00,$00,$60,$00,$00,$10,$00,$00,$07,$0E ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 13
  DEFB $00,$00,$00,$00,$00,$07,$08,$09,$00,$13,$01,$0F,$01,$0C,$0F,$15,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 14
  DEFB $00,$00,$00,$00,$00,$00,$07,$1F,$01,$21,$09,$00,$60,$02,$00,$02,$00,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 15
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$07,$08,$09,$00,$02,$00,$02,$60,$00,$00,$02 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 16
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$07,$08,$09,$02,$60,$02,$00,$00,$05,$11 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 17
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$07,$1F,$14,$01,$14,$01,$01,$0A,$04 ; MIDDLE
                                                                                       ; deck row
                                                                                       ; 18
  DEFB $00,$00,$00,$00,$00,$00,$18,$01,$0C,$01,$01,$01,$26,$27,$27,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 0
  DEFB $00,$4C,$4F,$57,$45,$52,$02,$60,$02,$00,$00,$00,$2D,$2C,$2C,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 1
  DEFB $00,$44,$45,$43,$4B,$00,$02,$00,$02,$00,$00,$00,$2D,$2C,$2C,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 2
  DEFB $00,$00,$00,$00,$00,$00,$02,$00,$02,$00,$00,$00,$2D,$2C,$2C,$2B,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 3
  DEFB $00,$00,$00,$00,$00,$05,$11,$00,$02,$00,$00,$00,$2D,$2C,$2C,$2C,$2B,$00,$00,$00 ; LOWER deck
                                                                                       ; row 4
  DEFB $00,$00,$00,$00,$05,$03,$04,$00,$02,$60,$00,$00,$2D,$2C,$2C,$2C,$2C,$2B,$00,$00 ; LOWER deck
                                                                                       ; row 5
  DEFB $00,$00,$00,$00,$40,$5B,$0F,$01,$1C,$01,$0F,$01,$2E,$2C,$2C,$2C,$5F,$3B,$01,$24 ; LOWER deck
                                                                                       ; row 6
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$02,$7B,$00,$00,$2D,$2C,$2C,$2C,$3C,$60,$00,$02 ; LOWER deck
                                                                                       ; row 7
  DEFB $00,$00,$00,$00,$10,$00,$00,$00,$02,$00,$00,$00,$2F,$3B,$3B,$3B,$3F,$00,$00,$02 ; LOWER deck
                                                                                       ; row 8
  DEFB $00,$00,$00,$00,$10,$60,$00,$00,$10,$00,$00,$00,$10,$00,$00,$60,$10,$00,$00,$02 ; LOWER deck
                                                                                       ; row 9
  DEFB $00,$00,$00,$00,$10,$00,$00,$00,$02,$60,$00,$00,$89,$3D,$3D,$3D,$3E,$00,$00,$02 ; LOWER deck
                                                                                       ; row 10
  DEFB $00,$00,$00,$00,$02,$00,$00,$00,$02,$00,$00,$00,$2D,$2C,$2C,$2C,$3C,$00,$00,$02 ; LOWER deck
                                                                                       ; row 11
  DEFB $00,$00,$00,$00,$5C,$5D,$0F,$01,$1C,$01,$0F,$01,$2E,$2C,$2C,$2C,$5E,$3D,$01,$25 ; LOWER deck
                                                                                       ; row 12
  DEFB $00,$00,$00,$00,$07,$08,$09,$00,$02,$60,$00,$00,$2D,$2C,$2C,$2C,$2C,$2A,$00,$00 ; LOWER deck
                                                                                       ; row 13
  DEFB $00,$00,$00,$00,$00,$07,$0E,$60,$02,$00,$00,$00,$2D,$2C,$2C,$2C,$2A,$00,$00,$00 ; LOWER deck
                                                                                       ; row 14
  DEFB $00,$00,$00,$00,$00,$00,$13,$0F,$15,$00,$00,$00,$2D,$2C,$2C,$2A,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 15
  DEFB $00,$00,$00,$00,$00,$00,$02,$00,$02,$00,$00,$00,$2D,$2C,$2C,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 16
  DEFB $00,$00,$00,$00,$00,$00,$02,$60,$02,$00,$00,$00,$2D,$2C,$2C,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 17
  DEFB $00,$00,$00,$00,$00,$00,$19,$01,$14,$01,$01,$01,$29,$28,$28,$00,$00,$00,$00,$00 ; LOWER deck
                                                                                       ; row 18

; Room-interior layout template 0 (RLE-compressed)
;
; Selected by room type (RoomTypeByte) at PickRoomLayout (PickRoomLayout) and
; decompressed into the grid buffer at RoomGridBuffer by RoomLayoutExpand
; (RoomLayoutExpand): byte < 30 = that many zero cells, 30-254 = literal tile
; code, 255 = end.
RoomLayout0:
  DEFB $14,$14,$14,$06,$7D,$7E,$7E,$7D,$7E,$7E,$7E,$7E,$7D,$05,$06,$7F
  DEFB $02,$7F,$04,$85,$05,$06,$7F,$02,$7F,$04,$85,$05,$06,$7F,$02,$7F
  DEFB $03,$80,$82,$05,$06,$7F,$02,$7F,$03,$7F,$06,$06,$7F,$02,$7F,$03
  DEFB $7F,$06,$06,$7F,$02,$7D,$7E,$7E,$7E,$7D,$7E,$7E,$7E,$7D,$83,$01
  DEFB $06,$7F,$02,$7F,$03,$7F,$04,$85,$01,$06,$7F,$02,$7F,$03,$7F,$04
  DEFB $8F,$01,$06,$7F,$02,$7F,$03,$81,$83,$05,$06,$7F,$02,$7F,$04,$85
  DEFB $05,$06,$7F,$02,$7F,$04,$85,$05,$06,$7D,$7E,$7E,$7D,$7E,$7E,$7E
  DEFB $7E,$7D,$05,$14,$14,$14,$FF

; Room-interior layout template 1 (RLE-compressed)
;
; Selected by room type (RoomTypeByte) at PickRoomLayout (PickRoomLayout) and
; decompressed into the grid buffer at RoomGridBuffer by RoomLayoutExpand
; (RoomLayoutExpand): byte < 30 = that many zero cells, 30-254 = literal tile
; code, 255 = end.
RoomLayout1:
  DEFB $14,$14,$09,$80,$7E,$7E,$7D,$7E,$7E,$83,$7D,$7E,$83,$01,$09,$7D
  DEFB $7E,$83,$03,$85,$02,$85,$01,$0B,$85,$03,$85,$02,$85,$01,$0B,$85
  DEFB $03,$85,$02,$85,$01,$05,$7D,$7E,$7E,$7E,$7E,$7E,$7D,$03,$85,$02
  DEFB $85,$01,$05,$7F,$02,$80,$7E,$7E,$82,$03,$85,$02,$85,$8E,$05,$7F
  DEFB $02,$7F,$06,$85,$02,$85,$85,$7D,$7E,$7E,$7E,$7E,$7D,$02,$7D,$02
  DEFB $7D,$7E,$7E,$7E,$7D,$7E,$7E,$7D,$82,$05,$7F,$02,$7F,$06,$85,$02
  DEFB $85,$01,$05,$7F,$02,$81,$7E,$7E,$83,$03,$85,$02,$85,$01,$05,$7D
  DEFB $7E,$7E,$7E,$7E,$7E,$7D,$03,$85,$02,$85,$01,$0B,$85,$03,$85,$02
  DEFB $85,$01,$0B,$85,$03,$85,$02,$85,$01,$09,$7D,$84,$82,$03,$85,$02
  DEFB $85,$01,$09,$81,$84,$84,$7D,$84,$84,$82,$7D,$84,$82,$01,$14,$14
  DEFB $FF

; Room-interior layout template 2 (RLE-compressed)
;
; Selected by room type (RoomTypeByte) at PickRoomLayout (PickRoomLayout) and
; decompressed into the grid buffer at RoomGridBuffer by RoomLayoutExpand
; (RoomLayoutExpand): byte < 30 = that many zero cells, 30-254 = literal tile
; code, 255 = end.
RoomLayout2:
  DEFB $14,$14,$01,$80,$7E,$7E,$7E,$7D,$7E,$7E,$7E,$83,$0A,$01,$7F,$07
  DEFB $85,$0A,$01,$7F,$07,$7D,$7E,$7E,$7E,$7E,$83,$05,$01,$7F,$0C,$85
  DEFB $01,$8E,$03,$01,$7F,$0C,$85,$01,$7F,$03,$01,$7F,$0C,$85,$01,$81
  DEFB $84,$7D,$01,$01,$7F,$0C,$85,$03,$85,$01,$01,$7D,$07,$7D,$84,$84
  DEFB $84,$84,$7D,$84,$84,$84,$82,$01,$01,$7F,$0C,$85,$05,$01,$7F,$0C
  DEFB $85,$05,$01,$7F,$0C,$85,$05,$01,$7F,$0C,$85,$05,$01,$81,$84,$84
  DEFB $84,$7D,$03,$7D,$84,$84,$84,$84,$82,$05,$05,$7F,$03,$85,$0A,$05
  DEFB $7F,$03,$85,$0A,$05,$7D,$84,$84,$84,$82,$0A,$14,$FF

; Room grid buffer — 19 rows x 20 columns
;
; Working buffer holding the decompressed cell grid of the current room view
; (or corridor view, decompressed from NarcissusLayout). Blitted to the screen
; by DrawRoomBackground (DrawRoomBackground); the door drawer walks it via
; RoomCellCursor (RoomCellCursor). The bytes below are load-image leftovers.
RoomGridBuffer:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00

; Per-room view origin table — 34 rooms x (row, column)
;
; Read by SetRoomOrigin (SetRoomOrigin): for the current room, byte 0 = grid
; row and byte 1 = column where exit/door drawing starts; converted to
; RoomCellCursor (RoomGridBuffer + 20*row + col) and RoomAttrCursor (attr file
; + 32*row + col). Narcissus has no entry.
RoomOriginTable:
  DEFB $03,$09            ; 0 Airlock #1
  DEFB $0F,$09            ; 1 Airlock #2
  DEFB $02,$0C            ; 2 Armoury
  DEFB $04,$09            ; 3 Cargopod#1
  DEFB $09,$09            ; 4 Cargopod#2
  DEFB $0E,$09            ; 5 Cargopod#3
  DEFB $09,$00            ; 6 CommdCentr
  DEFB $09,$11            ; 7 Computer
  DEFB $09,$05            ; 8 Corridor#1
  DEFB $09,$08            ; 9 Corridor#2
  DEFB $06,$0B            ; 10 Corridor#3
  DEFB $09,$0F            ; 11 Corridor#4
  DEFB $0C,$0B            ; 12 Corridor#5
  DEFB $09,$0D            ; 13 Corridor#6
  DEFB $09,$0E            ; 14 Corridor#7
  DEFB $09,$0B            ; 15 Cryo Vault
  DEFB $0E,$05            ; 16 Engineerng
  DEFB $02,$10            ; 17 Engine #1
  DEFB $09,$12            ; 18 Engine #2
  DEFB $10,$10            ; 19 Engine #3
  DEFB $11,$05            ; 20 Eng Stores
  DEFB $06,$05            ; 21 Infirmary
  DEFB $03,$09            ; 22 Inf Stores
  DEFB $0C,$05            ; 23 Laboratory
  DEFB $0F,$09            ; 24 Lab Stores
  DEFB $07,$12            ; 25 Life Suppt
  DEFB $03,$06            ; 26 Livng Qtrs
  DEFB $09,$09            ; 27 Mess
  DEFB $0F,$06            ; 28 RecrtnArea
  DEFB $10,$0C            ; 29 Stores #1
  DEFB $03,$0E            ; 30 Stores #2
  DEFB $0F,$0E            ; 31 Stores #3
  DEFB $09,$01            ; 32 ShuttleBay
  DEFB $02,$05            ; 33 Shttlstore

; Room marker screen-address table
;
; One word per room (0-33): the display-file address where the room's animated
; marker (24x12 pixels) is XOR-blitted on that room's deck map. Read by
; GetRoomMarkerAddr (GetRoomMarkerAddr), which feeds both marker channels: A —
; the walking crew figure (CrewWalkA1/A2) on the room being viewed, placed by
; PlaceRoomMarkerA (PlaceRoomMarkerA) — and B — the blinking box cursor
; (RoomCursor0-3) on a candidate room: an exit row under the cursor in the room
; view, or a room picked from the Indicate Location list, placed by
; PlaceRoomMarkerB (PlaceRoomMarkerB). Rooms on different decks may share an
; address (Cargopod#2/Mess); the Narcissus (34) has no entry.
RoomMarkerAddrTable:
  DEFW $408A              ; 0 Airlock #1
  DEFW $48AA              ; 1 Airlock #2
  DEFW $404D              ; 2 Armoury
  DEFW $4049              ; 3 Cargopod#1
  DEFW $4809              ; 4 Cargopod#2
  DEFW $48C9              ; 5 Cargopod#3
  DEFW $4801              ; 6 CommdCentr
  DEFW $482F              ; 7 Computer
  DEFW $4E04              ; 8 Corridor#1
  DEFW $4908              ; 9 Corridor#2
  DEFW $468D              ; 10 Corridor#3
  DEFW $484D              ; 11 Corridor#4
  DEFW $4E8D              ; 12 Corridor#5
  DEFW $482D              ; 13 Corridor#6
  DEFW $4E0D              ; 14 Corridor#7
  DEFW $482A              ; 15 Cryo Vault
  DEFW $4E86              ; 16 Engineerng
  DEFW $4070              ; 17 Engine #1
  DEFW $4810              ; 18 Engine #2
  DEFW $48D1              ; 19 Engine #3
  DEFW $4EE6              ; 20 Eng Stores
  DEFW $40A6              ; 21 Infirmary
  DEFW $402B              ; 22 Inf Stores
  DEFW $4885              ; 23 Laboratory
  DEFW $500B              ; 24 Lab Stores
  DEFW $4831              ; 25 Life Suppt
  DEFW $40C5              ; 26 Livng Qtrs
  DEFW $4809              ; 27 Mess
  DEFW $4845              ; 28 RecrtnArea
  DEFW $48ED              ; 29 Stores #1
  DEFW $408F              ; 30 Stores #2
  DEFW $48AF              ; 31 Stores #3
  DEFW $4826              ; 32 ShuttleBay
  DEFW $4086              ; 33 Shttlstore

; RoomCursor0
;
; Room indicator cursor, phase 0 (used by AnimateCrewB AnimateCrewB, phase 0).
; XOR-blit sprite: 24px wide x 12px tall (3 bytes per row). Largest box — wide
; outer border with a small inner rectangle. This cursor blinks over the
; current room on the corridor map.
RoomCursor0:
  DEFB $03,$FF,$C0        ; row  0: top border
  DEFB $02,$00,$40        ; row  1
  DEFB $02,$00,$40        ; row  2
  DEFB $02,$00,$40        ; row  3
  DEFB $02,$3C,$40        ; row  4: inner box top
  DEFB $02,$24,$40        ; row  5: inner box sides
  DEFB $02,$24,$40        ; row  6: inner box sides
  DEFB $02,$3C,$40        ; row  7: inner box bottom
  DEFB $02,$00,$40        ; row  8
  DEFB $02,$00,$40        ; row  9
  DEFB $02,$00,$40        ; row 10
  DEFB $03,$FF,$C0        ; row 11: bottom border

; RoomCursor1
;
; Room indicator cursor, phase 1 (AnimateCrewB phase 1). Narrower 1-pixel
; border with an oval interior — cursor drawn at reduced size.
RoomCursor1:
  DEFB $00,$00,$00        ; row  0: blank
  DEFB $01,$FF,$80        ; row  1: top border
  DEFB $01,$00,$80        ; row  2
  DEFB $01,$7E,$80        ; row  3: oval top
  DEFB $01,$42,$80        ; row  4: oval sides
  DEFB $01,$42,$80        ; row  5
  DEFB $01,$42,$80        ; row  6
  DEFB $01,$42,$80        ; row  7
  DEFB $01,$7E,$80        ; row  8
  DEFB $01,$00,$80        ; row  9
  DEFB $01,$FF,$80        ; row 10
  DEFB $00,$00,$00        ; row 11: blank

; RoomCursor2
;
; Room indicator cursor, phase 2 (AnimateCrewB phase 2). Centre-only pattern —
; horizontal bars with inner ring detail.
RoomCursor2:
  DEFB $00,$00,$00        ; row  0: blank
  DEFB $00,$00,$00        ; row  1: blank
  DEFB $00,$FF,$00        ; row  2: top bar
  DEFB $00,$81,$00        ; row  3: outer edge
  DEFB $00,$BD,$00        ; row  4: inner ring
  DEFB $00,$A5,$00        ; row  5: eye sockets
  DEFB $00,$A5,$00        ; row  6
  DEFB $00,$BD,$00        ; row  7
  DEFB $00,$81,$00        ; row  8
  DEFB $00,$FF,$00        ; row  9: bottom bar
  DEFB $00,$00,$00        ; row 10: blank
  DEFB $00,$00,$00        ; row 11: blank

; RoomCursor3
;
; Room indicator cursor, phase 3 (AnimateCrewB phase 3). Smallest oval shape —
; cursor at its most contracted phase.
RoomCursor3:
  DEFB $00,$00,$00        ; row  0: blank
  DEFB $00,$00,$00        ; row  1: blank
  DEFB $00,$00,$00        ; row  2: blank
  DEFB $00,$7E,$00        ; row  3: head oval
  DEFB $00,$42,$00        ; row  4
  DEFB $00,$5A,$00        ; row  5: inner detail
  DEFB $00,$5A,$00        ; row  6
  DEFB $00,$42,$00        ; row  7
  DEFB $00,$7E,$00        ; row  8: head oval
  DEFB $00,$00,$00        ; row  9: blank
  DEFB $00,$00,$00        ; row 10: blank
  DEFB $00,$00,$00        ; row 11: blank

; CrewWalkA1
;
; Crew member walking sprite, phase 1 (AnimateCrewA AnimateCrewA, phases 0-1).
; XOR-blit sprite: 24px wide x 12px tall (3 bytes per row). Upright
; standing/walking pose — arms close to body, feet together. Left and right
; bytes are zero (figure fits in the 8-pixel centre column).
CrewWalkA1:
  DEFB $00,$00,$00        ; row  0: blank
  DEFB $00,$08,$00        ; row  1: head top
  DEFB $00,$1C,$00        ; row  2: head
  DEFB $00,$08,$00        ; row  3: chin
  DEFB $00,$3E,$00        ; row  4: shoulders
  DEFB $00,$5D,$00        ; row  5: torso
  DEFB $00,$3E,$00        ; row  6: body
  DEFB $00,$1C,$00        ; row  7: hips
  DEFB $00,$14,$00        ; row  8: upper legs
  DEFB $00,$14,$00        ; row  9: mid legs
  DEFB $00,$14,$00        ; row 10: lower legs
  DEFB $00,$36,$00        ; row 11: feet

; CrewWalkA2
;
; Crew member walking sprite, phase 2 (AnimateCrewA AnimateCrewA, phases 2-3).
; Mid-stride pose — arms pumping outward, legs spread in step. Row 5 has a
; stray pixel in the right byte (arm extension).
CrewWalkA2:
  DEFB $00,$08,$00        ; row  0: head
  DEFB $00,$14,$00        ; row  1: head wide
  DEFB $00,$22,$00        ; row  2: arms raising
  DEFB $00,$36,$00        ; row  3: upper arms
  DEFB $00,$41,$00        ; row  4: arms spread
  DEFB $00,$A2,$80        ; row  5: torso + arm extension
  DEFB $00,$41,$00        ; row  6
  DEFB $00,$22,$00        ; row  7: arms return
  DEFB $00,$2A,$00        ; row  8: legs mid-stride
  DEFB $00,$2A,$00        ; row  9
  DEFB $00,$6B,$00        ; row 10: feet contact
  DEFB $00,$49,$00        ; row 11: feet split

; AnimCrewAFrames
;
; 4 × 16-bit pointers to crew animation frame-A sprite bitmaps. Indexed by
; frame phase (0-3); phases 0-1 share one bitmap, 2-3 share another, producing
; a two-frame walk cycle at rate 10 (10 frames/step). Used by AnimateCrewA
; (AnimateCrewA) to XOR-blit crew sprites onto the screen.
CrewWalkAnimA:
  DEFW CrewWalkA1,CrewWalkA1,CrewWalkA2,CrewWalkA2

; AnimCrewBFrames
;
; 4 × 16-bit pointers to room indicator cursor bitmaps (faster channel, rate
; 5). Used by AnimateCrewB (AnimateCrewB) to XOR-blit the animated room cursor
; that highlights the current room on the corridor map.
CrewWalkAnimB:
  DEFW RoomCursor0,RoomCursor1,RoomCursor2,RoomCursor3

; CharTileBitmaps
;
; Master 8×8 pixel tileset used by DrawSpriteRow (DrawSpriteRow) for all
; corridor and room graphics. 169 tiles × 8 bytes each (one byte per pixel
; row). The first 128 tiles are arranged so that tile N is the glyph for ASCII
; code N, which lets DrawSprite (DrawSprite) render the 10-character text
; labels stored in RoomNameTable (RoomNameTable / UIStringTable) by treating
; each ASCII byte as a tile index. Tiles 128+ are non-text graphics (sprites,
; arrows, etc.). Tile N starts at CharBitmaps + N×8.
;
; Sprite sheet — 16 tiles per row, 2× scale:
CharBitmaps:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$FF,$00,$00,$FF,$00,$00
  DEFB $24,$24,$24,$24,$24,$24,$24,$24
  DEFB $02,$04,$08,$11,$22,$44,$88,$10
  DEFB $20,$40,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$01
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $01,$00,$00,$00,$00,$00,$00,$00
  DEFB $10,$88,$44,$22,$11,$08,$04,$02
  DEFB $00,$00,$00,$00,$00,$80,$40,$20
  DEFB $02,$04,$F8,$01,$02,$FC,$00,$00
  DEFB $00,$00,$0F,$10,$20,$47,$88,$10
  DEFB $00,$00,$FF,$00,$00,$E7,$24,$24
  DEFB $00,$00,$FC,$02,$01,$F8,$04,$02
  DEFB $10,$88,$44,$24,$24,$24,$24,$24
  DEFB $00,$00,$00,$FF,$FF,$00,$00,$00
  DEFB $18,$18,$18,$18,$18,$18,$18,$18
  DEFB $24,$24,$24,$24,$24,$44,$88,$10
  DEFB $02,$04,$F8,$01,$02,$E4,$24,$24
  DEFB $24,$24,$27,$20,$20,$27,$24,$24
  DEFB $24,$24,$E7,$00,$00,$FF,$00,$00
  DEFB $24,$24,$E4,$04,$04,$E4,$24,$24
  DEFB $02,$04,$F8,$01,$01,$F8,$04,$02
  DEFB $20,$40,$80,$00,$00,$80,$40,$20
  DEFB $00,$00,$3F,$20,$20,$27,$24,$24
  DEFB $24,$24,$27,$20,$20,$3F,$00,$00
  DEFB $24,$24,$E4,$07,$07,$F8,$00,$00
  DEFB $00,$00,$1F,$E0,$E0,$27,$24,$24
  DEFB $24,$24,$E7,$00,$00,$E7,$24,$24
  DEFB $02,$04,$08,$11,$22,$24,$24,$24
  DEFB $24,$24,$24,$22,$11,$08,$04,$02
  DEFB $10,$88,$47,$20,$10,$0F,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $24,$24,$E4,$02,$01,$F8,$04,$02
  DEFB $00,$00,$F8,$07,$07,$E4,$24,$24
  DEFB $00,$44,$FE,$44,$44,$FE,$44,$00
  DEFB $00,$00,$FC,$04,$04,$E4,$24,$24
  DEFB $24,$24,$E4,$04,$04,$FC,$00,$00
  DEFB $00,$00,$FF,$07,$07,$E7,$27,$27
  DEFB $00,$00,$FF,$FF,$FF,$FF,$FF,$FF
  DEFB $FF,$FF,$FF,$FF,$FF,$FF,$00,$00
  DEFB $27,$27,$E7,$07,$07,$FF,$00,$00
  DEFB $FF,$FE,$FC,$F8,$F0,$E0,$C0,$80
  DEFB $80,$C0,$E0,$F0,$F8,$FC,$FE,$FF
  DEFB $FF,$FF,$FF,$FF,$FF,$FF,$FF,$FF
  DEFB $27,$27,$27,$27,$27,$27,$27,$27
  DEFB $27,$27,$E7,$07,$07,$E7,$27,$27
  DEFB $27,$27,$27,$20,$20,$27,$24,$24
  DEFB $7E,$46,$42,$42,$42,$42,$7E,$00
  DEFB $10,$10,$10,$18,$18,$18,$18,$00
  DEFB $7E,$06,$06,$06,$7E,$40,$7E,$00
  DEFB $7E,$06,$06,$06,$7E,$06,$7E,$00
  DEFB $40,$40,$40,$58,$7E,$18,$18,$00
  DEFB $7E,$40,$40,$40,$7E,$06,$7E,$00
  DEFB $40,$40,$40,$40,$7E,$46,$7E,$00
  DEFB $7E,$02,$02,$06,$06,$06,$06,$00
  DEFB $7E,$46,$46,$46,$7E,$46,$7E,$00
  DEFB $7E,$46,$46,$46,$7E,$06,$06,$00
  DEFB $00,$60,$60,$00,$00,$60,$60,$00
  DEFB $FF,$FF,$FF,$00,$00,$FF,$00,$00
  DEFB $E4,$E4,$E4,$E4,$E4,$E4,$E4,$E4
  DEFB $00,$00,$FF,$00,$00,$FF,$FF,$FF
  DEFB $24,$24,$E4,$04,$04,$E4,$E4,$E4
  DEFB $E4,$E4,$E4,$04,$04,$E4,$24,$24
  DEFB $02,$04,$08,$10,$20,$27,$24,$24
  DEFB $38,$44,$44,$7C,$44,$44,$EE,$00
  DEFB $FC,$42,$42,$7C,$42,$42,$FC,$00
  DEFB $3A,$46,$82,$80,$80,$42,$3C,$00
  DEFB $F8,$44,$42,$42,$42,$44,$F8,$00
  DEFB $7F,$21,$24,$3C,$24,$21,$7F,$00
  DEFB $7F,$21,$24,$3C,$24,$20,$70,$00
  DEFB $3A,$46,$82,$90,$9E,$42,$3C,$00
  DEFB $EE,$44,$44,$7C,$44,$44,$EE,$00
  DEFB $FE,$92,$10,$10,$10,$92,$FE,$00
  DEFB $FE,$92,$10,$10,$10,$90,$60,$00
  DEFB $C6,$48,$50,$68,$44,$44,$C6,$00
  DEFB $E0,$40,$40,$40,$40,$42,$FE,$00
  DEFB $44,$6C,$54,$54,$44,$44,$EE,$00
  DEFB $44,$64,$54,$54,$4C,$44,$EE,$00
  DEFB $38,$44,$82,$82,$82,$44,$38,$00
  DEFB $FC,$42,$42,$7C,$40,$40,$E0,$00
  DEFB $38,$44,$82,$82,$8A,$44,$3A,$00
  DEFB $FC,$42,$42,$7C,$44,$42,$E2,$00
  DEFB $7C,$82,$80,$7C,$02,$82,$7C,$00
  DEFB $FE,$92,$10,$10,$10,$10,$38,$00
  DEFB $82,$82,$82,$82,$82,$44,$38,$00
  DEFB $82,$82,$44,$44,$28,$28,$10,$00
  DEFB $92,$92,$92,$92,$BA,$C6,$82,$00
  DEFB $C6,$44,$28,$10,$28,$44,$C6,$00
  DEFB $82,$44,$28,$10,$10,$10,$38,$00
  DEFB $FE,$82,$0C,$10,$60,$82,$FE,$00
  DEFB $20,$40,$FF,$00,$00,$FF,$00,$00
  DEFB $24,$24,$27,$20,$10,$08,$04,$02
  DEFB $00,$00,$FF,$00,$00,$FF,$40,$20
  DEFB $E4,$E4,$E7,$E0,$E0,$FF,$FF,$FF
  DEFB $FF,$FF,$FF,$E0,$E0,$E7,$E4,$E4
  DEFB $00,$7E,$56,$6A,$56,$6A,$7E,$00
  DEFB $00,$7A,$84,$84,$84,$7A,$00,$00
  DEFB $40,$7C,$42,$42,$42,$FC,$00,$00
  DEFB $00,$7C,$82,$80,$82,$7C,$00,$00
  DEFB $04,$7C,$84,$84,$84,$7E,$00,$00
  DEFB $00,$7C,$82,$FE,$80,$7C,$00,$00
  DEFB $38,$40,$40,$78,$40,$40,$40,$00
  DEFB $00,$7A,$84,$84,$84,$7C,$04,$78
  DEFB $40,$78,$44,$44,$44,$EE,$00,$00
  DEFB $10,$00,$10,$10,$10,$38,$00,$00
  DEFB $10,$00,$10,$10,$10,$10,$90,$60
  DEFB $C0,$46,$58,$60,$68,$C6,$00,$00
  DEFB $30,$10,$10,$10,$10,$38,$00,$00
  DEFB $00,$7C,$92,$92,$92,$92,$00,$00
  DEFB $00,$F8,$84,$84,$84,$84,$00,$00
  DEFB $00,$7C,$82,$82,$82,$7C,$00,$00
  DEFB $00,$F8,$84,$84,$84,$F8,$80,$80
  DEFB $00,$7C,$84,$84,$84,$7C,$04,$06
  DEFB $00,$B8,$44,$40,$40,$E0,$00,$00
  DEFB $00,$7C,$80,$7C,$02,$7C,$00,$00
  DEFB $10,$10,$7C,$10,$12,$1C,$00,$00
  DEFB $00,$84,$84,$84,$84,$78,$00,$00
  DEFB $00,$82,$44,$44,$28,$10,$00,$00
  DEFB $00,$92,$92,$92,$92,$7C,$00,$00
  DEFB $00,$C6,$28,$10,$28,$C6,$00,$00
  DEFB $00,$84,$84,$84,$84,$7C,$04,$78
  DEFB $00,$FE,$86,$18,$62,$FE,$00,$00
  DEFB $3C,$24,$3C,$24,$3C,$24,$3C,$24
  DEFB $00,$7E,$66,$5A,$5A,$66,$7E,$00
  DEFB $FF,$CB,$D5,$CB,$D5,$CB,$D5,$FF
  DEFB $00,$F7,$00,$F7,$F7,$F7,$EF,$00
  DEFB $5E,$5E,$5E,$42,$1C,$5E,$5E,$5E
  DEFB $00,$0F,$38,$37,$69,$5E,$5E,$5E
  DEFB $5E,$5E,$5E,$69,$37,$38,$0F,$00
  DEFB $7A,$7A,$7A,$96,$EC,$1C,$F0,$00
  DEFB $00,$F0,$1C,$EC,$96,$7A,$7A,$7A
  DEFB $00,$EF,$F7,$F7,$F7,$00,$F7,$00
  DEFB $7A,$7A,$7A,$42,$38,$7A,$7A,$7A
  DEFB $24,$24,$27,$E0,$E0,$1F,$00,$00
  DEFB $01,$32,$34,$08,$10,$2C,$4C,$80
  DEFB $30,$48,$48,$48,$32,$54,$88,$76
  DEFB $24,$24,$27,$20,$20,$27,$27,$27
  DEFB $08,$1C,$3E,$08,$08,$08,$08,$00
  DEFB $00,$04,$06,$7F,$06,$04,$00,$00
  DEFB $08,$08,$08,$08,$3E,$1C,$08,$00
  DEFB $00,$20,$60,$FE,$60,$20,$00,$00
  DEFB $18,$24,$42,$42,$66,$7E,$5E,$5E
  DEFB $7E,$7E,$7E,$7E,$7E,$7E,$3C,$18
  DEFB $00,$00,$00,$00,$00,$10,$30,$00
  DEFB $08,$08,$10,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$03,$0F,$3F,$FF
  DEFB $FC,$F0,$C0,$00,$00,$00,$00,$00
  DEFB $3F,$0F,$03,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$C0,$F0,$FC,$FF
  DEFB $80,$80,$80,$80,$80,$80,$80,$80
  DEFB $01,$01,$01,$01,$01,$01,$01,$01
  DEFB $00,$00,$00,$00,$00,$00,$00,$FF
  DEFB $80,$80,$C0,$C0,$E0,$E0,$F0,$F0
  DEFB $01,$01,$03,$03,$07,$07,$0F,$0F
  DEFB $07,$07,$03,$03,$01,$01,$00,$FF
  DEFB $E0,$E0,$C0,$C0,$80,$80,$00,$FF
  DEFB $FF,$FF,$FF,$00,$00,$00,$00,$FF
  DEFB $00,$00,$00,$66,$66,$00,$00,$00
  DEFB $00,$00,$00,$18,$18,$00,$00,$00
  DEFB $1F,$1F,$1F,$1F,$1F,$1F,$1F,$1F
  DEFB $80,$80,$C0,$E0,$E0,$F0,$F8,$F8
  DEFB $07,$03,$01,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$80,$80,$C0,$E0
  DEFB $1F,$0F,$07,$07,$03,$03,$01,$00
  DEFB $01,$01,$03,$07,$07,$0F,$1F,$1F
  DEFB $C0,$80,$80,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$01,$01,$03,$07
  DEFB $FC,$FC,$F0,$F0,$E0,$E0,$C0,$C0

; RoomNameTable / UIStringTable
;
; 95-entry × 10-byte text table. DrawSprite (DrawSprite) reads 10 bytes
; starting at (RoomNameTable + N×10) and renders them as a 10-character label
; by blitting the CharBitmaps (CharBitmaps) glyph for each byte — i.e. the
; ASCII text *is* the "sprite data". Indices 0–46 are RoomNameTable (room /
; item / deck names); indices 47–94 are UIStringTable (UI / menu / action
; labels).
;
; Entries 57–60 embed the four cursor-arrow UDG codes ($8A–$8D) inside the
; otherwise-ASCII text.
RoomNameTable:
  DEFM "Airlock #1"       ; 0
  DEFM "Airlock #2"       ; 1
  DEFM "Armoury   "       ; 2
  DEFM "Cargopod#1"       ; 3
  DEFM "Cargopod#2"       ; 4
  DEFM "Cargopod#3"       ; 5
  DEFM "CommdCentr"       ; 6
  DEFM "Computer  "       ; 7
  DEFM "Corridor#1"       ; 8
  DEFM "Corridor#2"       ; 9
  DEFM "Corridor#3"       ; 10
  DEFM "Corridor#4"       ; 11
  DEFM "Corridor#5"       ; 12
  DEFM "Corridor#6"       ; 13
  DEFM "Corridor#7"       ; 14
  DEFM "Cryo Vault"       ; 15
  DEFM "Engineerng"       ; 16
  DEFM "Engine #1 "       ; 17
  DEFM "Engine #2 "       ; 18
  DEFM "Engine #3 "       ; 19
  DEFM "Eng Stores"       ; 20
  DEFM "Infirmary "       ; 21
  DEFM "Inf Stores"       ; 22
  DEFM "Laboratory"       ; 23
  DEFM "Lab Stores"       ; 24
  DEFM "Life Suppt"       ; 25
  DEFM "Livng Qtrs"       ; 26
  DEFM "Mess      "       ; 27
  DEFM "RecrtnArea"       ; 28
  DEFM "Stores #1 "       ; 29
  DEFM "Stores #2 "       ; 30
  DEFM "Stores #3 "       ; 31
  DEFM "ShuttleBay"       ; 32
  DEFM "Shttlstore"       ; 33
  DEFM "Narcissus "       ; 34
  DEFM "Elctrc Prd"       ; 35
  DEFM "Incineratr"       ; 36
  DEFM "Tracker   "       ; 37
  DEFM "Fire Extng"       ; 38
  DEFM "Harpn Gun "       ; 39
  DEFM "Laser Pist"       ; 40
  DEFM "Net       "       ; 41
  DEFM "Cat Box   "       ; 42
  DEFM "Spanner   "       ; 43
  DEFM "Upper Deck"       ; 44
  DEFM "MiddleDeck"       ; 45
  DEFM "Lower Deck"       ; 46
UIStringTable:
  DEFM "Get Item  "       ; 47
  DEFM "Leave Item"       ; 48
  DEFM "RmveGrille"       ; 49
  DEFM "Grille    "       ; 50
  DEFM "Get Jones "       ; 51
  DEFM "  ATTACK  "       ; 52
  DEFM "BlowLock#1"       ; 53
  DEFM "BlowLock#2"       ; 54
  DEFM "SealLock#1"       ; 55
  DEFM "SealLock#2"       ; 56
  DEFM "North ",$8A,"   " ; 57 (138 = up arrow UDG)
  DEFM "East  ",$8B,"   " ; 58 (139 = right arrow UDG)
  DEFM "South ",$8C,"   " ; 59 (140 = down arrow UDG)
  DEFM "West  ",$8D,"   " ; 60 (141 = left arrow UDG)
  DEFM "   QUIT   "       ; 61
  DEFM "Other List"       ; 62
  DEFM "AGF:PROTEK"       ; 63 joystick types
  DEFM " KEMPSTON "       ; 64
  DEFM " KEYBOARD "       ; 65
  DEFM " SINCLAIR "       ; 66
  DEFM "  FULLER  "       ; 67
  DEFM "Cat in Net"       ; 68
  DEFM "Cat in Box"       ; 69
  DEFM " Scuttle  "       ; 70
  DEFM " Nostromo "       ; 71
  DEFM " Override "       ; 72
  DEFM " Destruct "       ; 73
  DEFM "  Enter   "       ; 74
  DEFM "Hypersleep"       ; 75
  DEFM " Fight    "       ; 76
  DEFM "  Fire    "       ; 77
  DEFM "  Launch  "       ; 78
  DEFM " Indicate "       ; 79
  DEFM " Location "       ; 80
  DEFM "Narcissus "       ; 81
  DEFM " CONTROL  "       ; 82
  DEFM "Order:    "       ; 83
  DEFM " Display  "       ; 84
  DEFM "  Level   "       ; 85
  DEFM "move to:  "       ; 86
  DEFM " use:     "       ; 87
  DEFM "Special   "       ; 88
  DEFM "Damage    "       ; 89
  DEFM " in place "       ; 90
  DEFM " removed  "       ; 91
  DEFM "          "       ; 92 (blank)
  DEFM "In Duct   "       ; 93
  DEFM "Body of   "       ; 94

; Room adjacency map — corridor/door network
;
; The Nostromo's connectivity, as two 5-byte-per-room tables (this one and the
; air-duct network at RoomAdjDucts). Room IDs 0-34 index the room-name table at
; RoomNameTable. Each room has 5 direction slots holding the destination room
; reached by leaving in that direction; a slot equal to the room's own ID means
; "no exit that way". The graph is fully symmetric (every door works both
; ways).
;
; A direction slot is chosen by ScriptNibbleToDirection
; (ScriptNibbleToDirection) from the ROM script stream, so AI wandering is a
; deterministic weighted random walk. Read by the destination picker at
; PickNextRoom (which writes an actor's record byte +2), by the alien's own
; per-move room step inside UpdateJones, and by the door-exit-list builder
; inside DrawSprite that feeds the room view.
RoomAdjCorridors:
  DEFB $0D,$0D,$00,$00,$00 ; 0 Airlock #1 -> 13 Corridor#6
  DEFB $0D,$0D,$01,$01,$01 ; 1 Airlock #2 -> 13 Corridor#6
  DEFB $0A,$0A,$0A,$02,$02 ; 2 Armoury    -> 10 Corridor#3
  DEFB $04,$04,$03,$03,$03 ; 3 Cargopod#1 -> 4 Cargopod#2
  DEFB $20,$03,$0E,$05,$09 ; 4 Cargopod#2 -> 3 Cargopod#1, 5 Cargopod#3, 9
                           ; Corridor#2, 14 Corridor#7, 32 ShuttleBay
  DEFB $04,$04,$05,$05,$05 ; 5 Cargopod#3 -> 4 Cargopod#2
  DEFB $08,$08,$08,$06,$06 ; 6 CommdCentr -> 8 Corridor#1
  DEFB $0D,$0D,$1E,$1E,$07 ; 7 Computer   -> 13 Corridor#6, 30 Stores #2
  DEFB $06,$15,$09,$17,$1A ; 8 Corridor#1 -> 6 CommdCentr, 9 Corridor#2, 21
                           ; Infirmary, 23 Laboratory, 26 Livng Qtrs
  DEFB $08,$04,$0A,$0F,$0C ; 9 Corridor#2 -> 4 Cargopod#2, 8 Corridor#1, 10
                           ; Corridor#3, 12 Corridor#5, 15 Cryo Vault
  DEFB $09,$16,$02,$11,$0B ; 10 Corridor#3 -> 2 Armoury, 9 Corridor#2, 11
                           ; Corridor#4, 17 Engine #1, 22 Inf Stores
  DEFB $0A,$0A,$0C,$0C,$12 ; 11 Corridor#4 -> 10 Corridor#3, 12 Corridor#5, 18
                           ; Engine #2
  DEFB $09,$18,$1D,$13,$0B ; 12 Corridor#5 -> 9 Corridor#2, 11 Corridor#4, 19
                           ; Engine #3, 24 Lab Stores, 29 Stores #1
  DEFB $1B,$00,$07,$1F,$01 ; 13 Corridor#6 -> 0 Airlock #1, 1 Airlock #2, 7
                           ; Computer, 27 Mess, 31 Stores #3
  DEFB $04,$04,$19,$19,$0E ; 14 Corridor#7 -> 4 Cargopod#2, 25 Life Suppt
  DEFB $09,$09,$0F,$0F,$0F ; 15 Cryo Vault -> 9 Corridor#2
  DEFB $20,$20,$14,$14,$10 ; 16 Engineerng -> 20 Eng Stores, 32 ShuttleBay
  DEFB $0A,$0A,$11,$11,$11 ; 17 Engine #1  -> 10 Corridor#3
  DEFB $0B,$0B,$12,$12,$12 ; 18 Engine #2  -> 11 Corridor#4
  DEFB $0C,$0C,$13,$13,$13 ; 19 Engine #3  -> 12 Corridor#5
  DEFB $10,$10,$10,$14,$14 ; 20 Eng Stores -> 16 Engineerng
  DEFB $08,$08,$08,$15,$15 ; 21 Infirmary  -> 8 Corridor#1
  DEFB $0A,$0A,$0A,$16,$16 ; 22 Inf Stores -> 10 Corridor#3
  DEFB $08,$08,$08,$17,$17 ; 23 Laboratory -> 8 Corridor#1
  DEFB $0C,$0C,$0C,$18,$18 ; 24 Lab Stores -> 12 Corridor#5
  DEFB $0E,$0E,$19,$19,$19 ; 25 Life Suppt -> 14 Corridor#7
  DEFB $1C,$08,$1B,$1B,$1A ; 26 Livng Qtrs -> 8 Corridor#1, 27 Mess, 28
                           ; RecrtnArea
  DEFB $1A,$1C,$0D,$1B,$1B ; 27 Mess       -> 13 Corridor#6, 26 Livng Qtrs, 28
                           ; RecrtnArea
  DEFB $1A,$1A,$1B,$1B,$1C ; 28 RecrtnArea -> 26 Livng Qtrs, 27 Mess
  DEFB $0C,$0C,$0C,$1D,$1D ; 29 Stores #1  -> 12 Corridor#5
  DEFB $07,$07,$1E,$1E,$1E ; 30 Stores #2  -> 7 Computer
  DEFB $0D,$0D,$1F,$1F,$1F ; 31 Stores #3  -> 13 Corridor#6
  DEFB $21,$22,$04,$10,$20 ; 32 ShuttleBay -> 4 Cargopod#2, 16 Engineerng, 33
                           ; Shttlstore, 34 Narcissus
  DEFB $20,$20,$20,$21,$21 ; 33 Shttlstore -> 32 ShuttleBay
  DEFB $20,$20,$20,$22,$22 ; 34 Narcissus  -> 32 ShuttleBay

; Room adjacency map — air-duct network
;
; Companion table to RoomAdjCorridors (RoomAdjCorridors): same 5-slot-per-room
; format, but describing the air-duct/crawlway network that connects the same
; rooms along different edges (e.g. Mess connects directly to both Airlocks,
; Computer to Engine #2). Only 34 entries — the Narcissus (room 34) cannot be
; reached through the ducts.
;
; An actor whose record byte +1 has bit 6 set is travelling in this network;
; destinations fetched from here are stored with bit 6 set too, keeping the
; actor in duct space until a room handler clears the flag. Read by the duct
; branch of the destination picker at PickNextRoom, by the grille-list builder
; inside DrawSprite that lists a room's duct openings, and by the duct-entry
; move inside MoveToRoom that sends a crew member through a grille. The two
; one-way links (Airlock #2 -> Cargopod#1 and Stores #3 -> Airlock #2) are
; present in the original data.
RoomAdjDucts:
  DEFB $00,$1E,$1B,$1A,$00 ; 0 Airlock #1 -> 26 Livng Qtrs, 27 Mess, 30 Stores
                           ; #2
  DEFB $1B,$03,$01,$1C,$1B ; 1 Airlock #2 -> 3 Cargopod#1, 27 Mess, 28
                           ; RecrtnArea
  DEFB $02,$0B,$0B,$16,$16 ; 2 Armoury    -> 11 Corridor#4, 22 Inf Stores
  DEFB $21,$0E,$0E,$03,$03 ; 3 Cargopod#1 -> 14 Corridor#7, 33 Shttlstore
  DEFB $04,$0E,$0E,$0E,$04 ; 4 Cargopod#2 -> 14 Corridor#7
  DEFB $05,$0E,$14,$14,$05 ; 5 Cargopod#3 -> 14 Corridor#7, 20 Eng Stores
  DEFB $06,$08,$08,$06,$06 ; 6 CommdCentr -> 8 Corridor#1
  DEFB $07,$12,$12,$0D,$0D ; 7 Computer   -> 13 Corridor#6, 18 Engine #2
  DEFB $15,$08,$17,$06,$08 ; 8 Corridor#1 -> 6 CommdCentr, 21 Infirmary, 23
                           ; Laboratory
  DEFB $0A,$0A,$0C,$0C,$09 ; 9 Corridor#2 -> 10 Corridor#3, 12 Corridor#5
  DEFB $16,$0A,$09,$15,$0A ; 10 Corridor#3 -> 9 Corridor#2, 21 Infirmary, 22
                           ; Inf Stores
  DEFB $02,$12,$1D,$0F,$02 ; 11 Corridor#4 -> 2 Armoury, 15 Cryo Vault, 18
                           ; Engine #2, 29 Stores #1
  DEFB $09,$09,$18,$17,$17 ; 12 Corridor#5 -> 9 Corridor#2, 23 Laboratory, 24
                           ; Lab Stores
  DEFB $1E,$07,$1F,$1B,$0D ; 13 Corridor#6 -> 7 Computer, 27 Mess, 30 Stores
                           ; #2, 31 Stores #3
  DEFB $03,$19,$05,$04,$0E ; 14 Corridor#7 -> 3 Cargopod#1, 4 Cargopod#2, 5
                           ; Cargopod#3, 25 Life Suppt
  DEFB $0F,$0B,$0B,$0F,$0F ; 15 Cryo Vault -> 11 Corridor#4
  DEFB $10,$10,$14,$20,$20 ; 16 Engineerng -> 20 Eng Stores, 32 ShuttleBay
  DEFB $11,$12,$12,$11,$11 ; 17 Engine #1  -> 18 Engine #2
  DEFB $11,$07,$13,$0B,$19 ; 18 Engine #2  -> 7 Computer, 11 Corridor#4, 17
                           ; Engine #1, 19 Engine #3, 25 Life Suppt
  DEFB $13,$12,$12,$13,$13 ; 19 Engine #3  -> 18 Engine #2
  DEFB $10,$05,$05,$14,$14 ; 20 Eng Stores -> 5 Cargopod#3, 16 Engineerng
  DEFB $15,$0A,$08,$08,$15 ; 21 Infirmary  -> 8 Corridor#1, 10 Corridor#3
  DEFB $02,$0A,$0A,$16,$16 ; 22 Inf Stores -> 2 Armoury, 10 Corridor#3
  DEFB $08,$0C,$0C,$17,$17 ; 23 Laboratory -> 8 Corridor#1, 12 Corridor#5
  DEFB $18,$0C,$1D,$1D,$18 ; 24 Lab Stores -> 12 Corridor#5, 29 Stores #1
  DEFB $19,$19,$0E,$12,$12 ; 25 Life Suppt -> 14 Corridor#7, 18 Engine #2
  DEFB $1A,$00,$1C,$1C,$1A ; 26 Livng Qtrs -> 0 Airlock #1, 28 RecrtnArea
  DEFB $00,$0D,$01,$1B,$1B ; 27 Mess       -> 0 Airlock #1, 1 Airlock #2, 13
                           ; Corridor#6
  DEFB $1A,$01,$01,$1C,$1C ; 28 RecrtnArea -> 1 Airlock #2, 26 Livng Qtrs
  DEFB $1D,$0B,$0B,$18,$1D ; 29 Stores #1  -> 11 Corridor#4, 24 Lab Stores
  DEFB $1E,$1E,$0D,$00,$0D ; 30 Stores #2  -> 0 Airlock #1, 13 Corridor#6
  DEFB $0D,$0D,$1F,$01,$1F ; 31 Stores #3  -> 1 Airlock #2, 13 Corridor#6
  DEFB $21,$21,$10,$20,$20 ; 32 ShuttleBay -> 16 Engineerng, 33 Shttlstore
  DEFB $21,$03,$03,$20,$20 ; 33 Shttlstore -> 3 Cargopod#1, 32 ShuttleBay

; CrewNameTable
;
; 7 crew/role names separated by 255 ($FF) terminators: ALIEN, Dallas, Kane,
; Ripley, Ash, Lambert, Parker, Brett. Substituted into MessageTextTable
; placeholders 253/254 by the message renderer at RenderMessage.
CrewNameTable:
  DEFB $41,$4C,$49,$45,$4E,$FF,$44,$61
  DEFB $6C,$6C,$61,$73,$FF,$4B,$61,$6E
  DEFB $65,$FF,$52,$69,$70,$6C,$65,$79
  DEFB $FF,$41,$73,$68,$FF,$4C,$61,$6D
  DEFB $62,$65,$72,$74,$FF,$50,$61,$72
  DEFB $6B,$65,$72,$FF,$42,$72,$65,$74
  DEFB $74,$FF

; Actor record array — slot 0: the alien
;
; Eight 8-byte actor records (64 bytes in all). Slot k's name is entry k of
; CrewNameTable (CrewNameTable): slot 0 = ALIEN, slots 1-7 = Dallas, Kane,
; Ripley, Ash, Lambert, Parker, Brett. Most per-frame loops start from this
; base (LD IX,29566) and step 8 bytes per slot; the message renderer at
; RenderMessage passes the slot number straight to the name printer.
;
; Per-record field layout (IX-relative): +0 = sprite index for the actor's
; current pose (20 = idle; 92 = blank; 255 = none) — also stepped as an
; action/frame counter +1 = current room: bits 0-5 = room ID 0-34 (indexes
; RoomNameTable names and the RoomAdjCorridors adjacency map), bit 6 set =
; travelling in the air-duct network (RoomAdjDucts). $FF = HostMarker: slot
; excluded from normal crew processing +2 = destination room, written by the
; picker at PickNextRoom (bit 6 carries the duct flag along) +3 = action state
; (1=idle, 2=move, 3=wait, 5=investigate, 7=attack) +4 = strength / hit points
; (initial values 2-6; combat decrements) +5 = sprite X tile +6 = sprite Y tile
; +7 = status: 0 = alive/normal; 1 = killed (stamped by CrewHitsAlien's kill
; path); 2 = android defeated; $FF = removed from play (hypersleep, or
; HostMarker partner of +1=$FF)
;
; Slot 0 is the alien: its +1 byte (29566+1) holds the alien's current room at
; runtime (initialised inside CommonGameInit CommonInit_ItemsGrilles, rewritten
; by the airlock eject in BlowLock BlowLock; read by the combat dispatch
; CrewAction4_Handler CrewAction4_Handler and by Jones's avoidance check in
; UpdateJones). Its +7 byte (ActorStatusColumn) anchors the status-column scans
; (+7 column, step 8).
;
; The whole array is overwritten at game start by the templates at
; LongGameCrewInit (Long Game) / ShortGameCrewInit (Short Game); the row values
; below are the tape-image leftovers.
ActorRecords:
  DEFB $00,$00,$00,$00,$00,$00,$00 ; slot 0 (+0..+6) — unused

; Actor record array — status column anchor
;
; Slot 0's +7 (status) byte. The alive/status scans walk the +7 column from
; here or from slot 1's +7 (CrewStatusColumn) in steps of 8.
ActorStatusColumn:
  DEFB $00

; Actor record array — crew slots 1-7
;
; See ActorRecords for the field layout. Slot 1's row is split after +6 so that
; the status byte (CrewStatusColumn, base of the crew-extinction scan in
; CheckCrewAlive CheckCrewAlive) keeps its own address and label.
CrewRecords:
  DEFB $40,$0C,$00,$00,$02,$02,$02 ; slot 1 Dallas  +0..+6 — in Corridor#5
CrewStatusColumn:
  DEFB $00                ; slot 1 Dallas  +7 (alive-scan base)
  DEFB $40,$16,$00,$00,$04,$04,$04,$00 ; slot 2 Kane    — in Inf Stores
  DEFB $40,$4B,$00,$00,$04,$02,$02,$00 ; slot 3 Ripley  — in Corridor#4 duct
                                       ; (bit 6)
  DEFB $40,$09,$00,$00,$04,$04,$04,$00 ; slot 4 Ash     — in Corridor#2
  DEFB $40,$1B,$00,$00,$03,$04,$04,$00 ; slot 5 Lambert — in Mess
  DEFB $40,$0B,$00,$00,$03,$04,$04,$00 ; slot 6 Parker  — in Corridor#4
  DEFB $22,$13,$00,$00,$04,$02,$02,$00 ; slot 7 Brett   — in Engine #3

; CorridorPosTable — the 19-cell strip / action-menu rows
;
; 19 bytes indexed by CorridorCursor (CorridorCursor, range 0..18); the cell
; value under the cursor is mirrored to CursorCellValue ($8395) and is what the
; action key dispatches on. Cell r is drawn on screen row r+1, columns 22-31
; (row 0 holds the view title); a cell value >= 70 is the first row of a
; two-row label, so the cursor highlight covers two rows (see HighlightMenuRow
; HighlightMenuRow). The cells' meaning depends on the view: on the orders
; screen (mode 0) they hold the OrdersMenuTemplate (OrdersMenuTemplate) bytes;
; in the room view (mode 1) each cell holds a string index for the menu row
; drawn at that position (ResetActionMenu (ResetActionMenu) loads the skeleton
; from ActionMenuTemplate (ActionMenuTemplate); the load image holds a
; hypersleep- room variant: 73 " Destruct ", 74 "  Enter   ", 75 "Hypersleep");
; in the Indicate Location list (modes 2/3, IndicateLocation
; (IndicateLocation)) cells 1-17 hold room ids 0-16 or 17-33, framed by cell 0
; = 62 "Other List" (page toggle) and cell 18 = 61 "   QUIT   ". Read by the
; cursor-movement helpers around HandleInput at HandleInput.
CorridorPosTable:
  DEFB $49,$FF,$FF,$FF,$FF,$FF,$FF,$4A
; (cells 8-15; cell 8 = first " use:" row, written by BuildItemMenu)
MenuUseRows:
  DEFB $FF,$FF,$FF,$FF,$4B,$FF,$FF,$FF
; (cells 16-18; cell 16 = second fixture row, written by FixtureAction)
MenuFixtureRow:
  DEFB $FF,$FF,$FF
; 256 bytes of workspace/padding (zero in the load image)
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00

; Per-room type table
;
; One byte per room (0-34): the map-view layout type fed to RoomTypeByte
; (RoomTypeByte) — 0/1/2 pick the layout templates at RoomLayout0, RoomLayout1
; and RoomLayout2; 3 = the Narcissus. Read by LocationListAction
; (LocationListAction) when a room is picked from the Indicate Location list,
; and by UpdateRoomMarkerB (UpdateRoomMarkerB) to check an exit room against
; the displayed deck.
RoomTypeTable:
  DEFB $00,$00,$01,$02,$02,$02,$01 ; rooms 0-6
  DEFB $00,$01,$01,$01,$01,$01,$00 ; rooms 7-13
  DEFB $02,$01,$02,$01,$01,$01,$02 ; rooms 14-20
  DEFB $01,$01,$01,$01,$02,$00,$00 ; rooms 21-27
  DEFB $00,$01,$00,$00,$02,$02,$03 ; rooms 28-34

; Item location table
;
; One byte per portable item (ids 0-21, names via ItemIdToString
; (ItemIdToString)): the room where the item lies (bit 6 = inside the duct
; network), or a carry marker — 160+slot = held in that actor slot's front
; hand, 128+slot = back hand; 255 = does not exist (the two "Cat in ..." items
; are created when Jones is caught). Re-seeded from InitialItemRooms
; (InitialItemRooms) at game init. The Get Item picker handler ($8B22),
; LeaveItemHandler ($8BBA) and SwapHeldItems (SwapHeldItems) transfer items by
; rewriting these bytes.
ItemLocations:
  DEFB $18,$15,$14        ; 0-2: Elctrc Prd in Lab Stores/Infirmary/Eng Stores
  DEFB $06,$06,$10        ; 3-5: Incineratr in CommdCentr x2, Engineerng
TrackerLocations:
  DEFB $06,$10            ; 6-7: Tracker in CommdCentr, Engineerng
  DEFB $1B,$11,$12,$13    ; 8-11: Fire Extng in Mess, Engine #1-#3
  DEFB $20                ; 12: Harpn Gun in ShuttleBay
  DEFB $02,$02,$02        ; 13-15: Laser Pist in Armoury x3
  DEFB $18                ; 16: Net in Lab Stores
  DEFB $17                ; 17: Cat Box in Laboratory
  DEFB $1F,$14            ; 18-19: Spanner in Stores #3, Eng Stores
CatItemLocations:
  DEFB $FF,$FF            ; 20-21: Cat in Net / Cat in Box (not created)

; Viewed room's item list
;
; Item ids present in the viewed room (built by BuildRoomItemList
; (BuildRoomItemList) scanning ItemLocations top-down, so ids appear in
; descending order); 255-filled by ResetActionMenu (ResetActionMenu). The "Get
; Item" handler (GetItemMenu) turns it into a selection strip and GetItemPick
; (GetItemPick) moves the chosen item into the actor's hand.
RoomItemList:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00

; Per-item courage bonus
;
; Added to the carrier's courage (record byte +5) and morale (+6) while the
; item is held in the front hand (applied on Get at $8B26, removed on Leave at
; $8BAB and adjusted on Swap by SwapHeldItems (SwapHeldItems)). Weapons
; embolden the crew: prods/extinguishers/spanners +1,
; incinerators/harpoon/lasers +2, trackers and cat gear 0.
ItemCourageBonus:
  DEFB $01,$01,$01        ; Elctrc Prd
  DEFB $02,$02,$02        ; Incineratr
  DEFB $00,$00            ; Tracker
  DEFB $01,$01,$01,$01    ; Fire Extng
  DEFB $02                ; Harpn Gun
  DEFB $02,$02,$02        ; Laser Pist
  DEFB $00,$00            ; Net, Cat Box
  DEFB $01,$01            ; Spanner
  DEFB $01,$01            ; Cat in Net / Cat in Box

; Per-room damage meters
;
; 2-digit ASCII pairs ("00"), one per room (base + room*2, rooms 0-34, 70
; bytes). Incremented by AddRoomDamage (AddRoomDamage); the tens digit is the
; severity the engine scan compares against; crossing "30" raises alarms
; #22-#25.
RoomDamageDigits:
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 0-4
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 5-9
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 10-14
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 15-19
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 20-24
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 25-29
  DEFB $30,$30,$30,$30,$30,$30,$30,$30,$30,$30 ; rooms 30-34

; Per-room duct-grille state
;
; One byte per room (rooms 0-33; the Narcissus, room 34, has no grille): 255 =
; grille in place, 0 = removed. Game init ($A5A8, within
; CommonInit_ItemsGrilles) re-arms only the first 33 entries with 255 and then
; clears entry 13, so Corridor#6 (13) starts with its grille removed, and room
; 33's byte at $759D keeps its load-image value 0 (Shttlstore's grille is
; always open). Removed by crew action 3 (CrewAction3_RemoveGrille, a 50-frame
; job queued by the AI at $90EE or by picking "RmveGrille" from the action
; menu). While in place the room view offers string 49 "RmveGrille" in the
; menu's Special slot (CorridorPosTable cell 13); once removed the room shows
; "Grille removed" and string 50 "Grille" becomes a move-to target (cell 1)
; leading into the duct network. Read at $7E58 (DrawItemsAndGrille), $90E5,
; $B073 and RefreshSpecialRow.
RoomGrilleState:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00

; Crew-condition strings
;
; 255-terminated descriptions printed after "NAME is " by the room-info panel
; (DrawCrewCondition, DrawCrewCondition): four injury states selected by actor
; strength (record byte +4), then five morale states selected by record byte
; +6.
CrewInjuryText:
  DEFM "Dead ",$FF
  DEFM "Collapsed ",$FF
  DEFM "Wounded ",$FF
  DEFM "OK ",$FF
CrewMoraleText:
  DEFM "Broken",$FF
  DEFM "Shaken",$FF
  DEFM "Uneasy",$FF
  DEFM "Stable",$FF
  DEFM "Confident",$FF

; Action-menu strip template
;
; 19-byte skeleton of the room-view action menu, copied to the strip at
; CorridorPosTable by ResetActionMenu whenever the room view is rebuilt. Cell
; values are string indexes into RoomNameTable; 255 = empty slot. Layout: cell
; 0 = header 86 "move to:" (destinations fill cells 1-6: exits from
; BuildExitList (BuildExitList) at 2-6, the open duct "Grille" at 1); cell 7 =
; header 87 " use:" (items from BuildItemMenu (BuildItemMenu) fill 8-11); cell
; 12 = header 88 "Special" (room actions fill 13-16, e.g. "RmveGrille"/"
; ATTACK  " at 13); cell 17 = 61 "   QUIT   ".
ActionMenuTemplate:
  DEFB $56,$FF,$FF,$FF,$FF,$FF,$FF ; "move to:" + 6 destination slots
  DEFB $57,$FF,$FF,$FF,$FF ; " use:" + 4 item slots
  DEFB $58,$FF,$FF,$FF,$FF ; "Special" + 4 action slots
  DEFB $3D,$FF            ; "QUIT"

; Orders-menu template (the mode-0 menu strip)
;
; 19-byte template copied verbatim to CorridorPosTable (CorridorPosTable) by
; InitGameView (InitGameView) — the cell layout of the orders screen (room mode
; 0), drawn on screen rows 1-19 under the " CONTROL  " title: cell 0     83
; "Order:    "  header cells 1-7  52  crew rows — InitGameView draws the seven
; crew NAMES here (via PrintName PrintName), so the 52s are never rendered;
; they only give the rows a nonzero dispatch value cells 8-9  79,80  " Indicate
; "/" Location " (2-row label; the action key on row 8 opens the room-location
; list, IndicateLocation IndicateLocation) cells 10-11 84,85 " Display  "/"
; Level   " header (2-row label) cells 12-14 44,45,46 "Upper
; Deck"/"MiddleDeck"/"Lower Deck" (SelectDeckMap SelectDeckMap redraws the map
; pane) cells 15-18 255 blank The lower half (from cell 8) is also drawn
; directly from this template by the tail of OrdersMenuNextRow
; (OrdersMenuNextRow), pointer set to OrdersMenuLowerHalf. The next 504 bytes
; starting at CrewPortraits are the crew portrait bitmaps; see CrewPortraits.
OrdersMenuTemplate:
  DEFB $53,$34,$34,$34,$34,$34,$34,$34
OrdersMenuLowerHalf:
  DEFB $4F,$50,$54,$55,$2C,$2D,$2E,$FF
  DEFB $FF,$FF,$FF

; Crew portrait bitmaps
;
; Seven 3x3-character (24x24 pixel) raw bitmap portraits, drawn by
; BlitPortrait. Used by GameModeScreen (GameModeScreen -- all 7 at once) and by
; the crew-status display (single-portrait draw, crew index from DrawSlotIndex,
; via DrawSprite). Crew member names from CrewNameTable (CrewNameTable);
; portrait N is at address CrewPortraits + (N-1)*72. Portrait data continues
; into CrewPortraitsCont and CrewPortraitsCont2 due to block boundaries.
;
; Dallas (1):   Kane (2):     Ripley (3):   Ash (4):      Lambert (5):  Parker
; (6):   Brett (7):
CrewPortraits:
  DEFB $00,$00,$00,$01,$01
  DEFB $03,$03,$03,$7D,$FF,$FF,$DF,$BF
  DEFB $99,$00,$6B,$80,$C0,$C0,$E0,$E0
  DEFB $E0,$60,$70,$03,$03,$03,$03,$01
  DEFB $01,$01,$03,$08,$08,$14,$18,$2E
  DEFB $22,$FF,$7F,$70,$70,$30,$70,$60
  DEFB $E0,$C0,$40,$04,$04,$18,$E6,$01
  DEFB $00,$00,$00,$BE,$00,$80,$41,$12
  DEFB $00,$00,$00,$60,$90,$8C,$33,$C0
  DEFB $00,$00,$00,$00,$00,$01,$03,$03
  DEFB $03,$03,$03,$1E,$FF,$FF,$FF,$9F
  DEFB $03,$00,$73,$00,$00,$80,$C0,$E0
  DEFB $E0,$E0,$60,$03,$02,$02,$02,$01
  DEFB $00,$00,$03,$37,$44,$04,$0C,$00
  DEFB $9D,$81,$43,$60,$E0,$20,$60,$C0
  DEFB $80,$80,$60,$0C,$10,$E0,$1C,$02
  DEFB $00,$00,$00,$BF,$81,$C3,$FF,$7F
  DEFB $00,$00,$00,$18,$84,$8B,$03,$40
  DEFB $00,$00,$00,$00,$03,$07,$07,$0F
  DEFB $0F,$0F,$0F,$FF,$FF,$FF,$FF,$3B
  DEFB $00,$00,$73,$C0,$E0,$F0,$F0,$F8
  DEFB $78,$38,$B8,$0F,$0F,$0F,$0F,$0F
  DEFB $0F,$0F,$07,$33,$02,$02,$06,$80
  DEFB $8E,$80,$C0,$38,$38,$38,$78,$F8
  DEFB $78,$F8,$F8,$07,$07,$07,$03,$06
  DEFB $00,$00,$00,$E1,$DE,$C0,$E0,$33
  DEFB $00,$00,$00,$F8,$F8,$F8,$F0,$18
  DEFB $00,$00,$00,$00,$00,$00,$01,$03
  DEFB $03,$03,$02,$00,$7F,$FF,$8F,$C0
  DEFB $C0,$C0,$99,$00,$80,$C0,$E0,$20
  DEFB $30,$10,$90,$02,$02,$03,$01,$00
  DEFB $00,$00,$07,$02,$82,$46,$00,$8E
  DEFB $C0,$A1,$9E,$30,$30,$20,$60,$40
  DEFB $C0,$80,$E0,$08,$F0,$10,$13,$1C
  DEFB $00,$00,$00,$84,$49,$21,$D2

; CrewPortraitsCont
;
; Crew portrait bitmaps, continued (portraits 4 end, 5, 6 start).
CrewPortraitsCont:
  DEFB $0C,$00,$00,$00,$90,$0F,$04,$FC
  DEFB $00,$00,$00,$00,$00,$01,$03,$07
  DEFB $07,$07,$07,$07,$7F,$FF,$FF,$E7
  DEFB $C0,$80,$30,$09,$00,$C0,$E0,$F0
  DEFB $F0,$70,$B0,$30,$07,$07,$03,$03
  DEFB $03,$01,$01,$00,$32,$02,$02,$41
  DEFB $81,$80,$C7,$C0,$F0,$30,$30,$30
  DEFB $60,$60,$60,$C0,$00,$00,$01,$0E
  DEFB $00,$00,$00,$00,$E0,$58,$C7,$63
  DEFB $3F,$00,$00,$00,$40,$80,$C0,$F0
  DEFB $80,$00,$00,$00,$00,$00,$00,$01
  DEFB $03,$02,$00,$03,$3E,$7F,$FF,$FF
  DEFB $80,$00,$7F,$FF,$00,$80,$C0,$E0
  DEFB $20,$00,$80,$E0,$03,$03,$03,$01
  DEFB $01,$00,$00,$03,$CE,$FB,$FD,$F5
  DEFB $FB,$F1,$7F,$BF,$60,$E0,$C0,$C0
  DEFB $C0,$80,$80,$70,$7E,$82

; CrewPortraitsCont2
;
; Crew portrait bitmaps, continued (portrait 6 end, portrait 7).
CrewPortraitsCont2:
  DEFB $02,$01,$01,$00,$00,$00,$DF,$60
  DEFB $7F,$7F,$3E,$00,$00,$00,$4F,$90
  DEFB $90,$20,$40,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$01,$01,$00,$3F,$77
  DEFB $6B,$63,$FF,$FF,$C0,$BB,$80,$C0
  DEFB $C0,$C0,$C0,$C0,$60,$60,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$88,$88
  DEFB $8C,$40,$40,$4C,$20,$21,$60,$50
  DEFB $10,$20,$40,$C0,$C0,$40,$00,$0F
  DEFB $3E,$7E,$FF,$00,$00,$00,$FE,$20
  DEFB $A0,$A1,$52,$00,$00,$00,$78,$57
  DEFB $AF,$5F,$9F,$00,$00,$00

; Narcissus corridor-view layout (RLE)
;
; The shuttle's 20x19 view background in RoomLayoutExpand's RLE format (byte <
; 30 = that many blank cells, 30-254 = literal tile code, 255 = end; 141
; bytes). Expanded into the grid buffer and rendered by NarcissusBackground
; (NarcissusBackground) when a room of type 3 (the Narcissus) is drawn.
NarcissusLayout:
  DEFB $14,$02,$92,$93,$0C,$94,$95,$02
  DEFB $92,$93,$92,$93,$0C,$94,$95,$94
  DEFB $95,$92,$93,$10,$94,$95,$14,$14
  DEFB $02,$92,$93,$0C,$94,$95,$02,$92
  DEFB $93,$92,$93,$96,$0A,$97,$94,$95
  DEFB $94,$95,$92,$93,$02,$96,$A0,$03
  DEFB $7D,$7D,$03,$A0,$97,$02,$94,$95
  DEFB $04,$96,$A0,$03,$7D,$7D,$03,$A0
  DEFB $97,$04,$04,$96,$0A,$97,$04,$04
  DEFB $96,$02,$9E,$9E,$9E,$9E,$9E,$9E
  DEFB $02,$97,$04,$02,$92,$93,$01,$9A
  DEFB $9F,$9F,$9F,$9F,$9F,$9F,$9F,$9F
  DEFB $99,$01,$94,$95,$02,$92,$93,$03
  DEFB $9C,$98,$9D,$9D,$9D,$9D,$9D,$9D
  DEFB $98,$9B,$03,$94,$95,$14,$04,$A5
  DEFB $04,$A5,$01,$A1,$04,$A1,$03,$08
  DEFB $A7,$A6,$01,$A2,$A3,$07,$08,$A8
  DEFB $03,$A4,$07,$14,$FF

; Narcissus corridor-view attributes
;
; 20x19 = 380 attribute bytes copied over the attribute file by
; NarcissusBackground (NarcissusBackground); one line per screen row. The last
; nine bytes of this map were long mislabelled "GameStateBase (crew alive
; flags?)" — they are simply its bottom-right corner.
NarcissusAttrs:
  DEFB $09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09
  DEFB $09,$09,$0A,$11,$12,$12,$12,$12,$12,$12,$12,$12,$12,$12,$12,$12,$11,$0A,$09,$09
  DEFB $0A,$11,$10,$02,$12,$47,$47,$47,$47,$47,$47,$47,$47,$47,$47,$12,$02,$10,$11,$0A
  DEFB $10,$02,$07,$07,$12,$47,$47,$47,$47,$47,$47,$47,$47,$47,$47,$12,$07,$07,$02,$10
  DEFB $07,$07,$07,$07,$12,$47,$47,$47,$47,$47,$47,$47,$47,$47,$47,$12,$07,$07,$07,$07
  DEFB $07,$07,$07,$07,$12,$47,$47,$47,$47,$47,$47,$47,$47,$47,$47,$12,$07,$07,$07,$07
  DEFB $07,$07,$02,$10,$12,$12,$12,$12,$12,$12,$12,$12,$12,$12,$12,$12,$10,$02,$07,$07
  DEFB $02,$10,$11,$0A,$08,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$08,$0A,$11,$10,$02
  DEFB $11,$0A,$09,$09,$08,$0C,$24,$24,$09,$07,$07,$09,$24,$24,$21,$08,$09,$09,$0A,$11
  DEFB $09,$09,$09,$09,$08,$0C,$24,$24,$09,$07,$07,$09,$24,$24,$21,$08,$09,$09,$09,$09
  DEFB $09,$09,$09,$09,$08,$09,$09,$09,$09,$09,$09,$09,$09,$09,$09,$08,$09,$09,$09,$09
  DEFB $09,$09,$09,$09,$08,$09,$07,$07,$07,$07,$07,$07,$07,$07,$09,$08,$09,$09,$09,$09
  DEFB $09,$09,$0D,$29,$28,$28,$06,$06,$06,$06,$06,$06,$06,$06,$28,$28,$29,$0D,$09,$09
  DEFB $0D,$29,$2D,$2D,$2D,$05,$07,$07,$07,$07,$07,$07,$07,$07,$05,$2D,$2D,$2D,$29,$0D
  DEFB $2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D,$2D
  DEFB $2D,$2D,$2D,$2D,$2F,$38,$38,$38,$38,$39,$2D,$39,$38,$38,$38,$38,$2F,$2D,$2D,$2D
  DEFB $2D,$2D,$2D,$09,$09,$09,$09,$09,$39,$0F,$2D,$0F,$39,$09,$09,$09,$09,$09,$2D,$2D
  DEFB $2D,$2D,$2D,$09,$09,$09,$09,$09,$0F,$09,$2D,$09,$0F,$09,$09,$09,$09,$09,$2D,$2D
  DEFB $2D,$2D,$2D,$09,$09,$09,$09,$09,$09,$09,$2D,$09,$09,$09,$09,$09,$09,$09,$2D,$2D

; Per-screen RAM state
;
; First 26 bytes of the runtime RAM map. The remaining per-game-state
; scratchpad variables live further down in the code segment near AlienFlags —
; see that block for the rest of the RAM map.
;
; Variables: DrawSlotIndex DrawSlotIndex    actor slot (0-7) currently
; drawn/processed CurrentRoom CurrentRoom      room of that actor; "the viewed
; room" RoomTypeByte RoomTypeByte     view type: deck 0-2 of ShipMapData, 3 =
; Narcissus, 4 = alien-encounter screen RoomAttrStep RoomAttrStep     word:
; attr-cursor stride for door drawing RoomCellStep RoomCellStep     word:
; cell-grid stride for door drawing HeldItemFront HeldItemFront    item id in
; the actor's front hand (255 = none) HeldItemBack HeldItemBack     item id in
; the actor's back hand (255 = none) RoomMateA RoomMateA        first other
; actor in the room (255 = none) RoomMateB RoomMateB        second other actor
; in the room (255 = none) RoomModeByte RoomModeByte     current room mode
; (drives RoomDispatchTable) ShipSystemFlags ShipSystemFlags  bit 0 destruct
; armed; bits 1/2 airlocks blown; bits 3-5 engine state RoomAttrCursor
; RoomAttrCursor   word: attribute-file cursor (room drawing) DrawScreenPtr
; DrawScreenPtr    word: THE display-file draw pointer (~120 refs)
; RoomCellCursor RoomCellCursor   word: 20-wide room-cell-grid cursor
; CorridorTilePtr CorridorTilePtr  pointer into corridor tile source
; CrewAnimFlags CrewAnimFlags    XOR-blit toggle for crew sprite set A
; AnimFramePhase AnimFramePhase   XOR-blit toggle for set B / cursor
; ScriptPointer ScriptPointer    ROM-traversal AI script pointer
;
; The bytes from DrawRoomBackground onwards hold executable code (the view
; background renderer, invoked via CALL DrawRoomBackground from InitView_DrawBg
; and RedrawRoomScene), preserved as DEFB data only because the section was
; originally classified as RAM. Decoded, DrawRoomBackground reads: fill the
; 20x19 attribute window with 4 (green ink); A = RoomTypeByte; if A = 3 JP
; NarcissusBackground (NarcissusBackground); if A = 4 JP ResetAlienXAnim
; (ResetAlienXAnim, the encounter screen); else HL = ShipMapData + 380*A (that
; deck's 20x19 tile map) and fall into DrawRoomBackground_0
; (DrawRoomBackground_0), which renders the 20x19 tile map at HL onto the
; display via DrawSpriteRow — also called directly by NarcissusBackground with
; the RLE-expanded grid buffer.
DrawSlotIndex:
  DEFB $05
CurrentRoom:
  DEFB $06                ; room of the actor being processed (record byte +1,
                          ; cached by SelectSlotByDrawIdx SelectSlotByDrawIdx);
                          ; doubles as "the viewed room" in encounter checks
RoomTypeByte:
  DEFB $01
RoomAttrStep:
  DEFB $00,$00            ; word: attribute-cursor stride for door drawing
RoomCellStep:
  DEFB $00,$00            ; word: room-cell-grid stride for door drawing
HeldItemFront:
  DEFB $FF                ; corridor-strip cell (0-21) of the actor's id+160
                          ; marker; 12 = the alien's lair cell (see
                          ; FindHeldItems)
HeldItemBack:
  DEFB $FF                ; corridor-strip cell of the actor's id+128 marker
RoomMateA:
  DEFB $FF                ; slots of up to two OTHER actors in the current
RoomMateB:
  DEFB $FF                ; room (255 = none) - printed as "Also here" / "Also
                          ; Bodies of"; found by RecalcMorale
RoomModeByte:
  DEFB $00
ShipSystemFlags:
  DEFB $00                ; bit 0 = self-destruct armed; bits 1/2 = Airlock
                          ; #1/#2 blown open (set by BlowLock, cleared by
                          ; SealLock); bits 3-5 = Engine #1/#2/#3 state
                          ; (ship-systems tick at EngineStateScan)
  DEFB $00
RoomAttrCursor:
  DEFB $00,$58            ; word: attribute-file cursor for room/door drawing
                          ; (initially the attr-file base)
DrawScreenPtr:
  DEFB $00,$40            ; word: THE main display-file draw pointer used by
                          ; every blit routine (initially the display file)
RoomCellCursor:
  DEFB $00,$60            ; word: cursor into the 20-cell-wide room grid,
                          ; walked in step with RoomAttrCursor
CorridorTilePtr:
  DEFB $00,$00
CrewAnimFlags:
  DEFB $80
AnimFramePhase:
  DEFB $80
ScriptPointer:
  DEFB $00,$00
DrawRoomBackground:
  DEFB $11,$0C,$00,$21,$00,$58,$0E,$13
  DEFB $06,$14,$36,$04,$23,$10,$FB,$19
  DEFB $0D,$20,$F5,$11,$7C,$01,$21,$00
  DEFB $60,$3A,$0B,$7A,$FE,$03,$CA,$6C
  DEFB $83,$FE,$04,$CA,$07,$9B,$A7,$28
  DEFB $04,$19,$3D,$20,$FC
DrawRoomBackground_0:
  DEFB $22,$1B,$7A,$21,$00,$40,$22,$19
  DEFB $7A,$0E,$13,$06,$14,$C5,$ED,$5B
  DEFB $1B,$7A,$CD,$89,$7A,$21,$19,$7A
  DEFB $34,$2A,$1B,$7A,$23,$22,$1B,$7A
  DEFB $C1,$10,$EA,$11,$0C,$00,$2A,$19
  DEFB $7A,$7D,$FE,$E0,$38,$03,$11,$0C
  DEFB $07,$19,$22,$19,$7A,$0D,$20,$D3
  DEFB $C9

; DrawSpriteRow
;
; Draws one 8-pixel-tall column of a sprite tile onto the screen. Reads a tile
; index byte from (DE), multiplies by 8 to get an offset into the
; 8-byte-per-row character bitmap table at $6935 (CharBitmaps + index×8), then
; writes all 8 bytes vertically downward from the current draw pointer at
; DrawScreenPtr (INC H steps through ZX Spectrum display rows within the same
; character cell column).
;
; Input:  (DE) = tile/character index; (DrawScreenPtr) = screen destination
; address Output: screen updated; DE advanced; H incremented 8 times
;
; Used by the routines at DrawSprite, PrintName, DrawRoomTitle,
; DrawCrewCondition, DrawRoomMates, RenderMessage, OxygenCheckRedraw,
; DrawIntroScreen and PrintStr.
DrawSpriteRow:
  LD A,(DE)               ; read tile index from source pointer
; This entry point is used by the routines at DrawRoomTitle, DrawRoomMates,
; BlankRestOfRow, ClearTextWindow, ClearMessageRow and ClearAlienArea.
DrawTileA:
  LD L,A                  ; L = tile index
  LD H,$00
  ADD HL,HL               ; HL = index × 2
  ADD HL,HL               ; HL = index × 4
  ADD HL,HL               ; HL = index × 8
  LD BC,CharBitmaps       ; BC = $6935 (character bitmap base)
  ADD HL,BC               ; HL = address of first row of tile bitmap
  EX DE,HL                ; DE = bitmap source; HL = old DE (tile index ptr)
  LD HL,(DrawScreenPtr)   ; HL = screen destination (DrawScreenPtr)
  LD B,$08                ; 8 pixel rows per character
DrawTileA_RowLoop:
  LD A,(DE)               ; read bitmap byte for this pixel row
  LD (HL),A               ; write to display file
  INC H                   ; next display row (INC H steps within 8-row band)
  INC DE                  ; next bitmap byte
  DJNZ DrawTileA_RowLoop  ; repeat for all 8 rows
  RET

; NextScanLine
;
; Used by the routine at XorBlitMarker.
NextScanLine:
  LD A,H
  AND $07
  CP $07
  JR Z,NextScanLine_RowEnd
  INC H
  RET
NextScanLine_RowEnd:
  LD A,L
  CP $E0
  JR NC,NextScanLine_NextThird
  LD DE,$F920
  ADD HL,DE
  RET
NextScanLine_NextThird:
  LD DE,$0020
  ADD HL,DE
  RET

; InitGameView
;
; Initialises the corridor view for the current room. Clears the room mode
; byte, freezes both crew animation channels (sets bit 7 of
; CrewAnimFlags/AnimFramePhase), resets the corridor cursor to segment 8,
; copies the 19-byte corridor position template from OrdersMenuTemplate to
; CorridorPosTable, then draws the room's attribute colours and initial crew
; sprites.
;
; Used by the routines at OpenCrewRoomView, LocationListAction, QuitRoomView,
; GameEntry, CrewHitsAlien, VictimItemDrop, CrewHitsAndroid, KillActor_Bounce
; and AlienEventDispatch.
InitGameView:
  XOR A                   ; A = 0
  LD (RoomModeByte),A     ; clear room mode byte at RoomModeByte
  LD HL,CrewAnimFlags     ; HL = CrewAnimFlags (crew animation flags)
  SET 7,(HL)              ; freeze animation channel A (bit 7 = freeze)
  INC HL
  SET 7,(HL)              ; freeze animation channel B
  LD A,$80                ; $80
  LD (AlienXPhase),A      ; set alien pixel X sentinel (AlienXPhase)
  LD (JonesPhase),A       ; set alien pixel Y sentinel (JonesPhase)
  LD A,$08                ; segment 8 = centre of 19-segment corridor
  LD (CorridorCursor),A   ; store as current corridor cursor (CorridorCursor)
  LD A,$20                ; space char
  CALL ClearTextWindow    ; draw blank room border / clear game window
  LD A,(RoomTypeByte)     ; read room-type byte (RoomTypeByte)
  CP $04                  ; room type 4 = special (engine room)
  JR NZ,InitView_DrawBg
  LD A,$01                ; remap type 4 → type 1
  LD (RoomTypeByte),A
InitView_DrawBg:
  CALL DrawRoomBackground ; draw room background (walls, doors, floor tiles)
  LD DE,CorridorPosTable  ; DE = CorridorPosTable (corridor position table, 19
                          ; bytes)
  LD HL,OrdersMenuTemplate ; HL = OrdersMenuTemplate (corridor position
                           ; templates)
  LD BC,$0013             ; copy 19 entries
  LDIR                    ; initialise position table from template
  LD HL,$5816             ; HL = $5816 (attribute row for top of game window)
  LD A,$07                ; white ink on black paper
  LD C,$01                ; 1 column wide
  CALL FillAttrColumn     ; paint attribute column
  LD A,$28
  LD C,$08
  CALL FillAttrColumn
  LD A,$20
  LD C,$02
  CALL FillAttrColumn
  LD A,$17
  LD C,$05
  CALL FillAttrColumn
  XOR A
  LD C,$03
  CALL FillAttrColumn
  LD C,$01
  LD A,$07
  CALL FillAttrColumn
  LD HL,$4016
  LD (DrawScreenPtr),HL
  LD A,$52
  CALL DrawSprite
  LD HL,OrdersMenuTemplate
  LD (CorridorTilePtr),HL
  CALL DrawNextCorridorTile
  LD B,$07
InitView_BlankLoop:
  PUSH BC
  LD A,$5C
  CALL DrawSprite
  POP BC
  DJNZ InitView_BlankLoop
  LD HL,$4056
  LD (DrawScreenPtr),HL
  LD A,$01
  EX AF,AF'
  LD A,$07
; This entry point is used by the routine at OrdersMenuNextRow. Orders-screen
; crew-name loop head: A' holds the next crew id, A the remaining row count (7
; names on menu rows 2-8).
OrdersCrewNameLoop:
  EX AF,AF'               ; A = crew id to print
  DEFB $2A,$19            ; first 2 bytes of "LD HL,(draw pointer)": the
                          ; operand-high byte is the LD A,D at
                          ; OrdersCrewNameRow, so falling through loads HL and
                          ; continues after it

; Orders-screen crew-name row
;
; Body of the loop above (never entered at OrdersCrewNameRow itself — the LD
; A,D opcode is just the operand high byte of the overlapping instruction):
; prints crew name A via PrintName (PrintName), then steps the menu draw
; pointer (DrawScreenPtr) one character row down: +32 within a display-file
; third, or +1824 (via the fall-through block below) when crossing a third
; boundary (L >= $E0).
OrdersCrewNameRow:
  LD A,D                  ; (dead when fallen into: overlap operand)
  PUSH AF                 ; save crew id
  PUSH HL                 ; save draw pointer
  CALL PrintName          ; PrintName: draw crew name A on this row
  POP HL
  LD DE,$0020             ; normal stride: next char row in same third
  LD A,L
  CP $E0                  ; crossing into the next display third?
  JR C,OrdersMenuAdvance  ; no: advance by 32

; Orders-menu build, lower part
;
; Tail of the orders-screen build started in InitGameView (InitGameView). First
; the third-boundary case of the crew-name row stride (+1824), then the shared
; loop tail: store the draw pointer, advance A' to the next crew id and loop
; until all 7 names are printed. After the loop, draws menu rows 9-18 straight
; from OrdersMenuLowerHalf (OrdersMenuLowerHalf) (" Indicate "/" Location ", "
; Display  "/"  Level   ", the three deck rows, blanks), re-highlights the
; cursor row and mirrors its cell value (HighlightMenuRow), and redraws the
; oxygen clock on row 19 (OxygenCheckRedraw).
OrdersMenuNextRow:
  LD DE,$0720             ; big stride: next char row across a third boundary
; This entry point is used by the routine at OrdersCrewNameRow.
OrdersMenuAdvance:
  ADD HL,DE
  LD (DrawScreenPtr),HL   ; store draw pointer for the next row
  POP AF                  ; A = crew id just printed
  INC A
  EX AF,AF'               ; A' = next crew id, A = row counter
  DEC A
  JR NZ,OrdersCrewNameLoop ; loop for all 7 crew rows
  LD HL,OrdersMenuLowerHalf ; then draw rows 9-18 from OrdersMenuLowerHalf
  LD (CorridorTilePtr),HL
  LD B,$0A
OrdersMenuLowerLoop:
  PUSH BC
  CALL DrawNextCorridorTile ; draw next menu row from the template
  POP BC
  DJNZ OrdersMenuLowerLoop
  CALL HighlightMenuRow   ; highlight the cursor row / mirror its value
  CALL OxygenCheckRedraw  ; redraw the oxygen clock (row 19)
  RET

; FillAttrColumn
;
; Used by the routines at InitGameView, DrawSelectedName, UnhighlightMenuRow,
; HighlightMenuRow, IndicateLocationPage2 and GetItemMenu.
;
; Paint a 10-cell-wide attribute block — C rows of attribute A starting at HL
; (stride 22 = 32-10 to the next row). Used to colour the menu rows.
FillAttrColumn:
  LD DE,$0016             ; row stride remainder (32 - 10)
FillAttrCol_RowLoop:
  LD B,$0A                ; 10 attribute cells per row
FillAttrCol_CellLoop:
  LD (HL),A
  INC HL
  DJNZ FillAttrCol_CellLoop
  ADD HL,DE               ; next row
  DEC C
  JR NZ,FillAttrCol_RowLoop
  RET

; DrawNextCorridorTile
;
; Reads the next tile index from the corridor position pointer at
; CorridorTilePtr, draws it by falling through into DrawSprite (DrawSprite),
; and advances the pointer.
;
; Used by the routines at InitGameView, OrdersMenuNextRow, DrawSelectedName,
; DrawLocationList and GetItemMenu.
DrawNextCorridorTile:
  LD HL,(CorridorTilePtr) ; HL = corridor tile-source pointer (CorridorTilePtr)
  LD A,(HL)               ; A = next tile index
  INC HL                  ; advance pointer
  LD (CorridorTilePtr),HL ; save updated pointer; fall through to DrawSprite

; DrawSprite
;
; Renders a 10-character text label at the current draw position
; (DrawScreenPtr). The "sprite" data is the 10-byte text entry at RoomNameTable
; + A×10 (see RoomNameTable / UIStringTable); each byte is used as a tile index
; into CharBitmaps, which is laid out so that index N == ASCII N produces the
; glyph for that character. After drawing 10 columns, advances DrawScreenPtr to
; the next character row accounting for the ZX Spectrum display-file row
; wrapping.
;
; Input:  A = text/label index (255 → use blank label, entry 92)
; (DrawScreenPtr) = screen destination address
;
; Used by the routines at InitGameView, DrawRoomTitle, DrawSelectedName,
; DrawCrewStatusHalf, DrawLocationList, SwapHeldItems, SetActionTimer,
; RefreshRoomInfo, UpdateJones, RenderMessage, StartAttack,
; CrewAction4_Handler, UpdateHandRows, DestructArm, DestructAbort, SealLock,
; SealLock1, BlowLock, BlowLock1, FireDamageTick, CrewHitsAndroid,
; DestroyHeldItem and ForceAttackRowIfHostile.
DrawSprite:
  CP $FF                  ; sprite index 255 = invalid/absent
  JR NZ,DrawSprite_Lookup ; valid index: draw it
  LD A,$5C                ; substitute blank sprite (index 92)
DrawSprite_Lookup:
  LD L,A                  ; L = sprite index
  LD H,$00
  ADD HL,HL               ; HL = index × 2
  PUSH HL                 ; save HL = index × 2
  ADD HL,HL               ; HL = index × 4
  ADD HL,HL               ; HL = index × 8
  POP DE                  ; DE = index × 2
  ADD HL,DE               ; HL = index × 10 (label record size)
  LD DE,RoomNameTable     ; DE = RoomNameTable (RoomNameTable base)
  ADD HL,DE               ; HL = address of the 10-byte text entry
  EX DE,HL                ; DE = source pointer (10 ASCII tile-indices)
  LD B,$0A                ; 10 character columns per label
DrawSprite_ColLoop:
  PUSH BC                 ; save column counter
  PUSH DE                 ; save sprite data pointer
  CALL DrawSpriteRow      ; DrawSpriteRow: draw one tile column (8px tall)
  POP DE                  ; restore sprite data pointer
  INC DE                  ; advance to next tile index byte
  LD HL,DrawScreenPtr     ; HL → draw pointer (DrawScreenPtr)
  INC (HL)                ; move draw pointer right by 1 character column
  POP BC
  DJNZ DrawSprite_ColLoop ; repeat for all 10 columns
  LD HL,(DrawScreenPtr)   ; HL = draw pointer after last column
  DEC L                   ; back to start of this character row
  LD DE,$0017             ; advance 23 columns to next row (normal case)
  LD A,L
  CP $E0                  ; $E0: check for display-file third-bank boundary
  JR C,DrawSprite_Advance ; below $E0: normal row advance (+23)
  LD DE,$0717             ; at/above $E0: large offset to wrap to next band
DrawSprite_Advance:
  ADD HL,DE               ; move draw pointer to start of next character row
  LD (DrawScreenPtr),HL   ; save updated draw pointer
  RET

; PrintName
;
; Prints actor name A (0 = ALIEN, 1-7 = crew) by skipping A 255-terminated
; names in CrewNameTable and drawing the next one one glyph at a time. The
; second entry point three bytes in is also entered directly with DE pre-set to
; walk other string lists.
;
; Used by the routines at OrdersCrewNameRow, DrawCrewCondition, DrawRoomMates,
; DrawSelectedName, RenderMessage, GameModeScreen, PrintDeadRoster,
; PrintAndroidReveal and PrintNameThenPanel.
PrintName:
  LD DE,CrewNameTable     ; CrewNameTable
; This entry point is used by the routines at DrawCrewCondition and
; GameModeScreen.
PrintNameDE:
  AND A                   ; name 0: draw from the list head
  JR Z,PrintName_GlyphLoop
  LD B,A                  ; skip A 255-terminated names
PrintName_SkipLoop:
  LD A,(DE)
  INC DE
  CP $FF
  JR NZ,PrintName_SkipLoop
  DJNZ PrintName_SkipLoop
PrintName_GlyphLoop:
  PUSH DE                 ; draw one glyph via DrawSpriteRow
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr     ; draw pointer right one column
  INC (HL)
  LD A,(DE)               ; until the 255 terminator
  CP $FF
  JR NZ,PrintName_GlyphLoop
  RET

; SelectSlotByDrawIdx
;
; Re-points IX at the 8-byte record indexed by (DrawSlotIndex) and loads the
; slot's identity into (CurrentRoom) for downstream lookups. Critically, if the
; slot wears the HostMarker (byte+1 == $FF), the routine returns immediately
; without writing CurrentRoom / RoomTypeByte — every per-frame caller that
; walks into this entry point therefore skips that slot during routine corridor
; processing. In the Long Game this means the chestburster host is excluded
; from normal crew handling; in the Short Game the same exclusion applies to
; all four pre-stamped slots.
;
; This entry point is used by the routines at RedrawRoomScene and
; OpenCrewRoomView.
SelectSlotByDrawIdx:
  LD A,(DrawSlotIndex)    ; A = current draw index (slot 0..7)
  ADD A,A
  ADD A,A
  ADD A,A                 ; A *= 8 (record stride)
  LD E,A
  LD D,$00
  LD IX,ActorRecords      ; IX = $737E (slot table base)
  ADD IX,DE               ; IX = &slot[index]
  LD A,(IX+$01)           ; A = slot.byte+1
SelectSlot_HostCheck:
  CP $FF                  ; HostMarker?
  RET Z                   ; yes → bail out (slot is not a normal crew member)
  LD (CurrentRoom),A      ; cache slot identity for the rest of the room
                          ; handler
  AND $3F                 ; mask off direction bit → raw crew ID (0..63)
  LD E,A
  LD D,$00
  LD HL,RoomTypeTable     ; RoomTypeTable[crew's room]
  ADD HL,DE
  LD A,(HL)
  LD (RoomTypeByte),A     ; RoomTypeByte for this crew
  RET

; BuildExitList
;
; Builds the current room's door-exit list (CorridorPosTable + 2): walks the
; room's 5 direction slots in RoomAdjCorridors and stores each destination that
; differs from the current room (up to 5 entries). The room view draws one door
; per entry.
BuildExitList:
  LD A,(CurrentRoom)      ; A = current room
  LD L,A
  LD H,$00
  PUSH HL
  ADD HL,HL
  ADD HL,HL
  POP DE
  ADD HL,DE
  LD DE,RoomAdjCorridors
  ADD HL,DE
  LD DE,$73C0
  LD B,$05
BuildExit_SlotLoop:
  LD C,(HL)
  CP C
  JR Z,BuildExit_NextSlot
  LD A,(CurrentRoom)
  CP C
  JR Z,BuildExit_NextSlot
  LD A,C
  LD (DE),A
  INC DE
BuildExit_NextSlot:
  INC HL
  DJNZ BuildExit_SlotLoop
  RET

; BuildRoomItemList
;
; List the item ids lying in the current room — scan ItemLocations
; (ItemLocations) from the top down for entries equal to the room number,
; appending each match to RoomItemList (RoomItemList).
BuildRoomItemList:
  LD A,(CurrentRoom)      ; C = current room
  LD C,A
  LD HL,$7509             ; scan ItemLocations from item 21 down
  LD DE,RoomItemList      ; DE -> RoomItemList write pointer
  LD B,$16
BuildRoomItems_Loop:
  LD A,(HL)               ; item's location
  CP C                    ; lying in this room?
  JR NZ,BuildRoomItems_Next
  LD A,B                  ; yes: append its id (= B-1) to the list
  DEC A
  LD (DE),A
  INC DE
BuildRoomItems_Next:
  DEC HL
  DJNZ BuildRoomItems_Loop
  RET

; RefreshHeldItems
;
; Used by the routines at RedrawRoomScene, SwapHeldItems, SetActionTimer and
; PostCombatReset.
;
; Find what the current actor (DrawSlotIndex) holds.
RefreshHeldItems:
  LD A,(DrawSlotIndex)

; FindHeldItems
;
; Finds what actor A is carrying: scans ItemLocations (ItemLocations) for the
; slot+160 marker (front hand) into HeldItemFront (HeldItemFront) and the
; slot+128 marker (back hand) into HeldItemBack (HeldItemBack). The item id is
; the table position where the marker sits.
;
; Used by the routines at CrewHitsAlien, AlienScareCheck and CrewHitsAndroid.
FindHeldItems:
  ADD A,$80               ; C = slot + 128, E = slot + 160
  LD C,A
  ADD A,$20
  LD E,A
  LD HL,$7509             ; scan ItemLocations from item 21 down
  LD B,$16
FindHeld_Loop:
  LD A,(HL)
  CP C                    ; back-hand marker?
  JR NZ,FindHeld_FrontCheck
  LD A,B                  ; yes: HeldItemBack = item id (B-1)
  DEC A
  LD (HeldItemBack),A
  JR FindHeld_Next
FindHeld_FrontCheck:
  CP E                    ; front-hand marker?
  JR NZ,FindHeld_Next
  LD A,B                  ; yes: HeldItemFront = item id
  DEC A
  LD (HeldItemFront),A
FindHeld_Next:
  DEC HL
  DJNZ FindHeld_Loop
  RET
; This entry point is used by the routines at BuildItemMenu, SwapHeldItems,
; GetItemMenu, SetActionTimer and UpdateHandRows.

; ItemIdToString
;
; Maps an item id (in A) to its name's string index: ids 0-2 -> 35 Elctrc Prd,
; 3-5 -> 36 Incineratr, 6-7 -> 37 Tracker, 8-11 -> 38 Fire Extng, 12 -> 39
; Harpn Gun, 13-15 -> 40 Laser Pist, 16 -> 41 Net, 17 -> 42 Cat Box, 18-19 ->
; 43 Spanner; ids 20/21 (Jones captured) -> strings 68 "Cat in Net" / 69 "Cat
; in Box".
ItemIdToString:
  LD E,$23                ; E = first item string ("Elctrc Prd")
  CP $03                  ; ids 0-2: prods
  JR C,ItemIdStr_Specials
  INC E                   ; 3-5: incinerators
  CP $06
  JR C,ItemIdStr_Specials
  INC E                   ; 6-7: trackers
  CP $08
  JR C,ItemIdStr_Specials
  INC E                   ; 8-11: extinguishers
  CP $0C
  JR C,ItemIdStr_Specials
  INC E                   ; 12: the harpoon gun
  CP $0C
  JR Z,ItemIdStr_Specials
  INC E                   ; 13-15: laser pistols
  CP $10
  JR C,ItemIdStr_Specials
  INC E                   ; 16: the net
  CP $10
  JR Z,ItemIdStr_Specials
  INC E                   ; 17: the cat box
  CP $11
  JR Z,ItemIdStr_Specials
  INC E                   ; 18-19: spanners
ItemIdStr_Specials:
  CP $14                  ; ids 20/21 (the caught cat)
  JR NZ,ItemIdStr_CatBox  ; override the chain:
  LD E,$44                ; 68 "Cat in Net"
  JR ItemIdStr_Done
ItemIdStr_CatBox:
  CP $15
  JR NZ,ItemIdStr_Done
  LD E,$45                ; 69 "Cat in Box"
ItemIdStr_Done:
  LD A,E
  RET

; BuildItemMenu
;
; Fills the action menu's " use:" rows (strip cells 8-11) for the current
; actor: row 8 = name of the front-hand item, row 9 = name of the back-hand
; item, row 10 = "Get Item" (47) when the room has items lying about and the
; back hand is free, row 11 = "Leave Item" (48) when the front hand is loaded.
BuildItemMenu:
  LD C,$FF                ; C = "empty" sentinel
  LD HL,MenuUseRows       ; menu strip cell 8
  LD A,(HeldItemFront)    ; front-hand item
  CP C
  CALL NZ,ItemIdToString  ; -> item name string
  LD (HL),A               ; row 8
  INC HL
  LD A,(HeldItemBack)     ; back-hand item
  CP C
  CALL NZ,ItemIdToString
  LD (HL),A               ; row 9
  INC HL
  LD DE,RoomItemList      ; any items lying in this room?
  LD A,(DE)
  CP C
  JR Z,BuildItemMenu_Hands
  LD A,(HeldItemBack)     ; and the back hand free?
  CP C
  JR NZ,BuildItemMenu_Hands
  LD A,$2F                ; yes: row 10 = "Get Item"
  LD (HL),A
BuildItemMenu_Hands:
  INC HL
  LD A,(HeldItemFront)    ; front hand loaded?
  CP C
  RET Z
  LD A,$30                ; yes: row 11 = "Leave Item"
  LD (HL),A
  RET

; DrawRoomSpecials
;
; Fill the strip cells for the viewed room's special occupants and fixtures:
; the hostile Android and the alien (ATTACK on the Special slot), Jones ("Get
; Jones " on row 14), then the room's fixture label on cells 15/16 (destruct
; lever, airlock doors, hypersleep pod, engine fire, launch console).
DrawRoomSpecials:
  LD A,(AlienTargetID)    ; is there an Android this game,
  CP $08
  JR NC,DrawSpec_RoomChecks
  LD A,(AlienActiveFlag)  ; already hostile,
  AND A
  JR Z,DrawSpec_RoomChecks
  LD HL,(AlienTargetPtr)  ; and in the viewed room?
  INC HL
  LD A,(CurrentRoom)
  CP (HL)
  CALL Z,DrawSpec_AndroidHere ; yes: offer ATTACK
DrawSpec_RoomChecks:
  LD A,(CurrentRoom)      ; Jones in the viewed room?
  LD HL,JonesRoom
  CP (HL)
  CALL Z,DrawSpec_JonesHere ; yes: cat + "Get Jones " row
  LD HL,$737F             ; the alien in the viewed room?
  CP (HL)
  CALL Z,DrawSpec_AlienHere ; yes: offer ATTACK
  LD HL,ShipSystemFlags   ; HL -> ShipSystemFlags,
  LD DE,$73CD             ; DE -> strip cells 15/16: which
  CP $06                  ; fixture lives in this room?
  JR Z,DrawSpec_DestructLever ; CommdCentr: the destruct lever
  CP $0D                  ; Corridor#6: the airlock doors
  JR Z,DrawSpec_Airlock
  CP $0F                  ; Cryo Vault: the hypersleep pod
  JR Z,DrawSpec_HyperPod
  CP $11                  ; engine rooms: a fire,
  JR Z,DrawSpec_Engine1Fire ; if one is burning
  CP $12
  JR Z,DrawSpec_Engine2Fire
  CP $13
  JR Z,DrawSpec_Engine3Fire
  CP $22                  ; Narcissus: the launch console
  JR Z,DrawSpec_LaunchConsole
  RET                     ; other rooms: no fixture
DrawSpec_LaunchConsole:
  BIT 6,(HL)              ; (flag bit 6 would hide the console;
  RET NZ                  ; nothing sets it in this build)
  LD A,$4E                ; cells 15/16 = launch-console
  LD (DE),A               ; label (strings 78/81)
  LD A,$51
  INC DE
  LD (DE),A
  RET
DrawSpec_DestructLever:
  LD A,$46                ; lever off (70/71), or
  BIT 0,(HL)              ; armed (72/73) when the
  JR Z,DrawSpec_LeverStore ; destruct bit is set
  LD A,$48
DrawSpec_LeverStore:
  LD (DE),A               ; write the two-row label
  INC DE                  ; into cells 15/16
  INC A
  LD (DE),A
  RET
DrawSpec_HyperPod:
  LD A,$4A                ; hypersleep pod label (74/75)
  LD (DE),A               ; into cells 15/16
  INC DE
  INC A
  LD (DE),A
  RET
DrawSpec_Engine1Fire:
  BIT 3,(HL)              ; Engine #1 burning?
  JR DrawSpec_FireCells
DrawSpec_Engine2Fire:
  BIT 4,(HL)              ; Engine #2 burning?
  JR DrawSpec_FireCells
DrawSpec_Engine3Fire:
  BIT 5,(HL)              ; Engine #3 burning?
DrawSpec_FireCells:
  RET Z                   ; no fire: no label
  LD A,$4C                ; fire label (76/77)
  LD (DE),A               ; into cells 15/16
  INC A
  INC DE
  LD (DE),A
  RET
DrawSpec_Airlock:
  LD A,$35                ; cell 15 = airlock #1 door:
  BIT 1,(HL)              ; closed (53) or blown (55)
  JR Z,DrawSpec_StoreDoor1
  LD A,$37
DrawSpec_StoreDoor1:
  LD (DE),A
  INC DE
  LD A,$36                ; cell 16 = airlock #2 door:
  BIT 2,(HL)              ; closed (54) or blown (56)
  JR Z,DrawSpec_StoreDoor2
  LD A,$38
DrawSpec_StoreDoor2:
  LD (DE),A
  RET
; Jones is in the viewed room: menu Special row 14 ($73CC) offers string 51
; "Get Jones " (its handler needs the Net or the Cat Box), and the cat is
; drawn.
DrawSpec_JonesHere:
  PUSH AF
  LD DE,$73CC
  LD A,$33
  LD (DE),A
  CALL InitJonesAnim
  POP AF
  RET
; The alien is in the viewed room: draw it and offer "  ATTACK  ".
DrawSpec_AlienHere:
  PUSH AF
  JP AttackRowGate
; This entry point is used by the routine at AttackRowGate.
DrawSpec_AlienAttackRow:
  LD A,$34                ; menu row = "  ATTACK  "
  LD (DE),A
  XOR A
  LD (AlienXPhase),A
; This entry point is used by the routine at AttackRowGate.
DrawSpec_AlienDone:
  POP AF
  RET
; The activated Android is in the viewed room: draw it, offer ATTACK.
DrawSpec_AndroidHere:
  PUSH AF
  JP AndroidRowGate
; This entry point is used by the routine at AndroidRowGate.
DrawSpec_AndroidAttackRow:
  LD A,$34                ; menu row = "  ATTACK  "
  LD (DE),A
; This entry point is used by the routine at AndroidRowGate.
DrawSpec_AndroidDone:
  POP AF
  RET

; DrawRoomTitle
;
; Used by the routines at RedrawRoomScene and AddAlienRoomDamage.
;
; The panel's top line — the room name (or "In Duct") and, for rooms, "Damage"
; plus the two ASCII damage digits.
DrawRoomTitle:
  LD A,(CurrentRoom)      ; room-name string = room number,
  BIT 6,A                 ; or 93 "In Duct" inside the ducts
  JR Z,DrawTitle_Print
  LD A,$5D
DrawTitle_Print:
  LD HL,Screen_R19C0      ; draw pointer -> title row
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  LD HL,CurrentRoom       ; in the ducts: no damage meter
  BIT 6,(HL)
  RET NZ
  LD HL,$506B             ; "Damage    " (89)
  LD (DrawScreenPtr),HL
  LD A,$59
  CALL DrawSprite
  LD A,(CurrentRoom)      ; DE -> RoomDamageDigits[room]
  ADD A,A
  LD L,A
  LD H,$00
  LD DE,RoomDamageDigits
  ADD HL,DE
  EX DE,HL
  LD HL,$5072             ; draw the two ASCII digits
  LD (DrawScreenPtr),HL
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  LD HL,DrawScreenPtr
  INC (HL)
  INC DE
  CALL DrawSpriteRow
  LD HL,DrawScreenPtr
  INC (HL)
  LD A,$87                ; damage-meter icon glyph
  CALL DrawTileA
  RET
; Room-info panel text fragments (was mis-disassembled as code): ASCII strings
; drawn one glyph at a time by the panel routines below. Byte 136 is the
; grille-icon UDG.
ItemsPresentText:
  DEFM "Items Present"
GrilleText:
  DEFM "Grille"
IsText:
  DEFM " is ",$88," "
AlsoHereText:
  DEFM "Also here "
AndText:
  DEFM "and "
ButText:
  DEFM "but "
AlsoText:
  DEFM "Also "
BodiesOfText:
  DEFM "Bodies of "
GrilleIconText:
  DEFM " ",$88," "
; DrawItemsAndGrille: room-info panel section under the room name. Draws the
; "Items Present" header when the viewed room's item list ($750A) is non-empty,
; then (except aboard the Narcissus, room 34) prints "Grille" plus its state ("
; in place "/" removed ") from RoomGrilleState (RoomGrilleState) and plants the
; matching action-menu entry: string 49 "RmveGrille" in the Special slot (strip
; cell 13) while in place, or string 50 "Grille" as a move-to target (strip
; cell 1) once removed. Used by the routine at RedrawRoomScene.
DrawItemsAndGrille:
  LD HL,RoomItemList      ; HL -> viewed room's item list
  LD A,(HL)               ; first entry: 255 = no items here
  CP $FF
  JR Z,DrawTitle_GrilleInfo ; no items: skip the header
  LD HL,Screen_R20C0      ; draw pointer -> items header row
  LD (DrawScreenPtr),HL
  LD DE,ItemsPresentText  ; "Items Present"
  LD B,$0D                ; 13 characters
DrawTitle_ItemsHdrLoop:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow      ; DrawSpriteRow: one glyph
  POP DE
  POP BC
  INC DE                  ; next character
  LD HL,DrawScreenPtr
  INC (HL)                ; draw pointer right one column
  DJNZ DrawTitle_ItemsHdrLoop
DrawTitle_GrilleInfo:
  LD A,(CurrentRoom)      ; A = viewed room
  CP $22                  ; Narcissus: no grille aboard the shuttle
  RET Z
  LD DE,GrilleText        ; "Grille"
  LD HL,Screen_R20C16     ; draw pointer -> grille status row
  LD (DrawScreenPtr),HL
  LD B,$06                ; 6 characters
DrawTitle_GrilleLoop:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow      ; DrawSpriteRow: one glyph
  POP DE
  POP BC
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  DJNZ DrawTitle_GrilleLoop
  LD A,(CurrentRoom)      ; index RoomGrilleState by room number
  AND $3F                 ; (mask off the in-ducts flag, bit 6)
  LD L,A
  LD H,$00
  LD DE,RoomGrilleState
  ADD HL,DE
  LD A,(HL)
  CP $FF                  ; 255 = grille still in place
  JR Z,DrawTitle_GrilleInPlace
  LD A,$5B                ; removed: print strip 91 " removed  "
  CALL DrawSprite
  LD DE,$73BF             ; menu strip cell 1 (a move-to target):
  LD A,$32                ; string 50 "Grille" = enter the open duct
  LD (DE),A
  RET
DrawTitle_GrilleInPlace:
  LD A,$5A                ; in place: print strip 90 " in place "
  CALL DrawSprite
  LD DE,$73CB             ; menu strip cell 13 (the Special slot):
  LD A,$31                ; string 49 "RmveGrille"
  LD (DE),A
  RET

; DrawCrewCondition
;
; Prints the room-info line "NAME is <injury> and/ but <morale>" for the
; selected crew member (slot in DrawSlotIndex), using CrewInjuryText
; (CrewInjuryText, index = strength clamped to 0-3) and CrewMoraleText
; (CrewMoraleText, index = morale byte clamped to 0-4); "but" is picked when
; injury and morale disagree.
;
; Used by the routines at RedrawRoomScene, SwapHeldItems, WoundMsg,
; PickupItemBoost, RefreshCrewCondition, RefreshCondIfViewed, BumpCourageMorale
; and DecayMoraleStep.
DrawCrewCondition:
  LD A,(DrawSlotIndex)    ; A = selected crew slot
  LD HL,Screen_R21C0      ; draw pointer -> condition row
  LD (DrawScreenPtr),HL
  CALL PrintName          ; PrintName
  LD DE,IsText            ; " is " (shared tail of the grille text)
  LD B,$04                ; 4 characters
DrawCond_IsLoop:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ DrawCond_IsLoop
  LD A,(IX+$04)           ; injury index from strength:
  CP $03                  ; 0/1/2 as-is (Dead/Collapsed/Wounded),
  JR C,DrawCond_Injury    ; 3 -> 2 (Wounded), >3 -> 3 (OK)
  LD A,$03
  JR NZ,DrawCond_Injury
  LD A,$02
DrawCond_Injury:
  LD DE,CrewInjuryText    ; print CrewInjuryText[A]
  CALL PrintNameDE
  LD DE,AndText           ; "and "
  LD A,(IX+$04)           ; pick the conjunction: "but " when
  CP $04                  ; injury and morale disagree (healthy
  JR C,DrawCond_MoraleCheck ; but low morale, or hurt but high
  LD A,(IX+$06)           ; morale), else "and "
  CP $03
  JR NC,DrawCond_Conj
  JR DrawCond_But
DrawCond_MoraleCheck:
  LD A,(IX+$06)
  CP $03
  JR C,DrawCond_Conj
DrawCond_But:
  LD DE,ButText           ; "but "
DrawCond_Conj:
  LD B,$04                ; draw the 4-character conjunction
DrawCond_ConjLoop:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ DrawCond_ConjLoop
  LD A,(IX+$06)           ; morale index, clamped to 0-4
  CP $05
  JR C,DrawCond_Morale
  LD A,$04
DrawCond_Morale:
  LD DE,CrewMoraleText    ; print CrewMoraleText[A]
  CALL PrintNameDE
  JP PanelColCheck

; FindRoomMates
;
; Scan ActorRecords' +1 room bytes for other actors in the current room,
; storing the first two slot numbers found into RoomMateA/RoomMateB
; (31250/31251).
FindRoomMates:
  LD HL,$7387             ; HL -> slot 1's +1 (room) byte
  LD DE,RoomMateA         ; DE -> RoomMateA
  LD BC,$0701             ; B = 7 slots, C = slot number 1
  LD A,(DrawSlotIndex)    ; A' = the selected actor (excluded)
  EX AF,AF'
  LD A,(CurrentRoom)      ; A = the viewed room
FindMates_Loop:
  CP (HL)                 ; same room?
  JR NZ,FindMates_Next
  EX AF,AF'               ; and not the selected actor itself?
  CP C
  JR Z,FindMates_SkipSelf
  EX DE,HL                ; store the slot number
  LD (HL),C
  EX DE,HL
  INC DE
FindMates_SkipSelf:
  EX AF,AF'
FindMates_Next:
  PUSH DE                 ; next 8-byte record
  LD DE,$0008
  ADD HL,DE
  POP DE
  INC C
  DJNZ FindMates_Loop
  RET

; DrawRoomMates
;
; Used by the routines at RedrawRoomScene, RefreshRoomInfo and
; KillActorRefresh.
;
; The "Also here NAME" / "Also Bodies of NAME" panel line. FindRoomMates fills
; RoomMateA/B; if every listed mate is dead (ActorRecords byte +7 != 0) the
; line reads "Also Bodies of", else "Also here" with the first mate's name
; (dead mates get the strip-94 overprint from DrawCrewStatusHalf,
; DrawCrewStatusHalf).
DrawRoomMates:
  CALL FindRoomMates      ; FindRoomMates -> RoomMateA/B
  LD HL,Screen_R22C0      ; draw pointer -> "Also here" row
  LD (DrawScreenPtr),HL
  LD BC,$0200             ; B = 2 mates to check, C = 0
  LD DE,RoomMateA         ; DE -> RoomMateA
DrawMates_DeadScan:
  PUSH BC
  PUSH DE
  LD A,(DE)               ; no (more) mates: alive path
  CP $FF
  JR Z,DrawMates_Alive
  ADD A,A                 ; HL -> that slot's +7 status byte
  ADD A,A
  ADD A,A
  LD E,A
  XOR A
  LD D,A
  LD HL,ActorStatusColumn
  ADD HL,DE
  CP (HL)                 ; still alive? -> alive path
  JR Z,DrawMates_Alive
  POP DE                  ; this one is dead; check the next
  INC DE
  POP BC
  DJNZ DrawMates_DeadScan
  LD DE,AlsoText          ; "Also Bodies of " (15 contiguous chars)
  LD B,$0F                ; all mates dead:
  CALL PrintStr           ; draw "Also Bodies of "
  LD A,(RoomMateA)        ; first body's name
  CALL PrintName
  LD DE,GrilleIconText    ; " ",136," " grille-icon spacer
  LD B,$03
  CALL PrintStr
  LD A,(RoomMateB)        ; second body's name
  CALL PrintName
  JP BlankRestOfRow       ; blank-pad the rest of the row
DrawMates_Alive:
  POP BC
  POP DE
  LD HL,RoomMateA         ; anyone here at all?
  LD A,(HL)
  CP $FF
  JR Z,BlankRestOfRow     ; no: just blank the row
  LD DE,AlsoHereText      ; "Also here "
  LD B,$0A                ; draw "Also here " one glyph at a time
DrawMates_HereLoop:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ DrawMates_HereLoop
  LD A,(RoomMateA)        ; mate A: dead-overprint check + name
  CALL DrawCrewStatusHalf
  CALL PrintName
  LD A,(RoomMateB)        ; a second mate too?
  CP $FF
  JR Z,BlankRestOfRow
  LD A,$90                ; separator glyph
  CALL DrawTileA
  LD HL,DrawScreenPtr
  INC (HL)
  LD A,(RoomMateB)        ; mate B: dead-overprint check + name
  CALL DrawCrewStatusHalf
  JP PrintNameThenPanel

; BlankRestOfRow
;
; Used by the routines at DrawRoomMates and PrintNameThenPanel.
;
; Draw blank tiles until the draw pointer reaches column 0 of the next row.
BlankRestOfRow:
  XOR A
  CALL DrawTileA
  LD HL,DrawScreenPtr
  INC (HL)
  LD A,(HL)
  AND $1F
  RET Z
  JR BlankRestOfRow

; ResetActionMenu
;
; Restore the action-menu strip at CorridorPosTable (CorridorPosTable) from
; ActionMenuTemplate (ActionMenuTemplate), clear the viewed room's 20-entry
; item list ($750A) to 255 and invalidate the cached actor cell/name state
; ($7A10-$7A13).
ResetActionMenu:
  LD DE,CorridorPosTable
  LD HL,ActionMenuTemplate ; copy the 19-byte menu skeleton
  LD BC,$0013
  LDIR
  LD HL,RoomItemList      ; item list: 20 x 255 = empty
  LD A,$FF
  LD B,$14
ResetMenu_FillLoop:
  LD (HL),A
  INC HL
  DJNZ ResetMenu_FillLoop
  LD (HeldItemBack),A     ; no actor back/front cell
  LD (HeldItemFront),A
  LD (RoomMateA),A        ; no crew names cached
  LD (RoomMateB),A
  RET

; RedrawRoomScene
;
; Rebuilds the whole room scene: resets the action-menu strip and the room item
; state (ResetActionMenu), redraws the corridor strip, rebuilds the exit and
; item menus, the info panel (room name, damage, items present, grille state,
; crew condition) and the special actors, then updates the status panel. Called
; by everything that changes what the room shows (item transfers, deaths,
; grille removal, ...).
;
; Used by the routines at OpenCrewRoomView, GetItemPick, LeaveItemHandler,
; CrewAction1_Arrive, CrewAction2_DuctTransit, CrewAction3_RemoveGrille,
; TriggerAlienXAnim and PauseMenu.
RedrawRoomScene:
  CALL ResetActionMenu    ; ResetActionMenu
  LD HL,DrawMenuStrip     ; freeze both animation channels
  LD (CrewAnimFlags),HL   ; ($8080)
  LD A,$80                ; and the Jones / alien-body
  LD (JonesPhase),A       ; animation phases too
  LD (AlienXPhase),A
  CALL SelectSlotByDrawIdx ; point IX at the selected slot
  LD A,(CurrentRoom)      ; sharing the room with the alien?
  LD HL,$737F
  CP (HL)
  JR NZ,Redraw_DuctCheck
  LD HL,RoomTypeByte      ; yes: view type 4 —
  LD (HL),$04             ; the encounter screen
Redraw_DuctCheck:
  BIT 6,A                 ; viewing a duct position?
  JR NZ,Redraw_Ducts
  CALL DrawRoomBackground ; no: draw the room background
  CALL BuildExitList      ; and build the exit list
  JR Redraw_Common
Redraw_Ducts:
  CALL RedrawRoomView     ; yes: the duct/room interior view
Redraw_Common:
  CALL DrawCrewPortrait   ; the crew portrait,
  CALL BuildRoomItemList  ; the room's item list,
  CALL RefreshHeldItems   ; the held-items cache,
  CALL BuildItemMenu      ; the " use:" menu rows,
  CALL ClearTextWindow    ; a clear info panel,
  CALL DrawRoomTitle      ; room name + damage meter,
  CALL DrawItemsAndGrille ; "Items Present" / grille state,
  CALL DrawCrewCondition  ; the condition line,
  CALL DrawRoomMates      ; the "Also here" lists,
  CALL DrawRoomSpecials   ; hostiles/Jones/fixtures,
  CALL DrawSelectedName   ; the menu strip + status line
  LD A,(RoomTypeByte)     ; encounter screen?
  CP $04
  RET Z                   ; then no map marker
  LD A,(CurrentRoom)      ; in a room (not the ducts):
  BIT 6,A                 ; place the walking-figure marker
  CALL Z,PlaceRoomMarkerA ; on the deck map
  RET

; DrawSelectedName — the menu column's name row and colour bands
;
; Rebuild the right-hand menu column's fixed dressing: the selected crew
; member's name on screen row 0, the coloured attribute bands behind the 19
; menu rows, then (falling into DrawMenuStrip) the mode-1 strip itself from
; CorridorPosTable, the cursor highlight on the QUIT row and the status line.
;
; Used by the routines at RedrawRoomScene, DestroyHeldItem and
; RefreshGrilleRow.
DrawSelectedName:
  LD HL,$4016             ; blank the name row
  LD (DrawScreenPtr),HL   ; (screen row 0, col 22)
  PUSH HL
  LD A,$FF
  CALL DrawSprite
  POP HL
  INC HL                  ; and print the selected crew
  LD (DrawScreenPtr),HL   ; member's name at col 23
  LD A,(DrawSlotIndex)
  CALL PrintName
  LD HL,$5816             ; paint the colour bands (attr
  LD A,$07                ; col 22): the name row white,
  LD C,$01
  CALL FillAttrColumn
  LD A,$0F                ; cells 0-6 white-on-blue
  LD C,$07                ; (the "move to:" block),
  CALL FillAttrColumn
  LD A,$20                ; cells 7-9 green paper
  LD C,$03                ; (" use:" + the two hands),
  CALL FillAttrColumn
  LD A,$17                ; cell 10 white-on-red (Get Item),
  LD C,$01
  CALL FillAttrColumn
  LD A,$30                ; cell 11 yellow paper (Leave Item),
  LD C,$01
  CALL FillAttrColumn
  LD A,$1F                ; cells 12-16 white-on-magenta
  LD C,$05                ; (the Special block),
  CALL FillAttrColumn
  LD A,$38                ; cells 17-18 white paper (QUIT)
  LD C,$02
  CALL FillAttrColumn
  LD A,$3D                ; mirror = 61 "   QUIT   "
  LD (CursorCellValue),A
DrawMenuStrip:
  LD A,$11                ; park the cursor on the QUIT row
  LD (CorridorCursor),A
  LD A,$01                ; room mode 1 (the room view)
  LD (RoomModeByte),A
  LD HL,CorridorPosTable  ; tile source = CorridorPosTable
  LD (CorridorTilePtr),HL
  LD HL,$4036             ; draw the 18 strip rows from
  LD (DrawScreenPtr),HL   ; screen row 1, col 22
  LD B,$12
Redraw_StripLoop:
  PUSH BC
  CALL DrawNextCorridorTile
  POP BC
  DJNZ Redraw_StripLoop
  CALL HighlightMenuRow   ; highlight the cursor row
  CALL OxygenCheckRedraw  ; and redraw the status line
  RET
; Cell-glyph walk-handler table (was mis-disassembled as code): 3-byte records
; (glyph, handler word) looked up by DrawDoorArm (DrawDoorArm), which patches
; the handler into its CALL as it walks an exit path over the room-map grid.
; Glyphs 126/127/132/133 are straight wall segments -> ArmStraight
; (ArmStraight, a no-op: keep heading); 128-131 are the four corner pieces ->
; steer handlers that bend the walk by approach direction; 125 '}' (duct
; grille) and 142/143 (junction nodes) -> ArmEndNode (ArmEndNode): paint and
; end the walk.
CellDrawTable:
  DEFB $7D,$6C,$82        ; '}' duct grille -> ArmEndNode
  DEFB $7E,$FB,$81        ; horizontal wall -> ArmStraight
  DEFB $7F,$FB,$81        ; vertical wall -> ArmStraight
  DEFB $80,$FC,$81        ; top-left corner -> ArmCornerTopLeft
  DEFB $81,$18,$82        ; bottom-left corner -> ArmCornerBottomLeft
  DEFB $82,$34,$82        ; bottom-right corner -> ArmCornerBottomRight
  DEFB $83,$50,$82        ; top-right corner -> ArmCornerTopRight
  DEFB $84,$FB,$81        ; horizontal wall -> ArmStraight
  DEFB $85,$FB,$81        ; vertical wall -> ArmStraight
  DEFB $8E,$6C,$82        ; junction node -> ArmEndNode
  DEFB $8F,$6C,$82        ; junction node -> ArmEndNode

; RedrawRoomView
;
; Rebuilds the room-interior view for the current room: picks the layout
; template by room type, decompresses it into the grid buffer, draws
; walls/furniture, then walks the exit list drawing one door arm per neighbour
; (and grille arms for duct openings). Room type 4 (the alien's lair view)
; resets the encounter animation instead.
RedrawRoomView:
  LD A,(RoomTypeByte)     ; A = room type
  CP $04                  ; alien-lair view?
  JR NZ,PickRoomLayout
  CALL ResetAlienXAnim    ; yes: reset alien body animation
  CALL BuildDuctExitList
  RET

; PickRoomLayout
PickRoomLayout:
  LD HL,RoomLayout0       ; room type 0: layout template 0
  AND A
  JR Z,PickLayout_Expand
  LD HL,RoomLayout1
  CP $01
  JR Z,PickLayout_Expand
  LD HL,RoomLayout2
PickLayout_Expand:
  CALL RoomLayoutExpand
  CALL BuildDuctExitList
  CALL ClearRoomAttrs
  CALL SetRoomOrigin
  LD HL,(RoomAttrCursor)  ; mark the room's anchor cell (attr 71:
  LD (HL),$47             ; bright white) — the arms start here
  PUSH HL
  LD HL,(RoomCellCursor)
  PUSH HL
  LD A,($73C0)            ; 1st exit (menu strip cell 2)?
  CP $FF
  JR Z,PickLayout_Exit2
  LD DE,$FFEC             ; yes: walk the NORTH arm — heading -20
  LD (RoomCellStep),DE    ; (grid) / -32 (attr), starting one cell
  ADD HL,DE               ; above the anchor
  LD (RoomCellCursor),HL
  LD HL,(RoomAttrCursor)
  LD DE,$FFE0             ; DE = -32 (screen stride: one cell up within a
                          ; third)
  LD (RoomAttrStep),DE
  ADD HL,DE
  LD (RoomAttrCursor),HL
  CALL DrawDoorArm
PickLayout_Exit2:
  LD A,($73C1)            ; 2nd exit (menu strip cell 3)?
  CP $FF
  JR Z,PickLayout_Exit3
  POP HL                  ; yes: walk the EAST arm — heading +1/+1,
  POP DE                  ; starting one cell right of the anchor
  PUSH DE
  PUSH HL
  LD BC,$0001
  INC HL
  LD (RoomCellCursor),HL
  INC DE
  LD (RoomAttrCursor),DE
  LD (RoomAttrStep),BC
  LD (RoomCellStep),BC
  CALL DrawDoorArm
PickLayout_Exit3:
  LD A,($73C2)            ; 3rd exit (menu strip cell 4)?
  CP $FF
  JR Z,PickLayout_Exit4
  POP HL                  ; yes: walk the SOUTH arm — heading +20/+32,
  POP DE                  ; starting one cell below the anchor
  PUSH DE
  PUSH HL
  LD BC,$0014
  LD (RoomCellStep),BC
  ADD HL,BC
  LD (RoomCellCursor),HL
  LD HL,$0020
  LD (RoomAttrStep),HL
  ADD HL,DE
  LD (RoomAttrCursor),HL
  CALL DrawDoorArm
PickLayout_Exit4:
  POP HL
  POP DE
  LD A,($73C3)            ; 4th exit (menu strip cell 5)?
  CP $FF
  RET Z
  DEC DE                  ; yes: walk the WEST arm — heading -1/-1,
  DEC HL                  ; starting one cell left of the anchor
  LD (RoomCellCursor),HL
  LD (RoomAttrCursor),DE
  LD HL,$FFFF
  LD (RoomAttrStep),HL
  LD (RoomCellStep),HL
  CALL DrawDoorArm
  RET

; RoomLayoutExpand
;
; Decompresses the RLE room layout at HL into the 20x19 grid buffer: byte < 30
; = that many zero cells, 30-254 = literal tile code, 255 = end.
;
; Used by NarcissusBackground (NarcissusBackground) and PickRoomLayout
; (PickRoomLayout).
RoomLayoutExpand:
  LD DE,RoomGridBuffer    ; DE = grid buffer; HL = RLE source
RleExpand_Loop:
  LD A,(HL)
  CP $1E
  JR C,RleExpand_Zeros
  CP $FF
  RET Z
  LD (DE),A
  INC HL
  INC DE
  JR RleExpand_Loop
RleExpand_Zeros:
  LD B,A
  XOR A
RleExpand_ZeroLoop:
  LD (DE),A
  INC DE
  DJNZ RleExpand_ZeroLoop
  INC HL
  JR RleExpand_Loop

; SetRoomOrigin
;
; Loads the current room's (row, column) pair from RoomOriginTable and sets
; RoomCellCursor (grid buffer + 20*row + col) and RoomAttrCursor (attribute
; file + 32*row + col) for the door drawing pass.
SetRoomOrigin:
  LD A,(CurrentRoom)      ; A = current room
  AND $3F
  ADD A,A
  LD D,$00
  LD E,A
  LD HL,RoomOriginTable
  ADD HL,DE
  LD B,(HL)
  INC HL
  LD C,(HL)
  PUSH BC
  LD DE,$0014
  LD HL,RoomGridBuffer
RoomOrigin_GridLoop:
  ADD HL,DE
  DJNZ RoomOrigin_GridLoop
  ADD HL,BC
  LD (RoomCellCursor),HL
  POP BC
  LD HL,AttrFileOrigin
  LD DE,$0020
RoomOrigin_AttrLoop:
  ADD HL,DE
  DJNZ RoomOrigin_AttrLoop
  ADD HL,BC
  LD (RoomAttrCursor),HL
  RET

; DrawDoorArm
;
; Highlights one exit path of the room-map view: walks the (invisibly drawn)
; wall glyphs from the room's anchor point, painting each visited attribute
; cell cyan (attr 5), until it reaches a grille or junction node. Per cell it
; reads the grid under RoomCellCursor, looks the glyph up in CellDrawTable
; (CellDrawTable) and patches the handler into the CALL below (self-modifying).
; Straight segments keep the heading, corner glyphs 128-131 turn it, and node
; glyphs (125/142/143) end the walk via ArmEndNode (which pops the return
; address). The stride pair RoomAttrStep/RoomCellStep encodes the heading: attr
; +32/-32/+1/-1 = south/north/east/west (grid steps +20/-20/+1/-1 in lockstep,
; the grid being 20 cells wide).
DrawDoorArm:
  LD HL,(RoomCellCursor)  ; read grid cell under the cursor
  LD A,(HL)
  LD HL,CellDrawTable     ; find its 3-byte record in CellDrawTable
DoorArm_FindCell:
  CP (HL)
  JR Z,DoorArm_GotCell
  INC HL
  INC HL
  INC HL
  JR DoorArm_FindCell
DoorArm_GotCell:
  INC HL                  ; fetch the handler word
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
  LD ($81DF),HL           ; patch it into the CALL below
  CALL ArmStraight        ; steer/stop for this glyph
  LD HL,(RoomAttrCursor)  ; paint the attribute cell cyan
  LD (HL),$05
  LD DE,(RoomAttrStep)    ; step the attribute cursor by the heading
  ADD HL,DE
  LD (RoomAttrCursor),HL
  LD HL,(RoomCellCursor)  ; step the grid cursor in lockstep
  LD DE,(RoomCellStep)
  ADD HL,DE
  LD (RoomCellCursor),HL
  JR DrawDoorArm

; ArmStraight
;
; Walk handler for straight wall glyphs (126/127/132/133): a no-op, the walk
; keeps its current heading.
ArmStraight:
  RET
; ArmCornerTopLeft — walk handler for corner glyph 128, which joins a
; south-going edge to an east-going one: a walker heading north exits east; any
; other heading exits south.
ArmCornerTopLeft:
  LD HL,(RoomAttrStep)    ; L = low byte of current heading
  LD A,L
  LD DE,$0020             ; default: turn south
  LD HL,$0014
  CP $E0                  ; heading north (-32, low byte 224)?
  JR NZ,ArmCornerTL_SetSteps
  LD DE,$0001             ; yes: turn east
  LD HL,$0001
ArmCornerTL_SetSteps:
  LD (RoomAttrStep),DE
  LD (RoomCellStep),HL
  RET
; ArmCornerBottomLeft — corner glyph 129 joins a north-going edge to an
; east-going one: heading south exits east; otherwise exits north.
ArmCornerBottomLeft:
  LD HL,(RoomAttrStep)    ; L = low byte of current heading
  LD A,L
  LD DE,$0001             ; default: turn east
  LD HL,$0001
  CP $20                  ; heading south?
  JR Z,ArmCornerBL_SetSteps
  LD DE,$FFE0             ; no: turn north (DE = -32, HL = -20)
  LD HL,$FFEC
ArmCornerBL_SetSteps:
  LD (RoomAttrStep),DE
  LD (RoomCellStep),HL
  RET
; ArmCornerBottomRight — corner glyph 130 joins a west-going edge to a
; north-going one: heading east exits north; otherwise exits west.
ArmCornerBottomRight:
  LD HL,(RoomAttrStep)    ; L = low byte of current heading
  LD A,L
  LD DE,$FFE0             ; default: turn north (DE = -32, HL = -20)
  LD HL,$FFEC
  CP $01                  ; heading east?
  JR Z,ArmCornerBR_SetSteps
  LD DE,$FFFF             ; no: turn west
  LD HL,$FFFF
ArmCornerBR_SetSteps:
  LD (RoomAttrStep),DE
  LD (RoomCellStep),HL
  RET
; ArmCornerTopRight — corner glyph 131 joins a west-going edge to a south-going
; one: heading east exits south; otherwise exits west.
ArmCornerTopRight:
  LD HL,(RoomAttrStep)    ; L = low byte of current heading
  LD A,L
  LD DE,$0020             ; default: turn south
  LD HL,$0014
  CP $01                  ; heading east?
  JR Z,ArmCornerTR_SetSteps
  LD DE,$FFFF             ; no: turn west
  LD HL,$FFFF
ArmCornerTR_SetSteps:
  LD (RoomAttrStep),DE
  LD (RoomCellStep),HL
  RET
; ArmEndNode — walk handler for the duct grille (125) and junction nodes
; (142/143): pops DrawDoorArm's looping return address, paints the node cell
; cyan and ends the walk.
ArmEndNode:
  POP AF
  LD HL,(RoomAttrCursor)
  LD (HL),$05
  RET

; ClearRoomAttrs
ClearRoomAttrs:
  LD DE,$000C             ; black out the 20x19 room-view
  LD HL,AttrFileOrigin    ; attribute window
  LD C,$13
ClearRoomAttrs_RowLoop:
  LD B,$14
ClearRoomAttrs_CellLoop:
  LD (HL),$00
  INC HL
  DJNZ ClearRoomAttrs_CellLoop
  ADD HL,DE
  DEC C
  JR NZ,ClearRoomAttrs_RowLoop
  LD HL,RoomGridBuffer    ; then render the grid buffer
  CALL DrawRoomBackground_0 ; (DrawRoomBackground_0)
  RET

; BuildDuctExitList
;
; The duct view's "move to:" rows: fill strip cells 2-5 from the room's four
; RoomAdjDucts slots (skipping no-exit entries), then overwrite each present
; exit with an anonymous direction arrow — strings 57-60
; "North/East/South/West" — so the player sees where the crawlway leads but not
; what it leads to.
BuildDuctExitList:
  LD A,(CurrentRoom)      ; HL = RoomAdjDucts + room * 5
  AND $3F
  LD L,A
  LD H,$00
  PUSH HL
  ADD HL,HL
  ADD HL,HL
  POP DE
  ADD HL,DE
  LD DE,RoomAdjDucts
  ADD HL,DE
  LD DE,$73C0             ; DE -> strip cell 2
  LD B,$04                ; four duct directions:
DuctExits_SlotLoop:
  LD C,(HL)               ; C = the destination that way;
  CP C                    ; own room = no exit: leave the
  JR Z,DuctExits_NextSlot ; cell empty (checked twice — A
  LD A,(CurrentRoom)      ; holds a stale destination after
  AND $3F                 ; a stored exit, so the reload
  CP C                    ; makes the second test reliable)
  JR Z,DuctExits_NextSlot
  LD A,C                  ; else cell = the destination room
  LD (DE),A
DuctExits_NextSlot:
  INC DE
  INC HL
  DJNZ DuctExits_SlotLoop
  LD B,$04                ; second pass: disguise each
  LD HL,$73C0             ; present exit as a direction
  LD C,$39                ; arrow (57 North ... 60 West)
DuctExits_ArrowLoop:
  LD A,(HL)
  CP $FF
  JR Z,DuctExits_NextArrow
  LD (HL),C
DuctExits_NextArrow:
  INC C
  INC HL
  DJNZ DuctExits_ArrowLoop
  RET

; ClearTextWindow
;
; Used by the routines at InitGameView, RedrawRoomScene and PauseMenu.
ClearTextWindow:
  LD HL,Screen_R19C0
  LD (DrawScreenPtr),HL
  LD B,$80
ClearText_Loop:
  PUSH BC
  XOR A
  CALL DrawTileA
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ ClearText_Loop
  RET

; DrawCrewPortrait
;
; Draw the selected crew member's 24x24-pixel portrait (CrewPortraits
; CrewPortraits, 72 bytes each) at the bottom-left of the room view (screen
; rows 15-17, cols 1-3), blue ink on white. BlitPortrait is also entered
; directly by GameModeScreen with HL/DE preloaded.
DrawCrewPortrait:
  LD HL,$59E1             ; paint the 3x3 attribute square
  LD DE,$001D             ; (row 15, col 1) blue-on-white (57)
  LD C,$03
Portrait_AttrRowLoop:
  LD B,$03
Portrait_AttrCellLoop:
  LD (HL),$39
  INC HL
  DJNZ Portrait_AttrCellLoop
  DEC C
  ADD HL,DE
  JR NZ,Portrait_AttrRowLoop
  LD DE,$0048             ; DE -> CrewPortraits +
  LD HL,CrewPortraits     ; (slot-1) * 72 bytes
  LD A,(DrawSlotIndex)
  DEC A
  JR Z,Portrait_AddrDone
  LD B,A
Portrait_AddrLoop:
  ADD HL,DE
  DJNZ Portrait_AddrLoop
Portrait_AddrDone:
  EX DE,HL
  LD HL,$48E1             ; blit at screen row 15, col 1
; This entry point is used by the routine at GameModeScreen.
BlitPortrait:
  LD (DrawScreenPtr),HL
  LD B,$03                ; 3 cell rows
BlitPortrait_RowLoop:
  LD C,$03                ; of 3 cell columns
BlitPortrait_ColLoop:
  PUSH BC
  LD HL,(DrawScreenPtr)
  LD B,$08                ; 8 pixel bytes straight down
BlitPortrait_PixLoop:
  LD A,(DE)
  LD (HL),A
  INC H
  INC DE
  DJNZ BlitPortrait_PixLoop
  LD HL,DrawScreenPtr     ; next cell column
  INC (HL)
  POP BC
  DEC C
  JR NZ,BlitPortrait_ColLoop
  PUSH DE                 ; next cell row: usually -3 +32,
  LD HL,(DrawScreenPtr)   ; but crossing into the next
  LD DE,$001D             ; screen third needs the big
  LD A,L                  ; +1821 stride
  CP $E0
  JR C,BlitPortrait_Advance
  LD DE,$071D
BlitPortrait_Advance:
  ADD HL,DE
  LD (DrawScreenPtr),HL
  POP DE
  DJNZ BlitPortrait_RowLoop
  RET

; AdvanceScriptPtr
;
; Steps the ROM-traversal script pointer forward by one byte and returns the
; low nibble (0-15) of the new byte in A. The pointer lives at ScriptPointer
; and walks through ZX Spectrum ROM bytes starting at $1041 (SEED $9041 with H
; masked to $10). Each nibble encodes one game command for the AI scheduler.
;
; Output: A = next script command (0-15)
;
; Used by the routines at SetActionTimer, GameEntry, PickNextRoom, UpdateJones,
; UpdateAlienAI, AlienScareCheck, BlowLock1, CrewAction5_AlienAttack,
; VictimItemDrop and LongGameInit.
AdvanceScriptPtr:
  LD HL,(ScriptPointer)   ; HL = current script pointer (ScriptPointer)
  INC HL                  ; advance to next ROM byte
  LD (ScriptPointer),HL   ; save updated pointer
  LD A,(HL)               ; read ROM byte at new address
  AND $0F                 ; keep only low nibble → command 0-15
  RET

; ResetScriptPtr
;
; Resets the ROM-traversal script pointer to its starting address. Reads the
; SEED value from SysFrames (ZX Spectrum system variable SEED, updated by
; RAND), calls the address-masking helper at MaskToRomAddr to compute $1041
; (ROM address $9041 with H masked to $10), and stores the result at
; ScriptPointer.
;
; Used by the routines at GameEntry and ResetMsgAndScript.
ResetScriptPtr:
  LD HL,(SysFrames)       ; HL = SEED (SysFrames): ZX ROM random seed value
  CALL MaskToRomAddr      ; mask H to $10: HL → ROM address in range
                          ; $1000-$1FFF
  LD (ScriptPointer),HL   ; store as script pointer at ScriptPointer
  RET

; Prefix a dead room-mate's name with "Body of "
;
; Called by DrawRoomMates (DrawRoomMates) with A = actor slot before each
; mate's name is printed. If the actor's status byte (ActorRecords +7) is
; non-zero (dead/removed), draw strip 94 ("Body of   ") at the current draw
; position and advance DrawScreenPtr 8 cells so the name lands right after the
; prefix. A is preserved.
DrawCrewStatusHalf:
  PUSH AF                 ; save the actor slot
  LD HL,ActorStatusColumn ; HL = ActorRecords slot 0 byte +7 (status)
  LD DE,$0008
  LD B,A                  ; step to slot A's status byte (slot >= 1 here)
CrewStatus_Seek:
  ADD HL,DE
  DJNZ CrewStatus_Seek
  LD A,(HL)               ; status: 0 = alive
  AND A
  JR Z,CrewStatus_Done    ; alive: no prefix
  LD HL,(DrawScreenPtr)
  PUSH HL
  LD A,$5E                ; strip 94 = "Body of   "
  CALL DrawSprite         ; DrawSprite prints it at DrawScreenPtr
  POP HL
  LD DE,$0008             ; advance the draw pointer 8 cells: the name
  ADD HL,DE               ; overprints the strip's trailing spaces
  LD (DrawScreenPtr),HL
CrewStatus_Done:
  POP AF                  ; restore the actor slot
  RET

; NarcissusBackground — the shuttle's view background
;
; Reached only from DrawRoomBackground's hidden dispatch: the JP Z at $7A41
; (inside the code preserved as DEFBs at DrawRoomBackground) lands here when
; RoomTypeByte is 3, i.e. the Narcissus. Expands NarcissusLayout
; (NarcissusLayout) into the grid buffer, copies NarcissusAttrs
; (NarcissusAttrs) over the attribute file, then renders the grid via
; DrawRoomBackground_0 (DrawRoomBackground_0). No skoolkit-visible caller — the
; JP hides in DEFB data — and absent from the execution traces only because
; they never boarded the shuttle.
NarcissusBackground:
  LD HL,NarcissusLayout   ; NarcissusLayout: RLE source
  CALL RoomLayoutExpand   ; RoomLayoutExpand -> grid buffer
  LD DE,NarcissusAttrs    ; NarcissusAttrs
  LD HL,AttrFileOrigin    ; attribute file base
  LD C,$13                ; 19 rows...
Narcissus_AttrRows:
  LD B,$14                ; ...of 20 attribute bytes
Narcissus_AttrCells:
  LD A,(DE)
  LD (HL),A
  INC DE
  INC HL
  DJNZ Narcissus_AttrCells
  PUSH DE
  LD DE,$000C             ; skip the remaining 12 columns of the row
  ADD HL,DE
  POP DE
  DEC C
  JR NZ,Narcissus_AttrRows
  LD HL,RoomGridBuffer    ; grid buffer
  CALL DrawRoomBackground_0 ; render it as tiles
  RET

; Game state scratchpad RAM
;
; 110 bytes of mixed 1- and 2-byte variables stamped inline within the code
; region. The initial byte values come from the tape image, but every byte is
; treated as RAM by the running game: written by the main-loop subsystems and
; read back on subsequent frames. Despite living inside the code segment, none
; of these bytes are executed.
;
; Key variables: AlienFlags AlienFlags        bit-packed input (R,L,D,U,Fire)
; CorridorCursor CorridorCursor    current corridor index (0..18)
; SpriteDestAddr SpriteDestAddr    sprite blitter screen pointer FrameCounterB
; FrameCounterB     free-running 50Hz counter SoundRequest SoundRequest
; one-shot sound trigger CrewIndex CrewIndex         current crew member (0..6)
; AlienTargetPtr AlienTargetPtr         pointer to current crew record
; AlienTargetID AlienTargetID     crew ID being targeted (255=none)
; AlienActiveFlag AlienActiveFlag   alien is loose flag JonesRoom JonesRoom
; Jones the cat's current room (255 = off-map) SoundReqFlag SoundReqFlag
; pending sound parameters SprScreenAddrA SprScreenAddrA    crew sprite A
; destination SprScreenAddrB SprScreenAddrB    crew sprite B destination
; MusicActiveFlag MusicActiveFlag   music phrase is playing AnimFrameCnt
; AnimFrameCnt      crew animation tick
;
; All cells in this block now carry meaningful labels; the message-queue
; cluster (MsgDrawnCol through MsgLastEnqueued) is documented at
; TickMessageQueue (TickMessageQueue).
AlienFlags:
  DEFB $00                ; Keyboard/joystick scan bitmask (bit 0=R, 1=L, 2=D,
                          ; 3=U, 4=Fire)
CorridorCursor:
  DEFB $11                ; Currently selected corridor / cursor position
                          ; (0-18)
SavedRowAttr:
  DEFB $00                ; attribute saved from under the menu-cursor
                          ; highlight (HighlightMenuRow $8842 /
                          ; UnhighlightMenuRow $882B)
CursorCellValue:
  DEFB $00                ; CorridorPosTable cell value under the cursor
                          ; ($8395): what the action key dispatches on
SprDestA:
  DEFB $00
SprDestAHi:
  DEFB $40                ; high byte of SprDestA (marker A screen address)
SpriteDestAddr:
  DEFW DisplayFile        ; Screen destination pointer used by sprite blitter
FrameCounterB:
  DEFB $19                ; Free-running frame counter (low byte, ~50 Hz)
FrameCounterA:
  DEFB $19                ; frame counter for marker channel A (reset to 10)
NearRoomList:
  DEFB $FF,$FF,$FF        ; motion-tracker results (scan at $9707): entries for
                          ; Tracker #1/#2 and the carried cat = the holder's
                          ; room when nothing moves next door (+1 morale for
                          ; actors there, tested at $8E30), or 255 = motion
                          ; detected (beep / uneasy cat)
NearestCrewC:
  DEFB $FF                ; third nearest-crew slot; $839F-$83A1 are also
                          ; iterated as a 3-list
NearestCrewA:
  DEFB $FF                ; nearest crew ID, slot A (255 = none)
NearestCrewB:
  DEFB $FF                ; nearest crew ID, slot B (255 = none)
DistanceAccum:
  DEFB $00                ; running distance/count accumulator for the
                          ; nearest-crew scan
SoundRequest:
  DEFB $00                ; One-shot sound request (255 = idle)
CrewIndex:
  DEFB $01                ; Current crew member index (0-6)
AlienTargetPtr:
  DEFB $86,$73            ; pointer to the alien-target / Android ActorRecords
                          ; slot
AlienTargetID:
  DEFB $09                ; In the Long Game this is the **Android** slot index
                          ; (1..7): the dormant traitor crew member that
                          ; UpdateAlien (UpdateAlien) activates on first
                          ; encounter with the alien (the first frame
                          ; AlienActiveFlag is set and the slot still has its
                          ; template +0=+7=0). Once activated, the Android
                          ; enters action state 7 and wounds nearby crew via
                          ; CrewAction7_Handler (CrewAction7_Handler). Values
                          ; >= 8 (e.g. ShortGameInit's hard-wired 9) disable
                          ; the mechanic — TriggerAlienEvent
                          ; (TriggerAlienEvent) treats them as "no valid
                          ; target".
AlienActiveFlag:
  DEFB $00                ; 255 once the alien has taken 6+ wounds while the
                          ; Android is still dormant (TriggerAlienEvent
                          ; TriggerAlienEvent); gates all Android hostility
SoundPending:
  DEFB $00                ; SoundPending: nonzero = a sound effect is queued;
                          ; PlayMusic (PlayMusic) plays and clears it
JonesRoom:
  DEFB $06                ; Jones the cat's current room (255 = off-map)
JonesMoveTimer:
  DEFB $FF                ; frames until Jones's next move
MsgDrawnCol:
  DEFB $00                ; message line: column reached by the renderer (bit 7
                          ; set)
MsgVisibleTimer:
  DEFB $00                ; frames the current message stays on screen
MsgAttrPtr:
  DEFB $E0,$5A            ; pointer into the message line's attribute row
                          ; ($5AE0)
MsgParam1:
  DEFB $FF                ; current message parameter 1 (254 = actor name, 252
                          ; = room name)
MsgParam2:
  DEFB $FF                ; current message parameter 2 (253 = target name)
MessageQueue:
  DEFB $00,$00,$00        ; message queue: 3-byte entries (msg#, param1,
                          ; param2),
MessageQueueBody:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00,$00 ; ...rest of the
                                                               ; queue buffer
                                                               ; (write ptr at
                                                               ; $83CD)
SoundReqFlag:
  DEFB $00,$00,$00        ; Sound request word (3 bytes incl. frequency /
                          ; duration)
SprScreenAddrA:
  DEFB $00,$00            ; Screen destination for crew sprite A
SprScreenAddrB:
  DEFB $00                ; Screen destination low byte for crew sprite B
MusicActiveFlag:
  DEFB $00                ; Non-zero when background music phrase is playing
Unused83CA:
  DEFB $FF
AnimFrameCnt:
  DEFB $FF                ; Frame counter for crew sprite animation timing
Unused83CC:
  DEFB $FF
MsgQueueWritePtr:
  DEFB $B2,$83            ; message-queue write pointer (starts at MessageQueue
                          ; $83B2)
MsgLastEnqueued:
  DEFB $FF,$FF            ; last enqueued (msg#, param1) pair - suppresses
                          ; duplicates
AlienKillThreshold:
  DEFB $0F                ; wounds needed to kill the alien: CrewHitsAlien
                          ; compares the alien's +4 wound counter against this
                          ; and fires the endgame ($AC5D) on reaching it
ItemCharges:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00 ; per-item charge counters, items 0-7
                                       ; (prods, incinerators, trackers):
                                       ; consumed at $9344
ItemChargesHi:
  DEFB $03,$03,$03,$03,$01,$08,$08,$08 ; ...items 8-15: extinguishers 3
                                       ; charges, Harpoon Gun 1, laser pistols
                                       ; 8
JonesCatchBySlot:
  DEFB $00,$0E,$0E,$0D,$0D,$0D,$0F,$0E ; per-slot Get Jones threshold: the
                                       ; catch succeeds when the next script
                                       ; nibble beats it ($8C13; -4 when using
                                       ; the Net rather than the Cat Box)
GrilleTimeBySlot:
  DEFB $00,$64,$78,$B4,$64,$B4,$50,$50 ; per-slot RmveGrille base time in
                                       ; frames (StartGrilleRemoval $8BD3):
                                       ; Dallas 100, Kane 120, Ripley 180, Ash
                                       ; 100, Lambert 180, Parker/Brett 80
ActionTimeByStrength:
  DEFB $00,$00,$30,$10,$00,$00,$00,$00 ; SetActionTimer ($8BE6) adjustment by
                                       ; strength (IX+4)&7: Wounded (2) +48,
                                       ; strength 3 +16, else +0
ActionTimeBySlot:
  DEFB $00,$07,$05,$00,$07,$03 ; SetActionTimer per-slot adjustment. Only 6
                               ; bytes: slots 6-7 read past the end into
                               ; ActionTimeTail (10 and 5)

; ActionTimeBySlot overflow — the values for slots 6 and 7
;
; Read only by SetActionTimer's per-slot lookup running past the 6-byte
; ActionTimeBySlot table above: Parker +10, Brett +5.
ActionTimeTail:
  DEFB $0A,$05

; RoomDispatchTable
;
; Word table with a clever triple use. The action key (HandleInput at $881B)
; dispatches table[room_mode] with room_mode from RoomModeByte: 0-1
; RoomModeDispatch   (RoomModeDispatch) 2-3  LocationListAction
; (LocationListAction) — Indicate Location list, pages 1 (rooms 0-16) and 2
; (rooms 17-33) 4    GetItemPick        (GetItemPick) — item picked from the
; Get strip For modes 0 and 1 RoomModeDispatch then re-dispatches on the cursor
; ROW through overlapping halves of this same table (entry 5 onward = base
; $840C, entry 23 onward = base $8430):
;
; Mode-0 rows (orders screen), base $840C: 0     "Order:" header       ->
; RoomHandlerNop (RoomHandlerNop) 1-7   crew slots 1-7        ->
; OpenCrewRoomView (OpenCrewRoomView) 8     " Indicate "          ->
; IndicateLocation (IndicateLocation): opens the room-location list (mode 2) 9
; " Location "          -> nop (2nd row of the 2-row label) 10-11 " Display
; "/"  Level"-> nop (header for the deck rows) 12-14 Upper/Middle/Lower    ->
; SelectDeckMap (SelectDeckMap): deck map 15-18 blank                 -> nop
;
; Mode-1 rows (room view / action menu), base $8430: 0     "move to:" header
; -> nop 1     "Grille" (open duct)  -> MoveThroughGrille (MoveThroughGrille)
; 2-6   exit rooms            -> MoveToRoom (MoveToRoom) 7     " use:" header
; -> nop 8     front-hand item       -> RoomHandlerNop2 (RoomHandlerNop2); but
; the pre-dispatch hook ($B2C0) zeroes the actor's action countdown — pressing
; the held-item row cancels a pending timed job 9     back-hand item        ->
; SwapHeldItems (SwapHeldItems) 10    "Get Item"            -> GetItemMenu
; (GetItemMenu) 11    "Leave Item"          -> LeaveItemHandler
; (LeaveItemHandler) 12    "Special" header      -> nop 13    special action
; -> $8BCB -> SpecialRowAction ($B306): 52 ATTACK / 49 RmveGrille / 238 =
; RmveGrille swapped to ATTACK under the cursor (hostile in the room — see
; PreDispatchHook $B2B9) 14    "Get Jones"           -> GetJonesHandler
; (GetJonesHandler): needs the Net or the Cat Box 15-16 fixture actions
; -> FixtureAction (FixtureAction) 17    "   QUIT   "          -> QuitRoomView
; (QuitRoomView) 18    blank                 -> nop (reuses
; CrewActionDispatch+0)
;
; The mode-1 row words for rows 11-17 are the "RoomDispatchTailExtra" bytes at
; RoomDispatchTailExtra; row 18 overlaps CrewActionDispatch's first entry.
RoomDispatchTable:
  DEFW RoomModeDispatch,RoomModeDispatch,LocationListAction,LocationListAction
  DEFW GetItemPick,RoomHandlerNop,OpenCrewRoomView,OpenCrewRoomView
  DEFW OpenCrewRoomView,OpenCrewRoomView,OpenCrewRoomView,OpenCrewRoomView
  DEFW OpenCrewRoomView,IndicateLocation,RoomHandlerNop,RoomHandlerNop
  DEFW RoomHandlerNop,SelectDeckMap,SelectDeckMap,SelectDeckMap
  DEFW RoomHandlerNop,RoomHandlerNop,RoomHandlerNop,RoomHandlerNop
  DEFW MoveThroughGrille,MoveToRoom,MoveToRoom,MoveToRoom
  DEFW MoveToRoom,MoveToRoom,RoomHandlerNop,RoomHandlerNop2
  DEFW SwapHeldItems,GetItemMenu

; Dispatch-table tails and message text
;
; Three back-to-back data tables (once misclassified as a code routine,
; "GetCorridorTableEntry"):
;
; RoomDispatchTailExtra (14 bytes, 7 × 16-bit pointers) —
; RoomDispatchTailExtra, the tail of the RoomDispatchTable at
; RoomDispatchTable: the mode-1 row words for rows 11-17 (see that table's
; header), sharing the same small handler pool. CrewActionDispatch (16 bytes, 8
; × 16-bit pointers) — CrewActionDispatch, indexed by crew/alien action state
; in (IX+3). Action 0 returns early; actions 1-7 jump to the matching handler.
; Used by the routine at DispatchCrewAction. MessageTextTable (744 bytes) —
; MessageTextTable, 34 strings terminated by byte 255. Substitution codes
; inside the strings: 254 = actor name, 253 = target name, 252 = room name, 145
; = apostrophe glyph. Walked and rendered by the routine at RenderMessage.
;
; Triggers (message # -> firing condition): 0  Jones enters a living crew
; member's room (UpdateJones) 1  alien attack tick, victim strength still >= 4
; 2  Get Jones action succeeds (JonesRoom := 255 = carried) 3/4  crew nets /
; hits the alien (CrewHitsAlien outcomes) 5/6/7  weapon spent: Tracker /
; extinguisher (also fire handler with no charges) / Laser 8  room damage
; event, cell value 55 9  fire breaks out in a room 10 alien attack tick,
; victim strength 2-3 11/12 hypersleep pod entered (HypersleepStart) /
; completed (CrewAction6_Handler) 13/14 destruct lever armed / aborted
; (DestructArm/DestructAbort) 15 engine-room fire fully extinguished 16/17
; airlock blown (BlowLock) / resealed (SealLock) 18 alien re-enters via
; ShuttleBay after surviving an airlock blow 19 actor stands beside Jones's
; strip cells 20/21 20 actor shares a room with a dead crew member 21 victim
; strength exhausted - collapse 22/23/24/25 accumulated room damage crosses the
; '3x' threshold in CommdCentr / Computer / Life Suppt / Narcissus
; (AddRoomDamage AddRoomDamage, damage digits at RoomDamageDigits) 26 oxygen
; clock reaches "05:00" 27/28 launch check: Jones missing / crew not all aboard
; (LaunchGate LaunchGate) 29/30/31/32 Android combat set (UpdateAlien /
; CrewHitsAndroid) 33 Long Game opening: chestburster host named (LongGameInit)
RoomDispatchTailExtra:
  DEFW LeaveItemHandler,RoomHandlerNop ; rows 11-12
  DEFW SpecialRowJump,GetJonesHandler ; rows 13-14
  DEFW FixtureAction,FixtureAction,QuitRoomView ; rows 15-17
CrewActionDispatch:
  DEFW RoomHandlerNop,CrewAction1_Arrive
  DEFW CrewAction2_DuctTransit,CrewAction3_RemoveGrille
  DEFW CrewAction4_Handler,CrewAction5_AlienAttack
  DEFW CrewAction6_Handler,CrewAction7_Handler
; #0: "{actor} Sees Jones the Cat"
MessageTextTable:
  DEFB $FE
  DEFM " Sees Jones the Cat"
  DEFB $FF
; #1: "The ALIEN is attacking {actor}"
  DEFM "The ALIEN is attacking "
  DEFB $FE
  DEFB $FF
; #2: "{actor} has captured Jones"
  DEFB $FE
  DEFM " has captured Jones"
  DEFB $FF
; #3: "{actor} nets the ALIEN"
  DEFB $FE
  DEFM " nets the ALIEN"
  DEFB $FF
; #4: "{actor} hits the ALIEN"
  DEFB $FE
  DEFM " hits the ALIEN"
  DEFB $FF
; #5: "{actor}'s Tracker is broken"
  DEFB $FE,$91
  DEFM "s Tracker is broken"
  DEFB $FF
; #6: "{actor}'s extinguisher is empty"
  DEFB $FE,$91
  DEFM "s extinguisher is empty"
  DEFB $FF
; #7: "{actor}'s Laser is exhausted"
  DEFB $FE,$91
  DEFM "s Laser is exhausted"
  DEFB $FF
; #8: "Structural Damage in {room}"
  DEFM "Structural Damage in "
  DEFB $FC
  DEFB $FF
; #9: "WARNING:Fire in {room}"
  DEFM "WARNING:Fire in "
  DEFB $FC
  DEFB $FF
; #10: "{actor} is being wounded"
  DEFB $FE
  DEFM " is being wounded"
  DEFB $FF
; #11: "{actor} prepares for hypersleep"
  DEFB $FE
  DEFM " prepares for hypersleep"
  DEFB $FF
; #12: "{actor} enters hypersleep"
  DEFB $FE
  DEFM " enters hypersleep"
  DEFB $FF
; #13: "DESTRUCT SEQUENCE INITIATED"
  DEFM "DESTRUCT SEQUENCE INITIATED"
  DEFB $FF
; #14: "DESTRUCT SEQUENCE ABORTED"
  DEFM "DESTRUCT SEQUENCE ABORTED"
  DEFB $FF
; #15: "Fire extinguished in {room}"
  DEFM "Fire extinguished in "
  DEFB $FC
  DEFB $FF
; #16: "WARNING:{room}DEPRESSURISED"
  DEFM "WARNING:"
  DEFB $FC
  DEFM "DEPRESSURISED"
  DEFB $FF
; #17: "{room} PRESSURE RESTORED"
  DEFB $FC
  DEFM " PRESSURE RESTORED"
  DEFB $FF
; #18: "INTRUDER ENTERING {room}"
  DEFM "INTRUDER ENTERING "
  DEFB $FC
  DEFB $FF
; #19: "Jones is looking uneasy"
  DEFM "Jones is looking uneasy"
  DEFB $FF
; #20: "{actor} finds {target}'s body"
  DEFB $FE
  DEFM " finds "
  DEFB $FD,$91
  DEFM "s body"
  DEFB $FF
; #21: "{actor} has collapsed"
  DEFB $FE
  DEFM " has collapsed"
  DEFB $FF
; #22: "Partial loss of control systems"
  DEFM "Partial loss of control systems"
  DEFB $FF
; #23: "Computer Malfunction detected"
  DEFM "Computer Malfunction detected"
  DEFB $FF
; #24: "Environmental irregularities"
  DEFM "Environmental irregularities"
  DEFB $FF
; #25: "Narcissus status RED"
  DEFM "Narcissus status RED"
  DEFB $FF
; #26: "Oxygen status CRITICAL"
  DEFM "Oxygen status CRITICAL"
  DEFB $FF
; #27: "BioUnit Jones not present"
  DEFM "BioUnit Jones not present"
  DEFB $FF
; #28: "LAUNCH COUNTERMANDED"
  DEFM "LAUNCH COUNTERMANDED"
  DEFB $FF
; #29: "{target} is attacking {actor}"
  DEFB $FD
  DEFM " is attacking "
  DEFB $FE
  DEFB $FF
; #30: "{actor} is attacking {target}"
  DEFB $FE
  DEFM " is attacking "
  DEFB $FD
  DEFB $FF
; #31: "{actor} nets {target}"
  DEFB $FE
  DEFM " nets "
  DEFB $FD
  DEFB $FF
; #32: "{actor} hits the Android" Enqueued by CrewHitsAndroid
; (CrewHitsAndroid). The "Android" is the long-game AlienTargetID slot
; (AlienTargetID) — the dormant traitor crew member that UpdateAlien
; (UpdateAlien) activates on first encounter with the alien. The actor
; placeholder is filled from CrewIndex (CrewIndex) = the slot currently being
; dispatched in combat state 4 — whichever crew member stands in the Android's
; room when the ATTACK order fires (CrewAction4_Handler matches on the room
; byte).
  DEFB $FE
  DEFM " hits the Android"
  DEFB $FF
; #33: "Alien has hatched from {actor}"
  DEFM "Alien has hatched from "
  DEFB $FE
  DEFB $FF

; ScanInput — read the selected input device
;
; Per-frame input shim: calls whichever device scanner OptionsScreen selected
; (the CALL operand at $874D is patched from the vector table at $A405) to pack
; the active directional / fire controls into AlienFlags. Bit 4 = fire/action,
; bit 3 = right (key 8), bit 2 = left (key 5), bit 1 = up (key 6), bit 0 = down
; (key 7). Used by the routine at HandleInput (HandleInput).
ScanInput:
  CALL DefaultKeyScanner  ; scan into AlienFlags via the selected device's
  RET                     ; scanner (the CALL operand at $874D is patched from
                          ; InputScannerTable by OptionsScreen)

; DefaultKeyScanner
DefaultKeyScanner:
  LD HL,AlienFlags        ; HL points to AlienFlags
  LD (HL),$00             ; start from "no keys pressed"
  LD BC,$F7FE             ; port $F7FE: half-row 1-5
  IN A,(C)                ; read half-row 1-5
  LD B,$EF                ; next read: port $EFFE, half-row 6-0
  BIT 4,A                 ; key 5 pressed? (active low)
  JR NZ,KeyScan_LowRow
  SET 1,(HL)              ; bit 1 = key 5
KeyScan_LowRow:
  IN A,(C)                ; read half-row 6-0
  BIT 2,A                 ; key 8?
  JR NZ,KeyScan_Check7
  SET 0,(HL)              ; bit 0 = key 8
KeyScan_Check7:
  BIT 3,A                 ; key 7?
  JR NZ,KeyScan_Check6
  SET 2,(HL)              ; bit 2 = key 7
KeyScan_Check6:
  BIT 4,A                 ; key 6?
  JR NZ,KeyScan_Check0
  SET 3,(HL)              ; bit 3 = key 6
KeyScan_Check0:
  BIT 0,A                 ; key 0 (action)?
  RET NZ                  ; not pressed: done
  SET 4,(HL)              ; bit 4 = action
  RET

; HandleInput
;
; Reads the keyboard and moves the corridor cursor. Calls the key-scanner at
; ScanInput, which packs five keys into AlienFlags (set bit = pressed): bit 0 =
; key 8, bit 1 = key 5, bit 2 = key 7, bit 3 = key 6, bit 4 = key 0 (action).
; While the action key is held the border flashes yellow/black; on RELEASE
; control jumps to Input_ActionRelease, which dispatches through
; RoomDispatchTable (RoomDispatchTable) on the current room mode (RoomModeByte)
; — this is how a room's action handler / menu is opened. Otherwise: key 6 ->
; AdvanceCursor (advance the cursor along the corridor), key 7 ->
; Input_RetreatCursor (retreat), and keys 8/5 move between rows when the room
; view has more than one row (mode != 0).
;
; Used by the routine at GameEntry.
HandleInput:
  CALL ScanInput          ; scan keyboard → CrewStatus_Done bitmask
  LD A,(AlienFlags)       ; A = key bitmask (CrewStatus_Done)
  AND A                   ; any key pressed?
  RET Z                   ; no: return immediately
  BIT 4,A                 ; action/fire key held?
  JR Z,Input_Directional  ; no: check directional keys
  XOR A                   ; A = 0 (border black)
Input_FlashLoop:
  EX AF,AF'               ; save border colour to A'
  CALL ScanInput          ; re-scan keyboard (wait for release)
  LD A,(AlienFlags)       ; read updated key state
  BIT 4,A                 ; action key still held?
  JP Z,Input_ActionRelease ; released: open the room-action dispatch
  EX AF,AF'               ; restore A' border colour
  XOR $06                 ; toggle between 0 (black) and 6 (yellow)
  OUT ($FE),A             ; flash border
  JR Input_FlashLoop      ; repeat until key released
Input_Directional:
  BIT 3,A                 ; key 6 (advance)?
  JR NZ,AdvanceCursor     ; yes: advance cursor along the corridor
  BIT 2,A                 ; key 7 (retreat)?
  JR NZ,Input_RetreatCursor ; yes: retreat cursor along the corridor
  AND $03                 ; key 5 or key 8 (row movement)?
  RET Z                   ; no directional key: return
  LD A,(RoomModeByte)     ; A = room mode (RoomModeByte)
  AND A                   ; mode 0 = single-row corridor (no up/down)
  RET Z                   ; in single-row mode: up/down keys ignored
Input_RowMove:
  LD A,(CursorCellValue)  ; A = cell value under the cursor ($8395)
  CP $3D                  ; column 61 = leftmost valid position
  RET Z                   ; at left wall: cannot move
  CALL AdvanceCursor      ; move one step (shares forward-move code)
  JR Input_RowMove        ; keep moving while at edge

; AdvanceCursor
;
; Move the cursor one strip row forward (key 6), skipping cells that are not
; selectable; also holds the retreat handler (key 7) and the action-key
; dispatch. Cell values steer the movement: >= 82 is header text / empty
; padding (never a landing spot), 70-81 are two-row fixture labels whose second
; row must be stepped over.
;
; Used by the routines at HandleInput, SetActionTimer, UpdateJones and
; StartAttack.
AdvanceCursor:
  CALL UnhighlightMenuRow ; remove the highlight from the current row
  LD HL,CorridorCursor    ; HL -> cursor position
  LD A,(CursorCellValue)  ; A = cell value the cursor is leaving
  CP $46                  ; leaving a two-row fixture label?
  JR C,AdvCursor_Bounds
  INC (HL)                ; yes: step over its second row too
AdvCursor_Bounds:
  LD HL,CorridorCursor
  LD A,(HL)               ; A = cursor position
  CP $12                  ; already on the last strip row?
  JR C,AdvCursor_Store
  LD A,$FF                ; yes: wrap so the INC restarts at row 0
AdvCursor_Store:
  INC A                   ; step forward one row
  LD (HL),A
  LD D,$00
  LD E,A
  LD HL,CorridorPosTable  ; index CorridorPosTable by the new position
  ADD HL,DE
  LD A,(HL)               ; A = the cell value there
  CP $52                  ; header text / empty padding?
  JR NC,AdvCursor_Bounds  ; yes: not selectable, keep stepping
  CALL HighlightMenuRow   ; highlight the new row, mirror its cell value
  CALL UpdateRoomMarkerB  ; re-place the blinking box if it is a room row
  RET
Input_RetreatCursor:
  CALL UnhighlightMenuRow ; remove the highlight from the current row
Retreat_Step:
  LD A,(CorridorCursor)   ; A = cursor position
  AND A                   ; at the top row (0)?
  JR NZ,Retreat_Wrap
  LD A,$13                ; yes: wrap so the DEC restarts at row 18
Retreat_Wrap:
  DEC A                   ; step back one row
  LD (CorridorCursor),A
  LD D,$00
  LD E,A
  LD HL,CorridorPosTable  ; index CorridorPosTable by the new position
  ADD HL,DE
  LD A,(HL)               ; A = the cell value there
  CP $52                  ; header text / empty padding?
  JR NC,Retreat_Step      ; yes: not selectable, keep stepping
  CP $46                  ; a two-row fixture label?
  JR C,Retreat_Done       ; no: done
  DEC HL                  ; peek at the cell one row up
  LD A,(HL)
  CP $46                  ; is it also part of a two-row label?
  JR C,Retreat_Done       ; no: we are already on the first row
  CP $52
  JR NC,Retreat_Done
  LD HL,CorridorCursor    ; we landed on the label's second row:
  DEC (HL)                ; back up onto its first row
Retreat_Done:
  CALL HighlightMenuRow   ; highlight the new row, mirror its cell value
  CALL UpdateRoomMarkerB  ; re-place the blinking box if it is a room row
  RET
Input_ActionRelease:
  XOR A                   ; action key released:
  OUT ($FE),A             ; restore black border
  LD HL,RoomDispatchTable ; RoomDispatchTable
  LD A,(RoomModeByte)     ; A = current room mode
  LD E,A
  LD D,$00
  ADD HL,DE
  ADD HL,DE               ; HL = table + mode*2
  LD A,(HL)               ; fetch the handler address
  INC HL
  LD H,(HL)
  LD L,A
  JP (HL)                 ; jump to the room's action handler

; UnhighlightMenuRow
;
; Undo the cursor highlight — restore the saved attribute (SavedRowAttr) on the
; row under the cursor; two rows when the cell value is >= 70 (first word of a
; two-row label).
UnhighlightMenuRow:
  LD A,(CorridorCursor)   ; A = cursor row
  CALL GetMenuRowAttrAddr ; HL = its attribute address
  LD A,(CursorCellValue)  ; cell value >= 70 (two-row label)?
  LD C,$01                ; C = number of rows to repaint
  CP $46
  JR C,UnhighlightMenuRow_Restore
  INC C                   ; yes: restore two rows
UnhighlightMenuRow_Restore:
  LD A,(SavedRowAttr)     ; repaint the saved attribute
  CALL FillAttrColumn     ; FillAttrColumn over C rows
  RET

; HighlightMenuRow
;
; Used by the routines at OrdersMenuNextRow, DrawSelectedName, AdvanceCursor,
; DrawLocationList and GetItemMenu.
;
; Mirror the CorridorPosTable cell under the cursor into CursorCellValue
; ($8395), save the attribute under the cursor row into SavedRowAttr, then
; paint the row bright white (attr 120) — two rows when the cell value is >= 70
; (two-row label).
HighlightMenuRow:
  LD A,(CorridorCursor)   ; A = cursor row
  PUSH AF
  LD E,A
  LD D,$00
  LD HL,CorridorPosTable  ; index CorridorPosTable by the row
  ADD HL,DE
  LD A,(HL)               ; A = cell value under the cursor
  LD (CursorCellValue),A  ; mirror it for the dispatchers
  EX AF,AF'               ; keep the cell value in A'
  POP AF                  ; A = cursor row again
  CALL GetMenuRowAttrAddr ; HL = the row's attribute address
  LD A,(HL)
  LD (SavedRowAttr),A     ; save the attribute for the un-highlight
  EX AF,AF'               ; cell value >= 70 (two-row label)?
  LD C,$01                ; C = number of rows to paint
  CP $46
  JR C,HighlightMenuRow_Paint
  INC C                   ; yes: highlight two rows
HighlightMenuRow_Paint:
  LD A,$78                ; bright white paper, black ink
  CALL FillAttrColumn     ; FillAttrColumn over C rows
  RET

; GetMenuRowAttrAddr
;
; HL = attribute address of menu row A — the strip rows sit on screen rows
; 1-19, columns 22-31, so row 0 -> $5836 (22582) and each further row adds 32.
GetMenuRowAttrAddr:
  LD HL,$5836             ; attr addr of menu row 0 (screen row 1, col 22)
  AND A                   ; row 0?
  RET Z                   ; yes: HL is already right
  LD DE,$0020             ; one attribute row per menu row
  LD B,A                  ; B = rows still to skip
GetMenuRowAttrAddrLoop:
  ADD HL,DE               ; +32 per row
  DJNZ GetMenuRowAttrAddrLoop
  RET

; UpdateRoomMarkerB
;
; Cursor-move hook, called from HandleInput's cursor handlers. Erases the
; blinking candidate-room box (marker channel B), then, in the room view only,
; re-places it if the row now under the cursor is a room (an exit row, value <
; 34) that lies on the deck currently shown in the map pane.
UpdateRoomMarkerB:
  CALL EraseRoomMarkerB   ; erase/freeze marker channel B
  LD A,(RoomModeByte)     ; room view (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(CursorCellValue)  ; cell under the cursor a room id?
  CP $22                  ; (rooms are 0-33)
  RET NC                  ; no: done
  LD E,A
  LD D,$00
  LD HL,RoomTypeTable     ; RoomTypeTable[room]
  ADD HL,DE
  LD A,(RoomTypeByte)     ; on the deck currently displayed?
  CP (HL)
  RET NZ                  ; no: done
  LD A,(CursorCellValue)  ; yes: blink that room's indicator box
  CALL PlaceRoomMarkerB   ; via PlaceRoomMarkerB
  RET

; AnimateCrewA
;
; Animates marker channel A — the walking crew figure that marks the viewed
; room on the deck map (placed by PlaceRoomMarkerA PlaceRoomMarkerA). Skips if
; bit 7 of the animation-freeze flag at CrewAnimFlags is set. Decrements the
; frame counter at AnimFrameCnt; when it reaches zero, resets the counter to 10
; and XOR-blits two consecutive 3×12-byte sprite frames from the animation
; table at CrewWalkAnimA (indexed by the frame phase in CrewAnimFlags bits 0-1)
; onto the display at the address stored in SprDestA (SprDestA). Calling this
; routine twice per frame (once to erase, once to draw the next frame) produces
; flicker-free sprite animation via double-buffered XOR.
;
; Used by the routines at GameEntry, UpdateAlienAnim and IntroductionMode.
AnimateCrewA:
  LD A,(CrewAnimFlags)    ; A = animation-freeze / frame-phase byte
                          ; (CrewAnimFlags)
  BIT 7,A                 ; bit 7 = freeze flag
  RET NZ                  ; frozen: skip animation this frame
  LD HL,FrameCounterA     ; HL = frame counter A (AnimFrameCnt)
  DEC (HL)                ; tick down counter
  RET NZ                  ; not expired: nothing to do
  LD (HL),$0A             ; reset counter to 10 frames per phase
  LD B,$02                ; draw 2 sprite rows
; This entry point is used by the routines at PlaceRoomMarkerA and
; IntroductionMode.
BlitMarkerA:
  PUSH BC                 ; save row counter
  LD E,A                  ; E = frame phase (0-3)
  LD D,$00
  LD HL,CrewWalkAnimA     ; HL = base of crew-A animation table (CrewWalkAnimA)
  ADD HL,DE               ; index by phase × 2
  ADD HL,DE
  LD A,(HL)               ; read 16-bit sprite bitmap pointer
  INC HL
  LD H,(HL)
  LD L,A                  ; HL = pointer to 3-byte sprite row data
  EX DE,HL                ; DE = sprite data source
  LD HL,(SprDestA)        ; HL = screen destination address (SprDestA)
  CALL XorBlitMarker      ; XOR-blit 3 bytes × 12 rows onto screen
  POP BC                  ; restore row counter
  DEC B
  RET Z                   ; done after 2 rows
  LD A,(CrewAnimFlags)    ; advance to next frame phase
  INC A
  AND $03                 ; wrap phase 0-3
  LD (CrewAnimFlags),A    ; store updated phase
  JR BlitMarkerA          ; blit second sprite row

; AnimateCrewB
;
; Animates marker channel B — the blinking box cursor on a candidate room
; (placed by PlaceRoomMarkerB PlaceRoomMarkerB). Identical logic to
; AnimateCrewA (AnimateCrewA) but uses a faster frame rate (counter resets to 5
; rather than 10), the B-set animation table at CrewWalkAnimB, and the
; B-channel screen address at SpriteDestAddr (SpriteDestAddr). Cycles through
; four cursor shapes (RoomCursor0-3) at twice the speed of the crew walk
; animation.
;
; Used by the routines at GameEntry, UpdateAlienAnim and IntroductionMode.
AnimateCrewB:
  LD A,(AnimFramePhase)   ; A = animation-freeze / frame-phase byte
                          ; (AnimFramePhase)
  BIT 7,A                 ; bit 7 = freeze flag
  RET NZ                  ; frozen: skip
  LD HL,FrameCounterB     ; HL = frame counter B ($83CA)
  DEC (HL)                ; tick down counter
  RET NZ                  ; not expired
  LD (HL),$05             ; reset to 5 frames per phase (2× speed of set A)
  LD B,$02                ; draw 2 sprite rows
; This entry point is used by the routines at PlaceRoomMarkerB,
; EraseRoomMarkerB and IntroductionMode.
BlitMarkerB:
  PUSH BC
  LD E,A                  ; E = frame phase (0-3)
  LD D,$00
  LD HL,CrewWalkAnimB     ; base of crew-B animation table (CrewWalkAnimB)
  ADD HL,DE               ; index by phase × 2
  ADD HL,DE
  LD A,(HL)               ; read 16-bit sprite bitmap pointer
  INC HL
  LD H,(HL)
  LD L,A                  ; HL = pointer to sprite row data
  EX DE,HL                ; DE = sprite data source
  LD HL,(SpriteDestAddr)  ; HL = screen destination address (SpriteDestAddr)
  CALL XorBlitMarker      ; XOR-blit 3 bytes × 12 rows onto screen
  POP BC
  DEC B
  RET Z
  LD A,(AnimFramePhase)   ; advance frame phase
  INC A
  AND $03                 ; wrap 0-3
  LD (AnimFramePhase),A
  JR BlitMarkerB

; XorBlitMarker
;
; Used by the routines at AnimateCrewA and AnimateCrewB.
XorBlitMarker:
  LD C,$0C
XorBlit_RowLoop:
  LD B,$03
XorBlit_ByteLoop:
  LD A,(DE)
  XOR (HL)
  LD (HL),A
  INC HL
  INC DE
  DJNZ XorBlit_ByteLoop
  DEC HL
  DEC HL
  DEC HL
  PUSH DE
  CALL NextScanLine
  POP DE
  DEC C
  JR NZ,XorBlit_RowLoop
  RET

; GetRoomMarkerAddr
;
; A = room id (0-33) -> HL = display-file address of the room's indicator box
; on its deck map, from RoomMarkerAddrTable (RoomMarkerAddrTable).
GetRoomMarkerAddr:
  ADD A,A
  LD D,$00
  LD E,A
  LD HL,RoomMarkerAddrTable ; RoomMarkerAddrTable + room*2
  ADD HL,DE
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A                  ; HL = the room's marker screen address
  RET

; PlaceRoomMarkerA
;
; Used by the routine at RedrawRoomScene.
;
; Aim marker channel A (the walking crew figure that marks the viewed room on
; the deck map) at the current room (CurrentRoom) and draw its first frame; no
; marker on the Narcissus (deck type 3). Channel A then keeps animating via
; AnimateCrewA (AnimateCrewA).
PlaceRoomMarkerA:
  LD A,(RoomTypeByte)     ; on the Narcissus (type 3)?
  CP $03
  RET NC                  ; yes: no marker
  LD A,(CurrentRoom)      ; A = the viewed room
  CALL GetRoomMarkerAddr
  LD (SprDestA),HL        ; channel A dest = its marker address
  LD HL,CrewAnimFlags
  RES 7,(HL)              ; unfreeze channel A
  LD A,(HL)               ; A = current phase
  LD B,$01
  CALL BlitMarkerA        ; XOR-blit one frame (place the box)
  RET

; PlaceRoomMarkerB
;
; Used by the routines at UpdateRoomMarkerB and LocationListAction.
;
; Aim marker channel B (the "candidate room" blinker) at room A and draw its
; first frame. Used for the exit row under the cursor in the room view
; (UpdateRoomMarkerB UpdateRoomMarkerB) and for a room picked from the Indicate
; Location list (LocationListAction LocationListAction). Channel B then keeps
; blinking via AnimateCrewB (AnimateCrewB).
PlaceRoomMarkerB:
  CALL GetRoomMarkerAddr  ; HL = marker address of room A
  LD (SpriteDestAddr),HL  ; channel B dest = it
  LD HL,AnimFramePhase
  RES 7,(HL)              ; unfreeze channel B
  LD A,(HL)               ; A = current phase
  LD B,$01
  CALL BlitMarkerB        ; XOR-blit one frame (place the box)
  RET

; EraseRoomMarkerB
;
; Used by the routine at UpdateRoomMarkerB.
;
; If marker channel B is live, XOR-blit its current frame once more (which
; removes it from the screen) and freeze the channel.
EraseRoomMarkerB:
  LD HL,AnimFramePhase
  BIT 7,(HL)              ; already frozen?
  RET NZ                  ; yes: nothing to erase
  PUSH HL
  LD A,(HL)               ; A = current phase
  LD B,$01
  CALL BlitMarkerB        ; XOR-blit once = erase
  POP HL
  SET 7,(HL)              ; freeze the channel
  RET

; OpenCrewRoomView — orders-screen rows 1-7 (the crew names)
;
; Opens the room view for the crew member on the chosen row. The cursor row IS
; the actor slot number (1-7); it is saved as the selected slot (DrawSlotIndex)
; for everything that follows (menu dispatch, action timers, the alien-presence
; check at ForceAttackRowIfHostile). The menu cursor is parked on the action
; menu's QUIT row (17, value 61). SelectSlotByDrawIdx (SelectSlotByDrawIdx)
; then points IX at the slot's ActorRecords entry and caches its current room
; (CurrentRoom) and deck type (RoomTypeByte). Bails back to the orders screen
; (InitGameView) if the slot has been removed (IX+7 nonzero) or if the alien is
; loose and currently targeting this crew member (the encounter sequence owns
; the screen); otherwise builds the room view via RedrawRoomScene
; (RedrawRoomScene).
OpenCrewRoomView:
  LD A,(CorridorCursor)   ; A = cursor row = the actor slot picked (1-7)
  LD (DrawSlotIndex),A    ; save as the selected slot (DrawSlotIndex)
  LD A,$11
  LD (CorridorCursor),A   ; park the cursor on the QUIT row (17)
  LD A,$3D
  LD (CursorCellValue),A  ; mirrored cell value = 61 "   QUIT   " ($8395)
  CALL SelectSlotByDrawIdx ; IX = slot record; cache its room + deck type
  XOR A
  CP (IX+$07)             ; slot removed (+7 nonzero)?
  JP NZ,InitGameView      ; yes: back to the orders screen
  LD A,(AlienActiveFlag)  ; alien loose (AlienActiveFlag)?
  AND A
  JP Z,RedrawRoomScene    ; no: build the room view
  LD HL,AlienTargetID     ; HL = alien's target slot ($83C7)
  LD A,(DrawSlotIndex)    ; targeting the crew member just picked?
  CP (HL)
  JP Z,InitGameView       ; yes: bounce back to the orders screen
  CALL RedrawRoomScene    ; no: build the room view
  RET

; SelectDeckMap — orders-screen rows 12-14 ("Upper Deck"/"MiddleDeck"/ "Lower
; Deck"): redraw the map pane to show that deck. The cursor row minus 12 is
; exactly the deck's layout type (0/1/2), which is stored as RoomTypeByte and
; handed to the map-pane drawer at DrawRoomBackground.
SelectDeckMap:
  LD A,(CorridorCursor)   ; A = cursor row (12-14)
  SUB $0C                 ; -> deck layout type 0-2
  LD (RoomTypeByte),A     ; RoomTypeByte = chosen deck
  CALL DrawRoomBackground ; redraw the map pane for that deck
  RET

; IndicateLocation — orders-screen row 8 (" Indicate "/" Location ")
;
; Opens the room-location list: replaces the menu strip with a pageable list of
; room names. Page 1 (room mode 2, entered here) lists rooms 0-16; page 2 (room
; mode 3, entered at IndicateLocationPage2) lists rooms 17-33 — the Narcissus
; (34) is never listed. The 19 CorridorPosTable cells become: cell 0 = 62
; "Other List" (switches to the other page), cells 1-17 = the seventeen room
; ids, cell 18 = 61 "   QUIT   " (returns to the orders screen). The cursor
; starts on QUIT. While the list is up, the action key dispatches straight to
; LocationListAction (LocationListAction) via RoomDispatchTable entries 2/3:
; picking a room draws its deck map in the map pane with the room's indicator
; box blinking on it (marker channel B).
;
; Used by the routines at RoomDispatchTable and LocationListAction.
IndicateLocation:
  LD A,$02                ; room mode 2: page 1,
  LD E,$00                ; first room id 0
  JR BuildLocationList

; IndicateLocationPage2
;
; Used by the routine at LocationListAction.
IndicateLocationPage2:
  LD A,$03                ; room mode 3: page 2,
  LD E,$11                ; first room id 17
BuildLocationList:
  LD (RoomModeByte),A     ; set the room mode (2 or 3)
  LD A,$12
  LD (CorridorCursor),A   ; cursor to row 18 (the QUIT cell)
  LD A,$3D
  LD (CursorCellValue),A  ; mirrored cell value = 61 "   QUIT   "
  LD HL,CorridorPosTable  ; fill CorridorPosTable:
  LD (HL),$3E             ; cell 0 = 62 "Other List" (page toggle)
  INC HL
  LD B,$11
BuildLocationListLoop:
  LD (HL),E               ; cells 1-17 = room ids E..E+16
  INC HL
  INC E
  DJNZ BuildLocationListLoop
  LD (HL),$3D             ; cell 18 = 61 "   QUIT   "
  LD HL,$5836             ; colour the strip (attr rows 1-19, cols 22-31):
  LD A,$38
  LD C,$01                ; "Other List" row: black ink on white
  CALL FillAttrColumn
  LD A,$0F
  LD C,$11                ; the 17 room rows: white ink on blue
  CALL FillAttrColumn
  LD A,$38
  LD C,$01                ; QUIT row: black ink on white
  CALL FillAttrColumn
  LD HL,$4016             ; draw pointer to row 0, column 22
  LD (DrawScreenPtr),HL   ; (fall through to draw the list)

; DrawLocationList
;
; Renders the room-location list built above: the " Location " title on screen
; row 0, then the 19 CorridorPosTable cells (room names and the two control
; rows) on rows 1-19, and finally the cursor highlight via HighlightMenuRow
; (HighlightMenuRow).
DrawLocationList:
  LD A,$50                ; draw title 80 " Location " on row 0
  CALL DrawSprite
  LD HL,CorridorPosTable  ; then the 19 cells from CorridorPosTable
  LD (CorridorTilePtr),HL
  LD B,$13
DrawLocationListLoop:
  PUSH BC
  CALL DrawNextCorridorTile ; draw next cell (room name / control row)
  POP BC
  DJNZ DrawLocationListLoop
  CALL HighlightMenuRow   ; highlight the cursor row / mirror its value
  RET

; RoomModeDispatch
;
; Menu-row dispatch for room modes 0 and 1: re-dispatches the action key on the
; cursor ROW (CorridorCursor) through the second half of the same word table —
; base $840C for mode 0 (the orders screen) or $8430 for mode 1 (the room view;
; see the row list at the RoomDispatchTable header).
RoomModeDispatch:
  CALL PreDispatchHook    ; A = room mode (0 or 1 here)
  LD HL,$840C             ; mode-0 row-handler base
  LD DE,$0024
  AND A
  JR Z,DispatchOnCursorRow
  ADD HL,DE               ; mode 1: the room-view row table
DispatchOnCursorRow:
  LD A,(CorridorCursor)   ; cursor row
  LD E,A
  ADD HL,DE
  ADD HL,DE               ; base + row*2
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
  JP (HL)                 ; jump to the row's handler

; LocationListAction — action key in the room-location list (modes 2/3)
;
; Dispatched directly from RoomDispatchTable entries 2 and 3, on the cell value
; under the cursor: 61 "   QUIT   " leaves the list and rebuilds the orders
; screen (InitGameView InitGameView); 62 "Other List" switches to the other
; page (mode 2 -> page 2, mode 3 -> page 1); any other value is a room id —
; show where that room is: look up its deck in RoomTypeTable (RoomTypeTable),
; make it the displayed deck (RoomTypeByte), redraw the map pane and set the
; room's indicator box blinking on it (marker channel B via PlaceRoomMarkerB
; PlaceRoomMarkerB). The list stays up, so several rooms can be looked up in a
; row; note the displayed deck keeps its last value after QUIT.
LocationListAction:
  LD A,(CursorCellValue)  ; A = cell value under the cursor
  CP $3D                  ; "   QUIT   "?
  JP Z,InitGameView       ; yes: back to the orders screen
  CP $3E                  ; "Other List"?
  JR NZ,LocationListShowRoom
  LD A,(RoomModeByte)     ; yes: toggle page —
  CP $02                  ; on page 1 (mode 2)?
  JP Z,IndicateLocationPage2 ; build page 2
  JP IndicateLocation     ; else build page 1
LocationListShowRoom:
  LD E,A                  ; a room id was picked:
  LD D,$00
  LD HL,RoomTypeTable     ; RoomTypeTable[room]
  ADD HL,DE
  LD A,(HL)
  LD (RoomTypeByte),A     ; = the deck to display
  CALL DrawRoomBackground ; redraw the map pane for that deck
  LD A,(CursorCellValue)  ; and blink the room's indicator box
  CALL PlaceRoomMarkerB   ; on it (marker channel B)
  RET

; MoveToRoom — action on a "move to:" destination row (2-6): set the actor's
; destination room from the row's value; in the ducts the destination comes
; from RoomAdjDucts by direction slot instead.
MoveToRoom:
  LD A,$40
  CALL SetActionTimer     ; set the actor's action timer
  LD (IX+$03),$01         ; action state = 1
  LD A,(CurrentRoom)
  BIT 6,A                 ; in the duct network?
  JR NZ,MoveToRoom_Duct
  LD A,(CursorCellValue)  ; no: destination = the row's room number
  LD (IX+$02),A
  RET
MoveToRoom_Duct:
  LD A,(CorridorCursor)   ; yes: duct direction = row - 2
  SUB $02
  PUSH AF
  LD A,(CurrentRoom)
  AND $3F
  LD L,A
  LD H,$00
  PUSH HL
  ADD HL,HL
  ADD HL,HL
  POP DE
  ADD HL,DE               ; room * 5
  LD DE,RoomAdjDucts      ; + RoomAdjDucts
  ADD HL,DE
  POP AF
  LD D,$00
  LD E,A
  ADD HL,DE               ; + direction slot
  LD A,(HL)
  OR $40                  ; destination stays inside the ducts
  LD (IX+$02),A
  RET

; QuitRoomView
;
; Action on the QUIT row (17): freeze the crew animation and return to the ship
; view.
QuitRoomView:
  LD HL,CrewAnimFlags     ; freeze animation channel A
  SET 7,(HL)
  JP InitGameView         ; back to the orders/ship view

; No-op room-action handler
;
; Dispatch-table filler for rows and states that do nothing: the "Order:"
; header row (0), the blank rows, and crew action state 0 (CrewActionDispatch
; entry 0).
RoomHandlerNop:
  RET

; No-op room-action handler (second copy)
;
; Same as RoomHandlerNop; the front-hand item row (8) dispatches here.
RoomHandlerNop2:
  RET
  RET

; SwapHeldItems
;
; Action on the back-item row (9): swap the actor's front and back hands —
; exchange HeldItemFront/HeldItemBack, flip the two markers in ItemLocations
; (ItemLocations) by +/-32, adjust the sprite position by the difference of the
; two courage bonuses, and redraw the front item's name.
SwapHeldItems:
  CALL RefreshHeldItems   ; refresh HeldItemFront/Back for this actor
  LD (IX+$00),$00         ; park the actor's countdown
  LD DE,(HeldItemFront)   ; E = front item, D = back item
  LD HL,ItemCourageBonus  ; ItemCourageBonus:
  PUSH HL
  LD C,E
  LD B,$00
  ADD HL,BC
  LD E,(HL)               ; E = bonus[front]
  LD C,D
  POP HL
  ADD HL,BC
  LD D,(HL)               ; D = bonus[back]
  LD A,(IX+$05)           ; courage += bonus[back] - bonus[front]
  SUB E
  ADD A,D
  CP $7F                  ; (clamp underflow to 0)
  JR C,SwapItems_SetCourage
  XOR A
SwapItems_SetCourage:
  LD (IX+$05),A
  LD A,(IX+$06)           ; morale likewise
  SUB E
  ADD A,D
  CP $7F
  JR C,SwapItems_SetMorale
  XOR A
SwapItems_SetMorale:
  LD (IX+$06),A
  LD BC,(HeldItemFront)   ; C = front item, B = back item
  LD L,B                  ; swap HeldItemFront <-> HeldItemBack
  LD H,C
  LD (HeldItemFront),HL
  LD HL,ItemLocations     ; ItemLocations:
  PUSH HL
  LD E,C                  ; old front item's marker -32
  LD D,$00                ; (160+slot -> 128+slot: now in the back hand)
  ADD HL,DE
  LD A,(HL)
  SUB $20
  LD (HL),A
  POP HL
  LD E,B                  ; old back item's marker +32
  ADD HL,DE               ; (128+slot -> 160+slot: now in front)
  LD A,(HL)
  ADD A,$20
  LD (HL),A
  LD HL,$4836             ; redraw the " use:" rows:
  LD (DrawScreenPtr),HL
  LD A,(HeldItemFront)
  CALL ItemIdToString
  CALL DrawSprite         ; draw the new front item's name
  LD A,(HeldItemBack)
  CALL ItemIdToString     ; and the new back item's name
  CALL DrawSprite
  CALL DrawCrewCondition  ; refresh the condition line
  CALL RefreshRoomInfo    ; and the room info
  RET

; MoveThroughGrille
;
; Action on the "Grille" move-to row (1, offered once the room's grille is
; removed): send the actor through the duct opening — the destination is the
; same room with the in-ducts flag (bit 6) flipped.
;
; Used by the routines at RoomDispatchTable and PreActionCheck.
MoveThroughGrille:
  LD A,$40                ; base time 64
  CALL SetActionTimer     ; set the actor's action timer
  LD (IX+$03),$02         ; action state = 2 (duct transit)
  LD A,(CurrentRoom)
  XOR $40                 ; same room, other side of the grille
  LD (IX+$02),A
  RET

; GetItemPick
;
; Room mode 4: the "Get Item" picker strip is up (see GetItemMenu, GetItemMenu)
; and the action key fires on a row. QUIT rebuilds the room scene; otherwise
; the picked row's entry in RoomItemList (RoomItemList) is the item id to take:
; it goes into the front hand (marker 160+slot in ItemLocations
; (ItemLocations), courage/morale raised by ItemCourageBonus) if that hand is
; free, else into the back hand (128+slot).
GetItemPick:
  LD A,(CursorCellValue)  ; row value under the cursor
  CP $3D                  ; QUIT?
  JP Z,RedrawRoomScene
  LD A,(CorridorCursor)   ; cursor row
  LD E,A
  LD D,$00
  LD HL,RoomItemList      ; E = RoomItemList[row] = picked item id
  ADD HL,DE
  LD E,(HL)
  LD C,$80                ; assume back hand (marker = slot+128)
  LD A,(HeldItemFront)    ; front hand free?
  CP $FF
  JR NZ,GetItemPick_Take
  LD HL,ItemCourageBonus  ; yes: raise courage and morale
  ADD HL,DE               ; by ItemCourageBonus[item]
  LD A,(IX+$05)
  ADD A,(HL)
  LD (IX+$05),A
  LD A,(IX+$06)
  ADD A,(HL)
  LD (IX+$06),A
  LD C,$A0                ; and use the front hand (slot+160)
GetItemPick_Take:
  LD A,(DrawSlotIndex)    ; ItemLocations[item] = hand marker:
  ADD A,C                 ; the item is now carried
  LD HL,ItemLocations
  ADD HL,DE
  LD (HL),A
  JP RedrawRoomScene      ; rebuild the room scene

; GetItemMenu
;
; Action on the "Get Item" row (10): rebuild the strip as an item picker
; listing the room's items — copy RoomItemList (RoomItemList) into
; CorridorPosTable, converting each id to its name string via ItemIdToString
; (ItemIdToString) — put the cursor on the QUIT row and switch to room mode 4
; (GetItemPick above handles the selection).
GetItemMenu:
  LD (IX+$00),$00         ; clear the actor's action timer
  LD DE,CorridorPosTable  ; DE -> CorridorPosTable
  LD HL,RoomItemList      ; HL -> RoomItemList
  LD B,$11                ; copy 17 entries
GetItemMenu_CopyLoop:
  LD A,(HL)
  CP $FF                  ; 255 = empty row, copied as-is
  JR Z,GetItemMenu_StoreCell
  PUSH DE
  CALL ItemIdToString     ; item id -> name string index
  POP DE
GetItemMenu_StoreCell:
  LD (DE),A
  INC HL
  INC DE
  DJNZ GetItemMenu_CopyLoop
  LD A,$3D                ; row 17 = "   QUIT   ",
  LD ($73CF),A            ; cursor on it
  LD (CursorCellValue),A
  LD A,$11
  LD (CorridorCursor),A
  LD HL,$5836             ; menu attributes: 17 rows white,
  LD A,$0F                ; 1 row highlighted
  LD C,$11
  CALL FillAttrColumn
  LD A,$38
  LD C,$01
  CALL FillAttrColumn
  LD HL,CorridorPosTable  ; draw the 19 menu rows
  LD (CorridorTilePtr),HL
  LD HL,$4036
  LD (DrawScreenPtr),HL
  LD B,$13
GetItemMenu_DrawLoop:
  PUSH BC
  CALL DrawNextCorridorTile
  POP BC
  DJNZ GetItemMenu_DrawLoop
  LD A,$04                ; room mode = 4 (item picker)
  LD (RoomModeByte),A
  CALL HighlightMenuRow
  RET

; LeaveItemHandler
;
; Action on the "Leave Item" row (11): drop the back-hand item if there is one,
; otherwise the front-hand item (undoing its courage bonus); the dropped item's
; ItemLocations entry becomes the current room.
LeaveItemHandler:
  LD (IX+$00),$00
  LD A,(HeldItemBack)     ; back hand loaded?
  CP $FF
  JR NZ,LeaveItem_BackHand ; yes: drop the back item
  LD A,(HeldItemFront)    ; no: drop the front item —
  LD E,A
  LD D,$00
  LD HL,ItemCourageBonus  ; undo its courage bonus
  ADD HL,DE
  LD A,(IX+$05)
  SUB (HL)
  CP $7F                  ; (clamp underflow to 0)
  JR C,LeaveItem_SetCourage
  XOR A
LeaveItem_SetCourage:
  LD (IX+$05),A
  LD A,(IX+$06)
  SUB (HL)
  CP $7F
  JR C,LeaveItem_SetMorale
  XOR A
LeaveItem_SetMorale:
  LD (IX+$06),A
LeaveItem_Drop:
  LD HL,ItemLocations     ; ItemLocations[item] = current room:
  ADD HL,DE               ; the item now lies here
  LD A,(CurrentRoom)
  LD (HL),A
  CALL RedrawRoomScene    ; rebuild the room scene
  RET
LeaveItem_BackHand:
  LD E,A                  ; E = back item id (no bonus to undo)
  LD D,$00
  JR LeaveItem_Drop

; SpecialRowJump
;
; Special-row handler (row 13, from RoomDispatchTailExtra): the shim at $B306
; sorts out the 238 marker first, then re-enters below.
SpecialRowJump:
  JP SpecialRowAction

; SpecialRowNormal
;
; Used by the routine at SpecialRowAction.
SpecialRowNormal:
  CP $34                  ; "  ATTACK  " selected?
  JP Z,StartAttack        ; yes: attack handler
; This entry point is used by the routine at SpecialRowAction.
; StartGrilleRemoval: queue the timed RmveGrille job — action state 3 fires
; CrewAction3_RemoveGrille (CrewAction3_RemoveGrille) when the countdown
; expires.
StartGrilleRemoval:
  LD A,(DrawSlotIndex)    ; base time = GrilleTimeBySlot[slot]
  LD E,A                  ; (100-180 frames, crew-dependent)
  LD D,$00
  LD HL,GrilleTimeBySlot
  ADD HL,DE
  LD A,(HL)
  CALL SetActionTimer     ; set the action countdown
  LD (IX+$03),$03         ; action state = 3 (remove the grille)
  RET

; SetActionTimer
;
; Used by the routines at MoveToRoom, MoveThroughGrille, SpecialRowNormal,
; PreActionCheck and StartAttack.
;
; (IX+0) = A + ActionTimeBySlot[slot] + ActionTimeByStrength[(IX+4) & 7] — the
; shared countdown formula for every queued crew action (move, grille, attack,
; ...).
SetActionTimer:
  PUSH AF                 ; A = base time from the caller
  LD HL,ActionTimeBySlot
  LD A,(DrawSlotIndex)    ; + per-slot adjustment
  LD D,$00
  LD E,A
  ADD HL,DE
  LD C,(HL)
  LD A,(IX+$04)           ; + per-strength adjustment
  AND $07                 ; (Wounded +48, strength 3 +16, else +0)
  LD E,A
  LD HL,ActionTimeByStrength
  ADD HL,DE
  LD A,(HL)
  ADD A,C                 ; C = slot + strength adjustments
  LD C,A
  POP AF                  ; recover the caller's base time
  ADD A,C
  LD (IX+$00),A           ; arm the countdown
  RET
; GetJonesHandler — the "Get Jones" menu row (mode-1 row 14), reached through
; RoomDispatchTailExtra (RoomDispatchTailExtra). Needs the Net (item 16) or the
; Cat Box (17) in the front hand; rolls a script nibble against
; JonesCatchBySlot (JonesCatchBySlot) — the Net gets -4 on the threshold, so it
; is the easier tool. Failure spooks Jones into moving at once; success
; destroys the tool, puts "Cat in Net"/"Cat in Box" (tool+4) in the catcher's
; hand and takes Jones off the map.
GetJonesHandler:
  LD (IX+$00),$00         ; park the actor's action countdown
  LD A,(HeldItemFront)    ; front-hand item...
  CP $11                  ; ...the Cat Box?
  JR Z,GetJones_Threshold
  CP $10                  ; ...or the Net?
  RET NZ                  ; neither: cannot catch Jones
GetJones_Threshold:
  LD A,(DrawSlotIndex)    ; E = JonesCatchBySlot[slot],
  LD E,A                  ; this crew member's catch threshold
  LD D,$00
  LD HL,JonesCatchBySlot
  ADD HL,DE
  LD E,(HL)
  LD A,(HeldItemFront)    ; using the Cat Box?
  CP $11
  JR Z,GetJones_Roll
  LD A,E                  ; no, the Net: threshold -4
  SUB $04                 ; (easier to succeed)
  LD E,A
GetJones_Roll:
  CALL AdvanceScriptPtr   ; roll the next script nibble (0-15)
  CP E                    ; beat the threshold?
  JR NC,GetJones_Catch    ; yes: caught him
  LD HL,JonesMoveTimer    ; no: JonesMoveTimer = 1 —
  LD (HL),$01             ; the cat bolts,
  CALL UpdateJones        ; and UpdateJones moves him right now
  RET
GetJones_Catch:
  LD A,(HeldItemFront)    ; ItemLocations[tool] = 255:
  LD HL,ItemLocations     ; the Net / Cat Box ceases to exist...
  LD E,A
  LD D,$00
  ADD HL,DE
  LD (HL),$FF
  LD E,$04                ; ...and tool+4 ("Cat in Net" 20 /
  ADD HL,DE               ; "Cat in Box" 21) appears
  LD A,(DrawSlotIndex)
  ADD A,$A0               ; in the catcher's front hand (160+slot)
  LD (HL),A
  CALL RefreshHeldItems   ; refresh the HeldItemFront/Back cache
  LD A,(HeldItemFront)    ; front-hand strip cell ($73C6) =
  CALL ItemIdToString     ; the new "Cat in ..." item's string id
  LD (MenuUseRows),A
  LD HL,$4836             ; redraw the front-hand row
  LD (DrawScreenPtr),HL   ; (screen row 9, col 22)
  CALL DrawSprite
  CALL BumpCourageMorale  ; +1 courage/morale for the catcher, and...
  LD (HL),$FF             ; ...JonesRoom = 255: Jones is off the map
  LD HL,$48F6             ; blank the "Get Jones" row
  LD (DrawScreenPtr),HL   ; (screen row 15, col 22)
  LD A,$FF
  LD ($73CC),A            ; its strip cell 14 = empty
  CALL DrawSprite
  CALL AdvanceCursor      ; step the cursor off the now-empty row
  CALL TriggerJonesAnim   ; run the cat animation once
  LD A,(DrawSlotIndex)
  LD L,A                  ; L = capturing crew slot
  LD A,$02                ; msg #2 "{actor} has captured Jones"
  CALL EnqueueMessage
  RET

; ResetCrewTimers — per-frame crew action-countdown walk
;
; Called from GameEntry (GameEntry) each frame: walk all 8 actor records,
; decrement every non-parked action countdown (IX+0), and fire the queued
; action on the 1->0 transition — DispatchCrewAction (DispatchCrewAction) then
; jumps through CrewActionDispatch (CrewActionDispatch) into the per-state
; handlers (CrewAction1_Arrive move/idle, CrewAction2_DuctTransit duct transit,
; CrewAction3_RemoveGrille remove grille, ...).
ResetCrewTimers:
  PUSH IX
  XOR A                   ; CrewIndex = 0: start at the alien's slot
  LD (CrewIndex),A
  LD IX,ActorRecords      ; IX -> ActorRecords slot 0
  LD B,$08                ; all 8 actor slots
CrewTimersLoop:
  PUSH BC
  LD A,(IX+$00)           ; A = action countdown
  AND A                   ; parked (0)?
  JR Z,CrewTimersNext     ; yes: leave it alone
  DEC A                   ; tick it down
  LD (IX+$00),A
  CALL ActionTimerFire    ; fire the action on the 1->0 transition
CrewTimersNext:
  LD DE,$0008             ; step to the next 8-byte record
  ADD IX,DE
  LD HL,CrewIndex         ; keeping CrewIndex in step
  INC (HL)
  POP BC
  DJNZ CrewTimersLoop
  POP IX
  RET

; DispatchCrewAction
;
; Dispatches one crew/alien actor through CrewActionDispatch
; (CrewActionDispatch) based on (IX+3). Action 0 returns early. Otherwise jumps
; to the per-action handler via JP (HL). Called on an actor's action-countdown
; 1->0 transition: the crew-timer walk (ResetCrewTimers ResetCrewTimers) goes
; through the CALL Z trampoline at the tail of the PlayMusic block.
DispatchCrewAction:
  CALL PreActionCheck     ; preliminary actor update
  AND A                   ; A = current action state from caller
  RET Z                   ; action 0: nothing more to do
  LD E,(IX+$03)           ; E = action state (1..7)
  LD D,$00
  LD HL,CrewActionDispatch ; HL = CrewActionDispatch base
  ADD HL,DE               ; HL += action*2 (16-bit-pointer stride)
  ADD HL,DE
  LD A,(HL)               ; HL = *(table + action*2)
  INC HL
  LD H,(HL)
  LD L,A
  JP (HL)                 ; tail-call into action handler

; CrewAction1_Arrive — complete a move (action state 1)
;
; Dispatched via CrewActionDispatch entry 1. State 1 means "go to the room in
; (IX+2)" (or idle when the destination equals the current room — the AI parks
; wanderers that way). On firing, the actor arrives: current room :=
; destination, with a door sound unless the move happened inside the duct
; network. Arriving in an airlock that has been blown open (ShipSystemFlags bit
; 1 = Airlock #1, bit 2 = Airlock #2) dumps the mover into Corridor#6 (room 13)
; instead.
;
; Used by the routines at RoomDispatchTailExtra and AlienScareCheck.
CrewAction1_Arrive:
  LD A,(IX+$02)           ; A = destination room
  CP (IX+$01)             ; already there?
  RET Z                   ; yes: stay idle
  LD (IX+$01),A           ; arrive: current room = destination
  BIT 6,A                 ; was the move inside the ducts?
  JR NZ,CrewArrive_AirlockCheck
  LD A,$01                ; no: request the door sound
  LD (SoundRequest),A
CrewArrive_AirlockCheck:
  CALL RecalcMorale       ; recompute room-mates and morale
  LD A,(IX+$01)           ; entered Airlock #1 (room 0)?
  LD E,$02                ; (its "blown" flag is bit 1)
  AND A
  JR Z,CrewArrive_AirlockVented
  CP $01                  ; entered Airlock #2 (room 1)?
  JR NZ,CrewArrive_RefreshView ; neither: no airlock hazard
  LD E,$04                ; (its "blown" flag is bit 2)
CrewArrive_AirlockVented:
  LD A,E                  ; is that airlock blown open?
  LD HL,ShipSystemFlags
  AND (HL)
  JR Z,CrewArrive_RefreshView
  LD (IX+$01),$0D         ; yes: the mover ends up in Corridor#6
CrewArrive_RefreshView:
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(CrewIndex)        ; is this the crew member being viewed?
  LD HL,DrawSlotIndex
  CP (HL)
  JP Z,RedrawRoomScene    ; yes: rebuild the whole room view
  CALL RefreshCrewCondition ; no: refresh the info panel / condition
  RET

; CrewAction2_DuctTransit — climb through a grille (action state 2)
;
; Dispatched via CrewActionDispatch entry 2: the actor crosses between a room
; and the duct run above it (queued by MoveThroughGrille MoveThroughGrille or
; the AI's duct hop at AlienAI_DuctFlip). The cramped ducts cost a point of
; courage to enter; climbing back out earns it back.
CrewAction2_DuctTransit:
  LD E,(IX+$01)           ; E = current room (bit 6 = in ducts)
  LD A,(IX+$05)           ; A = courage
  BIT 6,E                 ; climbing out of the ducts?
  JR NZ,CrewDuct_BumpCourage ; yes: net +1 courage
  DEC A                   ; no, crawling in: net -1
  DEC A
CrewDuct_BumpCourage:
  INC A
  CP $7F                  ; clamp an underflow to 0
  JR C,CrewDuct_SetCourage
  XOR A
CrewDuct_SetCourage:
  LD (IX+$05),A
  LD A,E                  ; flip the in-ducts flag:
  XOR $40                 ; same room, other side of the grille
  LD (IX+$01),A
  CALL RecalcMorale       ; recompute room-mates and morale
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(CrewIndex)        ; is this the crew member being viewed?
  LD HL,DrawSlotIndex
  CP (HL)
  JP Z,RedrawRoomScene    ; yes: rebuild the whole room view
  JP RefreshCrewCondition ; no: refresh the info panel / condition

; CrewAction3_RemoveGrille
;
; Used by the routines at RoomDispatchTailExtra and PreActionCheck.
;
; Dispatched via CrewActionDispatch entry 3. Fires when a crew member's timed
; "RmveGrille" job completes (queued by the AI at $90EE or by the action-menu
; Special handler at $8BD3): clears the actor's room's byte in RoomGrilleState
; (RoomGrilleState), queues a sound effect, and rebuilds the room view if that
; room is on screen.
CrewAction3_RemoveGrille:
  LD A,$01                ; queue a sound effect for PlayMusic
  LD (SoundPending),A
  LD A,(IX+$01)           ; E = actor's current room
  AND $3F                 ; (mask off the in-ducts flag, bit 6)
  LD HL,RoomGrilleState
  LD E,A
  LD D,$00
  ADD HL,DE
  LD (HL),$00             ; RoomGrilleState[room] = 0: grille removed
  LD A,(RoomModeByte)     ; room mode 1 (normal room view)?
  CP $01
  RET NZ
  LD A,(CurrentRoom)      ; is the actor's room the one on screen?
  AND $3F
  CP E
  JP Z,RedrawRoomScene    ; yes: full room-view rebuild
  JP RefreshRoomInfo

; RecalcMorale — find room-mates and recompute the trio's morale
;
; After an actor moves: scan the 7 crew rooms for other actors on the same spot
; (same room, same side of the ducts), record up to two in RoomMateA/B, then
; run MoraleFromCompanion (MoraleFromCompanion) three times, rotating the
; (self, mateA, mateB) triple through NearestCrewC so each of the three has his
; morale recomputed with the other two as companions. A third room-mate's slot
; would spill past RoomMateB into RoomModeByte (RoomModeByte) —
; PreActionCheck's 3-crew room cap is what keeps that from happening.
RecalcMorale:
  LD HL,$FFFF             ; RoomMateA/B = 255/255 (none)
  LD (RoomMateA),HL
  LD HL,$7387             ; HL -> slot 1's room byte (+1)
  LD C,$01                ; C = candidate slot number
  LD A,(CrewIndex)        ; processing the alien (slot 0)?
  AND A
  RET Z                   ; yes: it has no morale
  EX AF,AF'               ; keep own slot number in A'
  LD DE,RoomMateA         ; DE -> RoomMateA
  LD A,(IX+$01)           ; A = own spot (room + duct bit)
  LD B,$07                ; scan the 7 crew slots
RecalcMorale_ScanLoop:
  CP (HL)                 ; on the same spot?
  JR NZ,RecalcMorale_NextSlot ; no: next slot
  EX AF,AF'               ; is it our own record?
  CP C
  JR Z,RecalcMorale_SkipSelf ; yes: skip it
  EX DE,HL                ; record the room-mate's slot
  LD (HL),C               ; (RoomMateA, then RoomMateB)
  EX DE,HL
  INC DE
RecalcMorale_SkipSelf:
  EX AF,AF'               ; (put the spot back in A)
RecalcMorale_NextSlot:
  PUSH DE
  LD DE,$0008             ; step to the next record's room byte
  ADD HL,DE
  POP DE
  INC C
  DJNZ RecalcMorale_ScanLoop
  PUSH IX
  LD A,(CrewIndex)        ; seed the rotation:
  LD (NearestCrewC),A     ; C-slot = self,
  LD HL,(RoomMateA)       ; A/B-slots = the two room-mates
  LD (NearestCrewA),HL
  CALL MoraleFromCompanion ; morale of self (companions: A and B)
  LD HL,(NearestCrewA)    ; rotate the triple: (C,A,B) =
  LD A,(NearestCrewC)     ; (mateA, mateB, self)
  LD (NearestCrewB),A
  LD (NearestCrewC),HL
  CALL MoraleFromCompanion ; morale of mateA (companions: B, self)
  LD HL,(NearestCrewA)    ; rotate again: (C,A,B) =
  LD A,(NearestCrewC)     ; (mateB, self, mateA)
  LD (NearestCrewB),A
  LD (NearestCrewC),HL
  CALL MoraleFromCompanion ; morale of mateB (companions: self, A)
  POP IX
  RET

; MoraleFromCompanion — morale = courage + companion effects
;
; Recompute the morale (+6) of the crew member whose slot is in NearestCrewC
; from his courage (+5) plus a term per companion slot in NearestCrewA/B: a
; live companion adds his own courage - 2 (a brave friend reassures, a coward
; unnerves); a dead one costs 1 — and a freshly dead body (status 1) is
; "found": everyone's courage decays, msg #20 fires and the body's status moves
; to 2 so it is only found once. Standing in one of the rooms in NearRoomList
; (NearRoomList, the tracker-holder scan at FindTrackerHolders) is worth +1.
; The sum is clamped to 0 on underflow.
MoraleFromCompanion:
  XOR A                   ; companion-effect accumulator = 0
  LD (DistanceAccum),A
  LD A,(NearestCrewC)     ; A = whose morale to compute
  CP $FF                  ; nobody?
  RET Z
  LD B,A                  ; IX -> that crew member's record
  LD IX,ActorRecords
  LD DE,$0008
MoraleComp_FindSlot:
  ADD IX,DE
  DJNZ MoraleComp_FindSlot
  XOR A                   ; dead or removed (+7 nonzero)?
  CP (IX+$07)
  RET NZ                  ; yes: no morale to compute
  PUSH IX
  LD B,$02                ; both companion slots:
  LD DE,NearestCrewA      ; NearestCrewA, then NearestCrewB
MoraleComp_PairLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)               ; A = companion slot (255 = none)
  CP $FF
  JR Z,MoraleComp_NextPair
  LD IX,ActorRecords      ; IX -> the companion's record
  LD DE,$0008
MoraleComp_RecSeek:
  ADD IX,DE
  DEC A
  JR NZ,MoraleComp_RecSeek
  CP (IX+$07)             ; companion alive (status 0)?
  JR Z,MoraleComp_AddCourage ; yes: courage support
  INC A                   ; a freshly dead body (status 1)?
  CP (IX+$07)
  JR NZ,MoraleComp_Minus  ; no (found earlier / removed): just -1
  CALL DecayCrewCourage   ; find the body: everyone's courage decays,
  POP DE
  PUSH DE
  LD A,(DE)               ; H = the body's slot
  LD H,A
  LD A,(NearestCrewC)
  LD L,A                  ; L = the discovering crew slot
  LD A,$14                ; msg #20 "{actor} finds {target}'s body"
  CALL EnqueueMessage
  INC (IX+$07)            ; status 2: the body is only found once
MoraleComp_Minus:
  LD HL,DistanceAccum     ; a dead companion:
  DEC (HL)                ; morale term -1
  JR MoraleComp_NextPair
MoraleComp_AddCourage:
  LD A,(IX+$05)           ; a live companion: morale term +=
  SUB $02                 ; his courage - 2
  LD E,A
  LD A,(DistanceAccum)
  ADD A,E
  LD (DistanceAccum),A
MoraleComp_NextPair:
  POP DE
  INC DE                  ; next companion slot
  POP BC
  DJNZ MoraleComp_PairLoop
  POP IX                  ; IX -> the actor again
  LD B,$03                ; is his spot one of the 3 rooms
  LD HL,NearRoomList      ; in NearRoomList?
  LD A,(IX+$01)
MoraleComp_RoomScan:
  CP (HL)
  JR Z,MoraleComp_RoomBonus
  INC HL
  DJNZ MoraleComp_RoomScan
  JR MoraleComp_Apply
MoraleComp_RoomBonus:
  LD HL,DistanceAccum     ; yes: +1
  INC (HL)
MoraleComp_Apply:
  LD HL,DistanceAccum     ; morale = courage + companion effects,
  LD A,(IX+$05)
  ADD A,(HL)
  CP $7F                  ; clamped to 0 on underflow
  JR C,MoraleComp_SetMorale
  XOR A
MoraleComp_SetMorale:
  LD (IX+$06),A
  RET

; DecayCrewCourage — everyone loses a point of courage and morale
;
; Grim events shake the whole crew: -1 courage and -1 morale for all 7 crew
; members (each clamped at 0). Triggered by finding a body
; (MoraleFromCompanion) and by witnessing a kill (KillActorMoraleHit). The loop
; body is split: DecayMoraleStep (DecayMoraleStep) handles the morale byte and
; the loop stepping, then refreshes the viewed condition line.
DecayCrewCourage:
  LD HL,$738B             ; HL -> slot 1's courage byte (+5)
  LD DE,$0008             ; record stride
  LD B,$07                ; all 7 crew
; This entry point is used by the routine at DecayMoraleStep.
DecayCourageLoop:
  DEC (HL)                ; courage -= 1,
  LD A,$7F
  CP (HL)                 ; clamping an underflow
  JR NC,DecayCourage_Tail
  LD (HL),$00             ; to 0
DecayCourage_Tail:
  JP DecayMoraleStep      ; morale -= 1 too, then loop
  RET                     ; (DecayMoraleStep returns from here)

; BusyWait
;
; Fixed delay loop, ~126,000 T-states (~36 ms).
;
; Used by the routines at DamageOverflowFlash, FrameTiming, IntroductionMode,
; ScreenTransition, WaitKeyPrompt and PlayMusic.
BusyWait:
  LD BC,$12FF             ; 4863 iterations of 26 T-states
BusyWaitLoop:
  DEC BC
  LD A,C
  OR B
  JR NZ,BusyWaitLoop
  RET

; GameEntry
;
; Main game entry point. Initialises the stack, runs the title and options
; screens, then enters the per-frame main loop. The loop calls every major
; subsystem once per frame and repeats indefinitely.
;
; Used by the routines at EndgameScreen and PauseMenu.
GameEntry:
  LD SP,$FFFA             ; stack at $FFFA (top of game RAM, below extra data)
  CALL ResetScriptPtr     ; ResetScriptPtr: seed ROM-traversal script pointer
  CALL DrawIntroScreen    ; DrawIntroScreen: animated Alien title screen
  CALL GameModeScreen     ; GameModeScreen: crew roster + game-mode menu
  CALL OptionsScreen      ; OptionsScreen: difficulty selection (keys 1-4)
  CALL ScreenTransition   ; ScreenTransition: wipe to black, set white
                          ; attributes
  CALL UpdateAllCrew      ; UpdateAllCrew: pairwise companion morale support
  CALL InitGameView       ; InitGameView: draw initial corridor view
; Main per-frame loop body. Re-entered every frame via JR back to here.
MainLoop:
  CALL HandleInput        ; HandleInput: scan keyboard, dispatch crew movement
  CALL FrameTiming        ; FrameTiming: ~1/50s frame wait or play queued sound
  CALL UpdateAlien        ; UpdateAlien: alien movement & crew-attack logic
  CALL TriggerAlienEvent  ; TriggerAlienEvent: poll alien event timer
  CALL UpdateAlienAI      ; UpdateAlienAI: give the idle alien its next action
  CALL TickMessageQueue   ; TickMessageQueue: tick the queued per-crew
                          ; animation slots
  CALL UpdateJones        ; UpdateJones: walk Jones the cat one room when due
  CALL ResetCrewTimers    ; ResetCrewTimers / corridor pass 1
  CALL AnimateCrewA       ; AnimateCrewA: XOR-blit crew sprite set A (10-frame)
  CALL AnimateCrewB       ; AnimateCrewB: XOR-blit cursor / sprite set B
                          ; (5-frame)
  CALL PlayMusic          ; PlayMusic: emit one beeper note from current phrase
  CALL AnimateCrewA       ; AnimateCrewA: second blit erases via XOR
                          ; (double-buf)
  CALL AnimateCrewB       ; AnimateCrewB: second blit erases via XOR
  CALL TickMessageQueue   ; TickMessageQueue: second animation-tick pass
  CALL UpdateRoomActors   ; UpdateRoomActors: corridor advance / room actor
                          ; logic
  CALL CheckCrewAlive     ; CheckCrewAlive: end the game if no human survives
  LD BC,$F7FE             ; BC = port 63486 ($F7FE): keyboard half-row 1-5
  IN A,(C)                ; read key row (active-low: 0 = key pressed)
  BIT 0,A                 ; test key 1 (pause / restart)
  CALL Z,PauseMenu        ; key 1 down → open PauseMenu
  CALL AdvanceScriptPtr   ; AdvanceScriptPtr: consume next ROM script nibble
  CP $00                  ; nibble 0 = no-op
  CALL Z,AdvanceScriptPtr ; skip a second nibble
  CP $00
  CALL Z,ResetScriptPtr   ; two consecutive 0s → ResetScriptPtr (reseed from
                          ; SEED)
  JR MainLoop             ; tail-loop back to the start of the per-frame body
; PreActionCheck's Android branch (see PreActionCheck): the Android quietly
; disobeys an ATTACK order.
PreAction_CancelCombat:
  LD A,(IX+$03)           ; Android ordered to ATTACK (state 4)?
  CP $04
  JR NZ,PreAction_MoveGate
  CALL PickNextRoom       ; yes: it disobeys — pick a random room
  LD (IX+$03),$01         ; and wander there instead (state 1)
  LD (IX+$00),$14         ; in 20 frames
  XOR A                   ; A = 0: cancel this dispatch
  RET
PreAction_MoveGate:
  CP $03                  ; other Android states: 3+ fire as-is,
  RET NC
  JR PreAction_DestScan   ; 1-2 go through the destination checks

; PreActionCheck — permission gate before a crew action fires
;
; Called by DispatchCrewAction just before an actor's queued action fires;
; returns A = 0 to cancel the dispatch, nonzero to let it proceed. The alien
; (slot 0) is always allowed. The Android quietly refuses an ATTACK order — it
; is re-assigned a wander instead (PreAction_CancelCombat). A crew member whose
; morale hits 0 (Broken) panics: in the ducts he opens the grille and climbs
; out; in a room he drops everything he carries and flees to a random adjacent
; room. And a pending move (state 1/2) is held back (retried in ~32+ frames)
; when the destination duct cell is already occupied — the ducts are
; single-file — or the destination room already holds 3 crew (the
; room-occupancy cap). Heading into the ducts also takes morale >= 2 (at least
; "Uneasy"); a more shaken crew member panics instead.
;
; Used by the routine at DispatchCrewAction.
PreActionCheck:
  LD A,(CrewIndex)        ; processing the alien (slot 0)?
  AND A
  CPL                     ; (A = 255: proceed)
  RET Z                   ; yes: always allowed, no checks
  XOR A                   ; action state 0 (parked)?
  CP (IX+$03)
  RET Z                   ; yes: A = 0, nothing to fire
  LD A,(AlienTargetID)    ; is this actor the Android?
  LD HL,CrewIndex
  CP (HL)
  JR Z,PreAction_CancelCombat ; yes: combat-refusal branch
  XOR A                   ; morale 0 (Broken)?
  CP (IX+$06)
  JR Z,PreAction_Broken   ; yes: panic
  LD A,(IX+$03)           ; states 3+ (grille/combat/...)
  CP $03                  ; fire unchecked;
  RET NC                  ; moves go through the checks:
PreAction_DestScan:
  LD A,(IX+$02)           ; A = the pending destination
  LD HL,$7387             ; HL -> slot 1's room byte (+1)
  LD DE,$0008
  LD BC,$0700             ; B = 7 slots, C = 0 occupants
PreAction_DestLoop:
  CP (HL)                 ; count the crew already at
  JR NZ,PreAction_DestNext ; the destination spot
  INC C
PreAction_DestNext:
  ADD HL,DE
  DJNZ PreAction_DestLoop
  LD A,(IX+$02)           ; heading into the duct network?
  BIT 6,A
  JR Z,PreAction_DoorGate
  LD A,(IX+$06)           ; too shaken for the ducts
  CP $02                  ; (morale < 2)?
  JR C,PreAction_Broken   ; yes: panic
  LD A,C                  ; duct cell already occupied?
  AND A
  JR NZ,PreAction_Requeue ; yes: wait and retry
  CPL                     ; no: A nonzero, proceed
  RET
PreAction_DoorGate:
  LD A,C                  ; already 3 crew in the room?
  CP $03                  ; (the room-occupancy cap)
  JR NC,PreAction_Requeue ; yes: wait and retry
  CPL                     ; no: A nonzero, proceed
  RET
PreAction_Requeue:
  LD A,$20                ; re-arm the countdown (32 + the
  CALL SetActionTimer     ; usual adjustments) and
  XOR A                   ; cancel this dispatch
  RET
PreAction_Broken:
  BIT 6,(IX+$01)          ; panicking in the ducts?
  JR Z,PreAction_DropScan
  LD A,(IX+$03)           ; only a pending move (state 1)
  CP $01                  ; is replaced
  RET NZ
  CALL CrewAction3_RemoveGrille ; bail out: open this room's grille
  CALL MoveThroughGrille  ; queue the climb out of the ducts
  XOR A                   ; and cancel this dispatch
  RET
PreAction_DropScan:
  LD HL,ItemLocations     ; panicking in a room: scan ItemLocations
  LD A,(CrewIndex)
  ADD A,$80               ; E = own back-hand marker (128+slot)
  LD E,A
  ADD A,$20               ; D = own front-hand marker (160+slot)
  LD D,A
  LD C,(IX+$01)           ; C = current room
  LD B,$16                ; all 22 items:
PreAction_DropLoop:
  LD A,(HL)
  CP E                    ; held in either hand?
  JR Z,PreAction_DropItem
  CP D
  JR NZ,PreAction_DropNext
PreAction_DropItem:
  LD (HL),C               ; yes: drop it on the floor here
PreAction_DropNext:
  INC HL
  DJNZ PreAction_DropLoop
  CALL PickNextRoom       ; flee: pick a random adjacent room
  JR PreAction_DestScan   ; and re-run the occupancy checks

; PickNextRoom
;
; Used by the routines at GameEntry, PreActionCheck, UpdateAlienAI,
; AlienScareCheck, VictimItemDrop and UpdateAlien.
;
; Choose the destination room for the actor whose record IX points at, and
; store it in record byte +2. The actor's current room is byte +1; bit 6 of +1
; selects which network it is travelling in.
PickNextRoom:
  LD A,(IX+$01)           ; A = actor's current room (bit 6 = duct network)
  LD HL,RoomAdjCorridors  ; assume corridor adjacency map (RoomAdjCorridors)
  BIT 6,A                 ; travelling in the ducts?
  JR Z,PickRoom_MapSeek   ; no: keep corridor map
  LD HL,RoomAdjDucts      ; yes: use duct map (RoomAdjDucts)
  AND $3F                 ; strip the network flag -> room ID 0-33
PickRoom_MapSeek:
  LD E,A
  LD D,$00
  LD B,$05
PickRoom_MulLoop:
  ADD HL,DE
  DJNZ PickRoom_MulLoop   ; HL = map + room*5 (the room's 5 direction slots)
  PUSH HL
  CALL AdvanceScriptPtr   ; AdvanceScriptPtr: next ROM script nibble
  CALL ScriptNibbleToDirection ; ScriptNibbleToDirection: nibble -> slot 0-4
  LD E,A
  POP HL
  ADD HL,DE
  LD A,(HL)               ; A = destination room for that direction
  BIT 6,(IX+$01)          ; moving through the ducts?
  JR Z,PickRoom_Store
  OR $40                  ; yes: tag the destination with the duct flag
PickRoom_Store:
  LD (IX+$02),A           ; record +2 = destination room
  RET

; ScriptNibbleToDirection
;
; Maps a 4-bit ROM-script command (in A, from AdvanceScriptPtr
; AdvanceScriptPtr) to one of 5 direction-class codes: 0-2 → 0 (no direction),
; 3-5 → 1, 6-8 → 2, 9-11 → 3, 12-15 → 4. Used to translate the deterministic
; ROM-byte stream into AI movement decisions.
;
; Used by the routines at PickNextRoom, UpdateJones, UpdateAlienAI and
; CrewAction5_AlienAttack.
ScriptNibbleToDirection:
  CP $03                  ; nibble 0..2?
  JR NC,NibbleDir_Check6
  XOR A                   ; → direction 0
  RET
NibbleDir_Check6:
  CP $06                  ; nibble 3..5?
  JR NC,NibbleDir_Check9
  LD A,$01                ; → direction 1
  RET
NibbleDir_Check9:
  CP $09                  ; nibble 6..8?
  JR NC,NibbleDir_Check12
  LD A,$02                ; → direction 2
  RET
NibbleDir_Check12:
  CP $0C                  ; nibble 9..11?
  JR NC,NibbleDir_Dir4
  LD A,$03                ; → direction 3
  RET
NibbleDir_Dir4:
  LD A,$04                ; default (12..15) → direction 4
  RET

; RefreshRoomInfo
;
; Refresh the viewed room's info: redraw the "Also here" room-mate lists, then
; handle a hostile presence — the alien in the room hands off to AlienInfoCheck
; (encounter screen / ATTACK row), the Android (once hostile) to
; AndroidInfoCheck.
;
; Used by the routines at SwapHeldItems, CrewAction3_RemoveGrille and
; RefreshCrewCondition.
RefreshRoomInfo:
  LD HL,$FFFF             ; forget the cached room-mates and
  LD (RoomMateA),HL       ; redraw the "Also here" lists
  CALL DrawRoomMates
  LD A,(CurrentRoom)      ; the alien in the viewed room?
  LD HL,$737F
  CP (HL)
  JR Z,RefreshInfo_Alien  ; yes: AlienInfoCheck
  CALL TriggerAlienXAnim  ; no: restart the encounter animation
  LD A,(AlienActiveFlag)  ; Android hostile yet?
  AND A
  RET Z                   ; no: done
  LD HL,(AlienTargetPtr)  ; is the Android in the viewed room
  INC HL                  ; (Z flag for AndroidInfoCheck)?
  LD A,(CurrentRoom)
  CP (HL)
  JP AndroidInfoCheck
RefreshInfo_Alien:
  JP AlienInfoCheck
; This entry point is used by the routine at AlienInfoCheck.
RefreshInfo_Encounter:
  LD HL,DrawMenuStrip     ; freeze both animation channels
  LD (CrewAnimFlags),HL   ; ($8080) and switch to view type 4:
  LD A,$04                ; the alien-encounter screen
  LD (RoomTypeByte),A
; This entry point is used by the routine at AndroidInfoCheck.
RefreshInfo_AttackRow:
  LD A,$34                ; Special cell (13) = "  ATTACK  "
  LD ($73CB),A
  LD HL,$48D6             ; and redraw the special row
  LD (DrawScreenPtr),HL   ; (screen row 14, col 22)
  CALL DrawSprite
  RET

; UpdateJones
;
; Walks Jones the cat around the ship. JonesRoom (JonesRoom) is the cat's
; current room (255 = off-map / disabled); JonesMoveTimer (JonesMoveTimer) is a
; frame countdown — when it hits zero (every 255 frames, ~5s) Jones takes one
; step along the corridor adjacency map (RoomAdjCorridors), steered by the ROM
; script via ScriptNibbleToDirection like every other wanderer.
;
; House-keeping around each step: - if Jones LEFT the room currently shown in
; the room view (mode 1), cell 14 of the corridor strip (CorridorPosTable + 14)
; is cleared; - if a living crew member (record +7 = 0) occupies Jones's new
; room, message #0 "{actor} sees Jones the cat" is enqueued via EnqueueMessage;
; - if Jones stepped into the ALIEN's room (slot 0 byte +1), the move is
; re-rolled — the cat refuses to share a room with the alien; - if Jones
; ENTERED the viewed room, sprite strip 51 (the cat) is drawn into corridor
; cell 14.
UpdateJones:
  LD A,(JonesRoom)        ; A = Jones's room —
  CP $FF                  ; 255 = caught / off the map:
  RET Z                   ; nothing to walk
  LD HL,JonesMoveTimer    ; HL → JonesMoveTimer
  DEC (HL)                ; tick it down
  RET NZ                  ; not yet zero: wait another frame
  LD (HL),$FF             ; expired: next move in ~255 frames
  EX AF,AF'               ; keep the OLD room in A'
Jones_PickMove:
  LD A,(JonesRoom)        ; A = Jones's current room
  LD HL,RoomAdjCorridors  ; corridor adjacency map (RoomAdjCorridors)
  LD E,A
  LD D,$00
  LD B,$05
Jones_AdjLoop:
  ADD HL,DE
  DJNZ Jones_AdjLoop      ; HL = map + room*5
  PUSH HL
  CALL AdvanceScriptPtr   ; AdvanceScriptPtr: next ROM script nibble
  CALL ScriptNibbleToDirection ; ScriptNibbleToDirection: nibble -> slot 0-4
  LD E,A
  POP HL
  ADD HL,DE
  LD A,(HL)               ; A = Jones's destination room
  LD HL,JonesRoom
  LD (HL),A               ; JonesRoom = destination
  LD A,(RoomModeByte)
  CP $01                  ; is the room view in corridor mode 1?
  JR NZ,Jones_ScanRooms   ; no: skip the display update
  LD HL,CurrentRoom       ; HL -> currently viewed room
  EX AF,AF'               ; A = Jones's OLD room
  CP (HL)                 ; did Jones just leave the viewed room?
  JR NZ,Jones_ScanRooms   ; no: nothing to erase
  LD A,$FF
  LD ($73CC),A            ; corridor cell 14 (Jones's slot) := empty
  LD HL,$48F6
  LD (DrawScreenPtr),HL   ; draw pointer -> cell 14's screen position
  CALL DrawSprite         ; blit blank strip (erase the cat)
  LD A,(CorridorCursor)
  CP $0E                  ; player cursor parked on Jones's cell?
  CALL Z,AdvanceCursor    ; yes: nudge it forward one position
  CALL TriggerJonesAnim   ; one-shot Jones (tail) animation
Jones_ScanRooms:
  LD A,(JonesRoom)        ; A = Jones's new room
  LD HL,$737F             ; slot 0 byte +1: walk the +1 (room) column
  LD BC,$0800             ; B = 8 slots, C = slot counter (0)
  LD DE,$0008             ; record stride
Jones_ScanLoop:
  CP (HL)                 ; is this actor in Jones's room?
  JR Z,Jones_Companion
Jones_ScanNext:
  INC C
  ADD HL,DE
  DJNZ Jones_ScanLoop     ; next slot
  RET                     ; nobody shares Jones's room
Jones_Companion:
  LD A,C
  AND A                   ; slot 0 = the ALIEN's room:
  JR Z,Jones_PickMove     ; the cat won't stay - re-roll the move
  PUSH HL
  PUSH DE
  LD E,$06
  ADD HL,DE               ; HL -> the slot's +7 status byte
  XOR A
  CP (HL)                 ; alive (status 0)?
  POP DE
  POP HL
  LD A,(JonesRoom)
  JR NZ,Jones_ScanNext    ; not alive: keep scanning
  XOR A                   ; A = message #0
  LD L,C                  ; L = witnessing crew slot
  LD H,$00
  CALL EnqueueMessage     ; enqueue "{actor} sees Jones the cat"
  LD A,(RoomModeByte)
  CP $01                  ; corridor view mode?
  RET NZ
  LD A,(JonesRoom)
  LD HL,CurrentRoom
  CP (HL)                 ; did Jones walk into the viewed room?
  RET NZ
  LD HL,$48F6             ; yes: draw pointer -> corridor cell 14
  LD (DrawScreenPtr),HL
  LD A,$33                ; sprite strip 51 = Jones the cat
  LD ($73CC),A            ; corridor cell 14 := Jones
  CALL DrawSprite         ; blit the cat into the corridor
  CALL InitJonesAnim      ; start the Jones animation from frame 0
  RET

; UpdateAlienAI — give the idle alien its next action
;
; Despite the old "UpdateCrewAI" name, this is the ALIEN's brain (IX -> slot 0;
; crew get their actions from the player). When the alien's countdown is parked
; (0), pick its next move from the ROM script: - crew share its room -> action
; state 5 (attack tick, timer 60) - alone with the grille still in place ->
; 3-in-16 chance to rip the grille out itself (state 3, timer 50) - alone,
; grille open -> 6-in-16 chance to slip through it into / out of the ducts
; (state 2, timer 50) - otherwise wander to an adjacent room (state 1, timer
; 80); when the roll leaves it where it stands, it damages the room instead (+1
; damage point — the alien trashes whatever it is lurking in)
;
; The entry goes through AIActionSelect (AIActionSelect), which first rescues
; the alien from spot 98 (ducts "over" the Narcissus, an escape artifact — see
; that routine).
;
; Used by the routine at GameEntry.
UpdateAlienAI:
  PUSH IX                 ; save caller's IX
  LD IX,ActorRecords      ; IX -> the alien's record (slot 0)
  XOR A                   ; is its action countdown parked (0)?
  CP (IX+$00)
  JR Z,AlienAI_Assign     ; yes: pick a new action
  POP IX                  ; no: it is busy
  RET
AlienAI_Assign:
  JP AIActionSelect       ; duct-escape guard, then FindVictims
; This entry point is used by the routine at AIActionSelect.
AlienAI_FindVictims:
  LD HL,$FFFF             ; NearestCrewC/A = 255/255
  LD (NearestCrewC),HL
  LD DE,NearestCrewC      ; collect crew on the alien's spot
  LD HL,$7387             ; (A = its room byte, from AIActionSelect)
  LD BC,$0701             ; B = 7 slots, C = slot 1
AlienAI_ScanLoop:
  CP (HL)                 ; on the same spot?
  JR NZ,AlienAI_ScanNext
  EX DE,HL                ; record the slot in
  LD (HL),C               ; NearestCrewC, then A, then B
  EX DE,HL
  INC DE
AlienAI_ScanNext:
  INC C
  PUSH DE
  LD DE,$0008             ; next record's room byte
  ADD HL,DE
  POP DE
  DJNZ AlienAI_ScanLoop
  LD A,(NearestCrewC)     ; anyone there?
  CP $FF
  JR Z,AlienAI_GrilleCheck ; no: movement options
  LD (IX+$00),$3C         ; yes: attack them —
  LD (IX+$03),$05         ; action state 5, timer 60
  POP IX
  RET
AlienAI_GrilleCheck:
  LD A,(IX+$01)           ; alien alone: is this room's
  AND $3F                 ; grille still in place?
  LD E,A
  LD D,$00
  LD HL,RoomGrilleState   ; RoomGrilleState[room]
  ADD HL,DE
  LD A,(HL)
  CP $FF
  JR NZ,AlienAI_TryDuct   ; no: it may slip through
  CALL AdvanceScriptPtr   ; yes: roll the script —
  CALL ScriptNibbleToDirection ; direction 0 (3 in 16)?
  AND A
  JR NZ,AlienAI_Wander    ; no: wander instead
  LD (IX+$00),$32         ; yes: the alien rips the grille
  LD (IX+$03),$03         ; out itself (state 3, timer 50)
  POP IX
  RET
AlienAI_TryDuct:
  CALL AdvanceScriptPtr   ; roll the script —
  CALL ScriptNibbleToDirection ; direction 0 or 1 (6 in 16)?
  CP $02
  JR NC,AlienAI_Wander    ; no: wander
; This entry point is used by the routine at AIActionSelect.
AlienAI_DuctFlip:
  LD A,(IX+$01)           ; yes: through the open grille —
  XOR $40                 ; same room, other side
  LD (IX+$02),A
  LD (IX+$00),$32         ; timer = 50 frames
  LD (IX+$03),$02         ; action state = 2 (duct transit)
  POP IX
  RET
AlienAI_Wander:
  CALL PickNextRoom       ; pick an adjacent room
  LD (IX+$00),$50         ; timer = 80 frames
  LD (IX+$03),$01         ; action state = 1 (move/idle)
  LD A,(IX+$01)           ; going nowhere (dest = current)?
  CP (IX+$02)
  LD A,$01                ; then the alien trashes the room:
  CALL Z,AddAlienRoomDamage ; +1 damage point
  POP IX
  RET

; EnqueueMessage
;
; Enqueue status message A (from MessageTextTable) with the actor and target
; slots for the 254/253 name substitutions passed in L and H. A repeat of the
; last-enqueued message number is dropped.
;
; Used by the routines at SetActionTimer, MoraleFromCompanion, UpdateJones,
; CrewHitsAlien, AddAlienRoomDamage, DestructArm, DestructAbort,
; HypersleepStart, FightFire, SealLock1, BlowLock1, CrewAction6_Handler,
; FireDamageTick, JonesUneasyMsg, CrewAction5_AlienAttack, WoundMsg,
; VictimItemDrop, OxygenCheckRedraw, LaunchGate, CrewAction7_Handler,
; UpdateAlien, CrewHitsAndroid, LongGameInit and KillActorMoraleHit.
EnqueueMessage:
  EX DE,HL                ; E = actor, D = target
  LD HL,(MsgLastEnqueued) ; same msg# and actor as the last
  CP L                    ; one enqueued?
  JR NZ,EnqMsg_Append
  LD A,E
  CP H
  RET Z                   ; yes: drop the duplicate
  LD A,L                  ; (A = the msg# again)
EnqMsg_Append:
  EX AF,AF'
  LD HL,(MsgQueueWritePtr) ; queue full? (the write ptr has hit
  LD A,(HL)               ; the 255 that starts MsgLastEnqueued,
  CP $FF                  ; ~9 entries in)
  RET Z                   ; yes: drop the message
  EX AF,AF'
  LD (HL),A               ; append the 3-byte entry:
  INC HL                  ; msg#, actor, target
  LD (HL),E
  INC HL
  LD (HL),D
  INC HL
  LD (MsgQueueWritePtr),HL
  LD L,A                  ; remember (msg#, actor) for the
  LD H,E                  ; duplicate check
  LD (MsgLastEnqueued),HL
  LD A,(MsgVisibleTimer)  ; a message currently on display?
  AND A
  RET Z
  LD A,$01                ; yes: cut its hold time short so
  LD (MsgVisibleTimer),A  ; the new one appears promptly
  RET

; RenderMessage
;
; Draw message number A onto the bottom message line (screen row 23). The text
; is drawn over BLACK attributes, so it stays invisible until
; TickMessageQueue's cursor sweep reveals it column by column. Substitution
; codes: 254 = actor name (MsgParam1), 253 = target name (MsgParam2), 252 =
; room name (MsgParam1 as a room id).
;
; Used by the routine at TickMessageQueue.
RenderMessage:
  LD HL,Screen_R23C0      ; draw at the message line (row 23)
  LD (DrawScreenPtr),HL
  LD DE,MessageTextTable  ; find string number A: skip A
  AND A                   ; 255-terminated strings
  JR Z,RenderMsg_CharLoop
  LD B,A
RenderMsg_SkipLoop:
  LD A,(DE)
  INC DE
  CP $FF
  JR NZ,RenderMsg_SkipLoop
  DJNZ RenderMsg_SkipLoop
RenderMsg_CharLoop:
  LD A,(DE)               ; next message byte:
  CP $FF                  ; end of string?
  JR Z,RenderMsg_Finish
  CP $FE                  ; the actor's name?
  JR Z,RenderMsg_ActorName
  CP $FD                  ; the target's name?
  JR Z,RenderMsg_TargetName
  CP $FC                  ; a room name?
  JR Z,RenderMsg_RoomName
  PUSH DE                 ; plain character: blit it
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr     ; one column right
  INC (HL)
  JR RenderMsg_CharLoop
RenderMsg_ActorName:
  LD A,(MsgParam1)        ; A = the actor's slot
  JR RenderMsg_PrintName
RenderMsg_TargetName:
  LD A,(MsgParam2)        ; A = the target's slot
RenderMsg_PrintName:
  INC DE
  PUSH DE                 ; print crew name A (CrewNameTable)
  CALL PrintName
  POP DE
  JR RenderMsg_CharLoop
RenderMsg_RoomName:
  INC DE
  PUSH DE
  LD A,(MsgParam1)        ; A = the room id = its name's
  LD HL,(DrawScreenPtr)   ; string index — a 10-character
  PUSH HL                 ; strip via DrawSprite
  CALL DrawSprite
  POP HL
  LD DE,$000A             ; advance 10 columns
  ADD HL,DE
  LD (DrawScreenPtr),HL
  POP DE
  JR RenderMsg_CharLoop
RenderMsg_Finish:
  LD HL,(DrawScreenPtr)   ; MsgDrawnCol = the end column,
  LD A,L                  ; bit 7 set = "reveal sweep due"
  AND $1F
  OR $80
  LD (MsgDrawnCol),A
  RET

; ClearMessageRow
;
; Blank the bottom message line: erase the 32 tile columns, black out the
; attribute row (with a red marker at column 0 priming the reveal sweep) and
; reset the display timer.
;
; Used by the routine at UpdateAllCrew.
ClearMessageRow:
  LD HL,Screen_R23C0      ; the message line (row 23)
  LD (DrawScreenPtr),HL
  LD B,$20                ; blank all 32 columns
ClearMsg_TileLoop:
  PUSH BC
  XOR A                   ; (entering DrawSpriteRow past its
  CALL DrawTileA          ; (DE) fetch draws tile A = blank)
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ ClearMsg_TileLoop
  LD HL,MsgLineAttrs      ; attr row 23: a red block at col 0
  LD (HL),$12             ; (the sweep cursor's start),
  LD (MsgAttrPtr),HL      ; MsgAttrPtr = col 0,
  INC HL
  LD B,$1F                ; and black over cols 1-31
ClearMsg_AttrLoop:
  LD (HL),$00
  INC HL
  DJNZ ClearMsg_AttrLoop
  LD HL,MsgVisibleTimer   ; display timer = 0
  LD (HL),B
  RET

; TickMessageQueue
;
; Per-frame driver of the message line's teletype effect. A message's life:
; dequeue -> RenderMessage draws it over black attributes -> a bright-red
; cursor sweeps left-to-right, turning each column black-on-cyan (the reveal)
; -> the message holds (timer 50, or just 2 when more messages wait) -> the
; cursor sweeps back right-to-left blacking it out -> the next message is
; fetched. MsgDrawnCol encodes the phase: 0 = line free, bit 7 set = revealing,
; bit 7 clear = erasing. Called twice per frame, so the sweeps run at ~100
; cells/s.
;
; Used by the routine at GameEntry.
TickMessageQueue:
  LD A,(MsgVisibleTimer)  ; message currently holding?
  AND A
  JR Z,MsgQueue_Fetch
  DEC A                   ; yes: run down the hold time
  LD (MsgVisibleTimer),A
  RET NZ
  LD A,(MsgAttrPtr)       ; hold over: start the erase sweep —
  AND $1F                 ; MsgDrawnCol = current column,
  LD (MsgDrawnCol),A      ; bit 7 clear
  RET
MsgQueue_Fetch:
  LD A,(MsgDrawnCol)      ; a sweep in progress?
  AND A
  JR NZ,MsgLine_Sweep
  LD A,(MsgQueueWritePtr) ; line free: anything queued?
  LD HL,MessageQueue
  CP L
  JR NZ,MsgQueue_Dequeue
  RET                     ; no: idle
MsgQueue_Dequeue:
  LD A,(HL)               ; pop the head entry:
  INC HL                  ; A = msg#,
  LD E,(HL)               ; E/D = actor/target params
  INC HL
  LD D,(HL)
  LD (MsgParam1),DE
  LD BC,$0015             ; compact the queue down
  LD DE,MessageQueue      ; by one 3-byte entry
  LD HL,MessageQueueBody
  LDIR
  LD HL,(MsgQueueWritePtr)
  DEC HL
  DEC HL
  DEC HL
  LD (MsgQueueWritePtr),HL
  CALL RenderMessage      ; draw it (still invisible)
  RET
MsgLine_Sweep:
  BIT 7,A                 ; which sweep phase?
  JR Z,MsgLine_SweepBack  ; bit 7 clear: erasing
  LD HL,(MsgAttrPtr)      ; revealing: the column behind turns
  LD (HL),$28             ; black-on-cyan (readable),
  INC HL                  ; the cursor block (bright red)
  LD (HL),$52             ; moves one right
  LD (MsgAttrPtr),HL
  DEC A                   ; reached the end of the text?
  CP $80
  JR Z,MsgLine_SetTimer
  LD (MsgDrawnCol),A      ; not yet: keep sweeping
  RET
MsgLine_SetTimer:
  XOR A                   ; reveal done: line phase = idle,
  LD (MsgDrawnCol),A
  LD A,(MsgQueueWritePtr) ; hold the message for 50 ticks —
  LD HL,MessageQueue      ; only 2 when more messages
  CP L                    ; are waiting
  LD A,$32
  JR Z,MsgLine_StoreTimer
  LD A,$02
MsgLine_StoreTimer:
  LD (MsgVisibleTimer),A
  RET
MsgLine_SweepBack:
  LD HL,(MsgAttrPtr)      ; erasing: black out this column,
  LD (HL),$00             ; the cursor block moves one left
  DEC HL
  LD (HL),$52
  LD (MsgAttrPtr),HL
  DEC A                   ; A = columns left (0 frees the line)
  DEFB $32                ; opcode of "LD (msg-head var),A": its operands are
                          ; the XOR H / ADD A,E at XorHAddE, so falling through
                          ; stores A and hits that entry's RET

; Overlap bytes shared with TickMessageQueue
;
; Not a real routine: these three bytes are the operand bytes of
; TickMessageQueue's trailing "DEFB 50" (LD (nn),A) — falling through from
; $9271 stores A into MsgDrawnCol and lands on the RET at $9274 (see the
; overlap note above). The combat/damage engine follows from StartAttack
; (StartAttack) onward.
XorHAddE:
  XOR H                   ; $AC,$83: the operand address ($83AC = MsgDrawnCol)
  ADD A,E                 ; of the DEFB 50 above — never executed as code
  RET                     ; the LD (nn),A fall-through lands on this RET

; StartAttack
;
; Used by the routine at SpecialRowNormal.
;
; Action on the "  ATTACK  " row: queue the combat job — action state 4
; (CrewAction4_Handler -> CrewHitsAlien / CrewHitsAndroid) with base countdown
; 40, replacing any pending action. Then put the special row back to what the
; room warrants (49 "RmveGrille" or blank, via the grille-state refresh at
; $B0B8) and move the cursor on.
StartAttack:
  LD A,$28                ; base countdown 40
  CALL SetActionTimer     ; arm it (SetActionTimer)
  LD (IX+$03),$04         ; action state = 4 (combat)
  LD A,$FF                ; (dead: A is overwritten inside the call)
  CALL RefreshSpecialRow  ; cell 13 = 49 or blank per RoomGrilleState
  LD HL,$48D6             ; and redraw the special row
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  CALL AdvanceCursor      ; advance the cursor off the row
  RET

; CrewAction4_Handler
;
; Dispatched via CrewActionDispatch entry 4: a queued ATTACK order fires. The
; actor attacks whatever hostile shares his room — the actor's room byte (+1)
; is compared with each hostile's:
;
; 1. The actor stands in the alien's room ($737F) → CALL CrewHitsAlien
; (CrewHitsAlien) — message #3 "nets the ALIEN" or #4 "hits the ALIEN".
;
; 2. Else, if the alien is loose (AlienActiveFlag) and the actor stands in the
; **Android's** room (record via AlienTargetPtr AlienTargetPtr) → CALL
; CrewHitsAndroid (CrewHitsAndroid) — message #31 "nets {target}" or #32 "hits
; the Android".
;
; 3. Neither hostile is here (it moved away before the countdown expired): the
; attack fizzles silently.
;
; The Android role: LongGameInit randomly picks one of crew slots {1, 2, 4, 6}
; (Dallas / Kane / Ash / Parker) and stores it in AlienTargetID
; (AlienTargetID). That crew member is dormant at first — they appear and
; behave like a normal crew member. UpdateAlien (UpdateAlien) turns them
; hostile the first frame the alien is loose and the slot is still in its
; template state (the "first encounter"), stamping them into action state 7 and
; giving them targets via NearestCrewA/B (see GatherSameRoomSlots). After
; activation the Android wounds nearby crew through CrewAction7_Handler
; (CrewAction7_Handler); CrewHitsAndroid here is the **player-response
; branch**, dispatched when other crew engage the Android in combat state 4.
; (The Short Game hard-wires AlienTargetID = 9 and never re-points
; AlienTargetPtr, so branch 2 is inert there.)
;
; After dispatch, CALL PostCombatReset parks IX+3 (action state) and returns.
CrewAction4_Handler:
  LD A,($737F)            ; A = the alien's current room (slot 0 byte +1)
  CP (IX+$01)             ; does the actor stand in it?
  JR Z,DispatchHitsAlien  ; yes → CrewHitsAlien branch
  LD A,(AlienActiveFlag)  ; A = AlienActiveFlag
  AND A                   ; alien loose on ship?
  RET Z                   ; no: nothing to dispatch
  LD HL,(AlienTargetPtr)  ; HL = pointer to the Android's crew record
  INC HL                  ; HL → its current-room byte (+1)
  LD A,(HL)               ; A = the Android's room
  CP (IX+$01)             ; does the actor stand in it?
  RET NZ                  ; no: neither hostile is here — fizzle
Combat_HitAlien:
  CALL CrewHitsAndroid    ; yes → CrewHitsAndroid (msg #32 / #31)
  JR ClearActionState
DispatchHitsAlien:
  CALL CrewHitsAlien      ; alien's room → CrewHitsAlien (msg #4 / #3)
ClearActionState:
  CALL PostCombatReset    ; reset IX+3 = 0 (action state cleared post-combat)
  NOP
  RET
Combat_RefreshRow:
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(DrawSlotIndex)    ; is the attacker the viewed crew member?
  LD HL,CrewIndex
  CP (HL)
  RET NZ                  ; no: done
  LD A,$34                ; keep "  ATTACK  " on the Special slot
  CALL SpecialRowAttack   ; (the hostile is still in the room)
  LD HL,$48D6             ; and redraw the special row
  LD (DrawScreenPtr),HL   ; (screen row 14, col 22)
  CALL DrawSprite
  RET

; CrewHitsAlien
;
; Alien-side combat handler. Called from CrewAction4_Handler
; (DispatchHitsAlien) when the state-4 actor stands in the alien's room
; ($737F).
;
; The outcome depends on the WEAPON: FindHeldItems (FindHeldItems) loads the
; attacking crew member's front-hand item id into HeldItemFront (HeldItemFront)
; and the effect dispatches on it. It also houses the Harpoon Gun's kill
; primitive at AlienKillPrimitive.
;
; Guards: re-asserts that the attacker still shares the alien's room, then
; requires strength (IX+4) >= 2 and morale (IX+6) >= 2 — a crew member who is
; Collapsed or Broken/Shaken cannot fight. Then the front-hand weapon decides:
;
; 17 (Cat Box)       → silent no-op >= 20 (Cat in Net/Box, or 255 = bare hands)
; → silent no-op 16 (Net)           → "net" outcome: stamp 255 into the alien's
; countdown byte ($737E, slot 0 byte +0) — netted, out of action — destroy the
; Net (single-use), then enqueue message #3 "{actor} nets the ALIEN" else
; → "hit" outcome at CrewHitsAlien_HitPath: enqueue message #4 "{actor} hits
; the ALIEN", then dispatch on the weapon id: - 0-5 Elctrc Prd/Incineratr,
; 18-19 Spanner: INC alien's wound counter at $7382 (byte +4 of slot 0). If it
; reaches the AlienKillThreshold (15), JP 44125 (alien- defeated path). - 6-7
; Tracker: the tracker shatters on the alien (destroyed, msg #5 "Tracker is
; broken") but still wounds it (+1) - 8-11 Fire Extng: no wound — spend a
; charge (ItemCharges) and try to scare the alien off (AlienScareCheck); with
; no charge left, msg #6 "extinguisher empty". - 12 Harpn Gun: the kill
; primitive at AlienKillPrimitive — see AlienKillPrimitive below. - 13-15 Laser
; Pist: spend a charge, wound the alien (+2) and damage the room (+6); with no
; charge left, msg #7 "Laser is exhausted".
;
; This routine parallels CrewHitsAndroid (CrewHitsAndroid) in structure but
; refers to the alien (slot 0 at $737E) and emits messages #3 / #4 instead of
; #31 / #32.
CrewHitsAlien:
  LD A,($737F)            ; A = the alien's current room (slot 0 byte +1)
  CP (IX+$01)             ; does the attacker still share it?
  RET NZ                  ; no: the alien slipped away — bail
  LD A,(IX+$04)           ; attacker's strength >= 2?
  CP $02
  RET C                   ; no: too hurt to fight
  LD A,(IX+$06)           ; attacker's morale >= 2?
  CP $02
  RET C                   ; no: too scared to fight
  CALL ClearHandCache     ; clear the hand cache, A = attacker's slot
  CALL FindHeldItems      ; FindHeldItems: what is he holding?
  LD A,(HeldItemFront)    ; A = the front-hand weapon
  CP $11                  ; the Cat Box?
  JP Z,Combat_RefreshRow  ; useless: silent no-op
  CP $14                  ; Cat in Net/Box, or bare hands (255)?
  JP NC,Combat_RefreshRow ; also useless
  CP $10                  ; the Net?
  JR NZ,CrewHitsAlien_HitPath ; no → "hits the ALIEN" branch
  LD HL,ActorRecords      ; yes: stamp 255 into the alien's countdown
  LD (HL),$FF             ; byte ($737E) — netted, out of action
  CALL DestroyFrontItem   ; the Net is used up (destroyed)
  LD A,(CrewIndex)        ; A = the attacker's slot
  LD L,A                  ; L = actor for the name substitution
  LD A,$03                ; A = msg #3 "{actor} nets the ALIEN"
  CALL EnqueueMessage     ; enqueue
  JP Combat_RefreshRow
; "{actor} hits the ALIEN" branch (CrewHitsAlien continued).
CrewHitsAlien_HitPath:
  PUSH AF                 ; stash the weapon id across the message
  LD A,(CrewIndex)        ; A = the attacker's slot
  LD L,A                  ; L = actor for the name substitution
  LD A,$04                ; A = msg #4 "{actor} hits the ALIEN"
  CALL EnqueueMessage     ; enqueue
  POP AF                  ; A = the weapon again
  CP $06                  ; 0-5: a prod or an incinerator?
  JR C,AlienStep_Advance
  CP $12                  ; 18/19: a spanner?
  JR Z,AlienStep_Advance
  CP $13
  JR NZ,WeaponCheck_Low   ; anything else: weapon-specific handling
AlienStep_Advance:
  LD HL,$7382             ; wound the alien: its +4 counter +1
  INC (HL)
  LD A,(AlienKillThreshold) ; wounds reached the kill threshold (15)?
  CP (HL)
  JP Z,EndgameScreen      ; yes: the alien dies — endgame
  JP AlienScareCheck      ; no: it may flee (AlienScareCheck)
WeaponCheck_Low:
  CP $08                  ; 6/7: a tracker?
  JR NC,WeaponCheck_Charged
  CALL DestroyFrontItem   ; yes: it shatters on the alien —
  LD HL,(CrewIndex)       ; destroyed; L = actor,
  LD A,$05                ; msg #5 "{actor}'s Tracker is broken"
  CALL EnqueueMessage
  JR AlienStep_Advance    ; and it still wounds the alien
WeaponCheck_Charged:
  CP $0C                  ; 8-11: an extinguisher?
  JR NC,HarpoonGate
  LD HL,ItemCharges       ; yes: HL -> its ItemCharges entry
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)               ; any CO2 left?
  AND A
  JR Z,WeaponSpentMsg
  DEC (HL)                ; yes: spend a charge — no wound, but
  JP AlienScareCheck      ; the spray may scare the alien off
WeaponSpentMsg:
  LD HL,(CrewIndex)       ; empty: L = actor,
  LD A,$06                ; msg #6 "{actor}'s extinguisher..empty"
  CALL EnqueueMessage
  JP Combat_RefreshRow
; AlienKillPrimitive — the Harpoon Gun outcome, reached from CrewHitsAlien
; (CrewHitsAlien) when the attacker's front-hand item (HeldItemFront) is 12,
; the Harpn Gun. A harpoon strike is devastating for both sides:
; GatherSameRoomSlots gathers the attacker and up to two other crew in the
; alien's room (NearestCrewC + NearestCrewA/B); for each, every ItemLocations
; (ItemLocations) entry that the slot carries or that lies in the alien's room
; is destroyed (set to 255), and if the slot is still alive (+7 = 0) it is
; killed (+7 = 1, strength +4 cleared) — firing the harpoon kills the firer and
; every bystander. The room takes 15 damage points, the alien's own wound
; counter is bumped by 5 (harpoon hits push it quickly over the kill threshold
; → dies via JP 44125), the corridor view is rebuilt (CALL 31417) and the chain
; continues at AlienScareCheck (AlienScareCheck).
HarpoonGate:
  JP NZ,WeaponCheck_Laser ; weapon != 12 → 13-15, the laser branch
AlienKillPrimitive:
  CALL GatherSameRoomSlots ; gather the attacker + up to two other crew in the
                           ; alien's room
  LD DE,NearestCrewC
  LD B,$03                ; all three gathered slots:
KillPrim_SlotLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)               ; A = victim slot (255 = none)
  CP $FF
  JR Z,KillPrim_NextSlot
  PUSH AF
  ADD A,$80               ; E = victim's back-hand marker
  LD E,A
  ADD A,$20               ; D = victim's front-hand marker
  LD D,A
  LD C,(IX+$01)           ; C = the alien's room
  LD HL,ItemLocations     ; destroy every item the victim holds
  LD B,$16                ; or that lies in the alien's room:
KillPrim_ItemLoop:
  LD A,(HL)
  CP E
  JR Z,KillPrim_WipeItem
  CP D
  JR Z,KillPrim_WipeItem
  CP C
  JR NZ,KillPrim_NextItem
KillPrim_WipeItem:
  LD (HL),$FF             ; ItemLocations entry -> gone
KillPrim_NextItem:
  INC HL
  DJNZ KillPrim_ItemLoop
  POP AF                  ; HL -> the victim's status byte (+7)
  LD HL,ActorRecords
  ADD A,A
  ADD A,A
  ADD A,A
  ADD A,$07
  LD E,A
  LD D,$00
  ADD HL,DE
  XOR A                   ; still alive?
  CP (HL)
  JR NZ,KillPrim_NextSlot
  LD (HL),$01             ; yes: killed by the blast (+7 = 1),
  DEC HL
  DEC HL
  DEC HL
  LD (HL),A               ; strength (+4) = 0
KillPrim_NextSlot:
  POP DE
  INC DE
  POP BC
  DJNZ KillPrim_SlotLoop
  LD A,$0F                ; the blast wrecks the room:
  CALL AddAlienRoomDamage ; +15 damage points
  LD HL,$7382             ; alien wound counter += 5
  LD A,(HL)
  ADD A,$05
  LD (HL),A
  LD HL,AlienKillThreshold ; reached the kill threshold (15)?
  CP (HL)
  JP NC,EndgameScreen     ; yes: the alien dies — endgame
  CALL InitGameView       ; rebuild the ship view
  JP AlienScareCheck      ; the alien may flee
WeaponCheck_Laser:
  LD E,A                  ; 13-15, a laser pistol:
  LD D,$00
  LD HL,ItemCharges       ; HL -> its ItemCharges entry
  ADD HL,DE
  XOR A                   ; any shots left?
  CP (HL)
  JR Z,LaserSpentMsg
  DEC (HL)                ; yes: spend one
  LD A,$06                ; the beam scorches the room:
  CALL AddAlienRoomDamage ; +6 damage points
  LD HL,$7382             ; alien wound counter += 2
  LD A,(HL)
  ADD A,$02
  LD (HL),A
  LD HL,AlienKillThreshold ; reached the kill threshold (15)?
  CP (HL)
  JP NC,EndgameScreen     ; yes: the alien dies — endgame
  JP AlienScareCheck      ; no: it may flee
LaserSpentMsg:
  LD HL,(CrewIndex)       ; out of shots: L = actor,
  LD A,$07                ; msg #7 "{actor}'s Laser is exhausted"
  CALL EnqueueMessage
  JP Combat_RefreshRow

; GatherSameRoomSlots
;
; Fill NearestCrewC with the current actor's slot (CrewIndex) and
; NearestCrewA/B with up to two other crew on the same spot (same room byte,
; same side of the ducts) — the same scan RecalcMorale uses for RoomMateA/B.
; GatherSlots_Scan is a direct entry with A = the slot to treat as "self".
;
; Used by the routines at CrewHitsAlien, AlienScareCheck, CrewAction7_Handler,
; UpdateAlien and CrewHitsAndroid.
GatherSameRoomSlots:
  LD HL,$FFFF             ; NearestCrewA/B = 255/255 (none)
  LD (NearestCrewA),HL
  LD DE,NearestCrewA
  LD A,(CrewIndex)        ; NearestCrewC = the current actor
  LD (NearestCrewC),A
; This entry point is used by the routine at CrewAction5_AlienAttack.
GatherSlots_Scan:
  LD HL,$7387             ; HL -> slot 1's room byte (+1)
  LD C,$01                ; C = candidate slot number
  EX AF,AF'               ; keep "self" slot in A'
  LD A,(IX+$01)           ; A = the actor's spot
  LD B,$07                ; scan the 7 crew slots
GatherSlots_Loop:
  CP (HL)                 ; on the same spot?
  JR NZ,GatherSlots_Next  ; no: next slot
  EX AF,AF'               ; is it "self"?
  CP C
  JR Z,GatherSlots_Skip   ; yes: skip it
  EX DE,HL                ; record the slot
  LD (HL),C               ; (NearestCrewA, then NearestCrewB)
  EX DE,HL
  INC DE
GatherSlots_Skip:
  EX AF,AF'               ; (put the spot back in A)
GatherSlots_Next:
  PUSH DE
  LD DE,$0008             ; step to the next record's room byte
  ADD HL,DE
  POP DE
  INC C
  DJNZ GatherSlots_Loop
  RET

; DestroyFrontItem
;
; The current actor's front-hand item is used up (Net) or broken (Tracker):
; point HL at its ItemLocations entry, aim the draw pointer at the front-hand
; menu row, and fall to DestroyHeldItem (DestroyHeldItem), which wipes the item
; and promotes the back-hand item to the front hand.
;
; Used by the routines at CrewHitsAlien and CrewHitsAndroid.
DestroyFrontItem:
  LD HL,$4836             ; draw pointer -> the front-hand row
  LD (DrawScreenPtr),HL   ; (screen row 9, col 22)
  LD BC,(HeldItemFront)   ; C = front item, B = back item
  LD E,C
  LD D,$00
  LD HL,ItemLocations     ; HL -> ItemLocations[front item]
  ADD HL,DE
  JP DestroyHeldItem      ; wipe it (DestroyHeldItem)

; UpdateHandRows
;
; Tail of DestroyHeldItem (DestroyHeldItem) for the on-screen case, entered
; with A = 255 and B = the back-hand item: if both hands are now empty, blank
; the front row; otherwise promote the back-hand item to the front hand (redraw
; both rows, move its ItemLocations marker +32 and apply its courage bonus via
; PickupItemBoost).
;
; Used by the routine at DestroyHeldItem.
UpdateHandRows:
  CP B                    ; back hand empty too (255)?
  JR NZ,UpdateHandRows_Swap
  LD (MenuUseRows),A      ; yes: front-row cell = blank
  CALL HandRows_Finish    ; redraw the hand rows
  RET
UpdateHandRows_Swap:
  LD L,B                  ; front := the back-hand item,
  LD H,A                  ; back := empty (255)
  LD (HeldItemFront),HL
  LD A,L                  ; front-row cell = its string id
  CALL ItemIdToString
  LD (MenuUseRows),A
  CALL DrawSprite         ; draw the front row
  LD A,$FF                ; blank the back row (cell 9)
  LD ($73C7),A
  CALL DrawSprite         ; and draw it
  LD A,(HeldItemFront)    ; A = the promoted item
; This entry point is used by the routine at DestroyHeldItem.
GetItemLocation:
  LD HL,ItemLocations     ; HL -> its ItemLocations entry
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)               ; A = its hand marker (128+slot)
  JP PickupItemBoost      ; make it front-hand: +32 and courage
  RET

; AlienScareCheck — will the alien flee the fight?
;
; After a crew attack: total up how frightening the resistance is, then roll a
; script nibble — if the nibble is below the total (and is not 15), the alien
; flees to a random adjacent room and every fighter present gains courage and
; morale (+1 each, courage capped at 8). The total counts, for each of up to
; three crew in the room (the attacker + room-mates from GatherSameRoomSlots):
; +1 if a fit (strength 3+), in-combat (state 4) fighter is Confident (morale
; 4+); plus the alien's wounds / 4 — a hurt alien is easier to drive off.
;
; The INC (HL) at the end of the Scare_TrackerBonus weapon check is
; unreachable: the two comparisons before it demand A = 6 AND A = 7 at once (JR
; NZ where the intent was presumably a 6-12 weapon range check), so the
; intended "armed with something serious" bonus never applies — an
; original-game bug, preserved as-is.
AlienScareCheck:
  CALL GatherSameRoomSlots ; gather the attacker + up to two other
  XOR A                   ; crew in the alien's room; scare total = 0
  LD (DistanceAccum),A
  PUSH IX
  LD DE,NearestCrewC
  LD B,$03                ; all three gathered slots:
Scare_SlotLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)               ; A = slot (>= 8 = none)
  CP $08
  JR NC,Scare_NextSlot
  PUSH AF
  LD IX,ActorRecords      ; IX -> his record
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  ADD IX,DE
  LD A,$02                ; fit to fight (strength 3+)?
  CP (IX+$04)
  JR NC,Scare_SkipSlot    ; no: he doesn't scare anything
  LD A,$04                ; actually in combat (state 4)?
  CP (IX+$03)
  JR NZ,Scare_SkipSlot    ; no: doesn't count
  LD HL,DistanceAccum
  DEC A                   ; Confident (morale 4+)?
  CP (IX+$06)
  JR NC,Scare_TrackerBonus
  INC (HL)                ; yes: +1 to the scare total
Scare_TrackerBonus:
  POP AF                  ; A = the slot again
  CALL FindHeldItems      ; FindHeldItems: what does he hold?
  LD HL,DistanceAccum
  LD A,(HeldItemFront)    ; front-hand weapon check —
  CP $0D                  ; but the INC below is unreachable:
  JR NC,Scare_AddDistance ; the two equality tests demand
  CP $06                  ; A = 6 and A = 7 at once
  JR NZ,Scare_AddDistance ; (original-game bug, see the header)
  CP $07
  JR NZ,Scare_AddDistance
  INC (HL)                ; (never executed)
Scare_AddDistance:
  LD A,($7382)            ; + the alien's wounds / 4:
  SRA A                   ; a hurt alien is easier
  SRA A                   ; to drive off
  ADD A,(HL)
  LD (HL),A
  PUSH AF                 ; (balances the POP below)
Scare_SkipSlot:
  POP AF
Scare_NextSlot:
  POP DE
  INC DE                  ; next gathered slot
  POP BC
  DJNZ Scare_SlotLoop
  CALL AdvanceScriptPtr   ; roll the next script nibble (0-15)
  CP $0F                  ; 15 = the alien always stands firm
  JR Z,Scare_Done
  LD HL,DistanceAccum     ; nibble below the scare total?
  CP (HL)
  JR NC,Scare_Done        ; no: it stays and fights
  LD IX,ActorRecords      ; yes: it flees — IX -> the alien
Scare_PickFlee:
  CALL PickNextRoom       ; pick an adjacent room, rerolling
  LD A,(IX+$01)           ; until it actually leaves
  CP (IX+$02)             ; (destination != current)
  JR Z,Scare_PickFlee
  LD A,(CrewIndex)        ; move it there at once:
  PUSH AF                 ; run CrewAction1_Arrive with
  XOR A                   ; CrewIndex temporarily = 0
  LD (CrewIndex),A
  CALL CrewAction1_Arrive
  POP AF
  LD (CrewIndex),A
  CALL TriggerAlienXAnim  ; restart the encounter-screen animation
  LD (IX+$00),$00         ; park the alien's countdown
  LD DE,NearestCrewC      ; every fighter present is emboldened:
  LD B,$03
Scare_FleeLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)               ; A = slot (>= 8 = none)
  CP $08
  JR NC,Scare_FleeNext
  ADD A,A                 ; +1 courage and +1 morale
  ADD A,A                 ; (IncPairAt on his +5/+6),
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,$7383
  CALL IncPairAt
  CP $09                  ; courage capped at 8
  JR C,Scare_FleeNext
  LD (HL),$08
Scare_FleeNext:
  POP DE
  INC DE
  POP BC
  DJNZ Scare_FleeLoop
  JP RefreshCondIfViewed  ; refresh the viewed condition line
Scare_Done:
  POP IX
  JP Combat_RefreshRow    ; keep ATTACK on the special row

; AddAlienRoomDamage
;
; Add A damage points to the ALIEN's current room (then fall into the general
; AddRoomDamage below).
;
; Used by the routines at UpdateAlienAI and CrewHitsAlien.
AddAlienRoomDamage:
  LD HL,$737F             ; E = the alien's current room
  LD E,(HL)
; This entry point is used by the routines at FireSpreadCheck and
; CrewHitsAndroid. AddRoomDamage: add A damage points to room E's damage meter
; — a 2-digit ASCII pair in RoomDamageDigits (RoomDamageDigits; tens digit is
; the severity the engine scan at EngineStateScan compares against). Crossing
; "30" raises the room-specific alarm messages #22-#25.
AddRoomDamage:
  BIT 6,E                 ; no damage meters in the duct network
  RET NZ
  LD HL,RoomDamageDigits  ; per-room ASCII damage digits (tens,units)
  LD D,$00
  ADD HL,DE
  ADD HL,DE
  INC HL                  ; HL -> units digit
  LD B,A                  ; B = damage points to add
Damage_PointLoop:
  PUSH BC                 ; per damage point: the meter climbs
  PUSH HL                 ; in half-steps of the tens digit —
  PUSH DE
  LD A,(HL)               ; units already '5'?
  CP $35
  JR Z,Damage_TensCarry
  LD (HL),$35             ; no: units 0 -> 5
  JR Damage_NextPoint
Damage_TensCarry:
  LD (HL),$30             ; yes: units '5' -> '0',
  DEC HL                  ; carry into the tens digit
  INC (HL)
  LD A,(HL)               ; tens reached '3'?
  CP $33
  JR NZ,Damage_ChkStructural
  LD A,E                  ; damage tens digit reached '3': room alarms
  CP $06                  ; CommdCentr?
  JR NZ,Damage_ChkComputer
  LD A,$16                ; msg #22 "Partial loss of control systems"
  JR Damage_SendMsg
Damage_ChkComputer:
  CP $07                  ; Computer room?
  JR NZ,Damage_ChkLifeSupt
  LD A,$17                ; msg #23 "Computer Malfunction detected"
  JR Damage_SendMsg
Damage_ChkLifeSupt:
  CP $19                  ; Life Support?
  JR NZ,Damage_ChkNarcissus
  LD A,$18                ; msg #24 "Environmental irregularities"
  JR Damage_SendMsg
Damage_ChkNarcissus:
  CP $22                  ; the Narcissus?
  JR NZ,Damage_ChkEngines
  LD A,$19                ; msg #25 "Narcissus status RED"
  JR Damage_SendMsg
Damage_ChkEngines:
  CP $11                  ; an engine room (17-19)?
  JR C,Damage_NextPoint
  CP $14
  JR NC,Damage_NextPoint  ; other rooms: no "30" alarm
  AND $03                 ; room 17/18/19 -> flag bit 3/4/5:
  LD B,A
  LD A,$04
Damage_EngineBit:
  ADD A,A                 ; A = 4 << (room & 3)
  DJNZ Damage_EngineBit
  LD HL,ShipSystemFlags   ; the engine room catches FIRE —
  OR (HL)                 ; set its ShipSystemFlags bit
  LD (HL),A
  LD L,E                  ; L = room for the {room} field
  LD A,$09                ; msg #9 "WARNING: Fire in {room}"
  CALL EnqueueMessage
  JR Damage_NextPoint
Damage_ChkStructural:
  CP $37                  ; tens reached '7' (70 damage)?
  JR NZ,Damage_ChkOverflow
  LD L,E                  ; L = room
  LD A,$08                ; msg #8 "Structural Damage in {room}"
Damage_SendMsg:
  CALL EnqueueMessage
  JR Damage_NextPoint
Damage_ChkOverflow:
  CP $3A                  ; tens past '9' (100 damage)?
  CALL Z,DamageOverflowFlash ; the ship breaks up - game over
Damage_NextPoint:
  POP DE
  POP HL
  POP BC
  DJNZ Damage_PointLoop   ; next damage point
  LD A,(RoomModeByte)     ; is the damaged room the one on
  CP $01                  ; screen?
  RET NZ
  LD A,(CurrentRoom)
  CP E
  CALL Z,DrawRoomTitle    ; yes: redraw the title/damage line
  RET

; DamageOverflowFlash — the hull gives way
;
; A room's damage meter has passed 99 (or the destruct countdown ran out): ~7
; seconds of screen flashing with a rainbow border, then the ship is lost
; (DamageGameOver DamageGameOver).
;
; Used by the routines at AddAlienRoomDamage and CrewAction6_Handler.
DamageOverflowFlash:
  PUSH BC
  PUSH AF
  LD B,$64                ; 100 flash cycles:
DamageFlash_Loop:
  PUSH BC
  CALL BusyWait           ; ~36 ms delay,
  LD A,$07                ; attributes = white ink on black,
  CALL FillAttributes
  CALL BusyWait           ; ~36 ms delay,
  LD A,$38                ; attributes = black ink on white
  CALL FillAttributes
  POP BC
  LD A,B                  ; border cycles through
  AND $07                 ; all 8 colours
  OUT ($FE),A
  DJNZ DamageFlash_Loop
  POP AF
  POP BC
  JP DamageGameOver       ; game over: the Nostromo is lost
; Ship-status dashboard: counters and ASCII digit cells (previously
; mis-disassembled as code). The digit cells double as the source text drawn on
; the status bar, and the endgame renderer copies them into the "The Nostromo
; will explode in .. minutes .. seconds" screen.
EngineFixCounts:
  DEFB $00,$00,$00        ; per-engine-room (17/18/19) fire-fighting progress,
                          ; +1 per squirt (FightFireSquirt FightFireSquirt)
ShipTickCounter:
  DEFB $32                ; ship-systems tick countdown (reloaded with 100 by
                          ; the engine-state scan at EngineStateScan)
DestructTickCounter:
  DEFB $03                ; destruct-second countdown (reloaded with 20 by the
                          ; ticker at DestructTicker)
AdjTestRoom:
  DEFB $00                ; scratch: room of the actor under adjacency test
OxygenTickInit:
  DEFB $0A                ; oxygen tick reload value
DestructTimeText:
  DEFM "TIME: "           ; status-bar label for the destruct countdown
DestructMinutes:
  DEFB $00                ; minutes digit, ASCII ('5' when armed at
                          ; DestructArm)
  DEFM ":"
DestructSeconds:
  DEFB $00,$00            ; seconds digits, ASCII ("00" when armed); the low
                          ; digit also drives the border flicker while armed
OxygenText:
  DEFM "TOOH: "           ; oxygen label as stored on the tape (sic)
OxygenHi:
  DEFM "75"               ; oxygen level, ASCII, counting down; msg #26
OxygenLo:
  DEFM "00"               ; "Oxygen status CRITICAL" fires at "05"+"00"
EventPaceCounter:
  DEFB $00                ; free-running pace counter gating an event every 8
                          ; ticks (inside UpdateRoomActors)

; UpdateRoomActors — per-frame ship-simulation driver
;
; Despite the old name, this is the SHIP-SIMULATION driver: six calls covering
; the destruct countdown, fixture proximity events, the oxygen clock, event
; pacing, engine states and the alien encounter animation. The fixture-action
; handlers the action key dispatches into (destruct lever, hypersleep pods,
; fires, airlocks, launch console) follow as their own routines from
; FixtureAction (FixtureAction).
;
; Used by the routine at GameEntry.
UpdateRoomActors:
  CALL DestructTicker     ; DestructTicker: countdown + border flash
  CALL FindTrackerHolders ; FindTrackerHolders: motion-tracker scan
  CALL OxygenTick         ; oxygen clock / status line
  CALL ViewedTrackerCheck ; ViewedTrackerCheck: beep / uneasy cat
  CALL EngineStateScan    ; EngineStateScan: engine fires
  CALL UpdateAlienAnim    ; alien encounter animation layers
  RET

; FixtureAction
;
; The action-key (key 0) handler for corridor fixtures, dispatched via the
; RoomDispatchTable tail. Reads the corridor cell under the cursor and
; dispatches on the CELL VALUE: 70 -> arm self-destruct     72 -> abort
; self-destruct 74 -> enter hypersleep pod  76 -> fight fire (engine rooms) 78
; -> Narcissus launch      53/54 -> blow airlock #1/#2 55/56 -> seal airlock
; #1/#2
FixtureAction:
  CALL CancelActionTimer  ; cancel any pending countdown;
  LD HL,CorridorPosTable  ; A = the cursor row —
  LD E,A                  ; index CorridorPosTable by it
  LD D,$00
  ADD HL,DE
  LD A,(HL)               ; A = cell value under the cursor
  LD (CursorCellValue),A
  CP $46                  ; destruct lever (off)?
  JR Z,DestructArm
  CP $48                  ; destruct lever (armed)?
  JR Z,DestructAbort
  CP $4A                  ; hypersleep pod?
  JR Z,HypersleepStart
  CP $4C                  ; fire?
  JR Z,FightFire
  CP $4E                  ; launch console?
  JP Z,LaunchGate
  CP $35                  ; airlock #1 door (closed)?
  JP Z,BlowLock           ; blow it
  CP $36                  ; airlock #2 door (closed)?
  JP Z,BlowLock           ; blow it
  CP $37                  ; airlock #1 door (blown)?
  JP Z,SealLock           ; seal it
  CP $38                  ; airlock #2 door (blown)?
  JP Z,SealLock           ; seal it
  RET                     ; anything else: no action

; DestructArm
DestructArm:
  LD A,$0D                ; msg #13 "DESTRUCT SEQUENCE INITIATED"
  CALL EnqueueMessage
  LD A,$48                ; cells 15/16 := armed lever sprites 72/73
  LD ($73CD),A
  LD HL,$5016             ; redraw both rows
  LD (DrawScreenPtr),HL   ; (screen rows 16/17, col 22)
  CALL DrawSprite
  LD A,$49
  LD (MenuFixtureRow),A
  CALL DrawSprite
  JR DestructSetTimer

; DestructAbort
DestructAbort:
  LD A,$0E                ; msg #14 "DESTRUCT SEQUENCE ABORTED"
  CALL EnqueueMessage
  LD A,$46                ; cells 15/16 := off lever sprites 70/71
  LD ($73CD),A
  LD HL,$5016             ; redraw both rows
  LD (DrawScreenPtr),HL   ; (screen rows 16/17, col 22)
  CALL DrawSprite
  LD A,$47
  LD (MenuFixtureRow),A
  CALL DrawSprite
DestructSetTimer:
  LD HL,$3030             ; HL = "00" in ASCII
  LD (DestructSeconds),HL ; DestructSeconds := "00"
  LD HL,DestructMinutes
  LD (HL),$35             ; DestructMinutes := '5' -> 5:00 countdown
  LD HL,ShipSystemFlags
  LD A,(HL)
  XOR $01                 ; toggle ShipSystemFlags bit 0 (destruct armed)
  LD (HL),A
  RET

; HypersleepStart
HypersleepStart:
  LD (IX+$03),$06         ; action state 6 = entering hypersleep
  LD (IX+$00),$FF         ; over a long 255-frame countdown
  LD A,$0B                ; msg #11 "{actor} prepares for hypersleep"
  LD HL,(DrawSlotIndex)   ; L = the viewed crew member
  CALL EnqueueMessage
  RET

; FightFire — action on the fire label (cell value 76)
;
; Needs a Fire Extng (item 8-11) in the front hand; a charged one delivers a
; squirt towards putting the engine-room fire out (FireDamageTick decides how
; many are needed).
FightFire:
  LD A,(HeldItemFront)    ; front-hand item an extinguisher
  CP $08                  ; (8-11)?
  RET C                   ; no: nothing to fight with
  CP $0C
  RET NC
  LD E,A
  LD D,$00
  LD HL,ItemCharges       ; HL -> that extinguisher's charges
  ADD HL,DE
  XOR A                   ; any CO2 left?
  CP (HL)
  JR NZ,FightFireSquirt   ; yes: squirt
  LD HL,(DrawSlotIndex)   ; empty: L = the viewed crew member,
  LD A,$06                ; msg #6 "{actor}'s extinguisher is empty"
  CALL EnqueueMessage
  RET

; FightFireSquirt
FightFireSquirt:
  DEC (HL)                ; use one extinguisher charge
  LD A,(CurrentRoom)
  SUB $11                 ; engine-room index 0-2 (rooms 17/18/19)
  LD E,A
  LD HL,EngineFixCounts
  ADD HL,DE
  INC (HL)                ; bump this engine room's fire-fighting progress
  RET

; SealLock
SealLock:
  LD HL,ShipSystemFlags   ; SealLock: repressurise an airlock
  LD A,(CorridorCursor)
  SUB $0F                 ; cursor cell - 15 -> 0 = airlock #1, 1 = #2
  AND A
  JR Z,SealLock1
  RES 2,(HL)              ; clear "airlock #2 blown" flag
  LD A,$36                ; cell := closed-door sprite 54
  LD (MenuFixtureRow),A
  LD HL,$5036
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  JR SealLockMsg

; SealLock1
SealLock1:
  RES 1,(HL)              ; clear "airlock #1 blown" flag
  LD A,$35                ; cell := closed-door sprite 53
  LD ($73CD),A
  LD HL,$5016
  LD (DrawScreenPtr),HL
  CALL DrawSprite
SealLockMsg:
  LD A,(CorridorCursor)
  SUB $0F
  LD L,A                  ; L = airlock room (0/1) for the {sprite} field
  LD A,$11                ; msg #17 "{room} PRESSURE RESTORED"
  CALL EnqueueMessage
  RET

; BlowLock
BlowLock:
  LD HL,ShipSystemFlags   ; BlowLock: blow an airlock open
  LD A,(CorridorCursor)
  SUB $0F                 ; cursor cell - 15 -> 0 = airlock #1, 1 = #2
  AND A
  JR Z,BlowLock1
  SET 2,(HL)              ; set "airlock #2 blown" flag
  LD A,$38                ; cell := blown-door sprite 56
  LD (MenuFixtureRow),A
  LD HL,$5036
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  JR BlowLockSweep

; BlowLock1
BlowLock1:
  LD A,$37                ; cell := blown-door sprite 55
  SET 1,(HL)              ; set "airlock #1 blown" flag
  LD ($73CD),A
  LD HL,$5016
  LD (DrawScreenPtr),HL
  CALL DrawSprite
BlowLockSweep:
  LD A,(CorridorCursor)
  SUB $0F                 ; A = the depressurised room (0/1)
  PUSH IX
  LD B,$08                ; sweep all 8 actor slots (alien included!)
  LD IX,ActorRecords
  LD DE,$0008
BlowLock_SweepLoop:
  CP (IX+$01)             ; actor in the depressurised room?
  JR NZ,BlowLock_NextActor
  LD (IX+$01),$FF         ; yes: swept into space - room := none,
  LD (IX+$07),$01         ; status := dead
BlowLock_NextActor:
  ADD IX,DE
  DJNZ BlowLock_SweepLoop
  POP IX
  LD HL,ItemLocations     ; clear their markers from the corridor strip
  LD B,$16
BlowLock_ItemLoop:
  CP (HL)
  JR NZ,BlowLock_NextItem
  LD (HL),$FF
BlowLock_NextItem:
  INC HL
  DJNZ BlowLock_ItemLoop
  LD L,A
  LD A,$10                ; msg #16 "WARNING: {room} DEPRESSURISED"
  CALL EnqueueMessage
  LD HL,$737F             ; was the ALIEN in there?
  LD A,$FF
  CP (HL)                 ; alien's room = 255 means it was swept out
  RET NZ
  LD A,($7382)            ; alien already wounded 3+ times:
  CP $03
  JP NC,EndgameScreen     ; it dies in space -> endgame/rating screen
  CALL AdvanceScriptPtr   ; else ROM-script roll: nibble >= 2
  CP $02
  JP NC,EndgameScreen     ; (14 in 16): the alien dies -> endgame
  LD A,$20                ; survived! it crawls back in:
  LD ($737F),A            ; alien's room := 32 (ShuttleBay)
  LD L,A
  LD A,$12                ; msg #18 "INTRUDER ENTERING {ShuttleBay}"
  CALL EnqueueMessage
  LD HL,ActorRecords
  LD (HL),$00             ; reset the alien's action byte
  RET

; CrewAction6_Handler — hypersleep completed (action state 6)
;
; Dispatched via CrewActionDispatch entry 6: the 255-frame countdown queued by
; HypersleepStart (HypersleepStart) has expired. The sleeper leaves play (+7 =
; 255, room = 255 — the same removed pattern as the chestburster host) and
; whatever he carried is stacked in the Cryo Vault (room 15).
CrewAction6_Handler:
  LD HL,(CrewIndex)       ; L = the sleeper
  LD A,$0C                ; msg #12 "{actor} enters hypersleep"
  CALL EnqueueMessage
  LD (IX+$07),$FF         ; status = removed from play,
  LD (IX+$01),$FF         ; room = none
  LD HL,ItemLocations
  LD A,(CrewIndex)
  ADD A,$80               ; E = his back-hand marker
  LD E,A
  ADD A,$20               ; D = his front-hand marker
  LD D,A
  LD C,(IX+$01)           ; (C = 255 now, unused)
  LD B,$16                ; anything he carried
RedirectRoomLoop:
  LD A,(HL)               ; is left in the
  CP E                    ; Cryo Vault (room 15):
  JR Z,RedirectRoomSet
  CP D
  JR NZ,RedirectRoomNext
RedirectRoomSet:
  LD (HL),$0F
RedirectRoomNext:
  INC HL
  DJNZ RedirectRoomLoop
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(DrawSlotIndex)    ; sleeper viewed -> rebuild the view,
  LD HL,CrewIndex         ; else redraw the room-mate lists
  JP KillActor_Bounce     ; (KillActor_Bounce)
  NOP
  NOP
DestructTicker:
  LD HL,ShipSystemFlags   ; DestructTicker (per frame)
  BIT 0,(HL)              ; destruct armed?
  RET Z                   ; no: done
  LD A,($95DF)            ; flash the border from the seconds digit
  AND $03
  OUT ($FE),A
  LD HL,DestructTickCounter
  DEC (HL)
  RET NZ
  LD (HL),$14             ; every 20 frames = one game second
  LD A,$2F                ; '/' = '0'-1: ASCII underflow sentinel
  LD HL,(DestructSeconds) ; L = seconds tens, H = seconds units
  DEC H
  CP H
  JR NZ,StorePanelPtr
  LD H,$39                ; units wrapped: back to '9'
  DEC L
  CP L
  JR NZ,StorePanelPtr
  LD L,$35                ; tens wrapped: back to '5' (fresh :59..:50)
  LD (DestructSeconds),HL
  LD HL,DestructMinutes
  DEC (HL)                ; borrow into the minutes digit
  CP (HL)
  JP Z,DamageOverflowFlash ; minutes below '0': BOOM - flash + game over
  JP OxygenCheckRedraw    ; redraw countdown / oxygen status
StorePanelPtr:
  LD (DestructSeconds),HL
  JP OxygenCheckRedraw

; FindTrackerHolders — run the motion-tracker scan
;
; The two Trackers (items 6/7) and the carried cat ("Cat in Net"/"Cat in Box",
; items 20/21) are motion detectors: for each one held in a FRONT hand (marker
; >= 160), TrackerHolderRoom (TrackerHolderRoom) sweeps the rooms adjacent to
; the holder and fills the matching NearRoomList entry (NearRoomList: tracker
; #1, tracker #2, the cat) with the holder's room if nothing moves next door
; ("all clear" — worth +1 morale to everyone standing there,
; MoraleFromCompanion), or 255 if something does (which makes the viewed
; holder's tracker beep, or Jones grow uneasy — ViewedTrackerCheck
; ViewedTrackerCheck).
;
; Used by the routine at UpdateRoomActors.
FindTrackerHolders:
  LD HL,NearRoomList      ; HL -> NearRoomList entry 0
  LD A,(TrackerLocations) ; Tracker #1 (item 6) in a front
  CP $A0                  ; hand (marker >= 160)?
  CALL NC,TrackerHolderRoom ; yes: scan around its holder
  INC HL                  ; entry 1:
  LD A,($74FB)            ; Tracker #2 (item 7)
  CP $A0
  CALL NC,TrackerHolderRoom
  INC HL                  ; entry 2 (shared):
  LD A,(CatItemLocations) ; Cat in Net (item 20)
  CP $A0
  CALL NC,TrackerHolderRoom
  LD A,($7509)            ; Cat in Box (item 21)
  CP $A0
  CALL NC,TrackerHolderRoom
  RET

; TrackerHolderRoom — sweep the rooms around one tracker holder
;
; A = the front-hand marker (160+slot), HL -> the NearRoomList entry to fill:
; walk the holder's 5 adjacency slots (corridor map, or duct map when he is in
; the ducts) and write the holder's room ("all clear") — unless Jones, the
; alien, or any live actor is in an adjacent room, in which case the entry
; becomes 255 ("motion detected", via PickRoom_Reject).
;
; Used by the routine at FindTrackerHolders.
TrackerHolderRoom:
  CP $FF                  ; marker 255 = the item does not exist
  RET Z
  PUSH HL                 ; save the NearRoomList entry ptr
  SUB $A0                 ; A = the holder's slot;
  ADD A,A                 ; HL -> his room byte (+1)
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,$737F
  ADD HL,DE
  LD A,(HL)               ; AdjTestRoom = the holder's room
  LD (AdjTestRoom),A
  LD BC,RoomAdjCorridors  ; BC = RoomAdjCorridors...
  BIT 6,A                 ; ...unless he is in the ducts:
  JR Z,AdjRowFromRoom
  CALL DuctSearchInit     ; then RoomAdjDucts (DuctSearchInit)
AdjRowFromRoom:
  LD L,A                  ; HL = map + room*5:
  LD H,$00                ; the room's 5 direction slots
  PUSH HL
  ADD HL,HL
  ADD HL,HL
  POP DE
  ADD HL,DE
  ADD HL,BC
  LD DE,$0008
  LD B,$05                ; each direction slot:
PickRoomScanLoop:
  PUSH BC
  PUSH HL
  LD C,(HL)               ; C = the adjacent room
  LD A,(AdjTestRoom)      ; own room = no exit that way:
  CP C                    ; skip the slot
  JR Z,PickRoomScanNext
  LD A,(JonesRoom)        ; anything moving in there?
  JP RoomRejectCheck      ; Jones / the alien / live actors

; PickRoom_ActorScan — tail of the tracker sweep
;
; C = an adjacent room: reject the sweep (motion detected) if any live actor
; stands in it; otherwise continue with the next direction slot, and after all
; five write the "all clear" result.
;
; Used by the routine at RoomRejectCheck.
PickRoom_ActorScan:
  LD B,$08                ; all 8 actor slots:
  LD HL,$737F             ; walk the +1 (room) column
PickRoom_ActorLoop:
  LD A,(HL)               ; in the adjacent room?
  AND $3F
  CP C
  JR NZ,PickRoom_ActorNext
  PUSH HL                 ; yes: alive (+7 = 0)?
  ADD HL,DE
  DEC HL
  DEC HL
  XOR A
  CP (HL)
  JR Z,PickRoom_PopReject ; yes: motion detected
  POP HL
PickRoom_ActorNext:
  ADD HL,DE
  DJNZ PickRoom_ActorLoop
PickRoomScanNext:
  POP HL                  ; this direction is quiet:
  INC HL                  ; next direction slot
  POP BC
  DJNZ PickRoomScanLoop
  POP HL                  ; all five quiet: NearRoomList
  LD A,(AdjTestRoom)      ; entry = the holder's room
  LD (HL),A               ; ("all clear")
  RET
PickRoom_PopReject:
  POP HL
; This entry point is used by the routine at RoomRejectCheck.
PickRoom_Reject:
  POP HL                  ; motion detected: unwind the scan
  POP BC                  ; and write 255 into the
  POP HL                  ; NearRoomList entry
  LD (HL),$FF
  RET

; EngineStateScan — tick the three engine-room fires
;
; ShipSystemFlags bits 3-5 mark fires in Engine #1/#2/#3 (rooms 17-19, lit by
; the damage-meter alarm at Damage_ChkEngines). Each burning engine goes
; through FireDamageTick; then the 100-frame ship tick that paces fire damage
; is advanced.
;
; Used by the routine at UpdateRoomActors.
EngineStateScan:
  LD D,$08                ; D = bit mask (bit 3),
  LD A,(ShipSystemFlags)  ; A = ShipSystemFlags,
  LD E,$11                ; E = Engine #1's room
  BIT 3,A                 ; Engine #1 on fire?
  CALL NZ,FireDamageTick
  SLA D                   ; next bit / next room:
  INC E
  BIT 4,A                 ; Engine #2 on fire?
  CALL NZ,FireDamageTick
  SLA D
  INC E
  BIT 5,A                 ; Engine #3 on fire?
  CALL NZ,FireDamageTick
  LD HL,ShipTickCounter   ; advance the 100-frame ship tick
  DEC (HL)
  RET NZ
  LD (HL),$64
  RET

; FireDamageTick — one burning engine room (E), fire bit in D
;
; The number of extinguisher squirts needed grows with the damage already done
; (1, 2 from "60", 3 from "80"). Enough squirts put the fire out: clear its
; ShipSystemFlags bit, blank the on-screen fire label, msg #15. Not enough: the
; fire keeps burning and adds a damage point every 100 frames
; (FireSpreadCheck).
;
; Used by the routine at EngineStateScan.
FireDamageTick:
  PUSH AF
  PUSH DE
  LD C,$01                ; C = squirts needed: 1,
  LD B,D                  ; (B = the engine's fire bit)
  LD D,$00
  LD HL,RoomDamageDigits  ; HL -> the room's damage tens digit
  ADD HL,DE
  ADD HL,DE
  LD A,(HL)
  CP $36                  ; +1 from "60" damage,
  JR C,FireThresholdCheck
  INC C
  CP $38                  ; +1 more from "80"
  JR C,FireThresholdCheck
  INC C
FireThresholdCheck:
  LD HL,$95BE             ; A = squirts delivered so far
  ADD HL,DE               ; (EngineFixCounts, base-17 indexed)
  LD A,(HL)
  CP C                    ; enough to put it out?
  JR C,FireSpreadCheck    ; no: the fire keeps burning
  LD A,(ShipSystemFlags)  ; yes: clear the engine's fire bit
  XOR B
  LD (ShipSystemFlags),A
  LD A,(RoomModeByte)     ; is the burning room on screen?
  CP $01
  JR NZ,FireExtinguishedMsg
  LD A,(CurrentRoom)
  CP E
  JR NZ,FireExtinguishedMsg
  LD HL,$FFFF             ; yes: blank the fire label —
  LD ($73CD),HL           ; cells 15/16 = empty
  LD A,L
  LD HL,$5016             ; and redraw those rows
  LD (DrawScreenPtr),HL   ; (screen rows 16/17, col 22)
  CALL DrawSprite
  LD A,$FF
  CALL DrawSprite
FireExtinguishedMsg:
  POP HL                  ; L = the room
  PUSH HL
  LD A,$0F                ; msg #15 "Fire extinguished in {room}"
  CALL EnqueueMessage
  POP DE
  POP AF
  RET

; FireSpreadCheck
;
; The fire is still burning: on the frame the 100-frame ship tick expires, it
; does another damage point to the room.
;
; Used by the routine at FireDamageTick.
FireSpreadCheck:
  LD A,(ShipTickCounter)  ; ship tick about to expire?
  DEC A
  LD A,$01                ; then the fire adds
  CALL Z,AddRoomDamage    ; +1 damage point to room E
  POP DE
  POP AF
  RET

; ViewedTrackerCheck — sound the viewed crew member's detector
;
; In the room view: if the viewed crew member holds a Tracker in the front
; hand, and its NearRoomList entry reads 255 (motion detected next door — the
; sweep at FindTrackerHolders), the tracker beeps every 8th pass; if he carries
; the caught cat instead, msg #19 "Jones is looking uneasy" fires.
;
; Used by the routine at UpdateRoomActors.
ViewedTrackerCheck:
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(DrawSlotIndex)    ; C = the viewed crew member's
  ADD A,$A0               ; front-hand marker
  LD C,A
  LD HL,NearRoomList      ; is he holding Tracker #1?
  LD A,(TrackerLocations)
  CP C
  JR Z,TrackerBeepTick    ; yes: beep check on entry 0
  LD A,($74FB)            ; Tracker #2?
  INC HL
  CP C
  JR Z,TrackerBeepTick    ; yes: beep check on entry 1
  INC HL                  ; the caught cat?
  LD A,(CatItemLocations)
  CP C
  JR Z,JonesUneasyMsg     ; yes: unease check on entry 2
  LD A,($7509)
  CP C
  JR Z,JonesUneasyMsg
  RET                     ; holding no detector

; TrackerBeepTick
;
; (HL) = the tracker's NearRoomList entry: when it reads 255 (motion detected
; next door), beep on every 8th pass.
;
; Used by the routine at ViewedTrackerCheck.
TrackerBeepTick:
  LD A,(HL)               ; motion detected (entry = 255)?
  CP $FF
  RET NZ                  ; no: silence
  LD HL,EventPaceCounter  ; yes: pace the alert —
  INC (HL)                ; a ROM beep every 8th pass
  LD A,(HL)
  AND $07
  CALL Z,RomBeep
  RET

; JonesUneasyMsg
;
; (HL) = the carried cat's NearRoomList entry: when it reads 255 (motion
; detected next door), the cat senses it.
;
; Used by the routine at ViewedTrackerCheck.
JonesUneasyMsg:
  LD A,(HL)               ; motion detected (entry = 255)?
  CP $FF
  RET NZ                  ; no: the cat is calm
  LD A,(CurrentRoom)
  LD L,A                  ; L = current room
  LD A,$13                ; msg #19 "Jones is looking uneasy"
  CALL EnqueueMessage
  RET

; RomBeep
;
; One short beep via the ZX ROM BEEPER routine ($03B5): DE = 5 cycles, HL = 549
; (~790 Hz).
;
; Used by the routines at TrackerBeepTick and IntroductionMode.
RomBeep:
  PUSH IX                 ; (BEEPER corrupts IX)
  LD DE,$0005             ; 5 cycles
  LD HL,$0225             ; at ~755 Hz
  CALL $03B5              ; ROM BEEPER
  POP IX
  RET

; CrewAction5_AlienAttack — the alien's attack tick (action state 5)
;
; Dispatched via CrewActionDispatch entry 5, queued by UpdateAlienAI
; (UpdateAlienAI) when crew share the alien's spot. Re-gathers the crew on the
; alien's spot (up to three into NearestCrewC/A/B), picks one by script roll,
; and drains a strength point: below 2 the victim collapses (VictimItemDrop
; VictimItemDrop, msg #21), at 2-3 msg #10 "is being wounded", and a still-fit
; victim (4+) gets msg #1 "The ALIEN is attacking {actor}". A chosen victim who
; is already dead may be dragged off the ship (Wound_PanicRoll, in the
; VictimItemDrop block), else another gathered slot is tried; with nobody left
; the alien moves on.
CrewAction5_AlienAttack:
  LD HL,$FFFF             ; NearestCrewA/B = 255/255
  LD (NearestCrewA),HL
  LD DE,NearestCrewC      ; NearestCrewC = 255 too
  LD A,$FF
  LD (DE),A
  XOR A                   ; gather the crew on the alien's spot
  CALL GatherSlots_Scan   ; into C/A/B ("self" = slot 0)
  LD C,$00                ; count them:
  LD HL,NearestCrewC
  LD A,$FF
  LD B,$03
AlienAtk_CountLoop:
  CP (HL)
  JR Z,AlienAtk_CountNext
  INC C
AlienAtk_CountNext:
  INC HL
  DJNZ AlienAtk_CountLoop
  LD A,C                  ; nobody there after all?
  AND A
  RET Z                   ; then no attack
  CP $01                  ; one victim:
  JR NZ,AlienAtk_PickTwo
  XOR A                   ; take entry 0
  JR AlienAtk_TakeSlot
AlienAtk_PickTwo:
  CP $02                  ; two victims:
  JR NZ,AlienAtk_PickAny
  CALL AdvanceScriptPtr   ; a script roll picks
  AND $01                 ; entry 0 or 1
  JR AlienAtk_TakeSlot
AlienAtk_PickAny:
  CALL AdvanceScriptPtr   ; three victims: script roll & 7,
  AND $07                 ; mapped through the direction table,
  CALL ScriptNibbleToDirection ; picks entry 0-2
AlienAtk_TakeSlot:
  LD E,A
  LD D,$00
  LD HL,NearestCrewC      ; C = the chosen victim's slot,
  ADD HL,DE
  LD C,(HL)
  LD (HL),$FF             ; taken off the list
  LD A,C
AlienAtk_SlotAddr:
  ADD A,A                 ; HL -> the victim's status byte (+7)
  ADD A,A
  ADD A,A
  ADD A,$07
  LD E,A
  LD D,$00
  LD HL,ActorRecords
  ADD HL,DE
  XOR A                   ; still alive?
  CP (HL)
  JP NZ,Wound_PanicRoll   ; no: a corpse — maybe drag it off
  DEC HL
  DEC HL
  DEC HL                  ; HL -> victim's +4 strength
  DEC (HL)                ; the alien drains one strength point
  LD A,(HL)
  CP $02                  ; below 2: victim collapses
  JR C,VictimItemDrop
  CP $04                  ; 2-3: wounded
  JR C,WoundMsg
  LD L,C                  ; still healthy:
  LD A,$01                ; msg #1 "The ALIEN is attacking {actor}"
  CALL EnqueueMessage
  RET

; WoundMsg
;
; A victim (slot in C) has been wounded to strength 2-3 by the alien: log it
; and refresh the viewed condition line.
WoundMsg:
  LD L,C                  ; L = the victim
  LD A,$0A                ; msg #10 "{actor} is being wounded"
  CALL EnqueueMessage
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  PUSH IX
  LD A,(DrawSlotIndex)    ; re-point IX at the viewed slot
  ADD A,A
  ADD A,A
  ADD A,A
  LD IX,ActorRecords
  LD E,A
  LD D,$00
  ADD IX,DE
  CALL DrawCrewCondition  ; and redraw his condition line
  POP IX
  RET

; VictimItemDrop — a victim collapses
;
; Entered with HL -> the victim's strength byte (+4, just dropped below 2) and
; C = his slot: mark him dead with witness-morale processing (KillActorRefresh
; -> KillActorMoraleHit), park his countdown, drop everything he carried onto
; the floor, and if he is the crew member on screen, log msg #21 and rebuild
; the view.
;
; Used by the routines at CrewAction5_AlienAttack and CrewAction7_Handler.
VictimItemDrop:
  INC HL                  ; HL -> his morale byte (+6):
  INC HL                  ; the KillActorMoraleHit entry point
  CALL KillActorRefresh   ; mark dead + witness morale + redraw
  LD DE,$FFF9             ; countdown (+0) = 0:
  ADD HL,DE               ; the body never acts again
  LD (HL),$00
  LD A,C                  ; A = the victim's slot
  PUSH AF
  ADD A,$80               ; E = his back-hand marker
  LD E,A
  ADD A,$20               ; D = his front-hand marker
  LD D,A
  LD C,(IX+$01)           ; C = the room he fell in
  LD B,$16                ; all 22 items:
  LD HL,ItemLocations
VictimItems_Loop:
  LD A,(HL)
  CP E                    ; held in either hand?
  JR Z,VictimItems_Set
  CP D
  JR NZ,VictimItems_Next
VictimItems_Set:
  LD (HL),C               ; yes: it drops to the floor here
VictimItems_Next:
  INC HL
  DJNZ VictimItems_Loop
  POP AF                  ; is the victim the crew member
  LD L,A                  ; on screen?
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(DrawSlotIndex)
  CP L
  RET NZ                  ; no: done
  LD A,$15                ; msg #21 "{actor} has collapsed"
  CALL EnqueueMessage
  JP InitGameView         ; and rebuild the whole view
; Wound_PanicRoll — the alien's chosen victim is already dead. If the alien is
; not carrying a body (+5 = 0), a 4-in-16 script roll lets it DRAG THE CORPSE
; OFF: the body's room becomes 255 (gone from the ship) and its slot is
; remembered in the alien's +5. Otherwise another gathered slot is attacked
; instead, or the alien moves on.
Wound_PanicRoll:
  XOR A                   ; already carrying a body (+5 != 0)?
  CP (IX+$05)
  JR NZ,Wound_TrioPick    ; yes: try another victim
  PUSH HL                 ; no: script roll < 4
  CALL AdvanceScriptPtr   ; (4 in 16)?
  POP HL
  CP $04
  JR NC,Wound_TrioPick
  LD DE,$FFFA             ; yes: drag the corpse away —
  ADD HL,DE               ; its room byte (+1) = 255,
  LD (HL),$FF             ; gone from the ship,
  CALL SetCourageRefresh  ; and the alien's +5 = the slot
  RET
Wound_TrioPick:
  LD DE,$0001             ; scan the gathered slots for another
  LD HL,NearestCrewC      ; victim (direction picked by the
  CP $07                  ; leftover roll: < 7 forward from C,
  JR C,Wound_TrioLoop     ; else backward from B)
  LD DE,$FFFF
  LD HL,NearestCrewB
Wound_TrioLoop:
  LD B,$03
Wound_TrioRead:
  LD A,(HL)               ; skip empties and the corpse
  CP $FF                  ; we just tried
  JR Z,Wound_TrioNext
  CP C
  JR Z,Wound_TrioNext
  LD (HL),$FF             ; take this one off the list
  LD C,A
  JP AlienAtk_SlotAddr    ; and attack him instead
Wound_TrioNext:
  ADD HL,DE
  DJNZ Wound_TrioRead
  CALL PickNextRoom       ; nobody left: the alien moves on
  LD (IX+$00),$28         ; (move/idle, timer 40)
  LD (IX+$03),$01
  RET

; OxygenTick — the oxygen clock
;
; A countdown whose period is 2 x the number of live actors: the fewer are left
; to run the ship, the faster the oxygen drains. Each expiry knocks one off the
; 4-digit ASCII oxygen counter ("7500" at the start), with the borrow rippling
; left; when the borrow runs past the digits into the label, the air is gone
; (see OxygenCheckRedraw).
;
; Used by the routine at UpdateRoomActors.
OxygenTick:
  LD HL,OxygenTickInit    ; tick the oxygen countdown
  DEC (HL)
  RET NZ
  LD HL,ActorStatusColumn ; expired: count the live actors
  LD DE,$0008             ; (status +7 = 0, alien included)
  LD BC,$0800             ; over all 8 slots
  XOR A
OxygenTick_CountLoop:
  CP (HL)
  JR Z,OxygenTick_CountNext
  INC C
OxygenTick_CountNext:
  ADD HL,DE
  DJNZ OxygenTick_CountLoop
  LD A,C                  ; reload period = 2 x live count
  ADD A,A
  LD (OxygenTickInit),A
  LD A,$2F                ; multi-digit ASCII decrement:
  LD HL,$95E9             ; units digit first,
OxygenTick_Borrow:
  DEC (HL)
  CP (HL)                 ; on '0'-1 ('/')
  JR NZ,OxygenCheckRedraw
  LD (HL),$39             ; wrap to '9' and
  DEC HL                  ; borrow one digit left
  JR OxygenTick_Borrow

; OxygenCheckRedraw — oxygen alarms + the bottom status line
;
; Fire msg #26 when the oxygen counter reads exactly "0500"; jump to
; Endgame_OxygenOut when the borrow has eaten past the digits (the air ran
; out); otherwise redraw the bottom-right status line — "TOOH: nnnn" (the
; oxygen counter), or "TIME: m:ss" while the self-destruct is armed.
;
; Used by the routines at OrdersMenuNextRow, DrawSelectedName,
; CrewAction6_Handler and OxygenTick.
OxygenCheckRedraw:
  LD HL,(OxygenHi)        ; high digits = "05"...
  LD A,L
  CP $30
  JR NZ,ClockPanelUpdate
  LD A,H
  CP $35
  JR NZ,ClockPanelUpdate
  LD HL,(OxygenLo)        ; ...and low digits = "00"?
  LD A,$30
  CP L
  JR NZ,ClockPanelUpdate
  CP H
  JR NZ,ClockPanelUpdate
  LD A,$1A                ; msg #26 "Oxygen status CRITICAL"
  CALL EnqueueMessage
ClockPanelUpdate:
  LD A,($95E5)            ; did the borrow ripple past the
  CP $20                  ; digits (oxygen fully exhausted)?
  JP NZ,Endgame_OxygenOut ; yes: Endgame_OxygenOut
  LD A,(RoomModeByte)     ; no status line over the
  CP $02                  ; Indicate Location list
  RET Z                   ; (modes 2/3)
  CP $03
  RET Z
  LD HL,$5076             ; draw at screen row 19, col 22:
  LD (DrawScreenPtr),HL
  LD DE,OxygenText        ; "TOOH: nnnn" — unless the
  LD HL,ShipSystemFlags   ; self-destruct is armed:
  BIT 0,(HL)
  JR Z,ClockDraw10
  LD DE,DestructTimeText  ; then "TIME: m:ss"
ClockDraw10:
  LD B,$0A                ; blit the 10 characters
ClockDrawLoop:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  POP BC
  INC DE
  LD HL,DrawScreenPtr     ; one column right each time
  INC (HL)
  DJNZ ClockDrawLoop
  RET
  RET

; InitJonesAnim
;
; Resets Jones animation and blits the first frame. Sets Y position to 8,
; clears the Y phase freeze flag, then calls BlitJonesFrame with B=1 to draw
; frame 0. This entry point is used by the routines at DrawRoomSpecials and
; UpdateJones.
InitJonesAnim:
  XOR A                   ; A = 0: clear freeze flag
  LD (JonesPhase),A       ; JonesPhase = 0
  LD A,$08
  LD (JonesPos),A         ; JonesPos = 8 (initial Y pixel offset)
  LD B,$01                ; B = 1 iteration
  CALL BlitJonesFrame     ; draw first tail frame
  RET

; TriggerJonesAnim
;
; Advances the Jones animation by one frame if not frozen. Sets the freeze flag
; (bit 7 of JonesPhase) after blitting so the animation runs exactly once. This
; entry point is used by the routines at SetActionTimer and UpdateJones.
TriggerJonesAnim:
  LD B,$01                ; B = 1 iteration
  LD A,(JonesPhase)       ; A = JonesPhase
  AND $80                 ; test freeze flag (bit 7)
  RET NZ                  ; already frozen: do nothing
  CALL BlitJonesFrame     ; blit one tail frame
  LD A,$80
  LD (JonesPhase),A       ; freeze: JonesPhase |= 128
  RET

; ResetAlienXAnim
;
; Clears the alien body animation area from the screen, then resets the X phase
; counter so the next trigger starts from frame 0. This entry point is used by
; the routines at RedrawRoomView and AlienInfoCheck.
ResetAlienXAnim:
  CALL ClearAlienArea     ; clear 19x20-cell screen area (ClearAlienArea)
  XOR A                   ; A = 0
  LD (AlienXPhase),A      ; AlienXPhase = 0 (unfreeze, frame 0)
  RET

; TriggerAlienXAnim
;
; Arms the alien body animation (sets freeze flag so a single blit occurs next
; frame), or ends the encounter sequence. If (RoomModeByte)=1 the crew member
; was killed: jump to RedrawRoomScene to handle that. Otherwise clears the
; screen area and returns. This entry point is used by the routines at
; RefreshRoomInfo and AlienScareCheck.
TriggerAlienXAnim:
  LD A,(AlienXPhase)      ; A = AlienXPhase
  AND $80                 ; test freeze flag
  RET NZ                  ; already frozen: do nothing
  LD A,$80
  LD (AlienXPhase),A      ; freeze: AlienXPhase |= 128
  LD A,(RoomModeByte)     ; A = encounter outcome byte
  CP $01                  ; crew member killed?
  JP Z,RedrawRoomScene    ; yes: handle death sequence
  CALL ClearAlienArea     ; no: clear alien screen area
  RET

; UpdateAlienAnim
;
; Per-frame alien encounter update: refreshes the map view, then advances both
; animation layers. Y layer (tail sweep) if not frozen, X layer (body) if not
; frozen.
UpdateAlienAnim:
  CALL AnimateCrewA       ; redraw room background
  CALL AnimateCrewB       ; redraw room actors
  LD A,(JonesPhase)       ; A = JonesPhase
  AND $80                 ; test freeze flag
  CALL Z,AnimJones        ; not frozen: advance Y animation
  LD A,(AlienXPhase)      ; A = AlienXPhase
  AND $80                 ; test freeze flag
  CALL Z,BlitAlienFrame   ; not frozen: blit next alien body frame
  RET

; AlienAnimVars
;
; Inline state variables for the alien encounter animation. Addresses are
; self-modified at runtime; pointer table entries are fixed in ROM.
AlienXPhase:
  DEFB $00                ; body frame index 0-3 (bit 7 = freeze flag)
  DEFW AlienFrame0        ; AlienFrame0 (AlienFrame0 sprite ptr)
  DEFW AlienFrame1        ; AlienFrame1 (AlienFrame1 sprite ptr)
  DEFW AlienFrame2        ; AlienFrame2 (AlienFrame2 sprite ptr)
  DEFW AlienFrame3        ; AlienFrame3 (AlienFrame3 sprite ptr)
JonesPos:
  DEFB $00                ; pixel Y offset 0-135
AlienScreenPtr:
  DEFW $4800              ; current screen draw ptr (runtime, initially $4800)
JonesPhase:
  DEFB $00                ; tail phase 0-4 (bit 7 = freeze flag)
  DEFW JonesFrame0        ; JonesFrame0 (JonesFrame0 sprite ptr)
  DEFW JonesFrame1        ; JonesFrame1 (JonesFrame1 sprite ptr)
  DEFW JonesFrame2        ; JonesFrame2 (JonesFrame2 sprite ptr)
  DEFW JonesFrame3        ; JonesFrame3 (JonesFrame3 sprite ptr)
  DEFW JonesFrame4        ; JonesFrame4 (JonesFrame4 sprite ptr)

; BlitAlienFrame
;
; Advances AlienXPhase (0->1->2->3->0), loads the corresponding sprite frame
; pointer from the table at AlienXPhase+2, then LDIRs 6 bytes x 63 rows of
; sprite data to the display file at col 7, char-row 1, pixel-row 0, handling
; ZX Spectrum interleaved scan-line addressing at each row boundary.
BlitAlienFrame:
  LD HL,AlienXPhase       ; HL -> AlienXPhase
  LD A,(HL)
  INC A
  AND $03                 ; cycle frame index 0->1->2->3->0
  LD (HL),A               ; store new frame index
  LD E,A
  LD D,$00
  INC HL                  ; HL -> frame pointer table
  ADD HL,DE
  ADD HL,DE               ; HL -> table[frame] (2 bytes/entry)
  LD E,(HL)
  INC HL
  LD D,(HL)               ; DE = sprite frame data ptr
  LD HL,$4807             ; HL = display file col 7, char-row 1, pixel-row 0
  EX DE,HL                ; DE = screen dst, HL = sprite src
  LD B,$3F                ; 63 pixel rows per frame
  LD C,$00
BlitAlienFrameRow:
  PUSH BC
  LD B,C
  LD C,$06
  LDIR
  EX DE,HL
  LD A,H
  AND $07
  CP $07
  JR Z,BlitAlienFrameBankCheck
  INC H
  JR BlitAlienFrameNextRow
; ZX Spectrum display-file scan-line bank wrap: H&7 = 7 means we finished a
; character row. If L < $E0 (224) add -1760 to jump to the next screen third;
; otherwise add 32 to advance to the next character row within the same third.
; Then subtract 6 from HL to step back past the LDIR'd bytes.
BlitAlienFrameBankCheck:
  LD A,L
  CP $E0                  ; L >= $E0: last char-row of this screen third?
  JR NC,BlitAlienFrameNextThird ; yes: next third
  LD BC,$F920             ; BC = -1760: wrap to next column in next third
  ADD HL,BC
  JR BlitAlienFrameNextRow
BlitAlienFrameNextThird:
  LD BC,$0020             ; BC = 32: advance to next character row
  ADD HL,BC
BlitAlienFrameNextRow:
  LD BC,$FFFA             ; BC = -6: rewind HL past the 6 LDIR'd bytes
  ADD HL,BC
  EX DE,HL                ; DE = screen dst, HL = sprite src
  POP BC
  DJNZ BlitAlienFrameRow  ; next row
  RET

; AnimJones
;
; Entry point called from UpdateAlienAnim with B=2 to advance the Y position
; counter before blitting. Calls BlitJonesFrame with B=2, which will call
; AdvanceJonesPos.
AnimJones:
  LD B,$02                ; B=2: advance position then blit

; BlitJonesFrame
;
; Blits the alien tail (Y channel) XOR sprite for the current phase and
; position. First XORs ink colour bits of a 4x2 attribute block to produce a
; flash effect. Then loads the current Y-phase sprite pointer (3 bytes/row, 21
; rows) and chooses between an unshifted XOR blit (JonesPos&7 < 4) or a shifted
; one (>= 4). The shift amount is encoded directly into the self-modifying JR
; at BlitJonesShiftCode (the JR offset byte is patched to select how many
; SRL/RR passes execute). DJNZ at the end re-uses B to call AdvanceJonesPos
; when B=2.
BlitJonesFrame:
  PUSH BC                 ; save B (iteration count)
  LD A,(AttrFileOrigin)   ; A = attribute at top-left of screen
                          ; (AttrFileOrigin)
  CPL
  AND $07                 ; invert and mask ink colour bits
  LD ($9BC1),A            ; patch XOR 7 opcode operand for attribute flash
  LD A,(JonesPos)         ; A = JonesPos (pixel Y offset 0-135)
  RRA
  RRA
  RRA                     ; A = JonesPos >> 3 (character row 0-16)
  AND $1F
  LD E,A
  LD D,$00                ; DE = char row index
  LD HL,Screen_R9C0L4     ; HL = $4C00+1 (display col 1, char-row 0)
  ADD HL,DE               ; HL = screen row address for JonesPos
  LD (AlienScreenPtr),HL  ; AlienScreenPtr = screen row ptr
  LD HL,AttrRow10         ; HL = AttrRow10 (attribute row 10, col 0)
  ADD HL,DE               ; HL = attr address for JonesPos char row
  LD DE,$001D             ; DE = attr row stride - 4 (skip to next pair)
  LD C,$02                ; 2 attribute rows
BlitJonesAttrRow:
  LD B,$04                ; 4 attribute columns per row
BlitJonesAttrInner:
  LD A,(HL)
  XOR $07                 ; toggle ink colour (self-modifying: operand byte
                          ; patched)
  LD (HL),A               ; write flashed attribute
  INC HL
  DJNZ BlitJonesAttrInner ; next column
  ADD HL,DE               ; skip to next attribute row
  DEC C
  JR NZ,BlitJonesAttrRow  ; next row
  LD HL,JonesPhase        ; HL -> JonesPhase
  LD E,(HL)               ; E = current Y phase (0-4)
  LD D,$00
  INC HL                  ; HL -> Y frame pointer table
  ADD HL,DE
  ADD HL,DE               ; HL -> table[phase] (2 bytes/entry)
  LD E,(HL)
  INC HL
  LD D,(HL)               ; DE = tail sprite data ptr for this phase
  LD A,(JonesPos)         ; A = JonesPos
  AND $07                 ; A = sub-character Y pixel offset (0-7)
  CP $04
  JP NC,BlitJonesShifted  ; >= 4: shifted blit (BlitJonesShifted)
  CPL
  AND $03                 ; shift count = 3-(JonesPos&7) in [0,3]
  LD L,A
  RLCA
  RLCA
  RLCA                    ; A = shift_count * 8
  SUB L                   ; A = shift_count * 7 (bytes to skip in shift chain)
  LD ($9BFC),A            ; patch JR offset: BlitJonesShiftCode entry skips N
                          ; SRL/RR pairs
  LD B,$15                ; 21 pixel rows per tail frame
BlitJonesFrameRow:
  PUSH BC
  EX DE,HL
  LD B,(HL)
  INC HL
  LD C,(HL)
  INC HL
  LD E,(HL)
  INC HL
  PUSH HL
  LD HL,(AlienScreenPtr)
  PUSH HL
  XOR A
; Self-modifying right-shift chain. The JR operand byte is patched before entry
; to skip 0-3 SRL/RR groups, giving a right-shift of 0-3 bits across the 3+1
; sprite bytes in {B,C,E,A}. After shifting, the result is XOR-blitted to
; screen.
BlitJonesShiftCode:
  JR BlitJonesShiftCode   ; self-modifying: JR offset patched to skip N SRL/RR
                          ; groups
  SRL B                   ; shift group 3 (shift 3 bits right)
  RR C
  RR E
  RRA
  SRL B                   ; shift group 2
  RR C
  RR E
  RRA
  SRL B                   ; shift group 1 (shift 1 bit right)
  RR C
  RR E
  RRA
  EX AF,AF'               ; swap: A' = carry spill byte
  LD A,B
  XOR (HL)
  LD (HL),A               ; XOR-blit byte 0 to screen
  INC HL
  LD A,C
  XOR (HL)
  LD (HL),A               ; XOR-blit byte 1
  INC HL
  LD A,E
  XOR (HL)
  LD (HL),A               ; XOR-blit byte 2
  INC HL
  EX AF,AF'
  XOR (HL)
  LD (HL),A               ; XOR-blit carry spill byte
  POP HL
  LD A,H
  AND $07
  CP $07
  JR Z,BlitJonesFrameBankCheck
  INC H
  JR BlitJonesFrameNextRow
BlitJonesFrameBankCheck:
  LD A,L
  CP $E0
  JR NC,BlitJonesFrameNextThird
  LD BC,$F920
  ADD HL,BC
  JR BlitJonesFrameNextRow
BlitJonesFrameNextThird:
  LD BC,$0020
  ADD HL,BC
BlitJonesFrameNextRow:
  LD (AlienScreenPtr),HL
  POP DE
  POP BC
  DJNZ BlitJonesFrameRow
  POP BC
  DJNZ AdvanceJonesPos
  RET

; AdvanceJonesPos
;
; Increments JonesPos by 4. If the new value >= 136 the tail sweep is complete:
; freeze the Y animation (JonesPhase |= 128) and return. Otherwise store the
; new position, cycle the phase counter (0->1->2->3->4->0) and jump back to
; BlitJonesFrame.
AdvanceJonesPos:
  LD A,(JonesPos)         ; A = JonesPos
  ADD A,$04               ; advance by 4 pixels
  CP $88                  ; reached bottom of sweep area?
  JR C,AdvanceJonesPosUpdate ; no: update position
  LD A,$80                ; yes: A = freeze flag
  LD (JonesPhase),A       ; JonesPhase = 128 (frozen)
  RET                     ; done
AdvanceJonesPosUpdate:
  LD (JonesPos),A         ; JonesPos = new value
  LD A,(JonesPhase)       ; A = current phase (0-4)
  INC A
  CP $05                  ; wrapped past phase 4?
  JR C,AdvanceJonesPosSetPhase ; no: keep incremented value
  XOR A                   ; yes: wrap to 0
AdvanceJonesPosSetPhase:
  LD (JonesPhase),A       ; JonesPhase = new phase
  JP BlitJonesFrame       ; blit next tail frame

; BlitJonesShifted
;
; Handles the alien tail XOR blit when JonesPos&7 >= 4 (sprite straddles the
; next character row). Uses SLA/RL chains for a left-shift instead of SRL/RR,
; mirroring the logic of BlitJonesFrame's right-shift section. The shift amount
; (pixels left past the half-character boundary) is patched into the
; self-modifying JR at the JR at BlitJonesShiftCode2, then 21 rows of 4 bytes
; are XOR-blitted.
BlitJonesShifted:
  SUB $04                 ; A = (JonesPos&7) - 4, left-shift amount in [0,3]
  LD L,A
  RLCA
  RLCA
  RLCA                    ; A = shift_count * 8
  SUB L                   ; A = shift_count * 7 (JR offset into SLA/RL chain)
  LD ($9C83),A            ; patch BlitJonesShiftCode2 JR offset
  LD B,$15                ; 21 pixel rows
BlitJonesShiftedRow:
  PUSH BC
  EX DE,HL
  LD B,(HL)
  INC HL
  LD C,(HL)
  INC HL
  LD E,(HL)
  INC HL
  PUSH HL
  LD HL,(AlienScreenPtr)
  PUSH HL
  XOR A
BlitJonesShiftCode2:
  JR BlitJonesShiftCode2
  SLA E
  RL C
  RL B
  RLA
  SLA E
  RL C
  RL B
  RLA
  SLA E
  RL C
  RL B
  RLA
  SLA E
  RL C
  RL B
  RLA
  XOR (HL)
  LD (HL),A
  INC HL
  LD A,B
  XOR (HL)
  LD (HL),A
  INC HL
  LD A,C
  XOR (HL)
  LD (HL),A
  INC HL
  LD A,E
  XOR (HL)
  LD (HL),A
  POP HL
  LD A,H
  AND $07
  CP $07
  JR Z,BlitJonesShiftedBankCheck
  INC H
  JR BlitJonesShiftedNextRow
BlitJonesShiftedBankCheck:
  LD A,L
  CP $E0
  JR NC,BlitJonesShiftedNextThird
  LD BC,$F920
  ADD HL,BC
  JR BlitJonesShiftedNextRow
BlitJonesShiftedNextThird:
  LD BC,$0020
  ADD HL,BC
BlitJonesShiftedNextRow:
  LD (AlienScreenPtr),HL
  POP DE
  POP BC
  DJNZ BlitJonesShiftedRow
  POP BC
  DEC B
  JP NZ,AdvanceJonesPos
  RET

; ClearAlienArea
;
; Clears the 19x20-character area of the ZX Spectrum display file and attribute
; RAM used by the alien body animation. Iterates 19 rows of 20 columns from
; DisplayFile, calling DrawTileA to zero each cell, then repeats for the
; attribute area at AttrFileOrigin.
ClearAlienArea:
  LD HL,DisplayFile       ; HL = DisplayFile: start of display file
  LD (DrawScreenPtr),HL   ; store current screen cell address
  LD C,$13                ; 19 character rows
ClearAlienAreaCol:
  LD B,$14
ClearAlienAreaRow:
  PUSH BC
  XOR A
  CALL DrawTileA
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ ClearAlienAreaRow
  LD DE,$000C
  LD HL,(DrawScreenPtr)
  LD A,L
  CP $E0
  JR C,ClearAlienAreaNextRow
  LD DE,$070C
ClearAlienAreaNextRow:
  ADD HL,DE
  LD (DrawScreenPtr),HL
  DEC C
  JR NZ,ClearAlienAreaCol
  LD DE,$000C
  LD HL,AttrFileOrigin
  LD C,$13
ClearAlien_RowLoop:
  LD B,$14
ClearAlien_CellLoop:
  LD (HL),$04
  INC HL
  DJNZ ClearAlien_CellLoop
  ADD HL,DE
  DEC C
  JR NZ,ClearAlien_RowLoop
  RET

; LaunchGate
;
; Fixture 78 (Narcissus launch console). Intended rule: the launch is
; COUNTERMANDED (msg #28) unless every living crew member's room is 34
; (Narcissus); then Jones must be aboard too (JonesRoom = 34, or the cat caught
; and the Cat in Net/Box item aboard or held) or msg #27 "BioUnit Jones not
; present" fires first.
;
; BUG (simulator-verified): the crew walk drifts off the +7 status column after
; the first LIVE crew member — the "back to the next record" step adds 8 from
; the +1 room byte instead of returning to +7, so every later slot has its ROOM
; byte read as a status byte. Rooms are almost never 0, so later crew are
; simply skipped: the launch really only requires the first-listed living crew
; member (plus Jones) to be aboard, and the rest are left behind. The one side
; effect: a later crew member standing in Airlock #1 (room 0) bogusly
; countermands the launch.
LaunchGate:
  LD HL,CrewStatusColumn  ; walk the +7 status column of all 7 crew
  LD DE,$0008
  LD B,$07
Launch_CrewLoop:
  XOR A                   ; is this crew member alive?
  CP (HL)
  JR NZ,Launch_NextCrew   ; no: he needn't board
  PUSH DE                 ; yes: HL -> his room byte (+1)
  LD DE,$FFFA
  ADD HL,DE
  POP DE
  LD A,(HL)               ; aboard the Narcissus (34)?
  CP $22
  JR NZ,Launch_Countermand ; no: LAUNCH COUNTERMANDED
Launch_NextCrew:
  ADD HL,DE               ; +8 — but from +1 after a live crew
  DJNZ Launch_CrewLoop    ; member: the walk drifts (see BUG)
  LD A,(JonesRoom)        ; Jones aboard on his own paws?
  CP $22
  JR Z,LaunchSequence     ; yes: LAUNCH
  CP $FF                  ; or caught (carried, room 255)?
  JR NZ,Launch_NoJones    ; neither: he is missing
  LD HL,CatItemLocations  ; caught: where is the Cat in
  LD B,$02                ; Net (20) / Cat in Box (21)?
Launch_CatItemLoop:
  LD A,(HL)
  CP $FF                  ; (nonexistent: try the other)
  JR Z,Launch_CatItemNext
  CP $22                  ; lying in the Narcissus,
  JR Z,LaunchSequence
  CP $80                  ; or in somebody's hand?
  JR NC,LaunchSequence    ; either way: LAUNCH
Launch_CatItemNext:
  INC HL
  DJNZ Launch_CatItemLoop
Launch_NoJones:
  LD A,$1B                ; msg #27 "BioUnit Jones not present"
  CALL EnqueueMessage
Launch_Countermand:
  LD A,$1C                ; msg #28 "LAUNCH COUNTERMANDED"
  CALL EnqueueMessage
  RET

; LaunchSequence — the Narcissus lifts off
;
; 75 frames of engine noise with the whole room-view area flashing (BRIGHT bit
; XOR), then the escape ending (Endgame_Escape).
;
; Used by the routine at LaunchGate.
LaunchSequence:
  LD B,$4B                ; 75 flash frames:
Launch_FlashLoop:
  PUSH BC
  LD A,$01                ; queue the engine-noise effect
  LD (SoundRequest),A
  CALL FrameTiming        ; play it (FrameTiming)
  LD DE,$000C             ; toggle the BRIGHT bit across the
  LD HL,AttrFileOrigin    ; 20x19 room-view attribute window
  LD C,$13
Launch_AttrRows:
  LD B,$14
Launch_AttrLoop:
  LD A,(HL)
  XOR $40
  LD (HL),A
  INC HL
  DJNZ Launch_AttrLoop
  ADD HL,DE
  DEC C
  JR NZ,Launch_AttrRows
  POP BC
  DJNZ Launch_FlashLoop
  JP Endgame_Escape       ; you got away: Endgame_Escape

; FrameTiming
;
; Controls frame timing and plays a queued sound effect. Reads the sound-
; request flag at SoundReqFlag. If zero, calls the standard one-frame wait at
; BusyWait (approx 1/50s on a 48K Spectrum) and returns. If non-zero, plays a
; beeper tone by reading 1-KB ROM bytes starting at $0200 and toggling bit 4 of
; port $FE for each byte; the outer loop runs 255 × 4 iterations with a short
; delay (D=64 counts) between each toggle. After playback, clears the
; sound-request flag.
;
; Used by the routines at GameEntry, LaunchSequence and IntroductionMode.
FrameTiming:
  LD A,(SoundRequest)     ; A = sound-request flag (SoundReqFlag)
  AND A                   ; any sound queued?
  JR NZ,Frame_PlayEffect  ; yes: play beeper effect
  CALL BusyWait           ; no: standard one-frame timing wait (~1/50s)
  RET
Frame_PlayEffect:
  LD HL,$0200             ; HL = $0200 (ROM tone data source)
  LD D,$40                ; D = inter-toggle delay count
  LD B,$FF                ; outer loop: 255 iterations
Frame_EffectOuter:
  PUSH BC
  LD B,$04                ; 4 sub-iterations per outer step
Frame_EffectStep:
  PUSH BC
  PUSH DE
  LD A,(HL)               ; read ROM byte
  INC HL                  ; advance data pointer
  AND $10                 ; isolate bit 4 (EAR/MIC output)
  OUT ($FE),A             ; toggle beeper
Frame_EffectDelay:
  DEC D                   ; delay loop
  JR NZ,Frame_EffectDelay
  POP DE
  POP BC
  DJNZ Frame_EffectStep   ; repeat 4 sub-iterations
  POP BC
  DJNZ Frame_EffectOuter  ; repeat 255 outer iterations
  XOR A
  LD (SoundRequest),A     ; clear sound-request flag
  RET

; NoteTable
;
; 9 × 4-byte entries used by PlayNoteByIndex. Each entry feeds the ROM BEEP
; routine at $03B5: DE = note duration (number of cycles), HL = T-state
; half-period (frequency).
NoteTable:
  DEFW $000D,$06AA        ; note 0: dur=13, period=1706 T-states
  DEFW $0011,$04F4        ; note 1: dur=17, period=1268 T-states
  DEFW $000D,$0642        ; note 2: dur=13, period=1602 T-states
  DEFW $000A,$086D        ; note 3: dur=10, period=2157 T-states
  DEFW $000B,$077A        ; note 4: dur=11, period=1914 T-states
  DEFW $0013,$0464        ; note 5: dur=19, period=1124 T-states
  DEFW $0019,$0342        ; note 6: dur=25, period=834 T-states
  DEFW $0014,$0425        ; note 7: dur=20, period=1061 T-states
  DEFW $000E,$058F        ; note 8: dur=14, period=1423 T-states

; PhrasePairs
;
; 8 × 2-byte phrase pairs consumed by PlayMusicPhrase. Each pair is
; (note_A_index, note_B_index) into NoteTable; the two notes are played
; alternately 8 times to form a trill, then the next phrase starts.
PhrasePairs:
  DEFB $00,$05            ; phrase 0: notes 0,5
  DEFB $00,$05            ; phrase 1: notes 0,5
  DEFB $01,$06            ; phrase 2: notes 1,6
  DEFB $01,$06            ; phrase 3: notes 1,6
  DEFB $02,$07            ; phrase 4: notes 2,7
  DEFB $02,$07            ; phrase 5: notes 2,7
  DEFB $03,$08            ; phrase 6: notes 3,8
  DEFB $04,$01            ; phrase 7: notes 4,1

; PhraseStateScratch — 2 bytes of mutable state used by PlayMusicPhrase: +0..+1
; = current pointer into PhrasePairs
PhraseStateScratch:
  DEFB $00,$00

; PlayNoteByIndex
;
; Plays one beeper note via the ZX Spectrum ROM BEEP routine at $03B5. Input: A
; = note index (0..8) into NoteTable at NoteTable. Each table entry is 4 bytes:
; 2-byte duration (passed in DE) and 2-byte period in T-states (passed in HL).
; Used twice per inner iteration of PlayMusicPhrase.
PlayNoteByIndex:
  ADD A,A                 ; A *= 2
  ADD A,A                 ; A *= 2 (now A = index × 4 = byte offset)
  LD E,A
  LD D,$00                ; DE = byte offset
  LD HL,NoteTable         ; HL → NoteTable base
  ADD HL,DE               ; HL → 4-byte entry for note A
  LD E,(HL)
  INC HL
  LD D,(HL)               ; DE = duration (cycles)
  INC HL
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A                  ; HL = T-state period
  CALL $03B5              ; ROM BEEP at $03B5 — play tone
  RET

; PlayMusicPhrase
;
; Plays the title-screen jingle: 8 successive 2-note phrases drawn from the
; phrase table at PhrasePairs (PhrasePairs). Each phrase byte is a (loA, hiB)
; pair selecting two notes from NoteTable at NoteTable (PlayNoteByIndex). The
; inner loop repeats each pair 8 times (so the phrase audibly trills on its
; 2-note interval), and the keyboard rows are polled between notes — any key
; press aborts playback via Music_Abort.
;
; Used by the routine at DrawIntroScreen (DrawIntroScreen).
PlayMusicPhrase:
  LD HL,PhrasePairs       ; HL = PhrasePairs base
  LD (PhraseStateScratch),HL
  LD B,$08
Music_NextNotePair:
  PUSH BC
  LD HL,(PhraseStateScratch)
  LD E,(HL)
  INC HL
  LD D,(HL)
  INC HL
  LD (PhraseStateScratch),HL
  LD ($9DDE),DE
  LD B,$08
Music_PlayPair:
  PUSH BC
  LD A,($9DDE)
  CALL PlayNoteByIndex
  LD A,($9DDF)
  CALL PlayNoteByIndex
  LD BC,$FEFE
Music_KeyPoll:
  IN A,(C)
  AND $1F
  XOR $1F
  JR NZ,Music_Abort
  RLC B
  JR C,Music_KeyPoll
  POP BC
  DJNZ Music_PlayPair
  POP BC
  DJNZ Music_NextNotePair
  RET
Music_Abort:
  POP BC
  POP BC
  RET
KeyboardModeFlag:
  NOP                     ; variable: 255 = keyboard input chosen
RatingScore:
  NOP                     ; variable: the endgame Competence Rating accumulator
                          ; (see ComputeRating)

; Intro-screen tile map and attribute table
;
; Two screen tables consumed by DrawIntroScreen (DrawIntroScreen): IntroTileMap
; at IntroTileMap (165 bytes) — a 15 × 11 grid of tile indices. The outer loop
; drives 15 vertical column-strips and the inner loop draws 11 tiles down each
; strip via DrawSpriteRow. A tile index N is converted into a bitmap address by
; multiplying by 8 and adding TileBitmaps (TileBitmaps). IntroAttributes at
; IntroAttributes (88 bytes) — an 8 × 11 grid of attribute bytes painted into
; the bottom rows of the attribute file. Mostly bright-white-on-black (7) with
; magenta (4) accents and a couple of brighter cells (56, 60).
IntroTileMap:
  DEFB $00,$00,$00,$01,$02,$03,$04,$00,$00,$00,$00 ; col  0
  DEFB $00,$00,$05,$06,$07,$00,$08,$09,$00,$00,$00 ; col  1
  DEFB $00,$00,$0A,$0B,$0C,$00,$00,$0D,$0E,$00,$00 ; col  2
  DEFB $00,$0F,$10,$11,$12,$00,$00,$00,$13,$14,$00 ; col  3
  DEFB $00,$15,$16,$17,$00,$00,$00,$00,$18,$19,$00 ; col  4
  DEFB $1A,$1B,$1C,$1D,$00,$00,$00,$00,$00,$1E,$00 ; col  5
  DEFB $1F,$20,$21,$22,$00,$00,$00,$00,$00,$23,$24 ; col  6
  DEFB $25,$26,$27,$28,$29,$00,$00,$00,$2A,$2B,$2C ; col  7
  DEFB $2D,$2E,$2F,$30,$31,$32,$33,$34,$35,$36,$37 ; col  8
  DEFB $38,$39,$3A,$3B,$3C,$3D,$3E,$3F,$40,$41,$42 ; col  9
  DEFB $43,$44,$45,$46,$47,$48,$49,$4A,$4B,$4C,$00 ; col 10
  DEFB $00,$4D,$4E,$4F,$50,$51,$52,$53,$54,$55,$00 ; col 11
  DEFB $00,$56,$57,$58,$59,$5A,$5B,$5C,$5D,$00,$00 ; col 12
  DEFB $00,$5E,$5F,$60,$61,$62,$63,$64,$00,$00,$00 ; col 13
  DEFB $00,$00,$00,$65,$66,$67,$68,$00,$00,$00,$00 ; col 14
IntroAttributes:
  DEFB $07,$07,$07,$07,$04,$07,$07,$07,$04,$04,$07 ; row  0
  DEFB $07,$07,$38,$04,$07,$04,$07,$04,$04,$07,$07 ; row  1
  DEFB $07,$07,$38,$04,$04,$07,$04,$07,$07,$07,$07 ; row  2
  DEFB $07,$07,$07,$07,$04,$07,$07,$3C,$07,$07,$07 ; row  3
  DEFB $07,$07,$07,$07,$04,$07,$07,$04,$07,$07,$07 ; row  4
  DEFB $07,$07,$07,$07,$04,$07,$04,$07,$07,$07,$07 ; row  5
  DEFB $07,$07,$04,$07,$04,$04,$07,$07,$07,$07,$07 ; row  6
  DEFB $07,$07,$07,$07,$04,$07,$07,$07,$07,$07,$07 ; row  7

; CrewAction7_Handler — the Android's attack tick (action state 7)
;
; Dispatched via CrewActionDispatch entry 7 for the activated Android (see
; UpdateAlien UpdateAlien). Gathers the crew sharing the Android's room
; (GatherSameRoomSlots) and wounds the first live one found (NearestCrewA, then
; NearestCrewB): strength (+4) -1. Below 2 the victim collapses (VictimItemDrop
; VictimItemDrop, msg #21); at 2-3, msg #29 announces the assault; a still-fit
; victim (4+) suffers silently. Returns without action when no live room-mate
; remains.
CrewAction7_Handler:
  CALL GatherSameRoomSlots ; gather the crew in the Android's room
  LD A,(NearestCrewA)     ; A = first room-mate (255 = none)
  CP $FF
  JR Z,Android_SecondTarget
  LD C,A                  ; C = victim slot
  ADD A,A                 ; HL -> his status byte (+7)
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,ActorStatusColumn
  ADD HL,DE
  XOR A                   ; alive?
  CP (HL)
  JR Z,Android_WoundVictim ; yes: wound him
Android_SecondTarget:
  LD A,(NearestCrewB)     ; try the second room-mate
  CP $FF
  RET Z                   ; nobody to attack
  LD C,A                  ; C = victim slot
  ADD A,A                 ; HL -> his status byte (+7)
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,ActorStatusColumn
  ADD HL,DE
  XOR A                   ; alive?
  CP (HL)
  RET NZ                  ; no: give up this tick
Android_WoundVictim:
  DEC HL                  ; HL -> the victim's strength (+4)
  DEC HL
  DEC HL
  DEC (HL)                ; the Android saps a strength point
  LD A,(HL)
  CP $02                  ; below 2: the victim collapses
  JP C,VictimItemDrop
  CP $04                  ; 4+: shrugs it off silently
  RET NC
  LD L,C                  ; wounded (2-3): L = victim,
  LD A,(AlienTargetID)    ; H = the Android,
  LD H,A
  LD A,$1D                ; msg #29 "{target} is attacking {actor}"
  CALL EnqueueMessage     ; — "<Android> is attacking <victim>"
  RET

; UpdateAlien
;
; Per-frame Android activation check. Once TriggerAlienEvent
; (TriggerAlienEvent) has set AlienActiveFlag — the crew has wounded the alien
; 6+ times — this routine pulls the alien_target slot into action state 7
; (attack), turning the Android from a normal-looking crew member into a
; hostile actor that wounds the crew in its room.
;
; Returns immediately if any of: - AlienActiveFlag (AlienActiveFlag) == 0 (the
; trigger has not fired) - alien_target's byte +0 != 0 (Android already
; activated; the activation stamp at Android_Activate sets +0 = 80, so on every
; subsequent frame this guard short-circuits the routine) - alien_target's byte
; +7 != 0 (lockout sentinel — CrewHitsAndroid's collapse path at
; Android_Collapsed writes +7 = 2, and that prevents re-activation if the
; Android has been defeated)
;
; In other words: the routine fires exactly ONCE per game for the Android, the
; first frame the trigger is set and the slot is still in its as-issued
; template state (LongGameCrewInit, LongGameCrewInit, gives +0 = +7 = 0 for
; every slot).
;
; When all guards pass: 1. IX = alien_target record; stamp (IX+0) = 80
; (activation marker and timer that also blocks future re-entry into this
; branch). 2. Cache AlienTargetID into CrewIndex (CrewIndex) for downstream
; message enqueues. 3. CALL GatherSameRoomSlots — gather up to two other crew
; standing in the Android's room into NearestCrewA/B (its victim pool). 4. If
; at least one of them is alive: enqueue message #30 "{actor} is attacking
; {target}" with actor = AlienTargetID (the Android's name, e.g. "Ash is
; attacking Lambert"), put the alien_target into action state 7 with timer 40,
; then return. 5. If nobody is there to attack, fall through to PickNextRoom
; (wander) and reset the Android to action state 1.
;
; Once the Android is in state 7, subsequent ResetCrewTimers frames dispatch it
; through CrewAction7_Handler (CrewAction7_Handler) which wounds a live crew
; member in its room and can collapse them via VictimItemDrop (msg #21 "{actor}
; has collapsed").
;
; This routine is NOT the alien's own attack: the alien kills crew that end up
; in its room (slot 0 record byte +1) — see the attack tick at
; AlienAtk_SlotAddr.
;
; Used by the routine at GameEntry.
UpdateAlien:
  LD A,(AlienActiveFlag)  ; A = AlienActiveFlag
  AND A                   ; alien loose on ship?
  RET Z                   ; no: no encounter yet, nothing to activate
  LD HL,(AlienTargetPtr)  ; HL = alien_target record (AlienTargetPtr)
  LD A,(HL)               ; A = byte +0
  AND A                   ; Android already activated (+0 stamped to 80)?
  RET NZ                  ; yes: this routine has already fired once, skip
  LD DE,$0007             ; offset to byte +7 (defeated-lockout sentinel)
  ADD HL,DE               ; HL → +7
Android_ActivateCheck:
  CP (HL)                 ; A=0 here; Z iff byte +7 == 0 (template, not
                          ; defeated)
  RET NZ                  ; +7 != 0: Android collapsed earlier, stay dormant
  PUSH IX                 ; save caller's IX
  LD IX,(AlienTargetPtr)  ; IX = Android record (= alien_target)
Android_Activate:
  LD (IX+$00),$50         ; stamp +0 = 80 — activation marker / blocks re-entry
  LD A,(AlienTargetID)    ; A = AlienTargetID (= Android slot index)
  LD (CrewIndex),A        ; CrewIndex = AlienTargetID (Android's slot)
  CALL GatherSameRoomSlots ; gather up to two other crew standing in the
                           ; Android's room as its victims
  LD A,(NearestCrewA)     ; A = first room-mate (NearestCrewA)
  CP $FF                  ; $FF = nobody there
  JR Z,Android_Wander     ; no primary target: wander
  LD C,A                  ; C = victim slot
  ADD A,A                 ; A × 8 (crew record size)
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,ActorStatusColumn ; HL -> that slot's status byte
  ADD HL,DE               ; (+7, base $7385)
  XOR A                   ; alive (status 0)?
  CP (HL)
  JR Z,Android_Attack     ; yes: attack him
  LD A,(NearestCrewB)     ; A = second room-mate
  CP $FF
  JR Z,Android_Wander     ; no secondary target either: wander
  LD C,A                  ; C = victim slot
  ADD A,A                 ; HL -> his status byte (+7)
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD HL,ActorStatusColumn
  ADD HL,DE
  XOR A                   ; alive (status 0)?
  CP (HL)
  JR Z,Android_Attack     ; yes: attack him
Android_Wander:
  CALL PickNextRoom       ; nobody to attack: pick a room to
  LD (IX+$03),$01         ; wander to (action state 1)
  LD (IX+$00),$3C         ; with timer 60 frames
  POP IX
  RET
Android_Attack:
  LD H,C                  ; H = the victim slot (message target)
  LD A,(AlienTargetID)    ; A = AlienTargetID
  LD L,A                  ; L = Android slot index (message actor)
  LD A,$1E                ; A = message #30 "{actor} is attacking {target}"
  CALL EnqueueMessage     ; enqueue: "{Android name} is attacking {nearest}"
  LD (IX+$03),$07         ; Android slot → action state 7 (attack)
  LD (IX+$00),$28         ; with timer 40 frames (next dispatch runs the
                          ; attack)
  POP IX
  RET

; CrewHitsAndroid
;
; Android-side combat handler, called from CrewAction4_Handler
; (Combat_HitAlien) when the state-4 actor stands in the Android's room (the
; slot picked by LongGameInit from LongGameTargetTable LongGameTargetTable, id
; in AlienTargetID AlienTargetID, record pointer in AlienTargetPtr
; AlienTargetPtr). The parallel of CrewHitsAlien (CrewHitsAlien): same guards,
; same weapon dispatch on the attacker's front-hand item, but aimed at the
; Android and emitting messages #31 / #32 instead of #3 / #4.
;
; Guards: strength (IX+4) >= 2 and morale (IX+6) >= 2 — a Collapsed or
; Broken/Shaken crew member cannot fight. Then the weapon decides:
;
; 17 (Cat Box) / >= 20 (Cat in Net/Box, bare hands) → silent no-op 16 (Net)
; → stamp 255 into the Android's countdown byte (+0) — netted, out of action —
; destroy the Net, msg #31 "{actor} nets {target}" 0-5, 18-19         → wound:
; Android strength (+4) -1; below 2 it collapses — status (+7) = 2 (the lockout
; UpdateAlien's gate at Android_ActivateCheck respects), msg #21 "{Android} has
; collapsed", and AlienActiveFlag is cleared 6-7 (Tracker)      → tracker
; shatters (destroyed, msg #5) but still wounds it 8-11 (Fire Extng)  → spend a
; charge, no effect (msg #6 if empty) 12 (Harpn Gun)     → the blast kills the
; attacker and every bystander and adds 15 room damage — but unlike the
; alien-side loop at KillPrim_ItemLoop, the final compare in the item wipe at
; AndroidKill_ItemLoop has an INVERTED branch (JR Z where the alien side falls
; through), so it destroys every ItemLocations entry EXCEPT items lying loose
; in this room: all items held by anyone or lying anywhere else on the ship are
; wiped — and the Android itself takes no wound. Verified in the simulator:
; items in other rooms / other hands -> 255, floor items survive, attacker +7 =
; 1. An original-game bug, preserved as-is. 13-15 (Laser Pist) → spend a
; charge, +6 room damage, then the normal wound path (msg #7 if empty)
CrewHitsAndroid:
  LD A,(IX+$04)           ; attacker's strength >= 2?
  CP $02
  RET C                   ; no: too hurt to fight
  LD A,(IX+$06)           ; attacker's morale >= 2?
  CP $02
  RET C                   ; no: too scared to fight
  LD A,(CrewIndex)        ; A = the attacker's slot
  CALL FindHeldItems      ; FindHeldItems: what is he holding?
  LD A,(HeldItemFront)    ; A = the front-hand weapon
  CP $11                  ; the Cat Box?
  JP Z,HitAndroid_Refresh ; useless: silent no-op
  CP $14                  ; Cat in Net/Box, or bare hands (255)?
  JP NC,HitAndroid_Refresh ; also useless
  CP $10                  ; the Net?
  JR NZ,CrewHitsAndroid_HitPath ; no → "hits the Android" branch
  LD HL,(AlienTargetPtr)  ; yes: stamp 255 into the Android's
  LD (HL),$FF             ; countdown (+0) — netted, out of action
  CALL DestroyFrontItem   ; the Net is used up (destroyed)
  LD A,(CrewIndex)        ; L = the attacker's slot,
  LD HL,($83A6)           ; H = the Android's slot (the byte
  LD L,A                  ; after AlienTargetPtr is AlienTargetID)
  LD A,$1F                ; A = msg #31 "{actor} nets {target}"
  CALL EnqueueMessage     ; enqueue
  JP HitAndroid_Refresh
; "{actor} hits the Android" branch (CrewHitsAndroid continued).
CrewHitsAndroid_HitPath:
  PUSH AF                 ; stash the weapon id across the message
  LD A,(CrewIndex)        ; A = the attacker's slot
  LD L,A                  ; L = actor for the message
  LD A,$20                ; A = msg #32 "{actor} hits the Android"
  CALL EnqueueMessage     ; enqueue
  POP AF                  ; A = the weapon again
  CP $06                  ; 0-5: a prod or an incinerator?
  JR C,Android_TakeHit
  CP $12                  ; 18/19: a spanner?
  JR Z,Android_TakeHit
  CP $13
  JR NZ,HitAndroid_Wound  ; anything else: weapon-specific handling
Android_TakeHit:
  LD HL,(AlienTargetPtr)  ; wound the Android:
  LD DE,$0004             ; its strength (+4) -1
  ADD HL,DE
  DEC (HL)
  LD A,(HL)               ; still 2+ ?
  CP $02
  JP NC,HitAndroid_Refresh ; yes: it stays up
  INC HL                  ; below 2: the Android collapses —
  INC HL
  INC HL
Android_Collapsed:
  LD (HL),$02             ; status (+7) = 2 (defeated, locked out
  LD DE,$FFF9             ; of re-activation),
  ADD HL,DE               ; countdown (+0) = 0
  LD (HL),$00
  LD A,(AlienTargetID)    ; L = the Android,
  LD L,A
  LD A,$15                ; msg #21 "{actor} has collapsed"
  CALL EnqueueMessage
  CALL RefreshGrilleRow   ; put the Special row back to the grille
  LD (HL),$00             ; state, and clear AlienActiveFlag
  RET                     ; (RefreshGrilleRow returns HL -> it)
HitAndroid_Wound:
  CP $08                  ; 6/7: a tracker?
  JR NC,HitAndroid_Extinguisher
  CALL DestroyFrontItem   ; yes: it shatters — destroyed,
  LD A,$05                ; msg #5 "{actor}'s Tracker is broken"
  LD HL,(CrewIndex)
  CALL EnqueueMessage
  JR Android_TakeHit      ; and it still wounds the Android
HitAndroid_Extinguisher:
  CP $0C                  ; 8-11: an extinguisher?
  JR NC,HitAndroid_HarpoonGate
  LD HL,ItemCharges       ; yes: HL -> its ItemCharges entry
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)               ; any CO2 left?
  AND A
  JR Z,HitAndroid_ExtSpent
  DEC (HL)                ; yes: spend a charge — no effect
  JP HitAndroid_Refresh   ; on the Android
HitAndroid_ExtSpent:
  LD HL,(CrewIndex)       ; empty: L = actor,
  LD A,$06                ; msg #6 "{actor}'s extinguisher is empty"
  CALL EnqueueMessage
  JP HitAndroid_Refresh
HitAndroid_HarpoonGate:
  JP NZ,HitAndroid_Laser  ; weapon != 12 → 13-15, the laser branch
  CALL GatherSameRoomSlots ; the Harpoon Gun: gather the attacker
  LD DE,NearestCrewC      ; + room-mates as blast victims
  LD B,$03                ; all three gathered slots:
AndroidKill_SlotLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)               ; A = victim slot (255 = none)
  CP $FF
  JR Z,AndroidKill_NextSlot
  PUSH AF
  ADD A,$80               ; E = victim's back-hand marker
  LD E,A
  ADD A,$20               ; D = victim's front-hand marker
  LD D,A
  LD C,(IX+$01)           ; C = the room
  LD HL,ItemLocations     ; item wipe — NB the room test below is
  LD B,$16                ; INVERTED vs the alien side (KillPrim_ItemLoop):
AndroidKill_ItemLoop:
  LD A,(HL)
  CP D                    ; held by the victim: wiped
  JR Z,AndroidKill_WipeItem
  CP E
  JR Z,AndroidKill_WipeItem
  CP C                    ; lying in this room: SURVIVES —
  JR Z,AndroidKill_NextItem ; but everything else on the ship
AndroidKill_WipeItem:
  LD (HL),$FF             ; falls through and is destroyed
AndroidKill_NextItem:
  INC HL                  ; (original-game bug, see the header)
  DJNZ AndroidKill_ItemLoop
  POP AF                  ; HL -> the victim's status byte (+7)
  LD HL,ActorStatusColumn
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  ADD HL,DE
  XOR A                   ; still alive?
  CP (HL)
  JR NZ,AndroidKill_NextSlot
  LD (HL),$01             ; yes: killed by the blast (+7 = 1),
  LD DE,$FFF9             ; countdown (+0) = 0
  ADD HL,DE
  LD (HL),A
AndroidKill_NextSlot:
  POP DE
  INC DE
  POP BC
  DJNZ AndroidKill_SlotLoop
  LD A,$0F                ; the blast wrecks the room:
  LD E,(IX+$01)           ; +15 damage points
  CALL AddRoomDamage
  JP InitGameView         ; rebuild the ship view
HitAndroid_Laser:
  LD E,A                  ; 13-15, a laser pistol:
  LD D,$00
  LD HL,ItemCharges       ; HL -> its ItemCharges entry
  ADD HL,DE
  XOR A                   ; any shots left?
  CP (HL)
  JR Z,HitAndroid_LaserSpent
  DEC (HL)                ; yes: spend one,
  LD A,$06                ; +6 room damage,
  LD E,(IX+$01)
  CALL AddRoomDamage
  JP Android_TakeHit      ; and wound the Android
HitAndroid_LaserSpent:
  LD HL,(CrewIndex)       ; out of shots: L = actor,
  LD A,$07                ; msg #7 "{actor}'s Laser is exhausted"
  CALL EnqueueMessage
  JP HitAndroid_Refresh
HitAndroid_Refresh:
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(DrawSlotIndex)    ; is the attacker the viewed crew member?
  LD HL,CrewIndex
  CP (HL)
  RET NZ                  ; no: done
  LD A,(AlienActiveFlag)  ; Android hostility still on?
  AND A
  LD A,(HL)               ; (leftover load, unused)
  RET Z                   ; no: leave the Special row alone
  LD A,$34                ; yes: keep "  ATTACK  " on the Special
  CALL SpecialRowAttack   ; slot and redraw the special row
  LD HL,$48D6             ; (screen row 14, col 22)
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  RET

; TriggerAlienEvent — arm the Android once the alien is hurt
;
; Per-frame check: once the crew has wounded the alien 6 or more times (slot 0
; byte +4), and a valid Android slot exists (AlienTargetID < 8 — Long Game
; only) whose record is still dormant (+7 = 0), set AlienActiveFlag = 255 via
; AlienEventDispatch (AlienEventDispatch). UpdateAlien (UpdateAlien) sees the
; flag and turns the Android hostile — the traitor reveals himself when the
; alien is in danger.
;
; Used by the routine at GameEntry.
TriggerAlienEvent:
  LD HL,$7382             ; HL -> the alien's wound counter (+4)
  LD A,(HL)
  CP $06                  ; wounded fewer than 6 times?
  RET C                   ; yes: no trigger yet
  LD A,(AlienTargetID)    ; is there a valid Android slot?
TriggerAlien_TargetCheck:
  CP $08                  ; (the Short Game hard-wires 9)
  RET NC                  ; no: nothing to trigger
  LD HL,(AlienTargetPtr)  ; HL -> the Android's record;
  LD DE,$0007             ; its status byte (+7):
  ADD HL,DE
  XOR A                   ; still dormant (0)?
  CP (HL)
  RET NZ                  ; no (collapsed / host): stay quiet
  LD A,$FF                ; yes: AlienActiveFlag = 255 —
  JP AlienEventDispatch   ; the Android turns hostile
  RET                     ; (unreachable)
  LD HL,AlienFlags
  LD (HL),$00
  LD BC,$001F
  IN A,(C)
  BIT 2,A
  JR Z,Kempston_UpBit
  SET 3,(HL)
Kempston_UpBit:
  BIT 3,A
  JR Z,Kempston_Merge
  SET 2,(HL)
Kempston_Merge:
  AND $13
  OR (HL)
  LD (HL),A
  RET
  LD HL,AlienFlags
  LD (HL),$00
  LD BC,$EFFE
  IN A,(C)
  CPL
  RRCA
  RRCA
  JR NC,Sinclair_Bit1
  SET 2,(HL)
Sinclair_Bit1:
  RRCA
  JR NC,Sinclair_Bit2
  SET 3,(HL)
Sinclair_Bit2:
  RRCA
  JR NC,Sinclair_Bit3
  SET 0,(HL)
Sinclair_Bit3:
  RRCA
  JR NC,Sinclair_Fire
  SET 1,(HL)
Sinclair_Fire:
  AND $08
  RET Z
  SET 4,(HL)
  RET

; AlienInitTable
;
; 16-byte lookup table indexed by a ROM-script nibble (0-15) inside
; CommonGameInit (CommonGameInit). The looked-up value is ORed with $40
; (direction flag, bit 6) and written to $737F (the alien slot's
; identity/heading byte), randomising the alien's starting attributes.
AlienInitTable:
  DEFB $03,$04,$05,$09,$0A,$0B,$0D,$0E,$12,$13,$14,$14,$16,$18,$19,$1E

; LongGameHostSlotTable
;
; 16-byte lookup table indexed by a ROM-script nibble (0-15) inside
; LongGameInit (LongGameInit). Selects which 8-byte record slot at ActorRecords
; will be the **chestburster host** for this run — i.e. the crew member whose
; name will be substituted into the opening status-bar message #33 "Alien has
; hatched from {actor}" enqueued at LongGame_HatchMsg. The chosen slot has byte
; +1 and byte +7 overwritten with $FF (the HostMarker pattern); that tag is
; what every "skip this slot during normal corridor processing" check keys off
; (e.g. `CP 255` on byte +1 at SelectSlot_HostCheck inside
; SelectSlotByDrawIdx). Values are restricted to {1, 2, 5}, so the host is
; always Dallas, Kane or Lambert.
LongGameHostSlotTable:
  DEFB $01,$02,$05,$01,$02,$05,$01,$02,$05,$01,$02,$05,$01,$02,$05,$02

; LongGameTargetTable
;
; 16-byte lookup table indexed by a ROM-script nibble (0-15) inside
; LongGameInit (LongGameInit). Used to randomise AlienTargetID (AlienTargetID):
; the selector loops until it picks a value different from the host slot chosen
; via LongGameHostSlotTable (LongGameHostSlotTable), then stores the value in
; AlienTargetID and uses it to compute the targeted crew's record pointer at
; AlienTargetPtr. Values are restricted to {1, 2, 4, 6} = the four crew slots
; {Dallas, Kane, Ash, Parker}, always different from the chestburster host
; slot.
;
; This selection is the long-game **Android assignment**. The chosen crew
; member behaves like a normal crew member at first; UpdateAlien (UpdateAlien)
; activates them as hostile the first time the alien is loose AND their slot is
; still in template state (i.e. they haven't been activated or defeated yet).
; After activation they enter action state 7 and wound their sprite-class kin
; via CrewAction7_Handler (CrewAction7_Handler). Status-bar messages #30 / #32
; name that slot.
;
; The four candidate slots correspond to the canonical hatch-victim (Kane = 2)
; plus three crew the alien can plausibly puppet (Dallas / Ash / Parker). Note
; Ash (slot 4) — the canonical film android — is one of the four but the pick
; is random per run.
LongGameTargetTable:
  DEFB $01,$02,$04,$06,$01,$02,$04,$06,$01,$02,$04,$06,$01,$02,$04,$06

; InitialItemRooms
;
; 22 bytes copied by CommonGameInit (CommonInit_ItemsGrilles) to ItemLocations
; (ItemLocations): the starting room of every portable item (see the
; ItemLocations header for the id -> name map). The final two entries are 255 —
; the "Cat in Net"/"Cat in Box" items don't exist until Jones is caught.
InitialItemRooms:
  DEFB $18,$15,$14,$06,$06,$10,$06,$10,$1B,$11,$12,$13,$20,$02,$02,$02
  DEFB $18,$17,$1F,$14,$FF,$FF

; ItemChargesInit
;
; 16 bytes copied by CommonGameInit (CommonInit_Charges) to ItemCharges
; ($83D2): zeros the ItemCharges area then writes 3,3,3,3,1,8,8,8 into
; ItemChargesHi.
ItemChargesInit:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00,$03,$03,$03,$03,$01,$08,$08,$08

; LongGameCrewInit
;
; 64-byte starting state (eight 8-byte records) copied by LongGameInit
; (LongGameInit) into $737E-$73BD: slot 0 ($737E) is the alien, then one 8-byte
; record per crew slot ($7386 Dallas ... $73B6 Brett — the per-line comments
; below name them).
;
; Per-record field layout: see ActorRecords (ActorRecords). In particular +1 is
; the STARTING ROOM (6 = CommdCentr, 27 = Mess) and +4 is the crew member's
; strength. The Long Game opens with Dallas, Kane and Ripley on the bridge and
; Ash, Lambert and Parker in the Mess — the film's dinner scene, ready for
; message #33 "Alien has hatched from {actor}". LongGameInit stamps $FF into
; +1/+7 of the host slot chosen via LongGameHostSlotTable (the HostMarker).
;
; All seven crew slots start with non-zero +4/+5/+6, so every crew member is
; alive and placed when a Long Game begins. LongGameInit's script-driven step
; at LongGame_PickHost-LongGame_PickTarget then marks one slot as the
; chestburster host — the only crew whose +1 and +7 will hold $FF after init.
LongGameCrewInit:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00 ; slot 0 ALIEN (room written by
                                       ; CommonGameInit)
  DEFB $00,$06,$00,$00,$06,$04,$04,$00 ; slot 1 Dallas  — CommdCentr, strength
                                       ; 6
  DEFB $00,$06,$00,$00,$05,$04,$04,$00 ; slot 2 Kane    — CommdCentr, strength
                                       ; 5
  DEFB $00,$06,$00,$00,$04,$03,$03,$00 ; slot 3 Ripley  — CommdCentr, strength
                                       ; 4
  DEFB $00,$1B,$00,$00,$05,$04,$04,$00 ; slot 4 Ash     — Mess, strength 5
  DEFB $00,$1B,$00,$00,$06,$04,$04,$00 ; slot 5 Lambert — Mess, strength 6
  DEFB $00,$1B,$00,$00,$06,$04,$04,$00 ; slot 6 Parker  — Mess, strength 6
  DEFB $00,$00,$00,$00,$05,$03,$03,$00 ; slot 7 Brett   — placed separately by
                                       ; init

; ShortGameCrewInit
;
; 64-byte starting state (eight 8-byte records) copied by ShortGameInit
; (ShortGameInit) into $737E-$73BD. Same record layout as LongGameCrewInit
; (LongGameCrewInit) — see that block for field semantics.
;
; Slot occupancy comparison (ShortGameCrewInit vs LongGameCrewInit):
;
; slot 0 ALIEN   : all-zero in both slot 1 Dallas  : Short: $FF in +1/+7 — out
; of play slot 2 Kane    : Short: $FF in +1/+7 — out of play slot 3 Ripley  :
; alive — starts in CommdCentr (+1=6), strength 4 slot 4 Ash     : Short: $FF
; in +1/+7 — out of play slot 5 Lambert : alive — starts in CommdCentr (+1=6),
; strength 4 slot 6 Parker  : alive — starts in CommdCentr (+1=6), strength 6
; slot 7 Brett   : Short: $FF in +1/+7 — out of play
;
; The Short Game is the film's final act: Dallas, Kane, Ash and Brett are
; already gone (their slots carry the same $FF removed-from-play pattern the
; Long Game applies to its single chestburster host), and Ripley, Lambert and
; Parker start together on the bridge. Combined with the fixed AlienTargetID
; AlienTargetID=9 (which the alien-target guard at TriggerAlien_TargetCheck
; reads as "no valid target", so no crew member turns Android), the Short Game
; is a pure evade-and-escape scenario — whereas the Long Game uses script
; randomisation to pick a single chestburster host plus the Android.
ShortGameCrewInit:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00 ; slot 0 ALIEN (room written by
                                       ; CommonGameInit)
  DEFB $00,$FF,$00,$00,$00,$00,$00,$FF ; slot 1 Dallas  — out of play
  DEFB $00,$FF,$00,$00,$00,$00,$00,$FF ; slot 2 Kane    — out of play
  DEFB $00,$06,$06,$00,$04,$03,$03,$00 ; slot 3 Ripley  — CommdCentr, strength
                                       ; 4
  DEFB $00,$FF,$00,$00,$00,$00,$00,$FF ; slot 4 Ash     — out of play
  DEFB $00,$06,$06,$00,$04,$03,$03,$00 ; slot 5 Lambert — CommdCentr, strength
                                       ; 4
  DEFB $00,$06,$00,$00,$06,$04,$04,$00 ; slot 6 Parker  — CommdCentr, strength
                                       ; 6
  DEFB $00,$FF,$00,$00,$00,$00,$00,$FF ; slot 7 Brett   — out of play

; UI and endgame text bank
;
; Plain-text strings for the ship-map legend, the input-device menu ("Select
; Keyboard or Joystick" - the options screen is device selection, not
; difficulty), the key-redefinition flow, the encounter epilogue ("The Alien
; has been killed / is dead / was the Android", "Competence Rating") and the
; endgame screens. Drawn B characters at a time from DE by the loop at PrintStr
; via the glyph blitter DrawSpriteRow.
;
; Runtime-patched cells: the Competence Rating digits (StrRatingDigits), the
; destruct countdown minutes and seconds (the blank fields after
; DestructTextIn), poked by the endgame renderer before drawing.
UITextBlock:
  DEFM "MAPPING SYMBOLS"  ; "MAPPING SYMBOLS" (ship-map legend title)
StrConnectingDoor:
  DEFM $01,$0F,$01," : Connecting Door" ; 1,15,1 + " : Connecting Door"
                                        ; (legend)
StrLadderUp:
  DEFM "{  : Ladder to Deck Above" ; "{  : Ladder to Deck Above" (legend)
StrHatchDown:
  DEFM "|  : Hatchway to Deck Below" ; "|  : Hatchway to Deck Below" (legend)
StrSubjectInd:
  DEFM ": Subject Indicator" ; ": Subject Indicator" (legend)
StrRoomInd:
  DEFM ": Room Indicator" ; ": Room Indicator" (legend)
StrGrilleDuct:
  DEFM "}  : Grille to Ducting" ; "}  : Grille to Ducting" (legend)
StrSelectDevice:
  DEFM "Select Keyboard or joystick" ; "Select Keyboard or Joystick" (options
                                     ; title)
StrKempston:
  DEFM "1 : KEMPSTON"     ; "1 : KEMPSTON"
StrAGFProtek:
  DEFM "2 : AGF",$90,"PROTEK" ; "2 : AGF/PROTEK"
StrSinclair:
  DEFM "3 : SINCLAIR"     ; "3 : SINCLAIR"
StrKeyboard:
  DEFM "4 : KEYBOARD"     ; "4 : KEYBOARD"
StrSelect:
  DEFM "5 : SELECT"       ; "5 : SELECT"
StrPressKey:
  DEFM "PRESS APPROPRIATE KEY" ; "PRESS APPROPRIATE KEY"
StrPressPause:
  DEFM "During Game Press 1 to Pause" ; "During Game Press 1 to Pause"
StrUserDefinable:
  DEFM "The keyboard is user definable" ; "The keyboard is user definable"
StrRedefineKeys:
  DEFM "Press Y to redefine keys" ; "Press Y to redefine keys"
StrAnyOtherKey:
  DEFM "Any other key to continue" ; "Any other key to continue"
StrWhichKey:
  DEFM "Which key do you wish to" ; "Which key do you wish to"
StrUseForUp:
  DEFM "use for: UP"      ; "use for: UP"
StrDown:
  DEFM "DOWN"             ; "DOWN"
StrFire:
  DEFM "FIRE"             ; "FIRE"
StrReturnTo:
  DEFM "Return to "       ; "Return to " prefix
  DEFM "QUIT"             ; "QUIT"
; Input-scanner vector table: 4 words indexed by the options-screen device
; choice (0-3). OptionsScreen ($AA5A) patches the chosen word into the CALL
; operand at $874D, so ScanInput's first CALL goes to the device's scanner:
; 41293 Kempston (port 31), 34640 AGF-Protek (cursor-joystick keys 5/6/7/8/0 =
; the keyboard default), 41320 Sinclair (port $EFFE), 44041 redefinable
; Keyboard.
InputScannerTable:
  DEFM "M",$A1,"P",$87,"h",$A1,$09,$AC ; DEFW 41293,34640,41320,44041
StrAlienKilled:
  DEFM "The Alien has been killed" ; "The Alien has been kil"
StrIsDead:
  DEFM " is dead"         ; "led" / " is dead"
StrWasAndroid:
  DEFM " was the Android" ; " was the Android"
StrCompetence:
  DEFM "Competence Rating: 0" ; "Competence Rating: "
StrRatingDigits:
  DEFM "0",$87            ; rating digits "00" (runtime-patched)
StrNarcissusLaunched:
  DEFM "The Narcissus is launched" ; "The Narcissus is launched" (escape
                                   ; ending)
StrWillExplode:
  DEFM "The Nostromo will explode " ; "The Nostromo will explode "
DestructTextIn:
  DEFM "in"               ; "in"
  DEFM "   "              ; destruct minutes field (runtime-patched)
  DEFM "minutes"          ; "minutes"
  DEFM "   "              ; destruct seconds field (runtime-patched)
  DEFM " seconds"         ; " seconds"
StrAlienUnleashed:
  DEFM "The Alien will be unleashed" ; "The Alien will be unleashed"
                                     ; (no-destruct ending)
StrUponEarth:
  DEFM "upon the Earth"   ; "upon the Earth"
StrNostromoExploded:
  DEFM "Nostromo has exploded" ; "Nostromo has exploded" (destruct ending)
StrLifeSupportGone:
  DEFM "Life Support Systems exhausted" ; "Life Support Systems exhausted"
StrLifeLevelsMinimal:
  DEFM "Humanoid life levels minimal " ; "Humanoid life levels minimal"
PressAnyKeyText:
  DEFM "PRESS ANY KEY"    ; "PRESS ANY KEY"

; LongGameInit
;
; Game-mode handler for "2: Long Game" (dispatched from GameModeDispatchTable).
;
; The Long Game is the full-length scenario. All 7 crew start alive but ONE of
; them is selected (via the ROM-traversal script, see AdvanceScriptPtr
; AdvanceScriptPtr) to be the **chestburster host** — the crew member the alien
; physically hatches out of. That slot is marked by stamping the HostMarker
; pattern ($FF on bytes +1 and +7 of its 8-byte record) so per-frame routines
; like SelectSlotByDrawIdx (SelectSlotByDrawIdx) can skip it with a `CP 255`
; test on byte +1. The host's name is also baked into the opening status-bar
; message #33 enqueued at step 7 below.
;
; The script independently chooses the alien's initial stalk target, so each
; Long Game has both a different host AND a different first victim.
;
; The android-traitor mechanic lives on the OTHER randomised slot picked below:
; AlienTargetID (LongGameTargetTable values {1, 2, 4, 6} = Dallas / Kane / Ash
; / Parker). That slot is dormant at game start — it appears as just another
; crew member. UpdateAlien (UpdateAlien) flips it hostile on the **first
; encounter** with the alien: the first frame AlienActiveFlag is non-zero AND
; the slot still carries the template's zero values in bytes +0 and +7,
; UpdateAlien stamps it into action state 7 (attack) and arms it with
; sprite-class kin as targets. From then on the Android wounds nearby crew via
; CrewAction7_Handler (CrewAction7_Handler), named in status-bar dialogue (msg
; #30 "{Android} is attacking {target}"). When other crew fight it in combat
; state 4, CrewHitsAndroid (CrewHitsAndroid, msg #32 "{actor} hits the
; Android") logs the response. The alien itself kills crew that share its room
; via CrewHitsAlien (CrewHitsAlien) and the kill primitive at
; AlienKillPrimitive — independent of this Android subplot.
;
; Steps: 1. Point the message queue head (MsgQueueWritePtr MsgQueueWritePtr) at
; the queue base MessageQueue (MessageQueue) and clear the two run-state bytes
; MsgDrawnCol/MsgVisibleTimer. 2. CALL ResetMsgAndScript: reseed the script
; pointer from the ZX SEED system variable and reset the message-dup marker
; (MsgLastEnqueued MsgLastEnqueued) to $FFFF. 3. LDIR 64 bytes from
; LongGameCrewInit (LongGameCrewInit) into $737E to populate the alien slot and
; the seven crew slots with the long-game starting state (all crew alive,
; normal alien position). 4. Pull one script nibble, look it up in
; LongGameHostSlotTable (LongGameHostSlotTable) to get the host slot index
; A_host (one of {1, 2, 5}), then stamp the HostMarker on that record by
; overwriting bytes +1 and +7 with $FF. 5. Loop pulling more script nibbles,
; looking them up in LongGameTargetTable (LongGameTargetTable), until the
; looked-up value A_target differs from A_host — the Android can't be the same
; slot the alien just hatched out of. 6. Store A_target in AlienTargetID
; (AlienTargetID) — this slot is the per-run **Android**: a dormant traitor
; crew member that UpdateAlien (UpdateAlien) flips hostile on first encounter
; with the loose alien. 7. Enqueue message #33 ("Alien has hatched from
; {actor}") via EnqueueMessage, with the host slot index as the actor parameter
; — this is the long game's opening narrative beat. 8. Store &slot[A_target] in
; AlienTargetPtr (AlienTargetPtr) so UpdateAlien and CrewHitsAndroid can access
; the Android's crew record directly. 9. Fall through into the common init at
; CommonGameInit (CommonGameInit).
;
; Used by the dispatcher at GameModeJump via the table at
; GameModeDispatchTable.
LongGameInit:
  LD HL,MessageQueue      ; HL = MessageQueue (event queue base)
  LD (MsgQueueWritePtr),HL ; event queue head = queue base (empty queue)
  XOR A                   ; A = 0
  LD (MsgVisibleTimer),A  ; clear run-state byte MsgVisibleTimer
  LD (MsgDrawnCol),A      ; clear run-state byte MsgDrawnCol
  CALL ResetMsgAndScript  ; reset message-dup marker + reseed script pointer
  LD HL,LongGameCrewInit  ; HL = LongGameCrewInit ($A1E4)
  LD DE,ActorRecords      ; DE = $737E (alien slot + crew table base)
LongGame_SetupRecords:
  LD BC,$0040             ; 64 bytes = 8 × 8-byte records
LongGame_CopyRecords:
  LDIR                    ; copy long-game starting crew/alien state
  CALL AdvanceScriptPtr   ; A = script nibble (0-15)
  LD E,A
  LD D,$00
  LD HL,LongGameHostSlotTable ; HL = LongGameHostSlotTable ($A19E)
  ADD HL,DE
LongGame_PickHost:
  LD A,(HL)               ; A_host = chestburster-host slot index for this run
  PUSH AF                 ; stash A_host
  ADD A,A
  ADD A,A
  ADD A,A                 ; A_host × 8 (record stride)
  LD E,A
  LD HL,ActorRecords
  ADD HL,DE               ; HL = &slot[A_host]
  INC HL                  ; HL = &slot[A_host] + 1 (crew ID byte)
  LD A,(HL)
  LD ($73B7),A            ; save original ID at $73E7 (never read; effectively
                          ; scratch)
  LD (HL),$FF             ; stamp HostMarker: slot[A_host].byte+1 = $FF
  LD DE,$0006
  ADD HL,DE               ; HL = &slot[A_host] + 7 (alive flag)
LongGame_PickTarget:
  LD (HL),$FF             ; stamp HostMarker: slot[A_host].byte+7 = $FF
PickAlienTargetLoop:
  CALL AdvanceScriptPtr   ; A = next script nibble
  LD HL,LongGameTargetTable ; HL = LongGameTargetTable ($A1AE)
  LD E,A
  LD D,$00
  ADD HL,DE
  LD C,(HL)               ; C = candidate alien-target index
  POP AF                  ; A = A_host
  CP C
LongGame_TargetOk:
  JR NZ,StoreAlienTarget  ; if C != A_host → accept
  PUSH AF
LongGame_Retry:
  JR PickAlienTargetLoop  ; else: pull another nibble (avoid colliding with
                          ; host)
StoreAlienTarget:
  LD HL,AlienTargetID     ; HL = &AlienTargetID
LongGame_SetTarget:
  LD (HL),C               ; AlienTargetID = C (randomised)
  PUSH BC
  LD L,A                  ; L = A_host (actor param for message)
  LD A,$21                ; A = 33 → message "Alien has hatched from {actor}"
LongGame_HatchMsg:
  CALL EnqueueMessage     ; enqueue opening narrative naming the host
LongGame_StoreTargetPtr:
  POP BC
  LD A,C                  ; A = AlienTargetID
  ADD A,A
  ADD A,A
  ADD A,A                 ; A × 8
  LD E,A
  LD D,$00
  LD HL,ActorRecords
  ADD HL,DE               ; HL = &slot[AlienTargetID]
  LD (AlienTargetPtr),HL  ; store alien-target record pointer at AlienTargetPtr
; This entry point is used by the routine at ShortGameInit. CommonGameInit:
; shared post-init for both Short and Long modes. Called (via JP) from
; ShortGameInit_Done (ShortGameInit) and fallen into from LongGameInit
; (LongGameInit). Resets the alien runtime state, seeds the crew workspace
; tables, clears the corridor cursor at $757C, picks the initial alien event
; timer from the script, and switches the room mode to 1 (corridor view) so the
; player drops straight into gameplay.
CommonGameInit:
  XOR A
  LD (AlienActiveFlag),A
  LD HL,DrawMenuStrip     ; HL = $8080: two "frozen" bytes for both channels
  LD (CrewAnimFlags),HL
  LD A,L
  LD (AlienXPhase),A
  LD (JonesPhase),A
CommonInit_ItemsGrilles:
  LD HL,InitialItemRooms  ; HL = InitialItemRooms ($A1BE)
  LD DE,ItemLocations     ; DE = ItemLocations ($74B4)
  LD BC,$0016
  LDIR                    ; place the 22 items in their start rooms
  LD B,$21                ; RoomGrilleState: put all grilles in place
  LD HL,RoomGrilleState   ; (33 bytes; room 33's byte stays 0 = open)
  LD A,$FF
CommonInit_GrilleLoop:
  LD (HL),A
  INC HL
  DJNZ CommonInit_GrilleLoop
  LD E,$0D                ; ...except Corridor#6 (room 13):
  LD D,$00                ; its grille starts removed
  LD HL,RoomGrilleState
  ADD HL,DE
  LD (HL),D
  LD HL,ShipSystemFlags
  LD (HL),D
  CALL AdvanceScriptPtr   ; Jones starts in a random
  LD (JonesRoom),A        ; room 0-15 (script nibble)
  CALL AdvanceScriptPtr   ; the alien's lair is drawn from
  LD E,A
  LD D,$00
  LD HL,AlienInitTable    ; HL = AlienInitTable ($A18E)
  ADD HL,DE
  LD A,(HL)               ; A = AlienInitTable[script_byte]
  OR $40                  ; ...hidden in the ducts above it (bit 6)
CommonInit_AlienRoom:
  LD ($737F),A            ; set the alien's starting room (slot 0 byte +1)
CommonInit_Charges:
  LD HL,ItemChargesInit   ; HL = ItemChargesInit ($A1D4)
  LD DE,ItemCharges
  LD BC,$0010
  LDIR
  LD HL,RoomDamageDigits
  LD (HL),$30
  LD DE,$7537
  LD BC,$0043
  LDIR
  LD HL,$3030
  LD (OxygenLo),HL
  LD HL,$3537
  LD (OxygenHi),HL
  LD A,$20
  LD ($95E5),A
  LD A,$01
  LD (RoomTypeByte),A
  RET

; ClearDisplay
;
; Fills the entire ZX Spectrum display file with zero bytes, blanking all pixel
; data. The display file occupies DisplayFile-$57FF (6144 bytes). Uses a single
; LDIR from DisplayFile into $4001 after priming the first byte to 0.
;
; Used by the routines at DrawIntroScreen, GameModeScreen, IntroductionMode,
; OptionsScreen and ScreenTransition.
ClearDisplay:
  LD HL,DisplayFile       ; HL = DisplayFile (start of display file)
  LD DE,$4001             ; DE = $4001 (destination = source+1)
  LD BC,$17FF             ; BC = 6143 bytes to copy
  LD (HL),$00             ; prime first byte = 0
  LDIR                    ; propagate zero across entire display file
  RET

; FillAttributes
;
; Fills the entire ZX Spectrum attribute file with the value in A. The
; attribute file occupies AttrFileOrigin-$5AFF (768 bytes). Uses a single LDIR
; after priming the first byte. Common attribute values: 0=black on black
; (hidden), 7=white ink on black paper, 32=green ink on black paper.
;
; Input: A = attribute byte to flood-fill
;
; Used by the routines at DamageOverflowFlash, DrawIntroScreen, GameModeScreen,
; IntroductionMode, OptionsScreen and ScreenTransition.
FillAttributes:
  LD HL,AttrFileOrigin    ; HL = AttrFileOrigin (start of attribute file)
  LD DE,$5801             ; DE = $5801
  LD (HL),A               ; prime first attribute byte
  LD BC,$02FF             ; BC = 767 remaining bytes
  LDIR                    ; propagate across entire attribute file
  RET

; Ship-map screen title and credits
;
; Plain ASCII (previously misclassified as code), drawn by GameModeScreen
; (GameModeScreen): the big "ALIEN" title (one glyph per cell, 3-column
; spacing) and the author/publisher credit lines "John Heap" / "Argus Press
; Software".
StrAlien:
  DEFM "ALIEN"
  DEFM "        "
StrJohn:
  DEFM "John"
StrHeap:
  DEFM "Heap"
StrArgus:
  DEFM "Argus"
StrPress:
  DEFM "Press"
StrSoftware:
  DEFM "Software"
StrCopyright:
  DEFM "Copyright 1984"

; DrawIntroScreen
;
; Draws and animates the Alien title screen. Clears the display, sets border to
; black, fills attributes (0 = black on black = hidden). Renders a 15×11 tile
; grid: each byte of the tile map at IntroTileMap is a tile index; multiplied
; by 8 gives an offset into the 8-byte-per-tile bitmap array at TileBitmaps
; (actually at TileBitmaps = TileBitmaps decimal, in the extra-data area loaded
; at AlienFrame0). Tile rows are blitted column by column down into the display
; file. After drawing, loops animating colours until any key on row $F7FE is
; pressed.
;
; Used by the routine at GameEntry.
DrawIntroScreen:
  CALL ClearDisplay       ; ClearDisplay: blank all pixel data
  XOR A                   ; A = 0 (black border, black attributes)
  OUT ($FE),A             ; set border colour = black
  CALL FillAttributes     ; FillAttributes(0): all black-on-black (invisible)
  LD DE,IntroTileMap      ; DE = IntroTileMap (intro tile-map data)
  LD HL,$408A             ; HL = $408A (screen address: top-left of tile area)
  LD (DrawScreenPtr),HL   ; store as current draw pointer (DrawScreenPtr)
  LD B,$0F                ; 15 tile columns
Intro_ColLoop:
  PUSH BC
  LD B,$0B
Intro_TileLoop:
  PUSH BC
  PUSH DE
  LD A,(DE)
  LD L,A
  LD H,$00
  ADD HL,HL
  ADD HL,HL
  ADD HL,HL
  LD DE,TileBitmaps
  ADD HL,DE
  LD DE,(DrawScreenPtr)
  LD B,$08
Intro_PixLoop:
  LD A,(HL)
  LD (DE),A
  INC D
  INC HL
; This entry point is used by the routine at FillAttributes.
  DJNZ Intro_PixLoop
  LD HL,DrawScreenPtr
  INC (HL)
; This entry point is used by the routine at FillAttributes.
  POP DE
  INC DE
  POP BC
  DJNZ Intro_TileLoop
  LD BC,$0015
  LD HL,(DrawScreenPtr)
  LD A,L
  CP $E0
  JR C,Intro_Advance
  LD BC,$0715
Intro_Advance:
  ADD HL,BC
  LD (DrawScreenPtr),HL
  POP BC
  DJNZ Intro_ColLoop
  LD A,$07
  CALL FillAttributes
  LD HL,$596A
  LD DE,IntroAttributes
  LD B,$08
Intro_AttrRowLoop:
  PUSH BC
  LD B,$0B
Intro_AttrCellLoop:
  LD A,(DE)
  LD (HL),A
  INC DE
  INC HL
  DJNZ Intro_AttrCellLoop
  LD BC,$0015
  ADD HL,BC
  POP BC
  DJNZ Intro_AttrRowLoop
  LD HL,$4029
  LD (DrawScreenPtr),HL
  LD DE,StrAlien
  LD B,$05
Intro_TitleLoop:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  LD HL,DrawScreenPtr
  INC (HL)
  INC (HL)
  INC (HL)
  CALL PlayMusicPhrase
  POP DE
  INC DE
  POP BC
  DJNZ Intro_TitleLoop
  LD HL,$5084
  LD (DrawScreenPtr),HL
  LD B,$04
  LD DE,StrJohn
  CALL PrintStr
  LD HL,$50A4
  LD (DrawScreenPtr),HL
  LD B,$04
  LD DE,StrHeap
  CALL PrintStr
  LD HL,$5096
  LD (DrawScreenPtr),HL
  LD B,$05
  LD DE,StrArgus
  CALL PrintStr
  LD HL,$50B6
  LD (DrawScreenPtr),HL
  LD B,$05
  LD DE,StrPress
  CALL PrintStr
  LD HL,$50D6
  LD (DrawScreenPtr),HL
  LD B,$08
  LD DE,StrSoftware
  CALL PrintStr
  LD HL,Screen_R23C8
  LD (DrawScreenPtr),HL
  LD B,$0E
  LD DE,StrCopyright
  CALL PrintStr
Intro_WaitLoop:
  CALL PlayMusicPhrase
  LD BC,$FEFE
Intro_KeyPoll:
  IN A,(C)
  AND $1F
  XOR $1F
  RET NZ
  RLC B
  JR C,Intro_KeyPoll
  JR Intro_WaitLoop

; GameModeScreen — crew roster + game-mode menu
;
; Despite the old "DrawShipMap" name, no ship map is drawn here. The screen
; shows the seven crew portraits (four across the top, three below) with their
; names, then the menu "1: Short Game / 2: Long Game / 3: Introduction / 4:
; Select" from the text table at GameModeText.
;
; Key input loop reads port $F7FE: Key 1 — highlight "Short Game"   (selection
; 0) Key 2 — highlight "Long Game"    (selection 1) Key 3 — highlight
; "Introduction" (selection 2) Key 4 — confirm: dispatch through the 3-word
; table at GameModeDispatchTable to the handler for the highlighted mode
;
; "4: Select" is a confirm key, not a fourth game mode.
;
; Used by the routines at GameEntry and IntroductionMode.
GameModeScreen:
  LD A,$04                ; green border,
  OUT ($FE),A
  LD A,$20                ; green paper, black ink
  CALL FillAttributes     ; FillAttributes(32)
  CALL ClearDisplay       ; ClearDisplay
  CALL WaitKeyRelease     ; wait for the keys to clear
  LD DE,CrewPortraits     ; four crew portraits across the
  LD HL,$4044             ; top (from row 2, col 4; 72 data
  LD B,$04                ; bytes / 7 columns each)
ShipMap_Portraits1:
  PUSH BC
  PUSH HL
  PUSH DE
  CALL BlitPortrait       ; BlitPortrait
  POP DE
  LD HL,$0048             ; next portrait's data,
  ADD HL,DE
  EX DE,HL
  POP HL
  LD BC,$0007             ; 7 columns right
  ADD HL,BC
  POP BC
  DJNZ ShipMap_Portraits1
  LD HL,$4807             ; three more below
  LD B,$03                ; (from row 8, col 7)
ShipMap_Portraits2:
  PUSH BC
  PUSH HL
  PUSH DE
  CALL BlitPortrait       ; BlitPortrait
  POP DE
  LD HL,$0048
  ADD HL,DE
  EX DE,HL
  POP HL
  LD BC,$0007
  ADD HL,BC
  POP BC
  DJNZ ShipMap_Portraits2
  LD HL,$40C3             ; the names under the portraits:
  LD A,$01                ; Dallas,
  LD (DrawScreenPtr),HL
  CALL PrintName
  LD HL,$40CB             ; Kane,
  LD (DrawScreenPtr),HL
  LD A,$02
  CALL PrintName
  LD HL,$40D1             ; Ripley,
  LD (DrawScreenPtr),HL
  LD A,$03
  CALL PrintName
  LD HL,$40D9             ; Ash,
  LD (DrawScreenPtr),HL
  LD A,$04
  CALL PrintName
  LD HL,$4885             ; Lambert,
  LD (DrawScreenPtr),HL
  LD A,$05
  CALL PrintName
  LD HL,$488D             ; Parker,
  LD (DrawScreenPtr),HL
  LD A,$06
  CALL PrintName
  LD HL,$4895             ; Brett
  LD (DrawScreenPtr),HL
  LD A,$07
  CALL PrintName
  LD DE,GameModeText      ; the menu lines:
  LD HL,$48E9             ; "1: Short Game" (row 15)
  LD (DrawScreenPtr),HL
  PUSH DE
  XOR A
  CALL PrintNameDE
  LD HL,$5029             ; "2: Long Game" (row 17)
  LD (DrawScreenPtr),HL
  POP DE
  PUSH DE
  LD A,$01
  CALL PrintNameDE
  LD HL,$5069             ; "3: Introduction" (row 19)
  LD (DrawScreenPtr),HL
  POP DE
  PUSH DE
  LD A,$02
  CALL PrintNameDE
  LD HL,$50A9             ; "4: Select" (row 21)
  LD (DrawScreenPtr),HL
  POP DE
  PUSH DE
  LD A,$03
  CALL PrintNameDE
  LD HL,$50E6             ; "Press Appropriate Key" (row 22)
  LD (DrawScreenPtr),HL
  POP DE
  LD A,$04
  CALL PrintNameDE
  XOR A                   ; selection = 0 (Short Game),
  LD (CorridorCursor),A
  CALL HighlightGameMode  ; highlighted
ShipMap_KeyLoop:
  LD BC,$F7FE             ; scan half-row 1-5:
  IN A,(C)
  BIT 0,A                 ; key 1?
  JR Z,ShipMap_Pick1
  BIT 1,A                 ; key 2?
  JR Z,ShipMap_Pick2
  BIT 2,A                 ; key 3?
  JR Z,ShipMap_Pick3
  BIT 3,A                 ; key 4 = confirm?
  JR Z,ShipMap_Confirm
  JR ShipMap_KeyLoop
ShipMap_Pick1:
  LD A,(CorridorCursor)   ; already on Short Game?
  AND A
  JR Z,ShipMap_KeyLoop
  CALL HighlightGameMode  ; no: unhighlight the old pick,
  XOR A                   ; select mode 0,
  LD (CorridorCursor),A
  CALL HighlightGameMode  ; highlight it
  JR ShipMap_KeyLoop
ShipMap_Pick2:
  LD A,(CorridorCursor)   ; already on Long Game?
  CP $01
  JR Z,ShipMap_KeyLoop
  CALL HighlightGameMode  ; no: repick mode 1
  LD A,$01
  LD (CorridorCursor),A
  CALL HighlightGameMode
  JR ShipMap_KeyLoop
ShipMap_Pick3:
  LD A,(CorridorCursor)   ; already on Introduction?
  CP $02
  JR Z,ShipMap_KeyLoop
  CALL HighlightGameMode  ; no: repick mode 2
  LD A,$02
  LD (CorridorCursor),A
  CALL HighlightGameMode
  JR ShipMap_KeyLoop
ShipMap_Confirm:
  LD A,(CorridorCursor)   ; key 4: dispatch the picked mode
  ADD A,A                 ; through GameModeDispatchTable
  LD HL,GameModeDispatchTable
  LD E,A
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
GameModeJump:
  JP (HL)

; GameModeDispatchTable
;
; 3-word jump table indexed by the highlighted game mode (0-2). JP (HL) at
; GameModeJump lands here; the word for the current selection is loaded into HL
; and jumped to.
;
; Mode 0 — Short Game   → ShortGameInit (ShortGameInit) Mode 1 — Long Game    →
; LongGameInit (LongGameInit) Mode 2 — Introduction → IntroductionMode
; (IntroductionMode)
;
; Short vs Long Game summary (see the two init routines for full detail):
;
; AlienTargetID (AlienTargetID): - Short Game = 9, hard-wired at
; ShortGame_SetTarget. Crucially, this is >= 8, which the alien-event guard at
; TriggerAlien_TargetCheck (TriggerAlienEvent's "CP 8 / RET NC") treats as "no
; valid target" — so the alien's targeted- stalk logic is effectively disabled
; in Short Game and the alien just roams. - Long Game  = random pick from
; LongGameTargetTable (LongGameTargetTable, values in {1, 2, 4, 6}) at
; LongGame_SetTarget. Always < 8, so the alien actively hunts the chosen crew
; member.
;
; Chestburster host slot (HostMarker = byte+1=$FF, byte+7=$FF): - Long Game  =
; exactly ONE slot, picked randomly via LongGameHostSlotTable
; (LongGameHostSlotTable, values {1,2,5}). That crew is the chestburster host —
; message #33 enqueued at startup ("Alien has hatched from {actor}")
; substitutes this crew's name in. - Short Game = FOUR pre-stamped slots in the
; template: Dallas (1), Kane (2), Ash (4) and Brett (7) are out of play,
; leaving only Ripley, Lambert and Parker — the film's final act — and no host
; subplot.
;
; (The in-game "Android" of message #32 "{actor} hits the Android" is the crew
; slot selected into AlienTargetID AlienTargetID — see the notes at
; LongGameTargetTable and the activation logic in UpdateAlien.)
;
; Starting crew/alien record state at $737E: - Short Game = ShortGameCrewInit
; (ShortGameCrewInit) — four slots pre-stamped with the HostMarker, yielding a
; smaller "active" starting cast - Long Game  = LongGameCrewInit
; (LongGameCrewInit) — every slot starts with normal (non-$FF) data;
; LongGameInit then picks ONE slot via LongGameHostSlotTable
; (LongGameHostSlotTable, values in {1, 2, 5}) and stamps the HostMarker on
; that record's bytes +1 and +7
;
; Script-driven startup message: - Short Game does not enqueue any startup
; message - Long Game enqueues message #33 ("Alien has hatched from {actor}")
; carrying the host slot index via EnqueueMessage — the long game's opening
; narrative beat naming the chestburster host
;
; Both modes then fall through to CommonGameInit (CommonGameInit), which
; finishes the per-session setup identically (alien position, animation tables,
; room mode = 1 corridor view).
;
; In short: the Short Game is a deterministic, abbreviated scenario; the Long
; Game uses the ROM-traversal script (AdvanceScriptPtr) to randomise both the
; alien's first target and one tagged crew slot per run, producing a longer and
; more varied game.
GameModeDispatchTable:
  DEFW ShortGameInit,LongGameInit,IntroductionMode

; GameModeText
;
; $FF-terminated text strings for the game-mode menu, rendered by
; GameModeScreen (GameModeScreen) via five calls to PrintNameDE. Indices 0-3
; are the four menu lines; index 4 is the footer prompt.
GameModeText:
  DEFB $31,$3A,$20,$53,$68,$6F,$72,$74,$20,$47,$61,$6D,$65,$FF ; "1: Short
                                                               ; Game"
  DEFB $32,$3A,$20,$4C,$6F,$6E,$67,$20,$47,$61,$6D,$65,$FF ; "2: Long Game"
  DEFB $33,$3A,$20,$49,$6E,$74,$72,$6F ; "3: Intro
  DEFB $64,$75,$63,$74,$69,$6F,$6E,$FF ; duction"
  DEFB $34,$3A,$20,$53,$65,$6C,$65,$63,$74,$FF ; "4: Select"
  DEFB $50,$72,$65,$73,$73,$20,$41,$70,$70,$72,$6F ; "Press Appro
  DEFB $70,$72,$69,$61,$74,$65,$20,$4B,$65,$79,$FF ; priate Key"

; HighlightGameMode
;
; XOR-toggles the attribute colour of the currently selected menu row, used by
; GameModeScreen (GameModeScreen) to highlight or erase the cursor. Loads the
; base attribute address $59E9 (row 15, col 9), steps 64 bytes per row
; (selection × 64), then XORs 15 consecutive attribute bytes with 36 (flipping
; ink/paper to show highlight).
HighlightGameMode:
  LD HL,$59E9
; This entry point is used by the routine at OptionsScreen.
HighlightModeAt:
  LD DE,$0040
  LD A,(CorridorCursor)
  AND A
  JR Z,HighlightMode_Row
  LD B,A
HighlightMode_Seek:
  ADD HL,DE
  DJNZ HighlightMode_Seek
HighlightMode_Row:
  LD B,$0F
HighlightMode_CellLoop:
  LD A,(HL)
  XOR $24
  LD (HL),A
  INC HL
  DJNZ HighlightMode_CellLoop
  RET

; IntroductionMode
;
; Game-mode handler for "3: Introduction" (dispatched from
; GameModeDispatchTable). Clears display, sets border/attrs white, then replays
; the animated intro screen and returns to the main loop.
IntroductionMode:
  CALL ClearDisplay
  XOR A
  OUT ($FE),A
  LD A,$07
  CALL FillAttributes
  LD HL,Screen_R1C8
  LD (DrawScreenPtr),HL
  LD DE,UITextBlock
  LD B,$0F
  CALL PrintStr
  LD HL,$40A4
  LD (DrawScreenPtr),HL
  LD DE,StrConnectingDoor
  LD B,$15
  CALL PrintStr
  LD HL,$40E5
  LD (DrawScreenPtr),HL
  LD DE,StrLadderUp
  LD B,$19
  CALL PrintStr
  LD HL,$4825
  LD (DrawScreenPtr),HL
  LD DE,StrHatchDown
  LD B,$1B
  CALL PrintStr
  LD HL,Screen_R12C8
  LD (DrawScreenPtr),HL
  LD DE,StrSubjectInd
  LD B,$13
  CALL PrintStr
  LD HL,Screen_R15C8
  LD (DrawScreenPtr),HL
  LD DE,StrRoomInd
  LD B,$10
  CALL PrintStr
  LD HL,$5025
  LD (DrawScreenPtr),HL
  LD DE,StrGrilleDuct
  LD B,$16
  CALL PrintStr
  LD HL,$0000
  LD (CrewAnimFlags),HL
  LD HL,$4864
  LD (SprDestA),HL
  LD HL,$48C4
  LD (SpriteDestAddr),HL
  XOR A
  LD B,$01
  CALL BlitMarkerB
  XOR A
  LD B,$01
  CALL BlitMarkerA
  LD HL,$5085
  LD (DrawScreenPtr),HL
  LD DE,StrSoundOfA
  LD B,$16
  CALL PrintStr
  LD HL,$50C5
  LD (DrawScreenPtr),HL
  LD DE,StrDoorOpened
  LD B,$11
  CALL PrintStr
  LD B,$05
IntroMode_SoundLoop1:
  PUSH BC
  LD A,$01
  LD (SoundRequest),A
  CALL FrameTiming
  CALL IntroMode_Animate
  POP BC
  DJNZ IntroMode_SoundLoop1
  LD HL,$50C5
  LD (DrawScreenPtr),HL
  LD DE,StrGrilleRemoved
  LD B,$14
  CALL PrintStr
  LD B,$05
IntroMode_SoundLoop2:
  PUSH BC
  LD A,$01
  LD (SoundPending),A
  CALL PlayMusic
  CALL IntroMode_Animate
  POP BC
  DJNZ IntroMode_SoundLoop2
  LD HL,$50C5
  LD (DrawScreenPtr),HL
  LD DE,StrTrackerReading
  LD B,$18
  CALL PrintStr
  LD B,$0F
IntroMode_DemoLoop:
  PUSH BC
  CALL RomBeep
  CALL IntroMode_Animate
  POP BC
  DJNZ IntroMode_DemoLoop
  XOR A
  LD (SysLastKey),A
IntroMode_WaitKey:
  CALL BusyWait
  CALL AnimateCrewB
  CALL AnimateCrewA
  LD A,(SysLastKey)
  AND A
  JR Z,IntroMode_WaitKey
  JP GameModeScreen
IntroMode_Animate:
  LD B,$14
IntroMode_AnimLoop:
  PUSH BC
  CALL AnimateCrewB
  CALL AnimateCrewA
  CALL BusyWait
  CALL AnimateCrewB
  CALL AnimateCrewA
  POP BC
  DJNZ IntroMode_AnimLoop
  RET

; Introduction-mode sound-demo captions
;
; Plain ASCII (previously misclassified as code): the caption prefix and the
; three effect names shown while the Introduction mode plays each sound.
StrSoundOfA:
  DEFM "This is the sound of a"
StrDoorOpened:
  DEFM "Door being opened"
StrGrilleRemoved:
  DEFM "Grille being removed"
StrTrackerReading:
  DEFM "Positive Tracker Reading"

; OptionsScreen — the input-device menu
;
; "Select Keyboard or joystick": lists 1 KEMPSTON / 2 AGF-PROTEK / 3 SINCLAIR /
; 4 KEYBOARD / 5 SELECT. Keys 1-4 move the highlight (the picked device index,
; 0-3, lives in CorridorCursor — CorridorCursor reused); key 5 confirms:
; Options_Select patches the input-scanner CALL operand from InputScannerTable,
; and picking the keyboard (device 3) continues into the key-redefinition
; offer.
;
; Used by the routine at GameEntry.
OptionsScreen:
  CALL ClearDisplay       ; ClearDisplay
  LD A,$20                ; green paper, black ink
  CALL FillAttributes     ; FillAttributes(32)
  LD HL,$4042             ; "Select Keyboard or joystick"
  LD (DrawScreenPtr),HL   ; on screen row 2
  LD DE,StrSelectDevice
  LD B,$1B
  CALL PrintStr
  LD HL,$40EB             ; "1 : KEMPSTON" (row 5)
  LD (DrawScreenPtr),HL
; This entry point is used by the routine at GameModeScreen.
  LD DE,StrKempston
  LD B,$0C
  CALL PrintStr
; This entry point is used by the routine at GameModeScreen.
  LD HL,$482B             ; "2 : AGF-PROTEK" (row 9)
  LD (DrawScreenPtr),HL
; This entry point is used by the routine at GameModeScreen.
  LD DE,StrAGFProtek
; This entry point is used by the routine at GameModeScreen.
  LD B,$0E
; This entry point is used by the routine at GameModeScreen.
  CALL PrintStr
  LD HL,$486B             ; "3 : SINCLAIR" (row 11)
  LD (DrawScreenPtr),HL
  LD DE,StrSinclair
  LD B,$0C
  CALL PrintStr
  LD HL,$48AB             ; "4 : KEYBOARD" (row 13)
  LD (DrawScreenPtr),HL
; This entry point is used by the routine at GameModeScreen.
  LD DE,StrKeyboard
  LD B,$0C
  CALL PrintStr
  LD HL,$48EB             ; "5 : SELECT" (row 15)
  LD (DrawScreenPtr),HL
; This entry point is used by the routine at GameModeScreen.
  LD DE,StrSelect
  LD B,$0A
  CALL PrintStr
  LD HL,$5066             ; "PRESS APPROPRIATE KEY" (row 19)
  LD (DrawScreenPtr),HL
  LD DE,StrPressKey
  LD B,$15
  CALL PrintStr
  LD HL,$50C2             ; "During Game Press 1 to Pause"
  LD (DrawScreenPtr),HL   ; (row 22)
  LD DE,StrPressPause
  LD B,$1C
  CALL PrintStr
  XOR A                   ; picked device = 0 (Kempston)
  LD (CorridorCursor),A
Options_Redraw:
  CALL Options_HighlightRow ; toggle the pick's highlight
Options_KeyLoop:
  LD HL,CorridorCursor    ; scan half-row 1-5:
  LD BC,$F7FE
  IN A,(C)
  RRA                     ; key 1?
  JR NC,Options_Pick1
  RRA                     ; key 2?
  JR NC,Options_Pick2
  RRA                     ; key 3?
  JR NC,Options_Pick3
  RRA                     ; key 4?
  JR NC,Options_Pick4
  RRA                     ; key 5?
  JR NC,Options_Select
  JR Options_KeyLoop
Options_Pick1:
  LD A,(HL)               ; already on device 0?
  AND A
  JR Z,Options_KeyLoop
  CALL Options_HighlightRow ; no: unhighlight the old pick,
  LD (HL),$00             ; pick Kempston,
  JR Options_Redraw       ; highlight it
Options_Pick2:
  LD A,(HL)               ; already on device 1?
  CP $01
  JR Z,Options_KeyLoop
  CALL Options_HighlightRow ; no: repick AGF-Protek
  LD (HL),$01
  JR Options_Redraw
Options_Pick3:
  LD A,(HL)               ; already on device 2?
  CP $02
  JR Z,Options_KeyLoop
  CALL Options_HighlightRow ; no: repick Sinclair
  LD (HL),$02
  JR Options_Redraw
Options_Pick4:
  LD A,(HL)               ; already on device 3?
  CP $03
  JR Z,Options_KeyLoop
  CALL Options_HighlightRow ; no: repick the keyboard
  LD (HL),$03
  JR Options_Redraw
Options_Select:
  LD A,(HL)               ; key 5: commit the pick —
  ADD A,A                 ; patch the input-scanner CALL
  LD E,A                  ; operand ($874D) from
  LD D,$00                ; InputScannerTable[device]
  LD HL,InputScannerTable
  ADD HL,DE
  LD A,(HL)
  INC HL
  LD H,(HL)
  LD L,A
  LD ($874D),HL
  LD A,(CorridorCursor)   ; a joystick (device < 3)?
  CP $03
  RET NZ                  ; yes: done — into the game
  CALL ClearDisplay       ; keyboard: fresh green screen
  LD A,$20
  CALL FillAttributes
  CALL WaitKeyRelease
  LD HL,$4041             ; "The keyboard is user definable"
  LD (DrawScreenPtr),HL
  LD DE,StrUserDefinable
  LD B,$1E
  CALL PrintStr
  LD A,(KeyboardModeFlag) ; first time on this screen the
  AND A                   ; redefinition is compulsory;
  JR Z,Options_Redefine   ; afterwards it is offered:
  LD HL,$4084             ; "Press Y to redefine keys"
  LD (DrawScreenPtr),HL
  LD DE,StrRedefineKeys
  LD B,$18
  CALL PrintStr
  LD HL,$40A4             ; "Any other key to continue"
  LD (DrawScreenPtr),HL
  LD DE,StrAnyOtherKey
  LD B,$19
  CALL PrintStr
  CALL WaitKeyPress       ; wait for a key
  LD BC,$DFFE             ; BC = port 57342 ($DFFE): keyboard half-row
                          ; P,O,I,U,Y
  IN A,(C)                ; read keyboard row
  AND $10                 ; bit 4 = the Y key (active low)
  RET NZ                  ; return if Y not pressed (keep the current keys)
  CALL WaitKeyRelease
; Key redefinition: each prompt waits for a press (WaitKeyPress returns BC =
; the port and A = the bit mask of the key found) and patches that (port, mask)
; pair straight into KeyboardScanner's self-modifying operands
; (KeyboardScanner).
Options_Redefine:
  LD HL,$4804             ; "Which key do you wish to"
  LD (DrawScreenPtr),HL
  LD DE,StrWhichKey
  LD B,$18
  CALL PrintStr
  LD HL,$4844             ; "use for: UP"
  LD (DrawScreenPtr),HL
  LD DE,StrUseForUp
  LD B,$0B
  CALL PrintStr
  CALL WaitKeyPress       ; the pressed key becomes
  LD ($AC0F),BC           ; UP (advance): patch the
  LD ($AC14),A            ; scanner's port + mask
  CALL WaitKeyRelease
  LD HL,$484D             ; overwrite the word: "DOWN"
  LD (DrawScreenPtr),HL
  LD DE,StrDown
  LD B,$04
  CALL PrintStr
  CALL WaitKeyPress       ; the pressed key becomes
  LD ($AC1B),BC           ; DOWN (retreat)
  LD ($AC20),A
  CALL WaitKeyRelease
  LD HL,$484D             ; "FIRE"
  LD (DrawScreenPtr),HL
  LD DE,StrFire
  LD B,$04
  CALL PrintStr
  CALL WaitKeyPress       ; the pressed key becomes
  LD ($AC27),BC           ; FIRE (action)
  LD ($AC2C),A
  CALL WaitKeyRelease
  LD HL,$484D             ; "Return to QUIT"
  LD (DrawScreenPtr),HL
  LD DE,StrReturnTo
  LD B,$0E
  CALL PrintStr
  CALL WaitKeyPress       ; the pressed key becomes
  LD ($AC33),BC           ; the row-move key
  LD ($AC38),A
  CALL WaitKeyRelease
  LD A,$FF                ; remember the keyboard was
  LD (KeyboardModeFlag),A ; configured once
  RET
Options_HighlightRow:
  PUSH HL                 ; XOR-toggle the highlight on
  LD HL,$58EB             ; option row (HL) (shared with
  CALL HighlightModeAt    ; HighlightGameMode's helper)
  POP HL
  RET

; KeyboardScanner — the (redefinable) keyboard input device
;
; The keyboard entry of InputScannerTable. Each control is a self-modified
; (port, mask) pair patched by the redefinition flow at Options_Redefine. A
; match sets the AlienFlags bit HandleInput expects: UP = bit 2 (cursor up =
; retreat), DOWN = bit 3 (advance), FIRE = bit 4 (action), "Return to QUIT" =
; bit 1 (row jump).
KeyboardScanner:
  LD HL,AlienFlags        ; AlienFlags = no keys
  LD (HL),$00
  LD BC,$7FFE             ; the UP key's port and mask
  IN A,(C)                ; (both operands are patched):
  AND $01                 ; pressed?
  JR NZ,KbdScan_Check2
  SET 2,(HL)              ; yes: cursor up (retreat)
  RET
KbdScan_Check2:
  LD BC,$7FFE             ; the DOWN key
  IN A,(C)
  AND $02
  JR NZ,KbdScan_Check3
  SET 3,(HL)              ; cursor down (advance)
  RET
KbdScan_Check3:
  LD BC,$7FFE             ; the FIRE key
  IN A,(C)
  AND $04
  JR NZ,KbdScan_Check4
  SET 4,(HL)              ; action
  RET
KbdScan_Check4:
  LD BC,$7FFE             ; the "Return to QUIT" key
  IN A,(C)
  AND $08
  RET NZ
  SET 1,(HL)              ; row jump
  RET

; WaitKeyPress — wait for any key and identify it
;
; Rotates through the 8 keyboard half-row ports until a key is down. Returns BC
; = that half-row's port and A = the key's bit mask — exactly the (port, mask)
; pair the redefinition flow patches into KeyboardScanner.
;
; Used by the routines at OptionsScreen and PauseMenu.
WaitKeyPress:
  LD BC,$FEFE             ; start at half-row $FE
WaitPress_Poll:
  IN A,(C)
  AND $1F                 ; any of its 5 keys down?
  XOR $1F                 ; (A = pressed bits as 1s)
  RET NZ                  ; yes: BC/A identify it
  RLC B                   ; next half-row
  JR C,WaitPress_Poll
  JR WaitKeyPress         ; wrapped: start over

; WaitKeyRelease — wait until no key is pressed anywhere
;
; Used by the routines at GameModeScreen, OptionsScreen and PauseMenu.
WaitKeyRelease:
  LD BC,$FEFE             ; all 8 half-rows must
WaitRelease_Poll:
  IN A,(C)                ; read clear
  AND $1F
  XOR $1F
  JR NZ,WaitKeyRelease    ; something still down: restart
  RLC B
  JR C,WaitRelease_Poll
  RET

; EndgameScreen — every way the game can end
;
; Five entry points, one per outcome; each wipes the screen, zeroes RatingScore
; and adds the terms that outcome earns before the shared
; roster/rating/wait-key tail (which restarts at GameEntry):
;
; EndgameScreen (EndgameScreen) — the alien was killed in combat or died in
; space: "The Alien has been killed"; scores crew + rooms + oxygen (the only
; ending that counts ship condition). Endgame_Escape (Endgame_Escape) — the
; Narcissus got away: with the self-destruct armed, the frozen countdown digits
; are patched into "The Nostromo will explode in m minutes ss seconds" and crew
; + oxygen are scored; WITHOUT it the alien survives — "The Alien will be
; unleashed upon the Earth", Competence Rating 0. Endgame_CrewLost
; (Endgame_CrewLost) — no human left alive: "Humanoid life levels minimal",
; then the same destruct-armed split. Endgame_OxygenOut (Endgame_OxygenOut) —
; the air ran out: "Life Support Systems exhausted", every crew member is
; marked dead, then the same destruct-armed split. Endgame_Summary
; (Endgame_Summary) — from DamageGameOver (the ship broke up or the countdown
; expired): the alien died with the ship, but only the oxygen term is scored.
;
; Used by the routines at CrewHitsAlien and BlowLock1.
EndgameScreen:
  CALL ScreenTransition   ; wipe the screen
  XOR A                   ; RatingScore = 0,
  LD (RatingScore),A
  CALL ComputeRating      ; + crew term
  CALL CountRoomDamage    ; + room-condition term
; This entry point is used by the routine at DamageGameOver.
Endgame_Summary:
  CALL OxygenScore        ; + oxygen term
  CALL PrintAlienKilled   ; "The Alien has been killed"
Endgame_Roster:
  CALL PrintDeadRoster    ; the dead-crew roster,
  CALL PrintAndroidReveal ; "<name> was the Android",
  CALL PrintRating        ; "Competence Rating nnn",
  CALL WaitKeyPrompt      ; wait for a key,
  JP GameEntry            ; and restart the game
; This entry point is used by the routine at LaunchSequence.
Endgame_Escape:
  CALL ScreenTransition   ; wipe the screen
  XOR A                   ; RatingScore = 0
  LD (RatingScore),A
  LD DE,StrNarcissusLaunched ; "The Narcissus is launched"
  LD B,$19
  LD HL,$4003
  LD (DrawScreenPtr),HL
  CALL PrintStr
  LD HL,ShipSystemFlags   ; was the self-destruct armed?
  BIT 0,(HL)
  JR NZ,Endgame_ExplodeText ; yes: the Nostromo (and alien) die
  CALL Endgame_UnleashedText ; no: the alien reaches Earth —
  JP Endgame_Roster       ; rating stays 0
Endgame_ExplodeText:
  LD A,(DestructMinutes)  ; patch the frozen countdown into
  LD ($A48A),A            ; the message: minutes digit,
  LD HL,(DestructSeconds) ; seconds digits
  LD ($A494),HL
  LD HL,$4043             ; "The Nostromo will explode"
  LD (DrawScreenPtr),HL
  LD B,$19
  LD DE,StrWillExplode
  CALL PrintStr
  LD HL,$4063             ; "in m minutes ss seconds"
  LD (DrawScreenPtr),HL
  LD B,$17
  LD DE,DestructTextIn
  CALL PrintStr
  CALL ComputeRating      ; + crew term
  CALL OxygenScore        ; + oxygen term
  JP Endgame_Roster
; This entry point is used by the routine at CheckCrewAlive.
Endgame_CrewLost:
  CALL ScreenTransition   ; wipe the screen
  XOR A                   ; RatingScore = 0
  LD (RatingScore),A
  LD HL,$4002             ; "Humanoid life levels minimal"
  LD (DrawScreenPtr),HL
  LD DE,StrLifeLevelsMinimal
  LD B,$1C
  CALL PrintStr
Endgame_DestructChk:
  LD HL,ShipSystemFlags   ; was the self-destruct armed?
  BIT 0,(HL)
  JP NZ,Endgame_ExplodeText ; yes: the explode ending
  CALL Endgame_UnleashedText ; no: the alien reaches Earth
  JP Endgame_Roster
; This entry point is used by the routine at OxygenCheckRedraw.
Endgame_OxygenOut:
  CALL ScreenTransition   ; wipe the screen
  LD HL,$4001             ; "Life Support Systems
  LD (DrawScreenPtr),HL   ; exhausted"
  LD DE,StrLifeSupportGone
  LD B,$1E
  CALL PrintStr
  XOR A                   ; RatingScore = 0
  LD (RatingScore),A
  LD HL,CrewStatusColumn  ; everyone suffocates: all 7 crew
  LD DE,$0008             ; status bytes = 2 (dead, found)
  LD B,$07
Endgame_MarkCrew:
  LD (HL),$02
  ADD HL,DE
  DJNZ Endgame_MarkCrew
  JR Endgame_DestructChk  ; then the destruct-armed split

; CheckCrewAlive — the per-frame crew-extinction gate
;
; Formerly mislabelled "DrawStatusPanel" — it draws nothing. Called once per
; frame from the main loop: walk the +7 status column of crew slots 1-7 and
; return as soon as any HUMAN crew member is still alive (status 0). The
; Android slot (AlienTargetID) is skipped — an Android alone does not keep the
; mission going. If no live human is found, fall through to Endgame_CrewLost
; (Endgame_CrewLost): "Humanoid life levels minimal". (Short Game:
; AlienTargetID is 9, so no slot is skipped; the four absent crew have non-zero
; status from ShortGameCrewInit.)
;
; Used by the routine at GameEntry.
CheckCrewAlive:
  LD HL,CrewStatusColumn  ; HL = slot 1's +7 status byte (CrewStatusColumn)
  XOR A                   ; A = 0
  LD BC,$0701             ; B=7 (7 crew members), C=1 (crew index, 1-based)
  LD DE,$0008             ; DE = 8 (crew record stride)
CrewAlive_Loop:
  LD A,(AlienTargetID)    ; A = the Android's slot index (AlienTargetID)
  CP C                    ; is this slot the Android?
  JR Z,CrewAlive_Next     ; yes: an Android doesn't count as a survivor
  XOR A                   ; A = 0
  CP (HL)                 ; status 0 = alive?
  RET Z                   ; a human lives: the game goes on
CrewAlive_Next:
  INC C                   ; advance crew index
  ADD HL,DE               ; HL -> next record's status byte
  DJNZ CrewAlive_Loop     ; repeat for all 7 crew
  JP Endgame_CrewLost     ; nobody left: Endgame_CrewLost

; PrintAlienKilled
;
; Used by the routine at EndgameScreen.
PrintAlienKilled:
  LD HL,$4044             ; "The Alien has been killed"
  LD (DrawScreenPtr),HL   ; on screen row 2
  LD B,$19
  LD DE,StrAlienKilled
  CALL PrintStr
  RET

; PrintDeadRoster
;
; List the fallen: one "<name> is dead" line per crew slot whose status is
; neither alive (0) nor removed-from-play (255) — the Android excluded (its
; reveal line follows separately).
;
; Used by the routine at EndgameScreen.
PrintDeadRoster:
  LD HL,$40A4             ; from screen row 5, col 4
  LD (DrawScreenPtr),HL
  LD BC,$0701             ; B = 7 slots, C = slot 1
  LD IX,CrewRecords
  LD DE,$0008
DeadRoster_Loop:
  PUSH BC
  PUSH DE
  LD A,(AlienTargetID)    ; the Android gets its own line
  CP C
  JR Z,DeadRoster_Next
  LD A,(IX+$07)           ; alive, or never in play?
  AND A
  JR Z,DeadRoster_Next    ; then not on the roster
  CP $FF
  JR Z,DeadRoster_Next
  LD HL,(DrawScreenPtr)
  PUSH HL
  LD A,C                  ; "<name> is dead"
  CALL PrintName
  LD DE,StrIsDead
  LD B,$08
  CALL PrintStr
  POP HL                  ; two screen rows down (+64,
  LD DE,$0040             ; or the screen-third stride)
  LD A,L
  CP $E0
  JR C,DeadRoster_Advance
  LD DE,$0740
DeadRoster_Advance:
  ADD HL,DE
  LD (DrawScreenPtr),HL
DeadRoster_Next:
  POP DE
  ADD IX,DE
  POP BC
  INC C
  DJNZ DeadRoster_Loop
  RET

; PrintAndroidReveal
;
; The post-mortem twist: name the traitor ("<name> was the Android") — Long
; Game only (a valid AlienTargetID).
;
; Used by the routine at EndgameScreen.
PrintAndroidReveal:
  LD HL,$5024             ; on screen row 16, col 4
  LD (DrawScreenPtr),HL
  LD A,(AlienTargetID)    ; was there an Android this game?
  CP $08
  RET NC                  ; no (Short Game): keep the secret
  CALL PrintName          ; print its name +
  LD DE,StrWasAndroid     ; " was the Android"
  LD B,$10
  CALL PrintStr
  RET

; PrintRating
;
; Print "Competence Rating: nnn": clamp RatingScore to 100, reset the ASCII
; digits inside the label string to "00", bump them A times with decimal
; carries (RatingHundred patches in the leading "1" for a perfect 100), then
; print the patched 22-character string.
;
; Used by the routine at EndgameScreen.
PrintRating:
  LD A,(RatingScore)      ; A = RatingScore,
  CP $64                  ; clamped to 100
  JR C,Rating_Digits
  LD A,$64
Rating_Digits:
  CALL ResetRatingDigits  ; digits = "  00"
  LD ($A451),HL
  AND A                   ; zero: print as-is
  JR Z,Rating_PrintLabel
  LD B,A                  ; else count the score out
  LD HL,StrRatingDigits   ; in ASCII: units +1,
  LD A,$3A
Rating_IncLoop:
  INC (HL)
  CP (HL)                 ; past '9'?
  JR NZ,Rating_Next
  LD (HL),$30             ; wrap to '0',
  DEC HL                  ; carry into the tens,
  INC (HL)
  CP (HL)
  JR NZ,Rating_CarryDone
  CALL RatingHundred      ; and past 99 write the "10"
  INC HL                  ; of a perfect 100
Rating_CarryDone:
  INC HL
Rating_Next:
  DJNZ Rating_IncLoop
Rating_PrintLabel:
  LD HL,$5084             ; print "Competence Rating: nnn"
  LD (DrawScreenPtr),HL   ; on screen row 19
  LD DE,StrCompetence
  LD B,$16
  CALL PrintStr
  RET

; PrintStr
;
; Draw B characters from DE at the current draw pointer — one DrawSpriteRow
; glyph per character, advancing one column each time. The workhorse text
; printer for the menu/intro/endgame screens.
;
; Used by the routines at DrawRoomMates, DrawIntroScreen, IntroductionMode,
; OptionsScreen, EndgameScreen, PrintAlienKilled, PrintDeadRoster,
; PrintAndroidReveal, PrintRating, DamageGameOver, WaitKeyPrompt and PauseMenu.
PrintStr:
  PUSH BC
  PUSH DE
  CALL DrawSpriteRow
  POP DE
  INC DE
  LD HL,DrawScreenPtr
  INC (HL)
  POP BC
  DJNZ PrintStr
  RET

; DamageGameOver
;
; Used by the routine at DamageOverflowFlash.
DamageGameOver:
  CALL ScreenTransition
  LD HL,CrewStatusColumn
  LD B,$07
  LD DE,$0008
DamageOver_Loop:
  LD (HL),$02
  ADD HL,DE
  DJNZ DamageOver_Loop
  LD HL,$4005
  LD (DrawScreenPtr),HL
  LD DE,StrNostromoExploded
  LD B,$15
  CALL PrintStr
  XOR A
  LD (RatingScore),A
  JP Endgame_Summary

; ScreenTransition
;
; Animated wipe-to-black transition between screens. Progressively sets each of
; the 24 attribute rows to 0 (black-on-black, making text invisible) with a
; one-frame delay between rows, creating a top-to-bottom blackout animation.
; After all 24 rows are zeroed, clears the display file and resets attributes
; to 7 (white ink on black paper) ready for the next screen.
;
; Used by the routines at GameEntry, EndgameScreen and DamageGameOver.
ScreenTransition:
  LD HL,AttrFileOrigin    ; HL = AttrFileOrigin (start of attribute file)
  XOR A                   ; A = 0 (black-on-black attribute)
  LD B,$18                ; 24 character rows to wipe
Wipe_RowLoop:
  PUSH BC                 ; save row counter
  LD B,$20                ; 32 attribute columns per row
Wipe_CellLoop:
  LD (HL),A               ; write black-on-black: text becomes invisible
  INC HL
  DJNZ Wipe_CellLoop      ; fill entire character row
  PUSH HL                 ; save attribute pointer
  CALL BusyWait           ; one-frame delay (~20ms): visible wipe effect
  POP HL
  POP BC
  DJNZ Wipe_RowLoop       ; next row
  CALL ClearDisplay       ; ClearDisplay: blank all pixels
  LD A,$07                ; white ink on black paper
  CALL FillAttributes     ; FillAttributes(7): restore normal attributes
  RET

; WaitKeyPrompt
;
; Used by the routine at EndgameScreen.
WaitKeyPrompt:
  CALL BusyWait
  LD HL,$50E9
  LD (DrawScreenPtr),HL
  LD DE,PressAnyKeyText
  LD B,$0D
  CALL PrintStr
  XOR A
  LD (SysLastKey),A
  CALL BusyWait
WaitKey_Poll:
  LD A,(SysLastKey)
  AND A
  JR Z,WaitKey_Poll
  RET
; This entry point is used by the routine at EndgameScreen.
Endgame_UnleashedText:
  LD DE,StrAlienUnleashed
  LD HL,$4043
  LD (DrawScreenPtr),HL
  LD B,$1B
  CALL PrintStr
  LD DE,StrUponEarth
  LD HL,$4069
  LD (DrawScreenPtr),HL
  LD B,$0E
  CALL PrintStr
  RET

; ComputeRating — the crew term of the Competence Rating
;
; RatingScore += 2 x strength for every crew member who is still alive (+7 =
; 0), the Android's slot excluded. Which terms an ending adds is what
; distinguishes the outcomes (see the EndgameScreen header): destroying the
; alien scores crew + rooms + oxygen; going up with the ship scores oxygen
; only; escaping with the destruct armed scores crew + oxygen; letting the
; alien survive scores 0.
;
; Used by the routine at EndgameScreen.
ComputeRating:
  LD IX,CrewRecords       ; IX -> slot 1's record
  LD DE,$0008
  XOR A                   ; A' = running total 0
  EX AF,AF'
  LD BC,$0701             ; B = 7 slots, C = slot 1
Rating_CrewLoop:
  LD A,(AlienTargetID)    ; the Android earns nothing
  CP C
  JR Z,Rating_SkipCrew
  LD A,(IX+$07)           ; alive?
  AND A
  JR NZ,Rating_SkipCrew
  LD L,(IX+$04)           ; yes: total += 2 x strength
  SLA L
  EX AF,AF'
  ADD A,L
  EX AF,AF'
Rating_SkipCrew:
  ADD IX,DE
  INC C
  DJNZ Rating_CrewLoop
  EX AF,AF'               ; RatingScore += the total
  LD C,A
  LD A,(RatingScore)
  ADD A,C
  LD (RatingScore),A
  RET

; CountRoomDamage — the ship-condition term of the Competence Rating
;
; RatingScore += 1 for every room whose damage meter is still below "20" (tens
; digit < '2') — 34 rooms, up to 34 points.
;
; Used by the routine at EndgameScreen.
CountRoomDamage:
  LD BC,$2200             ; B = 34 rooms, C = 0 points
  LD HL,RoomDamageDigits  ; walk the damage TENS digits
CountDmg_Loop:
  LD A,(HL)               ; below '2' (under 20 damage)?
  CP $32
  JR NC,CountDmg_Next
  INC C                   ; yes: +1
CountDmg_Next:
  INC HL
  INC HL
  DJNZ CountDmg_Loop
  LD A,(RatingScore)      ; RatingScore += the count
  ADD A,C
  LD (RatingScore),A
  RET

; OxygenScore — the oxygen term of the Competence Rating
;
; RatingScore += 2 x the oxygen counter's leading digit ('7' at the start of a
; game, so up to 14 points for a quick finish).
;
; Used by the routine at EndgameScreen.
OxygenScore:
  LD A,(OxygenHi)         ; the oxygen counter's first digit
  SUB $30
  SLA A                   ; x 2
  LD C,A
  LD A,(RatingScore)      ; RatingScore += it
  ADD A,C
  LD (RatingScore),A
  RET

; ShortGameInit
;
; Game-mode handler for "1: Short Game" (dispatched from
; GameModeDispatchTable).
;
; The Short Game is the abbreviated scenario. Several slots in the starting
; template (ShortGameCrewInit ShortGameCrewInit) are pre-stamped with the
; HostMarker pattern ($FF on bytes +1 and +7) and the alien's first target is
; hard-wired to crew ID 9 — a value the alien-targeting guard at
; TriggerAlien_TargetCheck treats as "no valid target", so the alien's
; stalk-the-host logic is effectively disabled. No script-driven randomisation
; runs. Every Short Game opens from exactly the same configuration, which is
; what makes it shorter and more deterministic than the Long Game.
;
; Steps: 1. Clear the two run-state bytes at MsgDrawnCol / MsgVisibleTimer. 2.
; Point the message queue head (MsgQueueWritePtr MsgQueueWritePtr) at the queue
; base MessageQueue (MessageQueue). 3. Hard-wire AlienTargetID (AlienTargetID)
; = 9 — disables alien hunt logic. 4. LDIR 64 bytes from ShortGameCrewInit
; (ShortGameCrewInit) into $737E to populate the alien slot and the seven crew
; slots with the short-game starting state (four slots wear the HostMarker so
; they're skipped by normal crew processing, leaving the alien header plus
; three active crew). 5. JP into the shared common init at CommonGameInit
; (CommonGameInit) — skipping the Long Game's script-driven host selection and
; target picking.
;
; Used by the dispatcher at GameModeJump via the table at
; GameModeDispatchTable.
ShortGameInit:
  XOR A                   ; A = 0
  LD (MsgDrawnCol),A      ; clear run-state byte MsgDrawnCol
  LD (MsgVisibleTimer),A  ; clear run-state byte MsgVisibleTimer
  LD HL,MessageQueue      ; HL = MessageQueue (event queue base)
  LD (MsgQueueWritePtr),HL ; event queue head = queue base (empty queue)
ShortGame_SetTarget:
  LD A,$09                ; A = 9 (fixed Short Game alien target)
  LD (AlienTargetID),A    ; AlienTargetID = 9 — no randomisation
  LD HL,ShortGameCrewInit ; HL = ShortGameCrewInit ($A224)
  LD DE,ActorRecords      ; DE = $737E (alien slot + crew table base)
  LD BC,$0040             ; 64 bytes = 8 × 8-byte records
  LDIR                    ; copy short-game starting crew/alien state
ShortGameInit_Done:
  JP CommonGameInit       ; → CommonGameInit (skip Long Game randomisation)

; Pause-menu texts
;
; Plain ASCII (previously misclassified as code): the two lines PauseMenu
; (PauseMenu) draws.
StrPress1NewGame:
  DEFM "PRESS 1 FOR NEW GAME"
StrAnyKeyContinue:
  DEFM "ANY OTHER KEY TO CONTINUE"

; PauseMenu
;
; Pause screen overlay shown when the player presses key 1 during gameplay.
; Draws two lines of text (pause message and restart prompt) over the game
; screen using the tile-blit helper at PrintStr. Sets the border to yellow (6)
; and waits for the player to press key 1 again. If key 1 is pressed, jumps
; directly to GameEntry (GameEntry) to restart the game. Any other key resumes
; play by returning normally. If the corridor view was active (mode=1), calls
; RedrawRoomScene to restore the room display before returning.
;
; Used by the routine at GameEntry.
PauseMenu:
  CALL ClearTextWindow    ; draw game-window border
  LD HL,$5086             ; HL = $5086 (mid-screen attribute row)
  LD (DrawScreenPtr),HL   ; set draw pointer
  LD B,$14                ; 20-character message line
; This entry point is used by the routine at ScreenTransition.
  LD DE,StrPress1NewGame  ; DE = text data for pause message line 1
  CALL PrintStr           ; blit text
  LD HL,$50C3             ; next screen row
  LD (DrawScreenPtr),HL
  LD B,$19                ; 25-character second line
  LD DE,StrAnyKeyContinue ; text data for "PRESS 1 TO RESTART" line
  CALL PrintStr           ; blit text
  CALL WaitKeyRelease     ; short wait (debounce key release)
; This entry point is used by the routine at ScreenTransition.
  LD A,$06                ; 6 = yellow border
  OUT ($FE),A             ; flash border yellow (indicates pause)
  CALL WaitKeyPress       ; wait loop (keyboard debounce / delay)
  LD BC,$F7FE             ; BC = $F7FE: keyboard row for keys 1-5
  IN A,(C)                ; read keyboard
; This entry point is used by the routine at ScreenTransition.
  RRA                     ; bit 0 → carry (key 1 = active-low)
  JP NC,GameEntry         ; key 1 held: restart game from entry
  XOR A                   ; A = 0
  OUT ($FE),A             ; restore border to black
  CALL ClearTextWindow    ; redraw game-window frame
  LD A,(RoomModeByte)     ; A = room mode (RoomModeByte)
; This entry point is used by the routine at ScreenTransition.
  CP $01                  ; mode 1 = corridor view active
; This entry point is used by the routine at ScreenTransition.
  CALL Z,RedrawRoomScene  ; if in corridor: redraw room contents
  RET                     ; resume game loop

; PlayMusic
;
; Plays a background music phrase through the ZX Spectrum beeper. Reads the
; music-active flag at MusicActiveFlag. If zero, falls through to the standard
; frame- timing wait at BusyWait and returns. Otherwise plays a descending
; sequence of tones: reads 32 consecutive ROM bytes from $0200 and for each
; byte toggles bit 4 of port $FE (beeper output), with a delay of C counts
; between toggles. After each 32-byte block, decrements C by 8 to raise the
; pitch, producing a descending arpeggio effect. Runs until C underflows (B=255
; exhausted), then returns.
;
; Used by the routines at GameEntry and IntroductionMode.
PlayMusic:
  LD A,(SoundPending)     ; A = sound-pending flag (queued by e.g.
  AND A                   ; CrewAction3_RemoveGrille); anything queued?
  JP Z,BusyWait           ; no: standard frame-timing wait and return
  LD HL,$0200             ; HL = $0200 (ROM data used as tone source)
  LD BC,$0880             ; B=8 (8 pitch steps), C=$88 (initial delay = 136)
Music_PitchLoop:
  PUSH BC                 ; save pitch counter
  LD B,$20                ; 32 bytes (ROM data) per pitch step
Music_ByteLoop:
  LD A,(HL)               ; read ROM byte
  AND $10                 ; isolate bit 4 (beeper toggle bit)
  OUT ($FE),A             ; output to port $FE (beeper + border)
  INC HL                  ; advance ROM pointer
  LD A,C                  ; A = current delay count
Music_Delay:
  DEC A                   ; count down delay
  JR NZ,Music_Delay       ; busy-wait
  DJNZ Music_ByteLoop     ; next ROM byte in this pitch step
  LD B,$FF                ; inter-note gap: 255 NOP-equivalent iterations
Music_Gap:
  DJNZ Music_Gap          ; wait
  POP BC                  ; restore pitch counter
  LD A,C                  ; A = current delay value
  SUB $08                 ; reduce delay → raise pitch
  LD C,A                  ; store updated delay
  DJNZ Music_PitchLoop
  XOR A
  LD (SoundPending),A
  RET

; PostCombatReset
;
; After a combat dispatch: park the actor's action state and reload the hand
; cache for the viewed crew member.
;
; Used by the routine at CrewAction4_Handler.
PostCombatReset:
  LD (IX+$03),$00         ; action state = 0 (parked)
  LD HL,$FFFF             ; clear the hand cache
  LD (HeldItemFront),HL
  CALL RefreshHeldItems   ; and reload it for the viewed slot
  RET

; DestroyHeldItem
;
; Entered from DestroyFrontItem (DestroyFrontItem) with HL -> the front-hand
; item's ItemLocations entry and B = the back-hand item: wipe the item out of
; existence and promote the back-hand item to the front hand — with a full
; hand-row redraw (UpdateHandRows UpdateHandRows) when the owner is the crew
; member on screen, marker/courage bookkeeping only (GetItemLocation
; GetItemLocation) when he is not.
DestroyHeldItem:
  LD A,$FF                ; ItemLocations[front item] = 255:
  LD (HL),A               ; the item ceases to exist
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  JR NZ,DestroyItem_BackHand
  PUSH HL
  LD A,(CrewIndex)        ; is the owner the viewed crew member?
  LD HL,DrawSlotIndex
  CP (HL)
  POP HL
  JR NZ,DestroyItem_BackHand
  LD A,$FF                ; yes: redraw the hand rows too
  JP UpdateHandRows
DestroyItem_BackHand:
  LD A,$FF                ; off screen: anything in the back hand?
  CP B
  LD A,(HeldItemBack)
  JP NZ,GetItemLocation   ; yes: promote it to the front hand
  RET
; This entry point is used by the routine at UpdateHandRows.
HandRows_Finish:
  CALL DrawSprite         ; draw the (blank) front row,
  LD A,$FF                ; blank the "Leave Item" row
  LD ($73C9),A            ; (cell 11 — nothing left to leave)
  CALL DrawSelectedName   ; and repaint the menu strip
  RET

; ClearHandCache
;
; Reset HeldItemFront/Back to 255/255 and return A = the current actor's slot —
; the input FindHeldItems (FindHeldItems) wants.
;
; Used by the routine at CrewHitsAlien.
ClearHandCache:
  LD HL,$FFFF
  LD (HeldItemFront),HL
  LD A,(CrewIndex)
  RET

; AttackRowGate
;
; Special-row draw hook (DrawRoomSpecials DrawRoomSpecials) when the alien is
; in the viewed room: while the viewed crew member's own ATTACK is still
; counting down (state 4, timer running), leave the row alone; otherwise force
; the "  ATTACK  " cell.
;
; Used by the routine at DrawRoomSpecials.
AttackRowGate:
  LD A,(IX+$03)           ; mid-attack (state 4, timer still
  CP $04                  ; running)?
  JR NZ,AttackRow_Force
  LD A,(IX+$00)
  OR A
  JP NZ,DrawSpec_AlienDone ; yes: draw the row as it stands
AttackRow_Force:
  CALL CursorGrilleToAttack ; no: swap a live "RmveGrille" pick too,
  LD DE,$73CB             ; and force the Special cell (13)
  JP DrawSpec_AlienAttackRow ; to "  ATTACK  "

; AlienInfoCheck
;
; Room-info hook (RefreshRoomInfo RefreshRoomInfo) when the alien shares the
; viewed room: if the viewed crew member is between attacks, offer ATTACK;
; while his attack countdown is still running, switch the view to the
; alien-encounter screen (room type 4) with both animation channels frozen.
;
; Used by the routine at RefreshRoomInfo.
AlienInfoCheck:
  PUSH IX
  PUSH DE
  LD A,(DrawSlotIndex)    ; IX -> the viewed slot's record
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD IX,ActorRecords
  ADD IX,DE
  LD A,(IX+$03)           ; mid-attack (state 4, timer still
  CP $04                  ; running)?
  JR NZ,AlienInfo_Attack
  LD A,(IX+$00)
  OR A
  JR NZ,AlienInfo_Combat
AlienInfo_Attack:
  POP DE                  ; not attacking yet:
  POP IX
  CALL CursorGrilleToAttack ; swap a live "RmveGrille" pick to ATTACK
  CALL ResetAlienXAnim    ; reset the encounter animation
  JP RefreshInfo_Encounter ; and continue the info refresh
AlienInfo_Combat:
  POP DE                  ; attack in progress:
  POP IX
  CALL ResetAlienXAnim    ; reset the encounter animation,
  LD HL,DrawMenuStrip     ; freeze both animation channels
  LD (CrewAnimFlags),HL   ; ($8080 -> CrewAnimFlags+AnimFramePhase)
  LD A,$04                ; and view type 4:
  LD (RoomTypeByte),A     ; the alien-encounter screen
  RET

; AndroidInfoCheck
;
; Room-info hook (RefreshRoomInfo RefreshRoomInfo) when the Android is in the
; viewed room (caller's Z flag): offer ATTACK unless the viewed crew member's
; own attack countdown is still running.
;
; Used by the routine at RefreshRoomInfo.
AndroidInfoCheck:
  RET NZ                  ; not the Android's room: done
  PUSH IX
  PUSH DE
  LD A,(DrawSlotIndex)    ; IX -> the viewed slot's record
  ADD A,A
  ADD A,A
  ADD A,A
  LD E,A
  LD D,$00
  LD IX,ActorRecords
  ADD IX,DE
  LD A,(IX+$03)           ; in combat (state 4)?
  CP $04
  JR Z,AndroidInfo_Timer
  POP DE                  ; no: offer ATTACK
  POP IX
  JR AndroidInfo_Attack
AndroidInfo_Timer:
  LD A,(IX+$00)           ; countdown still running?
  OR A
  POP DE
  POP IX
  RET NZ                  ; yes: leave the view alone
AndroidInfo_Attack:
  CALL CursorGrilleToAttack ; swap a live "RmveGrille" pick to ATTACK
  JP RefreshInfo_AttackRow ; and continue the info refresh

; AndroidRowGate
;
; Special-row draw hook (DrawRoomSpecials DrawRoomSpecials) when the Android is
; in the viewed room — the Android twin of AttackRowGate.
;
; Used by the routine at DrawRoomSpecials.
AndroidRowGate:
  LD A,(IX+$03)           ; mid-attack (state 4, timer still
  CP $04                  ; running)?
  JR NZ,AndroidRow_Force
  LD A,(IX+$00)
  OR A
  JP NZ,DrawSpec_AndroidDone ; yes: draw the row as it stands
AndroidRow_Force:
  CALL CursorGrilleToAttack ; no: swap a live "RmveGrille" pick too,
  LD DE,$73CB             ; and force the Special cell (13)
  JP DrawSpec_AndroidAttackRow ; to "  ATTACK  "

; RefreshGrilleRow
;
; Used by the routine at CrewHitsAndroid.
RefreshGrilleRow:
  LD A,(RoomModeByte)
  CP $01
  JR NZ,GrilleRow_Done
  LD HL,(AlienTargetPtr)
  INC HL
  LD A,(CurrentRoom)
  CP (HL)
  JR NZ,GrilleRow_Done
  LD HL,$737F
  CP (HL)
  JR Z,GrilleRow_Done
  AND $3F                 ; refresh the menu's Special slot from
  LD E,A                  ; RoomGrilleState[viewed room]:
  LD HL,RoomGrilleState
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  CP $FF
  JR Z,GrilleRow_Offer
  LD A,$FF                ; grille removed: clear the Special slot
  LD ($73CB),A
  CALL DrawSelectedName
  JR GrilleRow_Done
GrilleRow_Offer:
  LD A,$31                ; grille in place: offer "RmveGrille"
  LD ($73CB),A
  CALL DrawSelectedName
GrilleRow_Done:
  LD HL,AlienActiveFlag
  RET

; ResetRatingDigits
;
; Used by the routine at PrintRating.
ResetRatingDigits:
  LD HL,$3020
  LD ($A450),HL
  LD L,$30
  RET

; RatingHundred
;
; Used by the routine at PrintRating.
RatingHundred:
  LD (HL),$30
  DEC HL
  LD (HL),$31
  RET

; ResetMsgAndScript
;
; Used by the routine at LongGameInit.
ResetMsgAndScript:
  LD HL,$FFFF
  LD (MsgLastEnqueued),HL
  CALL ResetScriptPtr
  RET

; ActionTimerFire
;
; Countdown trampoline: the caller decremented (IX+0); on the 1->0 transition
; (Z set) dispatch the actor's queued action.
;
; Used by the routine at ResetCrewTimers.
ActionTimerFire:
  PUSH IX
  CALL Z,DispatchCrewAction ; countdown just hit 0: DispatchCrewAction
  POP IX
  RET

; RefreshSpecialRow
;
; Used by the routine at StartAttack.
RefreshSpecialRow:
  LD A,(CurrentRoom)      ; refresh the menu's Special slot from
  AND $3F                 ; RoomGrilleState[viewed room]:
  LD E,A
  LD HL,RoomGrilleState
  LD D,$00
  ADD HL,DE
  LD A,(HL)
  CP $FF
  JR Z,RefreshSpecialRow_InPlace
  LD A,$FF                ; grille removed: clear the Special slot
  LD ($73CB),A
  RET
RefreshSpecialRow_InPlace:
  LD A,$31                ; grille in place: offer "RmveGrille"
  LD ($73CB),A
  RET

; SpecialRowAttack
;
; Used by the routines at CrewAction4_Handler and CrewHitsAndroid.
SpecialRowAttack:
  CALL CursorGrilleToAttack ; alien in the room: the Special slot
  LD A,$34                ; becomes "  ATTACK  " (52)
  LD ($73CB),A
  RET

; CursorGrilleToAttack
CursorGrilleToAttack:
  LD A,(CursorCellValue)  ; if the live selection was "RmveGrille"
  CP $31                  ; (49), swap it for "  ATTACK  " too
  RET NZ
  LD A,$34
  LD (CursorCellValue),A
  RET

; MaskToRomAddr — mask an address into ROM range
;
; Called by ResetScriptPtr (ResetScriptPtr): AND H with 31 so HL becomes an
; address in $0000-$1FFF, turning the SEED value into a ROM script start.
MaskToRomAddr:
  LD A,H
  AND $1F                 ; HL -> $0000-$1FFF (ROM)
  LD H,A
  RET

; PickupItemBoost
;
; Used by the routine at UpdateHandRows.
;
; Promote the item marker at (HL) from back hand to front hand (+32: 128+slot
; -> 160+slot) and add its ItemCourageBonus entry (E = item id) to the actor's
; courage (IX+5) and morale (IX+6).
PickupItemBoost:
  ADD A,$20               ; hand marker +32: back hand -> front hand
  LD (HL),A
  LD C,E
  LD B,$00
  LD HL,ItemCourageBonus  ; B = ItemCourageBonus[item]
  ADD HL,BC
  LD B,(HL)
  LD A,(IX+$05)           ; courage += bonus
  ADD A,B
  LD (IX+$05),A
  LD A,(IX+$06)           ; morale += bonus
  ADD A,B
  LD (IX+$06),A
  LD A,(RoomModeByte)
  CP $01
  RET NZ
  LD A,(CrewIndex)
  LD HL,DrawSlotIndex
  CP (HL)
  RET NZ
  CALL DrawCrewCondition
  RET

; RefreshCrewCondition
;
; Called after a non-viewed actor arrives somewhere: refresh the room info
; panel, then redraw the viewed crew member's condition line.
;
; Used by the routines at CrewAction1_Arrive and CrewAction2_DuctTransit.
RefreshCrewCondition:
  CALL RefreshRoomInfo    ; refresh the room info panel
  LD A,(CrewIndex)        ; the alien moved?
  CP $00
  RET Z                   ; then no condition line to touch
  LD A,(DrawSlotIndex)    ; re-point IX at the viewed slot
  LD B,A
  LD IX,ActorRecords
  LD DE,$0008
RefreshCrewCond_Seek:
  ADD IX,DE
  DJNZ RefreshCrewCond_Seek
  CALL DrawCrewCondition  ; and redraw his condition line
  RET

; IncPairAt
;
; Increment two adjacent bytes at HL+DE — in practice a crew record's courage
; (+5) and morale (+6) pair. Returns A = the first byte.
;
; Used by the routine at AlienScareCheck.
IncPairAt:
  ADD HL,DE               ; HL -> the slot's courage byte
  INC (HL)                ; courage += 1
  INC HL
  INC (HL)                ; morale += 1
  DEC HL
  LD A,(HL)               ; A = the new courage
  RET

; RefreshCondIfViewed
;
; In the room view, redraw the viewed crew member's condition line.
;
; Used by the routine at AlienScareCheck.
RefreshCondIfViewed:
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  JR NZ,RefreshCond_Done  ; no: skip the redraw
  LD A,(DrawSlotIndex)    ; re-point IX at the viewed slot
  LD B,A
  LD IX,ActorRecords
  LD DE,$0008
RefreshCond_Seek2:
  ADD IX,DE
  DJNZ RefreshCond_Seek2
  CALL DrawCrewCondition  ; and redraw his condition line
RefreshCond_Done:
  POP IX                  ; (balances AlienScareCheck's PUSH IX)
  RET

; UpdateAllCrew — pairwise companion morale support
;
; Called from GameEntry. Walks every pair of crew records (slots 1-7): whenever
; two crew share a room, each gains the other's courage minus 2 as a morale
; adjustment — company emboldens (or, with a Broken companion, depresses) the
; crew. The kill/redraw and courage/morale maintenance helpers used by the
; simulation follow as their own routines (KillActorRefresh KillActorRefresh,
; BumpCourageMorale BumpCourageMorale, KillActorMoraleHit KillActorMoraleHit,
; WitnessMoraleLoss WitnessMoraleLoss, PreDispatchHook PreDispatchHook...).
UpdateAllCrew:
  LD IX,CrewRecords       ; IX = slot 1 record
  LD C,$06                ; 6 later slots to pair it with
CrewPass_Next:
  LD H,(IX+$01)           ; H = this actor's room
  LD L,(IX+$05)           ; L = this actor's courage
  LD B,C
  PUSH IX
CrewPass_Scan:
  CALL NextSlotIX         ; step IX to the next slot
  LD A,H                  ; same room?
  CP (IX+$01)
  JR Z,CrewPass_MateBoost ; yes: apply the mutual boost
CrewPass_NoMate:
  DJNZ CrewPass_Scan
  POP IX
  DEC C                   ; advance to the next base slot
  CALL NextSlotIX
  LD A,IXl
  CP $B6                  ; stop after slot 7 ($73B6)
  JR NZ,CrewPass_Next
  CALL ClearMessageRow    ; blank the message line
  RET
  NOP
CrewPass_MateBoost:
  LD A,L                  ; mate's morale += this actor's courage - 2
  SUB $02
  ADD A,(IX+$06)
  LD (IX+$06),A
  POP DE                  ; and the other way round:
  PUSH DE                 ; DE -> this actor's morale byte (+6)
  INC E
  INC E
  INC E
  INC E
  INC E
  INC E
  LD A,(DE)               ; += the mate's courage - 2
  ADD A,(IX+$05)
  SUB $02
  LD (DE),A
  JR CrewPass_NoMate

; NextSlotIX
NextSlotIX:
  LD DE,$0008
  ADD IX,DE
  RET

; KillActorRefresh
;
; Kill the victim (KillActorMoraleHit) and, unless the victim is the crew
; member on screen, redraw the "Also here" room-mate lists.
;
; Used by the routine at VictimItemDrop.
KillActorRefresh:
  CALL KillActorMoraleHit ; mark dead + witness morale
KillActor_View:
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(DrawSlotIndex)    ; the victim IS the viewed slot?
  CP C
  RET Z                   ; yes: his own path rebuilds the view
KillActor_Redraw:
  PUSH BC
  PUSH DE
  PUSH HL
  PUSH IX
  LD HL,$FFFF             ; forget the cached room-mates and
  LD (RoomMateA),HL       ; redraw the "Also here" /
  CALL DrawRoomMates      ; "Also Bodies of" lists
  POP IX
  POP HL
  POP DE
  POP BC
  RET

; BumpCourageMorale
;
; Catching Jones comforts the catcher: +1 courage, +1 morale, redraw the
; condition line; returns HL -> JonesRoom for the caller to clear.
;
; Used by the routine at SetActionTimer.
BumpCourageMorale:
  LD A,(IX+$05)           ; courage += 1
  ADD A,$01
  LD (IX+$05),A
  LD A,(IX+$06)           ; morale += 1
  ADD A,$01
  LD (IX+$06),A
  CALL DrawCrewCondition  ; redraw the crew-condition line
  LD HL,JonesRoom         ; hand back HL -> JonesRoom
  RET

; SetCourageRefresh
;
; Set the actor's +5 byte to C, then refresh the room-mate display via
; KillActor_View. (For its one caller the "actor" is the ALIEN, whose +5 holds
; the slot of a dragged-off body — see Wound_PanicRoll — not courage.)
;
; Used by the routine at VictimItemDrop.
SetCourageRefresh:
  LD (IX+$05),C           ; courage = C
  JP KillActor_View       ; and refresh the room-mate lists

; DecayMoraleStep — tail of the DecayCrewCourage loop
;
; After the courage byte: drop the slot's morale too, step to the next record,
; and once all 7 are done redraw the viewed crew member's condition line.
;
; Used by the routine at DecayCrewCourage.
DecayMoraleStep:
  INC HL                  ; morale byte (+6): -1 too,
  DEC (HL)
  LD A,$7F
  CP (HL)                 ; clamping an underflow
  JR NC,DecayMorale_Next
  LD (HL),$00             ; to 0
DecayMorale_Next:
  DEC HL                  ; back to the courage byte,
  ADD HL,DE               ; next record
  DEC B
  JP NZ,DecayCourageLoop  ; loop over all 7 crew
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  PUSH BC
  PUSH IX
  LD A,(DrawSlotIndex)    ; re-point IX at the viewed slot
  LD B,A
  LD IX,ActorRecords
  LD DE,$0008
ViewedSlotSeek:
  ADD IX,DE
  DJNZ ViewedSlotSeek
  CALL DrawCrewCondition  ; and redraw his condition line
  POP IX
  POP BC
  RET

; KillActorMoraleHit — mark a victim dead and shake the witnesses
;
; Entered with HL -> the victim's morale byte (+6) and C = his slot: stamp
; status (+7) = 1 (dead), then give every live actor gathered in
; NearestCrewC/A/B a point of morale loss (WitnessMoraleLoss). If at least one
; witness survives, the death is seen at once: the body is marked found (+7 =
; 2), msg #20 "{witness} finds {victim}'s body" fires, and the whole crew's
; courage decays. With no witness the body stays at status 1 for a later
; discovery (MoraleFromCompanion MoraleFromCompanion).
KillActorMoraleHit:
  INC HL                  ; status (+7) = 1: dead
  LD (HL),$01
  PUSH BC
  PUSH IX
  PUSH HL
  LD H,$00                ; H = last live witness (0 = none)
  LD A,(NearestCrewC)     ; each gathered slot (C, A, B)
  CP $FF                  ; that exists loses morale
  CALL NZ,WitnessMoraleLoss ; as a witness
  LD A,(NearestCrewA)
  CP $FF
  CALL NZ,WitnessMoraleLoss
  LD A,(NearestCrewB)
  CP $FF
  CALL NZ,WitnessMoraleLoss
  LD A,H                  ; any live witness?
  CP $00
  JR Z,KillMorale_Done    ; no: the body lies undiscovered
  POP HL                  ; yes: the death is seen at once —
  LD (HL),$02             ; the body is already "found" (+7 = 2),
  PUSH HL
  LD L,A                  ; L = witness, H = victim:
  LD H,C
  LD A,$14                ; msg #20 "{actor} finds {target}'s body"
  CALL EnqueueMessage
  CALL DecayCrewCourage   ; and everyone's courage decays
KillMorale_Done:
  POP HL
  POP IX
  POP BC
  RET

; WitnessMoraleLoss
;
; A = a witness slot: if he is still alive, -1 morale (clamped at 0) and record
; his slot in H ("a live witness saw it").
WitnessMoraleLoss:
  LD B,A                  ; IX -> the witness's record
  LD IX,ActorRecords
  LD DE,$0008
WitnessMorale_Seek:
  ADD IX,DE
  DJNZ WitnessMorale_Seek
  LD B,A                  ; keep the slot in B
  LD A,(IX+$07)           ; still alive?
  OR A
  RET NZ                  ; no: the dead see nothing
  DEC (IX+$06)            ; morale -1,
  LD A,$7F
  CP (IX+$06)             ; clamped at 0
  JR NC,WitnessMorale_Keep
  LD (IX+$06),$00
WitnessMorale_Keep:
  LD H,B                  ; H = this live witness's slot
  RET

; KillActor_Bounce
;
; A = the viewed slot, (HL) = the departing actor's slot: full view rebuild
; when they match, room-mate list redraw when they don't.
;
; Used by the routine at CrewAction6_Handler.
KillActor_Bounce:
  CP (HL)                 ; is the departed the viewed slot?
  JP Z,InitGameView       ; yes: rebuild the whole view
  JP KillActor_Redraw     ; no: redraw the room-mate lists

; DuctSearchInit
;
; The tracker holder is in the ducts: switch the sweep to the duct adjacency
; map and strip the network flag from his room.
;
; Used by the routine at TrackerHolderRoom.
DuctSearchInit:
  LD BC,RoomAdjDucts      ; BC = RoomAdjDucts
  AND $3F                 ; A = plain room id
  LD (AdjTestRoom),A      ; update AdjTestRoom
  RET

; PrintNameThenPanel
;
; Used by the routine at DrawRoomMates.
PrintNameThenPanel:
  CALL PrintName
; This entry point is used by the routine at DrawCrewCondition.
PanelColCheck:
  LD HL,DrawScreenPtr
  LD A,(HL)
  AND $1F
  RET Z
  JP BlankRestOfRow

; RoomRejectCheck
;
; Tracker-sweep test for one adjacent room (in C, A = JonesRoom): Jones or the
; alien there = motion detected; otherwise scan the actors too.
;
; Used by the routine at TrackerHolderRoom.
RoomRejectCheck:
  CP C                    ; Jones in the adjacent room?
  JP Z,PickRoom_Reject    ; yes: motion detected
  LD A,($737F)            ; the alien in it?
  AND $3F
  CP C
  JP Z,PickRoom_Reject    ; yes: motion detected
  JP PickRoom_ActorScan   ; else check the actors

; AIActionSelect
;
; Entry shim for the alien's AI: spot 98 (= room 34 | 64, the ducts "over" the
; Narcissus) is an escape artifact — the Narcissus has no grille byte, so the
; alien can duct-flip into a network that does not connect anywhere. If it is
; stuck there, flip it straight back out; otherwise run the victim scan.
;
; Used by the routine at UpdateAlienAI.
AIActionSelect:
  LD A,(IX+$01)           ; stuck in the Narcissus "ducts" (98)?
  CP $62
  JP Z,AlienAI_DuctFlip   ; yes: climb straight back out
  JP AlienAI_FindVictims  ; no: scan for victims (A = its spot)

; AlienEventDispatch
;
; Store A into AlienActiveFlag (TriggerAlienEvent passes 255) and, if the
; Android happens to be the crew member on screen, rebuild the view.
;
; Used by the routine at TriggerAlienEvent.
AlienEventDispatch:
  LD (AlienActiveFlag),A  ; AlienActiveFlag = A
  LD A,(RoomModeByte)     ; room view on screen (mode 1)?
  CP $01
  RET NZ                  ; no: done
  LD A,(AlienTargetID)    ; is the Android the viewed
  LD D,A                  ; crew member?
  LD A,(DrawSlotIndex)
  CP D
  RET NZ                  ; no: done
  JP InitGameView         ; yes: rebuild the whole view

; PreDispatchHook
;
; Used by the routine at RoomModeDispatch.
;
; The hostile-in-room ATTACK-row swap and the 238 marker
;
; PreDispatchHook is called by RoomModeDispatch (RoomModeDispatch) at the start
; of EVERY action-key dispatch in room modes 0/1, before the row handler runs.
; In the room view it forces the special-action row (cell 13) to 52 "  ATTACK
; " whenever a hostile is in the viewed room. The subtlety is a press that was
; aimed at 49 "RmveGrille": the row is swapped to ATTACK by this very hook,
; inside the same key release that dispatches the row — so the hook leaves the
; value 238 in CursorCellValue ($8395) as a marker meaning "this press was made
; while the row still read RmveGrille". The special-row shim at $B306 then
; honours the player's original intent: it restores the mirror to 52 (matching
; the now-displayed ATTACK) but still starts the timed grille removal. Only the
; NEXT press, taken with the row visibly reading ATTACK, goes down the attack
; path.
;
; (Verified in the simulator: press 1 with the alien in the room -> mirror
; 49->238->52, cell 13 -> 52, actor gets action state 3 with its full grille
; timer; press 2 -> attack handler StartAttack, action state 4 timer 40,
; replacing the pending grille job.)
PreDispatchHook:
  CALL ForceAttackRowIfHostile ; run the hostile-row check, then
  LD A,(RoomModeByte)     ; return the room mode to the dispatcher
  RET

; ForceAttackRowIfHostile
;
; Room view only, and never on the QUIT row. A press on row 8 (the front-hand
; item) zeroes the actor's action countdown (+0), cancelling any pending timed
; job — the countdown only fires its action when it decrements to zero in
; ResetCrewTimers (ResetCrewTimers). Then, if the alien (slot 0) is in the
; viewed actor's room — or, once the alien is active (flag $FF), the
; alien-target / Android slot (pointer at $83A5) is — swap the special row to
; ATTACK.
ForceAttackRowIfHostile:
  LD A,(RoomModeByte)     ; room view (mode 1)?
  CP $01
  RET NZ                  ; no: nothing to do
  LD A,(CorridorCursor)
  CP $11                  ; cursor on the QUIT row?
  RET Z                   ; yes: leave everything alone
  CP $08                  ; cursor on the front-hand item row?
  JR NZ,ForceAttack_RoomCheck
  LD (IX+$00),$00         ; yes: cancel the actor's pending countdown
ForceAttack_RoomCheck:
  LD A,($737F)            ; alien (slot 0) in the viewed actor's room?
  CP (IX+$01)             ; (raw compare, so the duct bit must match too)
  JR Z,ForceAttack_HostilePresent ; yes: hostile present
  LD A,(AlienActiveFlag)  ; alien active ($FF, set by TriggerAlienEvent)?
  CP $FF
  RET NZ                  ; no: done
  LD HL,(AlienTargetPtr)  ; the alien-target / Android record:
  INC HL
  LD A,(HL)               ; its current room (+1)
  CP (IX+$01)             ; in the viewed actor's room?
  RET NZ                  ; no: done
ForceAttack_HostilePresent:
  LD A,(CursorCellValue)  ; is the press landing on 49 "RmveGrille"?
  CP $31
  JR NZ,ForceAttack_ShowAttack
  LD A,$EE                ; yes: mark the mirror with 238 so the
  LD (CursorCellValue),A  ; special-row shim honours the grille press
ForceAttack_ShowAttack:
  LD A,$34                ; special row (cell 13) = 52 "  ATTACK  "
  LD ($73CB),A
  LD HL,$48D6             ; and redraw it (screen row 14, column 22)
  LD (DrawScreenPtr),HL
  CALL DrawSprite
  RET

; SpecialRowAction
;
; Used by the routine at SpecialRowJump.
;
; The special-row (13) handler, reached via the JP at $8BCB. 238 -> the press
; was aimed at RmveGrille before the swap above: restore the visible value 52
; and start the timed grille removal anyway. Anything else goes to the normal
; path at $8BCE (52 -> attack, 49 -> timed grille removal).
SpecialRowAction:
  LD A,(CursorCellValue)  ; A = value the press landed on
  CP $EE                  ; swapped-under-the-cursor marker?
  JP NZ,SpecialRowNormal  ; no: normal ATTACK / RmveGrille handling
  LD A,$34                ; yes: mirror = 52 to match the displayed
  LD (CursorCellValue),A  ; ATTACK row,
  JP StartGrilleRemoval   ; but still start the timed grille removal

; CancelActionTimer
;
; Used by the routine at FixtureAction.
;
; Zero the actor's action countdown (+0); a countdown parked at 0 never fires
; (ResetCrewTimers only dispatches on the 1 -> 0 transition).
CancelActionTimer:
  LD A,(CorridorCursor)
  LD (IX+$00),$00
  RET

; Zero-padding between game code and extra-data area
;
; 14146 bytes of zeros bridging the last byte of game code and the start of
; AlienFrame0 (AlienFrame0). Reserved space in the runtime image; no code or
; data lives here. Note: a 16-bit constant inside this gap is used elsewhere as
; a Z80 I/O port number for the keyboard half-row B/N/M/SymShift/Space, not as
; a memory reference.
  DEFS $3742

; AlienFrame0
;
; Alien body frame 0 (phase 0).
AlienFrame0:
  DEFB $00,$00,$00,$0F,$80,$00
  DEFB $00,$00,$00,$3F,$C0,$00
  DEFB $00,$00,$00,$7F,$E0,$00
  DEFB $02,$00,$38,$FF,$E0,$00
  DEFB $04,$00,$7D,$FF,$60,$00
  DEFB $0F,$FF,$E9,$FD,$E0,$00
  DEFB $0F,$FF,$63,$FB,$DC,$00
  DEFB $0F,$FF,$63,$F7,$3E,$00
  DEFB $05,$FF,$63,$FE,$3E,$00
  DEFB $06,$07,$67,$F8,$76,$00
  DEFB $07,$80,$2F,$BE,$62,$00
  DEFB $07,$00,$1F,$D2,$EC,$00
  DEFB $07,$80,$1F,$D1,$DE,$00
  DEFB $07,$00,$1F,$D3,$B9,$00
  DEFB $07,$80,$1F,$A7,$77,$00
  DEFB $07,$00,$0F,$4E,$EF,$80
  DEFB $07,$80,$10,$CC,$1F,$80
  DEFB $07,$78,$0F,$5C,$6F,$C0
  DEFB $07,$7E,$00,$97,$C7,$E0
  DEFB $07,$BF,$8F,$78,$43,$E0
  DEFB $03,$DF,$EE,$EF,$C3,$F4
  DEFB $02,$EF,$F8,$30,$81,$F4
  DEFB $02,$B7,$F5,$EF,$00,$F8
  DEFB $03,$97,$FB,$30,$80,$70
  DEFB $03,$A7,$FC,$9F,$01,$F0
  DEFB $03,$BF,$FC,$C0,$1C,$E0
  DEFB $01,$5D,$FF,$2D,$FB,$E0
  DEFB $01,$5E,$FF,$F3,$F9,$C0
  DEFB $00,$AE,$7F,$FF,$F7,$D0
  DEFB $00,$1E,$3F,$FF,$F7,$B0
  DEFB $00,$3E,$0F,$FF,$E7,$70
  DEFB $00,$3E,$01,$FF,$DE,$F0
  DEFB $00,$3E,$18,$3E,$06,$F0
  DEFB $00,$3E,$38,$00,$35,$F0
  DEFB $00,$1E,$F7,$00,$F9,$F0
  DEFB $00,$1E,$CC,$01,$D9,$E0
  DEFB $00,$1E,$34,$02,$B3,$E0
  DEFB $00,$5E,$D0,$02,$F3,$C0
  DEFB $00,$DE,$40,$01,$53,$C0
  DEFB $01,$C1,$00,$01,$57,$C0
  DEFB $03,$AF,$00,$00,$A7,$C0
  DEFB $0E,$4F,$00,$00,$47,$C0
  DEFB $0D,$8A,$80,$00,$07,$C0
  DEFB $1A,$0B,$80,$00,$03,$80
  DEFB $1B,$0B,$80,$00,$04,$80
  DEFB $34,$0F,$00,$00,$0F,$00
  DEFB $36,$1F,$00,$00,$0F,$00
  DEFB $3C,$1B,$00,$00,$05,$00
  DEFB $34,$36,$00,$00,$0D,$00
  DEFB $38,$7E,$00,$00,$0F,$80
  DEFB $3C,$D6,$00,$00,$05,$C0
  DEFB $38,$B4,$00,$00,$06,$E0
  DEFB $3E,$00,$00,$00,$07,$E0
  DEFB $1C,$00,$00,$00,$06,$A0
  DEFB $1F,$00,$00,$00,$02,$C0
  DEFB $0F,$C0,$00,$00,$00,$00
  DEFB $07,$F8,$00,$00,$00,$00
  DEFB $03,$FE,$00,$00,$00,$00
  DEFB $00,$7F,$80,$00,$00,$00
  DEFB $00,$03,$80,$00,$00,$00
  DEFB $00,$01,$C0,$00,$00,$00
  DEFB $00,$00,$C0,$00,$00,$00
  DEFB $00,$00,$C0,$00,$00,$00

; AlienFrame1
;
; Alien body frame 1 (phase 1).
AlienFrame1:
  DEFB $00,$00,$00,$3E,$00,$00
  DEFB $00,$00,$00,$7F,$00,$00
  DEFB $00,$00,$00,$FF,$80,$00
  DEFB $02,$00,$39,$FE,$80,$00
  DEFB $04,$00,$7D,$FF,$80,$00
  DEFB $0F,$FF,$EB,$FB,$80,$00
  DEFB $0F,$FF,$63,$F7,$1C,$00
  DEFB $0F,$FF,$67,$F4,$3E,$00
  DEFB $05,$FF,$67,$F8,$3E,$00
  DEFB $06,$07,$67,$B0,$76,$00
  DEFB $07,$80,$37,$DC,$62,$00
  DEFB $07,$00,$2F,$F2,$EC,$00
  DEFB $07,$80,$0F,$E9,$DE,$00
  DEFB $07,$00,$0F,$EB,$B9,$00
  DEFB $07,$80,$0F,$D7,$77,$00
  DEFB $07,$00,$07,$AE,$EF,$80
  DEFB $07,$80,$08,$6C,$1F,$80
  DEFB $07,$78,$07,$AC,$6F,$C0
  DEFB $07,$7E,$00,$57,$C7,$E0
  DEFB $07,$BF,$87,$B8,$43,$E0
  DEFB $03,$DF,$E7,$6F,$C3,$F4
  DEFB $02,$EF,$F8,$B0,$81,$F4
  DEFB $02,$B7,$F5,$EF,$00,$F8
  DEFB $03,$97,$FB,$30,$80,$70
  DEFB $03,$A7,$FC,$9F,$01,$F0
  DEFB $03,$BF,$FC,$C0,$1C,$E0
  DEFB $01,$5D,$FF,$2D,$FB,$E0
  DEFB $01,$5E,$FF,$2D,$FB,$E0
  DEFB $00,$AE,$7F,$F3,$F9,$C0
  DEFB $00,$1E,$3F,$FF,$F7,$D0
  DEFB $00,$3E,$0F,$FF,$F7,$B0
  DEFB $00,$3E,$01,$FF,$E7,$70
  DEFB $00,$3E,$18,$FF,$DE,$F0
  DEFB $00,$3E,$38,$3E,$06,$F0
  DEFB $00,$1E,$F7,$00,$35,$F0
  DEFB $00,$1E,$CC,$00,$F9,$F0
  DEFB $00,$1E,$34,$03,$59,$E0
  DEFB $00,$5E,$D0,$05,$B3,$E0
  DEFB $00,$DE,$40,$02,$E3,$C0
  DEFB $01,$C1,$00,$05,$63,$C0
  DEFB $03,$AF,$00,$0A,$A7,$C0
  DEFB $0E,$4F,$00,$05,$27,$C0
  DEFB $0D,$8A,$80,$00,$07,$C0
  DEFB $1A,$0B,$80,$00,$03,$80
  DEFB $1B,$0B,$80,$00,$04,$80
  DEFB $34,$0F,$00,$00,$0F,$00
  DEFB $36,$1F,$00,$00,$0F,$00
  DEFB $3C,$1B,$00,$00,$05,$00
  DEFB $34,$36,$00,$00,$0D,$00
  DEFB $38,$7E,$00,$00,$0F,$80
  DEFB $3C,$D6,$00,$00,$05,$C0
  DEFB $38,$B4,$00,$00,$06,$E0
  DEFB $3E,$00,$00,$00,$07,$E0
  DEFB $1C,$00,$00,$00,$06,$A0
  DEFB $1F,$00,$00,$00,$02,$C0
  DEFB $0F,$C0,$00,$00,$00,$00
  DEFB $07,$F8,$00,$00,$00,$00
  DEFB $03,$FF,$80,$00,$00,$00
  DEFB $00,$7F,$E0,$00,$00,$00
  DEFB $00,$01,$F0,$00,$00,$00
  DEFB $00,$00,$38,$00,$00,$00
  DEFB $00,$00,$18,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00

; AlienFrame2
;
; Alien body frame 2 (phase 2).
AlienFrame2:
  DEFB $00,$00,$00,$7C,$00,$00
  DEFB $00,$00,$01,$FE,$00,$00
  DEFB $00,$00,$03,$FF,$00,$00
  DEFB $02,$00,$3B,$FD,$00,$00
  DEFB $04,$00,$7B,$FB,$00,$00
  DEFB $0F,$FF,$E7,$F7,$00,$00
  DEFB $0F,$FF,$67,$F6,$1C,$00
  DEFB $0F,$FF,$67,$F8,$3E,$00
  DEFB $05,$FF,$67,$F0,$3E,$00
  DEFB $06,$07,$67,$F0,$76,$00
  DEFB $07,$80,$37,$DC,$62,$00
  DEFB $07,$00,$2F,$E2,$EC,$00
  DEFB $07,$80,$0F,$F1,$DE,$00
  DEFB $07,$00,$0F,$F3,$B9,$00
  DEFB $07,$80,$0F,$E7,$77,$00
  DEFB $07,$00,$07,$D6,$EF,$80
  DEFB $07,$80,$08,$34,$1F,$80
  DEFB $07,$78,$07,$EC,$6F,$C0
  DEFB $07,$7E,$03,$A7,$C7,$E0
  DEFB $07,$BF,$84,$58,$43,$E0
  DEFB $03,$DF,$E3,$AF,$C3,$F4
  DEFB $02,$EF,$FB,$B0,$81,$F4
  DEFB $02,$B7,$F4,$6F,$00,$F8
  DEFB $03,$97,$FB,$30,$80,$70
  DEFB $03,$A7,$FC,$9F,$01,$F0
  DEFB $03,$BF,$FC,$C0,$1C,$E0
  DEFB $01,$5D,$FF,$2D,$FB,$E0
  DEFB $01,$5E,$FF,$F3,$F9,$C0
  DEFB $00,$AE,$7F,$FF,$F7,$D0
  DEFB $00,$1E,$3F,$FF,$F7,$B0
  DEFB $00,$3E,$0F,$FF,$EF,$70
  DEFB $00,$3E,$01,$FF,$96,$F0
  DEFB $00,$3E,$18,$3E,$79,$F0
  DEFB $00,$3E,$38,$01,$F9,$F0
  DEFB $00,$1E,$F7,$06,$B1,$F0
  DEFB $00,$1E,$CC,$0B,$61,$E0
  DEFB $00,$1E,$34,$05,$C3,$E0
  DEFB $00,$5E,$D0,$0A,$C3,$C0
  DEFB $00,$DE,$40,$15,$43,$C0
  DEFB $01,$C1,$00,$0A,$47,$C0
  DEFB $03,$AF,$00,$00,$07,$C0
  DEFB $0E,$4F,$00,$00,$07,$C0
  DEFB $0D,$8A,$80,$00,$07,$C0
  DEFB $1A,$0B,$80,$00,$03,$80
  DEFB $1B,$0B,$80,$00,$04,$80
  DEFB $34,$0F,$00,$00,$0F,$00
  DEFB $36,$1F,$00,$00,$0F,$00
  DEFB $3C,$1B,$00,$00,$05,$00
  DEFB $34,$36,$00,$00,$0D,$00
  DEFB $38,$7E,$00,$00,$0F,$80
  DEFB $3C,$D6,$00,$00,$05,$C0
  DEFB $38,$B4,$00,$00,$06,$E0
  DEFB $3E,$00,$00,$00,$07,$E0
  DEFB $1C,$00,$00,$00,$06,$A0
  DEFB $1F,$00,$08,$00,$02,$C0
  DEFB $0F,$80,$18,$00,$00,$00
  DEFB $07,$F8,$F8,$00,$00,$00
  DEFB $03,$FF,$F0,$00,$00,$00
  DEFB $00,$FF,$C0,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00

; AlienFrame3
;
; Alien body frame 3 (phase 3).
AlienFrame3:
  DEFB $00,$00,$00,$7C,$00,$00
  DEFB $00,$00,$01,$FE,$00,$00
  DEFB $00,$00,$03,$FF,$00,$00
  DEFB $02,$00,$3B,$FF,$00,$00
  DEFB $04,$00,$7B,$FF,$00,$00
  DEFB $0F,$FF,$E7,$F9,$00,$00
  DEFB $0F,$FF,$67,$F6,$1C,$00
  DEFB $0F,$FF,$67,$F4,$3E,$00
  DEFB $05,$FF,$67,$F8,$3E,$00
  DEFB $06,$07,$67,$90,$76,$00
  DEFB $07,$80,$2F,$EC,$62,$00
  DEFB $07,$00,$2F,$F2,$EC,$00
  DEFB $07,$80,$0F,$F1,$DE,$00
  DEFB $07,$00,$0F,$E3,$B9,$00
  DEFB $07,$80,$07,$D7,$77,$00
  DEFB $07,$00,$08,$16,$EF,$80
  DEFB $07,$80,$07,$EC,$1F,$80
  DEFB $07,$78,$03,$CC,$6F,$C0
  DEFB $07,$7E,$04,$27,$C7,$E0
  DEFB $07,$BF,$85,$A8,$43,$E0
  DEFB $03,$DF,$E5,$AF,$C3,$F4
  DEFB $02,$EF,$FA,$50,$81,$F4
  DEFB $02,$B7,$F3,$CF,$00,$F8
  DEFB $03,$97,$F8,$30,$80,$70
  DEFB $03,$A7,$FF,$9F,$01,$F0
  DEFB $03,$BF,$FC,$C0,$18,$E0
  DEFB $01,$5D,$FF,$2D,$F7,$C0
  DEFB $01,$5E,$FF,$F3,$F3,$A0
  DEFB $00,$AE,$7F,$FF,$EF,$70
  DEFB $00,$1E,$3F,$FF,$CE,$F0
  DEFB $00,$3E,$0F,$FF,$1D,$F0
  DEFB $00,$3E,$01,$FC,$CB,$F0
  DEFB $00,$3E,$18,$23,$E0,$F0
  DEFB $00,$3E,$38,$1D,$61,$F0
  DEFB $00,$1E,$F7,$06,$C1,$F0
  DEFB $00,$1E,$CC,$1B,$C1,$E0
  DEFB $00,$1E,$34,$25,$43,$E0
  DEFB $00,$5E,$D0,$09,$43,$C0
  DEFB $00,$DE,$40,$12,$43,$C0
  DEFB $01,$C1,$00,$02,$07,$C0
  DEFB $03,$AF,$00,$00,$07,$C0
  DEFB $0E,$4F,$00,$00,$07,$C0
  DEFB $0D,$8A,$80,$00,$07,$C0
  DEFB $1A,$0B,$80,$00,$03,$80
  DEFB $1B,$0B,$80,$00,$04,$80
  DEFB $34,$0F,$00,$00,$0F,$00
  DEFB $36,$1F,$00,$00,$0F,$00
  DEFB $3C,$1B,$00,$00,$05,$00
  DEFB $34,$36,$00,$00,$0D,$00
  DEFB $38,$7E,$10,$00,$0F,$80
  DEFB $3C,$D6,$30,$00,$05,$C0
  DEFB $38,$B4,$30,$00,$06,$E0
  DEFB $3E,$00,$60,$00,$07,$E0
  DEFB $1E,$00,$E0,$00,$06,$A0
  DEFB $1F,$83,$C0,$00,$02,$C0
  DEFB $0F,$FF,$80,$00,$00,$00
  DEFB $07,$FF,$00,$00,$00,$00
  DEFB $01,$FC,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00

; JonesFrame0
;
; Jones the cat frame 0 (phase 0). 21 rows x 3 bytes, XOR-blitted.
JonesFrame0:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $30,$00,$00
  DEFB $18,$00,$00
  DEFB $0C,$00,$00
  DEFB $06,$00,$40
  DEFB $03,$E0,$E0
  DEFB $03,$FF,$F0
  DEFB $03,$FF,$F0
  DEFB $03,$FF,$C0
  DEFB $07,$BF,$C0
  DEFB $1F,$0F,$E8
  DEFB $34,$00,$74
  DEFB $20,$00,$18
  DEFB $00,$00,$0C
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00

; JonesFrame1
;
; Jones the cat frame 1 (phase 1). 21 rows x 3 bytes, XOR-blitted.
JonesFrame1:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $60,$00,$00
  DEFB $3C,$00,$10
  DEFB $07,$F8,$38
  DEFB $01,$FE,$FC
  DEFB $00,$FF,$FC
  DEFB $01,$FF,$F0
  DEFB $01,$FF,$E0
  DEFB $01,$E7,$C0
  DEFB $01,$80,$C0
  DEFB $01,$80,$C0
  DEFB $00,$C0,$C0
  DEFB $00,$C0,$E0
  DEFB $00,$00,$60
  DEFB $00

; JonesFrame2
;
; Jones the cat frame 2 (phase 2). 21 rows x 3 bytes, XOR-blitted.
JonesFrame2:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$10
  DEFB $80,$00,$38
  DEFB $E1,$E0,$FC
  DEFB $3F,$FF,$FC
  DEFB $07,$FF,$F0
  DEFB $03,$FF,$E0
  DEFB $01,$FF,$E0
  DEFB $00,$F6,$C0
  DEFB $00,$F0,$C0
  DEFB $00,$60,$C0
  DEFB $00,$31,$80
  DEFB $00,$1D,$80
  DEFB $00

; JonesFrame3
;
; Jones the cat frame 3 (phase 3). 21 rows x 3 bytes, XOR-blitted.
JonesFrame3:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$04
  DEFB $00,$00,$0E
  DEFB $00,$00,$7F
  DEFB $00,$0F,$FF
  DEFB $20,$FF,$F0
  DEFB $1F,$FF,$F0
  DEFB $0E,$7F,$F8
  DEFB $00,$FF,$3C
  DEFB $00,$F8,$14
  DEFB $01,$E0,$18
  DEFB $01,$80,$18
  DEFB $01,$80,$30
  DEFB $01,$80,$00
  DEFB $00,$C0,$00
  DEFB $00

; JonesFrame4
;
; Jones the cat frame 4 (phase 4). 21 rows x 3 bytes, XOR-blitted.
JonesFrame4:
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $00,$00,$00
  DEFB $38,$00,$08
  DEFB $0E,$00,$1C
  DEFB $03,$FF,$FE
  DEFB $00,$FF,$FE
  DEFB $00,$FF,$E0
  DEFB $00,$FF,$E0
  DEFB $03,$F7,$F0
  DEFB $03,$E0,$7C
  DEFB $03,$00,$36
  DEFB $06,$00,$30
  DEFB $04,$00,$18
  DEFB $04,$00,$08
  DEFB $00,$00,$00
  DEFB $00
; Padding / unused.
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00

; IntroTileData
;
; 8×8 pixel tile bitmaps for the game's extra-data area (AlienFrame0-$F5FF).
; The first 8 tiles ($F1B0-$F22F, addresses 61936-61999) are blank padding.
; Tile graphics used by DrawIntroScreen (DrawIntroScreen) start at TileBitmaps
; (TileBitmaps): tile index N maps to address TileBitmaps + N×8. The tile map
; at IntroTileMap indexes into this set to build the 15×11 title-screen
; graphic. Subsequent tiles (beyond the intro set) are used by other game
; screens and the ship map.
;
; Intro tile sheet — tiles starting at TileBitmaps, 16 per row, 2× scale:
IntroTileData:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
TileBitmaps:
  DEFB $00,$00,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$01,$03,$07
  DEFB $01,$0F,$1F,$3D,$C6,$EE,$F6,$ED
  DEFB $FC,$C3,$80,$00,$00,$00,$00,$00
  DEFB $00,$80,$40,$30,$10,$0C,$06,$03
  DEFB $00,$00,$00,$00,$00,$01,$03,$03
  DEFB $0E,$1C,$3F,$77,$EC,$F6,$BD,$FB
  DEFB $49,$A1,$0A,$D2,$D0,$40,$40,$40
  DEFB $01,$00,$00,$00,$00,$00,$00,$00
  DEFB $80,$C0,$60,$30,$18,$0C,$00,$07
  DEFB $07,$0F,$1F,$1F,$3F,$7F,$7F,$FD
  DEFB $12,$35,$E1,$C5,$8E,$10,$52,$84
  DEFB $88,$08,$00,$04,$00,$00,$40,$80
  DEFB $01,$05,$02,$01,$00,$01,$00,$00
  DEFB $00,$80,$80,$60,$20,$70,$90,$08
  DEFB $01,$01,$03,$07,$07,$0F,$0F,$0F
  DEFB $F3,$FE,$60,$F8,$E1,$E2,$E4,$68
  DEFB $28,$48,$90,$20,$00,$48,$70,$A0
  DEFB $80,$00,$00,$80,$00,$00,$00,$00
  DEFB $28,$04,$02,$03,$11,$00,$04,$01
  DEFB $00,$00,$00,$00,$00,$80,$80,$40
  DEFB $1F,$1D,$3F,$3F,$7E,$7C,$F8,$F8
  DEFB $D8,$E1,$E2,$04,$11,$84,$29,$02
  DEFB $20,$00,$80,$80,$40,$40,$04,$00
  DEFB $02,$00,$00,$06,$00,$01,$01,$00
  DEFB $C0,$A0,$60,$70,$30,$78,$28,$18
  DEFB $00,$01,$01,$01,$03,$03,$03,$07
  DEFB $FC,$F9,$FC,$F8,$FF,$AE,$EC,$FF
  DEFB $10,$25,$05,$48,$11,$01,$10,$A8
  DEFB $80,$20,$00,$00,$00,$08,$00,$00
  DEFB $2C,$0C,$14,$0A,$12,$12,$09,$05
  DEFB $07,$0F,$0F,$0F,$0F,$0F,$0F,$1F
  DEFB $BE,$AE,$FF,$FD,$A7,$FD,$EF,$FF
  DEFB $44,$82,$06,$DB,$8C,$2C,$0A,$C4
  DEFB $80,$40,$00,$00,$40,$00,$40,$40
  DEFB $05,$05,$0B,$07,$07,$0F,$01,$01
  DEFB $00,$00,$80,$80,$80,$80,$80,$C0
  DEFB $1F,$1F,$1F,$1F,$1F,$1F,$1F,$1F
  DEFB $FF,$FF,$E7,$FF,$F5,$ED,$FF,$E5
  DEFB $A0,$FE,$CC,$FA,$F4,$6A,$6A,$D9
  DEFB $00,$00,$00,$80,$10,$08,$21,$40
  DEFB $00,$00,$00,$00,$00,$10,$68,$04
  DEFB $00,$00,$00,$00,$00,$00,$08,$01
  DEFB $01,$01,$0B,$01,$83,$45,$8B,$3F
  DEFB $C0,$C0,$C0,$C0,$C0,$C0,$C0,$C0
  DEFB $1F,$1F,$1F,$1F,$1F,$1F,$0F,$0F
  DEFB $FF,$FB,$FE,$E9,$F2,$F4,$FF,$F2
  DEFB $A7,$F5,$DB,$6F,$BD,$36,$FB,$AC
  DEFB $CD,$CC,$A6,$59,$FC,$CA,$D0,$F4
  DEFB $70,$DC,$7F,$67,$D0,$61,$67,$ED
  DEFB $00,$80,$00,$80,$61,$7B,$B1,$1B
  DEFB $00,$00,$00,$00,$00,$00,$40,$C1
  DEFB $00,$00,$00,$02,$04,$01,$0A,$07
  DEFB $02,$03,$1F,$3E,$FF,$BD,$FF,$FF
  DEFB $7F,$BF,$7F,$FF,$FF,$7D,$BB,$FF
  DEFB $C0,$C0,$C0,$C0,$C0,$C0,$80,$80
  DEFB $0F,$0F,$0F,$07,$07,$03,$03,$01
  DEFB $FB,$FE,$FE,$FF,$FF,$FF,$FF,$FF
  DEFB $4E,$46,$32,$11,$89,$5D,$A9,$D7
  DEFB $FE,$DD,$AE,$7F,$EF,$D7,$FA,$9D
  DEFB $95,$FF,$E4,$E5,$B4,$5F,$FD,$76
  DEFB $2E,$6F,$9B,$FE,$79,$FD,$1F,$FF
  DEFB $41,$E1,$50,$CF,$EF,$F7,$BF,$DD
  DEFB $07,$1F,$31,$E7,$E7,$6B,$7F,$BE
  DEFB $FF,$7F,$BF,$FF,$FF,$FF,$FF,$FE
  DEFB $FA,$FF,$EF,$D3,$B7,$DE,$EE,$DC
  DEFB $80,$80,$00,$00,$00,$00,$00,$00
  DEFB $01,$00,$00,$00,$00,$00,$00,$00
  DEFB $FF,$FF,$FF,$7F,$7F,$3F,$3F,$1F
  DEFB $E1,$F6,$FF,$FF,$FE,$FE,$FE,$FF
  DEFB $9C,$8B,$9F,$AE,$E9,$FF,$AA,$B5
  DEFB $BF,$55,$BB,$DF,$6B,$61,$4E,$77
  DEFB $7F,$BF,$FF,$FF,$FF,$7F,$BF,$7F
  DEFB $FF,$FF,$FE,$FF,$FC,$FE,$FF,$FF
  DEFB $00,$00,$00,$00,$03,$1C,$BD,$FD
  DEFB $7D,$FB,$7F,$3F,$7F,$FF,$FF,$FF
  DEFB $FC,$F8,$F8,$F0,$F0,$E0,$E0,$C0
  DEFB $1F,$0F,$0F,$07,$01,$01,$00,$00
  DEFB $FF,$FF,$FF,$FF,$FF,$FF,$FF,$3F
  DEFB $FF,$F7,$F9,$F5,$FA,$FD,$FF,$FF
  DEFB $7F,$FF,$7F,$BF,$FF,$FF,$FF,$FF
  DEFB $7F,$FF,$7F,$BF,$FF,$FF,$FF,$FF
  DEFB $FF,$FF,$FF,$FE,$FF,$FF,$FF,$FB
  DEFB $5E,$7E,$F7,$FB,$7F,$BF,$FF,$96
  DEFB $FF,$FF,$7F,$7F,$FE,$FC,$7C,$F8
  DEFB $80,$80,$00,$00,$00,$00,$00,$00
  DEFB $00,$00,$00,$00,$00,$20,$31,$A9
  DEFB $1F,$0F,$07,$03,$01,$00,$80,$09
  DEFB $FE,$FF,$FE,$FF,$FE,$FF,$7F,$3F
  DEFB $37,$5F,$EF,$5F,$BF,$FF,$BF,$BF
  DEFB $FF,$FF,$7F,$7F,$FF,$FA,$EF,$E7
  DEFB $B7,$3F,$FF,$FF,$FE,$E8,$E4,$FF
  DEFB $BE,$B9,$7F,$F4,$6D,$F6,$22,$CC
  DEFB $F0,$E0,$40,$80,$00,$00,$00,$00
  DEFB $04,$0A,$03,$05,$00,$00,$00,$00
  DEFB $B1,$E6,$7C,$F0,$32,$3B,$03,$00
  DEFB $26,$81,$CB,$69,$D4,$6A,$B6,$9A
  DEFB $FF,$FF,$FD,$FF,$7F,$7F,$40,$A7
  DEFB $7F,$FF,$CE,$59,$E0,$CA,$14,$02
  DEFB $FF,$7A,$A5,$AA,$70,$20,$00,$04
  DEFB $D0,$80,$00,$00,$00,$00,$C0,$00
  DEFB $01,$00,$00,$00,$00,$00,$00,$00
  DEFB $22,$E2,$02,$01,$00,$00,$00,$00
  DEFB $42,$00,$58,$81,$00,$00,$00,$00
  DEFB $44,$20,$00,$00,$00,$00,$00,$00
  DEFB $A7,$8A,$F5,$00,$00,$01,$4B,$55
  DEFB $53,$45,$D2,$64,$A7,$AC,$F5,$00
  DEFB $00,$01,$4B,$55,$D0,$E6,$A7,$00
  DEFB $00,$00,$00,$01,$4B,$44,$4F,$57
  DEFB $CE,$F0,$A7,$00,$00,$00,$00,$01
  DEFB $4B,$46,$49,$52,$C5,$FA,$A7,$00
  DEFB $00,$00,$00,$01,$4B,$51,$55,$49
  DEFB $D4,$04,$A8,$00,$00,$00,$00,$01
  DEFB $53,$43,$41,$4E,$B1,$10,$A8,$11
  DEFB $F6,$00,$00,$01,$43,$52,$57,$52
  DEFB $41,$D4,$1B,$AA,$00,$00,$00,$00
  DEFB $01,$44,$41,$4D,$52,$41,$D4,$4A
  DEFB $AA,$04,$F6,$38,$F6,$01,$45,$4E
  DEFB $44,$45,$58,$D0,$2A,$A8,$00,$00
  DEFB $00,$00,$01,$54,$49,$4D,$52,$41
  DEFB $D4,$62,$AA,$00,$00,$00,$00,$01
  DEFB $41,$4C,$44,$45,$C4,$E3,$A8,$00
  DEFB $00,$00,$00,$01,$45,$4E,$44,$45
  DEFB $4E,$C4,$30,$A8,$00,$00,$00,$00
  DEFB $01,$43,$52,$57,$44,$45,$C4,$F2

; Zero-padding between intro tile data and boot-time residual
;
; 2304 bytes of zeros separating the last tile row of IntroTileData from the
; small residual data block at BootResidual. Reserved space, never touched by
; the running game.
  DEFS $900

; Boot-time residual data
;
; 232 bytes of leftover content loaded into the top of memory by the tape
; loader. Includes a 64-byte data table (probably loader / BASIC interpreter
; state) followed by a partial 8x8-pixel font containing the upper-case letters
; A through U (21 glyphs from offset +84 onwards, each 8 bytes). The running
; game sets the stack pointer just below this region at GameEntry (GameEntry)
; so this content overlaps the active stack and is overwritten as soon as the
; game begins running. Not referenced by any game routine; the only references
; to in-range 16-bit values treat them as signed screen strides (e.g. -32 for
; one cell up within a third of the display), not as memory pointers.
BootResidual:
  DEFB $F3,$0D,$CE,$0B,$E3,$50,$CE,$0B
  DEFB $E4,$50,$1D,$17,$DC,$0A,$CE,$0B
  DEFB $E7,$50,$1A,$17,$DC,$0A,$D7,$18
  DEFB $38,$00,$38,$00,$0D,$19,$DB,$02
  DEFB $4D,$00,$06,$55,$FA,$00,$DB,$02
  DEFB $4D,$00,$00,$00,$21,$00,$B6,$5C
  DEFB $08,$01,$F3,$0D,$21,$17,$C6,$1E
  DEFB $FF,$5F,$76,$1B,$03,$13,$00,$3E
  DEFB $00,$3C,$42,$42,$7E,$42,$42,$00
  DEFB $00,$7C,$42,$7C,$42,$42,$7C,$00
  DEFB $00,$3C,$42,$40,$40,$42,$3C,$00
  DEFB $00,$78,$44,$42,$42,$44,$78,$00
  DEFB $00,$7E,$40,$7C,$40,$40,$7E,$00
  DEFB $00,$7E,$40,$7C,$40,$40,$40,$00
  DEFB $00,$3C,$42,$40,$4E,$42,$3C,$00
  DEFB $00,$42,$42,$7E,$42,$42,$42,$00
  DEFB $00,$3E,$08,$08,$08,$08,$3E,$00
  DEFB $00,$02,$02,$02,$42,$42,$3C,$00
  DEFB $00,$44,$48,$70,$48,$44,$42,$00
  DEFB $00,$40,$40,$40,$40,$40,$7E,$00
  DEFB $00,$42,$66,$5A,$42,$42,$42,$00
  DEFB $00,$42,$62,$52,$4A,$46,$42,$00
  DEFB $00,$3C,$42,$42,$42,$42,$3C,$00
  DEFB $00,$7C,$42,$42,$7C,$40,$40,$00
  DEFB $00,$3C,$42,$42,$52,$4A,$3C,$00
  DEFB $00,$7C,$42,$42,$7C,$44,$42,$00
  DEFB $00,$3C,$40,$3C,$02,$42,$3C,$00
  DEFB $00,$FE,$10,$10,$10,$10,$10,$00
  DEFB $00,$42,$42,$42,$42,$42,$3C,$00

